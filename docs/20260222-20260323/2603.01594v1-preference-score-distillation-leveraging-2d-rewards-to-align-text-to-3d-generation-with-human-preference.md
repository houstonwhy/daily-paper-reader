---
title: "Preference Score Distillation: Leveraging 2D Rewards to Align Text-to-3D Generation with Human Preference"
title_zh: 偏好分数蒸馏：利用 2D 奖励使文本到 3D 生成符合人类偏好
authors: "Jiaqi Leng, Shuyuan Tu, Haidong Cao, Sicheng Xie, Daoguo Dong, Zuxuan Wu, Yu-Gang Jiang"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01594v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用2D奖励使文本到3D生成符合人类偏好
tldr: 针对文本生成3D模型中人类偏好对齐的难题，本文提出偏好分数蒸馏（PSD）框架。该方法无需3D训练数据，通过将预训练的2D奖励模型转化为类似无分类器指导（CFG）的机制，解决了奖励梯度与去噪过程不兼容的问题。此外，通过自适应协同优化负向文本嵌入，进一步提升了生成结果的美学质量和对齐度，实现了高效的人类偏好对齐。
selection_source: fresh_fetch
motivation: 现有的文本生成3D模型在人类偏好对齐方面存在挑战，且受限于3D领域偏好数据的匮乏和微调的高昂成本。
method: 提出PSD框架，将2D奖励模型重新构想为CFG风格的引导机制，并引入负向文本嵌入的在线自适应优化策略。
result: 实验结果显示，PSD在美学评分上显著优于现有方法，并能灵活集成到多种主流的文本生成3D管线中。
conclusion: 本研究首次在分数蒸馏框架下将人类偏好对齐与CFG理论相结合，为无需3D数据的偏好对齐生成提供了有效方案。
---

## 摘要
人类偏好对齐是文本到 3D 生成中扩散模型面临的一个关键但尚未得到充分探索的挑战。现有的解决方案通常需要针对特定任务进行微调，这在数据稀缺的 3D 领域构成了重大障碍。为此，我们提出了偏好分数蒸馏（Preference Score Distillation, PSD），这是一个基于优化的框架，它利用预训练的 2D 奖励模型，在无需 3D 训练数据的情况下实现符合人类偏好的文本到 3D 合成。我们的核心见解源于像素级梯度的不兼容性：由于奖励模型训练期间缺乏噪声样本，直接应用 2D 奖励梯度会干扰去噪过程。注意到在条件扩散模型的朴素分类器引导中也存在类似问题，我们通过隐式奖励模型，从根本上将偏好对齐重新构想为一种无分类器引导（CFG）风格的机制。此外，考虑到冻结的预训练扩散模型会限制性能，我们引入了一种自适应策略来共同优化偏好分数和负面文本嵌入。通过在优化过程中结合 CFG，负面文本嵌入的在线细化动态地增强了对齐效果。据我们所知，我们是首个在分数蒸馏框架下将人类偏好对齐与 CFG 理论联系起来的研究。实验证明了 PSD 在美学指标上的优越性、与多种流水线的无缝集成以及强大的可扩展性。

## Abstract
Human preference alignment presents a critical yet underexplored challenge for diffusion models in text-to-3D generation. Existing solutions typically require task-specific fine-tuning, posing significant hurdles in data-scarce 3D domains. To address this, we propose Preference Score Distillation (PSD), an optimization-based framework that leverages pretrained 2D reward models for human-aligned text-to-3D synthesis without 3D training data. Our key insight stems from the incompatibility of pixel-level gradients: due to the absence of noisy samples during reward model training, direct application of 2D reward gradients disturbs the denoising process. Noticing that similar issue occurs in the naive classifier guidance in conditioned diffusion models, we fundamentally rethink preference alignment as a classifier-free guidance (CFG)-style mechanism through our implicit reward model. Furthermore, recognizing that frozen pretrained diffusion models constrain performance, we introduce an adaptive strategy to co-optimize preference scores and negative text embeddings. By incorporating CFG during optimization, online refinement of negative text embeddings dynamically enhances alignment. To our knowledge, we are the first to bridge human preference alignment with CFG theory under score distillation framework. Experiments demonstrate the superiority of PSD in aesthetic metrics, seamless integration with diverse pipelines, and strong extensibility.

---

## 论文详细总结（自动生成）

这是一份关于论文《Preference Score Distillation: Leveraging 2D Rewards to Align Text-to-3D Generation with Human Preference》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的文本到 3D（Text-to-3D）生成技术（如 SDS 及其变体）虽然能生成 3D 资产，但往往在**人类偏好对齐**（如美学质量、细节丰富度、文本一致性）方面表现不佳。
*   **研究背景与挑战**：
    *   **数据稀缺**：3D 领域的偏好标注数据极度匮乏，难以像 2D 领域那样训练强大的 3D 奖励模型。
    *   **梯度不兼容**：现有的 2D 奖励模型是在干净图像上训练的，直接将其梯度应用于扩散模型的高噪声阶段会导致严重的视觉伪影和梯度失调。
    *   **模型冻结限制**：在蒸馏过程中，预训练的扩散模型通常是冻结的，限制了其向人类偏好分布迁移的能力。

### 2. 方法论：偏好分数蒸馏 (PSD)
*   **核心思想**：将人类偏好对齐重新构想为一种**无分类器指导（CFG）风格的机制**。通过在线构建“胜出-失败”（Win-Lose）样本对，利用隐式奖励模型产生引导信号，使生成过程向高奖励区域偏移。
*   **关键技术细节**：
    1.  **偏好分数引导（Preference Score Guidance）**：作者从理论上推导了偏好梯度与扩散得分之间的联系，将偏好对齐转化为一个 CFG 类型的项。在每个去噪步骤中，在线生成两个带噪样本，由 2D 奖励模型判断优劣，从而产生指向“胜出”样本的梯度。
    2.  **自适应负向嵌入优化（Adaptive Negative Embedding Update）**：为了突破冻结模型的限制，PSD 不直接微调扩散模型参数，而是将**负向文本嵌入（Negative Embedding）**视为可学习参数。在优化 3D 表示的同时，通过奖励模型的回传信号动态更新负向嵌入，从而“推开”低质量特征。
    3.  **统一优化框架**：最终的梯度由三部分组成：无条件先验、文本条件引导（CFG）以及新增的偏好分数引导。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 **Eval3d** 基准测试中的 200 个提示词进行单阶段蒸馏。
    *   针对高分辨率生成，在 40 个复杂的提示词子集上测试了 **2阶段 NeRF** 和 **3阶段 DMTet** 管线。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括 MVDream、DreamDPO、DreamReward、RichDreamer（利用 3D 先验）、Trellis（大模型 3D 生成）以及 CFD。
    *   **评估指标**：ImageReward (I.R.)、PickScore、Aesthetic Score（美学评分）、MPS（多维偏好得分）以及利用 Qwen2.5-VL 进行的文本对齐度（T.A.）评估。
*   **用户研究**：邀请 24 名参与者对生成结果的外观、结构、对齐度和整体偏好进行主观评价。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **效率信息**：文中提到了推理/优化速度。例如，在 MVDream 实验中，PSD 的速度约为 **1.19 it/s**，相比于基础 MVDream（2.92 it/s）和 DreamDPO（1.45 it/s），由于引入了奖励模型的前向计算和负向嵌入优化，计算开销有所增加，但在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 200 个提示词的大规模定量评估。
    *   涵盖了从单阶段到多阶段（NeRF, Mesh）的多种主流 3D 生成管线。
    *   **消融实验**：针对负向嵌入学习率、不同奖励模型（HPSv2.1 vs ImageReward）、以及在 2D 图像生成上的泛化能力进行了详细测试。
*   **充分性评价**：实验设计较为充分且客观。作者不仅对比了同类基于优化的方法，还对比了基于 3D 大规模数据训练的模型（如 Trellis），证明了即使不使用 3D 数据，通过偏好对齐也能达到极高的视觉质量。

### 6. 主要结论与发现
*   **偏好对齐至关重要**：即使是拥有强大 3D 先验的模型，如果缺乏偏好对齐，也会在文本对齐和美感上逊色于 PSD。
*   **CFG 理论的成功应用**：证明了偏好对齐可以被形式化为一种采样引导机制，无需复杂的 RLHF 微调。
*   **负向嵌入的有效性**：动态优化负向嵌入是一种高效且轻量化的替代模型微调的方案，能显著提升生成资产的纯净度和美感。

### 7. 优点（亮点）
*   **无需 3D 数据**：完全依赖现有的 2D 奖励模型，解决了 3D 领域数据匮乏的痛点。
*   **理论完备性**：首次在分数蒸馏框架下建立了偏好学习与 CFG 理论的数学联系。
*   **即插即用**：PSD 作为一个优化框架，可以轻松集成到现有的各种 Text-to-3D 流程中（如 Gaussian Splatting, NeRF, Mesh）。
*   **视觉质量高**：生成的 3D 模型在纹理细节和符合人类审美方面有显著提升。

### 8. 不足与局限
*   **计算开销**：由于每步需要额外的奖励模型推理和嵌入优化，整体优化时间比标准 SDS 长。
*   **奖励作弊（Reward Hacking）风险**：如果负向嵌入的学习率设置过高，模型可能会过度优化奖励分数而导致视觉畸变（虽然文中提出了自适应策略来缓解）。
*   **依赖 2D 奖励模型**：PSD 的上限受限于所使用的 2D 奖励模型的能力，如果奖励模型本身存在偏差，生成结果也会受到影响。
*   **Janus 问题（多头问题）**：虽然有所改善，但在使用 Stable Diffusion 2.1 等模型时，仍可能出现 3D 生成中常见的视角不一致问题。

（完）
