---
title: "BetterScene: 3D Scene Synthesis with Representation-Aligned Generative Model"
title_zh: BetterScene：基于表示对齐生成模型的 3D 场景合成
authors: "Yuci Han, Charles Toth, John E. Anderson, William J. Shuart, Alper Yilmaz"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22596v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 基于表示对齐生成模型的3D场景合成
tldr: BetterScene 旨在解决从极稀疏、无约束照片中进行高质量 3D 场景合成的挑战。该方法以 Stable Video Diffusion (SVD) 为基础，通过改进 VAE 模块，引入时间等变性正则化和视觉基础模型对齐表示，解决了现有扩散模型在生成新视角时细节不一致和伪影的问题。结合前馈 3D 高斯泼溅 (3DGS) 模型，BetterScene 在 DL3DV-10K 数据集上实现了卓越的视图一致性和图像质量。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的新视角合成方法由于仅微调 UNet 而忽略了 VAE 潜在空间的优化，导致生成的细节不一致且存在伪影。
method: 在 SVD 的 VAE 模块中引入时间等变性正则化和视觉基础模型对齐表示，并结合前馈 3DGS 模型生成连续且无伪影的视图。
result: 在挑战性的 DL3DV-10K 数据集上，BetterScene 的表现优于现有的最先进方法，显著提升了合成视图的质量和一致性。
conclusion: 通过优化扩散模型的潜在空间表示并结合 3DGS，BetterScene 有效提升了稀疏输入下的 3D 场景合成效果。
---

## 摘要
我们提出了 BetterScene，这是一种利用极稀疏、无约束的照片来增强各种真实场景新视角合成（NVS）质量的方法。BetterScene 利用在数十亿帧上预训练的生产级 Stable Video Diffusion (SVD) 模型作为强大的骨干网络，旨在推理阶段减少伪影并恢复视角一致的细节。传统方法已经开发了类似的基于扩散的解决方案来应对新视角合成的这些挑战。尽管取得了显著改进，但这些方法通常依赖于现成的预训练扩散先验，并且仅微调 UNet 模块而保持其他组件冻结，这即使在引入深度或语义条件等几何感知正则化时，仍会导致细节不一致和伪影。为解决这一问题，我们研究了扩散模型的潜空间，并引入了两个组件：(1) 时间等变性正则化和 (2) 视觉基础模型对齐表示，两者均应用于 SVD 流水线中的变分自编码器 (VAE) 模块。BetterScene 集成了一个前馈 3D 高斯泼溅 (3DGS) 模型，将渲染的特征作为 SVD 增强器的输入，从而生成连续、无伪影且一致的新视角。我们在具有挑战性的 DL3DV-10K 数据集上进行了评估，并证明了其性能优于现有最先进的方法。

## Abstract
We present BetterScene, an approach to enhance novel view synthesis (NVS) quality for diverse real-world scenes using extremely sparse, unconstrained photos. BetterScene leverages the production-ready Stable Video Diffusion (SVD) model pretrained on billions of frames as a strong backbone, aiming to mitigate artifacts and recover view-consistent details at inference time. Conventional methods have developed similar diffusion-based solutions to address these challenges of novel view synthesis. Despite significant improvements, these methods typically rely on off-the-shelf pretrained diffusion priors and fine-tune only the UNet module while keeping other components frozen, which still leads to inconsistent details and artifacts even when incorporating geometry-aware regularizations like depth or semantic conditions. To address this, we investigate the latent space of the diffusion model and introduce two components: (1) temporal equivariance regularization and (2) vision foundation model-aligned representation, both applied to the variational autoencoder (VAE) module within the SVD pipeline. BetterScene integrates a feed-forward 3D Gaussian Splatting (3DGS) model to render features as inputs for the SVD enhancer and generate continuous, artifact-free, consistent novel views. We evaluate on the challenging DL3DV-10K dataset and demonstrate superior performance compared to state-of-the-art methods.

---

## 论文详细总结（自动生成）

以下是对论文《BetterScene: 3D Scene Synthesis with Representation-Aligned Generative Model》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在极稀疏（Sparse-view）且无约束的输入条件下，如何实现高质量、视角一致且无伪影的 3D 场景新视角合成（NVS）。
*   **研究背景**：
    *   传统的 NeRF 和 3D 高斯泼溅（3DGS）在稀疏视角下会出现严重的几何畸变和区域缺失。
    *   现有的基于扩散模型（Diffusion Models）的增强方法大多只微调 U-Net 去噪模块，而忽略了变分自编码器（VAE）潜空间（Latent Space）对细节恢复和视角一致性的限制。
    *   标准 VAE 的低维潜空间（如 4 通道）难以重建高频细节，且缺乏空间变换的等变性，导致生成的视频序列在视角切换时出现闪烁或内容漂移。

### 2. 核心方法论
BetterScene 提出了一个两阶段框架，结合了前馈 3DGS 和表示对齐的视频扩散模型：

*   **核心思想**：通过优化 VAE 的潜空间表示（高维化、对齐基础模型、引入等变性），提升扩散模型在 NVS 任务中的重建精度和生成一致性。
*   **关键技术细节**：
    1.  **BetterScene-VAE 训练（第一阶段）**：
        *   **高维潜空间**：将潜空间通道从标准的 4 扩展到 64，并采用 16× 空间下采样，以捕捉更丰富的视觉特征。
        *   **表示对齐损失（Alignment Loss）**：利用视觉基础模型 DINOv2 引导 VAE 学习。通过余弦相似度和距离相似度损失，使 VAE 的潜特征与 DINOv2 的鲁棒特征空间对齐，解决高维特征下生成性能下降的难题。
        *   **等变性正则化（Equivariance Regularization）**：强制要求图像在空间变换（如平移、旋转）后的潜表示，与先编码再变换的结果一致。这保证了视频生成过程中的时间/视角稳定性。
    2.  **视频扩散增强器微调（第二阶段）**：
        *   **前馈 3DGS 引导**：使用 MVSplat 模型根据稀疏输入快速生成粗糙的 3D 高斯参数和初步渲染图。
        *   **特征条件注入**：将粗糙的渲染特征直接与 SVD（Stable Video Diffusion）的噪声潜变量拼接，作为去噪条件。
        *   **端到端微调**：冻结预训练的 BetterScene-VAE，仅微调 SVD 的 U-Net 去噪器，并结合 L2 和 LPIPS 重建损失。

### 3. 实验设计
*   **数据集**：主要使用 **DL3DV-10K**，这是一个包含 10,510 个真实世界场景、超过 5000 万帧的大规模场景数据集。
*   **Benchmark 设置**：
    *   输入：5 张稀疏视角图像。
    *   评估：均匀采样 56 个新视角进行测试。
*   **对比方法**：
    *   **MVSplat**（前馈 3DGS 基准）。
    *   **latentSplat**（基于潜空间的 3D 重建）。
    *   **MVSplat360**（目前最先进的基于 SVD 的 NVS 增强方法）。
*   **评估指标**：PSNR、SSIM、LPIPS 和 FID。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 台 NVIDIA H100 GPU**。
*   **训练细节**：文中提到 SVD 模型的训练计算成本昂贵，但未明确给出具体的总训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模**：在包含 140 个测试场景的 DL3DV 子集上进行了详尽测试。
*   **消融实验**：针对 VAE 的潜空间通道数（16、32、64）做了对比实验，验证了高维通道对重建质量（rFID 从 13.8 降至 4.9）的显著提升。
*   **充分性评价**：实验设计较为客观，直接采用了 SOTA 方法（MVSplat360）的相同设置进行公平对比，定量指标和定性视觉对比均显示了明显进步。

### 6. 主要结论与发现
*   **VAE 是关键**：仅仅微调扩散模型的去噪器是不够的，优化 VAE 的潜空间表示是解决 NVS 细节丢失和不一致性的核心。
*   **高维优势**：增加潜空间维度能显著提升重建精度，而通过基础模型（DINOv2）对齐可以克服高维特征带来的生成训练困难。
*   **性能领先**：BetterScene 在 SSIM、LPIPS 和 FID 等感知质量指标上全面超越了现有方法，生成的视角更加清晰且视角切换平滑。

### 7. 优点（亮点）
*   **创新视角**：首次深入探讨了扩散模型 VAE 模块在 3D 场景合成中的作用，而非仅仅关注 U-Net。
*   **等变性设计**：引入等变性正则化有效解决了视频扩散模型在 NVS 中常见的“漂移”问题。
*   **高质量重建**：通过 64 通道 VAE，成功恢复了如墙上文字等极细微的纹理细节。

### 8. 不足与局限
*   **计算开销**：基于 SVD 的微调和高维 VAE 的训练对算力要求极高，难以在消费级显卡上快速部署。
*   **推理速度**：虽然使用了前馈 3DGS 提速，但后续的扩散模型去噪过程仍然比纯 3DGS 渲染慢得多。
*   **依赖性**：模型性能在一定程度上受限于底层前馈模型（MVSplat）生成的初始几何质量。

（完）
