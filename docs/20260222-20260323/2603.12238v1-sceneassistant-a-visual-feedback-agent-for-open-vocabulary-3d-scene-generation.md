---
title: "SceneAssistant: A Visual Feedback Agent for Open-Vocabulary 3D Scene Generation"
title_zh: SceneAssistant：一种用于开放词汇 3D 场景生成的视觉反馈智能体
authors: "Jun Luo, Jiaxiang Tang, Ruijie Lu, Gang Zeng"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12238v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 开放词汇文本到3D场景生成框架
tldr: SceneAssistant是一个基于视觉反馈的智能体，旨在解决现有3D场景生成方法在开放词汇和空间布局上的局限性。该框架结合了3D物体生成模型与视觉语言模型（VLM）的推理能力，通过一系列原子操作（如缩放、旋转）对场景进行迭代优化。实验表明，该方法在生成多样化、高质量3D场景及场景编辑方面优于现有技术，显著提升了生成场景的连贯性与文本对齐度。
selection_source: fresh_fetch
motivation: 现有的3D场景生成方法受限于特定领域或预定义的空间关系，难以实现无约束的开放词汇场景合成。
method: 利用视觉语言模型作为智能体，通过视觉反馈和原子操作迭代地调整3D物体的位置、比例和旋转，以优化场景布局。
result: 定性和定量评估均显示，该方法在生成高质量、开放词汇的3D场景以及执行自然语言编辑指令方面表现优异。
conclusion: SceneAssistant通过引入视觉反馈循环和VLM的推理能力，为灵活且高质量的开放词汇3D场景生成提供了有效的解决方案。
---

## 摘要
从自然语言进行文本到 3D 场景生成在数字内容创作中具有极高的应用价值。然而，现有方法大多受限于特定领域或依赖于预定义的空间关系，限制了其在无约束、开放词汇 3D 场景合成方面的能力。在本文中，我们介绍了 SceneAssistant，这是一种专为开放词汇 3D 场景生成设计的视觉反馈驱动智能体。我们的框架利用了现代 3D 物体生成模型，并结合了视觉语言模型（VLM）的空间推理和规划能力。为了实现开放词汇的场景构图，我们为 VLM 提供了一套全面的原子操作（例如：缩放、旋转、聚焦）。在每个交互步骤中，VLM 接收渲染的视觉反馈并采取相应行动，迭代地优化场景，以实现更连贯的空间布局以及与输入文本更好的对齐。实验结果表明，我们的方法能够生成多样化、开放词汇且高质量的 3D 场景。定性分析和定量人工评估均证明了我们的方法优于现有方法。此外，我们的方法允许用户通过自然语言指令指导智能体编辑现有场景。我们的代码可在 https://github.com/ROUJINN/SceneAssistant 获取。

## Abstract
Text-to-3D scene generation from natural language is highly desirable for digital content creation. However, existing methods are largely domain-restricted or reliant on predefined spatial relationships, limiting their capacity for unconstrained, open-vocabulary 3D scene synthesis. In this paper, we introduce SceneAssistant, a visual-feedback-driven agent designed for open-vocabulary 3D scene generation. Our framework leverages modern 3D object generation model along with the spatial reasoning and planning capabilities of Vision-Language Models (VLMs). To enable open-vocabulary scene composition, we provide the VLMs with a comprehensive set of atomic operations (e.g., Scale, Rotate, FocusOn). At each interaction step, the VLM receives rendered visual feedback and takes actions accordingly, iteratively refining the scene to achieve more coherent spatial arrangements and better alignment with the input text. Experimental results demonstrate that our method can generate diverse, open-vocabulary, and high-quality 3D scenes. Both qualitative analysis and quantitative human evaluations demonstrate the superiority of our approach over existing methods. Furthermore, our method allows users to instruct the agent to edit existing scenes based on natural language commands. Our code is available at https://github.com/ROUJINN/SceneAssistant

---

## 论文详细总结（自动生成）

以下是对论文《SceneAssistant: A Visual Feedback Agent for Open-Vocabulary 3D Scene Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的文本到 3D 场景生成方法主要受限于两个方面：一是**领域受限**（如仅限于室内场景），二是**依赖预定义的空间关系**（如“在……之上”、“在……前面”），这使得它们难以处理复杂的、非传统的或开放词汇的场景描述。
*   **研究动机**：为了实现真正的“开放词汇”生成，需要一种能够理解任意文本指令并具备自主规划能力的系统。作者认为，现代视觉语言模型（VLM）已具备潜在的空间推理能力，通过引入“视觉反馈”闭环，可以模仿人类在 Blender 等软件中的创作过程，从而解决生成过程中的不确定性和布局复杂性。

### 2. 方法论
*   **核心思想**：提出 **SceneAssistant**，一个基于 **ReAct** 范式的智能体框架。它将 VLM 作为核心控制器，通过观察渲染图像（视觉反馈）并调用一系列原子操作 API 来迭代优化 3D 场景。
*   **关键技术细节**：
    *   **原子操作 API**：设计了一套功能完备的 API，包括物体管理（创建、克隆、删除）、6 自由度空间操作（平移、放置、旋转、缩放）以及相机控制（视角切换、聚焦、移动）。
    *   **视觉反馈机制**：在每一步迭代中，系统渲染当前场景并提供给 VLM。为了增强 VLM 的感知，图像中加入了**视觉提示**（物体名称标签）和**坐标轴 HUD**（全局参考系）。
    *   **闭环优化**：VLM 接收当前图像、物体元数据、历史动作和系统消息（如碰撞检测警告），先进行推理（Reasoning），再输出下一组动作（Action）。
    *   **3D 资产生成**：集成先进的文本到 3D 模型（如 Hunyuan3D 2.0）来生成单个物体，智能体负责将这些物体组合成连贯的场景。
    *   **人机协作编辑**：支持用户在生成过程中或生成后通过自然语言插入指令，利用 VLM 的指令遵循能力进行场景微调。

### 3. 实验设计
*   **场景与数据集**：涵盖了 8 个室内场景（如卧室、厨房）和 22 个开放词汇场景（如乐高教室、日本街角、皮卡丘在草地上等）。
*   **对比方法（Benchmark）**：
    *   **室内场景对比**：与 **Holodeck** 和 **SceneWeaver** 两个 SOTA 方法进行对比。
    *   **开放词汇对比**：设置了两个变体作为 Baseline：`NoActionAPI`（直接输出 JSON 布局而非调用 API）和 `NoVisFeedback`（单次生成，无视觉反馈）。
*   **评估指标**：采用人工评估（10 名参与者），针对**布局正确性**（1-10 分）、**物体质量**（1-10 分）和**人类偏好率**进行打分。

### 4. 资源与算力
*   **模型使用**：核心 VLM 使用了 **Gemini-3.0-Flash**；3D 资产生成使用了 **Hunyuan3D 2.0**；渲染引擎为 **Blender (EEVEE)**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架主要基于现成的预训练大模型进行推理和 API 调用，不涉及大规模的基座模型训练，其算力消耗主要集中在 3D 资产的生成推理和 Blender 的实时渲染上。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 30 组不同复杂程度的场景生成实验，并包含详细的消融实验（针对视觉提示和碰撞检测）。
*   **充分性与客观性**：
    *   实验涵盖了从简单排列到复杂语义约束的多种情况，证明了其泛化能力。
    *   通过人工评估而非单一的自动指标（如碰撞率），更客观地反映了场景的语义对齐度和美学质量。
    *   消融实验清晰地展示了视觉反馈和碰撞检测对最终结果的贡献，实验设计较为严谨。

### 6. 主要结论与发现
*   **视觉反馈至关重要**：闭环反馈机制显著提升了物体比例的协调性和旋转的准确性，弥补了 3D 生成模型初始输出的不确定性。
*   **API 抽象的优势**：相比于让模型直接生成复杂的 JSON 或代码，简洁的原子操作 API 能让 VLM 更专注于高层空间规划。
*   **超越领域限制**：SceneAssistant 在处理长尾物体（如“哆啦 A 梦”、“乐高老师”）和复杂空间约束（如“15 个可乐罐堆成金字塔”）方面表现出极强的灵活性，远超基于检索或固定规则的传统方法。

### 7. 优点（亮点）
*   **纯视觉驱动**：不依赖复杂的外部布局优化器或 3D 空间数据结构，完全依靠 VLM 的视觉理解能力。
*   **高度可扩展**：Action API 具有模块化特征，可以轻松集成新的功能（如地面纹理生成）。
*   **交互性强**：支持自然语言编辑，降低了 3D 场景创作的门槛，实现了“所说即所得”。

### 8. 不足与局限
*   **感知精度瓶颈**：VLM 有时仍难以分辨细微的空间关系（如椅子是否完全正对桌子），可能需要多次迭代或人工干预。
*   **资产质量依赖**：最终场景的质量高度受限于底层 3D 资产生成模型；如果生成的单个物体模型有缺陷，智能体虽能尝试重新生成，但无法从根本上修复几何错误。
*   **效率问题**：由于采用迭代式生成，完成一个复杂场景可能需要多步交互（最大步数设为 20），相比单次生成方法，时间成本更高。
*   **批处理倾向**：智能体有时倾向于一次性初始化所有物体，而非逐步构建，这在极复杂场景下会增加纠错难度。

（完）
