---
title: "Points-to-3D: Structure-Aware 3D Generation with Point Cloud Priors"
title_zh: Points-to-3D：基于点云先验的结构感知 3D 生成
authors: "Jiatong Xia, Zicheng Duan, Anton van den Hengel, Lingqiao Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18782v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 几何可控的3D资产与场景生成
tldr: Points-to-3D 是一种基于扩散模型的 3D 生成框架，旨在利用点云先验实现几何可控的 3D 资产和场景生成。该方法基于 TRELLIS 模型，通过结构修复网络和分阶段采样策略，将稀疏点云转化为完整的 3D 几何结构。它不仅能处理精确的传感器数据，还能处理单图预测的点云，在渲染质量和几何保真度上均优于现有方法，显著提升了 3D 生成的结构准确性。
selection_source: fresh_fetch
motivation: 现有的 3D 生成模型多依赖文本或图像，未能充分利用 LiDAR 等传感器提供的显式几何约束。
method: 基于 TRELLIS 框架，引入结构修复网络和分阶段采样策略，将点云先验融入潜在 3D 扩散过程以补全全局几何。
result: 在物体和场景生成任务中，该方法在渲染质量和几何保真度方面均显著优于当前最先进的基准模型。
conclusion: 通过显式嵌入点云先验，Points-to-3D 实现了更精确且结构可控的 3D 生成，证明了 3D 先验在提升生成质量方面的关键作用。
---

## 摘要
近期 3D 生成的进展主要由以图像或文本为条件的模型驱动，而现成的 3D 先验仍未得到充分利用。在许多现实场景中，可见区域的点云很容易通过 LiDAR 等主动传感器或 VGGT 等前馈预测器获得，它们提供了当前方法未能利用的显式几何约束。在这项工作中，我们介绍了 Points-to-3D，这是一个基于扩散的框架，利用点云先验进行几何可控的 3D 资产和场景生成。Points-to-3D 构建在潜空间 3D 扩散模型 TRELLIS 之上，首先将纯噪声的稀疏结构潜变量初始化替换为针对点云先验定制的输入公式。随后，在 TRELLIS 框架内使用旨在学习全局结构修复的任务特定数据训练的结构修复网络，通过阶段性采样策略（结构修复后进行边界细化）进行推理，在保留输入先验可见区域的同时补全全局几何。在实践中，Points-to-3D 可以接受精确的点云先验或从单张图像中由 VGGT 估计的点云作为输入。在物体和场景方案上的实验一致表明，在渲染质量和几何保真度方面，该方法优于最先进的基准模型，突显了显式嵌入点云先验对于实现更准确且结构可控的 3D 生成的有效性。

## Abstract
Recent progress in 3D generation has been driven largely by models conditioned on images or text, while readily available 3D priors are still underused. In many real-world scenarios, the visible-region point cloud are easy to obtain from active sensors such as LiDAR or from feed-forward predictors like VGGT, offering explicit geometric constraints that current methods fail to exploit. In this work, we introduce Points-to-3D, a diffusion-based framework that leverages point cloud priors for geometry-controllable 3D asset and scene generation. Built on a latent 3D diffusion model TRELLIS, Points-to-3D first replaces pure-noise sparse structure latent initialization with a point cloud priors tailored input formulation.A structure inpainting network, trained within the TRELLIS framework on task-specific data designed to learn global structural inpainting, is then used for inference with a staged sampling strategy (structural inpainting followed by boundary refinement), completing the global geometry while preserving the visible regions of the input priors.In practice, Points-to-3D can take either accurate point-cloud priors or VGGT-estimated point clouds from single images as input. Experiments on both objects and scene scenarios consistently demonstrate superior performance over state-of-the-art baselines in terms of rendering quality and geometric fidelity, highlighting the effectiveness of explicitly embedding point-cloud priors for achieving more accurate and structurally controllable 3D generation.

---

## 论文详细总结（自动生成）

以下是对论文《Points-to-3D: Structure-Aware 3D Generation with Point Cloud Priors》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的 3D 生成模型（如 TRELLIS、LGM 等）主要依赖 2D 图像或文本提示词作为条件。虽然这些模型能生成视觉上合理的 3D 资产，但由于缺乏显式的几何约束，它们无法严格遵循真实的 3D 测量数据。
在现实应用中，通过 LiDAR 传感器或前馈预测器（如 VGGT）获取局部点云非常容易。**Points-to-3D 的核心目标**是填补这一空白：将易于获取的局部点云作为“硬约束”引入 3D 生成流程，实现几何可控的生成，确保生成的模型在可见区域与观测数据精确对齐，同时在未观测区域进行合理的几何补全。

### 2. 方法论
该方法基于潜空间 3D 扩散模型 **TRELLIS** 进行改进，核心思想是将 3D 生成任务转化为一个**潜空间结构修复（Latent Structural Inpainting）**问题。

*   **核心思想**：不再从纯高斯噪声开始生成，而是利用点云先验初始化稀疏结构（SS）潜变量。
*   **关键技术细节**：
    1.  **输入公式化**：将可见区域的点云 $P$ 体素化为占用网格，通过 VAE 编码器 $E_s$ 转换为潜变量 $q_{vis}$。
    2.  **掩码感知输入**：构造输入 $x_{inp}$，它由可见区域潜变量、填充未观测区域的噪声以及一个下采样的占用掩码 $m_s$ 在通道维度拼接而成。
    3.  **结构修复网络 ($G_{inp}$)**：修改 TRELLIS 的 Flow Transformer，增加输入通道以接收掩码信息，并使用条件流匹配（Conditional Flow Matching, CFM）损失进行全量微调。
    4.  **分阶段采样策略**：
        *   **阶段 1：结构修复**（前 $s$ 步）：在掩码约束下生成全局一致的骨架。
        *   **阶段 2：边界细化**（后 $t-s$ 步）：移除掩码约束，进行标准去噪，以消除生成区域与观测区域交界处的几何“空洞”或不连续。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：3D-FUTURE、HSSD 和 ABO（涵盖约 2 万个物体）。
    *   **测试集**：Toys4K（物体级）、3D-FRONT（场景级）、Pix3D（真实世界图像）。
*   **Benchmark 与对比方法**：
    *   对比了 8 种 SOTA 方法，包括：TRELLIS、GaussianAnything、Real3D、LGM、VoxHammer（3D 反演）、SAM3D、MIDI、SceneGen。
*   **评估指标**：
    *   **渲染质量**：PSNR、SSIM、LPIPS、DINO 特征相似度。
    *   **几何质量**：Chamfer Distance (CD)、F-score、法线图 PSNR/LPIPS。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 Nvidia A100 GPU**。
*   **训练细节**：进行了 **20k 次迭代**，Batch Size 为 8。
*   **推理设置**：总采样步数为 50 步（25 步修复 + 25 步细化）。

### 5. 实验数量与充分性
*   **实验规模**：论文在物体级（Toys4K）和复杂的场景级（3D-FRONT）数据集上均进行了大规模测试（各 500 个样本）。
*   **消融实验**：针对采样步数的分配（修复步数 vs 细化步数）进行了详细对比，验证了“边界细化”阶段对消除几何缺陷的必要性。
*   **输入多样性**：测试了“精确点云先验”和“VGGT 预测点云”两种模式，证明了模型在不同精度输入下的鲁棒性。
*   **客观性**：实验对比了多种不同架构的模型（如基于高斯、基于体素、基于反演的方法），并引入了真实世界数据（Pix3D）进行验证，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **几何保真度飞跃**：在引入点云先验后，模型在可见区域的 F-score 达到了 0.998，远超传统生成模型。
*   **场景生成优势**：在多物体的复杂场景中，显式几何先验能显著降低生成模型在空间布局上的盲目性。
*   **预测先验的有效性**：即使点云是由 VGGT 从单图预测的（存在一定误差），Points-to-3D 的生成质量依然显著优于直接从图像生成的基准模型。
*   **采样策略的重要性**：简单的修复会导致边界不连续，分阶段采样是保证全局几何平滑的关键。

### 7. 优点（亮点）
*   **强可控性**：首次在 latent 3D diffusion 框架中实现了直接利用点云作为硬约束的生成流程。
*   **灵活性**：兼容传感器数据（LiDAR）和算法预测数据，具有很强的实用价值。
*   **高质量补全**：不仅保留了已知区域，还能利用扩散模型的先验知识，生成与已知结构逻辑一致的未观测部分。
*   **即插即用潜力**：基于 TRELLIS 架构，易于集成到现有的 3D 资产创作工作流中。

### 8. 不足与局限
*   **依赖基础模型**：其性能上限受限于预训练的 TRELLIS VAE 的压缩能力和表达能力。
*   **预测误差传递**：当使用 VGGT 预测点云时，如果预测的几何存在严重畸变，生成结果会受到直接影响。
*   **计算开销**：虽然是前馈生成，但相比于纯 2D 提升方法，体素化和稀疏结构编码增加了额外的预处理步骤。
*   **遮挡区域的幻觉**：对于完全遮挡且缺乏语义线索的区域，模型仍可能生成不符合实际但“看起来合理”的几何。

（完）
