---
title: "Scene Generation at Absolute Scale: Utilizing Semantic and Geometric Guidance From Text for Accurate and Interpretable 3D Indoor Scene Generation"
title_zh: 绝对尺度下的场景生成：利用文本语义和几何引导实现准确且可解释的三维室内场景生成
authors: "Stefan Ainetter, Thomas Deixelberger, Edoardo A. Dominici, Philipp Drescher, Konstantinos Vardis, Markus Steinberger"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13910v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 文本到3D室内场景生成框架
tldr: GuidedSceneGen是一个文本驱动的3D室内场景生成框架，旨在解决现有方法中的几何漂移和尺度模糊问题。该方法通过预测全局3D布局作为引导，结合语义和深度条件的全景扩散模型以及视频扩散模型，在绝对坐标系下生成具有度量准确性、全局一致性和语义可解释性的场景。最终利用3D高斯泼溅技术进行融合，实现了高效且可导航的3D重建。
selection_source: fresh_fetch
motivation: 现有的文本转3D场景生成方法常面临几何漂移、尺度模糊以及缺乏全局一致性和语义可解释性的挑战。
method: 该框架首先从文本生成全局3D布局，随后利用语义和深度引导的全景及视频扩散模型合成多视角图像，并使用3D高斯泼溅进行融合。
result: 实验表明，该方法在3D一致性和布局合理性上优于现有基准，且采样速度比穷举路径探索提升了高达10倍。
conclusion: GuidedSceneGen通过引入绝对尺度和几何引导，成功实现了高精度、可扩展且语义一致的室内场景生成。
---

## 摘要
我们提出了 GuidedSceneGen，这是一个文本到三维（text-to-3D）生成框架，能够产生度量准确、全局一致且语义可解释的室内场景。与以往经常受到几何漂移或尺度歧义困扰的文本驱动方法不同，我们的方法在整个生成过程中保持了绝对的世界坐标系。从文本场景描述出发，我们预测一个编码了语义和几何结构的全局三维布局，作为后续阶段的引导代理。随后，一个受语义和深度约束的全景扩散模型合成与全局布局对齐的 360° 图像，显著提高了空间连贯性。为了探索未观测区域，我们采用受优化相机轨迹引导的视频扩散模型，该模型平衡了覆盖范围和碰撞规避，与穷举路径探索相比，采样速度提升了高达 10 倍。生成的视图使用三维高斯泼溅（3D Gaussian Splatting）进行融合，从而产生一个绝对尺度下一致且完全可导航的三维场景。GuidedSceneGen 能够将物体位姿和语义标签从布局准确转移到重建结果中，并支持无需重新对齐的渐进式场景扩展。定量结果和用户研究表明，与最近的全景文本到三维基准模型相比，本方法具有更强的三维一致性和布局合理性。

## Abstract
We present GuidedSceneGen, a text-to-3D generation framework that produces metrically accurate, globally consistent, and semantically interpretable indoor scenes. Unlike prior text-driven methods that often suffer from geometric drift or scale ambiguity, our approach maintains an absolute world coordinate frame throughout the entire generation process. Starting from a textual scene description, we predict a global 3D layout encoding both semantic and geometric structure, which serves as a guiding proxy for downstream stages. A semantics- and depth-conditioned panoramic diffusion model then synthesizes 360° imagery aligned with the global layout, substantially improving spatial coherence. To explore unobserved regions, we employ a video diffusion model guided by optimized camera trajectories that balances coverage and collision avoidance, achieving up to 10x faster sampling compared to exhaustive path exploration. The generated views are fused using 3D Gaussian Splatting, yielding a consistent and fully navigable 3D scene in absolute scale. GuidedSceneGen enables accurate transfer of object poses and semantic labels from layout to reconstruction, and supports progressive scene expansion without re-alignment. Quantitative results and a user study demonstrate greater 3D consistency and layout plausibility compared to recent panoramic text-to-3D baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Scene Generation at Absolute Scale: Utilizing Semantic and Geometric Guidance From Text for Accurate and Interpretable 3D Indoor Scene Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
当前的文本到三维（Text-to-3D）场景生成方法虽然在视觉保真度上取得了显著进步，但普遍面临以下挑战：
*   **几何漂移与尺度歧义**：现有方法多依赖局部视图的迭代修复或自回归扩展，导致在大规模或多房间场景中积累几何畸变，且缺乏真实的物理度量尺度（Metric Scale）。
*   **缺乏语义可解释性**：生成的场景往往只是视觉上的堆砌，难以直接提取准确的物体位姿（9D Pose）和语义标签，限制了其在机器人导航和具身智能中的应用。
*   **生成效率低下**：为了覆盖未观测区域，现有方法通常需要详尽的路径探索，计算成本极高。

**GuidedSceneGen** 的核心动机是引入**绝对世界坐标系**和**显式几何/语义引导**，实现度量准确、全局一致且可解释的室内场景生成。

### 2. 方法论：核心思想与技术细节
该框架采用“从粗到精”的策略，主要分为四个阶段：
1.  **全局场景布局估计（Text-to-Layout）**：利用 Holodeck 模型根据文本生成三维代理布局（Proxy Layout）。该布局确立了绝对坐标系，并提供物体的 9D 位姿、语义标签和初始几何结构。
2.  **引导式全景图像生成**：
    *   将全景图表示为六面体贴图（Cubemap），以减少球面投影畸变。
    *   使用多视图潜扩散模型（LDM），通过膨胀注意力机制（Inflated Attention）确保视图间一致性。
    *   引入 **Multi-ControlNet**，分别以代理布局渲染的深度图和语义图作为条件，强制生成的 RGB 图像与全局几何对齐。
3.  **优化的相机轨迹采样与新视图合成（NVS）**：
    *   **碰撞感知采样**：基于代理几何自动规划避开墙壁和物体的相机路径，确保高覆盖率。
    *   **深度引导对齐**：通过二分查找优化相机尺度参数，使视频扩散模型生成的帧与全局坐标系精确对齐。
4.  **基于 3DGS 的场景重建**：
    *   利用 3D 高斯泼溅（3DGS）融合所有生成的视图。
    *   **损失函数**：结合了标准的 3DGS 损失、几何一致性损失（$L_{geom}$）、3D 最近邻正则化（$L_{NN}$）以及掩码深度损失（$L_{depth}$），确保重建结果符合度量尺度。

### 3. 实验设计
*   **数据集**：主要使用 **Structured3D**（提供高质量室内全景图和标注）进行训练和评估，同时参考了 Matterport3D 和 NYUv2-40 语义标准。
*   **对比基准（Benchmarks）**：
    *   **全景生成对比**：Diffusion360, MVDiffusion, LayerPano3D。
    *   **3D 场景一致性对比**：DreamScene360, LayerPano3D, WorldExplorer。
*   **评估指标**：
    *   2D 质量：CLIP Score（文本对齐）、Q-Align（美学）、IS（多样性）。
    *   3D 一致性：MEt3R（多视图一致性）、用户研究评分（感知质量、3D 一致性、整体质量）。
    *   对齐精度：相机平移/旋转误差、深度误差。

### 4. 资源与算力
*   **训练细节**：
    *   多视图扩散模型训练了 120k 次迭代（Batch size 2）。
    *   ControlNet 分支微调了 90k 次迭代（Batch size 1）。
    *   使用了 AdamW 优化器。
*   **推理算力**：文中提到在**单张消费级显卡**上运行。
*   **耗时**：单个场景重建约需 0.75 小时，比 WorldExplorer（约 7 小时）快约 10 倍，与 DreamScene360 处于同一量级。

### 5. 实验数量与充分性
*   **实验规模**：进行了定量对比、定性视觉对比、消融实验以及用户研究。
*   **用户研究**：邀请了 31 名具有专业背景的参与者，收集了 2945 个数据点，并进行了 ANOVA 方差分析以验证统计显著性（p < 0.05）。
*   **消融实验**：分别验证了深度引导、语义引导以及轨迹对齐策略对最终效果的影响。
*   **评价**：实验设计较为全面，涵盖了从图像质量到 3D 几何精度的多个维度，对比方法均为近年来的 SOTA，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **绝对尺度的重要性**：通过在生成全过程中保持绝对坐标系，GuidedSceneGen 成功消除了尺度模糊，实现了度量准确的重建。
*   **引导信号的有效性**：语义和深度引导显著减少了物体的几何畸变和类别混淆（如避免将窗户误生成为画框）。
*   **效率提升**：优化的轨迹采样策略在保证场景覆盖的同时，将采样速度提升了 10 倍。
*   **应用潜力**：该方法支持**渐进式场景扩展**，新生成的房间可以无缝拼接到现有场景中，无需重新对齐。

### 7. 优点（亮点）
*   **首个实现绝对尺度生成的框架**：填补了文本转 3D 场景中度量准确性的空白。
*   **高可解释性**：能够直接从代理布局中继承 9D 物体位姿和语义分割，便于下游任务使用。
*   **无缝扩展性**：支持多房间的连续生成，且保持全局坐标一致。
*   **鲁棒的几何约束**：通过 3DGS 与多重几何损失的结合，解决了传统方法中常见的“漂浮物”和几何空洞问题。

### 8. 不足与局限
*   **场景类型限制**：目前主要针对室内环境（依赖 Holodeck 布局生成），尚未扩展到复杂的室外场景。
*   **代理布局依赖**：最终生成的质量在很大程度上取决于第一阶段生成的 3D 代理布局的合理性。如果代理布局存在逻辑错误，后续生成也会受限。
*   **动态物体缺失**：目前仅支持静态场景生成，无法处理包含行人或移动物体的动态环境。
*   **应用限制**：虽然速度有所提升，但近 1 小时的生成时间对于实时交互应用仍显略长。

（完）
