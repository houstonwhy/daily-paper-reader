---
title: "SceneTransporter: Optimal Transport-Guided Compositional Latent Diffusion for Single-Image Structured 3D Scene Generation"
title_zh: SceneTransporter：用于单图像结构化 3D 场景生成的、由最优传输引导的组合式潜扩散模型
authors: "Ling Wang, Hao-Xiang Guo, Xinzhou Wang, Fuchun Sun, Kai Sun, Pengkun Liu, Hang Xiao, Zhong Wang, Guangyuan Fu, Eric Li, Yang Liu, Yikai Wang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22785v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从单张图像生成结构化3D场景的框架
tldr: SceneTransporter是一个从单张图像生成结构化3D场景的端到端框架。针对现有方法在开放场景中难以将部件组织成独立实例的问题，该研究将其重新定义为全局相关性分配问题。通过在组合式扩散模型（DiT）中引入最优传输（OT）引导，实现了图像块到3D潜变量的一对一路由和相干分组，显著提升了实例级的一致性和几何保真度。
selection_source: fresh_fetch
motivation: 现有3D场景生成方法缺乏内部结构约束，导致生成的部件难以正确组织成独立的物体实例。
method: 在组合式DiT的去噪循环中引入熵正则化最优传输（OT）目标，通过传输计划引导交叉注意力，实现图像块与3D潜变量的精确匹配与分组。
result: 实验表明，该方法在开放世界场景生成中显著优于现有技术，大幅提升了实例的连贯性和几何质量。
conclusion: 通过将结构化生成建模为最优传输问题，SceneTransporter有效解决了3D场景生成中的部件纠缠和碎片化难题。
---

## 摘要
我们提出了 SceneTransporter，这是一个用于从单张图像生成结构化 3D 场景的端到端框架。尽管现有方法能够生成部件级的 3D 对象，但它们往往难以在开放世界场景中将这些部件组织成独立的实例。通过去偏聚类探针，我们揭示了一个关键见解：这种失败源于模型内部分配机制中缺乏结构性约束。基于这一发现，我们将结构化 3D 场景生成任务重新定义为一个全局相关性分配问题。为解决此问题，SceneTransporter 在组合式 DiT 模型的去噪循环中构建并求解了一个熵正则化最优传输（OT）目标。该方案引入了两个强大的结构性约束：首先，所得的传输方案对交叉注意力进行门控，以强制实现图像块到部件级 3D 潜变量的排他性一对一路由，从而防止特征纠缠；其次，传输的竞争特性促进了相似图像块的聚合，这一过程通过基于边缘的代价进一步正则化，从而形成连贯的对象并防止碎片化。大量实验表明，SceneTransporter 在开放世界场景生成方面优于现有方法，显著提升了实例级连贯性和几何保真度。代码和模型将在 https://2019epwl.github.io/SceneTransporter/ 公开。

## Abstract
We introduce SceneTransporter, an end-to-end framework for structured 3D scene generation from a single image. While existing methods generate part-level 3D objects, they often fail to organize these parts into distinct instances in open-world scenes. Through a debiased clustering probe, we reveal a critical insight: this failure stems from the lack of structural constraints within the model's internal assignment mechanism. Based on this finding, we reframe the task of structured 3D scene generation as a global correlation assignment problem. To solve this, SceneTransporter formulates and solves an entropic Optimal Transport (OT) objective within the denoising loop of the compositional DiT model. This formulation imposes two powerful structural constraints. First, the resulting transport plan gates cross-attention to enforce an exclusive, one-to-one routing of image patches to part-level 3D latents, preventing entanglement. Second, the competitive nature of the transport encourages the grouping of similar patches, a process that is further regularized by an edge-based cost, to form coherent objects and prevent fragmentation. Extensive experiments show that SceneTransporter outperforms existing methods on open-world scene generation, significantly improving instance-level coherence and geometric fidelity. Code and models will be publicly available at https://2019epwl.github.io/SceneTransporter/.

---

## 论文详细总结（自动生成）

以下是对论文《SceneTransporter: Optimal Transport-Guided Compositional Latent Diffusion for Single-Image Structured 3D Scene Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何从单张图像生成具有“结构化”特征（即物体与背景、物体与物体之间相互独立且可编辑）的 3D 场景。
*   **研究背景**：现有的 3D 生成模型大多生成“单体式”（monolithic）网格，难以进行下游的物理模拟或资产编辑。虽然新兴的“部件级”生成器（如 PartPacker）尝试通过多个潜变量（latents）表示场景，但在处理复杂的开放世界场景时，常出现**结构误分区**（一个物体被拆碎到不同潜变量中）和**几何冗余**（多个潜变量重叠描述同一区域）的问题。
*   **核心动机**：作者通过探针实验发现，模型内部其实已经学到了物体的特征，但由于缺乏显式的结构约束，导致特征分配混乱。因此，需要一种机制来强制模型在生成过程中进行“正确且排他”的特征关联。

### 2. 方法论
SceneTransporter 将结构化生成重新定义为一个**全局相关性分配问题**，并引入最优传输（OT）理论进行约束。
*   **去偏聚类探针（Debiased Clustering Probe）**：利用典型相关分析（CCA）去除场景共性特征，证明了在残差特征中存在清晰的实例结构，为引入显式约束提供了理论依据。
*   **最优传输引导的关联分配**：
    *   **熵正则化 OT 目标**：在 DiT（扩散变换器）的去噪循环中，每一步都求解一个 OT 问题，计算图像块（patches）与 3D 部件 token 之间的全局最优传输计划（Transport Plan）。
    *   **OT 计划门控交叉注意力**：利用得到的传输计划作为门控信号，强制实现图像块到 3D 潜变量的“一对一”路由。这防止了多个 3D 部件竞争同一个图像区域，从而消除特征纠缠。
    *   **边缘正则化分配代价**：在计算分配成本时引入图像边缘信息（如 Canny 边缘），惩罚跨越显著边缘的特征分配，确保生成的 3D 部件边界与图像中的物体边界对齐。
*   **算法流程**：在去噪步骤 $t$ 中，提取 Query 和 Key 计算相似度代价矩阵 $\rightarrow$ 结合边缘正则化 $\rightarrow$ 通过 Sinkhorn 迭代求解 OT 计划 $\rightarrow$ 门控交叉注意力更新潜变量 $\rightarrow$ 解码生成 3D 场景。

### 3. 实验设计
*   **数据集/场景**：
    *   **开放世界测试集**：从 Web 收集的 74 张高质量多样化场景图像。
    *   **真实世界数据**：使用 DL3DV-10K 数据集进行零样本（Zero-shot）泛化测试。
*   **Benchmark 与对比方法**：
    *   对比了三种最先进的结构化生成方法：**MIDI**（基于分割的方案）、**PartCrafter** 和 **PartPacker**（端到端组合式方案）。
*   **评估指标**：
    *   **几何保真度**：ULIP, ULIP-2, Uni3D（衡量生成 3D 模型与原图的语义一致性）。
    *   **部件解耦度**：IoU max 和 IoU mean（衡量不同部件之间的空间重叠程度，越低越好）。
    *   **用户研究**：30 名参与者对几何质量、布局连贯性和分割合理性进行强制排序评分。

### 4. 资源与算力
*   **模型架构**：基于 PartPacker 的 Rectified-flow DiT，包含 24 个注意力块。
*   **算力细节**：文中**未明确说明**具体的训练 GPU 型号、数量及总训练时长。
*   **推理效率**：文中提到了推理时间，SceneTransporter 生成一个场景约需 **54.99 秒**，略慢于基准模型 PartPacker（47.41 秒），但远快于 MIDI（149.68 秒）和 PartCrafter（157.97 秒）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了定量对比实验（表 1）和定性视觉对比（图 4）。
    *   **消融实验**（表 4）：涵盖了 OT 门控模块的有效性、边缘正则化的影响、熵正则化系数 $\epsilon_t$、边缘敏感度 $\gamma_{edge}$ 以及 OT 作用于 DiT 不同层数的效果。
    *   **收敛性分析**：展示了 Sinkhorn 迭代在 40 次内能稳定收敛（图 9）。
*   **充分性评价**：实验设计较为全面，不仅有标准指标，还引入了针对“结构化”特有的 IoU 重叠度指标，并进行了真实世界图像的泛化测试，客观地展示了方法在解决“特征泄露”方面的优势。

### 6. 主要结论与发现
*   **OT 约束的必要性**：简单的组合式扩散模型无法自发形成清晰的实例边界，引入 OT 的一对一约束是实现结构化生成的关键。
*   **性能领先**：SceneTransporter 在保持高几何保真度的同时，显著降低了部件间的重叠（IoU 从 0.21 降至 0.09），生成的物体更加完整且独立。
*   **边缘信息的价值**：即使没有语义分割掩码，仅靠低级的图像边缘信息也能有效辅助 OT 找到正确的物体边界。

### 7. 优点（亮点）
*   **数学严谨性**：将复杂的特征分配问题转化为成熟的最优传输问题，理论支撑强。
*   **端到端且无需掩码**：相比 MIDI 等需要预先分割的方法，本方法在推理时不需要额外的实例分割模型，减少了误差累积。
*   **解决实际痛点**：直接针对“部件纠缠”和“几何冗余”这两个结构化生成中的核心病态问题提出解决方案。

### 8. 不足与局限
*   **密集微小物体处理**：在处理极其密集的微小实例（如远处的一群小船）时，OT 可能会因为容量限制将多个微小物体合并为一个。
*   **训练数据依赖**：模型主要在合成数据上训练，直接处理真实照片时存在领域差距（Domain Gap），文中需通过 GPT-5 进行风格转换（Style Transfer）来缓解。
*   **计算开销**：虽然比多阶段方法快，但相比原始 DiT，每一步去噪都要解 Sinkhorn 迭代，增加了约 15% 的推理耗时。

（完）
