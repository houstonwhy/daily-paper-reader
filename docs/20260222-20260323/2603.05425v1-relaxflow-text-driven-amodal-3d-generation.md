---
title: "RelaxFlow: Text-Driven Amodal 3D Generation"
title_zh: "RelaxFlow: 文本驱动的非遮挡 3D 生成"
authors: "Jiayin Zhu, Guoji Fu, Xiaolu Liu, Qiyuan He, Yicong Li, Angela Yao"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05425v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 文本驱动的非模态3D生成与补全
tldr: 针对图像转3D生成中遮挡导致的语义歧义问题，本文提出了RelaxFlow框架，旨在通过文本提示引导不可见区域的补全，同时严格保留输入观测。该框架采用无需训练的双分支结构，通过多先验共识模块和松弛机制解耦控制粒度。理论证明松弛机制等效于低通滤波，能有效隔离几何结构以适应观测。实验表明，该方法在保持视觉保真度的同时，能准确按提示意图生成缺失部分。
selection_source: fresh_fetch
motivation: 现有的图像转3D方法在处理遮挡时存在语义歧义，难以在保留已知观测的同时根据文本意图补全未知区域。
method: 提出一种无需训练的双分支框架RelaxFlow，利用多先验共识模块和松弛机制（等效于低通滤波）来平衡观测的刚性控制与文本的结构控制。
result: 在ExtremeOcc-3D和AmbiSem-3D基准测试上，RelaxFlow成功实现了符合文本描述的非可见区域补全，且不损害视觉质量。
conclusion: RelaxFlow通过解耦控制粒度，为文本驱动的非全貌（amodal）3D生成提供了一种有效且理论完备的解决方案。
---

## 摘要
图像到 3D 生成在遮挡情况下由于部分观测往往不足以确定物体类别，面临着固有的语义歧义。在这项工作中，我们形式化了文本驱动的非遮挡 3D 生成，其中文本提示引导未见区域的补全，同时严格保留输入观测。关键在于，我们发现这些目标需要不同的控制粒度：对观测值的刚性控制与对提示词的松弛结构控制。为此，我们提出了 RelaxFlow，这是一个无需训练的双分支框架，通过多先验共识模块和松弛机制解耦了控制粒度。在理论上，我们证明了我们的松弛机制等同于在生成向量场上应用低通滤波器，从而抑制高频实例细节，以分离出适应观测值的几何结构。为了便于评估，我们引入了两个诊断基准：ExtremeOcc-3D 和 AmbiSem-3D。大量实验表明，RelaxFlow 成功引导了未见区域的生成以匹配提示词意图，且不损害视觉保真度。

## Abstract
Image-to-3D generation faces inherent semantic ambiguity under occlusion, where partial observation alone is often insufficient to determine object category. In this work, we formalize text-driven amodal 3D generation, where text prompts steer the completion of unseen regions while strictly preserving input observation. Crucially, we identify that these objectives demand distinct control granularities: rigid control for the observation versus relaxed structural control for the prompt. To this end, we propose RelaxFlow, a training-free dual-branch framework that decouples control granularity via a Multi-Prior Consensus Module and a Relaxation Mechanism. Theoretically, we prove that our relaxation is equivalent to applying a low-pass filter on the generative vector field, which suppresses high-frequency instance details to isolate geometric structure that accommodates the observation. To facilitate evaluation, we introduce two diagnostic benchmarks, ExtremeOcc-3D and AmbiSem-3D. Extensive experiments demonstrate that RelaxFlow successfully steers the generation of unseen regions to match the prompt intent without compromising visual fidelity.

---

## 论文详细总结（自动生成）

### RelaxFlow: 文本驱动的非遮挡 3D 生成论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在“图像转 3D”生成任务中，遮挡会导致严重的**语义歧义**。例如，仅看到一个木质靠背，它可能属于沙发、床或梳妆台。现有的前馈模型（如 SAM3D）通常会坍缩到一种过拟合观测值的单一形状，缺乏可控性；而基于优化的方法虽然能遵循文本提示，但往往会扭曲原始观测的细节。
论文提出了**文本驱动的非遮挡（Amodal）3D 生成**任务：要求模型在严格保留可见区域细节的同时，利用文本提示显式地引导不可见区域的补全。其核心挑战在于如何平衡两种冲突的约束：对观测值的“刚性控制”与对文本语义的“松弛结构控制”。

#### 2. 方法论：核心思想与关键技术
论文提出了 **RelaxFlow**，一个无需训练的双分支推理框架。
*   **核心思想**：解耦控制粒度。通过“观测分支”保持高频细节，通过“语义先验分支”提供低频结构引导。
*   **关键技术细节**：
    *   **多先验共识模块 (Multi-Prior Consensus)**：由于 3D 生成器通常接受图像特征而非文本，该模块将文本提示转换为多个视觉代理（Prior Images）。通过在交叉注意力层中对多个先验进行拼接，利用模型自身的注意力机制提取共识属性，抑制特定实例的纹理干扰。
    *   **松弛机制 (Relaxation Mechanism)**：在语义分支中，对交叉注意力层（Cross-Attention）的 Logits 应用**高斯模糊**。
    *   **理论证明**：论文从数学上证明了 Logit 模糊等效于在生成向量场上应用**低通滤波器**。这能滤除高频的实例噪声，仅保留粗粒度的几何结构，从而避免语义先验与原始观测在细节上的冲突。
    *   **可见性感知融合 (Visibility-Aware Fusion)**：在推理过程中，根据相机视角计算每个体素的可见性。可见区域由观测分支主导，遮挡区域由语义分支引导，并随时间步（Time-step）动态调整融合权重。

#### 3. 实验设计
论文引入了两个专门的诊断基准：
*   **ExtremeOcc-3D**：基于 3D-FRONT 室内场景，筛选遮挡率超过 80% 的案例，测试模型在极端信息缺失下通过类别标签补全物体的能力。
*   **AmbiSem-3D**：包含 21 个具有语义分支的案例（如同一观测可对应不同物体），测试文本能否有效消除歧义。
*   **对比方法**：包括非遮挡感知模型（TRELLIS）、遮挡感知模型（SAM3D、Amodal3R）以及“2D 编辑+3D 重建”流水线。
*   **评估指标**：CLIP-Score（语义一致性）、LPIPS（观测保真度）、FID/Point-FID（3D 质量与分布相似度）。

#### 4. 资源与算力
*   **算力设备**：实验在一台 **NVIDIA A40 GPU** 上运行。
*   **训练时长**：该方法是**无需训练（Training-free）**的插件式模块，直接应用于预训练的 TRELLIS 或 SAM3D 模型，推理过程中的计算开销增加极小（仅增加了一次并行的向量场计算和简单的 1D 卷积模糊）。

#### 5. 实验数量与充分性
*   **实验规模**：在 ExtremeOcc-3D 上测试了 264 个案例，在 AmbiSem-3D 上测试了 21 个典型歧义案例。
*   **消融实验**：针对低通松弛（LP Relax）、几何掩码（Geom Mask）、截断阈值（$\rho$）、模糊强度（$\sigma$）以及先验图像数量（$N$）进行了详细的消融研究。
*   **主观评价**：组织了 32 名志愿者进行用户研究，评估文本对齐度和 3D 保真度。
*   **评价**：实验设计针对性强，通过极端遮挡和语义分支两种场景客观地验证了方法的有效性，对比基准涵盖了当前最先进的生成模型，实验结果较为充分且公平。

#### 6. 主要结论与发现
*   **解耦是关键**：统一的控制粒度会导致观测保真度与语义遵循之间的张力，通过低通滤波松弛语义分支是解决冲突的有效途径。
*   **性能提升**：RelaxFlow 在不牺牲观测保真度（LPIPS 保持领先）的前提下，显著提升了文本对齐度（CLIP 提升）和 3D 几何的合理性（Point-FID 显著下降）。
*   **通用性**：作为一种推理策略，它可以无缝集成到不同的基于 Rectified Flow 的 3D 生成器中。

#### 7. 优点（亮点）
*   **理论完备**：不仅提出了直觉上的模糊操作，还给出了向量场低通滤波的数学证明，增强了方法的说服力。
*   **无需训练**：在不改变模型权重的情况下实现了强大的文本可控性，极具实用价值。
*   **新基准**：提出的 ExtremeOcc-3D 和 AmbiSem-3D 填补了 3D 生成领域在非全貌感知和语义歧义处理评估上的空白。

#### 8. 不足与局限
*   **输入冲突风险**：当输入观测与文本先验存在本质冲突（如两个完全不同的物体正面）时，模型可能会产生“概念融合”或类似变形（Morphing）的失败结果，而非完美的补全。
*   **依赖视觉代理**：虽然支持文本，但核心仍依赖于将文本转为图像先验，这引入了额外的文本转图像步骤，可能受限于 2D 生成器的质量。
*   **应用限制**：目前主要针对具有明确几何结构的物体，对于极度复杂或无固定结构的场景效果尚待验证。

（完）
