---
title: "TIMI: Training-Free Image-to-3D Multi-Instance Generation with Spatial Fidelity"
title_zh: TIMI：具有空间保真度的免训练图像到 3D 多实例生成
authors: "Xiao Cai, Lianli Gao, Pengpeng Zeng, Ji Zhang, Heng Tao Shen, Jingkuan Song"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01371v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 图像到3D多实例场景生成
tldr: 针对图像到3D多实例生成中空间保真度不足和实例纠缠问题，本文提出了一种无需训练的框架TIMI。该方法利用预训练模型中未被充分利用的空间先验，通过实例感知分离引导（ISG）在去噪早期解耦实例，并结合空间稳定几何自适应更新（SGU）保持几何特征和相对关系。实验证明，TIMI在无需额外训练的情况下，实现了更优的全局布局和局部实例生成，且推理速度更快。
selection_source: fresh_fetch
motivation: 现有的多实例3D生成方法依赖昂贵的微调且难以保证空间保真度，而预训练模型中蕴含的空间先验尚未被有效利用。
method: 提出了包含实例感知分离引导（ISG）和空间稳定几何自适应更新（SGU）的无需训练框架，用于解耦实例并稳定几何关系。
result: 实验结果显示，该方法在全局布局和局部实例质量上均优于现有方法，且具备更快的推理速度。
conclusion: TIMI证明了通过合理利用预训练模型的空间先验，无需训练即可实现高质量、高保真度的多实例图像到3D生成。
---

## 摘要
在图像到 3D 多实例生成中，精确的空间保真度对于下游现实世界应用至关重要。最近的工作尝试通过在多实例数据集上微调预训练的图像到 3D (I23D) 模型来解决这一问题，但这会产生巨大的训练开销，且难以保证空间保真度。事实上，我们观察到预训练的 I23D 模型已经具备了有意义的空间先验，但由于实例纠缠问题，这些先验仍未得到充分利用。受此启发，我们提出了 TIMI，这是一个用于图像到 3D 多实例生成的创新型免训练框架，实现了高空间保真度。具体而言，我们首先引入了一个实例感知分离引导 (ISG) 模块，该模块在早期去噪阶段促进了实例解耦。接下来，为了稳定 ISG 引入的引导，我们设计了一个空间稳定几何自适应更新 (SGU) 模块，该模块在保持实例相对关系的同时，促进了实例几何特征的保留。大量的实验表明，与现有的多实例方法相比，我们的方法在全局布局和清晰的局部实例方面都表现出更好的性能，且无需额外训练，推理速度更快。

## Abstract
Precise spatial fidelity in Image-to-3D multi-instance generation is critical for downstream real-world applications. Recent work attempts to address this by fine-tuning pre-trained Image-to-3D (I23D) models on multi-instance datasets, which incurs substantial training overhead and struggles to guarantee spatial fidelity. In fact, we observe that pre-trained I23D models already possess meaningful spatial priors, which remain underutilized as evidenced by instance entanglement issues. Motivated by this, we propose TIMI, a novel Training-free framework for Image-to-3D Multi-Instance generation that achieves high spatial fidelity. Specifically, we first introduce an Instance-aware Separation Guidance (ISG) module, which facilitates instance disentanglement during the early denoising stage. Next, to stabilize the guidance introduced by ISG, we devise a Spatial-stabilized Geometry-adaptive Update (SGU) module that promotes the preservation of the geometric characteristics of instances while maintaining their relative relationships. Extensive experiments demonstrate that our method yields better performance in terms of both global layout and distinct local instances compared to existing multi-instance methods, without requiring additional training and with faster inference speed.

---

## 论文详细总结（自动生成）

### 论文总结：TIMI - 具有空间保真度的免训练图像到 3D 多实例生成

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的图像到 3D（Image-to-3D）生成技术在处理包含多个物体的复杂场景时，面临**空间保真度低**和**实例纠缠（Instance Entanglement）**的问题。
*   **研究背景**：
    *   **组合式方法**：先独立生成单个物体再拼凑，容易导致全局布局不一致、空间漂移或物体碰撞。
    *   **微调式方法**：在多实例数据集上微调预训练模型，训练开销巨大，且往往会破坏模型原有的空间先验，导致布局模糊或物体融合。
*   **研究动机**：作者观察到预训练的 3D 扩散模型（如 Hunyuan3D 2.0）其实已经具备了一定的空间先验，只是未被有效利用。因此，论文旨在提出一种**无需训练（Training-free）**的方法，通过引导（Guidance）机制直接从预训练模型中提取并强化这些先验，实现高保真度的多实例生成。

#### 2. 方法论
TIMI 框架由两个核心模块组成，旨在去噪过程中实现实例解耦并保持几何稳定：
*   **实例感知分离引导（ISG, Instance-aware Separation Guidance）**：
    *   **核心思想**：在去噪早期阶段，利用图像的实例掩码（Masks）来约束 3D-to-Image 的交叉注意力（Cross-Attention）图。
    *   **技术细节**：通过“实例感知注意力锚定”将 3D 潜变量（Latent Tokens）与 2D 实例区域对齐。引入**实例一致性分离损失（$L_{sep}$）**，这是一种空间加权的负对数似然损失，促使每个 3D Token 明确归属于特定的实例，从而在早期消除实例间的粘连。
*   **空间稳定几何自适应更新（SGU, Spatial-stabilized Geometry-adaptive Update）**：
    *   **核心思想**：为了防止 ISG 引入的梯度破坏物体的几何结构，对更新过程进行平滑和调制。
    *   **技术细节**：
        *   **空间稳定正则化（SR）**：使用 3D 高斯平滑处理分离梯度，确保梯度传播在空间上是连续的，避免几何破碎。
        *   **几何自适应调制（GM）**：根据当前潜变量的能量分布动态调整更新步长（$\lambda_{adap}$），防止对细长或脆弱结构过度变形。
        *   **动量更新**：引入动量机制平滑时间轴上的更新轨迹，减少震荡。

#### 3. 实验设计
*   **数据集**：
    1.  **合成数据**：从 3D-Front 随机抽取 30 个包含多个相邻物体的场景。
    2.  **真实世界数据**：从 Real-Data 收集 20 张真实室内图像。
    3.  **风格化数据**：使用 Flux.1 生成 20 张不同艺术风格的多实例图像。
*   **对比方法（Baselines）**：
    *   **Hunyuan3D 2.0**：作为基础的单实例生成模型。
    *   **DPA**：代表性的组合式（Decompose-and-Assemble）方法。
    *   **MIDI**：代表性的基于微调（Training-based）的多实例生成方法。
*   **评估指标**：
    *   全局/局部倒角距离（CD-S/CD-O）和 F-Score（FS-S/FS-O）。
    *   **布局一致性距离（LCD）**：衡量物体中心点与真值的对齐程度。
    *   **分离成功率（SSR）**：衡量生成的独立实例数量是否准确。

#### 4. 资源与算力
*   **训练开销**：**零训练开销**。该方法不需要对预训练模型进行任何参数更新。
*   **推理效率**：
    *   TIMI 的推理时间约为 **59.2秒**，仅比基础模型（54.2s）略多，显著快于 MIDI（90.1s）和 DPA（783s）。
*   **硬件说明**：文中未明确列出具体的 GPU 型号和数量，但强调了其推理速度的优势和无需微调的特性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了合成、真实、风格化三大类场景，共计 70 个测试案例。
*   **消融实验**：非常详尽。作者分别验证了 ISG 和 SGU 的必要性，并对引导层数（$l$）、去噪步数（$t$）、引导强度（$\alpha$）和高斯平滑因子（$\sigma$）进行了细致的参数敏感性分析。
*   **主观评价**：进行了用户研究（User Study），结果显示 TIMI 在布局对齐（57.5% 偏好）和实例清晰度（60.1% 偏好）上均大幅领先对比方法。
*   **评价**：实验设计科学、对比公平，通过多维度指标（客观几何指标 + 布局指标 + 主观评价）证明了方法的有效性。

#### 6. 主要结论与发现
*   **无需训练即可超越微调**：TIMI 在全局布局准确性和局部实例独立性上均优于经过专门微调的 MIDI 模型。
*   **空间先验的有效性**：证明了预训练 3D 扩散模型内部确实存在强大的空间布局能力，通过合理的注意力引导即可将其激发出。
*   **几何稳定性至关重要**：直接应用分离梯度会导致几何畸变，必须结合空间平滑和自适应调制才能生成高质量的 3D 资产。

#### 7. 优点
*   **高效性**：无需昂贵的 GPU 训练资源，即插即用，推理速度快。
*   **高保真度**：有效解决了多物体生成中常见的“物体融合”和“布局漂移”问题。
*   **泛化性强**：在真实图像和 AI 生成的风格化图像上均表现稳健，不局限于特定的训练分布。
*   **模块化设计**：ISG 和 SGU 逻辑清晰，易于集成到其他基于 DiT 的 3D 生成框架中。

#### 8. 不足与局限
*   **依赖外部掩码**：方法需要预先获取图像的实例掩码（文中使用了 Grounded-SAM），掩码的质量会直接影响生成效果。
*   **基础模型限制**：作为一种引导方法，其生成的上限仍受限于预训练基础模型（如 Hunyuan3D 2.0）本身的建模能力。
*   **极端遮挡处理**：虽然论文展示了相邻物体的处理，但在物体极度重叠或遮挡严重的复杂场景下，交叉注意力的引导可能会变得模糊。
*   **超参数敏感性**：虽然提供了默认配置，但在某些极端案例下可能仍需微调引导强度 $\alpha$ 以获得最佳效果。

（完）
