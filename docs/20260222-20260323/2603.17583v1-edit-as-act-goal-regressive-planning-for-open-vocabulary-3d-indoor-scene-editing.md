---
title: "Edit-As-Act: Goal-Regressive Planning for Open-Vocabulary 3D Indoor Scene Editing"
title_zh: Edit-As-Act：面向开放词汇 3D 室内场景编辑的目标回归规划
authors: "Seongrae Noh, SeungWon Seo, Gyeong-Moon Park, HyeongYeop Kang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17583v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 基于自然语言的开放词汇3D室内场景编辑
tldr: 本研究针对开放词汇3D室内场景编辑中存在的全局变化过多和物理不一致问题，提出了Edit-As-Act框架。该框架将编辑视为实现目标状态的最小动作序列，而非单纯的生成任务。通过设计受PDDL启发的EditLang动作语言，并结合语言驱动的规划器与物理验证器，实现了可解释且物理连贯的场景变换。在E2A-Bench基准测试中，该方法在指令忠实度、语义一致性和物理合理性上均显著优于现有范式。
selection_source: fresh_fetch
motivation: 现有3D场景编辑方法常导致全局非预期变化或物理不一致，因为它们将其视为生成任务而非动作序列。
method: 提出Edit-As-Act框架，通过设计受PDDL启发的EditLang动作语言，将编辑任务转化为3D空间中的目标回归规划。
result: 在包含63个任务的E2A-Bench基准测试中，该方法在指令忠实度、语义一致性和物理合理性方面显著优于现有方法。
conclusion: 将场景编辑视为最小动作序列规划能有效提升开放词汇3D室内场景编辑的物理一致性和可解释性。
---

## 摘要
根据自然语言编辑 3D 室内场景在概念上直观但在技术上具有挑战性。现有的开放词汇系统通常会重新生成场景的大部分内容，或者依赖于破坏空间结构的图像空间编辑，从而导致意外的全局变化或物理上不一致的布局。这些局限性源于将编辑主要视为一项生成任务。我们提出了一种不同的观点：用户指令定义了一个期望的世界状态，而编辑应该是使该状态成真并同时保留其他所有内容的最短动作序列。这一视角促使了 Edit-As-Act 的诞生，这是一个将开放词汇场景编辑视为 3D 空间中目标回归规划的框架。给定源场景和自由形式的指令，Edit-As-Act 预测符号化目标谓词，并在 EditLang 中进行规划；EditLang 是我们设计的一种受 PDDL 启发的动作语言，具有编码支撑、接触、碰撞和其他几何关系的显式前置条件和效果。语言驱动的规划器提出动作，验证器强制执行目标导向性、单调性和物理可行性，从而产生可解释且物理连贯的变换。通过将推理与底层生成分离，Edit-As-Act 实现了指令保真度、语义一致性和物理合理性——这是现有范式无法同时满足的三个标准。在包含 9 个室内环境、63 个编辑任务的基准测试 E2A-Bench 上，Edit-As-Act 在所有编辑类型和场景类别中均显著优于之前的方法。

## Abstract
Editing a 3D indoor scene from natural language is conceptually straightforward but technically challenging. Existing open-vocabulary systems often regenerate large portions of a scene or rely on image-space edits that disrupt spatial structure, resulting in unintended global changes or physically inconsistent layouts. These limitations stem from treating editing primarily as a generative task. We take a different view. A user instruction defines a desired world state, and editing should be the minimal sequence of actions that makes this state true while preserving everything else. This perspective motivates Edit-As-Act, a framework that performs open-vocabulary scene editing as goal-regressive planning in 3D space. Given a source scene and free-form instruction, Edit-As-Act predicts symbolic goal predicates and plans in EditLang, a PDDL-inspired action language that we design with explicit preconditions and effects encoding support, contact, collision, and other geometric relations. A language-driven planner proposes actions, and a validator enforces goal-directedness, monotonicity, and physical feasibility, producing interpretable and physically coherent transformations. By separating reasoning from low-level generation, Edit-As-Act achieves instruction fidelity, semantic consistency, and physical plausibility - three criteria that existing paradigms cannot satisfy together. On E2A-Bench, our benchmark of 63 editing tasks across 9 indoor environments, Edit-As-Act significantly outperforms prior approaches across all edit types and scene categories.

---

## 论文详细总结（自动生成）

以下是对论文《Edit-As-Act: Goal-Regressive Planning for Open-Vocabulary 3D Indoor Scene Editing》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何根据自然语言指令精确、局部且物理一致地编辑 3D 室内场景。
*   **研究动机**：现有的 3D 场景编辑方法（如生成式扩散模型、约束优化或图像提升方法）存在“不可能三角”：难以同时满足**指令忠实度**（准确执行命令）、**语义一致性**（不改动无关区域）和**物理合理性**（无碰撞、有支撑）。
*   **核心转变**：论文认为编辑不应是“重新生成”，而应是一个**目标驱动的规划过程**。用户指令定义了期望的“世界状态”，编辑则是实现该状态所需的**最小动作序列**。

### 2. 方法论：Edit-As-Act 框架
该框架将编辑任务建模为 3D 空间中的**目标回归规划（Goal-Regressive Planning）**：
*   **EditLang 符号语言**：设计了一种受 PDDL 启发的动作语言。它包含：
    *   **谓词（Predicates）**：描述几何和物理关系，如 `on(x, y)`、`supported(x)`、`colliding(x, y)`。
    *   **动作（Actions）**：定义了原子操作（移动、旋转、添加、样式化），每个动作都有明确的**前置条件（Preconditions）**和**效果（Effects）**。
*   **规划器-验证器循环（Planner-Validator Loop）**：
    *   **规划器（LLM驱动）**：将自然语言转化为符号目标，并提议能满足目标的动作。
    *   **验证器**：强制执行目标导向性、单调性（不撤销已达成的目标）和物理可行性（通过几何检查如碰撞检测和支撑分析）。
*   **源感知目标回归（Source-Aware Regression）**：
    *   公式：$Gt-1 = (Gt \setminus add(at)) \cup (pre(at) \setminus s0)$。
    *   逻辑：从最终目标状态倒推，仅针对源场景中尚未满足的条件生成子目标，从而避免冗余操作，确保编辑的最小化。
*   **执行层**：将规划好的符号动作序列通过 Python DSL 运行时转化为具体的几何变换或资产生成（利用 Hyper3D 等工具）。

### 3. 实验设计
*   **基准测试（E2A-Bench）**：创建了一个包含 **63 个编辑任务**的基准，涵盖 **9 种室内环境**（浴室、卧室、厨房、办公室等）。任务包括简单的移动到复杂的组合编辑（如“旋转椅子面对窗户并在旁边放一盏灯”）。
*   **对比方法**：
    *   **LayoutGPT-E**：直接 3D 布局编辑（生成式）。
    *   **AnyHome**：基于约束的优化方法。
    *   **ArtiScene-E**：图像驱动编辑后进行 3D 提升。
    *   **推理基线**：GPT-5、Gemini-3-Pro、Claude-4.5 以及迭代式框架 SceneWeaver。
*   **评估指标**：指令忠实度 (IF)、语义一致性 (SC)、物理合理性 (PP)，均采用 LVLM 评分并辅以人工评估验证。

### 4. 资源与算力
*   **模型调用**：主要依赖大语言模型 API。实验中使用了 **GPT-5 API**（作为规划器和验证器内核）、**Gemini 2.5 Flash**（用于图像生成）和 **Hyper3D Gen-2**（用于 3D 资产生成）。
*   **算力细节**：文中未明确提及具体的 GPU 训练时长，因为该方法属于 **Training-free（免训练）** 框架，核心开销在于 LLM 的推理调用（平均每个任务约 5.9 次调用，1.9 万个 Token）。

### 5. 实验数量与充分性
*   **实验规模**：在 9 类场景下进行了 63 组完整任务测试，并进行了 10 人的用户主观研究。
*   **消融实验**：非常充分，分别验证了**验证器**、**源感知机制**、**EditLang 语言**、**正向规划 vs 逆向规划**以及**坐标直接预测**对结果的影响。
*   **客观性**：通过标准化所有对比方法的底层模型（统一使用 GPT-5）和资产生成管线，确保了性能差异仅源于“推理策略”而非模型能力的基准差异。

### 6. 主要结论与发现
*   **规划优于生成**：将编辑视为符号动作序列显著提升了局部编辑的准确性，SC 指标（语义一致性）比生成式方法高出约 30-40 分。
*   **逆向推理的优势**：目标回归（从结果推过程）比正向搜索更能有效处理复杂的空间依赖关系。
*   **物理验证的必要性**：显式的几何检查（验证器）是防止物体漂浮、碰撞或穿墙的关键，使物理合理性（PP）稳定在 90 分以上。

### 7. 优点
*   **高局部性**：通过符号化推理，仅修改受影响的物体，完美保留了场景的其他部分。
*   **物理严谨**：引入了经典的 PDDL 思想，使 LLM 的常识推理与硬性的几何约束相结合。
*   **可解释性**：输出的是人类可读的动作序列（如 `move_to`, `place_on`），而非黑盒生成的坐标。
*   **免训练**：无需在特定数据集上微调，具有极强的开放词汇泛化能力。

### 8. 不足与局限
*   **谓词依赖**：EditLang 的谓词集（如 `on`, `near`）是预设的，对于极其模糊或高度抽象的指令（如“让房间看起来更乱”）处理能力有限。
*   **静态限制**：目前仅针对单场景静态编辑，尚未扩展到多房间或动态交互场景。
*   **规划死锁**：在极少数情况下，严格的单调性约束可能导致规划器陷入死锁（无法在不破坏已达成目标的前提下完成新目标）。
*   **风格局限**：虽然能处理几何和基本样式，但对于复杂的艺术风格迁移或精细的纹理编辑仍依赖外部生成模型。

（完）
