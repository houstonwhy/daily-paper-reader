---
title: "Dehallu3D: Hallucination-Mitigated 3D Generation from Single Image via Cyclic View Consistency Refinement"
title_zh: Dehallu3D：通过循环视图一致性细化实现单图像幻觉缓解的 3D 生成
authors: "Xiwen Wang, Shichao Zhang, Hailun Zhang, Ruowei Wang, Mao Li, Chenyu Zhou, Qijun Zhao, Ji-Zhe Zhou"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01601v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 减轻单图3D生成中的幻觉问题
tldr: 针对大型3D重建模型中因多视图生成不连续导致的幻觉（如结构异常、空洞或突起）问题，本文提出了Dehallu3D框架。该框架通过即插即用的优化模块，利用相邻视图一致性和自适应平滑约束，在保持几何细节的同时消除结构异常。此外，研究还提出了ORM指标来量化几何保真度，实验证明该方法能显著提升单图生成3D模型的质量。
selection_source: fresh_fetch
motivation: 现有的3D重建模型因多视图生成存在较大视点间隙和不连续性，容易产生偏离输入的结构性幻觉（如异常突起或孔洞）。
method: 提出Dehallu3D框架，通过相邻一致性约束确保视图间的几何连续性，并结合自适应平滑约束在消除幻觉的同时保留精细几何特征。
result: 实验表明Dehallu3D能有效移除幻觉离群点并保留结构细节，在定性和定量评估（包括新提出的ORM指标）中均表现优异。
conclusion: Dehallu3D通过强化密集中间视点的平滑过渡，为缓解3D生成中的幻觉问题提供了一种高效且即插即用的解决方案。
---

## 摘要
大型 3D 重建模型彻底改变了 3D 内容生成领域，在虚拟现实和游戏领域实现了广泛的应用。与其他大模型一样，大型 3D 重建模型也存在幻觉问题，会引入偏离输入数据的结构性离群值（例如，奇怪的孔洞或突起）。然而，与其他大模型不同，大型 3D 重建模型中的幻觉问题仍未得到充分探索，这导致了 3D 打印物体畸形或虚拟场景沉浸感不足。这种幻觉主要源于现有方法是从稀疏生成的多视图图像中重建 3D 内容，而这些图像存在较大的视角间隙和不连续性。为了通过消除离群值来减轻幻觉，我们提出了用于 3D 网格生成的 Dehallu3D。我们的核心思想是设计一种平衡的多视图连续性约束，以强制密集中间视角之间的平滑过渡，同时避免可能抹除尖锐几何特征的过度平滑。因此，Dehallu3D 采用了一个即插即用的优化模块，包含两个关键约束：(i) 相邻一致性，以确保跨视图的几何连续性；(ii) 自适应平滑，以保留精细细节。我们还进一步提出了离群值风险度量 (ORM) 指标，从离群值的角度量化 3D 生成中的几何保真度。大量实验表明，Dehallu3D 通过有效保留结构细节并消除幻觉离群值，实现了高保真 3D 生成。

## Abstract
Large 3D reconstruction models have revolutionized the 3D content generation field, enabling broad applications in virtual reality and gaming. Just like other large models, large 3D reconstruction models suffer from hallucinations as well, introducing structural outliers (e.g., odd holes or protrusions) that deviate from the input data. However, unlike other large models, hallucinations in large 3D reconstruction models remain severely underexplored, leading to malformed 3D-printed objects or insufficient immersion in virtual scenes. Such hallucinations majorly originate from that existing methods reconstruct 3D content from sparsely generated multi-view images which suffer from large viewpoint gaps and discontinuities. To mitigate hallucinations by eliminating the outliers, we propose Dehallu3D for 3D mesh generation. Our key idea is to design a balanced multi-view continuity constraint to enforce smooth transitions across dense intermediate viewpoints, while avoiding over-smoothing that could erase sharp geometric features. Therefore, Dehallu3D employs a plug-and-play optimization module with two key constraints: (i) adjacent consistency to ensure geometric continuity across views, and (ii) adaptive smoothness to retain fine details.We further propose the Outlier Risk Measure (ORM) metric to quantify geometric fidelity in 3D generation from the perspective of outliers. Extensive experiments show that Dehallu3D achieves high-fidelity 3D generation by effectively preserving structural details while removing hallucinated outliers.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Dehallu3D** 的框架，旨在解决单张图像生成 3D 模型时常见的“幻觉”（Hallucination）问题，即生成的模型中出现不符合逻辑的结构性离群值（如异常突起或孔洞）。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 重建模型（如 LRM 或基于扩散模型的方法）在从单图生成 3D 内容时，常会产生偏离输入的几何离群值（Outliers）。
*   **研究动机**：作者认为这些“幻觉”主要源于生成的多视图图像过于稀疏，视点之间存在巨大间隙和不连续性，导致重建算法在填补空白时引入了错误的几何细节。这种缺陷会严重影响 3D 打印的成功率和虚拟场景的沉浸感。

### 2. 方法论：Dehallu3D 框架
Dehallu3D 采用“粗到精”的两阶段优化策略，核心是一个即插即用的 **循环视图一致性细化（CVCR）** 模块。

*   **核心思想**：通过插入密集的中间视点（每 5° 一个视点，共 72 个），强制相邻视点之间保持几何连续性，从而消除突兀的离群点。
*   **关键技术细节**：
    *   **粗重建阶段**：使用“表面暴露权重法线损失”（$L_{SE}$），根据顶点在各视角下的投影面积动态分配权重，优先优化高可见度区域，快速建立全局拓扑。
    *   **CVCR 细化阶段**：
        1.  **循环深度一致性损失（$L_{DC}$）**：在 360° 旋转路径上，利用 SSIM 和余弦相似度强制相邻密集视点的深度图保持一致。
        2.  **自适应深度平滑损失（$L_{DS}$）**：为了防止过度平滑，利用彩色图像的梯度作为权重。在图像颜色变化剧烈处（通常是边缘或尖锐特征），降低平滑约束，从而保留几何细节。
*   **新指标 ORM**：提出了“离群值风险度量”（Outlier Risk Measure），基于条件风险价值（CVaR）理论，通过点云密度和 VAE 重建误差来量化 3D 模型的幻觉程度。

### 3. 实验设计
*   **数据集**：主要在 **Google Scanned Objects (GSO)** 数据集上进行评估，所有物体渲染为 512x512 分辨率。
*   **对比方法（Benchmarks）**：对比了当前最先进的（SOTA）方法，包括 **SF3D、Unique3D、CRM、InstantMesh、TripoSR 和 Wonder3D**。
*   **评估指标**：
    *   外观质量：PSNR, SSIM, LPIPS, Clip-Similarity。
    *   几何质量：Chamfer Distance (CD), F-Score。
    *   幻觉评估：新提出的 ORM 指标。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA GeForce RTX 4090 GPU** 上完成。
*   **耗时情况**：论文在消融实验中提到，使用 5° 间隔的 CVCR 模块进行细化大约需要 **163.3 秒**。
*   **训练细节**：该方法属于优化型（Optimization-based）微调，而非从头训练大型基础模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了定量对比、定性视觉对比、消融实验以及 ORM 指标的有效性验证。
*   **消融实验**：详细测试了 $L_{SE}$、$L_{DC}$、$L_{DS}$ 各组件的作用，以及不同视点采样间隔（3° 到 15°）对结果的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了生成质量，还通过将 CVCR 模块集成到 Unique3D 中证明了其“即插即用”的泛化能力。引入 ORM 指标填补了 3D 幻觉量化评估的空白，实验逻辑闭环。

### 6. 主要结论与发现
*   **幻觉抑制有效**：Dehallu3D 在保持纹理细节的同时，显著减少了模型表面的异常突起和空洞。
*   **性能领先**：在 GSO 数据集的所有视觉和几何指标上，Dehallu3D 均达到了最优（Best）或次优（Second-best）水平。
*   **平衡点**：5° 的视点间隔是质量与效率的最佳平衡点；过密的间隔（如 3°）反而可能因过度约束导致几何保真度轻微下降。

### 7. 优点
*   **即插即用**：CVCR 模块不依赖特定的初始化方法，可集成到现有的各种 3D 生成管线中。
*   **细节保留**：通过自适应平滑机制，解决了传统平滑算法容易抹除尖锐特征（如刺、棱角）的问题。
*   **评估创新**：ORM 指标为 3D 生成领域的“幻觉”研究提供了客观的量化工具。

### 8. 不足与局限
*   **时间开销**：由于引入了密集的视图渲染和优化过程，生成速度比纯前馈模型（如 TripoSR）慢，不适合对实时性要求极高的场景。
*   **计算成本**：密集的视图渲染增加了显存和计算压力。
*   **依赖初始化**：虽然是即插即用，但如果初始化的粗糙网格拓扑结构存在根本性错误，该模块的修复能力可能受限。

（完）
