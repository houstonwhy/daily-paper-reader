---
title: "One2Scene: Geometric Consistent Explorable 3D Scene Generation from a Single Image"
title_zh: One2Scene：从单张图像生成几何一致的可探索 3D 场景
authors: "Pengfei Wang, Liyi Chen, Zhiyuan Ma, Yanjun Guo, Guowen Zhang, Lei Zhang"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19766v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从单张图像生成可探索的3D场景
tldr: One2Scene 针对单图生成可探索 3D 场景中的几何畸变问题，提出了一种将任务分解为全景图生成、3D 几何支架构建和新视角生成的框架。通过将全景图投影为多视角锚点并利用高斯泼溅（Gaussian Splatting）网络构建几何支架，结合双向特征融合确保一致性，实现了在大范围相机移动下仍能保持几何准确且高保真的 3D 场景探索。
selection_source: fresh_fetch
motivation: 现有的单图 3D 场景生成方法在视角大幅移动时容易产生严重的几何畸变和噪声，难以支持自由探索。
method: 该框架首先生成全景锚点图，随后通过前馈高斯泼溅网络将其转化为 3D 几何支架，并利用该支架引导新视角生成。
result: 实验表明，One2Scene 在全景深度估计、360° 重建和可探索场景生成方面均显著优于现有最先进方法。
conclusion: One2Scene 通过引入显式的 3D 一致性几何支架，有效解决了单图生成 3D 场景中的几何稳定性问题，支持沉浸式场景探索。
---

## 摘要
从单张图像生成可探索的 3D 场景是 3D 视觉中一个极具挑战性的问题。现有方法难以支持自由探索，当视点远离原始视角时，往往会产生严重的几何失真和噪声伪影。我们提出了 One2Scene，这是一个有效的框架，它将这一病态问题分解为三个可处理的子任务，以实现沉浸式可探索场景的生成。我们首先使用全景图生成器从单张输入图像中生成锚点视图作为初始化。然后，我们通过一个通用的前馈高斯泼溅（Gaussian Splatting）网络，将这些 2D 锚点提升为显式的 3D 几何支架。我们没有将全景图视为单张图像进行重建，而是将其投影到多个稀疏锚点视图中，并将重建任务重新表述为多视图立体匹配，这使我们能够利用从大规模多视图数据集中学习到的鲁棒几何先验。使用双向特征融合模块来增强跨视图一致性，从而产生高效且几何可靠的支架。最后，该支架作为新视图生成器的强先验，在任意相机位姿下生成具有照片级真实感且几何准确的视图。通过显式地以 3D 一致性支架为条件进行重建，One2Scene 在大范围相机运动下也能稳定工作，支持沉浸式场景探索。大量实验表明，One2Scene 在全景深度估计、前馈 360° 重建和可探索 3D 场景生成方面显著优于现有最先进的方法。代码和模型将会开源。

## Abstract
Generating explorable 3D scenes from a single image is a highly challenging problem in 3D vision. Existing methods struggle to support free exploration, often producing severe geometric distortions and noisy artifacts when the viewpoint moves far from the original perspective. We introduce \textbf{One2Scene}, an effective framework that decomposes this ill-posed problem into three tractable sub-tasks to enable immersive explorable scene generation. We first use a panorama generator to produce anchor views from a single input image as initialization. Then, we lift these 2D anchors into an explicit 3D geometric scaffold via a generalizable, feed-forward Gaussian Splatting network. Instead of treating the panorama as a single image for reconstruction, we project it into multiple sparse anchor views and reformulate the reconstruction task as multi-view stereo matching, which allows us to leverage robust geometric priors learned from large-scale multi-view datasets. A bidirectional feature fusion module is used to enforce cross-view consistency, yielding an efficient and geometrically reliable scaffold. Finally, the scaffold serves as a strong prior for a novel view generator to produce photorealistic and geometrically accurate views at arbitrary cameras. By explicitly conditioning on a 3D-consistent scaffold to perform reconstruction, One2Scene works stably under large camera motions, supporting immersive scene exploration. Extensive experiments show that One2Scene substantially outperforms state-of-the-art methods in panorama depth estimation, feed-forward 360° reconstruction, and explorable 3D scene generation. Code and models will be released.

---

## 论文详细总结（自动生成）

以下是对论文《One2Scene: Geometric Consistent Explorable 3D Scene Generation from a Single Image》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何从单张图像生成一个可以自由探索、且在长距离视角移动下保持几何一致性和视觉高保真度的 3D 场景。
*   **研究背景**：
    *   现有的稀疏视图重建方法（如 NeRF, 3DGS）在处理大幅度视角外推时表现不佳。
    *   基于视频生成或迭代修复（Inpainting）的方法在长序列生成中容易产生几何畸变、语义漂移和累积误差。
    *   全景图生成方法虽然提供了全局覆盖，但缺乏显式的 3D 结构，导致视角移动时出现严重的拉伸和伪影。
*   **整体含义**：One2Scene 提出将这一复杂的病态问题分解为三个可处理的子任务：全景初始化、3D 几何支架构建、以及支架引导的视角合成。

### 2. 方法论
One2Scene 框架由三个阶段组成：
*   **阶段一：全景图生成（初始化）**：
    *   使用预训练的图像到全景图模型（如 Hunyuan-Pano-DiT）将单图扩展为 360° 全景图，作为场景的全局视觉基础。
*   **阶段二：前馈 3D 几何支架构建**：
    *   **锚点视图投影**：将全景图投影为 6 个立方体贴图（Cubemap）视角，并将单目深度估计问题转化为多视角立体匹配（MVS）问题，以利用大规模多视角数据集的几何先验。
    *   **双向融合模块（BFM）**：通过“立方体到等距柱状投影（C2E）”和“等距柱状到立方体投影（E2C）”的转换，在统一的潜空间中融合特征，确保跨视图边界的几何一致性。
    *   **前馈 3DGS**：直接预测每个像素的 3D 高斯参数（位置、不透明度、协方差、颜色），在 0.5 秒内构建出显式的 3D 几何支架。
*   **阶段三：支架引导的新视角合成（NVS）**：
    *   **双 LoRA 训练策略**：使用两个独立的 LoRA 模块分别处理高质量的锚点视图和从 3D 支架渲染出的粗糙但几何准确的视图，通过 3D 感知注意力机制进行融合。
    *   **记忆条件（Memory Condition）**：在推理时引入之前生成的帧作为参考，确保长序列生成的时空一致性。

### 3. 实验设计
*   **数据集**：
    *   **3D 重建训练**：Structured3D, Deep360 (合成); Matterport3D, Stanford2D3D (真实)。
    *   **视角合成训练**：DL3DV, RealEstate10K。
    *   **评估基准**：WorldScore 评测集（包含室内/室外、真实/风格化共 40 个场景）。
*   **对比方法**：
    *   **场景生成**：DreamScene360, WonderJourney, VMem, SEVA。
    *   **深度估计**：UniFuse, HoHoNet, PanoFormer, Depth Anywhere 等。
    *   **重建质量**：AnySplat。
*   **评估指标**：
    *   **视觉质量**：NIQE, Q-Align。
    *   **语义一致性**：CLIP-I。
    *   **几何稳定性**：旋转误差 (RotError)、平移误差 (TransError)、相机运动一致性 (CamMC)。

### 4. 资源与算力
*   **推理效率**：在 NVIDIA **H20 GPU** 上构建 3D 支架需 **0.5 秒**；在更强大的 **H100 GPU** 上仅需 **0.1 秒**。
*   **训练细节**：
    *   3DGS 模型训练 80,000 次迭代，使用 AdamW 优化器。
    *   NVS 模型训练 40,000 次迭代，Batch Size 为 16。
*   **算力总量**：文中未明确给出总 GPU 小时数，但提到了使用了 H20 和 H100 进行测试。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从深度估计、360° 重建到最终场景生成的全流程对比。
*   **消融实验**：针对双 LoRA 策略、记忆条件、双向融合模块（BFM）均做了详细的消融分析。
*   **充分性评价**：实验设计非常充分。通过在不同风格（真实 vs 风格化）和不同环境（室内 vs 室外）下的测试，证明了模型的泛化能力。引入几何误差指标（如 CamMC）来量化 3D 一致性，比单纯的视觉指标更具说服力。

### 6. 主要结论与发现
*   **几何支架的重要性**：显式的 3D 几何支架能有效抑制长距离生成中的误差累积和尺度漂移。
*   **MVS 转化有效性**：将全景深度估计转化为多视角匹配问题，可以显著提升深度预测的准确度（在 Stanford2D3D 上 AbsRel 降低了 50% 以上）。
*   **双 LoRA 优于拼接**：相比于简单的通道拼接，双 LoRA 能够更好地处理高质量输入与粗糙几何先验之间的异构信息融合。

### 7. 优点
*   **任务分解合理**：将病态的单图生成问题分解为全景扩展、几何建模和细节合成，降低了学习难度。
*   **高效前馈**：无需针对每个场景进行优化，实现了秒级的 3D 支架生成。
*   **几何一致性强**：通过显式 3D 约束，解决了以往方法中常见的“穿墙”、物体扭曲和闭环不一致问题。

### 8. 不足与局限
*   **微小不一致性**：虽然整体几何稳定，但在极端视角下仍可能存在细微的视觉不一致。
*   **依赖后处理优化**：为了达到极致的几何完美，可能仍需要类似 CAT3D 的后重建处理。
*   **数据集限制**：目前的性能受限于训练数据的规模，未来需要更大规模的 3D 场景数据来进一步提升鲁棒性。

（完）
