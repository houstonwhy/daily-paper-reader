---
title: "FlowScene: Style-Consistent Indoor Scene Generation with Multimodal Graph Rectified Flow"
title_zh: FlowScene：基于多模态图修正流的风格一致室内场景生成
authors: "Zhifei Yang, Guangyao Zhai, Keyang Lu, YuYang Yin, Chao Zhang, Zhen Xiao, Jieyi Long, Nassir Navab, Yikai Wang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19598v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 基于多模态图的风格一致室内场景生成
tldr: 针对室内场景生成中真实感与控制力难以兼得的问题，本文提出FlowScene。该模型基于多模态图，采用三分支架构协同生成场景布局、物体形状和纹理。其核心是紧耦合的修正流（Rectified Flow）模型，通过在生成过程中交换物体信息实现跨图协作推理。FlowScene不仅实现了细粒度的物体级控制，还确保了场景整体在结构和外观上的风格一致性，显著提升了生成质量。
selection_source: fresh_fetch
motivation: 现有场景生成方法在物体级精细控制、场景风格一致性以及高保真纹理生成方面仍面临挑战。
method: 提出一种基于多模态图的三分支生成模型，利用紧耦合的修正流机制协同生成布局、形状和纹理。
result: 实验表明，FlowScene在生成真实感、风格一致性和人类偏好方面均优于现有的语言和图驱动基准模型。
conclusion: FlowScene通过多模态图与修正流的结合，为高保真且风格统一的室内场景生成提供了有效的解决方案。
---

## 摘要
场景生成具有广泛的工业应用，要求具备高真实感以及对几何和外观的精确控制。语言驱动的检索方法通过大型物体数据库构建合理的场景，但忽略了物体层面的控制，且往往无法保证场景层面的风格一致性。基于图的表述通过显式建模关系，提供了更高的物体可控性并有助于实现整体一致性，但现有方法难以生成高保真的纹理结果，从而限制了其实用性。我们提出了 FlowScene，这是一种以多模态图为条件的场景生成模型，采用三分支结构协同生成场景布局、物体形状和物体纹理。其核心是一个紧耦合的修正流（rectified flow）模型，在生成过程中交换物体信息，从而实现跨图的协同推理。这实现了对物体形状、纹理和关系的细粒度控制，同时在结构和外观上强制执行场景层面的风格一致性。大量实验表明，FlowScene 在生成真实感、风格一致性以及与人类偏好的一致性方面，均优于基于语言条件和基于图条件的基准模型。

## Abstract
Scene generation has extensive industrial applications, demanding both high realism and precise control over geometry and appearance. Language-driven retrieval methods compose plausible scenes from a large object database, but overlook object-level control and often fail to enforce scene-level style coherence. Graph-based formulations offer higher controllability over objects and inform holistic consistency by explicitly modeling relations, yet existing methods struggle to produce high-fidelity textured results, thereby limiting their practical utility. We present FlowScene, a tri-branch scene generative model conditioned on multimodal graphs that collaboratively generates scene layouts, object shapes, and object textures. At its core lies a tight-coupled rectified flow model that exchanges object information during generation, enabling collaborative reasoning across the graph. This enables fine-grained control of objects' shapes, textures, and relations while enforcing scene-level style coherence across structure and appearance. Extensive experiments show that FlowScene outperforms both language-conditioned and graph-conditioned baselines in terms of generation realism, style consistency, and alignment with human preferences.

---

## 论文详细总结（自动生成）

### 论文总结：FlowScene —— 基于多模态图修正流的风格一致室内场景生成

#### 1. 论文的核心问题与整体含义（研究动机和背景）
室内场景生成在工业设计、VR/AR及机器人领域有广泛应用。现有方法主要面临三大挑战：
*   **控制力与一致性的矛盾**：语言驱动方法（如检索式）缺乏对单个物体的精细控制，且难以维持场景整体的风格一致性（如家具配套）；
*   **高保真纹理缺失**：现有的基于图（Graph-based）的方法多侧重于布局和形状生成，难以端到端地产生高质量纹理；
*   **输入多样性不足**：难以同时处理文本描述、参考图像及GUI交互等多种模态的约束。
**FlowScene** 的研究动机在于通过多模态图表示和改进的生成算法，同时实现高保真度、细粒度控制和跨物体的风格一致性。

#### 2. 论文提出的方法论
FlowScene 提出了一个基于**多模态图修正流（Multimodal Graph Rectified Flow, MGRF）**的三分支生成框架：
*   **核心思想**：利用修正流（Rectified Flow）的直线轨迹特性实现快速且稳定的生成，并通过“信息交换单元”在生成过程中让图中各节点（物体）相互通信，确保风格和空间的协同。
*   **关键技术细节**：
    *   **多模态图 (GM)**：节点可包含类别标签、文本特征（CLIP）和视觉特征（DINOv2），边表示空间或语义关系（如“左侧”、“同风格”）。
    *   **三分支架构**：
        1.  **布局分支 (Layout Branch)**：生成物体的3D包围盒（位置、大小、旋转）。
        2.  **形状分支 (Shape Branch)**：利用VQ-VAE将物体体素化并编码为离散潜码，生成物体的几何结构。
        3.  **纹理分支 (Texture Branch)**：在生成的几何基础上，通过结构化潜码生成高保真纹理。
    *   **信息交换单元 (InfoExchangeUnit)**：基于 5 层 Triplet-GCN，在修正流的每一个去噪步中，根据图结构交换节点信息，强制执行全局约束。
*   **算法流程**：训练阶段通过最小化预测速度与直线目标速度之间的二阶损失；推理阶段从高斯噪声出发，通过 ODE 求解器（如 Euler 法）在少量步数（25步）内完成生成。

#### 3. 实验设计
*   **数据集**：主要使用 **3D-FRONT** 及其增强版 **SG-FRONT**（包含约4.5万个物体实例和15类关系）。
*   **对比基准 (Benchmarks)**：
    *   **训练免除/语言驱动类**：Holodeck, LayoutVLM。
    *   **图驱动生成类**：CommonScenes, EchoScene, MMGDreamer。
*   **评估指标**：
    *   **真实感**：FID, FID-CLIP, KID（场景级）；MMD, COV, 1-NNA（物体级）。
    *   **可控性与一致性**：CLIPScore, FPVScore（第一人称视角评分），以及图约束满足率。
    *   **人类偏好**：开展了包含25名参与者的主观感知研究（PA, LC, VQ, SC, OP五项指标）。

#### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA A100 (80GB)** GPU 上完成。
*   **训练细节**：采用 AdamW 优化器，初始学习率 1e-4，Batch Size 为 196。
*   **效率**：推理速度显著优于扩散模型，生成布局和形状仅需 6.83 秒，包含纹理的全流程需 37.38 秒（比 MMGDreamer 快约 85%）。

#### 5. 实验数量与充分性
论文进行了较为充分的实验验证：
*   **定量对比**：在三个房间类型（卧室、客厅、餐厅）上分别进行了场景级和物体级的指标评测。
*   **消融实验**：验证了信息交换单元（LEU/SEU/TEU）的必要性、不同生成骨干（扩散 vs 修正流）的效果，以及不同模态输入比例的影响。
*   **鲁棒性测试**：测试了在关系掩码（缺失关系）情况下的生成稳定性。
*   **主观评价**：通过人类感知研究补充了自动指标在审美和风格判断上的不足。实验设计客观、对比公平（在相同协议下训练）。

#### 6. 论文的主要结论与发现
*   **性能领先**：FlowScene 在所有核心指标（FID, KID, FPVScore）上均优于现有基准，尤其在风格一致性上表现突出。
*   **效率提升**：修正流机制使得生成速度大幅提升，且在更少的采样步数下保持了更高的质量。
*   **协同效应**：通过在生成过程中交换信息，模型能够根据参考图像自动推断出图中其他“仅文本”节点的视觉风格，实现了真正的场景级风格统一。

#### 7. 优点
*   **端到端纹理生成**：填补了图驱动场景生成中高保真纹理生成的空白。
*   **多模态灵活性**：支持文本、图像、GUI等多种混合输入，适应实际应用场景。
*   **风格一致性强**：通过图卷积网络在流模型中的紧耦合设计，有效解决了物体间“各长各的”问题。
*   **推理高效**：相比传统的扩散模型，修正流提供了更快的收敛和推理速度。

#### 8. 不足与局限
*   **数据集限制**：目前仅在合成数据集（3D-FRONT）上验证，尚未在复杂的室外场景或真实扫描场景中测试。
*   **上游依赖**：生成质量高度依赖于多模态图的构建质量，若 LLM/VLM 解析出的图结构有误，会直接导致生成失败。
*   **物理冲突风险**：在缺乏空间关系约束的情况下，仍可能出现物体穿插（Interpenetration）现象。
*   **视角偏差**：若输入的参考图像视角受限，生成的物体形状可能存在局部缺陷。

（完）
