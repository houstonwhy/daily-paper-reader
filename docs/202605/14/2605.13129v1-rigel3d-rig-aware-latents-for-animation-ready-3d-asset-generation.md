---
title: "Rigel3D: Rig-aware Latents for Animation-Ready 3D Asset Generation"
title_zh: Rigel3D：面向动画就绪 3D 资产生成的绑定感知潜变量
authors: "Nikitas Chatzis, Marios Loizou, Evangelos Kalogerakis"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13129v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 动画就绪3D资产的生成方法
tldr: Rigel3D 针对现有 3D 生成模型仅能生成静态资产、缺乏动画绑定（Rigging）的问题，提出了一种生成可动画化 3D 资产的新方法。该方法通过耦合表面与骨架的结构化潜空间表示，联合建模几何与绑定结构。利用潜空间生成模型和开放词汇关节标注模块，Rigel3D 能够生成具备完整骨架、关节和蒙皮权重的 3D 模型，支持下游动画工作流，在生成质量和动画可用性上均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的 3D 生成模型通常只能生成静态模型，缺乏游戏和影视等领域所需的骨骼绑定和蒙皮权重，限制了其在动态场景中的应用。
method: 提出一种基于耦合表面与骨架潜表示的生成框架，通过潜空间自编码器和两阶段生成模型，同步合成 3D 几何、骨架拓扑及蒙皮权重。
result: 在大规模绑定资产数据集上的实验表明，该方法能生成高质量且多样化的可动画资产，在多项指标上显著超越了现有的自动绑定基准。
conclusion: Rigel3D 实现了从图像到可动画 3D 资产的高效生成，并通过开放词汇标注增强了与现有动画模板的兼容性，为 3D 内容创作提供了有力工具。
---

## 摘要
最近的 3D 生成模型能够合成高质量资产，但其输出通常是静态的：缺乏动画所需的骨骼绑定、关节层级和蒙皮权重。这限制了它们在游戏、电影、模拟、虚拟代理和具身智能中的应用，在这些领域中，资产不仅需要看起来真实，还需要运动得真实。我们提出了 Rigel3D，这是一种用于生成以绑定网格表示的动画就绪 3D 资产的方法。与在完成的形状上附加骨架的后验自动绑定方法不同，我们的方法通过耦合的表面和骨架结构化潜表示，对几何形状和绑定结构进行联合建模。绑定感知自编码器将这些表示解码为网格几何、骨架拓扑、关节坐标和蒙皮权重，而两阶段潜生成模型则合成表面和骨架表示，用于图像条件生成。为了支持下游动画工作流，我们进一步引入了一个开放词汇关节标注模块，将生成的关节嵌入到共享的视觉-语言空间中，从而实现与任意重定向模板的对应。在大规模绑定资产数据集上的实验表明，我们的方法能够生成多样化、高质量的动画就绪资产，并在多项指标上优于现有的绑定基准方法。

## Abstract
Recent 3D generative models can synthesize high-quality assets, but their outputs are typically static: they lack the skeletal rigs, joint hierarchies, and skinning weights required for animation. This limits their use in games, film, simulation, virtual agents, and embodied AI, where assets must not only look plausible but also move plausibly. We introduce Rigel3D, a generative method for animation-ready 3D assets represented as rigged meshes. Unlike post-hoc auto-rigging methods that attach rigs to completed shapes, our method jointly models geometry and rig structure through coupled surface and skeleton structured latent representations. A rig-aware autoencoder decodes these representations into mesh geometry, skeleton topology, joint coordinates, and skinning weights, while a two-stage latent generative model synthesizes both surface and skeleton representations for image-conditioned generation. To support downstream animation workflows, we further introduce an open-vocabulary joint labeling module that embeds generated joints into a shared vision-language space, enabling correspondence to arbitrary retargeting templates. Experiments on large-scale rigged asset datasets demonstrate that our method generates diverse, high-quality animation-ready assets and outperforms existing rigging baselines across multiple metrics.

---

## 论文详细总结（自动生成）

### 论文总结：Rigel3D - 面向动画就绪 3D 资产生成的绑定感知潜变量

#### 1. 核心问题与整体含义（研究动机和背景）
现有的 3D 生成模型（如 TRELLIS、Gaussian Splatting 等）虽然能生成高质量的静态几何形状，但生成的资产通常缺乏**骨骼绑定（Rigging）**、**关节层级**和**蒙皮权重（Skinning Weights）**。这意味着生成的模型无法直接用于游戏、影视或具身智能等需要动态交互的场景，必须经过繁琐的后期人工或自动绑定处理。
**Rigel3D 的核心动机**是改变“先生成形状、后进行绑定”的传统后验模式，提出一种**联合建模几何与绑定结构**的生成框架，直接输出“动画就绪”（Animation-Ready）的 3D 资产。

#### 2. 方法论
Rigel3D 基于结构化潜变量（SLats）表示法，通过以下关键技术实现：
*   **耦合的结构化潜表示（Coupled SLats）**：将 3D 资产分解为两种潜变量：
    *   **表面 SLat（Surface SLat）**：捕捉几何形状和外观。
    *   **骨架 SLat（Skeleton SLat）**：捕捉内部关节和骨骼结构。
*   **绑定感知自编码器（Rig-aware Autoencoder）**：
    *   **编码端**：分别提取表面特征和基于蒙皮权重加权的骨骼特征，映射到稀疏体素网格中。
    *   **解码端**：包含三个子模块：1) 骨架感知的网格解码器（利用 FlexiCubes 提取网格）；2) 自回归骨架解码器（按广度优先搜索顺序生成关节坐标和父子关系）；3) 基于注意力机制的蒙皮权重解码器。
*   **两阶段生成模型**：使用整流流变换器（Rectified Flow Transformers）分阶段生成体素占用率（Occupancy）和具体的潜变量编码。
*   **开放词汇关节标注（Open-vocabulary Labeling）**：引入一个基于 CLIP 的标注模块，将生成的关节嵌入视觉-语言空间。这使得生成的骨架可以根据文本标签（如“左手”、“脊柱”）与任意动画模板进行匹配和重定向。

#### 3. 实验设计
*   **数据集**：
    *   **Anymate**：目前最大的公开绑定资产数据集（约 22.5 万个模型），用于训练和主要评估。
    *   **ModelsResource**：包含 270 个多样化资产的测试集，用于验证泛化能力。
    *   **ArticulationXL**：用于与同期工作对比。
*   **基准方法（Benchmarks）**：
    *   **自动绑定基准**：Anymate (2025)、Puppeteer (2025a)。
    *   **生成模型基准**：AniGen (2026，同期工作)、TRELLIS（用于几何质量对比）。
*   **评估指标**：
    *   **骨架精度**：J2J（关节到关节距离）、J2B（关节到骨骼距离）、B2B（骨骼到骨骼距离）。
    *   **蒙皮精度**：L1、L2 距离及 KL 散度。
    *   **几何质量**：PSNR、LPIPS、Chamfer Distance、F-score 等。

#### 4. 资源与算力
论文详细列出了算力消耗：
*   **自编码器微调**：使用 4 张 **NVIDIA RTX 6000 Ada (48GB)**，训练约 1 天。
*   **联合训练（骨架/蒙皮/解码器）**：使用相同硬件，训练约 **10 天**。
*   **潜变量生成模型（Flow Models）**：使用 8 张 **NVIDIA A5000 (24GB)**，每个模型训练约 2 天（共 4 个模型）。
*   **标注模块**：使用 4 张 A5000，训练约 3 天。

#### 5. 实验数量与充分性
*   **实验规模**：在超过 22 万个样本的大规模数据集上训练，并在多个独立测试集上验证，实验规模属于同类研究的顶尖水平。
*   **消融实验**：针对潜变量耦合方式（单网格 vs 双网格）、骨架生成策略、标注模块的遍历顺序（BFS vs DFS 等）进行了详细的消融实验。
*   **客观性**：对比了最新的同期工作（AniGen），并使用了标准化的自动绑定评估协议，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **联合建模的优越性**：相比于先生成网格再绑定的后验方法，联合生成几何与骨架能显著提升骨架的解剖学合理性和蒙皮权重的准确度。
*   **骨架 SLat 的必要性**：引入专门的骨架潜变量比单纯增加几何特征更能有效捕捉复杂的内部关节层级。
*   **开放词汇标注的实用性**：通过 CLIP 嵌入，生成的资产无需重新训练即可适配不同的动画重定向模板，极大地增强了下游兼容性。

#### 7. 优点
*   **端到端生成**：首个实现从单图到完整“网格+骨架+蒙皮+标签”全流程生成的框架。
*   **结构化创新**：通过双潜空间设计解决了表面几何与内部稀疏骨架在特征表示上的异质性问题。
*   **高度兼容性**：开放词汇标注解决了生成骨架与工业标准命名不统一的痛点。

#### 8. 不足与局限
*   **拓扑错误风险**：生成的骨架偶尔会出现多余的分支、断裂或关节缺失，尤其是在结构极其复杂的非生物物体上。
*   **极端姿态变形**：由于蒙皮权重主要基于静态几何学习，在极端动作下可能会出现网格穿插或不自然的拉伸。
*   **遮挡敏感性**：作为图像条件生成模型，当输入图像存在严重遮挡或视角单一时，背面或内部结构的生成质量会下降。

（完）
