---
title: "OmniLiDAR: A Unified Diffusion Framework for Multi-Domain 3D LiDAR Generation"
title_zh: OmniLiDAR：一种用于多域 3D LiDAR 生成的统一扩散框架
authors: "Youquan Liu, Weidong Yang, Ao Liang, Xiang Xu, Lingdong Kong, Yang Wu, Dekai Zhu, Xin Li, Runnan Chen, Ben Fei, Tongliang Liu, Wanli Ouyang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13815v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 用于3D LiDAR场景生成的文本条件扩散框架
tldr: OmniLiDAR是一个统一的文本条件扩散框架，旨在解决多领域3D LiDAR数据生成的难题。它通过共享的距离图像表示，涵盖了恶劣天气、传感器配置变化及跨平台采集等8个领域。该研究引入了跨领域训练策略、特征建模和自适应缩放技术，并构建了大规模多领域数据集。实验证明，该框架在生成保真度、下游感知任务增强及鲁棒性评估方面均表现出色。
selection_source: fresh_fetch
motivation: 现有的LiDAR生成模型通常局限于单一领域，难以在异构分布偏移下实现统一且可控的合成。
method: 提出了包含跨领域训练策略（CDTS）、各向异性特征建模（CDFM）和领域自适应特征缩放（DAFS）的统一扩散框架。
result: 在8个领域的综合基准测试中，该方法显著提升了生成质量，并有效增强了语义分割和目标检测等下游任务的性能。
conclusion: OmniLiDAR为多领域LiDAR数据生成提供了高效的统一方案，展示了生成式数据增强在提升自动驾驶感知系统鲁棒性方面的巨大潜力。
---

## 摘要
LiDAR 场景生成对于可扩展仿真和合成数据创建日益重要，特别是在大规模采集成本高昂的多样化传感条件下。通常，基于扩散的 LiDAR 生成器是在单域设置下开发的，需要针对不同的数据集或传感条件使用单独的模型，这阻碍了在异构分布偏移下的统一、可控合成。为此，我们提出了 OmniLiDAR，这是一个统一的文本条件扩散框架，它在共享的距离图像（range-image）表示中生成跨越八个代表性域的 LiDAR 扫描，涵盖三种偏移类型：恶劣天气、传感器配置变化（例如线束减少）以及跨平台采集（车辆、无人机和四足机器人）。为了在异构域上训练单个模型而不按域隔离优化，我们引入了跨域训练策略（CDTS），该策略在每个 mini-batch 中混合不同域，并利用条件机制引导生成。我们进一步提出了跨域特征建模（CDFM），它捕获沿方位角和俯仰角轴的方向依赖性，以反映距离图像的各向异性扫描结构；以及域自适应特征缩放（DAFS），作为一种轻量级调制，用于处理去噪过程中结构化的域相关特征偏移。在缺乏公开整合基准的情况下，我们通过将真实世界扫描与基于物理的天气模拟和系统性线束减少相结合，构建了一个包含 8 个域的数据集，并遵循官方划分。大量实验证明了强大的生成保真度以及在下游用例中的持续增益，包括用于 LiDAR 语义分割和 3D 目标检测的生成式数据增强，以及在损坏情况下的鲁棒性评估，且在有限标签场景中具有一致的收益。

## Abstract
LiDAR scene generation is increasingly important for scalable simulation and synthetic data creation, especially under diverse sensing conditions that are costly to capture at scale. Typically, diffusion-based LiDAR generators are developed under single-domain settings, requiring separate models for different datasets or sensing conditions and hindering unified, controllable synthesis under heterogeneous distribution shifts. To this end, we present OmniLiDAR, a unified text-conditioned diffusion framework that generates LiDAR scans in a shared range-image representation across eight representative domains spanning three shift types: adverse weather, sensor-configuration changes (e.g., reduced beams), and cross-platform acquisition (vehicle, drone, and quadruped). To enable training a single model over heterogeneous domains without isolating optimization by domain, we introduce a Cross-Domain Training Strategy (CDTS) that mixes domains within each mini-batch and leverages conditioning to steer generation. We further propose Cross-Domain Feature Modeling (CDFM), which captures directional dependencies along azimuth and elevation axes to reflect the anisotropic scanning structure of range images, and Domain-Adaptive Feature Scaling (DAFS) as a lightweight modulation to account for structured domain-dependent feature shifts during denoising. In the absence of a public consolidated benchmark, we construct an 8-domain dataset by combining real-world scans with physically based weather simulation and systematic beam reduction while following official splits. Extensive experiments demonstrate strong generation fidelity and consistent gains in downstream use cases, including generative data augmentation for LiDAR semantic segmentation and 3D object detection, as well as robustness evaluation under corruptions, with consistent benefits in limited-label regimes.

---

## 论文详细总结（自动生成）

以下是对论文《OmniLiDAR: A Unified Diffusion Framework for Multi-Domain 3D LiDAR Generation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：LiDAR数据在实际应用中存在显著的“分布偏移”，主要由恶劣天气（雨、雪、雾）、传感器配置（线束多少）和采集平台（车载、无人机、四足机器人）引起。
*   **核心挑战**：现有的基于扩散模型的LiDAR生成器通常针对单一领域设计，难以在统一框架下处理异构的分布变化。为不同领域训练独立模型不仅效率低下，也无法利用跨领域的几何先验。
*   **整体含义**：本文提出了 **OmniLiDAR**，这是首个统一的文本条件扩散框架，能够通过单个模型在八个代表性领域（涵盖天气、传感器、平台偏移）中生成高质量、可控的3D LiDAR扫描。

### 2. 方法论
*   **核心思想**：利用共享的距离图像（Range-image）表示，结合文本提示词（Text Prompts）作为领域描述符，通过改进的扩散架构实现跨域生成。
*   **关键技术细节**：
    *   **跨域训练策略 (CDTS)**：在每个mini-batch中混合来自不同领域的样本，而非传统的单域训练，以增强跨域交互并稳定优化过程。
    *   **跨域特征建模 (CDFM)**：针对距离图像的各向异性结构，利用 **Mamba** 架构沿方位角（水平）和俯仰角（垂直）轴进行方向性序列建模，捕获长程几何依赖。
    *   **域自适应特征缩放 (DAFS)**：一种轻量级的调制模块，通过学习领域特定的增益（Gain）和偏置（Bias）来校准去噪过程中的特征统计分布。
    *   **文本条件机制**：使用预训练的 CLIP 文本编码器处理领域描述符（如“雪天驾驶场景”），通过交叉注意力机制引导生成过程。

### 3. 实验设计
*   **数据集与场景**：构建了一个包含 **8个领域** 的综合基准：
    *   **清洁平台**：车载（SemanticKITTI）、无人机、四足机器人（Pi3DET）。
    *   **模拟天气**：雾、雪、湿地（基于Robo3D物理模拟）、雨（基于LISA模拟）。
    *   **传感器变化**：系统性下采样生成的32线束数据。
*   **对比方法**：包括 LiDARGen, LiDM, R2DM, Text2LiDAR, WeatherGen 等最先进的扩散生成模型。
*   **下游任务评估**：通过生成式数据增强（GDA）评估其在语义分割（MinkUNet, SPVCNN）和3D目标检测（PVRCNN, VoxelRCNN）上的增益。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA H100 GPU** 进行训练。
*   **训练细节**：Batch size 为 16，总训练迭代次数为 **500k次**。
*   **推理效率**：尽管模型参数量较大（约97.7M），但在 H100 上的推理延迟（约3.52s/帧）与现有方法相当，具有实际应用潜力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在8个不同领域上进行了生成质量的定量对比（FRD, JSD, MMD等指标）。
    *   针对语义分割，测试了1%、10%、20%等不同标签比例下的增强效果。
    *   针对目标检测，在无人机和四足机器人平台上验证了跨平台泛化性。
    *   进行了详细的消融实验（CDTS, CDFM, DAFS的有效性）。
*   **充分性评价**：实验设计非常充分，涵盖了从生成质量到下游感知的全方位评估。通过在真实世界恶劣天气数据集（SemanticSTF）上的验证，证明了其模拟数据的有效性，实验过程客观且对比公平。

### 6. 主要结论与发现
*   **统一模型的优越性**：单个 OmniLiDAR 模型在多个领域上的表现优于或等同于针对每个领域单独训练的专用模型。
*   **数据增强价值**：在**有限标签（Limited-label）**场景下，OmniLiDAR 生成的数据能显著提升分割和检测模型的性能（例如在1%标签下，分割性能提升近20% mIoU）。
*   **鲁棒性提升**：通过生成特定损坏类型的样本进行训练，可以显著增强感知模型在面对现实世界传感器损坏时的鲁棒性。

### 7. 优点（亮点）
*   **通用性强**：打破了LiDAR生成模型“一域一模型”的限制，实现了真正的多域统一。
*   **几何感知建模**：引入 Mamba 处理距离图像的扫描特性，比纯卷积或全局注意力更符合 LiDAR 的物理采集逻辑。
*   **即插即用的增强**：生成的样本带有高质量的伪标签，能直接用于提升现有感知算法的性能。

### 8. 不足与局限
*   **分辨率限制**：目前统一采用 64×1024 的距离图像分辨率，对于更高线束（如128线）或非旋转式 LiDAR 的适配性尚未深入探讨。
*   **文本控制粒度**：文本提示目前主要作为领域标签使用，尚未实现像图像生成领域那样细粒度的物体级语义控制（如“左边有一辆红色卡车”）。
*   **模拟与真实的鸿沟**：虽然在多个数据集上表现良好，但物理模拟生成的天气效果与极端复杂的现实环境仍存在一定差异。

（完）
