---
title: "HetScene: Heterogeneity-Aware Diffusion for Dense Indoor Scene Generation"
title_zh: HetScene：用于密集室内场景生成的异质性感知扩散模型
authors: "Zini Chen, Junming Huang, Rong Zhang, Jiamin Xu, Cheng Peng, Chi Wang, Weiwei Xu"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13586v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 使用扩散模型生成稠密室内场景
tldr: 针对具身智能仿真中密集室内场景生成难的问题，本文提出 HetScene 框架。该方法通过识别物体间的结构异构性，将生成过程解耦为结构布局生成（SLG）和上下文布局生成（CLG）两个阶段。SLG 首先基于文本和空间约束生成主要家具的宏观骨架，随后 CLG 填充次要物体。这种异构感知的方法有效解决了传统同质化生成在处理复杂空间依赖时的局限性，显著提升了场景的物理合理性与可扩展性。
selection_source: fresh_fetch
motivation: 传统方法将所有物体视为同质实例，难以处理密集室内场景中复杂的空间依赖和物理合理性要求。
method: 提出一种异构两阶段生成框架，将物体分为主要和次要两类，分别进行结构布局生成和上下文布局生成。
result: 成功生成了符合文本描述、具有全局一致性且物理合理的密集高保真室内布局。
conclusion: 通过对场景结构的异构分解与解耦生成，HetScene 显著增强了复杂室内环境合成的可控性和真实感。
---

## 摘要
生成可控且符合物理规律的室内场景是构建具身智能（Embodied AI）高保真模拟环境的关键前提。然而，现有的基于深度学习的方法通常在统一的生成过程中将所有物体视为同质实例。虽然这些方法在处理稀疏且简单的布局时有效，但在建模具有密集物体排列和复杂空间依赖关系的真实布局时却面临困难，导致可扩展性受限且物理合理性下降。为了应对这些挑战，我们从结构异质性的角度重新审视室内布局生成，并根据物体在塑造场景中的不同作用，将其分解为主要物体和次要物体。基于这种分解，我们提出了 HetScene，这是一个异质的两阶段生成框架，将室内布局合成解耦为结构布局生成（SLG）和上下文布局生成（CLG）。SLG 首先根据文本描述、自顶向下的二值房间掩码和空间关系图，生成仅包含主要物体的全局一致结构布局，从而建立大型核心家具的稳定全局宏观骨架。

## Abstract
Generating controllable and physically plausible indoor scenes is a pivotal prerequisite for constructing high-fidelity simulation environments for embodied AI. However, existing deeplearning-based methods usually treat all objects as homogeneous instances within a unified generation process. While effective for sparse and simplistic layouts, they struggle to model realistic layouts with dense object arrangements and complex spatial dependencies, leadingto limited scalability and degraded physical plausibility. To deal with these challenges, we revisit indoor layout generation from the perspective of structural heterogeneity and decompose the objects into primary objects and secondary objects according to their distinct roles in shaping a scene. Based on this decomposition, we propose HetScene, a heterogeneous two-stage generation framework that decouples indoor layout synthesis into Structural Layout Generation (SLG) and Contextual Layout Generation (CLG). SLG first generates globally coherent structural layouts with only primary objects conditioned on text descriptions, top-down binary room masks, and spatial relation graphs, establishing a stable global macro-skeleton of large core furniture.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HetScene** 的异质性感知扩散模型框架，旨在解决高保真、高密度室内场景生成的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的深度学习室内布局生成方法通常将所有物体视为“同质实例”（Homogeneous），即在统一的生成过程中处理所有家具和摆件。
*   **研究动机**：
    *   **建模失配**：在真实场景中，大型家具（主要物体，如床、沙发）和小型摆件（次要物体，如台灯、枕头）遵循完全不同的空间分布规律。主要物体受全局房间约束，而次要物体则强烈依赖于主要物体的物理支撑和语义共现。
    *   **扩展性差**：同质化建模在处理物体密集、空间依赖复杂的场景时，容易出现物体缺失、悬空、重叠等物理不合理现象，且难以扩展到包含数百个物体的复杂环境。

### 2. 方法论
HetScene 提出了一个**异质两阶段生成框架**，将生成过程解耦：
*   **核心思想**：将室内布局合成分解为“结构布局生成（SLG）”和“上下文布局生成（CLG）”。
*   **关键技术细节**：
    *   **结构布局生成 (SLG)**：生成主要物体的宏观骨架。输入包括文本描述、二值房间掩码和**可学习场景图 (LSG)**。LSG 通过编码物体间的显式拓扑关系（如“靠墙”、“在...旁边”），为全局布局提供强约束。
    *   **上下文布局生成 (CLG)**：以 SLG 生成的主要布局为空间锚点，通过局部条件生成网络合成次要物体。CLG 采用自注意力机制，让次要物体 token 聚合主要物体的空间信息。
    *   **可学习空间-语义调制机制**：引入一个可学习标量 $\gamma_{pos}$ 来调节几何属性（位置、大小）与离散语义嵌入之间的比例。这防止了数值较大的几何信号淹没语义信号，是密集场景下模型收敛的关键。
    *   **Transformer 骨干网络**：弃用了传统的 1D-UNet，改用基于 Transformer 的去噪网络，并结合 AdaLN 注入扩散步长信息，以更好地捕捉长程依赖。

### 3. 实验设计
*   **数据集**：使用大规模多源 3D 室内布局基准数据集 **M3DLayout**，该数据集整合了 3D-FRONT、Matterport3D (MP3D) 和 Inf3DLayout。
*   **Benchmark 规模**：包含 101 个语义类别（64 类主要物体，37 类次要物体）。筛选后包含 11,508 个训练场景和 1,496 个验证场景。
*   **对比方法**：
    *   **ATISS**：基于 Transformer 的自回归模型。
    *   **DiffuScene**：基于 U-Net 的扩散模型。
    *   **MiDiffusion**：混合离散-连续扩散模型。
*   **评估指标**：FID、KID（针对 XZ、XY、YZ 三个投影面）以及 CLIP 分数（衡量文本一致性）。

### 4. 资源与算力
*   **硬件设备**：使用了两台服务器，分别配备了 **2 张 NVIDIA GeForce RTX 4090** 和 **4 张 NVIDIA A800 GPU**。
*   **训练细节**：
    *   初始学习率 $10^{-4}$，每 10,000 epoch 减半。
    *   总训练时长为 **30,000 epoch**。
    *   Batch size 为 64。
    *   扩散步数为 1,000 步。

### 5. 实验数量与充分性
*   **实验规模**：论文在全测试集以及三个子数据集（Inf3DLayout, MP3D, 3DFRONT）上分别进行了定量评估。
*   **消融实验**：设计了四组消融实验，逐步验证了 Transformer 骨干网络、异质生成流水线（HP）和可学习场景图条件（LC）的有效性。
*   **充分性评价**：实验设计较为充分，涵盖了从合成数据到真实扫描数据的跨度，且通过多视角投影指标（XZ/XY/YZ）客观地评价了三维空间的合理性，对比实验具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：HetScene 在所有指标上均优于现有 SOTA 方法，尤其在 XZ 平面（平面布局）的 FID 指标上比次优模型提升了约 11.7%。
*   **解耦的必要性**：通过将全局规划与局部支撑感知放置解耦，模型能更稳定地生成高密度场景（支持多达 100 个次要物体），有效缓解了小物体遗漏和重叠问题。
*   **调制机制的作用**：实验证明，如果没有空间-语义调制机制，模型在极高密度的场景下难以收敛。

### 7. 优点
*   **结构创新**：通过异质性感知（主要 vs 次要）重构了生成管线，符合室内设计的层级逻辑。
*   **即插即用**：CLG 阶段可以作为一个独立组件，兼容任何上游生成的粗略布局。
*   **物理合理性强**：引入场景图约束和两阶段细化，显著减少了家具“漂浮”或“穿模”的现象。

### 8. 不足与局限
*   **真实数据挑战**：在 MP3D（真实扫描数据）上的表现提升不如在合成数据上明显，这反映出模型对真实世界噪声和不规则房间形状的泛化能力仍有提升空间。
*   **计算开销**：两阶段生成虽然提升了质量，但相比单阶段模型，推理时的计算步骤和时间成本会有所增加。
*   **类别依赖**：模型性能高度依赖于预定义的“主要/次要”类别划分，对于某些界限模糊的物体可能存在分类偏差。

（完）
