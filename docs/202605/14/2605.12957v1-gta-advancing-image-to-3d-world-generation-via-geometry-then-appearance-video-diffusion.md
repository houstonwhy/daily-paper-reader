---
title: "GTA: Advancing Image-to-3D World Generation via Geometry Then Appearance Video Diffusion"
title_zh: GTA：通过“先几何后外观”视频扩散模型推进图像到三维世界生成
authors: "Hanxin Zhu, Cong Wang, Peiyan Tu, Jiayi Luo, Tianyu He, Xin Jin, Zhibo Chen"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.12957v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 图像到3D世界的生成与合成
tldr: 本研究针对现有3D世界生成方法中几何建模不足导致的结构不可靠和跨视角不一致问题，提出了GTA框架。该框架遵循“先几何后外观”的范式，利用两阶段视频扩散模型，先从单张图像生成粗略几何结构，再据此合成精细外观。通过引入随机潜变量打乱和测试时缩放策略，GTA在保真度、视觉质量和几何准确性上显著优于现有方法，并可作为通用增强模块提升其他管线的生成质量。
selection_source: fresh_fetch
motivation: 现有3D生成方法过度关注外观而忽视几何建模，导致场景结构不稳定且跨视角一致性较差。
method: 采用两阶段视频扩散模型，先预测新视角的粗略几何结构，再以几何为条件合成精细的外观细节。
result: 实验证明GTA在保真度、视觉质量和几何精度上均超越了现有技术，并展现出极高的训练效率和通用性。
conclusion: GTA通过“先几何后外观”的策略，为高质量、高一致性的3D世界生成提供了一种高效且通用的新方案。
---

## 摘要
生成模型和大规模数据集的最新进展显著推动了三维世界生成的发展，促进了包括空间智能、具身智能和自动驾驶在内的广泛领域。尽管取得了显著进展，现有的三维世界生成方法通常优先考虑外观预测，而对底层几何的建模有限，导致场景结构估计不可靠以及跨视图一致性下降等问题。为了解决这些局限性，受人类视觉感知由粗到精特性的启发，我们提出了 GTA，一种遵循“先几何后外观”范式的创新图像到三维世界生成方法。具体而言，给定单张输入图像，为了提高合成三维场景的结构保真度，GTA 采用了一个包含两个专用视频扩散模型的两阶段框架，首先从新视角生成粗糙的几何结构，然后以预测的几何为条件合成细粒度的外观。为了进一步增强跨视图外观一致性，我们在训练过程中引入了随机潜变量打乱策略，并结合测试时缩放方案，在不损害定量性能的情况下提高了感知质量。大量实验表明，我们提出的方法在保真度、视觉质量和几何精度方面始终优于现有方法。此外，GTA 被证明可以作为一个通用的增强模块，进一步提升现有图像到三维世界流水线的生成质量，同时支持多种下游应用，并在模型训练期间表现出良好的数据效率，突显了其多功能性和广泛的适用性。项目页面：https://hanxinzhu-lab.github.io/GTA/。

## Abstract
Recent developments in generative models and large-scale datasets have substantially advanced 3D world generation, facilitating a broad range of domains including spatial intelligence, embodied intelligence, and autonomous driving. While achieving remarkable progress, existing approaches to 3D world generation typically prioritize appearance prediction with limited modeling of the underlying geometry, leading to issues such as unreliable scene structure estimation and degraded cross-view consistency. To address these limitations, motivated by the coarse-to-fine nature of human visual perception, we propose GTA, a novel image-to-3D world generation method following a Geometry-Then-Appearance paradigm. Specifically, given a single input image, to improve the structural fidelity of synthesized 3D scenes, GTA adopts a two-stage framework with two dedicated video diffusion models, which first generate coarse geometric structure from novel viewpoints and then synthesize fine-grained appearance conditioned on the predicted geometry. To further enhance cross-view appearance consistency, we introduce a random latent shuffle strategy during the training process, along with a test-time scaling scheme that improves perceptual quality without compromising quantitative performance. Extensive experiments have demonstrated that our proposed method consistently outperforms existing approaches in terms of fidelity, visual quality, and geometric accuracy. Moreover, GTA is shown to be effective as a general enhancement module that further improves the generation quality of existing image-to-3D world pipelines, as well as supporting multiple downstream applications and exhibiting favorable data efficiency during model training, highlighting its versatility and broad applicability. Project page: https://hanxinzhu-lab.github.io/GTA/.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GTA**（Geometry Then Appearance）的新型图像到三维世界生成框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的三维世界生成方法（从单张图像生成可探索的三维场景）主要存在两个痛点：
    1.  **重外观轻几何**：大多方法偏向纹理合成，缺乏对底层几何结构的显式建模，导致场景结构不可靠且跨视角一致性差。
    2.  **联合预测干扰**：同时预测几何和外观会导致两种不同分布的模态相互干扰，产生几何畸变和画面闪烁。
*   **研究动机**：受人类视觉“由粗到精”（先感知全局结构，再处理表面细节）的启发，作者提出将几何生成与外观合成显式解耦。

### 2. 方法论：GTA 框架
GTA 采用两阶段视频扩散模型架构，将生成过程分为“几何预测”和“外观合成”：
*   **核心思想**：先构建一个全局一致的几何支架（深度图序列），再以此为约束填充细粒度的纹理。
*   **关键技术细节**：
    1.  **几何视频扩散（Diffusion Model-G）**：
        *   利用单目深度估计将输入图像提升为点云，并投影到目标视角生成初步的“空洞”观察。
        *   模型在潜空间（Latent Space）内补全并生成连续、一致的多视角深度视频。
    2.  **外观视频扩散（Diffusion Model-A）**：
        *   以第一阶段生成的深度视频为显式条件，通过残差融合和投影模块引导 RGB 纹理的合成。
    3.  **随机潜变量打乱（Random Latent Shuffle）**：
        *   **技术点**：在训练时以一定概率随机打乱视角序列的顺序。
        *   **目的**：打破视频模型固有的“时间顺序”偏差，强制模型学习空间一致性而非简单的帧间平滑，解决长距离视角下的颜色漂移问题。
    4.  **测试时缩放（Test-time Scaling）**：
        *   **流程**：采用迭代细化策略。先生成近处视角，将其作为可靠参考，通过“掩码扭曲”（Masked-warping）机制逐步引导远处视角的生成。

### 3. 实验设计
*   **数据集**：
    *   **DL3DV**：包含约 10k 个真实场景，用于训练（100k 序列）和评测（55 个场景）。
    *   **RealEstate10K**：作为完全独立的测试集，验证模型的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了 6 种 SOTA 方法：包括外观中心化的 **See3D, ViewCrafter, TrajectoryCrafter, FlexWorld, Gen3C** 以及联合预测的 **Voyager**。
*   **评价指标**：
    *   保真度：PSNR, SSIM, LPIPS。
    *   视觉质量：FID, Q-Align（人类感知对齐评分）。
    *   几何准确性：旋转误差（R-err）和平移误差（T-err）。

### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：几何模型和外观模型各训练了 **40,000 次迭代**。
*   **推理算力**：单张 NVIDIA A800 GPU，峰值显存占用约 **20 GB**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（两个数据集）、定性视觉对比、以及针对三个核心组件（GTA 范式、打乱策略、缩放策略）的消融实验。
*   **充分性评价**：实验设计非常充分。除了标准生成任务，作者还验证了 GTA 作为**通用增强模块**的效果（提升了 FlexWorld 和 Voyager 的表现），并展示了在 **3D 场景编辑**和**视频深度估计**等下游任务中的应用。对比实验涵盖了 U-Net 和 DiT 两种主流架构的方法，保证了对比的公平性。

### 6. 主要结论与发现
*   **解耦优于联合**：先生成几何再生成外观的策略显著降低了学习难度，生成的场景在结构稳定性和纹理一致性上均优于同步预测方法。
*   **打乱策略有效性**：随机打乱潜变量能有效抑制视角变换时的颜色褪色和纹理漂移。
*   **数据效率高**：GTA 在仅使用 10k 训练数据时即可达到其他方法使用更多数据时的效果，具有极强的扩展性。

### 7. 优点
*   **结构保真度高**：通过显式几何引导，解决了三维场景生成中常见的“墙壁扭曲”或“物体变形”问题。
*   **通用性强**：不仅是一个独立的生成器，还可以作为后处理插件提升现有算法的质量。
*   **即插即用**：测试时缩放策略无需额外训练即可提升大跨度视角的生成质量。

### 8. 不足与局限
*   **推理开销**：测试时缩放策略涉及多次迭代推理，虽然提升了质量，但会增加生成单段视频的时间成本。
*   **依赖初始深度**：第一阶段的几何生成仍部分依赖于初始单目深度估计的准确性，若初始深度存在极端错误，可能会影响后续合成。
*   **动态场景限制**：目前主要针对静态 3D 世界生成，尚未显式处理场景中的动态物体（如行人、车辆）。

（完）
