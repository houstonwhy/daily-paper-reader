---
title: "HERMES++: Toward a Unified Driving World Model for 3D Scene Understanding and Generation"
title_zh: HERMES++：迈向用于 3D 场景理解与生成的统一驾驶世界模型
authors: "Xin Zhou, Dingkang Liang, Xiwu Chen, Feiyang Tan, Dingyuan Zhang, Hengshuang Zhao, Xiang Bai"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28196v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 集成了3D场景理解与未来几何生成的统一模型
tldr: HERMES++ 是一款统一的驾驶世界模型，旨在解决现有模型在 3D 场景理解与未来几何预测之间的脱节问题。它通过 BEV 表示将多视图空间信息与大语言模型（LLM）结合，并引入 LLM 增强查询和时空链接机制，实现了语义理解与物理模拟的协同。该模型在 3D 场景理解和未来点云预测任务上均优于专门化模型，为自动驾驶提供了更全面的环境模拟能力。
selection_source: fresh_fetch
motivation: 现有的驾驶世界模型往往侧重于生成而忽视理解，且 LLM 缺乏预测未来几何演化的能力，导致语义解释与物理模拟之间存在断层。
method: 提出了一种集成 BEV 表示、LLM 增强世界查询、当前到未来的时空链接以及联合几何优化策略的统一框架。
result: 在多个基准测试中，HERMES++ 在未来点云预测和 3D 场景理解任务上的表现均超过了现有的专门化方法。
conclusion: HERMES++ 成功弥合了语义理解与物理模拟的差距，证明了统一框架在提升自动驾驶系统综合感知与预测能力方面的潜力。
---

## 摘要
驾驶世界模型通过模拟环境动力学，成为自动驾驶的关键技术。然而，现有方法主要集中在未来场景生成，往往忽视了全面的 3D 场景理解。相反，虽然大语言模型（LLMs）展示了令人印象深刻的推理能力，但它们缺乏预测未来几何演变的能力，导致语义解释与物理模拟之间存在显著差距。为了弥补这一差距，我们提出了 HERMES++，这是一个在单一框架内集成 3D 场景理解和未来几何预测的统一驾驶世界模型。我们的方法通过协同设计解决了这些任务的不同需求。首先，BEV（鸟瞰图）表示将多视图空间信息整合为与 LLM 兼容的结构。其次，我们引入了 LLM 增强的世界查询（world queries），以促进理解分支的知识迁移。第三，设计了一个“当前到未来链接”（Current-to-Future Link）来弥合时间跨度，使几何演变以语义上下文为条件。最后，为了增强结构完整性，我们采用了一种联合几何优化策略，将显式几何约束与隐式潜空间正则化相结合，使内部表示与几何感知先验对齐。在多个基准测试上的广泛评估验证了我们方法的有效性。HERMES++ 表现强劲，在未来点云预测和 3D 场景理解任务中均优于专门的方法。模型和代码将在 https://github.com/H-EmbodVis/HERMESV2 公开发布。

## Abstract
Driving world models serve as a pivotal technology for autonomous driving by simulating environmental dynamics. However, existing approaches predominantly focus on future scene generation, often overlooking comprehensive 3D scene understanding. Conversely, while Large Language Models (LLMs) demonstrate impressive reasoning capabilities, they lack the capacity to predict future geometric evolution, creating a significant disparity between semantic interpretation and physical simulation. To bridge this gap, we propose HERMES++, a unified driving world model that integrates 3D scene understanding and future geometry prediction within a single framework. Our approach addresses the distinct requirements of these tasks through synergistic designs. First, a BEV representation consolidates multi-view spatial information into a structure compatible with LLMs. Second, we introduce LLM-enhanced world queries to facilitate knowledge transfer from the understanding branch. Third, a Current-to-Future Link is designed to bridge the temporal gap, conditioning geometric evolution on semantic context. Finally, to enforce structural integrity, we employ a Joint Geometric Optimization strategy that integrates explicit geometric constraints with implicit latent regularization to align internal representations with geometry-aware priors. Extensive evaluations on multiple benchmarks validate the effectiveness of our method. HERMES++ achieves strong performance, outperforming specialist approaches in both future point cloud prediction and 3D scene understanding tasks. The model and code will be publicly released at https://github.com/H-EmbodVis/HERMESV2.

---

## 论文详细总结（自动生成）

这是一份关于论文《HERMES++: Toward a Unified Driving World Model for 3D Scene Understanding and Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的自动驾驶世界模型存在“理解”与“生成”的脱节。生成式模型（如预测未来点云）擅长模拟物理演化，但缺乏语义解释能力；而基于大语言模型（LLM）的驾驶模型擅长语义推理，却无法预测未来的几何演变。
*   **研究动机**：作者旨在构建一个统一的驾驶世界模型，将**3D场景理解**（如视觉问答、场景描述）与**未来几何预测**（如点云生成）集成在单一框架内，实现语义解释与物理模拟的协同。

### 2. 方法论：核心思想与关键技术
HERMES++ 采用基于鸟瞰图（BEV）的统一表示，核心组件包括：
*   **视觉分词器（Visual Tokenizer）**：将多视图图像转换为 BEV 特征，通过下采样和展平操作将其转化为 LLM 可理解的视觉 Token，解决了多视图输入与 LLM Token 长度限制的矛盾。
*   **LLM 增强的世界查询（World Queries）**：在 LLM 处理序列中插入可学习的查询向量。这些查询通过因果注意力机制吸收 LLM 的世界知识和当前场景的语义上下文，作为未来生成的先验。
*   **当前到未来链接（Current-to-Future Link）**：利用世界查询、文本嵌入（Textual Injection）和自身运动（Ego Modulation）作为条件，将当前的 BEV 特征传播到未来时间戳。
*   **联合几何优化策略（Joint Geometric Optimization）**：
    *   **显式约束**：对渲染出的点云深度进行 L1 损失监督。
    *   **隐式正则化**：引入预训练的冻结几何特征提取器，通过余弦相似度损失和 Gram 矩阵损失，使模型内部表示与几何感知先验对齐，确保结构完整性。
*   **共享渲染器（Shared Render）**：基于 SDF（符号距离函数）的神经渲染技术，将预测的 BEV 特征解码为 3D 点云。

### 3. 实验设计
*   **数据集**：
    *   **nuScenes**：用于几何表示学习和点云预测。
    *   **OmniDrive-nuScenes**：用于场景描述和视觉问答（VQA）。
    *   **NuScenes-QA**：大规模空间感知问答。
    *   **DriveLM**：基于图的逻辑推理问答。
    *   **NuInteract**：用于视觉-语言对齐预训练。
*   **Benchmark 与对比方法**：
    *   **生成任务**：对比了 SPFNet、ViDAR、DriveX 等点云预测专家模型，使用倒角距离（CD）衡量。
    *   **理解任务**：对比了 LLaVA-OV、OmniDrive、ORION 等理解专家模型，使用 CIDEr、METEOR、ROUGE-L 等指标。

### 4. 资源与算力
*   **模型规模**：使用了 InternVL2 作为基础 LLM，参数量涵盖 1.8B 和 3.8B 两个版本。
*   **训练细节**：采用了三阶段训练（几何预训练、模态对齐、统一指令微调）。
*   **算力说明**：文中提到了 Batch Size（32 到 128 不等）和训练轮数（最高 36 Epochs），但**未明确说明具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：作者在 4 个主流自动驾驶数据集上进行了测试，并针对 BEV 分辨率、几何优化策略、任务交互机制、查询初始化方式等进行了详尽的消融实验。
*   **充分性与客观性**：实验覆盖了从感知、推理到预测、规划（Open-loop Planning）的多个维度。通过与专门化（Specialist）模型的对比，证明了统一模型在多任务下的互补性，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **任务协同效应**：统一训练不仅可行，而且语义理解能显著引导几何预测，几何约束也能为语言推理提供物理支撑。
*   **BEV 的优越性**：相比直接输入多视图图像 Token，BEV 表示能更好地保留空间拓扑结构，减少几何信息的坍塌。
*   **性能领先**：HERMES++ 在 3s 未来点云预测上比 DriveX 误差降低了 8.2%，在场景理解上比 Omni-Q 提升了 9.2%（CIDEr 指标）。

### 7. 优点（亮点）
*   **架构统一**：成功将生成式世界模型与理解式 LLM 结合，打破了任务间的壁垒。
*   **知识迁移**：通过“世界查询”机制，巧妙地将 LLM 内部的常识和推理能力迁移到物理世界的几何预测中。
*   **结构增强**：提出的联合几何优化策略有效解决了生成模型中常见的深度模糊和投影伪影问题。

### 8. 不足与局限
*   **训练复杂性**：采用多阶段训练流程，对数据流水线和超参数调节要求较高。
*   **Token 瓶颈**：虽然使用了 BEV 压缩，但在处理极高分辨率或超长时序输入时，仍受限于 LLM 的上下文窗口长度。
*   **模态局限**：目前生成端主要集中在几何（点云），尚未实现视觉外观（视频）与几何的同步高保真生成。

（完）
