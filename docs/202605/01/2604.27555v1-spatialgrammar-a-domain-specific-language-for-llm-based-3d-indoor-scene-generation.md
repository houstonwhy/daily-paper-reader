---
title: "SpatialGrammar: A Domain-Specific Language for LLM-Based 3D Indoor Scene Generation"
title_zh: SpatialGrammar：一种用于基于大语言模型的 3D 室内场景生成的领域特定语言
authors: "Song Tang, Kaiyong Zhao, Yuliang Li, Qingsong Yan, Penglei Sun, Junyi Zou, Qiang Wang, Xiaowen Chu"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27555v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 基于大语言模型的自然语言3D室内场景生成
tldr: 针对现有大模型在生成3D室内场景时易出现空间错误和碰撞的问题，本文提出了一种领域特定语言SpatialGrammar。该语言通过鸟瞰图网格表示布局，并能确定性地编译为3D几何体。基于此，研究者开发了利用编译器反馈进行迭代优化的SG-Agent，以及在合成数据上训练的小型模型SG-Mini。实验表明，该方法显著提升了场景的空间保真度和物理合理性。
selection_source: fresh_fetch
motivation: 现有的LLM在生成3D场景时，由于缺乏有效的空间表示，常导致物体位置错误和物理碰撞。
method: 提出SpatialGrammar领域特定语言，并构建了具备闭环反馈机制的SG-Agent系统和轻量化模型SG-Mini。
result: 在多种复杂场景测试中，SG-Agent在空间保真度上优于现有方法，SG-Mini在单次生成任务中表现出色。
conclusion: 通过引入结构化的领域特定语言和编译器反馈机制，可以有效提升大模型生成3D室内场景的准确性与物理一致性。
---

## 摘要
从自然语言自动生成交互式 3D 室内场景对于虚拟现实、游戏和具身智能至关重要。然而，现有的基于大语言模型（LLM）的方法通常存在空间错误和碰撞问题，部分原因是常见的场景表示方式（如原始坐标或冗长的代码）使得模型难以推理 3D 空间关系和物理约束。我们提出了 SpatialGrammar，这是一种领域特定语言，它将重力对齐的室内布局表示为鸟瞰图（BEV）网格放置，并能确定性地编译为有效的 3D 几何体，从而实现可验证的约束检查。基于这种表示，我们开发了：(1) SG-Agent，一个利用编译器反馈迭代优化场景并强制执行碰撞约束的闭环系统；(2) SG-Mini，一个完全在经编译器验证的合成数据上训练的 1.04 亿参数模型。在涵盖五种不同复杂度场景的 159 个测试场景中，SG-Agent 相比以往方法提升了空间保真度和物理合理性，而 SG-Mini 在单次生成任务中与参数量更大的 LLM 基准模型相比具有竞争力。

## Abstract
Automatically generating interactive 3D indoor scenes from natural language is crucial for virtual reality, gaming, and embodied AI. However, existing LLM-based approaches often suffer from spatial errors and collisions, in part because common scene representations-raw coordinates or verbose code-are difficult for models to reason about 3D spatial relationships and physical constraints. We propose SpatialGrammar, a domain-specific language that represents gravity-aligned indoor layouts as BEV grid placements with deterministic compilation to valid 3D geometry, enabling verifiable constraint checking. Building on this representation, we develop (1) SG-Agent, a closed-loop system that uses compiler feedback to iteratively refine scenes and enforce collision constraints, and (2) SG-Mini, a 104M-parameter model trained entirely on compiler-validated synthetic data. Across 159 test scenes spanning five scenarios of different complexity, SG-Agent improves spatial fidelity and physical plausibility over prior methods, while SG-Mini performs competitively against larger LLM-based baselines on single-shot generation scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《SpatialGrammar: A Domain-Specific Language for LLM-Based 3D Indoor Scene Generation》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：从自然语言生成交互式 3D 室内场景对 VR、游戏和具身 AI 至关重要。然而，现有的大语言模型（LLM）在生成场景时常出现**空间位置错误**和**物体碰撞**。
*   **核心痛点**：现有的场景表示法（如原始 6-DoF 坐标或冗长的代码/JSON）对 LLM 而言过于抽象且缺乏直观性，导致模型难以理解复杂的空间关系和物理约束。
*   **整体含义**：本文旨在设计一种既符合 LLM 直觉（空间直观），又具备物理可验证性（确定性编译）的中间语言，将复杂的 3D 生成任务简化为结构化的程序编写任务。

### 2. 方法论
*   **SpatialGrammar DSL（领域特定语言）**：
    *   **BEV 抽象**：受自动驾驶启发，将 3D 场景简化为鸟瞰图（BEV）网格。LLM 只需指定物体在网格上的位置（行、列）和偏航角（Yaw），高度和物理支撑由编译器自动处理。
    *   **双子语言**：包含用于家具布局的 **LLMSLI** 和用于建筑结构（墙、门窗）的 **LLMSLB**。
    *   **递归子布局（Sub-layouts）**：通过“ON”机制（如 `TV_on_top`）支持层级化放置，允许在局部坐标系中定义桌面摆放、墙挂物体等复杂结构。
*   **SG-Agent（闭环代理系统）**：
    *   采用 ReAct 框架，集成编译器和轻量级渲染引擎（Draft Engine）。
    *   **闭环反馈**：编译器提供符号化反馈（如“物体 A 与 B 碰撞”），视觉模型提供审美反馈，Agent 根据反馈迭代修改 DSL 代码。
*   **SG-Mini（轻量化模型）**：
    *   基于 1.04 亿参数的小规模语言模型（SLM），专门学习 SpatialGrammar 的生成。
    *   **错误链（Error Chain）方法**：在 DPO 训练阶段，通过主动注入语义、空间、碰撞和语法错误来构造负样本，提升模型的鲁棒性。

### 3. 实验设计
*   **测试场景**：涵盖 5 大类任务（单物体放置、多物体生成、多轮对话编辑、层级化放置、建筑生成），共 159 个测试场景，包含 1,198 个原子检查点。
*   **对比基准（Baselines）**：对比了 SceneTeller、Respace、HoloDeck、DirectLayout 等现有主流方法。
*   **评估指标**：
    *   **DRFR**：原子需求遵循率（物体是否存在、属性、空间关系等）。
    *   **CR_obj**：物体碰撞率。
    *   **CLIP Score**：视觉-语义一致性。
    *   **GAS/HAS**：Gemini 自动审美评分与人类专业人士审美评分。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 台 NVIDIA RTX 4090 GPU**。
*   **训练时长**：整个合成数据训练流水线（包括预训练、SFT 和 DPO）在 **一周内** 完成。
*   **模型规模**：SG-Mini 仅有 **104M（1.04 亿）** 参数，远小于对比的商业大模型。

### 5. 实验数量与充分性
*   **实验规模**：进行了 159 组端到端测试，并针对 1,198 个细粒度约束进行了验证，样本量足以支撑结论。
*   **消融实验**：非常充分。分别针对 Agent 的反馈组件（物理反馈 vs 视觉反馈）、DPO 的错误注入类型（碰撞、空间、语义、语法）、以及 DSL 的网格大小（50cm 到 200cm）进行了详细对比。
*   **客观性**：使用了统一的渲染器和资产库来评估所有对比方法，确保了视觉评估的公平性。

### 6. 主要结论与发现
*   **空间保真度提升**：引入 SpatialGrammar 后，DRFR 从 0.62 提升至 0.83，碰撞率从 66.7% 降至 13.6%。
*   **闭环优势**：SG-Agent 通过迭代反馈，在多项任务中实现了 **零碰撞**。
*   **小模型潜力**：仅有 104M 参数的 SG-Mini 在单次生成任务中表现优于许多参数量更大的通用模型，证明了专用 DSL 能显著降低模型的认知负担。
*   **层级化能力**：子布局机制使得模型在处理“桌面上摆放杂物”等复杂层级任务时具有明显优势。

### 7. 优点
*   **设计巧妙**：将 3D 坐标预测转化为 2D 网格占位，极大地降低了 LLM 的推理难度。
*   **确定性**：DSL 具有确定性的编译逻辑，消除了传统生成方法中的随机性和不可解释性。
*   **数据高效**：通过合成数据和错误链方法，实现了无需人工标注的自给自足式训练。

### 8. 不足与局限
*   **模型能力上限**：SG-Mini 虽然在布局上表现优异，但在处理极其复杂的多轮逻辑推理时，仍依赖于更强大的底座模型（如 Claude）。
*   **审美评估的主观性**：尽管引入了 GAS 和 HAS，但 3D 场景的“美感”和“合理性”评估仍存在一定噪声。
*   **应用限制**：目前主要针对重力对齐（水平放置）的室内场景，对于非规则形状或非重力对齐的极端 3D 环境（如零重力空间）支持有限。

（完）
