---
title: "CasLayout: Cascaded 3D Layout Diffusion for Indoor Scene Synthesis with Implicit Relation Modeling"
title_zh: CasLayout：基于隐式关系建模的级联式三维布局扩散室内场景合成
authors: "Yingrui Wu, Youkang Kong, Mingyang Zhao, Weize Quan, Dong-Ming Yan, Yang Liu"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27361v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 用于3D室内场景合成和布局生成的级联扩散框架
tldr: 针对室内3D场景合成中数据稀缺及全局与局部一致性难以兼顾的问题，本文提出CasLayout级联扩散框架。该框架将生成任务分解为家具预测、尺寸细化、空间关系建模和OBB生成四个阶段，并引入建筑元素约束与稀疏关系图。通过VAE编码关系，实现了高保真度、多样性及强可控性的场景生成，并支持零样本图像转场景等任务。
selection_source: fresh_fetch
motivation: 现有方法往往忽视建筑边界且依赖冗余的关系图，导致生成场景缺乏物理合理性和语义一致性。
method: 提出一种级联扩散框架，将场景生成解耦为四个子阶段，并结合建筑约束与基于VAE的稀疏关系图建模。
result: 实验表明，该方法在保真度和多样性上达到了SOTA水平，并显著提升了实际应用中的可控性。
conclusion: CasLayout通过解耦架构和隐式关系建模，有效解决了室内场景合成的复杂性，为零样本跨模态生成提供了新思路。
---

## 摘要
由于数据稀缺以及同时施加全局建筑约束和局部语义一致性的困难，合成逼真的三维室内场景仍然具有挑战性。现有方法通常忽略结构边界，或依赖于会引入冗余生成误差的全连接关系图。受人类设计认知的启发，我们提出了 CasLayout，这是一个级联扩散框架，它将联合场景生成任务分解为具有明确物理和语义角色的四个条件子阶段：(1) 预测家具数量和类别，(2) 细化物体尺寸和特征嵌入，(3) 在潜空间中建模空间关系，以及 (4) 生成有向包围盒 (OBB)。这种解耦架构降低了对数据的需求，并能够灵活集成大语言模型 (LLM) 和视觉语言模型 (VLM)，以执行诸如图像到场景生成等零样本任务。为了在复杂的平面图中保持物理有效性，我们将建筑元素（如墙壁、门和窗户）显式建模为条件约束。此外，为了解决稠密关系图的高熵问题，我们引入了一种与人类空间描述相一致的稀疏关系图表述。通过使用双向变分自编码器 (VAE) 将这些稀疏图编码到紧凑的潜空间中，所提出的框架提供了增强的关系可控性，使生成的布局能够更好地遵循功能组织。实验表明，CasLayout 在保真度和多样性方面达到了最先进的性能，同时在实际应用中实现了更高的可控性。

## Abstract
Synthesizing realistic 3D indoor scenes remains challenging due to data scarcity and the difficulty of simultaneously enforcing global architectural constraints and local semantic consistency. Existing approaches often overlook structural boundaries or rely on fully connected relation graphs that introduce redundant generation errors. Inspired by human design cognition, we present CasLayout, a cascaded diffusion framework that decomposes the joint scene generation task into four conditional sub-stages with explicit physical and semantic roles: (1) predicting furniture quantity and categories, (2) refining object sizes and feature embeddings, (3) modeling spatial relationships in a latent space, and (4) generating Oriented Bounding Boxes (OBBs). This decoupled architecture reduces data requirements and enables flexible integration of Large Language Models (LLMs) and Vision Language Models (VLMs) for zero-shot tasks such as image-to-scene generation. To maintain physical validity within complex floor plans, we explicitly model building elements (e.g., walls, doors, and windows) as conditional constraints. Furthermore, to address the high entropy of dense relation graphs, we introduce a sparse relation graph formulation aligned with human spatial descriptions. By encoding these sparse graphs into a compact latent space using a bidirectional Variational Autoencoder (VAE), the proposed framework provides enhanced relational controllability, allowing generated layouts to better respect functional organization. Experiments demonstrate that CasLayout achieves state-of-the-art performance in fidelity and diversity while enabling improved controllability in practical applications.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CasLayout** 的新型三维室内场景合成框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 3D 室内场景合成方法面临两大挑战：一是**数据稀缺**，难以直接学习高维的家具属性联合分布；二是**缺乏物理与语义一致性**，现有模型往往忽略房间的建筑边界（墙、门、窗），且依赖全连接的稠密关系图，这会引入大量冗余噪声和冲突约束。
*   **核心目标**：受专业设计师工作流启发，通过级联分解和隐式稀疏关系建模，提升场景生成的保真度、多样性以及对建筑约束的可控性。

### 2. 方法论
CasLayout 将复杂的布局生成任务分解为四个级联的条件扩散阶段：
1.  **家具类别扩散 (Furniture Type Diffusion)**：根据房间建筑元素预测家具的数量和类别。
2.  **家具属性扩散 (Furniture Property Diffusion)**：确定每个家具的精确尺寸和特征嵌入（用于从数据库检索 CAD 模型）。
3.  **关系潜变量扩散 (Relation Latent Diffusion)**：
    *   **核心创新**：提出**隐式稀疏关系建模**。通过剪枝非功能性的冗余连接，将稠密图转为稀疏图，降低熵值。
    *   **技术细节**：设计了一个**双向关系 VAE**，利用“入度-出度交叉注意力机制”将稀疏关系图编码为与节点对齐的紧凑潜向量。
4.  **包围盒布局扩散 (Box Layout Diffusion)**：在类别、尺寸和关系潜变量的约束下，生成家具的最终位置和朝向（OBB）。

*   **关键技术**：
    *   **建筑元素建模**：将墙、门、窗显式建模为全局条件约束，确保家具不遮挡动线。
    *   **联合训练**：关系 VAE 与布局扩散阶段进行联合训练，以增强潜空间与几何布局的兼容性。

### 3. 实验设计
*   **数据集**：主要使用 **3D-FRONT**（包含 4041 个卧室、900 个餐厅、813 个客厅）。此外在 **ScanNet** 上验证了泛化性。
*   **Benchmark 与指标**：
    *   **图像质量**：FID（Fréchet Inception Distance）、KID。
    *   **语义与分布**：TKL（类别 KL 散度）、SCA（场景分类准确率）。
    *   **物理合理性**：IoU（碰撞率）、Rout（越界率）、Rwalk（可行走区域比例）。
*   **对比方法**：包括自回归模型（ATISS）、扩散模型（DiffuScene、InstructScene、GLTScene）以及基于大模型的方案（LayoutGPT）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU**。
*   **训练细节**：每个阶段训练 2000 个 epoch，batch size 为 256。
*   **推理效率**：通过 UniPC 采样器，仅需 15-25 步即可生成，单场景推理时间约为 **25-40 毫秒**，支持实时应用。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了场景生成、布局补全、家具重排、LLM/VLM 驱动生成等多种任务。
*   **消融实验**：非常充分。分别验证了级联阶段设计的必要性、生成顺序的影响、稀疏 vs 稠密关系的对比、VAE 结构的有效性以及联合训练的作用。
*   **主观评价**：组织了 36 名参与者的用户研究，对比了不同方法在合理性和建筑一致性上的表现。
*   **客观性**：实验在统一的数据分割和评估标准下进行，对比了近年来最先进的 SOTA 方法，结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能领先**：CasLayout 在所有主流指标（FID, KID, SCA 等）上均优于现有 SOTA 方法。
*   **可控性强**：通过引入建筑元素约束，显著降低了家具越界和遮挡门窗的概率。
*   **稀疏性优势**：证明了稀疏关系图比全连接图更能捕捉设计逻辑，且更符合人类的交互习惯（如“在床前放个地毯”）。
*   **跨模态潜力**：由于阶段解耦，该框架能无缝集成 LLM 和 VLM，实现高质量的文本转场景和图像转场景。

### 7. 优点（亮点）
*   **设计逻辑契合**：级联架构模仿人类设计过程，将不可解的高维分布转化为可处理的条件分布。
*   **隐式关系建模**：利用 VAE 压缩关系空间，解决了 $N^2$ 复杂度带来的冗余和计算压力。
*   **建筑感知**：显式考虑门窗等动态元素的物理空间，这在以往的布局研究中常被忽视。
*   **多功能性**：一个模型即可处理生成、编辑、补全和重排等多种任务。

### 8. 不足与局限
*   **误差累积风险**：作为级联模型，如果早期阶段（如家具类别预测）出现严重错误，后期阶段难以纠正。
*   **检索限制**：家具风格受限于预定义的 CAD 库，目前尚不支持生成全新的家具几何形状。
*   **关系定义局限**：目前主要建模成对（Pairwise）关系，对于更复杂的多物体组合（Group）关系建模尚显不足。
*   **极端分布挑战**：当用户输入的家具列表严重偏离训练分布（如在小房间塞入 20 把椅子）时，模型仍可能产生物理碰撞。

（完）
