---
title: "Full-4D: Generating Full-Scope 4D Scenes from a Single-View Video"
title_zh: Full-4D：从单视图视频生成全范围4D场景
authors: "Tingxi Chen, Ke Hao, Yabo Chen, Zhengxue Cheng, Rong Xie, Li Song, Haibin Huang, Chi Zhang, Xuelong Li"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25500v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从单视图视频生成全范围4D场景的框架
tldr: 针对单视图视频生成全范围4D场景的难题，本文提出Full-4D框架。通过引入大规模真实多视图视频数据集Real-MV-4D，并结合融合时空注意力的扩散模型生成同步多视图视频，最后利用4D高斯泼溅（4DGS）和流匹配蒸馏技术实现高质量4D重建，显著提升了生成场景的视觉保真度和几何一致性。
selection_source: fresh_fetch
motivation: 现有的单视图视频转4D方法受限于视角范围小且缺乏大规模多视图动态数据集，难以恢复完整的动态场景。
method: 提出一种端到端的框架，利用融合几何先验的时空注意力扩散模型生成多视图视频，并结合4DGS与流匹配蒸馏进行优化重建。
result: 实验证明该方法在视觉质量和几何一致性上均优于现有技术，实现了从单视图视频到全范围4D场景的跨越。
conclusion: 本研究通过创新的数据集和模型架构，为单视图视频生成全范围、高一致性的4D动态场景提供了有效的解决方案。
---

## 摘要
从单视图视频生成4D场景本质上是一个病态问题：单一视角缺乏恢复具有全覆盖范围的完整动态场景所需的信息。现有方法通常局限于单目视频、简单的3D效果，或者仅在原始视角周围进行微小的视角扰动，未能实现真正的4D生成。同时，缺乏捕获具有同步多视图视频的全范围4D场景的大规模数据集，进一步阻碍了这一方向的进展。我们提出了一种新颖的单视图视频到4D框架，将全范围4D生成建模为多视图视频合成，随后从生成的视图中进行基于优化的4D重建。为了端到端地实例化这一方案，我们做出了三个关键贡献。首先，我们引入了 Real-MV-4D，这是一个在多种真实世界环境中捕获的同步多视图视频的大规模数据集，用以提供4D监督。其次，我们训练了一个多视图视频扩散模型，该模型由一种新颖的时间(T)-视图(V)融合注意力机制驱动，该机制直接将几何重投影先验和显式相机条件嵌入到其视图-时间交互中。与基础特征融合不同，这种直接绑定将生成过程与物理3D先验严格对齐，从而产生密集且同步的 T×V 视频网格。第三，我们不再依赖非交互且不一致的2D视频插值，而是将合成的多视图视频提升为显式的4D表示（即 4DGS），并通过流匹配蒸馏（Flow Matching Distillation）损失进行正则化，该损失利用多视图先验来改进新视图渲染。广泛的实验表明，我们的方法在视觉保真度和几何一致性方面均优于现有方法，实现了从单视图视频生成全范围4D场景。

## Abstract
Generating 4D scenes from a single-view video is inherently ill-posed: a single viewpoint lacks the information needed to recover a complete, dynamic scene with full coverage. Existing methods are typically limited to monocular videos, simple 3D effects, or only small viewpoint perturbations around the original viewpoint, falling short of true 4D generation. Meanwhile, the lack of large-scale datasets capturing full-scope 4D scenes with synchronized multi-view videos further hinders progress in this direction. We propose a novel single-view video-to-4D framework that casts full-scope 4D generation as a multi-view video synthesis followed by optimization-based 4D reconstruction from the generated views. To instantiate this formulation end-to-end, we make three key contributions. First, we introduce Real-MV-4D, a large-scale dataset of synchronized multi-view videos captured in diverse real-world environments to provide the 4D supervision. Second, we train a multi-view video diffusion model driven by a novel fused time(T)-view(V) attention mechanism that directly embeds geometric reprojection priors and explicit camera conditioning into its view-time interactions. Unlike basic feature fusion, this direct binding strictly aligns the generation process with physical 3D priors to produce a dense, synchronized T$\times $V video grid. Third, rather than relying on non-interactive and inconsistent 2D video interpolations, we lift the synthesized multi-view videos into an explicit 4D representation (i.e. 4DGS), regularized by a Flow Matching Distillation loss that exploits the multi-view prior to improve novel-view rendering. Extensive experiments demonstrate that our method outperforms existing approaches in both visual fidelity and geometric consistency, enabling full-scope 4D scene generation from single-view videos.

---

## 论文详细总结（自动生成）

### 论文总结：Full-4D：从单视图视频生成全范围4D场景

#### 1. 核心问题与整体含义（研究动机和背景）
从单视图视频生成动态4D场景（即具有时间连续性和全方位视角覆盖的3D场景）是一个极具挑战性的“病态问题”。
*   **研究动机**：现有的4D生成方法主要面临两个瓶颈：一是**数据匮乏**，缺乏大规模、真实世界、同步的多视图动态场景数据集；二是**技术限制**，现有模型大多只能处理物体中心场景或在原始视角附近进行微小扰动，难以实现大范围视角的几何一致性。
*   **核心目标**：通过引入大规模真实数据集和改进的扩散模型架构，实现从单目视频到全范围（Full-Scope）4D场景的端到端生成。

#### 2. 论文提出的方法论
该框架将4D生成分为两个阶段：**多视图视频合成**和**基于优化的4D重建**。

*   **核心思想**：利用扩散模型联合生成 $T \times V$（时间 $\times$ 视图）的视频网格，确保时空一致性，随后将其提升为4D高斯泼溅（4DGS）表示。
*   **关键技术细节**：
    1.  **Real-MV-4D 数据集**：构建了一个包含2000多个场景、6000多个受试者的同步6视角视频数据集，提供强有力的4D监督。
    2.  **空间引导（Projection Conditioning）**：利用单目深度估计将输入视频提升为动态点云，并投影到目标视角，作为扩散模型的几何先验。
    3.  **T-V 融合稀疏注意力机制（T-V Fused Sparse Attention）**：在 DiT 模块中引入特殊的掩码策略，包括：
        *   **视图内掩码**：确保单视频的时间连续性。
        *   **时间内掩码**：确保同一时刻不同视角间的几何一致性。
        *   **跨半部分掩码**：使生成帧直接参考输入视频的特征。
    4.  **流匹配蒸馏（Flow Matching Distillation, FMD）**：在4DGS优化阶段，利用预训练视频扩散模型的先验知识来正则化未观察到的视角，填补遮挡区域并增强一致性。

#### 3. 实验设计
*   **数据集**：
    *   **Real-MV-4D**（自建）：用于训练和主要评估，包含室内外复杂场景。
    *   **DyCheck**（iPhone 数据集）：用于验证模型的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了 **ReCamMaster**（相机控制生成）、**Free4D**（无微调4D生成）、**DimensionX**（时空解耦生成）和 **TrajectoryCrafter**。
*   **评估指标**：
    *   **视觉质量**：FID、FVD、CLIP-F（时间相干性）。
    *   **视图同步性**：Mat. Pix.（特征匹配点数）、FVD-V、CLIP-V、View Consistency（重投影误差）。
    *   **综合性能**：VBench 评分。

#### 4. 资源与算力
*   **训练阶段**：使用了 **8 台 NVIDIA H100 GPU**。在 384x672 分辨率下训练了 10k 步。
*   **重建阶段**：使用了 **2 台 NVIDIA H100 GPU**（一台用于计算 FMD 损失，另一台用于优化 4DGS 表示）。
*   **推理耗时**：在单 GPU 上生成 6 个同步视频约需 10 分钟。

#### 5. 实验数量与充分性
*   **实验规模**：在 500 个多样化的测试场景上进行了定量评估，涵盖了不同的人数、环境和动作类型。
*   **消融实验**：针对 T-V 融合注意力（三种掩码）、投影条件、FMD 损失等核心组件进行了详尽的消融研究。
*   **充分性评价**：实验设计较为全面，不仅在自建的大规模数据集上表现优异，还在第三方数据集（DyCheck）上进行了跨域验证，证明了其客观性和公平性。

#### 6. 论文的主要结论与发现
*   **全范围覆盖**：Full-4D 能够生成大角度（如 90° 侧面甚至背面）的连贯动态场景，突破了以往方法仅限小范围扰动的限制。
*   **几何一致性**：通过直接在注意力机制中绑定相机参数和几何先验，模型生成的视频网格在物理上更加对齐。
*   **重建质量**：FMD 损失显著改善了 4DGS 在插值视角下的渲染效果，消除了稀疏高斯伪影。

#### 7. 优点
*   **数据集贡献**：Real-MV-4D 是目前少有的高质量、大规模真实世界同步多视图动态数据集，对社区具有重要价值。
*   **架构创新**：T-V 融合注意力机制巧妙地解决了多视图视频生成的同步难题，且不增加额外参数。
*   **端到端流程**：从单图/单视频到 4DGS 的流程完整，且生成的 4D 场景支持实时交互渲染。

#### 8. 不足与局限
*   **极端视角幻觉**：当输入视频仅包含背面时，生成正面面部特征仍极具挑战（缺乏视觉证据）。
*   **风格偏移**：在真实数据上训练可能导致在处理纯合成内容时出现风格偏移。
*   **计算开销**：尽管使用了稀疏注意力，但 4D 重建过程仍需消耗大量显存和计算时间，难以实现秒级生成。
*   **深度依赖**：模型性能受限于前端深度估计的准确性，复杂场景下的深度误差会传播至生成阶段。

（完）
