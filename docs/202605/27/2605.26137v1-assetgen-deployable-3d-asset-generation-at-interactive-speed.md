---
title: "AssetGen: Deployable 3D Asset Generation at Interactive Speed"
title_zh: AssetGen：交互式速度下的可部署三维资产生成
authors: "Dilin Wang, Xiaoyu Xiang, Kihyuk Sohn, Tom Monnier, Yu-Ying Yeh, Thu Nguyen-Phuoc, Jiawen Zhang, Yuchen Fan, Antoine Toisoul, Hyunyoung Jung, Prithviraj Dhar, Michael Bunnell, Nikolaos Sarafianos, Chuhang Zou, Roman Shapovalov, Andrea Vedaldi, Rakesh Ranjan"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.26137v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 以交互速度生成可部署的3D资产
tldr: 针对现有3D生成模型在部署和交互体验上的不足，本文提出AssetGen。该系统仅需单张参考图，即可在30秒内生成包含烘焙法线、彩色纹理且多边形数量可控的高质量3D网格，适用于移动端实时渲染。通过VecSet框架、并行UV展开及纹理修复技术，AssetGen在保证视觉质量的同时显著提升了生成速度，为AI辅助的3D内容创作提供了高效、可部署的解决方案。
selection_source: fresh_fetch
motivation: 现有3D生成研究多关注高分辨率，忽视了用户体验、生成速度以及在移动端等实时场景下的部署能力。
method: 采用粗到精的VecSet框架实现GPU加速的网格简化与法线烘焙，并结合多视图纹理生成、反向投影及3D修复技术。
result: AssetGen在30秒内生成的资产质量可媲美商业方案，而Flash版本可在14秒内提供预览级结果。
conclusion: 该系统成功实现了兼具高质量与交互速度的3D资产生成，极大地推动了可部署AI辅助创作流程的发展。
---

## 摘要
尽管三维生成技术正在飞速发展，但近期的工作往往侧重于获取高分辨率资产，而将用户体验和可部署性视为次要考虑。我们提出了 AssetGen，这是一种专注于这两个方面的三维生成器。给定一张参考图像，它能在 30 秒内生成高质量网格，包含烘焙法线、彩色纹理以及受控的多边形预算，适用于包括移动端在内的实时渲染场景。AssetGen Flash 变体进一步将延迟降低至 14 秒，适用于交互式和智能体创作循环。我们的模型采用由粗到精的 VecSet 框架生成物体几何，该框架在 GPU 上实现了网格简化、清理和法线烘焙，并具备快速并行 UV 展开功能。随后，它以多视图方式生成纹理，接着进行反向投影和三维修复。模型蒸馏、算子优化和流水线并行化经过协同设计，以实现系统的端到端加速。我们引入了大量的自动化评估和盲测人工评估，证明了该系统在 30 秒内即可达到与领先商业方案相媲美的视觉质量，并在不到 15 秒内提供预览级结果。最终成果是一个支持在交互式工作流中进行 AI 辅助、可部署三维内容创作的系统。

## Abstract
While 3D generation is progressing rapidly, recent work has often focused on obtaining high-resolution assets, leaving user experience and deployability as afterthoughts. We present AssetGen, a 3D generator that focuses instead on these two aspects. Given one reference image, in 30 seconds it produces a high-quality mesh with baked normals, a color texture, and a controlled polygon budget suitable for real-time rendering, including mobile use cases. The AssetGen Flash variant further reduces latency to 14 seconds for interactive and agentic creation loops. Our model generates the object geometry with a coarse-to-refine VecSet framework, which implements mesh simplification, cleaning, and normal baking on the GPU, and a fast parallel UV unwrapping. It then generates textures in a multi-view fashion, followed by backprojection and 3D inpainting. Model distillation, kernel optimization, and pipeline parallelization are co-designed to accelerate the system end-to-end. We introduce numerous automated and blind human evaluations and demonstrate competitive visual quality against leading commercial solutions in 30 seconds and preview-quality results in less than 15 seconds. The final result is a system that supports AI-assisted, deployable 3D content creation in interactive workflows.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AssetGen** 的系统，旨在解决当前 3D 生成领域中“生成速度慢”和“资产难以直接部署”的两大痛点。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 3D 生成技术（Image-to-3D）取得了长足进步，但现有模型生成的资产往往过于复杂（多边形数量巨大）、缺乏合理的 UV 展开和法线烘焙，且生成时间通常需要数分钟。这使得生成的资产无法直接用于实时渲染（如移动端、AR/VR）或交互式创作流。
*   **核心问题**：如何在保持高质量视觉效果的同时，在**交互式速度**（30 秒以内）下生成**可直接部署**（低多边形、带 UV 和法线贴图）的 3D 资产。

### 2. 方法论：核心思想与技术细节
AssetGen 采用了一个三阶段的显式几何优先流水线：
*   **MeshGen（几何生成）**：
    *   采用由粗到精的 **VecSet** 扩散框架。第一阶段生成粗略形状，第二阶段进行局部细节细化。
    *   **数据预处理**：提出一种基于 GPU 加速的**洪水填充（Flood-fill）**算法，将原始 3D 资产转换为高质量的等值面（SDF）数据，解决了传统方法在处理带孔洞或非流形网格时的失效问题。
    *   **模型蒸馏**：通过渐进式蒸馏将扩散步数从 120 步压缩至 30 步，并合并了分类器自由引导（CFG）以减少推理次数。
*   **几何后处理（可部署化）**：
    *   **网格简化**：在 GPU 上实现并行边缘折叠（QEM），并优化了顶点放置策略，在极低面数（如 5k-10k）下保持几何保真度。
    *   **UV 展开与法线烘焙**：引入并行化的 UV 展开算法（基于 xatlas）和 GPU 驻留的法线烘焙技术，将高模细节转移到低模切线空间。
*   **TextureGen（纹理生成）**：
    *   使用多视图扩散 Transformer（DiT）同步生成 10 个视角的纹理图。
    *   **几何条件渲染**：利用 GPU 栅格化器渲染位置图和法线图作为扩散模型的条件。
    *   **融合与修复**：通过各向异性过滤的反向投影将多视图融合到 UV 空间，并利用 3D 感知的几何亲和力进行纹理修复（Inpainting）。

### 3. 实验设计
*   **数据集**：使用 Meta 内部授权的大规模 3D 数据集，经过严格的几何过滤（去重、面数筛选）和语义过滤（VLM 剔除背景、扫描件、错误朝向）。
*   **Benchmark**：
    *   **AssetBench**：包含 101 个高质量通用物体，评估形状质量（IoU, Chamfer Distance）和忠实度（VLM-as-a-Judge, CLIP）。
    *   **CharacterBench**：包含 100 个角色图像，专门评估面部细节、手指完整性和全身一致性。
*   **对比方法**：对比了四个领先的商业模型（Commercial Model A-D）以及开源模型 Trellis 2。

### 4. 资源与算力
*   **推理算力**：主要在 **NVIDIA H100 GPU** 上运行。
*   **训练细节**：MeshGen 训练分为三个阶段，Latent Token 数量从 512 增加到 4096，使用 AdamW 优化器，Batch Size 最大达 2048。
*   **延迟表现**：
    *   **AssetGen (标准版)**：约 30 秒生成高质量资产（H100）。
    *   **AssetGen Flash (快速版)**：约 14 秒生成预览级资产（跳过细化步骤，降低纹理分辨率）。

### 5. 实验数量与充分性
*   **实验规模**：进行了 200+ 组基准测试对比，以及超过 1000 次的人工盲测投票。
*   **消融实验**：详细分析了模型蒸馏、GPU 算子优化（FlashAttention-3、图编译）、并行流水线对延迟的影响。
*   **充分性评价**：实验设计非常全面，不仅涵盖了标准的几何指标，还引入了针对角色生成的细粒度评估（如手指计数、面部对称性），并对比了商业闭源方案，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：AssetGen 在 30 秒内生成的资产质量在人工评估中优于或等同于生成时间需 100-180 秒的商业模型。
*   **速度优势**：通过端到端优化（模型蒸馏 + GPU 驻留处理），AssetGen 实现了比现有系统快 4-10 倍的生成速度。
*   **可部署性**：生成的低多边形网格（约 23k 面）配合烘焙法线，在移动端实时渲染中表现出色。

### 7. 优点（亮点）
*   **端到端协同设计**：不只是优化模型，而是将几何处理、UV 展开、法线烘焙全部集成进 GPU 流水线，消除了 I/O 瓶颈。
*   **实用性极强**：生成的资产符合工业标准（带 UV、法线、受控面数），真正做到了“即生成即使用”。
*   **双模式设计**：提供 30s 高质量模式和 14s 极速预览模式，适应不同的创作节奏。

### 8. 不足与局限
*   **单图歧义性**：对于极度复杂或非对称的物体背面，模型仍可能产生推测性错误。
*   **拓扑结构**：虽然面数可控，但生成的网格拓扑是基于 Marching Cubes 的三角面，并非艺术家偏好的四边形网格（Quad-dominant），且不支持自动骨骼绑定（Rigging）。
*   **数据依赖**：模型主要在合成数据上训练，虽然在真实照片上表现尚可，但在极端光照或复杂背景下的鲁棒性仍有提升空间。

（完）
