---
title: "SceneCode: Executable World Programs for Editable Indoor Scenes with Articulated Objects"
title_zh: SceneCode：针对包含关节物体的可编辑室内场景的可执行世界程序
authors: "Puyi Wang, Yuhao Wang, Linjie Li, Zhengyuan Yang, Kevin Qinghong Lin, Yangguang Li, Yu Cheng"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.19587v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 将自然语言提示编译为可执行的室内场景
tldr: SceneCode是一个将自然语言提示词转化为可执行代码驱动的室内场景合成框架。针对现有方法生成静态网格且缺乏交互性的问题，它通过代理架构生成房屋布局，并利用代码生成策略将物体请求转化为Blender Python程序。该方法不仅能生成高质量、可编辑的关节物体，还支持物理仿真，为具身智能和机器人操作提供了可追溯且可局部编辑的模拟环境。
selection_source: fresh_fetch
motivation: 现有的室内场景合成方法通常生成静态网格，缺乏物体层面的可控性和按需生成可交互关节资产的能力。
method: 采用代理架构将提示词转化为布局，并通过执行引导的修复循环将物体请求编译为可执行的Blender Python程序。
result: 实验表明SceneCode在场景合成质量、资产网格结构以及模拟器加载的关节元数据方面均优于现有方法。
conclusion: 该研究证明了程序化世界生成在创建可编辑、可交互且支持物理仿真的室内场景方面的有效性。
---

## 摘要
室内场景合成是具身智能、机器人操纵和基于模拟的策略评估的基础，在这些领域中，一个实用的场景不仅必须指定环境的外观，还必须指定其物体的结构。然而，现有的流程通常将生成的内容表示为静态网格，并且仅从策划好的资产库中继承关节连接，这限制了物体级的可控性，并阻碍了按需生产新的可交互资产。我们通过将物理可交互的室内场景合成公式化为程序化世界生成来填补这一空白，并提出了 SceneCode，这是一个将自然语言提示词编译为可执行的、代码驱动的室内世界，而非一系列不透明网格的框架。一个房间级的智能体骨干网络首先将提示词转换为结构化的房屋布局，并通过“规划者-设计者-评论者”循环发出针对每个物体的资产请求（AssetRequests）。每个请求随后被路由到五种代码生成策略之一，并转换为合成的分部件 Blender Python 程序，这些程序通过执行引导的修复与完善循环进行验证。生成的程序被编译为可供模拟的资产，并导出为用于物理模拟的 SDF。一个持久化的场景状态注册表链接了物体请求、可执行程序、渲染几何体和模拟资产，将场景组装转变为一个可追溯且可局部编辑的世界构建过程。我们在场景级合成、物体级资产质量、人类评判和下游机器人交互方面对 SceneCode 进行了评估。结果表明，可执行世界程序改进了符合提示词的室内场景生成，并产生了具有更清晰网格结构和模拟器可加载关节元数据的资产。项目页面：https://scene-code.github.io/。

## Abstract
Indoor scene synthesis underpins embodied AI, robotic manipulation, and simulation-based policy evaluation, where a useful scene must specify not only what the environment looks like, but also how its objects are structured. Existing pipelines, however, typically represent generated content as static meshes and inherit articulation only from curated asset libraries, which limits object-level controllability and prevents new interactable assets from being produced on demand. We address this gap by formulating physically interactable indoor scene synthesis as programmatic world generation, and present SceneCode, a framework that compiles a natural language prompt into an executable, code-driven indoor world rather than a collection of opaque meshes. A room-level agentic backbone first turns the prompt into a structured house layout and emits per-object AssetRequests through a planner--designer--critic loop. Each request is then routed to one of five code-generation strategies and converted into a synthesized part-wise Blender Python programs that are validated through an execution-guided repair-and-refine loop. The resulting programs are compiled into simulation-ready assets, and exported as SDF for physics simulation. A persistent scene-state registry links object requests, executable programs, rendered geometry, and simulation assets, turning scene assembly into a traceable and locally editable world-building process. We evaluate SceneCode across scene-level synthesis, object-level asset quality, human judgment, and downstream robot interaction. Results show that executable world programs improve prompt-faithful indoor scene generation and produce assets with cleaner mesh structure, and simulator-loadable articulation metadata. Project page: https://scene-code.github.io/.

---

## 论文详细总结（自动生成）

# SceneCode: 针对包含关节物体的可编辑室内场景的可执行世界程序

### 1. 论文的核心问题与整体含义（研究动机和背景）
室内场景合成是具身智能（Embodied AI）和机器人仿真的基础。现有的生成流程存在两个主要瓶颈：
*   **静态表示限制**：大多数方法生成的是静态、不透明的 3D 网格，缺乏内部结构和物理交互逻辑。
*   **关节物体依赖性**：现有的可交互物体（如能开门的柜子、能拉抽屉的桌子）通常只能从预定义的资产库中检索，无法根据用户需求（如“一个带三个抽屉的橡木床头柜”）进行定制化生成。

**SceneCode** 的核心动机是将场景合成视为**程序化世界生成**。它不直接生成网格，而是将自然语言提示词编译为可执行的 Blender Python 代码。这种方法使场景具备了极高的可编辑性、可追溯性，并能按需生成具有复杂关节结构的物体。

### 2. 论文提出的方法论
SceneCode 采用了一种分层的代理架构，将场景合成分为房间级规划和物体级生成：

*   **核心思想**：利用视觉语言模型（VLM）的推理和编程能力，将场景转化为一套可执行的代码库和持久化的状态注册表。
*   **关键技术流程**：
    1.  **房间级智能体（Room-Level Agent）**：通过“规划者-设计者-评论者”循环，将文本提示转化为结构化的房屋布局和一系列物体请求（AssetRequests）。
    2.  **策略路由（Strategy Routing）**：根据物体类型（如墙面艺术、静态家具、关节物体等）将请求分发至 5 种专门的代码生成策略。
    3.  **ObjectPlan 构建与验证**：先生成物体的结构化计划（包含部件、基元类型、材质、对称性等），经过规则和 LLM 检查后再进入代码合成阶段。
    4.  **执行引导的修复与完善（Execution-Guided Loop）**：在无头 Blender 环境中运行生成的 Python 代码。若运行失败则利用 Traceback 自动修复（最多 3 次）；若运行成功则由评论者代理根据渲染图提出完善建议（最多 2 次）。
    5.  **物理编译**：将生成的视觉网格转化为支持物理仿真的 SDF 和 URDF 格式，自动推断关节类型（旋转或平移）、轴向和运动范围。
    6.  **持久化状态注册表**：通过 `house_state.json` 链接所有代码、网格和物理参数，支持局部重新生成和参数化编辑。

### 3. 实验设计
*   **数据集/场景**：使用了 **SceneEval-100** 中的 30 个自然语言提示词，涵盖卧室、客厅、餐厅、厨房、地下室和浴室 6 类房间，难度从简单到困难不等。
*   **对比方法**：
    *   **场景级**：对比了 SceneSmith（最新的代理式仿真场景生成）、HSM（层次化场景基元）、LayoutVLM（布局优化模型）。
    *   **物体级**：对比了 SAM 3D Objects（图像转 3D 模型）。
*   **评估指标**：
    *   **语义一致性**：物体计数（CNT）、属性准确度（ATR）。
    *   **物理可用性**：导航性（NAV）、碰撞率（COL）、越界率（OOB）等。
    *   **资产质量**：材质槽数量、PBR 通道覆盖率、非流形边缘数、UV 岛数量等。
    *   **人工评测**：9 名参与者对提示词忠实度和真实感进行评分。

### 4. 资源与算力
*   **算力说明**：论文未明确指出运行 Blender 和 VLM 代理的具体 GPU 型号和数量。
*   **成本统计**（附录 H）：
    *   **货币成本**：平均每个场景的 API Token 费用约为 **21.73 美元**。
    *   **时间成本**：平均生成一个场景需要 **7 小时 25 分钟**（包含多次迭代修复），最长耗时近 17 小时。这表明该方法目前更偏向于高质量离线生成，而非实时交互。

### 5. 实验数量与充分性
*   **实验规模**：针对 30 个复杂场景进行了详尽的端到端测试，并对生成的物体进行了独立的几何质量分析。
*   **消融实验**：验证了“路由策略”对生成质量的影响，证明了特定类别的提示词设计能显著减少几何畸变。
*   **充分性评价**：实验设计较为全面，不仅涵盖了自动化的语义和物理指标，还通过 MuJoCo 仿真演示了机器人与生成物体的交互。对比基线涵盖了当前最前沿的同类工作，实验结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **语义忠实度最高**：SceneCode 在物体计数和属性匹配（ATR）上显著优于所有基线，因为它直接在代码层面定义属性，而非从有限的库中检索。
*   **资产质量更优**：相比于图像转 3D 方法，代码生成的物体具有更清晰的网格拓扑（0 个非流形边缘）、更合理的 UV 布局（UV 岛数量减少 4.4 倍）和更轻量级的面数。
*   **物理仿真就绪**：生成的关节物体可以直接导入 MuJoCo 进行接触式机器人操作，证明了其在具身 AI 训练中的实用价值。
*   **可编辑性强**：通过修改 Python 程序中的参数（如抽屉数量、尺寸），可以轻松实现物体的局部更新，而无需重新生成整个场景。

### 7. 优点
*   **创新的表示法**：将 3D 场景表示为可执行代码，解决了静态网格难以编辑和追溯的问题。
*   **按需生成关节**：打破了对关节物体数据集（如 PartNet-Mobility）的依赖，能够根据描述生成任意结构的交互式资产。
*   **闭环验证**：引入执行引导的修复机制，极大地提高了 VLM 生成代码的鲁棒性和成功率。
*   **结构化输出**：生成的资产具有天然的语义分割，非常适合下游的感知和操纵任务。

### 8. 不足与局限
*   **生成效率低**：由于涉及多次 VLM 调用和 Blender 渲染验证，生成速度较慢，难以满足实时应用需求。
*   **视觉细节限制**：基于几何基元（Primitive-based）的代码生成在表现极其复杂的有机形状（如复杂的雕刻或植物细节）时，可能不如神经渲染或高精细度扫描模型。
*   **场景范围**：目前主要集中在室内环境，对于室外大规模场景或地形复杂的环境，其代码生成策略可能需要进一步扩展。
*   **成本较高**：依赖高性能 VLM API，大规模生成场景的 Token 费用不菲。

（完）
