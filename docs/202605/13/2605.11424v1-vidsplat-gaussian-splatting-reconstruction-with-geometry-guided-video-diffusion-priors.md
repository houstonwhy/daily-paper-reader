---
title: "VidSplat: Gaussian Splatting Reconstruction with Geometry-Guided Video Diffusion Priors"
title_zh: VidSplat：利用几何引导视频扩散先验的高斯泼溅重建
authors: "Jimin Tang, Wenyuan Zhang, Junsheng Zhou, Zian Huang, Kanle Shi, Shenkun Xu, Yu-Shen Liu, Zhizhong Han"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11424v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 完整3D场景的生成式重建
tldr: VidSplat 针对高斯泼溅在稀疏视图下重建效果差、难以推断未观测区域的问题，提出了一种无需训练的生成式重建框架。该框架利用视频扩散先验，通过几何引导的去噪策略和迭代式新视角合成机制，能够从极少数甚至单张图像中恢复完整的 3D 场景，显著提升了稀疏视图下的重建质量和一致性。
selection_source: fresh_fetch
motivation: 现有的高斯泼溅方法在稀疏视图下难以推断被遮挡或未观测区域，导致重建场景不完整且缺乏一致性。
method: 利用视频扩散先验，结合几何引导的阶段性去噪策略和迭代式视角探索机制，通过置信度加权精细化来补充训练数据。
result: 在多个基准测试中表现优异，能够从极稀疏输入甚至单张图像中实现鲁棒且高质量的 3D 场景重建。
conclusion: VidSplat 证明了将视频生成先验与几何约束相结合，是解决稀疏视图 3D 重建难题并实现完整场景恢复的有效途径。
---

## 摘要
高斯泼溅（Gaussian Splatting）在多视图表面重建方面取得了显著进展，但在仅有少量视图可用时，其性能会出现明显下降。尽管最近的研究通过增强多视图一致性来生成合理的表面，从而缓解了这一问题，但它们仍难以推断输入覆盖范围之外的未见、遮挡或弱约束区域。为了解决这一局限性，我们提出了 VidSplat，这是一个无需训练的生成式重建框架。它利用强大的视频扩散先验迭代地合成新视图，以补偿缺失的输入覆盖，从而从稀疏输入中恢复完整的 3D 场景。具体而言，我们解决了实现生成与重建有效集成的两个关键挑战。首先，为了实现 3D 一致性生成，我们设计了一种无需训练的分阶段去噪策略，该策略利用渲染的 RGB 和掩码图像，自适应地引导去噪方向趋向底层几何结构。其次，为了增强重建效果，我们开发了一种迭代机制，通过采样相机轨迹、探索未观测区域、合成新视图，并通过置信度加权细化来补充训练。VidSplat 对稀疏输入甚至单张图像都表现出鲁棒性。在广泛使用的基准测试上进行的广泛实验证明，我们在稀疏视图场景重建方面具有优越的性能。

## Abstract
Gaussian Splatting has achieved remarkable progress in multi-view surface reconstruction, yet it exhibits notable degradation when only few views are available. Although recent efforts alleviate this issue by enhancing multi-view consistency to produce plausible surfaces, they struggle to infer unseen, occluded, or weakly constrained regions beyond the input coverage. To address this limitation, we present VidSplat, a training-free generative reconstruction framework that leverages powerful video diffusion priors to iteratively synthesize novel views that compensate for missing input coverage, and thereby recover complete 3D scenes from sparse inputs. Specifically, we tackle two key challenges that enable the effective integration of generation and reconstruction. First, for 3D consistent generation, we elaborate a training-free, stage-wise denoising strategy that adaptively guides the denoising direction toward the underlying geometry using the rendered RGB and mask images. Second, to enhance the reconstruction, we develop an iterative mechanism that samples camera trajectories, explores unobserved regions, synthesizes novel views, and supplements training through confidence weighted refinement. VidSplat performs robustly to sparse input and even a single image. Extensive experiments on widely used benchmarks demonstrate our superior performance in sparse-view scene reconstruction.

---

## 论文详细总结（自动生成）

这是一份关于论文《VidSplat: Gaussian Splatting Reconstruction with Geometry-Guided Video Diffusion Priors》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3D 高斯泼溅（3DGS）在多视图重建中表现优异，但在**稀疏视图（Sparse-view）**输入下会严重退化。
*   **研究动机**：
    *   现有的稀疏视图重建方法主要依赖多视图一致性，难以推断输入视角之外的未见区域或被遮挡区域，导致重建结果不完整。
    *   虽然生成式先验（如图像扩散模型）被引入，但往往缺乏 3D 几何一致性，导致渲染图像虽然好看但底层几何结构破碎。
    *   **VidSplat** 旨在利用预训练视频扩散模型（VDM）的强大先验，通过几何引导的方式合成一致的新视角，从而实现从极少数视角（甚至单图）恢复完整的 3D 场景。

### 2. 方法论
VidSplat 是一个**无需训练（Training-free）**的生成式重建框架，其核心思想是将视频生成迭代地嵌入到 3DGS 重建循环中。

*   **核心技术细节**：
    1.  **可见性感知相机采样（Visibility-Based Sampling）**：在球面上采样相机轨迹，通过评估深度和掩码来选择能够覆盖更多未见区域且不被遮挡（如墙壁）的有效路径。
    2.  **几何引导的视频生成（Geometry-Guided Generation）**：
        *   利用 VDM（如 Wan2.1）生成视频，但通过**分阶段去噪策略**解决内容漂移问题。
        *   **公式逻辑**：在去噪过程中，将渲染的 RGB 和掩码作为参考。
        *   **三阶段控制**：
            *   *早期（高噪声）*：严格遵循渲染参考，锚定场景全局结构。
            *   *中期*：逐渐放松约束，允许模型推断缺失区域。
            *   *后期（低噪声）*：完全释放约束，精细化局部细节并修复渲染伪影。
    3.  **迭代优化框架**：
        *   **初始化阶段**：利用 DUSt3R 生成初始点云，通过 VDM 补全缺失视角，构建更密集的初始点云。
        *   **训练阶段**：在 3DGS 优化过程中，定期采样新轨迹并生成视频，将生成的关键帧作为伪标签（置信度加权）加入训练集，并利用单目深度估计反投影新增高斯点。

### 3. 实验设计
*   **数据集**：
    *   **Tanks and Temples (TNT)**：室外大场景，每场景仅 5 个输入视角。
    *   **Replica**：室内场景，每场景 10 个输入视角。
    *   **DL3DV**：包含 4 个室内和 4 个室外场景，每场景 6 个输入视角。
*   **对比方法**：
    *   **稀疏重建类**：FSGS, PGSR, Sparse2DGS, MAtCha, QGS 等。
    *   **生成式合成类**：DIFIX3D+, GuidedVD, FatesGS 等。
    *   **视频生成类**：CamTrol, ViewCrafter, TrajectoryCrafter。
*   **评价指标**：Chamfer Distance (CD), F-Score, Normal Consistency (NC), PSNR, SSIM, LPIPS, FID, FVD。

### 4. 资源与算力
*   **模型骨干**：采用了预训练的 **Wan2.1 I2V** 作为视频扩散模型，重建后端基于 **MAtCha**。
*   **训练参数**：总计训练 15,000 次迭代。从第 7,000 次开始，每 4,000 次迭代执行一次视频生成和视角扩展，共循环两轮。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及总训练时长。但考虑到使用了大型视频扩散模型（Wan2.1）进行多次迭代推理，其计算开销显著高于传统的 3DGS 优化。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了测试，涵盖了室内、室外、物体和场景等多种尺度。
*   **消融实验**：非常充分。分别验证了：
    *   初始化补全与训练补全的必要性。
    *   分阶段去噪策略的有效性（对比了无引导、全引导等情况）。
    *   不同视频扩散模型底座（Wan2.1, Hunyuan, SVD）的鲁棒性。
    *   曝光一致性预处理（BracketDiffusion）的影响。
*   **客观性**：通过定量指标和定性视觉对比展示了其在“补全未见区域”方面的压倒性优势，实验设计较为公平。

### 6. 主要结论与发现
*   **性能领先**：VidSplat 在表面重建精度（F-Score）和新视角合成质量（PSNR）上均显著优于现有 SOTA 方法。
*   **完整性突破**：相比于只能重建可见区域的传统方法，VidSplat 能够生成完整的、闭合的场景表面。
*   **泛化能力**：该框架具有极强的通用性，甚至可以从**单张图像**出发，通过迭代探索生成全景 3D 场景。
*   **几何一致性**：通过几何引导去噪，生成的视频在 3D 空间上比纯生成模型更具一致性。

### 7. 优点与亮点
*   **无需训练**：直接利用现有的视频大模型先验，无需在特定 3D 数据集上微调。
*   **分阶段去噪**：巧妙地平衡了“遵循已有几何”与“生成未知内容”之间的矛盾。
*   **可见性采样**：主动探索场景的策略比随机采样或固定轨迹采样更高效。
*   **应用广泛**：不仅限于稀疏视图修复，还展示了在单图生成 3D 场景方面的潜力。

### 8. 不足与局限
*   **计算成本**：迭代调用视频扩散模型进行推理会大幅增加重建时间，难以达到实时性。
*   **幻觉风险**：虽然有几何引导，但在极度缺乏信息的情况下，视频模型仍可能产生不符合物理真实的“幻觉”内容（如视频末尾帧的伪影）。
*   **深度依赖**：反投影新增高斯点依赖于单目深度估计的准确性，若深度估计偏差过大，可能导致几何对齐困难。
*   **硬件门槛**：运行 Wan2.1 等大型视频模型需要较高的显存支持。

（完）
