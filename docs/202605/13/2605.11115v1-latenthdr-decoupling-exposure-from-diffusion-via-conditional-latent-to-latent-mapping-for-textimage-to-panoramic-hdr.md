---
title: "LatentHDR: Decoupling Exposure from Diffusion via Conditional Latent-to-Latent Mapping for Text/Image-to-Panoramic HDR"
title_zh: LatentHDR：通过条件潜空间到潜空间映射实现曝光与扩散解耦，用于文本/图像驱动的全景 HDR 生成
authors: "Pedram Fekri, WenChen Li, William Chen, Peter Altamirano"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11115v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 文本/图像到全景HDR场景生成
tldr: 针对生成模型在HDR生成中存在的计算成本高和曝光不一致问题，本文提出LatentHDR框架。该框架将场景生成与曝光建模解耦，利用预训练扩散模型生成场景表示，再通过轻量级潜空间映射头生成特定曝光的表示。该方法实现了单次推理生成结构一致的曝光堆栈，支持文本或图像引导的全景HDR合成，在提升动态范围的同时显著降低了计算开销。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的HDR生成方法通常需要多次采样不同曝光，导致计算量大且不同曝光间结构不一致。
method: 提出一种解耦架构，使用预训练扩散模型生成单一场景表示，并配合轻量级条件潜空间映射头实现确定性的曝光转换。
result: 在SI-HDR基准测试中达到了SOTA的动态范围和竞争力的感知质量，同时计算量减少了一个数量级。
conclusion: 该研究证明了通过结构化潜空间建模可以实现高质量HDR生成，无需复杂的随机多曝光采样。
---

## 摘要
高动态范围（HDR）生成对于生成模型而言仍具挑战性，因为这些模型很大程度上局限于低动态范围输出。最近基于扩散的方法通过生成多个受曝光条件的样本来近似 HDR，这导致了高昂的计算成本以及不同曝光之间的结构不一致。我们提出了 LatentHDR，这是一个在潜空间中将场景生成与曝光建模解耦的框架。预训练的扩散主干网络产生一个单一且连贯的场景表示，而一个轻量级的条件潜空间到潜空间（latent-to-latent）头部则将其确定性地映射到特定曝光的表示。这使得在单次推理中即可生成密集且结构一致的曝光堆栈。该设计消除了多轮扩散过程，确保了跨曝光的对齐，并实现了可扩展的 HDR 合成。LatentHDR 支持透视和全景场景的文本及图像条件 HDR 生成。在合成数据和 SI-HDR 基准测试上的实验表明，LatentHDR 在达到最先进动态范围的同时，具备极具竞争力的感知质量，并将计算量降低了一个数量级。我们的结果证明，通过结构化潜空间建模可以实现高质量的 HDR 生成，挑战了对随机多曝光生成的必要性。

## Abstract
High Dynamic Range (HDR) generation remains challenging for generative models, which are largely limited to low dynamic range outputs. Recent diffusionbased approaches approximate HDR by generating multiple exposure-conditioned samples, incurring high computational cost and structural inconsistencies across exposures. We propose LatentHDR, a framework that decouples scene generation from exposure modeling in latent space. A pretrained diffusion backbone produces a single coherent scene representation, while a lightweight conditional latent to-latent head deterministically maps it to exposure-specific representations. This enables the generation of a dense, structurally consistent exposure stack in a single pass. This design eliminates multi-pass diffusion, ensures cross-exposure alignment, and enables scalable HDR synthesis. LatentHDR supports both textand image-conditioned HDR generation for perspective and panoramic scenes. Experiments on synthetic data and the SI-HDR benchmark show that LatentHDR achieves state-of-the-art dynamic range with competitive perceptual quality, while reducing computation by an order of magnitude. Our results demonstrate that high-quality HDR generation can be achieved through structured latent modeling, challenging the need for stochastic multi-exposure generation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **LatentHDR** 的框架，旨在解决生成模型在合成高动态范围（HDR）图像时面临的效率与一致性挑战。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的扩散模型（Diffusion Models）大多局限于生成低动态范围（LDR）图像。虽然近期有研究通过生成多个不同曝光条件的样本来近似 HDR，但存在两大痛点：
    1.  **计算成本高**：需要多次去噪过程（$O(N)$ 复杂度），生成速度慢。
    2.  **结构不一致**：独立生成的不同曝光图像之间往往存在微小的结构漂移，导致最终融合的 HDR 图像出现伪影。
*   **研究动机**：作者认为曝光变化在物理上是场景辐射度的结构化变换，而非随机生成过程。因此，应该将“场景内容生成”与“曝光建模”解耦，以实现高效且结构一致的 HDR 合成。

### 2. 方法论
*   **核心思想**：将 HDR 合成重新表述为**潜空间分解问题**。首先生成一个共享的场景潜变量（Anchor），然后通过一个确定性的映射函数将其转换为不同曝光下的潜变量。
*   **关键技术细节**：
    *   **解耦架构**：使用预训练的扩散主干（如 FLUX.1）生成单一的场景潜变量 $z_{x_{base}}$。
    *   **确定性潜空间映射**：设计了一个轻量级的 **FiLM 条件 U-Net** 头部。该头部接收场景潜变量和目标曝光值（EV）作为输入，预测曝光残差：$z_{e_i} = z_{x_{base}} + f_\theta(z_{x_{base}}, \phi(e_i))$。
    *   **潜空间分析**：通过对 VAE 后验分布的实证分析，证明了 VAE 潜空间具有近乎确定性的性质，支持使用确定性映射来模拟曝光轨迹。
    *   **辐射度重建**：将生成的潜变量堆栈解码为像素空间，通过伽马扩展线性化，并使用加权对数域积分算法融合为最终的 HDR 辐射图。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：来自 Poly Haven 的 954 张全景 HDR 图像，生成了约 23,850 个 LDR 曝光样本。
    *   **测试集**：SI-HDR 基准测试（186 个场景）用于有参考评估；自定义合成数据集（300 张透视，300 张全景）用于无参考评估。
*   **对比方法**：
    *   **传统/深度学习方法**：HDRCNN, MaskHDR, SingleHDR, ExpandNet。
    *   **基于扩散的方法**：LEDiff, Bracket Diffusion (BD-Glide, BD-DPS)。

### 4. 资源与算力
*   **硬件**：单台 **NVIDIA RTX PRO 6000 GPU**。
*   **训练细节**：
    *   主干网络冻结，仅训练曝光头部和全景 LoRA。
    *   训练时长：19,080 步（约 20 个 Epoch），Batch Size 为 1。
    *   推理：文本到 HDR 模式下仅需 28 步扩散采样生成基础潜变量，随后曝光映射几乎是瞬时的。
*   **效率提升**：相比于需要多次扩散运行的方法，LatentHDR 将计算复杂度从 $O(N)$ 降至 $O(1)$，延迟和内存占用降低了一个数量级。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了文本转 HDR（t2h）和图像转 HDR（l2h）两种模式，并在透视和全景两种投影格式下进行了测试。
*   **消融实验**：作者对曝光堆栈的范围与密度、潜变量采样方式（均值 vs 随机采样）、以及 EV 条件机制进行了详细的消融研究。
*   **评价指标**：使用了动态范围（Stops）、PU21-PIQE（感知质量）、FID、HDR-VDP3 等多维度指标。
*   **充分性评价**：实验设计较为全面，通过跨数据集验证（SI-HDR 和合成数据）展示了模型的泛化能力，对比实验考虑了后处理（Blending）对结果的影响，具有较高的客观性。

### 6. 主要结论与发现
*   **性能领先**：LatentHDR 在动态范围上达到了 SOTA（约 11-13 Stops），同时保持了极具竞争力的感知质量。
*   **结构一致性**：由于所有曝光均源自同一个场景潜变量，生成的曝光堆栈在结构上完美对齐，避免了传统生成方法的漂移问题。
*   **确定性优势**：实验证明曝光变化在潜空间中遵循平滑且单调的轨迹，验证了确定性映射的可行性。

### 7. 优点
*   **高效性**：单次扩散即可生成完整的曝光堆栈，极大地提升了 HDR 生成的实用性。
*   **兼容性**：曝光模块独立于扩散主干，可以无缝集成到现有的 Stable Diffusion 或 FLUX 生态系统中，支持各种 LoRA 适配器。
*   **物理合理性**：通过解耦随机生成与确定性变换，更符合图像形成的物理过程。

### 8. 不足与局限
*   **极端饱和挑战**：在极端过曝（如窗户区域完全饱和）的情况下，确定性映射可能会出现过度平滑的现象，缺乏高频细节的恢复。
*   **数据依赖**：训练集规模相对较小（约 1k 场景），可能限制了模型在极端光照环境下的多样性表现。
*   **VAE 瓶颈**：模型的表现受限于预训练 VAE 的重建质量，如果 VAE 在压缩过程中丢失了关键辐射信息，曝光头部也难以完全找回。

（完）
