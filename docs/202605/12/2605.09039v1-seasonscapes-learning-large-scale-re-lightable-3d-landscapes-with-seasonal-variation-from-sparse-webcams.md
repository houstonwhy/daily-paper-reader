---
title: "SeasonScapes: Learning Large-scale Re-lightable 3D Landscapes with Seasonal Variation from Sparse Webcams"
title_zh: SeasonScapes：利用稀疏网络摄像头学习具有季节性变化的大规模可重照明 3D 景观
authors: "Timo Kleger, Qi Ma, Deheng Zhang, Luc Van Gool, Danda Pani Paudel"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09039v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 使用扩散模型构建季节性3D景观
tldr: 本研究提出SeasonScapes框架及包含8.5万张瑞士山脉图像的大规模数据集，旨在解决大范围3D景观在不同季节下的外观变化建模问题。通过将稀疏网络摄像头捕获的多时相图像投影到3D网格，并利用条件扩散模型修复遮挡与缺失数据，该方法成功构建了可随季节变化且支持物理重光照的3D景观模型，为大规模自然场景的动态重建提供了新方案。
selection_source: fresh_fetch
motivation: 传统3D场景重建难以捕捉大尺度地理空间随季节变化的动态外观，且缺乏有效的重光照能力。
method: 将不同时间点的图像投影至3D网格，并采用条件扩散模型在网格上进行图像引导修复，最后结合物理渲染器实现重光照。
result: 成功构建了覆盖50x60公里范围的季节性3D景观，并利用包含32个地点、13个时间节点的稀疏数据集实现了高质量重建。
conclusion: SeasonScapes为利用稀疏网络摄像头数据进行大规模、可重光照且具备季节动态的3D场景建模开辟了新路径。
---

## 摘要
我们介绍了 SeasonScapes 框架和 SeasonScapes 数据集：具有季节性变化的瑞士稀疏视角山地场景。该数据集覆盖范围超过 50 km x 60 km，由一年中 13 个时间点、32 个不同地点拍摄的 85,000 多张网络摄像头图像组成。通过将这些特定时间点的图像投影到 3D 网格上，我们构建了能够反映随时间变化的自然外观的季节性 3D 景观。为了解决遮挡和数据缺失问题，我们利用条件扩散模型直接在网格上进行图像引导的修复。生成的完整网格可以使用标准的基于物理的渲染器进行进一步的重照明。

## Abstract
We introduce SeasonScapes framework and a the SeasonScapes dataset: Swiss Sparse-view Mountain Scenes with Seasonal Changes that covers over 50 km x 60 km, composed of more than 85,000 webcam images captured from 32 different locations across 13 timestamps throughout a full year. By projecting these timestamp-specific images onto a 3D mesh, we construct seasonal 3D landscapes that reflect natural appearance changes over time. To address occlusions and missing data, we leverage conditional diffusion models for image-guided inpainting directly on the mesh. The resulting completed meshes can be further relighted using standard physically-based renderer.

---

## 论文详细总结（自动生成）

以下是对论文《SeasonScapes: Learning Large-scale Re-lightable 3D Landscapes with Seasonal Variation from Sparse Webcams》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在超大规模（数千平方公里）且无约束的自然环境下，利用分布稀疏的网络摄像头（Webcam）图像，重建出具有**季节性变化**和**可重照明（Relightable）**能力的 3D 景观模型。
*   **研究动机**：
    *   现有的 3D 重建技术（如 NeRF, 3DGS）多集中于静态、小尺度或城市环境，难以处理自然景观中剧烈的光照、天气和季节变化。
    *   传统地图系统（如 Google Earth）依赖静态卫星贴图，缺乏对昼夜循环和季节更替的动态表现力。
    *   稀疏视角下的遮挡和数据缺失导致大规模场景重建极具挑战。

### 2. 方法论：核心思想与关键技术
该框架通过结合几何先验、扩散模型和动态高斯泼溅技术，实现从 2D 稀疏图像到 4D（空间+时间）场景的转化。
*   **数据预处理与对齐**：
    *   利用 Google Earth Engine 获取低分辨率数字高程模型（DEM）和卫星图像，构建基础 3D 网格（Mesh）。
    *   **相机参数优化**：通过手动建立 2D-3D 对应点，最小化投影像素 L1 损失，校准网络摄像头的位姿和内参。
*   **UV 空间纹理化与修复（核心流程）**：
    *   **投影贴图**：将预处理后的摄像头图像投影到 3D 网格的 UV 贴图上。
    *   **迭代修复（Iterative Inpainting）**：针对未覆盖区域，沿预设轨迹渲染深度图和遮罩，利用**深度条件扩散模型（ControlNet）**、**IP-Adapter（图像提示）**和文本提示进行图像引导修复。
    *   **UV 融合**：将修复后的图像重新投影回 UV 空间，通过多视角融合生成完整的季节性纹理。
*   **时间嵌入与 Wild Gaussian 训练**：
    *   使用修复后的稠密视图训练 **WildGS（Wild Gaussian Splatting）** 模型。
    *   引入**外观 MLP** 和**时间嵌入（Time Embedding）**，通过仿射变换调整高斯基元的颜色，从而模拟不同时间点和季节的外观。

### 3. 实验设计
*   **数据集**：自研 **SeasonScapes 数据集**，覆盖瑞士伯尔尼高地 3,000 $km^2$ 区域，包含 32 个地点、13 个时间节点、超过 85,000 张图像，跨越一整年。
*   **实验场景**：
    1.  **Grindelwald**：小规模单山峰场景。
    2.  **Jungfrau**：中等规模，包含多个山峰和山谷。
    3.  **Bernese Highlands**：大规模区域（21 km × 36.5 km）。
*   **Benchmark 与对比**：
    *   **指标**：LPIPS（感知相似度）、PSNR（峰值信噪比）、SSIM（结构相似性）。
    *   **对比对象**：与 Google Earth 的渲染效果进行定性对比；针对 ControlNet 的各种组件（Prompt, Depth, Mask, IP-Adapter）进行消融实验。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及训练时长。
*   **实现细节**：提到使用了 `xatlas` 进行 UV 展开，`Tkinter` 开发了手动标注工具，代码已在 GitHub 开源。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同尺度的场景上进行了验证，涵盖了从单点到区域级的跨度。
*   **消融实验**：针对 6 种配置（全功能、无提示词、无深度控制、无遮罩、无 IP-Adapter、无初始投影）进行了详细对比，验证了各模块的必要性。
*   **充分性评价**：实验设计较为客观，通过 LPIPS 重点评估了生成内容的感知质量（而非单纯的像素拟合），这对于包含大量生成内容的修复任务是公平的。但由于依赖手动校准，实验的自动化程度和在大规模推广上的效率评估略显不足。

### 6. 主要结论与发现
*   **季节性建模成功**：该框架能有效捕捉从夏季绿地到冬季积雪的景观转变，并支持平滑的昼夜光照切换。
*   **扩散模型的作用**：深度引导的扩散模型在填充大规模自然场景的缺失区域时表现优异，生成的纹理在视觉上与真实地理特征高度一致。
*   **优于传统系统**：在处理陡峭悬崖等垂直地形时，该方法生成的纹理质量显著优于 Google Earth 的卫星俯视投影。

### 7. 优点（亮点）
*   **时空跨度大**：成功处理了 3,000 $km^2$ 级别的 4D 场景重建，这在现有文献中较为罕见。
*   **多源数据融合**：巧妙结合了低精度 DEM、卫星图和高精度实时摄像头图像，实现了“宏观结构+微观细节”的统一。
*   **鲁棒的修复策略**：引入 IP-Adapter 保持了生成内容与原始摄像头图像在色调和风格上的一致性。

### 8. 不足与局限
*   **人工依赖**：2D-3D 对应点的建立仍需手动操作，限制了系统向全球规模扩展的自动化能力。
*   **局部细节瑕疵**：ControlNet 在修复极小区域时偶尔失效；Wild Gaussian 输出在某些视角下存在模糊（Blurry）现象。
*   **校准误差**：由于地形复杂和 GPS 精度限制，残余的相机校准误差会导致渲染结果出现伪影。
*   **应用限制**：目前主要针对远景山地景观，对于包含大量植被细节或人工建筑的近景处理能力尚待验证。

（完）
