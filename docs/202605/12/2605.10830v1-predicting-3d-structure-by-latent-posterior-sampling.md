---
title: Predicting 3D structure by latent posterior sampling
title_zh: 通过潜空间后验采样预测三维结构
authors: "Azmi Haider, Dan Rosenbaum"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10830v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 使用扩散模型和NeRF的3D场景生成模型
tldr: 该研究结合了NeRF的3D表示能力与扩散模型的概率建模优势，将3D重建视为具有不确定性的感知问题。通过将3D场景表示为随机隐变量，并利用扩散模型学习其先验，实现了基于后验采样的3D结构预测。该方法在处理单视图、多视图、噪声图像及稀疏深度等多种观测数据时表现出色，能有效建模重建过程中的不确定性并生成高质量的3D结构。
selection_source: fresh_fetch
motivation: 旨在解决3D重建中固有的不确定性问题，通过结合生成模型与神经场表示来提升重建的鲁棒性和准确性。
method: 采用两阶段训练法，先训练基于NeRF的隐变量重建模型，再利用扩散模型学习隐空间先验，并通过得分推理进行后验采样。
result: 实验证明该方法能从单视图、多视图、噪声图像及稀疏像素或深度等多样化且不完整的观测中准确预测3D结构。
conclusion: 该方法提供了一个通用的概率推理框架，能够灵活处理不同信息丰度的观测数据并有效量化3D重建中的不确定性。
---

## 摘要
二维图像生成模型和三维场景神经场表示所取得的显著成就，为整合这两种方法的优势提供了一个极具吸引力的机会。在这项工作中，我们提出了一种将基于 NeRF 的三维场景表示与使用扩散模型的概率建模和推理相结合的方法。我们将三维重建视为一个具有固有不确定性的感知问题，因此可以从概率推理方法中获益。其核心思想是将三维场景表示为一个随机潜变量，我们可以学习其先验分布，并在给定一组观测值的情况下利用它进行后验推理。我们利用扩散模型的基于分数的推理方法，结合从包含体渲染的重建模型中计算出的似然项，来构建后验采样。我们采用两阶段过程训练模型：首先在对三维场景数据集的潜表示进行自动解码的同时训练重建模型，然后使用扩散模型训练潜变量的先验。通过使用该模型从后验分布中生成样本，我们证明了可以执行各种三维重建任务，这些任务因作为输入的观测类型而异。我们展示了基于单视图、多视图、噪声图像、稀疏像素和稀疏深度数据的重建。这些观测值在为场景提供的信息量方面各不相同，我们展示了我们的方法能够对与每项任务相关的不同程度的固有不确定性进行建模。我们的实验表明，这种方法提供了一种全面的手段，能够从多种类型的观测中准确预测三维结构。

## Abstract
The remarkable achievements of both generative models of 2D images and neural field representations for 3D scenes present a compelling opportunity to integrate the strengths of both approaches. In this work, we propose a methodology that combines a NeRF-based representation of 3D scenes with probabilistic modeling and reasoning using diffusion models. We view 3D reconstruction as a perception problem with inherent uncertainty that can thereby benefit from probabilistic inference methods. The core idea is to represent the 3D scene as a stochastic latent variable for which we can learn a prior and use it to perform posterior inference given a set of observations. We formulate posterior sampling using the score-based inference method of diffusion models in conjunction with a likelihood term computed from a reconstruction model that includes volumetric rendering. We train the model using a two-stage process: first we train the reconstruction model while auto-decoding the latent representations for a dataset of 3D scenes, and then we train the prior over the latents using a diffusion model. By using the model to generate samples from the posterior we demonstrate that various 3D reconstruction tasks can be performed, differing by the type of observation used as inputs. We showcase reconstruction from single-view, multi-view, noisy images, sparse pixels, and sparse depth data. These observations vary in the amount of information they provide for the scene and we show that our method can model the varying levels of inherent uncertainty associated with each task. Our experiments illustrate that this approach yields a comprehensive method capable of accurately predicting 3D structure from diverse types of observations.

---

## 论文详细总结（自动生成）

这篇论文《Predicting 3D structure by latent posterior sampling》提出了一种结合神经辐射场（NeRF）与扩散模型（Diffusion Models）的概率框架，旨在解决三维重建中固有的不确定性问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：三维重建（从有限观测推断3D结构）本质上是一个“病态问题”（ill-posed problem），即同一组观测可能对应多种合理的3D解释。
*   **核心问题**：现有的3D重建方法大多提供单一的确定性预测，难以量化不确定性。作者希望将3D重建视为一个**概率推理问题**，通过结合强大的生成先验（扩散模型）和物理渲染约束（NeRF），实现对三维场景后验分布的采样。

### 2. 方法论
核心思想是将3D场景表示为压缩的隐变量，并利用扩散模型在隐空间进行后验推理。
*   **两阶段训练流程**：
    1.  **重建模型（RM）与自动解码**：使用三平面（Tri-plane）作为隐表示。在训练集上，通过自动解码（Auto-decoding）同时优化共享的解码器网络和每个场景特有的隐向量 $z$。解码器将 $z$ 映射为三平面，再通过体渲染生成图像。
    2.  **先验训练**：在第一阶段获得的隐向量数据集上，训练一个基于 U-Net 的扩散模型，学习 3D 场景隐空间的概率分布 $p(z)$。
*   **后验采样（推理阶段）**：
    *   给定新的观测 $y$（如单张图片或稀疏像素），通过**得分推理（Score-based inference）**进行采样。
    *   采样过程结合了扩散模型的先验得分（指导生成合理的3D结构）和重建模型的似然梯度（指导生成符合观测的结构）。
    *   利用近似的 Langevin 动力学，在每一步迭代中根据观测误差修正隐变量。

### 3. 实验设计
*   **数据集**：
    *   **SRN Cars**：包含 3200 个汽车场景。
    *   **Objaverse-lvis (椅子类)**：包含 439 个椅子实例，形状更为多样。
*   **基准与对比方法**：
    *   **重建精度对比**：与 Functa（隐表示方法）对比 PSNR。
    *   **鲁棒性对比**：在噪声图像重建任务中与 TensoRF（无先验的优化方法）对比。
    *   **3D一致性对比**：与 2D 生成模型（如 3Dim, Zero-1-to-3）对比，通过生成的视图训练 NeRF 并测量其 PSNR 来评估视图间的一致性。
*   **任务场景**：单视图重建、半图重建、极稀疏像素（5%）重建、稀疏深度数据重建。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA GeForce RTX 4090** GPU。
*   **时长**：重建模型（第一阶段）和扩散模型（第二阶段）各需训练约 **1 天**。
*   **参数**：隐向量维度为 1024，图像分辨率为 128x128。

### 5. 实验数量与充分性
*   **实验规模**：论文针对两个主要数据集进行了多维度的测试，包括不同类型的观测信号（RGB、深度、噪声）。
*   **充分性**：实验设计较为全面，不仅展示了视觉效果，还通过**不确定性图（Uncertainty Maps）**量化了模型在未观测区域的预测波动。
*   **客观性**：通过对比“平均样本”与“后验采样”的区别，客观地指出了单纯追求高 PSNR（平均化效果）会导致结果过度平滑，而采样能保留更多细节。

### 6. 主要结论与发现
*   **隐空间采样的有效性**：通过在压缩的隐空间而非原始 3D 空间运行扩散模型，可以高效地捕捉复杂的 3D 先验。
*   **不确定性量化**：模型能准确识别哪些区域是确定的（如观测到的正面），哪些是高度不确定的（如被遮挡的背面），并给出多种合理的补全方案。
*   **跨模态灵活性**：由于似然项是在推理时定义的，同一个预训练模型可以处理 RGB、深度、甚至极稀疏的像素输入，无需针对特定任务重新训练。

### 7. 优点（亮点）
*   **通用的概率框架**：将 3D 重建统一在后验采样的框架下，解决了传统方法难以处理的不确定性表达问题。
*   **极强的鲁棒性**：在极高噪声（$\sigma=0.8$）或极稀疏观测下，依然能生成连贯的 3D 结构，显著优于无先验的优化方法。
*   **3D 一致性**：相比于基于 2D 扩散模型的 3D 生成，该方法由于直接在 3D 隐空间操作，生成的场景在不同视角下具有天然的几何一致性。

### 8. 不足与局限
*   **采样速度**：受限于扩散模型的迭代本质，推理过程（1000 步迭代）相对缓慢，难以做到实时重建。
*   **场景复杂度**：目前实验主要集中在孤立的目标物体（汽车、椅子），尚未在复杂的室内外大规模场景中验证。
*   **分辨率限制**：受限于隐表示的压缩率和算力，生成的图像分辨率（128x128）较低，精细纹理的恢复能力有限。

（完）
