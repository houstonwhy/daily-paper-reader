---
title: "Pixal3D: Pixel-Aligned 3D Generation from Images"
title_zh: Pixal3D：基于图像的像素对齐 3D 生成
authors: "Dong-Yang Li, Wang Zhao, Yuxin Chen, Wenbo Hu, Meng-Hao Guo, Fang-Lue Zhang, Ying Shan, Shi-Min Hu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10922v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于高保真资产的像素对齐3D生成范式
tldr: Pixal3D 针对图像转 3D 生成中保真度不足的问题，提出了一种像素对齐的生成范式。通过将多尺度图像特征显式反投影到 3D 特征体积中，建立了直接的像素与 3D 关联，从而在保持生成质量的同时显著提升了与输入图像的一致性。该方法不仅支持单视图和多视图生成，还能扩展到复杂场景的 3D 重建，达到了接近重建水平的高保真度。
selection_source: fresh_fetch
motivation: 现有 3D 生成模型因在规范空间生成且依赖注意力机制注入图像信息，导致 2D-3D 对应关系模糊，难以实现像素级的高保真度。
method: 提出像素反投影调节方案，将多尺度图像特征显式提升至 3D 特征体积，实现像素对齐的 3D 原生生成。
result: 实验表明 Pixal3D 在生成质量和保真度上均有显著提升，且能自然扩展至多视图生成和物体分离的场景合成。
conclusion: Pixal3D 首次实现了大规模的 3D 原生像素对齐生成，为从单/多视图图像中获取高保真 3D 资产提供了新路径。
---

## 摘要
近期 3D 生成模型的进展迅速提升了图像到 3D 合成的质量，实现了更高分辨率的几何结构和更逼真的外观。然而，衡量生成的 3D 资产与输入图像在像素级忠实度的“保真度”（fidelity）仍然是一个核心瓶颈。我们认为这源于隐式的 2D-3D 对应问题：大多数原生 3D 生成器在规范空间（canonical space）中合成形状，并通过注意力机制注入图像线索，导致像素到 3D 的关联变得模糊。为了解决这一问题，我们从 3D 重建中汲取灵感，提出了 Pixal3D，这是一种用于从图像创建高保真 3D 资产的像素对齐 3D 生成范式。Pixal3D 不在规范姿态下生成，而是直接以像素对齐的方式生成 3D，并与输入视角保持一致。为实现这一点，我们引入了一种像素反向投影条件方案，显式地将多尺度图像特征提升到 3D 特征体素中，从而建立了无歧义的直接像素到 3D 对应关系。我们证明了 Pixal3D 不仅具有可扩展性且能够生成高质量的 3D 资产，还显著提高了保真度，达到了接近重建水平的保真度。此外，Pixal3D 通过聚合跨视角的反向投影特征体素，自然地扩展到多视角生成。最后，我们展示了像素对齐生成对场景合成的益处，并提出了一个模块化流水线，可以从图像中生成高保真、物体分离的 3D 场景。Pixal3D 首次在大规模上展示了原生 3D 像素对齐生成，并为从单视角或多视角图像进行物体或场景的高保真 3D 生成提供了一种富有启发性的新途径。

## Abstract
Recent advances in 3D generative models have rapidly improved image-to-3D synthesis quality, enabling higher-resolution geometry and more realistic appearance. Yet fidelity, which measures pixel-level faithfulness of the generated 3D asset to the input image, still remains a central bottleneck. We argue this stems from an implicit 2D-3D correspondence issue: most 3D-native generators synthesize shape in canonical space and inject image cues via attention, leaving pixel-to-3D associations ambiguous. To tackle this issue, we draw inspiration from 3D reconstruction and propose Pixal3D, a pixel-aligned 3D generation paradigm for high-fidelity 3D asset creation from images. Instead of generating in a canonical pose, Pixal3D directly generates 3D in a pixel-aligned way, consistent with the input view. To enable this, we introduce a pixel back-projection conditioning scheme that explicitly lifts multi-scale image features into a 3D feature volume, establishing direct pixel-to-3D correspondence without ambiguity. We show that Pixal3D is not only scalable and capable of producing high-quality 3D assets, but also substantially improves fidelity, approaching the fidelity level of reconstruction. Furthermore, Pixal3D naturally extends to multi-view generation by aggregating back-projected feature volumes across views. Finally, we show pixel-aligned generation benefits scene synthesis, and present a modular pipeline that produces high-fidelity, object-separated 3D scenes from images. Pixal3D for the first time demonstrates 3D-native pixel-aligned generation at scale, and provides a new inspiring way towards high-fidelity 3D generation of object or scene from single or multi-view images. Project page: https://ldyang694.github.io/projects/pixal3d/

---

## 论文详细总结（自动生成）

这是一份关于论文《Pixal3D: Pixel-Aligned 3D Generation from Images》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的图像转 3D 生成模型虽然在几何细节和外观上取得了进步，但**保真度（Fidelity）**——即生成的 3D 模型与输入图像在像素级的一致性——仍是瓶颈。
*   **研究动机**：作者认为保真度不足源于**隐式的 2D-3D 对应关系**。大多数模型在“规范空间”（Canonical Space）生成形状，通过注意力机制（Cross-Attention）注入图像特征，这种方式在处理局部细节或重复部件时存在歧义。
*   **整体含义**：借鉴 3D 重建的思路，提出 **Pixal3D** 范式。它直接在相机坐标系下生成像素对齐的 3D 模型，将生成模型的“创造力”与重建算法的“几何严谨性”结合。

### 2. 方法论
*   **核心思想**：放弃规范空间生成，改为**像素对齐生成**。通过显式的几何反投影（Back-projection）将 2D 图像特征提升到 3D 空间，建立确定性的 2D-3D 映射。
*   **关键技术细节**：
    *   **反投影调节方案（Back-projection Conditioning）**：不再使用 Cross-Attention，而是将图像特征图（DINOv2 提取）沿相机射线投影到 3D 体素网格中。每个体素通过采样对应像素的特征来获取条件信号。
    *   **多尺度特征融合**：为了捕捉细微结构，使用特征上采样模型（NAF）将 DINOv2 特征提升至高分辨率，确保低级纹理和高级语义信息同时注入。
    *   **3D 潜空间扩散模型（LDM）**：基于 Direct3D-S2 架构，包含两个阶段：
        1.  **结构生成**：生成粗糙的占用网格。
        2.  **结构化潜变量生成**：在稀疏体素上进行去噪，最后通过 VAE 解码器输出稀疏 SDF，再经由 Marching Cubes 提取网格。
    *   **多视图扩展**：通过对多个视角的反投影特征体积进行平均聚合，实现多视图一致性生成。
    *   **场景生成流水线**：结合 SAM3（分割）、Qwen-image-edit（补全）、Pixal3D（单体生成）和 MoGe（全局深度对齐），实现物体分离的高保真场景合成。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Objaverse 的子集 TRELLIS-500K。
    *   **测试集**：Toys4K（用于定量评估几何精度）和 150 张野外/AI 生成图像（用于定性及用户调研）。
*   **Benchmark 与对比方法**：
    *   **单视图生成**：对比了 TRELLIS、TripoSG、Hunyuan3D-2.1 和 Direct3D-S2。
    *   **多视图生成**：对比了 VGGT 和 TRELLIS（多视图版）。
    *   **场景生成**：对比了 SAM3D。
*   **评估指标**：IoU、PSNR、SSIM、LPIPS，以及法向误差（Mean/Median Angular Error）和用户主观评分。

### 4. 资源与算力
*   **训练细节**：
    *   **稀疏 DiT**：采用由粗到精的策略，分辨率从 256 提升至 1024，总计约 420k 次迭代。
    *   **稠密 DiT**：训练 500k 次迭代，学习率在 1e-4 到 2e-5 之间切换。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量（如 A100/H100 的具体规模），但从迭代次数和模型参数量推断，属于大规模工业级训练规模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（Toys4K 全量测试）。
    *   包含 30 人参与的用户调研，涵盖保真度和质量两个维度。
    *   **消融实验**：验证了“反投影条件”和“特征上采样”的必要性。
*   **充分性评价**：实验设计非常充分。不仅涵盖了单体生成，还扩展到了多视图和复杂的场景级生成，证明了该范式在不同任务下的通用性和鲁棒性。对比实验选择了当前最先进的（SOTA）开源和闭源模型，具有较高的客观性。

### 6. 主要结论与发现
*   **像素对齐是关键**：直接在相机空间生成并使用显式几何对应，能显著提升 3D 模型对输入图像的还原度。
*   **保真度飞跃**：Pixal3D 在法向误差和 IoU 等指标上大幅领先现有模型，达到了接近“重建”算法的水平，同时保持了生成模型的“补全”能力。
*   **可扩展性**：该范式可以无缝支持多视图输入，且视图越多，生成的确定性越强。

### 7. 优点
*   **高保真度**：解决了 3D 生成中长期存在的“长得像但对不上”的问题。
*   **架构解耦**：反投影机制是通用的，可以适配不同的 3D 表征（如 Gaussian Splatting 或 Tri-planes）。
*   **场景处理能力**：提出的模块化流水线规避了复杂的 7 自由度姿态估计，通过像素对齐简化了场景合成。

### 8. 不足与局限
*   **噪声敏感性**：对像素级噪声（如分割边缘不精细）较敏感，可能导致反投影产生细小的几何伪影。
*   **相机参数依赖**：多视图模式下仍依赖已知且准确的相机姿态。
*   **遮挡补全风险**：场景生成依赖 2D 修复（Inpainting）来处理遮挡，在极端复杂的遮挡情况下可能引入 2D 层面的一致性错误。

（完）
