---
title: "SplatWeaver: Learning to Allocate Gaussian Primitives for Generalizable Novel View Synthesis"
title_zh: SplatWeaver：学习为可泛化的新视角合成分配高斯基元
authors: "Yecong Wan, Fan Li, Mingwen Shao, Wangmeng Zuo"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07287v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于场景合成的3D高斯泼溅
tldr: 针对通用新视角合成中固定高斯基元分配导致的效率与细节表现不平衡问题，本文提出 SplatWeaver 框架。该框架通过引入基数高斯专家和像素级路由机制，实现了高斯基元的动态按需分配。结合高频先验引导，SplatWeaver 能在复杂区域增加基元数量，在平滑区域减少冗余，从而以更少的基元实现更高质量的渲染，在多个数据集上超越了现有先进方法。
selection_source: fresh_fetch
motivation: 现有的前馈 3D 高斯喷绘方法采用固定数量的基元分配，无法适应真实场景中空间变化的复杂度，导致资源浪费或细节缺失。
method: 提出一种包含基数高斯专家和像素级路由方案的框架，利用高频先验引导模型为不同区域动态分配 0 到 M 个高斯基元。
result: 实验表明，SplatWeaver 在多种场景下均优于现有最先进方法，能以更少的基元数量提供更真实的新视角渲染效果。
conclusion: 通过学习区域相关的基元分配策略，SplatWeaver 显著提升了通用新视角合成的表达能力与紧凑性。
---

## 摘要
可泛化的新视角合成旨在从未经校准的输入图像中渲染未见视角，而无需针对每个场景进行优化。近期基于 3D 高斯泼溅（3D Gaussian Splatting）的前馈方法在效率和渲染质量方面取得了显著进展。然而，大多数方法为每个像素或体素分配固定数量的高斯分布，忽略了真实场景中随空间变化的复杂性。这种均匀分配往往在平滑区域浪费高斯基元，而在精细结构、复杂几何和高频细节处提供的容量不足。这促使我们预测与区域相关的基元基数，而不是在所有位置施加固定的基元预算，从而实现更具表现力且紧凑的 3D 场景表示。因此，我们提出了 SplatWeaver，这是一个可泛化的新视角合成框架，能够以前馈方式在不同区域动态分配高斯基元。具体而言，SplatWeaver 引入了基数高斯专家（cardinality Gaussian experts）和像素级路由方案，其中每个专家专门生成 0 到 M 之间特定数量的基元，而路由方案协调这些专家，以自适应地确定每个空间位置应分配多少个高斯基元。此外，SplatWeaver 结合了高频先验及其伴随的引导模块和路由正则化，以稳定专家选择并促进感知复杂性的分配。通过利用高频结构线索，路由过程被鼓励为精细结构、复杂几何和纹理区域分配更多的高斯基元，同时抑制平滑区域的冗余基元。在多种场景下的广泛实验表明，SplatWeaver 一贯优于现有最先进的方法，能够以更少的高斯基元提供更真实的新视角渲染效果。

## Abstract
Generalizable novel view synthesis aims to render unseen views from uncalibrated input images without requiring per-scene optimization. Recent feed-forward approaches based on 3D Gaussian Splatting have achieved promising efficiency and rendering quality. However, most of them assign a fixed number of Gaussians to each pixel or voxel, ignoring the spatially varying complexity of real-world scenes. Such uniform allocation often wastes Gaussian primitives in smooth regions while providing insufficient capacity for fine structures, complex geometry, and high-frequency details. This motivates us to predict region-dependent primitive cardinalities rather than impose a fixed primitive budget everywhere, enabling a more expressive yet compact 3D scene representation. Therefore, we propose SplatWeaver, a generalizable novel view synthesis framework that is able to dynamically allocate Gaussian primitives over different regions in a feed-forward manner. Specifically, SplatWeaver introduces cardinality Gaussian experts and a pixel-level routing scheme, wherein each expert specializes in producing a specific number of primitives from 0 to M, and the routing scheme coordinates these experts to adaptively determine how many Gaussian primitives should be allocated to each spatial location. Moreover, SplatWeaver incorporates a high-frequency prior with attendant guidance module and routing regularization to stabilize expert selection and promote complexity-aware allocation. By leveraging high-frequency structural cues, the routing process is encouraged to assign more Gaussian primitives to fine structures, complex geometry, and textured regions, while suppressing redundant primitives in smooth areas. Extensive experiments across diverse scenarios show that SplatWeaver consistently outperforms state-of-the-art methods, delivering more faithful novel-view renderings with fewer Gaussian primitives.

---

## 论文详细总结（自动生成）

以下是对论文《SplatWeaver: Learning to Allocate Gaussian Primitives for Generalizable Novel View Synthesis》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**可泛化的新视角合成（Generalizable NVS）**。现有的前馈式 3D 高斯泼溅（3DGS）方法通常为每个像素或体素分配固定数量的高斯基元。
*   **研究动机**：真实场景的复杂度在空间上是不均匀的。固定分配会导致平滑区域（如白墙）产生大量冗余基元，而复杂区域（如精细纹理、几何边缘）则因基元不足而导致渲染模糊。
*   **核心目标**：实现一种“**复杂处密集，平滑处稀疏**”的自适应分配机制，在降低计算和存储开销的同时提升渲染质量。

### 2. 方法论
SplatWeaver 提出了一种基于专家混合（MoE）思想的动态分配框架：
*   **基数高斯专家（Cardinality Gaussian Experts）**：预设一组专家 $E_0, E_1, \dots, E_M$，每个专家专门负责生成特定数量（0 到 $M$ 个）的高斯基元。其中 $E_0$ 为“空专家”，允许模型在无用区域不生成任何基元。
*   **像素级路由方案（Pixel-level Routing）**：利用路由器根据像素特征动态选择最合适的专家。为了保持可微性，采用了 Gumbel-Softmax 技巧。
*   **高频先验引导（Frequency Prior Guidance）**：
    *   利用**离散小波变换（DWT）**提取图像的高频能量图，作为场景复杂度的直观指标。
    *   引入路由正则化损失，强制高频区域选择基数大的专家，低频区域选择基数小或为空的专家。
*   **邻域条件参数预测（Neighbor-Conditioned Prediction）**：专家仅预测高斯位置和潜特征。最终的旋转、缩放、不透明度和颜色通过 **K 近邻（KNN）** 搜索，利用 Point Transformer 风格的注意力机制聚合局部空间上下文信息来精细化生成。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：整合了 Hypersim、ScanNet++、CO3D-v2、Objaverse 等 9 个多样化数据集。
    *   **测试集**：DL3DV（140 个未见场景）、RealEstate10K（零样本泛化）、Mip-NeRF 360（零样本泛化）。
*   **Benchmark 设置**：评估了 4、8、16、24 个输入视角下的表现，涵盖了**无位姿（Pose-free）**和**已知位姿**两种设定。
*   **对比方法**：包括 NoPoSplat、AnySplat（目前最强基准之一）、YoNoSplat、EcoSplat（剪枝类）、C3G（查询类）等。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU** 进行训练。
*   **训练细节**：初始学习率 2e-4，采用余弦退火衰减至 1e-6。输入分辨率限制在 448 像素（长边）。
*   **效率**：在推理阶段，利用 Faiss 库进行 CUDA 加速的 KNN 搜索，实现了毫秒级的处理速度。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准上进行了详尽的定量对比（PSNR, SSIM, LPIPS），并提供了大量的定性视觉对比图。
*   **消融实验**：针对基数专家数量、高频引导模块、邻域聚合机制、高斯预算控制因子（$\epsilon$）等关键组件均做了独立验证。
*   **充分性评价**：实验设计非常充分，不仅对比了渲染质量，还对比了高斯基元数量、存储空间和推理帧率（FPS），客观地展示了该方法在“质量-效率”权衡上的优势。

### 6. 主要结论与发现
*   **高效性**：SplatWeaver 在使用比 AnySplat **少 70%** 的高斯基元的情况下，依然在 PSNR 上取得了约 **1.02 dB** 的提升。
*   **自适应能力**：模型能够根据场景复杂度自动调整高斯预算，展现出“按需分配”的智能行为。
*   **几何保真度**：通过邻域特征聚合，生成的高斯分布在物理上更连贯，能够重建出更精确的深度图和精细结构。

### 7. 优点
*   **创新的分配机制**：将 MoE 引入 3DGS 基元生成，解决了前馈模型中长期存在的冗余与欠拟合矛盾。
*   **物理直觉强**：引入小波变换作为高频先验，符合视觉感知逻辑，且不增加过多的计算负担。
*   **紧凑性**：生成的 3D 表示极其紧凑，显著降低了存储需求，有利于在移动端或 Web 端部署。

### 8. 不足与局限
*   **KNN 依赖**：虽然使用了 Faiss 加速，但 KNN 搜索在大规模场景下仍是计算瓶颈之一。
*   **超参数敏感性**：路由正则化中的比例参数（$\rho$）和预算因子（$\epsilon$）需要根据经验设定，虽然论文证明了其具有一定的鲁棒性，但在极端场景下可能需要微调。
*   **训练复杂度**：相比于直接回归的方法，带有路由和正则化的 MoE 结构在训练初期可能存在收敛不稳定的风险（因此论文采用了分阶段训练策略）。

（完）
