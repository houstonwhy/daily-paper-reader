---
title: "SpatialPrompt: XR-Based Spatial Intent Expression as Executable Constraints for AI Generative 3D Design"
title_zh: SpatialPrompt：基于 XR 的空间意图表达作为 AI 生成式 3D 设计的可执行约束
authors: "Yichen Andy Yu, Wanru Li, Qiaoran Wang, Jymon Ross, Gavin Johnson, Mandy Lui, Qiao Jin"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07894v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 基于XR的空间意图用于AI生成式3D设计
tldr: SpatialPrompt是一个基于扩展现实（XR）的系统，旨在将用户的空间草图转化为AI生成3D设计的可执行约束。用户通过3D画笔绘制粗略结构并结合语音指令表达意图，系统支持在共享空间中进行迭代优化和多人同步协作。该研究在Apple Vision Pro上实现，通过启发式评估证明了其在提升创作直观性和协作理解方面的潜力，为可控3D生成提供了新的交互范式。
selection_source: fresh_fetch
motivation: 旨在解决现有3D生成AI缺乏直观的空间控制手段以及难以支持多人协作设计的问题。
method: 开发了一个集成Apple Vision Pro和3D画笔的XR系统，将空间草图与语音提示结合作为生成约束，并支持多人同步协作。
result: 启发式评估表明该工作流直观易用，能有效促进协作创作中的共同理解。
conclusion: SpatialPrompt为可控3D生成提供了有效的交互框架，但未来仍需在生成速度和反馈机制上进一步优化。
---

## 摘要
我们提出了 SpatialPrompt，这是一个扩展现实（XR）系统，可将空间草图转化为可控 3D 生成的可执行约束。用户使用 3D 笔绘制粗略结构，并添加语音提示以表达语义和风格意图。该系统支持在共享空间中进行迭代优化和同步协同创作，并对不同贡献者的内容进行颜色编码。该系统在 Apple Vision Pro 上实现，并结合了 Logitech Muse 和 Meshy。启发式评估表明，该工作流程直观且支持协作创作中的共同理解，同时也揭示了对更快生成速度和更清晰反馈的需求。

## Abstract
We present SpatialPrompt, an Extended Reality(XR) system that turns spatial sketches into executable constraints for controllable 3D generation. Users draw rough structures with a 3D pen and add voice prompts for semantic and stylistic intent. The system supports iterative refinement and synchronous co-creation in shared space with color-coded contributions. Implemented on Apple Vision Pro with Logitech Muse and Meshy, a heuristic evaluation suggests that the workflow is intuitive and supports shared understanding in collaborative creation, while revealing needs for faster generation and clearer feedback.

---

## 论文详细总结（自动生成）

以下是对论文《SpatialPrompt: XR-Based Spatial Intent Expression as Executable Constraints for AI Generative 3D Design》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管生成式 AI 降低了 3D 内容创作的门槛，但非专业用户仍难以通过纯文本或图像精确表达空间设计意图（如结构、比例、布局）。
*   **研究背景**：现有的“文本转 3D”或“图像转 3D”方法往往存在空间描述不足的问题，导致生成结果具有不可预测性。在协作设计中，非专家与设计师之间由于缺乏直观的空间参考，常导致反复修改和沟通成本高昂。
*   **整体含义**：本研究旨在探索一种基于扩展现实（XR）的原生输入范式，将用户的 3D 空间草图转化为 AI 生成模型的可执行约束，从而实现更精准、可控的 3D 设计。

### 2. 方法论：核心思想与关键技术细节
*   **核心思想**：提出 **SpatialPrompt** 系统，将 XR 交互视为一种空间通信媒介。它不要求用户进行精细建模，而是将粗略的 3D 草图作为“空间提示词”（Spatial Prompt），定义生成过程中的几何边界和结构约束。
*   **关键技术流程**：
    1.  **空间意图表达**：用户在 XR 环境中使用 3D 笔（Logitech Muse）绘制物体的轮廓和框架，利用真实环境作为比例参考。
    2.  **多模态复合提示**：系统结合 3D 线框草图（表达结构、比例、体积）和语音提示（表达语义、材质、风格），形成复合输入。
    3.  **可执行约束转化**：系统将草图解析为结构引导信号（如包围盒、相对比例、结构支架），指导后端生成模型在定义的边界内进行创作。
    4.  **迭代与协作**：支持多人同步在线，通过颜色编码区分不同用户的贡献，并允许用户根据生成结果实时修改草图并重新生成。

### 3. 实验设计
*   **实验场景**：在 Apple Vision Pro 平台上实现，后端调用 Meshy API 进行 3D 生成。
*   **评估方法**：进行了**启发式评估（Heuristic Evaluation）**。
*   **参与者与任务**：
    *   3 名具有 HCI 或 XR 背景的实验室成员。
    *   **任务 1（单人）**：10 分钟内完成草图绘制、语音输入、触发生成及至少一次迭代修改。
    *   **任务 2（协作）**：两名参与者在共享空间中共同编辑草图并触发生成。
*   **衡量指标**：意图表达力、生成模型质量、预期一致性的数值评分，以及定性访谈。

### 4. 资源与算力
*   **硬件设备**：Apple Vision Pro（头显）、Logitech Muse（空间笔）、Xcode 开发环境。
*   **算力资源**：文中**未明确说明**具体的本地 GPU 算力，因为生成任务是通过调用外部 **Meshy API**（云端服务）完成的。

### 5. 实验数量与充分性
*   **实验规模**：属于初步的形成期评估（Formative Evaluation），仅包含 3 名参与者和两组短时间任务。
*   **充分性评价**：作为一篇 4 页的短论文（Companion Paper），其实验规模较小，**不够充分**。实验对象仅限于实验室成员，缺乏非专家用户和专业设计师的对比测试，且未进行大规模的消融实验或定量基准测试。

### 6. 主要结论与发现
*   **直观性**：空间草图能有效帮助用户快速外化复杂的 3D 结构意图。
*   **协作优势**：颜色编码的笔触支持了协作中的归属感和协调性，减少了口头解释的需求。
*   **一致性**：草图+语音的工作流被认为比纯文本更直观，能显著提高生成结果与用户预期的一致性。
*   **改进需求**：用户反馈生成速度仍是瓶颈，且在协作过程中需要更清晰的视觉反馈。

### 7. 优点
*   **交互创新**：将 XR 空间草图定义为“可执行约束”而非最终模型，巧妙地平衡了用户控制力与 AI 生成能力。
*   **降低门槛**：为非专业用户提供了一种无需掌握复杂建模软件即可参与 3D 创作的手段。
*   **协作友好**：通过共享 XR 空间解决了设计沟通中的“空间理解偏差”问题。

### 8. 不足与局限
*   **评估局限**：样本量极小且参与者具有专业背景，可能存在评估偏差，无法完全代表普通用户。
*   **性能依赖**：高度依赖外部 API，导致生成质量受限于后端模型，且存在响应延迟。
*   **控制精度**：当草图过于稀疏或语音提示存在歧义时，生成结果仍可能偏离预期。
*   **反馈机制**：缺乏实时预览或交互式约束可视化，用户在生成完成前无法确切知道草图如何影响结果。

（完）
