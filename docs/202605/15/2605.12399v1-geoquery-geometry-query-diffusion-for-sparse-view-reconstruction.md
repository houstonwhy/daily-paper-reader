---
title: "GeoQuery: Geometry-Query Diffusion for Sparse-View Reconstruction"
title_zh: GeoQuery：用于稀疏视图重建的几何查询扩散模型
authors: "Xiao Cao, Yuze Li, Youmin Zhang, Jiayu Song, Cheng Yan, Wen Li, Lixin Duan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12399v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于3D重建和新视角合成的几何引导扩散模型
tldr: 针对3D高斯泼溅（3DGS）在稀疏视图下重建质量差、伪影严重的问题，本文提出GeoQuery框架。该框架通过几何引导的扩散模型，利用深度图和相机位姿构建几何诱导对应场，生成代理查询特征以替代受损的渲染特征，并结合局部窗口交叉视图注意力机制（GCA）实现鲁棒的特征聚合，显著提升了稀疏视图下的重建与渲染质量。
selection_source: fresh_fetch
motivation: 现有的扩散模型在修复3DGS稀疏视图伪影时，常因渲染图质量过差导致跨视图特征检索失败，难以保持多视图一致性。
method: 提出几何引导交叉视图注意力（GCA），利用深度和位姿构建代理查询并限制局部搜索范围，以精确提取参考视图特征。
result: 实验证明GeoQuery在稀疏视图合成和渲染伪影去除任务上表现优异，能够有效抑制伪影并显著提升重建鲁棒性。
conclusion: GeoQuery通过引入显式几何先验增强了扩散模型的跨视图检索能力，为极端稀疏视图下的3D重建提供了有效的解决方案。
---

## 摘要
3D 高斯泼溅 (3DGS) 已成为 3D 重建和新视角合成的一种重要范式。然而，在稀疏视图约束下进行训练时，它仍然容易产生严重的伪影。虽然最近的方法尝试使用图像扩散模型来纠正渲染视图中的伪影，但它们通常依赖多视图自注意力机制从参考图像中检索信息。我们观察到，当 3DGS 输出的渲染新视角严重损坏时，这种机制往往会失效：受损的查询特征会导致错误的跨视图检索，从而产生不一致的渲染优化结果。为解决这一问题，我们提出了 GeoQuery，这是一个几何引导的扩散框架，它通过一种新颖的几何引导跨视图注意力 (GCA) 机制，将生成先验与显式几何线索相结合。首先，通过利用预测的深度图和相机位姿，我们构建了一个几何诱导的对应场来采样参考特征，形成一个几何对齐的代理查询，以替代受损的渲染特征。此外，我们设计了一种新的跨视图特征聚合流水线，其中我们将跨视图注意力限制在每个代理查询周围的局部窗口内，以有效地检索有用特征，同时抑制伪匹配。GeoQuery 可以无缝集成到现有的基于扩散的流水线中，即使在极端的视图稀疏情况下也能实现鲁棒的重建。在稀疏视图新视角合成和渲染伪影去除方面的广泛实验证明了我们方法的有效性。

## Abstract
3D Gaussian Splatting (3DGS) has emerged as a prominent paradigm for 3D reconstruction and novel view synthesis. However, it remains vulnerable to severe artifacts when trained under sparse-view constraints. While recent methods attempt to rectify artifacts in rendered views using image diffusion models, they typically rely on multi-view self-attention to retrieve information from reference images. We observe that this mechanism often fails when the rendered novel views output by 3DGS are heavily corrupted: damaged query features lead to erroneous cross-view retrieval, resulting in inconsistent rendering refinement. To address this, we propose GeoQuery, a geometry-guided diffusion framework that integrates generative priors with explicit geometric cues via a novel Geometry-guided Cross-view Attention (GCA) mechanism. First, by leveraging predicted depth maps and camera poses, we construct a geometry-induced correspondence field to sample reference features, forming a geometry-aligned proxy query that replaces the corrupted rendering features. Furthermore, we design a new cross-view feature aggregation pipeline, in which we restrict the cross-view attention to a local window around each proxy query to effectively retrieve useful features while suppressing spurious matches. GeoQuery can be seamlessly integrated into existing diffusion-based pipelines, enabling robust reconstruction even under extreme view sparsity. Extensive experiments on sparse-view novel view synthesis and rendering artifact removal demonstrate the effectiveness of our approach.

---

## 论文详细总结（自动生成）

以下是对论文《GeoQuery: Geometry-Query Diffusion for Sparse-View Reconstruction》的结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：3D 高斯泼溅（3DGS）在稀疏视图（Sparse-view）训练下容易产生严重的几何崩溃、漂浮物和伪影。
*   **研究动机**：现有的“渲染-优化”流水线利用扩散模型（Diffusion Models）修复伪影，但其核心的“多视图自注意力机制”存在**查询污染（Query Contamination）**问题。即：当 3DGS 渲染图本身质量极差时，提取的查询特征（Query）也是损坏的，导致模型从参考视图中检索到错误的、几何不一致的纹理，从而强化了伪影而非消除它们。

### 2. 方法论
GeoQuery 提出了一个几何引导的扩散框架，核心思想是**用“代理查询”替代“受损查询”**。
*   **几何对应场构建**：利用预训练的度量深度估计模型（如 MVSFormer++）和相机位姿，将参考视图的像素投影到目标视图，建立显式的几何对应关系 $C_{t \to r}$。
*   **几何引导跨视图注意力（GCA）**：
    *   **代理查询（Proxy Query）**：不再使用受损渲染图的特征作为 Query，而是根据几何对应关系，直接从干净的参考视图特征图中采样对应位置的特征作为“代理查询”。
    *   **局部窗口限制**：将跨视图注意力的检索范围限制在对应点周围的 $k \times k$ 局部窗口内，以抑制长程的错误匹配并提高计算效率。
*   **自适应融合机制**：通过一个可学习的门控单元（Gating Mechanism），动态平衡全局语义上下文和局部几何引导特征的注入。
*   **训练目标**：结合了像素级 $L_2$ 损失、感知损失（LPIPS）和风格损失（Gram Matrix Loss），以确保生成图像的结构准确性和纹理锐度。

### 3. 实验设计
*   **数据集**：
    *   **DL3DV-Benchmark**：包含 123 个场景用于训练（约 10 万对数据），12 个场景用于测试。
    *   **Mip-NeRF360**：用于验证模型的泛化能力。
*   **实验任务**：
    *   **渲染伪影去除**：直接修复受损的 3DGS 渲染图。
    *   **稀疏视图重建**：在 3、6、9 个输入视图的极端稀疏条件下进行完整重建。
*   **对比方法**：包括原始 3DGS、FSGS（针对稀疏视图优化的 3DGS 变体）以及 SOTA 扩散修复模型 DIFIX3D+。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU** 进行训练。
*   **模型配置**：基于预训练的 **SD-Turbo** 扩散模型。
*   **训练细节**：分辨率为 $576 \times 1024$，训练 10 万次迭代，Batch Size 为 1，学习率为 $2 \times 10^{-5}$。
*   **推理效率**：在 A100 上，单张图像的扩散优化耗时约 **1.2 秒**，峰值显存占用约 **21.13 GB**。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了定量对比、定性视觉对比、区域级误差分析以及详尽的消融实验。
*   **充分性**：
    *   在两个主流数据集上进行了 3/6/9 视图的梯度测试，验证了模型在不同稀疏度下的鲁棒性。
    *   **消融实验**验证了代理查询、局部窗口大小、自适应融合等每个核心组件的贡献。
    *   **区域级分析**客观地展示了该方法在“高误差区域”修复能力上远超基准模型。
*   **公平性**：对比方法均采用了官方实现或复现结果，评价指标涵盖了 PSNR、SSIM、LPIPS 和 FID 等多个维度。

### 6. 主要结论与发现
*   **性能领先**：GeoQuery 在所有指标上均优于现有方法，特别是在极端的 **3 视图** 场景下，有效防止了基准方法中常见的几何崩溃。
*   **解决污染**：通过代理查询机制，成功解决了“查询污染”导致的伪影传播问题，实现了更精确的细节转移。
*   **鲁棒性**：即使在几何对应关系较弱（如遮挡区域）的情况下，通过自适应门控，模型仍能退化回全局语义合成模式，保持渲染的稳定性。

### 7. 优点（亮点）
*   **物理锚定**：将生成模型的强大幻觉能力通过显式几何线索“锚定”在物理空间，减少了结构性幻觉。
*   **效率优化**：局部窗口注意力将复杂度从 $O(N^2)$ 降低到 $O(Nk^2)$，更适合高分辨率处理。
*   **即插即用**：该模块可以无缝集成到现有的基于扩散的 3D 重建流水线中。

### 8. 不足与局限
*   **深度依赖**：模型性能高度依赖于初始深度估计的准确性。在无纹理区域或镜面反射区域，深度估计失效会导致几何引导失效。
*   **极端视角差**：当目标视图与参考视图之间存在巨大的视角差异或严重遮挡时，缺乏重叠区域会导致模型完全依赖扩散模型的生成能力，可能产生不一致。
*   **算力开销**：尽管使用了单步扩散模型（SD-Turbo），但对于实时应用来说，1.2 秒/帧的优化速度仍有提升空间。

（完）
