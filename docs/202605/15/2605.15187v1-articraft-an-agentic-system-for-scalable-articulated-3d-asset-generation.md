---
title: "Articraft: An Agentic System for Scalable Articulated 3D Asset Generation"
title_zh: Articraft：一种用于可扩展关节式 3D 资产生成的智能体系统
authors: "Matt Zhou, Ruining Li, Xiaoyang Lyu, Zhaomou Song, Zhening Huang, Chuanxia Zheng, Christian Rupprecht, Andrea Vedaldi, Shangzhe Wu"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15187v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 使用大语言模型进行可扩展关节式3D资产生成的智能体系统
tldr: Articraft是一个基于大语言模型（LLM）的智能体系统，旨在解决高质量、多样化关节式3D资产匮乏的问题。该系统将资产生成转化为编程任务，通过领域特定SDK定义几何、关节及测试，并利用反馈机制进行自动化验证。研究者据此构建了包含245个类别、超过1万个资产的Articraft-10K数据集，在生成质量上超越了现有SOTA方法，为机器人仿真和虚拟现实提供了重要支持。
selection_source: fresh_fetch
motivation: 针对现有关节式3D物体数据集规模小、多样性不足的问题，探索利用LLM自动化生成大规模高质量资产的方法。
method: 提出Articraft智能体系统，通过编写调用领域特定SDK的代码来定义物体结构与关节，并结合受限工作区与结构化反馈进行迭代优化。
result: 成功构建了包含1万多个资产的Articraft-10K数据集，且生成质量优于现有的关节资产生成器和通用编程智能体。
conclusion: Articraft证明了通过编程接口引导LLM生成复杂3D资产的有效性，为大规模3D数据合成及下游机器人应用提供了新途径。
---

## 摘要
学习理解关节式 3D 物体的一个瓶颈是缺乏大规模且多样化的数据集。在本文中，我们提出利用大语言模型 (LLMs) 来弥补这一差距，并大规模生成关节式资产。我们将生成关节式 3D 资产的问题简化为编写构建该资产的程序。随后，我们引入了一种新的智能体系统 Articraft，它可以自动编写此类程序。我们设计了一个编程接口和测试框架 (harness) 来帮助 LLM 有效地完成此任务。LLM 调用特定领域 SDK 编写代码，用于定义部件、组合几何形状、指定关节以及编写测试以验证生成的资产。该框架向 LLM 暴露了一个受限的工作空间和接口，验证生成的资产，并返回结构化反馈。通过这种方式，LLM 不会被诸如编写 URDF 文件或管理复杂软件环境等细节所干扰。我们证明，与最先进的关节式资产生成器和通用代码智能体相比，该系统能产生更高质量的资产。利用 Articraft，我们构建了 Articraft-10K，这是一个包含 245 个类别、超过 1 万个关节式资产的精选数据集，并展示了其在训练关节式资产模型以及机器人仿真和虚拟现实等下游应用中的实用性。

## Abstract
A bottleneck in learning to understand articulated 3D objects is the lack of large and diverse datasets. In this paper, we propose to leverage large language models (LLMs) to close this gap and generate articulated assets at scale. We reduce the problem of generating an articulated 3D asset to that of writing a program that builds it. We then introduce a new agentic system, Articraft, that writes such programs automatically. We design a programmatic interface and harness to help the LLM do so effectively. The LLM writes code against a domain-specific SDK for defining parts, composing geometry, specifying joints, and writing tests to validate the resulting assets. The harness exposes a restricted workspace and interface to the LLM, validates the resulting assets, and returns structured feedback. In this way, the LLM is not distracted by details such as authoring a URDF file or managing a complex software environment. We show that this produces higher-quality assets than both state-of-the-art articulated-asset generators and general-purpose coding agents. Using Articraft, we build Articraft-10K, a curated dataset of over 10K articulated assets spanning 245 categories, and show its utility both for training models of articulated assets and in downstream applications such as robotics simulation and virtual reality.

---

## 论文详细总结（自动生成）

这是一份关于论文《Articraft: An Agentic System for Scalable Articulated 3D Asset Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
关节式 3D 物体（如带抽屉的柜子、可折叠椅子、工业工具等）在机器人仿真、虚拟现实（VR）和游戏开发中至关重要。然而，现有的关节式 3D 数据集（如 PartNet-Mobility）面临**规模小、类别覆盖窄、质量参差不齐**的问题，导致学习模型难以泛化到新类别。
论文的核心目标是利用大语言模型（LLM）的编程能力，将 3D 资产生成转化为**自动化程序编写**任务，从而大规模合成高质量、具备物理属性且可交互的 3D 资产。

### 2. 方法论：核心思想与技术细节
Articraft 采用“推理-行动-观察”的智能体模式，通过一个迭代的“编辑-执行-修复”循环来构建资产。其核心组件包括：
*   **领域特定 SDK（LLM-friendly SDK）：** 为 LLM 提供了一套专门用于定义部件（Parts）、几何形状（Geometry）、关节（Joints）和测试用例的 Python 接口。它包含高层抽象（如一键生成合页、轮子）和底层原语（如 CadQuery 几何操作）。
*   **智能体框架（Agent Harness）：** 
    *   **受限工作区：** 仅允许 LLM 修改 `model.py`，并提供只读的文档和示例库。
    *   **受限动作空间：** LLM 只能执行读取、补丁编辑、编译、探测（Probe）等特定指令。
    *   **结构化反馈：** 框架运行代码并进行几何检查（如检测零件是否漂浮、是否发生非预期重叠），将错误以结构化文本形式反馈给 LLM，而非依赖昂贵的图像渲染反馈。
*   **自验证机制：** 允许 LLM 在代码中编写 `run_tests()` 函数，定义物体特有的约束（例如：抽屉在拉动时不能脱离轨道）。

### 3. 实验设计
*   **数据集构建：** 创建了 **Articraft-10K**，包含 245 个类别、超过 10,000 个关节式资产。
*   **对比基准（Benchmarks）：** 
    *   **对比方法：** 包括 Articulate-Anything、PhysX-Anything、URDF-Anything+ 以及通用的代码生成模型 Codex (GPT-5.5)。
    *   **评估方式：** 进行了大规模用户研究（125 名参与者，5000 次随机排序比较），评估生成资产与提示词的对齐度及整体质量。
*   **下游应用验证：** 
    *   使用 Articraft-10K 重新训练关节估计模型 Particulate。
    *   在 NVIDIA Isaac Sim 中进行机器人抓取/操作仿真。
    *   在 VR 环境中进行实时交互测试。

### 4. 资源与算力
*   **生成端算力：** 论文强调该系统是**轻量级**的。生成过程**不需要 GPU**，本地框架（几何计算、CAD 处理、URDF 导出）完全在 **CPU** 上运行。
*   **LLM 后端：** 使用了外部 API（如 GPT-5.4、GPT-5.5、Gemini 3.1 Pro、Claude Opus 4.7）。
*   **成本：** 生成 10,880 次尝试的总 API 成本约为 12,387 美元，平均每个保留资产的成本约为 **1.13 美元**。

### 5. 实验数量与充分性
*   **实验规模：** 生成了超过 1 万个资产，保留率为 91.8%，样本量极大。
*   **消融实验：** 针对不同 LLM 后端（GPT vs Gemini vs Claude）以及不同的推理努力程度（Low/Med/High Effort）进行了对比，展示了推理强度对几何细节的影响。
*   **客观性：** 实验不仅包含主观的用户评价，还通过下游任务（模型训练性能提升、物理仿真成功率）提供了客观的定量指标。

### 6. 主要结论与发现
*   **编程接口的重要性：** 即使是强大的 GPT-5.5，在没有 Articraft 框架的情况下直接生成 URDF 的表现也很差，证明了**特定领域 SDK 和反馈框架**是成功的关键。
*   **数据价值：** 使用 Articraft-10K 训练的模型在处理分布外（OOD）类别时表现显著优于仅在传统数据集上训练的模型。
*   **多模态潜力：** 系统支持图像调节生成，能够根据单张照片重建出具备精确关节结构的 3D 数字孪生。

### 7. 优点（亮点）
*   **无需视觉反馈：** 与以往依赖渲染图像进行视觉比对的方法不同，Articraft 通过代码探测和几何检查实现闭环，极大地降低了计算开销。
*   **结构化强：** 生成的资产自带语义分割、运动学链和物理参数，直接适配工业级仿真器。
*   **可扩展性：** 这种基于代码生成的方法可以轻松扩展到数千个类别，不受手工建模的限制。

### 8. 不足与局限
*   **全局形状质量：** 虽然局部结构和关节正确，但有时物体的全局形状（如瓶子的曲面）可能出现视觉上的畸变或不自然。
*   **复杂机构限制：** 对于某些极其复杂的机械结构（如喷雾瓶的内部触发机制），现有的 SDK 抽象可能不足以完美描述。
*   **语义理解偏差：** 验证机制主要关注物理结构（是否漂浮、是否重叠），尚不能完全理解物体的“美学”或“常识性完整度”（如冰箱内部是否应该有隔板）。

（完）
