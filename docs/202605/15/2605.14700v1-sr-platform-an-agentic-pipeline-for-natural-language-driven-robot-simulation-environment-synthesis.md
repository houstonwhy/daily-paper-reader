---
title: "SR-Platform: An Agentic Pipeline for Natural Language-Driven Robot Simulation Environment Synthesis"
title_zh: SR-Platform：一种自然语言驱动的机器人仿真环境合成智能体流水线
authors: "Ben Wei Lim, Minh Duc Le, Thang Truong, Thanh Nguyen Canh"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14700v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 自然语言驱动的机器人仿真环境合成
tldr: SR-Platform是一个基于智能体的工作流系统，旨在解决机器人仿真环境构建复杂的问题。它能将自然语言描述转化为可执行且物理有效的MuJoCo场景。该系统通过LLM编排、CadQuery几何生成、布局规划和模型集成四个阶段，实现了从意图到仿真环境的自动化合成。在生产环境测试中，它能在1分钟内生成包含多个物体的复杂场景，显著降低了机器人学习环境的开发门槛。
selection_source: fresh_fetch
motivation: 传统的MuJoCo仿真场景构建需要深厚的3D建模和物理引擎专业知识，成为机器人学习中的主要瓶颈。
method: 提出一种四阶段智能体流水线，结合LLM编排、CadQuery代码生成、语义资产检索和自动化布局校验。
result: 系统在生产环境中实现了中位延迟约50秒的场景生成速度，且具备自动重试机制和高效的资产缓存能力。
conclusion: SR-Platform证明了智能体驱动的场景合成能有效减少人工干预，实现从自然语言到高质量机器人仿真环境的快速转化。
---

## 摘要
生成机器人仿真环境仍然是基于仿真的机器人学习中的主要瓶颈。构建一个可用于训练的 MuJoCo 场景通常需要 3D 资产建模、MJCF 规范、空间布局、碰撞避免以及机器人模型集成方面的专业知识。我们提出了 SR-Platform，这是一个已部署到生产环境的智能体系统，可将自由格式的自然语言描述转换为可执行且物理有效的 MuJoCo 环境。SR-Platform 将场景合成分解为四个阶段：基于大语言模型（LLM）的编排器，将用户意图转换为结构化场景计划；资产锻造器，用于检索缓存资产或通过 LLM-to-CadQuery 合成生成新的 3D 几何体；布局架构师，负责分配物体位姿并验证工业约束；以及桥接层，负责组装最终的 MJCF 场景并合并选定的机器人模型。该系统部署为包含九个服务的 Docker 栈，具有 WebSocket 进度流、基于 MinIO 的网格存储、基于 Qdrant 的语义资产检索、Redis 作业状态和 InfluxDB 遥测。利用涵盖 611 次成功 LLM 调用的 30 天生产遥测数据，SR-Platform 生成包含五个物体的场景的中值端到端延迟约为 50 秒，而缓存加速的场景在约 30-40 秒内完成。资产锻造器的首次尝试重试率为 11.3% 并具有自动恢复功能，且缓存资产检索消除了对先前生成的物体类型的逐个物体 LLM 调用。这些结果表明，智能体场景合成可以减少创建多样化机器人训练环境所需的人工工作量，使用户能够在不到一分钟的时间内从纯英文提示词生成可执行的 MuJoCo 场景。

## Abstract
Generating robot simulation environments remains a major bottleneck in simulation-based robot learning. Constructing a training-ready MuJoCo scene typically requires expertise in 3D asset modeling, MJCF specification, spatial layout, collision avoidance, and robot-model integration. We present SR-Platform, a production-deployed agentic system that converts free-form natural language descriptions into executable, physically valid MuJoCo environments. SR-Platform decomposes scene synthesis into four stages: an LLM-based orchestrator that converts user intent into a structured scene plan; an asset forge that retrieves cached assets or generates new 3D geometry through LLM-to-CadQuery synthesis; a layout architect that assigns object poses and verifies industrial constraints; and a bridge layer that assembles the final MJCF scene and merges the selected robot model. The system is deployed as a nine-service Docker stack with WebSocket progress streaming, MinIO-backed mesh storage, Qdrant-based semantic asset retrieval, Redis job state, and InfluxDB telemetry. Using 30 days of production telemetry covering 611 successful LLM calls, SR-Platform generates five-object scenes with a median end-to-end latency of approximately 50 s, while cache-accelerated scenes complete in approximately 30-40 s. The asset forge shows an 11.3% first-attempt retry rate with automatic recovery, and cached asset retrieval removes per-object LLM calls for previously generated object types. These results show that agentic scene synthesis can reduce the manual effort required to create diverse robot training environments, enabling users to produce executable MuJoCo scenes from plain English prompts in under one minute.

---

## 论文详细总结（自动生成）

### SR-Platform: 自然语言驱动的机器人仿真环境合成智能体流水线论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：构建高质量的机器人仿真环境（如 MuJoCo 场景）是机器人学习（强化学习、模仿学习等）的主要瓶颈。这一过程通常需要工程师具备 3D 建模、MJCF 规范编写、空间布局规划、碰撞检测以及机器人模型集成等跨学科的专业知识，难以大规模扩展。
*   **核心问题**：如何通过自然语言指令，自动化地生成物理有效、可直接执行且符合工业约束的机器人仿真场景。

#### 2. 论文提出的方法论
SR-Platform 采用了一种**四层智能体流水线（Agentic Pipeline）**架构，将复杂的场景合成任务分解为模块化步骤：
*   **L1: 编排器 (Orchestrator)**：利用 LLM 将用户的自由格式文本（如“一个实验室工作台”）解析为结构化的 JSON 场景计划，确定房间尺寸、物体列表和机器人型号。
*   **L2: 资产锻造器 (Asset Forge)**：
    *   **语义检索**：首先在 Qdrant 向量数据库中检索已有的 3D 资产。
    *   **代码合成**：若缓存未命中，则调用 LLM 生成 **CadQuery**（基于 Python 的参数化 CAD）代码，实时合成 STL 几何体并存入 MinIO。
*   **L3: 布局架构师 (Layout Architect)**：分配物体的空间位姿（位置和旋转），并集成 **工业规则检查器 (IndustryRulesChecker)**，验证电力间隙、消防通道、机械安全距离等约束。
*   **L4: 桥接层 (Bridge)**：将上述信息组装成最终的 MJCF (XML) 文件，并自动合并选定的机器人模型，确保坐标系一致。
*   **系统架构**：基于 Docker 部署，包含 WebSocket 流式反馈、Redis 作业管理和 InfluxDB 遥测监控。

#### 3. 实验设计
*   **数据集与场景**：
    *   **生产遥测数据**：基于 30 天的实际运行数据，涵盖 611 次成功的 LLM 调用。
    *   **abstract_45**：包含 45 个自由格式物体提示词（分简单、中等、困难三级），用于评估视觉语义合成。
    *   **standard_100**：包含 100 个机械 CAD 任务（基本几何体、机械零件、复合组件），用于评估几何精度。
*   **Benchmark 与对比**：
    *   对比了多种 LLM 后端（Claude-4、GPT-4o、DeepSeek-v4-flash、Qwen3-Coder 等）在 CadQuery 代码生成上的表现。
    *   评估指标包括：端到端延迟、Chamfer Distance (CD) 几何相似度、首次尝试成功率及 Token 成本。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练这些模型所使用的具体 GPU 型号或数量，因为该系统主要是一个基于现有 LLM API 的推理侧智能体系统。
*   **模型路由**：生产环境主要路由至 `venice/deepseek-v4-flash` 进行生成，使用 `text-embedding-3-large` 生成向量。
*   **超时设置**：CadQuery 生成超时设定为 120 秒。

#### 5. 实验数量与充分性
*   **实验规模**：分析了 611 次生产环境调用，并针对 145 个基准物体进行了详细的消融和模型对比实验。
*   **充分性评价**：实验设计较为充分且具有客观性。它不仅关注实验室环境下的成功率，还通过 30 天的生产遥测展示了系统在真实负载下的延迟分布、重试行为和缓存效率。对比了主流的闭源和开源模型，为实际部署提供了参考。

#### 6. 论文的主要结论与发现
*   **生成效率**：生成一个包含 5 个物体的场景，中位延迟约为 50 秒；若资产已缓存，延迟可降至 30-40 秒。
*   **可靠性**：资产生成的首次尝试重试率为 11.3%，通过自动重试机制可实现自我修复。
*   **模型表现**：在机械 CAD 精度上，Claude 系列表现最优，但 Qwen3-Coder 在性价比和速度上极具竞争力。
*   **缓存价值**：随着资产库的扩大，缓存机制显著降低了 LLM 调用成本和端到端耗时。

#### 7. 优点
*   **模块化解耦**：将场景生成分解为四个清晰的层级，降低了单次 LLM 调用的复杂度，便于调试和验证。
*   **物理与工业合规**：不同于仅追求视觉效果的生成模型，该系统生成的 CadQuery 几何体是水密的、比例一致的，且布局经过了工业安全规则检查。
*   **生产就绪**：系统设计考虑了多用户并发、异步作业队列、实时进度流和遥测监控，具有很强的实用性。

#### 8. 不足与局限
*   **几何复杂度限制**：对于高度不规则、有机形状或易变形物体（如织物、复杂植被），LLM-to-CadQuery 的合成能力有限。
*   **空间推理瓶颈**：在处理超大型设施或需要精细任务关联（Affordance）推理的复杂场景时，布局架构师可能达到能力上限。
*   **任务生成缺失**：目前仅生成环境（场景），尚未实现自动生成配套的机器人控制任务、奖励函数或专家演示数据。
*   **测量偏差**：由于遥测埋点限制，部分阶段（L1 和 L3）的延迟数据被合并统计，未能完全拆解。

（完）
