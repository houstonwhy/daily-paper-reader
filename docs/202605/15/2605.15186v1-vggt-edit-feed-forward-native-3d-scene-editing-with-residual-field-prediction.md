---
title: "VGGT-Edit: Feed-forward Native 3D Scene Editing with Residual Field Prediction"
title_zh: VGGT-Edit：基于残差场预测的前馈原生 3D 场景编辑
authors: "Kaixin Zhu, Yiwen Tang, Yifan Yang, Renrui Zhang, Bohan Zeng, Ziyu Guo, Ruichuan An, Zhou Liu, Qizhi Chen, Delin Qu, Jaehong Yoon, Wentao Zhang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15186v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 前馈原生3D场景编辑及复杂环境生成
tldr: VGGT-Edit是一个前馈式原生3D场景编辑框架，旨在解决传统2D提升方法导致的纹理模糊和几何不一致问题。该方法通过深度同步文本注入和残差变换头，直接在3D空间预测几何位移，实现了高效且一致的文本驱动场景编辑。此外，研究者还构建了DeltaScene数据集以支持高质量监督，实验证明该方法在细节、一致性和推理速度上均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的3D编辑方法多依赖2D视图编辑再提升至3D，导致空间感知不足、纹理模糊且几何不一致。
method: 提出一种原生3D编辑框架，利用深度同步文本注入对齐语义，并通过残差变换头直接预测3D几何位移。
result: 实验表明，VGGT-Edit在物体细节清晰度、多视图一致性以及推理速度方面显著优于传统的2D提升基准。
conclusion: 该研究通过原生3D残差预测和高质量数据集，为实现快速、高质量且空间一致的交互式3D场景编辑提供了有效方案。
---

## 摘要
高质量 3D 场景重建近期已向可泛化的前馈架构迈进，能够在单次前向传递中生成复杂的环境。然而，尽管这些模型在静态场景感知方面表现强劲，但在响应动态人类指令方面仍存在局限，这限制了它们在交互式应用中的使用。现有的编辑方法通常依赖于 2D 提升（2D-lifting）策略，即独立编辑各个视图，然后将其提升回 3D 空间。这种间接流水线通常会导致纹理模糊和几何不一致，因为 2D 编辑器缺乏跨视角保持结构所需的空间感知能力。为了解决这些局限性，我们提出了 VGGT-Edit，这是一个用于文本条件原生 3D 场景编辑的前馈框架。VGGT-Edit 引入了深度同步文本注入，以将语义引导与骨干网络的空间位姿对齐，从而确保稳定的指令落地。该语义信号随后由残差变换头处理，直接预测 3D 几何位移以使场景变形，同时保持背景稳定性。为了确保高保真结果，我们使用多项目标函数对框架进行监督，以强制执行几何准确性和跨视图一致性。我们还构建了 DeltaScene 数据集，这是一个通过自动化流水线生成的大规模数据集，并采用 3D 一致性过滤以确保真值质量。实验表明，VGGT-Edit 显著优于 2D 提升基线，产生了更清晰的物体细节、更强的多视图一致性以及近乎瞬时的推理速度。

## Abstract
High-quality 3D scene reconstruction has recently advanced toward generalizable feed-forward architectures, enabling the generation of complex environments in a single forward pass. However, despite their strong performance in static scene perception, these models remain limited in responding to dynamic human instructions, which restricts their use in interactive applications. Existing editing methods typically rely on a 2D-lifting strategy, where individual views are edited independently and then lifted back into 3D space. This indirect pipeline often leads to blurry textures and inconsistent geometry, as 2D editors lack the spatial awareness required to preserve structure across viewpoints. To address these limitations, we propose VGGT-Edit, a feed-forward framework for text-conditioned native 3D scene editing. VGGT-Edit introduces depth-synchronized text injection to align semantic guidance with the backbone's spatial poses, ensuring stable instruction grounding. This semantic signal is then processed by a residual transformation head, which directly predicts 3D geometric displacements to deform the scene while preserving background stability. To ensure high-fidelity results, we supervise the framework with a multi-term objective function that enforces geometric accuracy and cross-view consistency. We also construct the DeltaScene Dataset, a large-scale dataset generated through an automated pipeline with 3D agreement filtering to ensure ground-truth quality. Experiments show that VGGT-Edit substantially outperforms 2D-lifting baselines, producing sharper object details, stronger multi-view consistency, and near-instant inference speed.

---

## 论文详细总结（自动生成）

这是一份关于论文《VGGT-Edit: Feed-forward Native 3D Scene Editing with Residual Field Prediction》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 场景编辑方法大多依赖“2D 提升（2D-lifting）”策略，即先独立编辑 2D 视图再重建 3D 场景。这种方式由于 2D 编辑器缺乏空间感知，容易导致多视角不一致、纹理模糊和几何畸变。
*   **研究动机**：随着前馈式 3D 重建技术（如 VGGT、$\pi^3$）的发展，重建速度已大幅提升，但如何实现高效、原生且空间一致的指令驱动型 3D 编辑仍是空白。作者旨在提出一种直接在 3D 几何场中进行残差预测的前馈框架，以实现近乎瞬时的原生 3D 编辑。

### 2. 方法论
VGGT-Edit 的核心思想是将编辑任务建模为对预训练几何先验的**残差位移预测**。
*   **核心架构**：基于冻结的 $\pi^3$ 重建骨干网络，引入轻量级编辑模块。
*   **深度同步文本注入（Depth-Synchronized Text Injection）**：将文本指令的 Embedding 注入到 Transformer 解码器的特定层（与骨干网络的位姿调制层同步），确保语义引导与空间几何特征在同一深度对齐。
*   **视点感知重要性权重（View-Aware Importance Weighting）**：通过 MLP 预测各视角的权重，根据掩码面积、边界比例和置信度动态调整，抑制遮挡或边缘视角的噪声。
*   **残差变换头（Residual Transformation Head）**：不重新学习整个场景，而是预测密集的几何位移场 $\Delta P$。最终编辑后的点云为 $P_{edit} = P_{base} + \Delta P \odot M$（$M$ 为编辑掩码）。
*   **训练目标**：组合了掩码尺度对齐损失、编辑重建损失、背景保持损失、法线一致性损失以及相机框架一致性损失，确保编辑区域准确且背景稳定。

### 3. 实验设计
*   **数据集**：构建了 **DeltaScene 数据集**，包含约 100,000 个高质量编辑对（95,000 训练，500 手工验证测试）。数据源自 Replica、ScanNet 和 ScanNet++。
*   **自动化流水线**：利用 Qwen3.5-Plus 生成指令，SAM3 进行分割，并通过 3D 一致性过滤和顺序多视图编辑确保真值质量。
*   **Benchmark 与对比方法**：
    *   **优化类**：GaussCtrl, EditSplat。
    *   **2D 提升/前馈类**：Omni-3DEdit, Edit3r, 以及适配后的 NoPoSplat。
*   **评价指标**：CLIP Score（语义对齐）、C-FID/C-KID（多视角一致性）、推理时间。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：训练迭代次数为 10k 次，Batch Size 为 16。
*   **推理速度**：单场景编辑仅需约 **5 秒**（相比优化类方法提速 20-120 倍）。

### 5. 实验数量与充分性
*   **实验规模**：在 500 个手动验证的复杂测试案例上进行了评估，涵盖了增加（Add）、删除（Delete）、修改（Modify）和移动（Move）四种原子操作及其组合。
*   **消融实验**：针对深度同步注入、视点权重模块和残差头进行了详细的消融研究，验证了各组件对几何稳定性和语义对齐的贡献。
*   **充分性评价**：实验设计较为全面，不仅有定量指标，还展示了在未见指令（如旋转物体）上的零射（Zero-shot）泛化能力，证明了实验的客观性和公平性。

### 6. 主要结论与发现
*   **原生 3D 编辑的优越性**：直接在 3D 空间预测残差比在 2D 域编辑再提升更稳定，能有效消除多视角闪烁和几何错位。
*   **性能领先**：VGGT-Edit 在 CLIP Score 上达到 30.2（提升 1.3 分），C-FID 降至 122.4（历史新低）。
*   **极速推理**：实现了秒级的 3D 场景编辑，为交互式空间计算和机器人仿真提供了实用基础。

### 7. 优点
*   **高效性**：前馈架构避免了耗时的逐场景优化。
*   **一致性**：通过残差学习和 3D 原生处理，极好地保持了背景的静止和物体的几何完整性。
*   **数据质量**：DeltaScene 数据集的 3D 一致性过滤机制显著提升了监督信号的可靠性。
*   **泛化性**：展现了对复杂几何变换指令的理解能力。

### 8. 不足与局限
*   **骨干网络依赖**：编辑质量高度依赖于冻结的重建骨干网络（$\pi^3$），若原始重建存在空洞，编辑模块难以修复。
*   **场景范围**：目前主要集中在室内场景（基于 ScanNet 等），在超大规模室外环境或极端光照条件下的表现尚待验证。
*   **操作限制**：虽然支持四种基本操作，但对于涉及物体大幅度形变（如将椅子变成桌子）的极极端编辑，残差预测可能面临挑战。

（完）
