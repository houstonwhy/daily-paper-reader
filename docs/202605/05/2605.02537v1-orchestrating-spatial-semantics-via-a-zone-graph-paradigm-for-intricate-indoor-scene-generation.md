---
title: Orchestrating Spatial Semantics via a Zone-Graph Paradigm for Intricate Indoor Scene Generation
title_zh: 通过区域图范式编排空间语义以实现复杂室内场景生成
authors: "Meisheng Zhang, Shizhao Sun, Yang Zhao, Ziyuan Liu, Zhijun Gao, Jiang Bian"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02537v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 从语义意图生成复杂室内场景的框架
tldr: 针对复杂非凸室内场景生成中空间约束耦合及语义碎片化问题，本文提出ZoneMaestro框架。该框架将生成范式从以物体为中心转向区域图（Zone-Graph）编排，通过Zone-Scene-10K数据集和交替对齐策略（含Z-GRPO），在无需物理引擎的情况下实现了语义丰富度与几何有效性的平衡。此外，发布了SCALE基准测试，证明了该方法在处理复杂空间关系和不规则建筑形式上的卓越性能。
selection_source: fresh_fetch
motivation: 现有的室内场景生成方法在处理非凸房间和紧密空间约束时，因缺乏拓扑先验而导致几何结构脆弱且语义逻辑破碎。
method: 提出Zone-Graph编排范式，通过内化区域逻辑并结合交替对齐策略（Z-GRPO），将语义意图转化为功能区域与拓扑约束。
result: 在SCALE压力测试基准下，该方法在结构连贯性和意图遵循度上显著超越了现有技术，有效解决了生成场景的密度与安全性冲突。
conclusion: ZoneMaestro通过引入区域图范式，显著增强了模型在复杂不规则室内环境中的空间编排能力和生成质量。
---

## 摘要
自主 3D 室内场景合成在具有紧耦合空间约束的非凸房间中往往会失效。数据驱动的生成器缺乏用于长程规划的拓扑先验，而迭代智能体则会割裂语义并在几何上变得脆弱。我们提出了 ZoneMaestro，这是一个将范式从以物体为中心的合成转向区域图编排（Zone-Graph Orchestration）的统一框架。通过内化一种新颖的基于区域的逻辑，ZoneMaestro 将高层语义意图转化为功能区域和拓扑约束，从而能够稳健地适应各种建筑形式。为了支持这一研究，我们构建了 Zone-Scene-10K，这是一个包含显式区域图标注的大规模数据集。我们进一步引入了一种交替对齐策略，在推理内化与区域感知群体相对策略优化（Z-GRPO）之间循环，在不依赖外部物理引擎的情况下，有效调和了语义丰富度与几何有效性之间的矛盾。为了严格评估超越凸原语的空间智能，我们正式定义了复杂空间编排（Intricate Spatial Orchestration）任务，并发布了 SCALE，这是一个针对具有复杂、密集空间关系的不规则室内场景的压力测试基准。大量实验表明，ZoneMaestro 解决了密度与安全性之间的二律背反，在结构连贯性和意图遵循方面均显著优于现有最先进的基准模型。

## Abstract
Autonomous 3D indoor scene synthesis breaks down in non-convex rooms with tightly coupled spatial constraints. Data-driven generators lack topological priors for long-horizon planning, while iterative agents fragment semantics and become geometrically brittle. We present ZoneMaestro, a unified framework that shifts the paradigm from object-centric synthesis to Zone-Graph Orchestration. By internalizing a novel zone-based logic, ZoneMaestro translates high-level semantic intent into functional zones and topological constraints, enabling robust adaptation to diverse architectural forms. To support this, we construct Zone-Scene-10K, a large-scale dataset enriched with explicit Zone-Graph annotations. We further introduce an Alternating Alignment Strategy that cycles between reasoning internalization and Zone-Aware Group Relative Policy Optimization (Z-GRPO), effectively reconciling the tension between semantic richness and geometric validity without relying on external physics engines. To rigorously evaluate spatial intelligence beyond convex primitives, we formally define the task of Intricate Spatial Orchestration and release SCALE, a stress-test benchmark for irregular indoor scenarios with complex, dense spatial relations. Extensive experiments demonstrate that ZoneMaestro resolves the density-safety dichotomy, significantly outperforming state-of-the-art baselines in both structural coherence and intent adherence.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ZoneMaestro** 的统一框架，旨在解决复杂室内场景生成中的空间语义编排问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 室内场景生成方法在处理**非凸（Non-convex）房间**（如 L 型、T 型等不规则形状）和**高密度空间约束**时表现不佳。
*   **研究背景**：
    *   **数据驱动生成器**：缺乏长程规划所需的拓扑先验，容易导致物体堆叠或超出边界。
    *   **迭代智能体/LLM 方法**：往往会割裂语义逻辑，且在几何处理上非常脆弱，难以平衡物体密度与物理有效性（即“密度-安全性”二律背反）。
*   **整体含义**：论文提出将生成范式从“以物体为中心”转向“**区域图编排（Zone-Graph Orchestration）**”，通过将空间视为功能区域的拓扑组合，提升模型在复杂建筑环境中的空间智能。

### 2. 论文提出的方法论
*   **核心思想**：将场景分解为功能区域（Zones），通过区域图（Zone-Graph）来定义区域内的物体关系（局部）和区域间的拓扑连接（全局）。
*   **关键技术细节**：
    1.  **Zone-Graph 范式**：将场景表示为 $S = (D, G, T, A)$，分别代表区域清单、区域内空间图、全局拓扑和建筑边界。
    2.  **Zone-Scene-10K 数据集**：基于 InternScenes 构建，利用 GPT-4o 逆向工程生成了包含“设计独白（Reasoning Monologues）”的标注，将抽象意图与度量坐标对齐。
    3.  **交替空间对齐（Alternating Spatial Alignment）策略**：
        *   **推理内化（Reasoning Internalization）**：通过监督微调（SFT）让模型学习输出结构化的设计推理链。
        *   **几何去噪（Geometric Denoising）**：引入 **Z-GRPO（区域感知群体相对策略优化）**。这是一种强化学习算法，通过以下奖励函数优化几何精度：
            *   **边界奖励**：惩罚超出不规则墙体的物体。
            *   **区域解耦奖励**：防止不同功能区（如睡眠区与工作区）的凸包重叠。
            *   **物体碰撞奖励**：惩罚物体间的体积交集。
*   **算法流程**：模型首先生成设计推理（<think>），确定区域布局，再输出最终的 JSON 坐标（<answer>），并在训练中通过 SFT 和 RL 的循环迭代来不断精细化。

### 3. 实验设计
*   **数据集**：
    *   **Zone-Scene-10K**：用于训练和基础测试。
    *   **SCALE (Spatially Complex Architectural Layout Evaluation)**：论文发布的新基准测试，包含 824 个实例，涵盖 9 种不规则边界类型（L/U/T/H 型、梯形等）。
*   **对比方法**：
    *   **数据驱动类**：DiffuScene, ReSpace。
    *   **智能体/LLM 类**：LayoutGPT, LayoutVLM, Holodeck, i-Design。
*   **评估指标**：物理有效性（越界率 OOB、碰撞率 Col）、语义质量（GPT-4o-mini 评分：美观度、现实感、结构逻辑等）、生成效率（LLM 调用次数）及人类偏好排序。

### 4. 资源与算力
*   **基础模型**：Qwen3-8B。
*   **硬件资源**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：进行了两个循环的交替对齐。SFT 阶段每个循环 2 个 epoch；Z-GRPO 阶段每个循环 40 个优化步骤，batch size 为 32。

### 5. 实验数量与充分性
*   **实验规模**：训练集约 8,500 个样本，SCALE 基准测试 824 个复杂场景。
*   **充分性**：
    *   **消融实验**：系统分析了 Zone-Graph 标注、Z-GRPO 强化学习以及交替训练策略的贡献。
    *   **压力测试**：SCALE 基准专门针对非凸边界，相比传统的矩形房间测试更具挑战性。
    *   **主观评估**：进行了 10 人参与的用户研究，涵盖 140 个布局，证明了自动化指标与人类审美的一致性。
*   **客观性**：对比了当前最先进的多种开源和闭源（基于 GPT-4）方法，实验设计较为全面。

### 6. 主要结论与发现
*   **解决密度冲突**：ZoneMaestro 在保持高物体密度（平均每间房 23.35 个物体）的同时，保持了极低的碰撞率和越界率，打破了基准模型“要么空旷、要么混乱”的局限。
*   **几何适应性**：在不规则边界下，ZoneMaestro 能自动将家具对齐墙面法线，并利用壁龛（Nook）布置次要功能区。
*   **效率优势**：相比需要多次迭代修正的智能体方法，ZoneMaestro 通过内化推理，仅需单次推理即可生成高质量布局，成功率高达 98.7%。

### 7. 优点
*   **范式创新**：引入区域图（Zone-Graph）有效地缓解了长程生成的语义漂移问题。
*   **基准贡献**：SCALE 基准填补了 3D 场景生成领域缺乏复杂建筑拓扑评估的空白。
*   **训练策略**：交替对齐策略（SFT + Z-GRPO）在不依赖外部物理引擎的情况下实现了物理约束的硬性满足。

### 8. 不足与局限
*   **物理细节缺失**：目前主要关注静态布局，未考虑物体的运动学属性（如门窗的开启半径、抽屉的拉出空间）。
*   **渲染局限**：研究重点在于几何编排，未涉及材质纹理合成和光照模拟。
*   **文化偏差**：训练数据主要源于现有的 3D 扫描数据集，可能存在特定文化或地理背景下的建筑风格偏见（如偏向西方住宅模式）。

（完）
