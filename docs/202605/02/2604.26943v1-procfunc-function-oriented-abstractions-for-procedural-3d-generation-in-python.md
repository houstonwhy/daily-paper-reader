---
title: "ProcFunc: Function-Oriented Abstractions for Procedural 3D Generation in Python"
title_zh: ProcFunc：Python 中面向函数的程序化 3D 生成抽象
authors: "Alexander Raistrick, Karhan Kayan, Jack Nugent, David Yan, Lingjie Mei, Meenal Parakh, Hongyu Wen, Dylan Li, Yiming Zuo, Erich Liang, Jia Deng"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26943v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 室内房间的程序化3D生成
tldr: ProcFunc是一个基于Python和Blender的程序化3D生成库，通过面向函数的抽象简化了3D内容的创建、组合与执行。它支持语义组件的组合式生成，能够高效产生大规模多样化的训练数据，并显著降低了视觉语言模型（VLM）在编辑和生成程序化代码时的错误率。该库还包含一个高效的室内场景生成器，展示了其在3D合成数据生成方面的强大潜力。
selection_source: fresh_fetch
motivation: 旨在解决现有程序化3D生成工具在代码组合、分析以及与大语言模型集成时存在的复杂性和高错误率问题。
method: 开发了一套面向函数的Python抽象库，通过封装Blender操作实现语义组件的灵活组合、分析和自动化执行。
result: 成功降低了VLM生成3D代码的难度，并构建了一个具备高细节、运行高效且多样性强的室内房间及材质生成器。
conclusion: ProcFunc为3D合成数据生成提供了高效的工具框架，增强了AI模型对复杂程序化3D内容的生成与编辑能力。
---

## 摘要
我们介绍了 ProcFunc，这是一个基于 Blender 的 Python 程序化 3D 生成库。ProcFunc 提供了一系列易于使用的 Python 函数库，简化了程序化生成代码的创建、组合、分析和执行。通过语义组件的组合式构建，ProcFunc 能够轻松创建大规模且多样化的训练数据。视觉语言模型（VLM）可以利用 ProcFunc 编辑程序化材质和几何代码，并能以显著更少的编码错误创建新的程序化代码。最后，作为一个示例用例，我们使用 ProcFunc 开发了一个新的室内房间程序化生成器，其中包含一系列新的组合式程序化材质。我们展示了该房间生成器的细节、运行效率和多样性，以及它在 3D 合成数据生成中的应用。请访问 https://github.com/princeton-vl/procfunc 获取源代码。

## Abstract
We introduce ProcFunc, a library for Blender-based procedural 3D generation in Python. ProcFunc provides a library of easy-to-use Python functions, which streamline creating, combining, analyzing, and executing procedural generation code. ProcFunc makes it easy to create large-scale diverse training data, by combinatorial compositions of semantic components. VLMs can use ProcFunc to edit procedural material and geometry code and can create new procedural code with significantly fewer coding errors. Finally, as an example use case, we use ProcFunc to develop a new procedural generator of indoor rooms, which includes a collection of new compositional procedural materials. We demonstrate the detail, runtime efficiency, and diversity of this room generator, as well as its use for 3D synthetic data generation. Please visit https://github.com/princeton-vl/procfunc for source code.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **ProcFunc**，一个旨在简化 Python 环境下基于 Blender 的程序化 3D 生成的库。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的程序化 3D 生成工具（如 Blender 原生 API 或 Infinigen）主要为 GUI（图形用户界面）设计，导致 Python 编程极其复杂。开发者需要处理全局状态、动态 UI 字符串和复杂的节点连接，这使得代码难以组合、难以分析，且视觉语言模型（VLM）在生成此类代码时错误率极高。
*   **整体含义**：ProcFunc 通过提供一套“面向函数”的抽象，将复杂的图形操作转化为原子化的 Python 函数。这不仅提升了人类开发者的效率，还显著增强了 AI 模型（如 LLM/VLM）生成和编辑 3D 内容的能力，从而能够高效产生大规模、高多样性的合成训练数据。

### 2. 方法论
ProcFunc 的核心思想是**将 3D 生成逻辑解耦为显式的、无状态的函数调用**。其技术细节分为三个层次：
*   **原子基元 API (Primitives API)**：
    *   定义了 497 个 Python 函数，涵盖了材质（Shader Nodes）、几何（Geometry Nodes）及物体级操作。
    *   **关键特性**：每个函数都是原子的，所有输入（如颜色、向量）均为显式参数，消除了对 Blender 全局状态的依赖。支持自动数据类型推断和 NumPy 数组转换。
*   **模块化组合框架 (Module Composition Framework)**：
    *   **标准化接口**：为材质、物体、场景定义了统一的输入输出规范，使得不同模块可以像乐高积木一样随意组合（例如：将“裂纹”层叠加在“大理石”材质上）。
    *   **随机采样器**：区分确定性生成和随机生成，通过显式的随机数生成器（RNG）确保结果的可复现性。
*   **程序追踪与静态分析 (Program Tracing)**：
    *   开发了一个类似于 PyTorch `fx` 的追踪器，可以在不执行昂贵图形运算的情况下捕获计算图。
    *   支持**实例级**和**分布级**分析，允许开发者在生成数据集前优化参数分布或消除无效路径。

### 3. 实验设计
*   **实验场景与任务**：
    1.  **程序化参数编辑**：在 **BlenderGym** 基准测试上，使用 VLM 修改现有材质和几何代码。
    2.  **从零开始生成**：挑战 VLM 在没有任何起始代码的情况下，仅根据参考图像编写材质生成代码。
    3.  **室内场景生成**：构建了一个包含 22 类语义材质和自动家具布局的房间生成器。
    4.  **下游任务验证**：生成 5 万对立体匹配（Stereo Matching）图像，训练 RAFT-Stereo 模型。
*   **对比方法**：主要对比了 **Infinigen** 的 API 及其室内场景生成系统。
*   **评估指标**：光度误差 (PL)、CLIP 距离 (N-CLIP)、倒角距离 (CD)、代码错误率 (%)、API 调用成本 ($) 以及渲染效率（CPU/GPU 时间、内存、存储）。

### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**：Intel Xeon Gold 5320 (使用 4 个核心进行场景构建)。
    *   **GPU**：Nvidia L40 (用于渲染和模型训练)。
*   **效率表现**：
    *   场景构建时间：ProcFunc 仅需 **0.02 - 1.1 分钟**，而 Infinigen Indoors 在高精度模式下需 **270+ 分钟**。
    *   存储优化：通过视频压缩技术，将图像和真值（Ground Truth）的存储成本降低了 **50% - 65%**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 VLM 编辑任务，测试了多个模型（Gemini 2.5 Pro/Flash, GPT-5.2 等）。
    *   针对多样性分析，计算了数千种材质组合的熵和循环复杂度。
    *   针对立体匹配，生成了 50k 样本并在 Middlebury、KITTI-15、ETH3D 等多个标准数据集上验证。
*   **充分性评价**：实验设计较为全面，涵盖了从底层代码生成到高层视觉模型训练的全链路验证。对比实验在相同硬件和任务下进行，具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **VLM 友好性**：使用 ProcFunc 后，VLM 在从零编写代码时的错误率从 30-44% 降至 **5-21%**，且生成的视觉质量更高。
*   **组合多样性**：通过多层模块组合，ProcFunc 能够生成比单一模块多出数个数量级的语义组合（如“带划痕的瓷砖”）。
*   **高效性**：在保持相似几何细节的前提下，ProcFunc 的场景生成速度比前代系统快 **30 倍以上**，且内存占用更低。
*   **合成数据有效性**：仅用 5 万对 ProcFunc 生成的数据训练的模型，在多个立体匹配基准测试中达到了与百万级数据集（如 FSD）相当的性能。

### 7. 优点
*   **API 设计优雅**：将复杂的图形节点逻辑转化为符合 Python 直觉的函数式编程，极大地降低了门槛。
*   **极高的组合性**：支持材质和几何的多层嵌套，能够产生极具真实感的细节（如边缘磨损、污渍）。
*   **静态分析能力**：引入追踪器使得程序化生成过程变得“可解释”且“可优化”。
*   **实用性强**：不仅是一个库，还提供了一个高效的室内场景生成器，对合成数据社区贡献巨大。

### 8. 不足与局限
*   **资产覆盖范围**：目前预设的生成器主要集中在室内房间和基础材质，对于复杂的室外场景或特定类别的物体（如生物、车辆）覆盖不足。
*   **渲染引擎依赖**：虽然支持 EEVEE，但某些复杂材质在不同渲染引擎间的表现一致性仍需进一步优化。
*   **VLM 偏见**：由于现有 LLM 主要在旧版 Blender/Infinigen 代码上训练，ProcFunc 在零样本（Zero-shot）情况下的潜力可能受限于模型的预训练分布。

（完）
