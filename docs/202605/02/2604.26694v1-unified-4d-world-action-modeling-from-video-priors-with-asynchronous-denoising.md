---
title: Unified 4D World Action Modeling from Video Priors with Asynchronous Denoising
title_zh: 基于视频先验与异步去噪的统一 4D 世界动作建模
authors: "Jun Guo, Qiwei Li, Peiyan Li, Zilong Chen, Nan Sun, Yifei Su, Heyun Wang, Yuan Zhang, Xinghang Li, Huaping Liu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26694v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 统一的4D世界合成与3D重建
tldr: X-WAM是一个统一的4D世界模型，旨在解决现有模型在2D像素空间建模及动作执行效率上的局限。它利用预训练视频扩散模型的视觉先验，通过多视图RGB-D视频预测实现高保真4D合成。核心创新包括用于深度预测的轻量化结构适配和异步噪声采样（ANS）策略，后者实现了动作的快速解码与视频的高质量生成。在5800多小时机器人数据上预训练后，该模型在RoboCasa等基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有的统一世界模型仅限于2D像素空间建模，且难以在动作执行效率与世界建模质量之间取得平衡。
method: 提出X-WAM框架，通过轻量化深度分支预测RGB-D视频，并利用异步噪声采样（ANS）在推理时实现动作的快速解码与视频的高保真生成。
result: "在RoboCasa和RoboTwin 2.0基准测试中分别达到79.2%和90.7%的平均成功率，且在4D重建质量上超越了现有方法。"
conclusion: X-WAM成功统一了实时机器人动作执行与高保真4D世界合成，证明了利用视频先验构建高效、精确世界模型的可行性。
---

## 摘要
我们提出了 X-WAM，一种统一的 4D 世界模型，它在单一框架内统一了实时机器人动作执行和高保真 4D 世界合成（视频 + 3D 重建），解决了先前统一世界模型（如 UWM）仅对 2D 像素空间建模且无法平衡动作效率与世界建模质量的关键局限性。为了利用预训练视频扩散模型的强大视觉先验，X-WAM 通过预测多视图 RGB-D 视频来想象未来世界，并通过轻量级的结构适配高效获取空间信息：将预训练 Diffusion Transformer 的最后几个模块复制到专门的深度预测分支中，用于重建未来的空间信息。此外，我们提出了异步噪声采样（ANS），以共同优化生成质量和动作解码效率。ANS 在推理过程中应用了专门的异步去噪调度，通过较少的步数快速解码动作以实现高效的实时执行，同时利用完整的步数序列生成高保真视频。在训练期间，ANS 并非完全解耦时间步，而是从它们的联合分布中采样，以与推理分布保持一致。X-WAM 在超过 5,800 小时的机器人数据上进行了预训练，在 RoboCasa 和 RoboTwin 2.0 基准测试中分别实现了 79.2% 和 90.7% 的平均成功率，同时产生的高保真 4D 重建和生成在视觉和几何指标上均超越了现有方法。

## Abstract
We propose X-WAM, a Unified 4D World Model that unifies real-time robotic action execution and high-fidelity 4D world synthesis (video + 3D reconstruction) in a single framework, addressing the critical limitations of prior unified world models (e.g., UWM) that only model 2D pixel-space and fail to balance action efficiency and world modeling quality. To leverage the strong visual priors of pretrained video diffusion models, X-WAM imagines the future world by predicting multi-view RGB-D videos, and obtains spatial information efficiently through a lightweight structural adaptation: replicating the final few blocks of the pretrained Diffusion Transformer into a dedicated depth prediction branch for the reconstruction of future spatial information. Moreover, we propose Asynchronous Noise Sampling (ANS) to jointly optimize generation quality and action decoding efficiency. ANS applies a specialized asynchronous denoising schedule during inference, which rapidly decodes actions with fewer steps to enable efficient real-time execution, while dedicating the full sequence of steps to generate high-fidelity video. Rather than entirely decoupling the timesteps during training, ANS samples from their joint distribution to align with the inference distribution. Pretrained on over 5,800 hours of robotic data, X-WAM achieves 79.2% and 90.7% average success rate on RoboCasa and RoboTwin 2.0 benchmarks, while producing high-fidelity 4D reconstruction and generation surpassing existing methods in both visual and geometric metrics.

---

## 论文详细总结（自动生成）

这是一份关于论文《Unified 4D World Action Modeling from Video Priors with Asynchronous Denoising》（X-WAM）的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的具身智能模型主要分为“策略模型”（预测动作）和“世界模型”（预测未来观测）。虽然近期出现了统一两者（UWM）的趋势，但仍存在两大瓶颈：
    1.  **空间感知缺失**：现有模型多局限于2D像素空间，缺乏显式的3D几何理解，容易产生物理上不合理的预测。
    2.  **效率与质量的矛盾**：高质量视频生成需要大量的去噪步数（慢），而机器人实时控制需要极少的去噪步数（快），两者在单一框架下难以兼顾。
*   **核心目标**：构建一个统一的 **4D 世界动作模型 (X-WAM)**，在单一架构中同时实现高保真视频生成、3D空间重建、高成功率的策略执行以及实时的动作输出。

### 2. 论文提出的方法论
*   **核心思想**：基于预训练视频扩散模型（Wan2.2-5B），通过轻量化结构扩展引入深度预测，并利用异步去噪机制平衡动作与视频的生成需求。
*   **关键技术细节**：
    *   **轻量化深度适配（Lightweight Depth Adaptation）**：为了不增加计算负担（若将深度图视为额外视频帧会使序列长度翻倍），X-WAM 复制了 Diffusion Transformer (DiT) 的最后 $M$ 个块作为专门的深度分支。该分支通过“单向注意力”读取主干网络的特征，但主干网络不读取深度分支，从而保护了预训练的视觉先验。
    *   **异步噪声采样（ANS）**：
        *   **推理阶段**：采用异步调度，动作和状态仅需少量步数（如 5-10 步）即可快速解码并执行；而视频生成则继续完成剩余步数（如 25-50 步）以保证画质。
        *   **训练阶段**：不再采用完全独立的随机采样，而是从联合分布中采样，确保视频的噪声水平始终大于或等于动作的噪声水平（$t_O \ge t_a$），使训练分布与异步推理过程严格对齐。
    *   **4D 重建流程**：模型同时预测多视图 RGB 视频、深度视频以及机器人末端位姿。利用预测的位姿推导相机外参，将多视图 RGB-D 融合为统一的 4D 点云序列。

### 3. 实验设计
*   **数据集**：使用了超过 **5,800 小时** 的机器人数据，涵盖真实世界（AgibotWorld, DROID）和仿真数据（InternA1, RoboCasa, RoboTwin 2.0）。
*   **Benchmark**：
    *   **RoboCasa**：24 个厨房操作任务。
    *   **RoboTwin 2.0**：50 个双臂操作任务（包含 Clean 和 Randomized 两种设置）。
    *   **真实机器人**：AC One 双臂平台上的“耳机包装”长程任务。
*   **对比方法**：
    *   **VLA 模型**：π0, π0.5, GR00T-N1.5。
    *   **世界动作模型 (WAM)**：UWM, DreamZero, Cosmos Policy, Motus, GigaWorld-Policy。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **预训练**：使用 **256 张 NVIDIA H20 GPU**。
    *   **微调**：使用 **32 张 NVIDIA H20 GPU**。
*   **训练时长/规模**：
    *   预训练阶段：Batch Size 2048，训练 40,000 步。
    *   微调阶段：Batch Size 128，训练 20,000 步。
*   **推理延迟**：在 8 步去噪下，单次动作块推理延迟约为 300ms，通过实时分块（RTC）技术实现了 15Hz 的控制频率。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流仿真基准（共 74 个任务）上进行了大规模测试，并进行了真实机器人部署。
*   **消融实验**：针对“深度架构设计”和“噪声采样策略”进行了详细对比，验证了轻量化分支和 ANS 耦合采样的必要性。
*   **客观性**：实验不仅评估了策略成功率，还引入了 PSNR、SSIM（视觉质量）、AbsRel（深度精度）和 Chamfer Distance（点云质量）等几何指标，评估维度全面且客观。

### 6. 论文的主要结论与发现
*   **性能领先**：X-WAM 在 RoboCasa 达到 79.2% 成功率（超过最强基线 12.1%），在 RoboTwin 2.0 达到 90.7% 成功率。
*   **空间感知的增益**：实验证明，引入显式的深度监督不仅能实现 3D 重建，还能显著提升策略的成功率（从 63.0% 提升至 67.8%），说明几何直觉有助于动作理解。
*   **ANS 的有效性**：ANS 成功解决了“动作需要快”和“视频需要好”的冲突，在保持 1s 左右低延迟的同时，重建质量接近同步去噪的高标准。

### 7. 优点
*   **架构优雅**：通过复制少量 DiT 块实现深度预测，既保留了预训练模型的强大能力，又避免了计算量爆炸。
*   **训练-推理一致性**：ANS 策略通过耦合采样解决了以往 WAM 模型在异步推理时出现的分布偏移问题。
*   **多功能性**：一个模型同时充当了策略控制器、视频生成器和 3D 重建系统，展示了具身大模型的通用性。

### 8. 不足与局限
*   **上下文长度限制**：目前采用固定长度的观测窗口，缺乏长程历史记忆或自回归机制，在极长程任务中可能面临挑战。
*   **推理延迟挑战**：尽管有异步去噪，但作为扩散模型，其推理延迟（~300ms）仍高于纯 Transformer 架构的 VLA 模型，对高速动态任务可能存在滞后。
*   **数据依赖**：高性能高度依赖于 5800 小时的大规模预训练数据，对于缺乏数据的场景迁移成本较高。

（完）
