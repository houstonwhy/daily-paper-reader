---
title: Generalizable Sparse-View 3D Reconstruction from Unconstrained Images
title_zh: 从无约束图像中进行可泛化的稀疏视图三维重建
authors: "Vinayak Gupta, Chih-Hao Lin, Shenlong Wang, Anand Bhattad, Jia-Bin Huang"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28193v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 稀疏视图3D场景重建的前馈框架
tldr: 针对非约束、稀疏且无位姿的互联网图像，本文提出 GenWildSplat 框架。该方法无需场景优化，通过前馈网络预测深度、相机参数及 3D 高斯分布。利用外观适配器和语义分割处理光照变化与瞬态遮挡，结合课程学习提升泛化性，在 PhotoTourism 等基准上实现了实时推理和领先的渲染质量。
selection_source: fresh_fetch
motivation: 现有稀疏视图 3D 重建方法依赖耗时的场景特定优化，且在处理真实世界的光照变化和动态遮挡时表现不佳。
method: 提出一种基于 3D 高斯喷溅的前馈框架，结合几何先验预测、外观适配器和语义分割来处理非约束图像。
result: 在 PhotoTourism 和 MegaScenes 基准测试中，该方法在无需测试时优化的前提下，达到了最先进的渲染质量和实时推理速度。
conclusion: GenWildSplat 证明了通过前馈网络和几何先验，可以在复杂多变的真实场景中实现高效且泛化性强的稀疏视图 3D 重建。
---

## 摘要
在光照变化和瞬时遮挡的现实世界条件下，从稀疏且未标注位姿的图像中重建三维场景仍然具有挑战性。现有方法依赖于使用外观嵌入或动态掩码的场景特定优化，这需要对每个场景进行大量的训练，且在稀疏视图下表现不佳。此外，在有限场景上的评估也引发了对其泛化能力的质疑。我们提出了 GenWildSplat，这是一个用于稀疏视图室外重建的前馈框架，无需逐场景优化。给定未标注位姿的网络图像，GenWildSplat 利用学习到的几何先验，在规范空间中预测深度、相机参数和 3D 高斯。外观适配器根据目标光照条件调节外观，而语义分割则用于处理瞬时物体。通过在合成和真实数据上的课程学习，GenWildSplat 能够泛化到多样的光照和遮挡模式。在 PhotoTourism 和 MegaScenes 基准测试上的评估表明，该方法达到了最先进的前馈渲染质量，且无需测试时优化即可实现实时推理。

## Abstract
Reconstructing 3D scenes from sparse, unposed images remains challenging under real-world conditions with varying illumination and transient occlusions. Existing methods rely on scene-specific optimization using appearance embeddings or dynamic masks, which requires extensive per-scene training and fails under sparse views. Moreover, evaluations on limited scenes raise questions about generalization. We present GenWildSplat, a feed-forward framework for sparse-view outdoor reconstruction that requires no per-scene optimization. Given unposed internet images, GenWildSplat predicts depth, camera parameters, and 3D Gaussians in a canonical space using learned geometric priors. An appearance adapter modulates appearance for target lighting conditions, while semantic segmentation handles transient objects. Through curriculum learning on synthetic and real data, GenWildSplat generalizes across diverse illumination and occlusion patterns. Evaluations on PhotoTourism and MegaScenes benchmark demonstrate state-of-the-art feed-forward rendering quality, achieving real-time inference without test-time optimization

---

## 论文详细总结（自动生成）

这是一份关于论文《Generalizable Sparse-View 3D Reconstruction from Unconstrained Images》的结构化总结：

### 1. 论文的核心问题与研究背景
*   **研究动机**：从互联网或手持设备拍摄的“非约束”（In-the-wild）图像中重建三维场景极具挑战。这些图像通常具有**光照剧烈变化**、**瞬态遮挡物**（如行人、车辆）以及**视图极度稀疏且无相机位姿**（Unposed）的特点。
*   **核心问题**：现有的神经辐射场（NeRF）或 3D 高斯喷溅（3DGS）方法大多依赖于逐场景的长时间优化，在视图稀疏时容易产生几何畸变（如“尖刺”伪影），且难以在不进行测试时优化的情况下处理光照和动态物体。
*   **整体含义**：本文提出了 **GenWildSplat**，这是首个能够直接从稀疏、无位姿的真实世界图像中，通过单次前馈推理（无需优化）实现几何重建、外观控制和遮挡剔除的框架。

### 2. 论文提出的方法论
*   **核心思想**：利用大规模预训练的几何先验，结合外观适配器和语义分割掩码，在前馈网络中实现几何与外观的解耦。
*   **关键技术细节**：
    1.  **几何转换器 (Geometry Transformer)**：基于 VGGT 架构，从 2-6 张输入图像中提取多视图特征，预测深度图、相机内外参及规范空间（Canonical Space）的高斯属性。
    2.  **外观适配器 (Appearance Adapter)**：包含一个光照编码器（Light Encoder）提取 16 维光照码，以及一个 MLP 调制器，将规范空间的高斯颜色转换为目标光照下的颜色。
    3.  **遮挡建模**：引入预训练的语义分割网络（YOLOv8）识别瞬态类别，生成二进制掩码。在训练损失函数中利用掩码忽略动态区域，确保 3D 场景只包含静态背景。
    4.  **课程学习 (Curriculum Learning)**：采用三阶段训练策略：
        *   阶段 I：单场景光照变化学习（解耦光照与几何）。
        *   阶段 II：多场景泛化学习（建立几何先验）。
        *   阶段 III：合成遮挡处理（学习处理瞬态物体）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 DL3DV 户外场景，通过扩散渲染器（DiffusionRenderer）合成光照变化，并随机粘贴 COCO 物体作为瞬态遮挡。
    *   **测试集**：PhotoTourism（经典基准）和 MegaScenes（更具挑战性的真实稀疏场景，仅包含少于 20 张图像的场景）。
*   **对比方法**：
    *   **优化类**：WildGaussians、NexusSplats、GS-W（均为针对非约束场景的 3DGS 变体）。
    *   **前馈类**：AnySplat（基础模型）及其结合风格迁移或扩散渲染的变体。
*   **Benchmark 指标**：PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **硬件支持**：使用单张 **NVIDIA RTX A6000** GPU。
*   **训练时长**：总计 40,000 次迭代，耗时约 **2 天**。
*   **推理效率**：单次前馈推理仅需约 **3 秒**，而对比的优化类方法通常需要 2.4 到 8 小时。

### 5. 实验数量与充分性
*   **实验规模**：在 700 多个户外场景上进行训练，在 20 个极具挑战性的 MegaScenes 真实场景上进行泛化评估。
*   **消融实验**：针对外观适配器、遮挡处理模块和课程学习策略分别进行了消融实验，证明了每个组件对最终渲染质量和几何稳定性的贡献。
*   **客观性**：实验对比非常客观。例如，为了公平对比，作者为那些依赖 COLMAP 的基线方法提供了更高质量的 VGGT 预测位姿，即便如此，GenWildSplat 在稀疏视图下的表现依然全面超越了这些经过优化的基线。

### 6. 论文的主要结论与发现
*   **前馈胜过优化**：在稀疏视图下，利用大规模数据预训练的前馈模型比逐场景优化模型更具鲁棒性，能有效避免几何崩溃。
*   **成功解耦**：模型成功实现了几何与外观的解耦，甚至可以实现**跨场景的光照迁移**（将 A 场景的光照应用到 B 场景的几何上）。
*   **课程学习的必要性**：直接在复杂数据上训练会导致颜色坍缩，分阶段从简单到复杂的训练是实现稳定收敛的关键。

### 7. 优点
*   **极速推理**：将数小时的优化过程缩短至 3 秒，支持实时应用。
*   **强泛化性**：无需任何测试时微调即可处理未见过的场景和光照。
*   **端到端处理**：集成了位姿估计、深度预测、外观调节和遮挡剔除，是一个完整的解决方案。

### 8. 不足与局限
*   **几何空洞**：在视图极度稀疏时，未观察到的区域仍会出现几何缺失。
*   **视角外伪影**：当测试视角远离输入视图的分布时，会出现“双层几何”或模糊。
*   **掩码依赖**：遮挡处理的效果高度依赖于外部语义分割模型的准确性，若分割失败（如室内复杂物体），重建质量会下降。
*   **物理一致性**：目前尚不支持建模投射阴影（Cast Shadows）或严格的物理重光照。

（完）
