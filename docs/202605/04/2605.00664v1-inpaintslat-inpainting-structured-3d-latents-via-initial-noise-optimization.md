---
title: "InpaintSLat: Inpainting Structured 3D Latents via Initial Noise Optimization"
title_zh: InpaintSLat：通过初始噪声优化实现结构化 3D 潜空间修复
authors: "Jaeyoung Chung, Suyoung Lee, Kyoung Mu Lee"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00664v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 潜扩散框架中的可控3D补全
tldr: 本研究提出InpaintSLat，一种无需训练的结构化3D潜空间修复方法。针对扩散模型早期几何结构对初始噪声高度敏感导致的修复不一致问题，该方法通过优化初始噪声来增强上下文一致性。利用基于修正流模型的反向传播近似和谱参数化设计，实现了高效且高保真度的3D修复，在保持现有上下文的同时确保了与提示词的对齐，为3D内容编辑提供了新途径。
selection_source: fresh_fetch
motivation: 结构化3D潜空间扩散模型在修复时，由于早期几何结构对初始噪声极度敏感，导致生成内容难以与现有上下文保持严格一致。
method: 通过修正流模型的反向传播近似和谱参数化设计来优化初始噪声，从而在无需训练的情况下实现高保真度的3D修复。
result: 实验证明，该方法在上下文一致性和提示词对齐方面显著优于现有的无需训练的修复基准。
conclusion: 初始噪声控制是3D修复中一个独立且关键的维度，能够与传统的采样轨迹操作互补，提升3D生成的稳定性与可控性。
---

## 摘要
我们提出了一种基于初始噪声优化的无需训练的可控 3D 修复方法。在结构化 3D 潜扩散框架中，我们观察到潜在的几何结构是在扩散过程的早期阶段建立的，并且对初始噪声表现出高度敏感性。这种特性在修复和编辑等任务中会影响稳定性，因为模型在合成新结构时必须确保与现有上下文的严格对齐。在本文中，我们引入了一种在结构化 3D 潜扩散框架内优化初始噪声的策略，以确保高保真度的 3D 修复。具体而言，我们利用基于修正流（rectified flow）模型的反向传播近似来更新初始噪声，并采用了专为鲁棒且高效的结构化 3D 潜空间优化而设计的谱参数化。实验表明，与代表性的无需训练的修复基线相比，该方法在上下文一致性和提示词对齐方面具有持续的改进，将初始噪声控制确立为 3D 修复的一个独立维度，且与传统的采样轨迹操作正交。

## Abstract
We present a training-free approach for controllable 3D inpainting based on initial noise optimization. In the structured 3D latent diffusion framework, we observe that the underlying geometric structure is established during the early stages of the diffusion process and exhibits high sensitivity to the initial noise. Such characteristics compromise stability in tasks like inpainting and editing, where the model must ensure strict alignment with the existing context while synthesizing a new structure. In this paper, we introduce a strategy to optimize the initial noise within the structured 3D latent diffusion framework, ensuring high-fidelity 3D inpainting. Specifically, we update the initial noise by leveraging a backpropagation approximation grounded in the rectified flow model, with the spectral parameterization specially designed for robust and efficient structured 3D latent optimization. Experiments demonstrate consistent improvements in contextual consistency and prompt alignment over representative training-free inpainting baselines, establishing initial noise control as an independent dimension for 3D inpainting, orthogonal to conventional sampling trajectory manipulation.

---

## 论文详细总结（自动生成）

这是一份关于论文《InpaintSLat: Inpainting Structured 3D Latents via Initial Noise Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在 3D 生成领域，结构化潜空间扩散模型（如 TRELLIS）虽能生成高质量资产，但在执行“3D 修复”（Inpainting）任务时面临挑战。
*   **核心问题**：3D 几何结构在扩散过程的早期就已确立，且对**初始噪声种子（Initial Noise）**极度敏感。传统的 2D 修复方法（如 RePaint、SDEdit）主要通过操纵采样轨迹来施加约束，但在 3D 中，如果初始种子与目标结构不匹配，强制引导往往会导致几何崩溃、纹理破碎或结构不连贯。
*   **整体含义**：本文提出了一种无需训练（Training-free）的方法，通过优化初始噪声而非仅仅修正采样路径，使生成的 3D 结构在保持现有上下文一致性的同时，能自然地合成缺失部分。

### 2. 方法论
核心思想是将修复任务转化为一个针对初始潜变量的优化问题。
*   **初始噪声优化**：不直接修改模型参数，而是通过反向传播更新初始噪声 $x_T$，使其在经过扩散模型去噪后能重建出保留区域。
*   **关键技术细节**：
    *   **修正流近似反向传播（Approx. Backpropagation via Rectified Flow）**：为了避免存储整个扩散轨迹带来的巨大显存开销，利用修正流模型的线性特性，通过单步去噪预测来近似整个轨迹的梯度。
    *   **谱参数化（Spectral Parameterization）**：针对稀疏结构特征，在频率域（3D FFT）进行参数化优化。这允许使用较大的学习率快速调整全局几何，同时抑制高频伪影，增强优化稳定性。
    *   **分布正则化（Distributional Regularization）**：引入高斯分布匹配损失，约束优化后的噪声保持在预训练模型所期望的 i.i.d. 高斯流形内，防止过拟合导致的生成质量下降。
    *   **正交性**：该方法与现有的轨迹引导方法（如 ILVR）是正交的，可以结合使用以进一步提升效果。

### 3. 实验设计
*   **数据集**：使用了 **Toys4K**（主要定量评估）、**ABO** 和 **HSSD**（补充评估）数据集。
*   **实验场景**：将 3D 物体体积对半切开，一半作为保留区域（Context），另一半作为修复区域（Inpainting），要求模型根据保留部分和文本提示词补全几何与外观。
*   **对比基线（Benchmark）**：对比了 6 种代表性的无需训练的扩散编辑方法：**RePaint、SDEdit、Blended Diffusion、MultiDiffusion、DPS、ILVR**。
*   **评估指标**：
    *   **重建质量**：PSNR、SSIM、LPIPS（外观）；Chamfer Distance、F-score（几何）；Normal Map 指标。
    *   **生成质量**：CLIP Score（提示词对齐）；DINOv2 基础上的 FD 和 KD（分布相似性）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A6000 GPU** 上进行。
*   **参数设置**：基础模型使用 TRELLIS 的 text-to-3D xlarge 版本。
*   **耗时**：初始潜变量优化通常需要约 15 个步骤（Adam 优化器）。虽然优化增加了运行时间（约 145 秒，而基线方法约 25-30 秒），但实验表明在 6 步左右即可达到较高的 IoU 和 Dice 分数，存在性能与速度的权衡空间。

### 5. 实验数量与充分性
*   **实验规模**：在 Toys4K 数据集的 3180 个带标注样本上进行了大规模定量测试，并在 ABO 和 HSSD 上进行了验证。
*   **消融实验**：专门针对“谱参数化”和“高斯分布损失”进行了消融研究，证明了这两个组件对稳定性和质量的关键作用。
*   **充分性评价**：实验设计涵盖了外观、几何、语义对齐等多个维度，对比方法均为该领域的经典或前沿算法，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **初始噪声是关键**：证明了在 3D 任务中，优化初始噪声比单纯操纵采样轨迹更能保证几何的完整性和一致性。
*   **性能领先**：InpaintSLat 在所有重建指标（尤其是几何保真度）上显著优于所有基线方法。
*   **收敛迅速**：通过谱参数化，优化过程在极短的步数内即可确立正确的几何骨架。
*   **多样性保持**：通过改变随机种子并进行优化，模型能够生成与上下文一致但形态各异的多种补全结果。

### 7. 优点
*   **无需训练**：直接利用预训练的 3D 扩散模型，具有很强的通用性。
*   **几何鲁棒性**：解决了 3D 修复中常见的几何崩溃问题，生成的表面更加平滑且符合逻辑。
*   **高效优化**：引入的近似梯度和频域优化策略，巧妙地平衡了显存压力与优化效率。

### 8. 不足与局限
*   **计算开销**：相比于直接采样，迭代优化过程显著增加了推理时间，不适合实时交互场景。
*   **先验依赖**：修复效果高度依赖于预训练模型的先验知识。如果保留的结构与提示词在预训练分布中极度不匹配（例如要求在汽车后半部分补全出房屋），模型可能无法生成合理的逻辑结果。
*   **近似误差**：基于修正流的线性近似梯度虽然高效，但在处理极度非线性的去噪路径时可能存在方向偏差。

（完）
