---
title: "GOR-IS: 3D Gaussian Object Removal in the Intrinsic Space"
title_zh: GOR-IS：内在空间中的三维高斯物体移除
authors: "Yonghao Zhao, Yupeng Gao, Jian Yang, Jin Xie, Beibei Wang"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00498v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 3D高斯泼溅场景编辑与物体移除
tldr: 针对3D高斯泼溅（3DGS）中的物体移除任务，现有方法往往忽略全局光照一致性且难以处理非朗伯表面。本文提出GOR-IS框架，通过将场景分解为内在分量并显式建模光传输，在材质和光照域直接进行内在空间修复。该方法有效解决了遮挡区域的物理一致性问题，在合成和真实数据集上均显著优于现有技术，提升了视觉连贯性。
selection_source: fresh_fetch
motivation: 现有的3D物体移除方法在处理全局光照效果和视点相关的非朗伯表面时，容易产生物理不一致和修复不可靠的结果。
method: 提出GOR-IS框架，将场景分解为内在分量，并引入一个直接在材质和光照域操作的内在空间修复模块以显式建模光传输。
result: "实验结果显示，该方法在感知相似度（LPIPS）上提升了13%，峰值信噪比（PSNR）提升了2dB，显著改善了移除后的物理一致性。"
conclusion: GOR-IS通过在内在空间进行建模和修复，为实现物理一致且视觉连贯的3D物体移除提供了一种高效且鲁棒的解决方案。
---

## 摘要
神经辐射场 (NeRF) 和三维高斯泼溅 (3DGS) 的最新进展已使从多视图图像重建三维场景成为标准做法。从此类三维表示中移除物体是一项基础的编辑任务，它要求对遮挡区域进行完整且无缝的修复，并确保几何和外观的一致性。尽管现有方法在提高修复一致性方面取得了显著进展，但它们往往忽略了全局光照效果，导致物理上不合理的结果。此外，这些方法在处理视点相关的非朗伯表面（其外观随视点变化而变化）时表现不佳，导致修复结果不可靠。在本文中，我们提出了内在空间中的三维高斯物体移除 (GOR-IS)，这是一个用于物理一致且视觉连贯的三维物体移除的新型框架。我们的方法将场景分解为内在分量，并显式建模光线传输，以保持全局光照效果的一致性。此外，我们引入了一个内在空间修复模块，该模块直接在材质和光照域中运行，有效地解决了非朗伯表面带来的挑战。在合成和真实世界数据集上的大量实验表明，我们的框架显著提高了物体移除的物理一致性和视觉连贯性，在感知相似度 (LPIPS) 上优于现有方法 13%，在峰值信噪比 (PSNR) 上优于 2dB。代码已在 https://applezyh.github.io/GOR-IS-project-page/ 公开。

## Abstract
Recent advances in Neural Radiance Fields (NeRF) and 3D Gaussian Splatting (3DGS) have made it standard practice to reconstruct 3D scenes from multi-view images. Removing objects from such 3D representations is a fundamental editing task that requires complete and seamless inpainting of occluded regions, ensuring consistency in geometry and appearance. Although existing methods have made notable progress in improving inpainting consistency, they often neglect global lighting effects, leading to physically implausible results. Moreover, these methods struggle with view-dependent non-Lambertian surfaces, where appearance varies across viewpoints, leading to unreliable inpainting. In this paper, we present 3D Gaussian Object Removal in the Intrinsic Space (GOR-IS), a novel framework for physically consistent and visually coherent 3D object removal. Our approach decomposes the scene into intrinsic components and explicitly models light transport to maintain global lighting effects consistency. Furthermore, we introduce an intrinsic-space inpainting module that operates directly in the material and lighting domains, effectively addressing the challenges posed by non-Lambertian surfaces. Extensive experiments on both synthetic and real-world datasets demonstrate that our framework substantially improves the physical consistency and visual coherence of object removal, outperforming existing methods by 13% in perceptual similarity (LPIPS) and 2dB in peak signal-to-noise ratio (PSNR). Code is publicly available at https://applezyh.github.io/GOR-IS-project-page/

---

## 论文详细总结（自动生成）

这篇论文提出了 **GOR-IS**，一个旨在实现物理一致且视觉连贯的三维物体移除框架。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在 3D 场景（如 NeRF 或 3DGS）中移除物体时，现有方法主要关注几何补全和外观修复，但往往忽略了**全局光照效果**（如物体在地面上的反射）和**非朗伯表面**（高光、金属等视点相关表面）的一致性。
*   **核心问题**：当一个物体被移除时，它投射在周围环境中的反射也应随之消失；同时，被遮挡的非朗伯表面在修复后应在不同视角下保持物理合理的外观。

### 2. 论文提出的方法论
*   **核心思想**：将场景分解为**内在空间（Intrinsic Space）**分量（如材质、光照、法线），并在该空间内进行显式光传输建模和修复，而非直接在 RGB 颜色空间操作。
*   **关键技术细节**：
    1.  **材质与光照解耦**：扩展 3D 高斯属性，包含漫反射（Diffuse）、菲涅尔项（Fresnel）、粗糙度（Roughness）和法线。利用基于物理的渲染（PBR）模型将场景分解。
    2.  **显式光传输建模**：结合 3DGS 射线追踪器和环境贴图。为了平衡效率，提出**屏幕空间过滤（Screen-space filtering）**模型，通过粗糙度引导的 Mipmap 金字塔模拟模糊的高光反射，仅需每像素追踪一条射线。
    3.  **内在空间修复模块**：
        *   **材质修复**：由于材质属性（如反射率、粗糙度）是视点无关的，直接对这些材质图进行 2D 修复并提升至 3D，可有效解决非朗伯表面的视点一致性问题。
        *   **光照感知掩码（Lighting-aware Masking）**：通过射线追踪识别受目标物体反射影响的区域，在修复过程中抑制这些残留的反射伪影。

### 3. 实验设计
*   **数据集**：
    *   **GOR-IS-Synthetic**：包含 8 个具有强全局光照效果和非朗伯表面的合成场景。
    *   **GOR-IS-Real**：包含 2 个真实世界捕获的复杂反射场景。
    *   **SPIn-NeRF 数据集**：用于验证在传统朗伯表面场景下的泛化能力。
*   **对比方法（Baselines）**：包括 3DGIC、AuraFusion360、InFusion、Gaussian Grouping、GScream 等 SOTA（先进）的 3D 修复/移除方法，以及基于 NeRF 的 SPIn-NeRF。
*   **评价指标**：PSNR、SSIM、LPIPS、FID，以及针对移除区域的局部指标 M-LPIPS 和 M-FID。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 GPU**。
*   **训练时长**：
    *   第一阶段（场景解耦与光传输构建）：30,000 步，约耗时 **1.5 小时**。
    *   第二阶段（物体移除与修复）：4,000 步。
*   **效率**：推理速度在完整模型下约为 **15 FPS**；通过将射线追踪蒸馏到球谐函数（SH）表示后，推理速度可提升至 **301 FPS**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了合成与真实、朗伯与非朗伯多种场景。
*   **消融实验**：非常详尽，分别验证了显式光传输建模、屏幕空间过滤、材质修复模块、光照感知掩码以及法线/分割先验对结果的影响。
*   **客观性**：实验对比了多种不同技术路径的基准方法，并使用了标准化的裁剪方式以确保评估的公平性。

### 6. 论文的主要结论与发现
*   **性能提升**：在具有强光照效果的场景中，GOR-IS 在 LPIPS 指标上比现有方法提升了 **13%**，PSNR 提升了 **2dB**。
*   **物理一致性**：通过内在空间建模，该方法能自动移除物体的反射伪影，这是此前方法无法做到的。
*   **视觉连贯性**：在处理高光表面修复时，相比直接修复 RGB 颜色，修复材质属性显著减少了多视角下的闪烁和鬼影。

### 7. 优点
*   **物理建模深入**：首次在 3D 物体移除任务中显式考虑全局光照一致性。
*   **修复策略创新**：将修复任务从颜色域转移到材质域，从根本上解决了非朗伯表面的多视角一致性难题。
*   **效率平衡**：通过屏幕空间过滤技术，在保持高光反射质量的同时，规避了昂贵的多路径射线追踪。

### 8. 不足与局限
*   **漫反射全局光照缺失**：目前主要处理镜面/高光反射，未显式建模漫反射相关的间接光照（如颜色溢出），可能导致某些场景下的微小不一致。
*   **单次弹射限制**：目前的射线追踪仅考虑单次弹射，难以处理多个非朗伯表面相互反射的极端情况（如两面镜子相对）。
*   **依赖 2D 修复质量**：最终效果仍受限于底层 2D 修复模型（如 LaMa）的质量，若 2D 修复结果在参考视角间存在严重纹理差异，3D 结果仍可能模糊。

（完）
