---
title: "Sparse-to-Complete: From Sparse Image Captures to Complete 3D Scenes"
title_zh: Sparse-to-Complete：从稀疏图像采集到完整的三维场景
authors: "Yiyang Shen, Yin Yang, Kun Zhou, Tianjia Shao"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05664v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从稀疏图像捕获中进行完整的3D场景重建
tldr: 本研究提出S2C-3D框架，旨在通过仅6到8张稀疏视图实现高保真且完整的3D场景重建。该方法结合了针对特定场景微调的扩散模型以修复渲染图像，并引入免训练的视图一致性采样过程和相机轨迹规划方案，确保了场景的全面覆盖并消除了伪影。S2C-3D在处理极稀疏输入时，能有效生成无缺失区域、无模糊的高质量3D高斯场景，显著优于现有先进方法。
selection_source: fresh_fetch
motivation: 针对极稀疏视图下3D重建存在的场景覆盖不全、模糊及伪影等挑战，提出一种能实现高保真完整重建的新框架。
method: 核心方法包括用于图像修复的场景特定扩散模型、确保覆盖的相机轨迹规划以及解决多视图冲突的视图一致性采样。
result: 实验证明S2C-3D在仅有6-8张输入图像的情况下，能生成无缺失区域且细节丰富的高质量3D场景，性能超越了现有技术。
conclusion: 该框架通过结合扩散模型的生成能力与轨迹优化，为稀疏视图下的高质量3D场景重建提供了有效的解决方案。
---

## 摘要
我们提出了 S2C-3D，这是一种新颖的稀疏视图三维重建框架，仅需 6 到 8 张图像即可实现高保真且完整的场景重建。我们的框架包含三个核心组件：一个用于特定场景图像修复的专用扩散模型；一个扩散模型中无需训练的视图一致性条件采样过程，用于精细化高斯优化；以及一个确保全面场景覆盖的相机轨迹规划方案。该专用扩散模型通过在输入视图及其对应的退化副本上微调预训练架构而开发。对场景分布的适应使模型能够修复高斯渲染图，同时有效消除领域差距。同时，轨迹规划方案通过将每个新采样的相机与其两个最近邻连接来优化场景覆盖。通过迭代构建路径并仅保留显著增强可见性的路径，该方案建立了一条覆盖整个场景的轨迹。为了解决多视图冲突，视图一致性条件采样过程量化了相邻修复图像之间的一致性。该信息作为条件注入到冻结的扩散模型的采样过程中，从而在无需额外训练的情况下促进视图一致性图像的生成。因此，我们的方法生成了对伪影具有鲁棒性的高保真三维高斯（3D Gaussians）。实验结果表明，S2C-3D 优于现有最先进的方法，在输入极度稀疏的情况下，能够构建出没有区域缺失、模糊或其他伪影的高质量场景。源代码和数据可在 https://gapszju.github.io/S2C-3D 获取。

## Abstract
We introduce S2C-3D, a novel sparse-view 3D reconstruction framework for high-fidelity and complete scene reconstruction from as few as six to eight images. Our framework features three components: a specialized diffusion model for scene-specific image restoration, a training-free view-consistency conditioned sampling process in the diffusion model for refined Gaussian optimization, and a camera trajectory planning scheme to ensure comprehensive scene coverage. The specialized diffusion model is developed by finetuning a pretrained architecture on the input views and their corresponding degraded counterparts. The adaptation to the scene distribution allows the model to repair Gaussian renderings while effectively eliminating domain gaps. Meanwhile, the trajectory planning scheme optimizes scene coverage by connecting each newly sampled camera to its two nearest neighbors. By iteratively constructing paths and retaining only those that significantly enhance visibility, the scheme establishes a trajectory that covers the entire scene. To address multi-view conflicts, the view-consistency conditioned sampling process quantifies the consistency between neighboring repaired images. This information is injected as a condition into the sampling process of the frozen diffusion model, facilitating the generation of view-consistent images without additional training. Consequently, our approach produces high-fidelity 3D Gaussians that are robust to artifacts. Experimental results demonstrate that S2C-3D outperforms state-of-the-art methods, constructing high-quality scenes that are free from missing regions, blurring, or other artifacts with very sparse inputs. The source code and data are available at https://gapszju.github.io/S2C-3D.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **S2C-3D** 的新型三维重建框架，旨在解决从极稀疏图像（仅 6-8 张）中重建高保真且完整三维场景的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在仅有 6-8 张输入图像的极端稀疏条件下，重建出既具有高保真细节又覆盖完整的 3D 场景。
*   **研究背景与动机**：
    *   现有的 3D 高斯泼溅（3DGS）等技术通常需要数十张图像。
    *   现有的稀疏视图重建方法存在三大痛点：
        1.  **视图不一致性**：利用扩散模型修复的图像在不同视角间存在冲突，导致重建结果模糊或有伪影。
        2.  **领域差距（Domain Gap）**：预训练扩散模型未针对特定场景优化，修复效果不佳。
        3.  **场景覆盖不全**：简单的相机插值无法覆盖输入视角之外的遮挡或盲区，导致重建场景“支离破碎”。

### 2. 方法论
S2C-3D 框架分为三个主要阶段：
*   **高斯初始化**：利用 $\pi^3$ 模型从稀疏图像中估计相机位姿并生成初始点云，随后优化得到初步的低质量 3D 高斯模型。
*   **专用扩散模型微调**：
    *   **核心思想**：在目标场景的输入视图上微调预训练扩散模型（如 Difix3D+）。
    *   **关键技术**：通过对初始高斯模型添加随机噪声和随机掩码（Masking）来生成退化图像对，训练模型学习如何修复特定场景的遮挡和细节，消除通用模型与特定场景间的领域差距。
*   **相机轨迹规划方案**：
    *   **核心思想**：基于“信息增益”的贪婪算法。
    *   **流程**：在场景的有向包围盒（OBB）内随机采样相机，计算其对场景表面（由 Poisson 重建得到）的覆盖贡献。只有当新路径能显著增加可见区域时，才将其加入轨迹，确保虚拟相机覆盖整个场景。
*   **视图一致性条件采样**：
    *   **核心思想**：一种免训练的引导机制。
    *   **技术细节**：在扩散模型的采样过程中注入一个“一致性能量函数”。该函数通过深度图将相邻视角的修复图像投影到当前视角，计算像素级 $L_1$ 差异，引导扩散模型生成在空间上相互匹配的图像。

### 3. 实验设计
*   **数据集**：
    *   **Replica**（合成室内场景）：8 个场景。
    *   **ScanNet++**（真实室内场景）：100 个场景。
    *   **S2C-Scene**（作者自采真实场景）：11 个场景。
*   **实验设置**：ScanNet++ 使用 6/7/8 视图；Replica 和 S2C-Scene 使用 4/5/6 视图。
*   **对比方法（Benchmark）**：
    *   原始 3DGS、DNGaussian、Difix3D+、GenFusion、VD-3DGS。
*   **评价指标**：PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090 GPU** 和 AMD 9950X3D CPU。
*   **时长**：扩散模型微调阶段设置为 3000 个训练步（Step）。具体总重建时长未在文中明确给出，但提到了使用 AdamW 优化器和线性预热。

### 5. 实验数量与充分性
*   **实验规模**：在超过 100 个室内场景上进行了测试，涵盖了合成与真实数据。
*   **消融实验**：针对“噪声微调”、“掩码微调”、“相机轨迹规划”和“视图一致性引导”分别做了消融对比。
*   **充分性评价**：实验设计较为充分，不仅有数值指标对比，还提供了大量的定性视觉对比图和场景覆盖率分析，客观地展示了该方法在处理“大面积缺失区域”时的优势。

### 6. 主要结论与发现
*   **性能领先**：S2C-3D 在所有数据集和所有稀疏设置下，各项指标（PSNR/SSIM/LPIPS）均优于现有最先进方法。
*   **完整性突破**：通过轨迹规划和场景特定微调，该方法能有效填补现有方法无法处理的巨大场景空洞（如天花板、桌底等）。
*   **一致性提升**：免训练的引导采样过程显著减少了稀疏重建中常见的伪影和模糊现象。

### 7. 优点与亮点
*   **场景自适应**：通过简单的微调让扩散模型“认识”当前场景，极大地提升了修复质量。
*   **主动覆盖**：不再被动依赖输入视角间的插值，而是主动规划轨迹以实现“从稀疏到完整”的跨越。
*   **免训练一致性**：无需重新训练复杂的视频扩散模型，通过能量函数引导即可实现多视图一致性。

### 8. 不足与局限
*   **场景限制**：主要针对封闭的室内场景，对于开放的室外场景，其轨迹规划和重建范围仍受限于输入视角的分布。
*   **轨迹规划风险**：采用贪婪算法，采样相机有时会出现在不理想的位置（如物体内部或桌子底下），虽然不影响主区域，但会产生局部低质量区域。
*   **几何局限**：在处理凹陷房间（Concave rooms）时可能会产生墙壁外的非预期区域。
*   **计算开销**：虽然微调步数不多，但每个场景都需要单独微调扩散模型，这在实时性要求极高的场景下可能存在限制。

（完）
