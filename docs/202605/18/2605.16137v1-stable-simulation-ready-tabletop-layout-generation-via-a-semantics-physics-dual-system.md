---
title: "STABLE: Simulation-Ready Tabletop Layout Generation via a Semantics-Physics Dual System"
title_zh: STABLE：通过语义-物理双系统生成可模拟的桌面布局
authors: "Zhen Luo, Yixuan Yang, Xudong Xu, Jinkun Hao, Zhaoyang Lyu, Feng Zheng, Jiangmiao Pang, Yanwei Fu"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.16137v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 针对桌面布局的任务到场景生成
tldr: 本研究针对具身智能中的桌面场景生成问题，提出了STABLE系统。针对现有大语言模型在3D空间推理中易导致物体碰撞或悬空的局限，STABLE结合了语义推理器（微调LLM）和物理修正器（流式去噪模型），通过语义-物理双系统交替迭代，从任务关键物体到背景物体逐步生成符合物理规律且语义对齐的仿真场景，显著提升了场景的物理真实性。
selection_source: fresh_fetch
motivation: 现有基于大语言模型的场景生成方法缺乏3D空间推理能力，导致生成的物体布局常出现碰撞或悬空等物理不合理现象。
method: 提出一种由语义推理器生成初步布局和物理修正器进行姿态优化的双系统，并采用渐进式生成范式逐步扩展场景。
result: 实验表明，STABLE能生成严格遵循任务指令且物理有效的仿真桌面场景，在物理合理性上显著优于现有方法。
conclusion: STABLE通过结合语义理解与物理约束，为具身智能提供了一种生成高质量、可仿真桌面布局的有效方案。
---

## 摘要
从任务指令生成可模拟的桌面场景是具身智能（Embodied AI）领域中一个引人入胜且充满前景的研究方向。然而，现有的任务到场景生成方法完全依赖大语言模型（LLMs）来预测场景布局，由于 LLMs 在 3D 空间推理方面的固有局限性，不可避免地会导致物体碰撞或悬空。在本文中，我们提出了 STABLE，这是一个专为生成可模拟桌面场景而设计的语义-物理双系统。STABLE 由两个互补的模块组成：(i) 语义推理器（Semantic Reasoner），一个在结构化桌面场景数据集上经过微调的 LLM，用于根据输入的任务指令生成粗略布局；(ii) 物理修正器（Physics Corrector），一个感知物理的基于流（flow-based）的去噪模型，通过输出姿态更新来细化布局，在确保场景物理合理性的同时，保持与任务指令的语义一致性。STABLE 采用渐进式生成范式：通过在语义推理器和物理修正器之间交替运行，它将场景从任务关键物体逐步扩展到背景物体。实验表明，STABLE 能够成功生成严格符合任务指令的可模拟桌面场景，并显著提升了场景相较于现有技术的物理有效性。

## Abstract
Generating simulation-ready tabletop scenes from task instructions is an intriguing and promising research direction in the field of Embodied AI. However, existing task-to-scene generation methods rely exclusively on large language models (LLMs) to predict scene layouts, inevitably yielding object collisions or floating due to LLMs' inherent limitations in 3D spatial reasoning. In this paper, we present STABLE, a semantics-physics dual-system tailored for simulation-ready tabletop scene generation. STABLE consists of two complementary modules: (i) a Semantic Reasoner, a fine-tuned LLM trained on a structured tabletop scene dataset to generate coarse layouts from input task instructions, and (ii) a Physics Corrector, a physics-aware flow-based denoising model that outputs pose updates to refine layouts, which ensures the physical plausibility of scenes while preserves semantic alignment with task instructions. STABLE adopts a progressive generation paradigm: by alternating between the Semantic Reasoner and Physics Corrector, it incrementally expands the scene from task-critical objects to background objects. Experiments demonstrate that STABLE successfully generates simulation-ready tabletop scenes that strictly conform to task instructions and significantly enhances the physical validity of scenes over prior art.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **STABLE** 的系统，旨在解决具身智能（Embodied AI）中从任务指令生成“仿真就绪”（Simulation-Ready）桌面场景布局的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的任务到场景生成方法主要依赖大语言模型（LLM）。虽然 LLM 擅长理解复杂的语义指令，但在 **3D 空间推理**方面存在固有缺陷，导致生成的物体布局经常出现相互穿插（碰撞）或悬空（不符合重力）的现象。
*   **研究动机**：现有的后处理优化方法（如基于物理引擎的调整）计算开销大，且容易破坏原始指令的语义约束（例如：要求苹果在杯子左边，优化后可能跑到了右边）。
*   **整体含义**：STABLE 提出了一个**语义-物理双系统**，模仿人类思维的“系统 1（直觉语义）”与“系统 2（逻辑物理）”，通过解耦语义推理与物理修正，生成既符合指令又可直接用于物理仿真的高质量桌面布局。

### 2. 方法论
STABLE 由两个核心模块组成，采用**渐进式生成范式**：
*   **语义推理器（Semantic Reasoner, SR）**：
    *   基于微调的 LLM（Qwen3-8B），在结构化桌面数据集上训练。
    *   负责将自然语言指令转化为粗略的 JSON 布局。
    *   采用**渐进式扩展**：先生成任务关键物体（Task-oriented），再生成重要的背景物体，最后是次要背景物体。
*   **物理修正器（Physics Corrector, PC）**：
    *   一个轻量级的基于**流匹配（Flow Matching）**的去噪模型。
    *   **几何感知**：利用预训练的 PointTransformerV3 提取物体的点云特征作为条件，而非仅依赖粗糙的包围盒（BBox）。
    *   **物理约束损失函数**：引入了基于符号距离函数（SDF）的物体间碰撞损失、物体与桌面碰撞损失，以及确保物体稳固放置的支撑接触损失（Support-contact loss）。
*   **算法流程**：SR 生成初始布局 -> PC 修正姿态（平移和偏航角） -> 修正后的布局反馈给 SR 进行下一阶段的物体添加。这种交替迭代避免了误差累积。

### 3. 实验设计
*   **数据集**：主要使用 **MesaTask-10K**，包含 10,000 个带有任务指令的桌面场景。
*   **Benchmark 与对比方法**：
    *   **任务到场景方法**：MesaTask, I-Design-Table, Holodeck-Table。
    *   **后处理基准**：Steerable, MesaTask w/ refine。
    *   **闭源模型**：GPT-4o（Zero-shot）。
    *   **图像驱动方法**：TabletopGen。
*   **评估指标**：FID（视觉真实性）、GPT Score（综合质量）、OC（物体碰撞率）、AwT/AwS（任务/场景图对齐度）、Float（悬空率）。

### 4. 资源与算力
*   **模型规格**：语义推理器使用了 **Qwen3-8B** 进行全参数微调。
*   **训练细节**：
    *   SR 训练：学习率 1e-5，训练 1 个 epoch，每张 GPU 的 micro-batch size 为 4。
    *   PC 训练：学习率 2e-4，训练 5000 个 epoch，batch size 为 2048。
*   **算力说明**：论文提到了使用多 GPU 进行训练，但**未明确指出具体的 GPU 型号（如 A100 或 H100）及总显卡数量**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（表 1）。
    *   针对物理约束和渐进式生成进行了详细的**消融实验**（表 3）。
    *   扩展了**场景编辑（Editing）**和**物体重排（Rearrangement）**任务的实验。
    *   测试了对**未见过的桌面类型**（如电视柜、床头柜）和**未见过的 3D 资产**的泛化能力。
*   **充分性评价**：实验设计非常全面，不仅涵盖了标准的生成质量评估，还深入探讨了物理修正器在极端碰撞情况下的鲁棒性，对比了学习法与传统优化法的差异，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **物理有效性突破**：STABLE 在所有测试场景中实现了 **0% 的碰撞率**，显著优于所有基准方法。
*   **语义保持**：相比于传统的后处理优化，STABLE 的物理修正器在调整位置时能更好地保留物体间的相对空间关系，任务对齐度（AwT）更高。
*   **可靠性**：学习得到的物理修正器比基于迭代的后处理优化器更鲁棒，尤其是在初始布局存在严重碰撞时，传统优化器往往无法收敛。

### 7. 优点
*   **双系统解耦**：巧妙地解决了 LLM 在连续空间坐标预测上的“幻觉”问题。
*   **几何感知修正**：引入点云特征和 SDF 损失，使得模型能处理复杂的堆叠和容器装载（Containment）关系。
*   **渐进式推理**：通过分阶段生成，增强了模型对复杂、拥挤场景的处理能力。

### 8. 不足与局限
*   **自由度限制**：目前的物理修正器仅预测 3D 平移和 1D 偏航角（Yaw），**不支持全 6 自由度（6-DoF）旋转**，无法处理物体倾斜或侧放的场景。
*   **静态场景**：目前仅关注静态布局生成，未涉及物体的关节运动（如打开抽屉）或形变。
*   **应用范围**：主要局限于桌面场景，虽然展示了向室内家具的泛化，但在更大规模、多平面的复杂室内环境（如整个厨房或仓库）中的表现尚待验证。

（完）
