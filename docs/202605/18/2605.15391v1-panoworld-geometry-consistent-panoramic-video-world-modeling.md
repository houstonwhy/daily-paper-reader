---
title: "PanoWorld: Geometry-Consistent Panoramic Video World Modeling"
title_zh: PanoWorld：几何一致的全景视频世界建模
authors: "Le Jiang, Xiangyu Bai, Bishoy Galoaa, Shayda Moezzi, Caleb James Lee, Tooba Imtiaz, Edmund Yeh, Jennifer Dy, Yanzhi Wang, Sarah Ostadabbas"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15391v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 全景视频世界建模与生成
tldr: PanoWorld 是一种全景视频世界模型，旨在从单张图像和文本生成几何一致的 360° 视频。针对现有方法在深度和运动一致性上的不足，该研究将生成过程建模为几何与动力学一致的潜状态建模问题。通过引入深度和轨迹一致性正则化项，并结合球形几何感知适配，PanoWorld 在保持视觉真实感的同时，显著提升了空间一致性，为具身智能应用提供了更可靠的环绕视觉支持。
selection_source: fresh_fetch
motivation: 现有的全景视频生成方法缺乏对底层 3D 场景状态的显式约束，导致生成的视频在深度、点对应关系和球面运动上存在不一致性。
method: 在预训练透视视频模型基础上，引入了深度一致性损失和轨迹一致性损失，并针对球形几何进行了条件输入与位置编码的适配。
result: 实验证明 PanoWorld 在提升几何一致性的同时保持了高水平的视觉真实感，并推出了包含深度和轨迹标注的 PanoGeo 数据集。
conclusion: 全景视频生成必须被视为几何建模问题，才能满足具身智能应用对整体空间理解的严苛要求。
---

## 摘要
我们提出了 PanoWorld，这是一种全景视频世界模型，能够根据单张图像和一段文本描述生成几何一致的 360° 视频。现有的全景视频方法主要针对视觉真实感进行优化，而没有显式地约束底层的 3D 场景状态，导致生成的输出虽然看起来合理，但在球面表面上表现出深度不一致、对应关系断裂以及不合理的运动。我们通过将全景视频生成定义为一个几何与动力学一致的潜状态建模问题，而非纯粹的视觉合成，从而填补了这一空白。基于预训练的透视视频世界模型，我们引入了两个轻量级正则化项：一个是针对伪真值全景深度的深度一致性损失，另一个是监督跟踪点随时间变化的 3D 世界坐标系位置的轨迹一致性损失。我们进一步对条件输入和位置编码应用了球面几何感知适配。此外，我们还推出了 PanoGeo，这是一个统一的几何感知全景视频数据集，包含来自多种真实和合成来源的具有一致深度、轨迹和提示词标注的数据，用于训练和分层评估。实验表明，PanoWorld 在保持具有竞争力的视觉真实感的同时，比以往的全景生成方法提高了几何一致性，证明了全景视频生成必须被视为一个几何建模问题，以支持具身智能（Embodied AI）应用对整体空间理解的需求。代码可在 https://github.com/ostadabbas/PanoWorld 获取。

## Abstract
We present PanoWorld, a panoramic video world model that generates geometry-consistent 360$\degree$ video from a single image and a caption. Existing panoramic video methods optimize primarily for visual realism and do not explicitly constrain the underlying 3D scene state, producing outputs that appear plausible yet exhibit inconsistent depth, broken correspondences, and implausible motion across the spherical surface. We address this gap by framing panoramic video generation as a geometry- and dynamics-consistent latent state modeling problem rather than pure visual synthesis. Building on a pre-trained perspective video world model, we introduce two lightweight regularizers: a depth consistency loss against pseudo ground-truth panoramic depth, and a trajectory consistency loss that supervises the 3D world-frame positions of tracked points across time. We further apply spherical-geometry-aware adaptation to the conditioning and positional encoding. We additionally introduce PanoGeo, a unified geometry-aware panoramic video dataset with consistent depth, trajectory, and prompt annotations across diverse real and synthetic sources, used for both training and stratified evaluation. Experiments show that PanoWorld improves geometric consistency over prior panoramic generation methods while maintaining competitive visual realism, establishing that panoramic video generation must be treated as a geometric modeling problem to support the holistic spatial understanding requirements of embodied AI applications. Code is available at https://github.com/ostadabbas/PanoWorld.

---

## 论文详细总结（自动生成）

以下是对论文《PanoWorld: Geometry-Consistent Panoramic Video World Modeling》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的全景视频生成方法主要关注“视觉真实感”（看起来像真的），但缺乏对底层 3D 场景几何状态的显式约束。这导致生成的 360° 视频在深度一致性、点对应关系以及球面运动逻辑上存在缺陷（如物体漂移、深度突变），无法满足具身智能（Embodied AI）在导航、模拟和规划中对空间一致性的严苛要求。
*   **研究背景**：视频世界模型（Video World Models）旨在学习环境的结构化表示。然而，大多数模型局限于透视投影（有限视野），而全景视频能提供全方位的环境感知。论文提出将全景视频生成视为一个**几何与动力学一致的潜状态建模问题**，而非单纯的像素合成。

### 2. 方法论：核心思想与技术细节
PanoWorld 基于预训练的透视视频模型（Cosmos Predict 2.5），通过以下关键技术实现全景适配与几何约束：
*   **球面几何感知适配**：
    *   **纬度感知位置编码（RoPE）**：修改旋转位置编码，使其符合等距柱状投影（ERP）的几何特性，解决极点附近的像素拉伸问题。
    *   **球面面积加权**：在计算损失函数时，根据纬度对像素进行加权（$\cos \phi$），确保极点附近的小面积像素不会过度影响梯度。
    *   **循环移位增强**：通过水平滚动图像训练模型，确保全景图左右边界的无缝衔接。
*   **多路径条件输入**：
    *   利用 **SigLIP2** 提取透视图像的语义特征。
    *   将透视图像投影至 ERP 画布作为参考潜变量（Reference Latent），并配合掩码（Mask）进行混合扩散。
*   **几何一致性正则化（核心创新）**：
    *   **深度一致性损失 ($L_{depth}$)**：引入轻量级深度头，监督生成的潜变量与伪真值全景深度（由 DAP 模型生成）的一致性。
    *   **轨迹一致性损失 ($L_{track}$)**：利用 CoTracker3 跟踪 2D 点，并将其投影至 3D 世界坐标系，监督其 3D 位置、速度和加速度的平滑性，确保物体运动符合物理逻辑。

### 3. 实验设计
*   **数据集 (PanoGeo)**：
    *   包含超过 8,000 个全景视频剪辑。
    *   来源包括：自采的 4K 360° 视频（15 小时）、公开数据集（x360, 360DVD）以及 Habitat 模拟器渲染的合成数据。
    *   所有数据均经过统一标注：深度图、2D/3D 轨迹、相机运动补偿及文本描述。
*   **Benchmark 与对比方法**：
    *   **对比方法**：360DVD, Imagine360, ARGUS, Follow-Your-Canvas, OmniRoam。
    *   **评估指标**：
        *   视觉质量：FVD, FAED, FID。
        *   文本对齐：CLIP-T。
        *   **几何自一致性（关键指标）**：3D-Smooth（3D 轨迹平滑度）、Depth-$\sigma$（深度时间标准差）、Tr-Life（跟踪点存活率）。

### 4. 资源与算力
*   **硬件**：论文提到在 **NVIDIA H100 80GB GPU** 上进行训练。
*   **训练时长**：采用两阶段训练策略：
    *   第一阶段：256×512 分辨率，6,000 次迭代。
    *   第二阶段：512×1024 分辨率，2,000 次迭代。
*   **参数量**：基于 Cosmos 2B 模型进行微调，深度头仅约 13.8 万参数。

### 5. 实验数量与充分性
*   **实验规模**：评估集包含 150 个剪辑，涵盖自采、真实世界（YouTube）和合成（Habitat）三种场景，具有较好的代表性。
*   **消融实验**：详细对比了开启/关闭深度损失和轨迹损失的效果，证明了两者在提升几何稳定性方面的互补作用。
*   **公平性**：采用了“输入对等协议”（Input-parity protocol），确保所有基线模型在相同的初始信息（单张透视图+提示词）下进行对比，实验设计较为客观。

### 6. 主要结论与发现
*   **几何一致性大幅提升**：PanoWorld 在 3D-Smooth 和 Depth-$\sigma$ 指标上显著优于所有基线模型（例如，3D 平滑度比 ARGUS 提升了 22%）。
*   **视觉质量保持领先**：在提升几何稳定性的同时，FVD 和 FID 指标依然保持最优或次优水平，证明了几何约束不会损害视觉真实感。
*   **全景生成的本质**：实验证明，如果不加几何约束，即使画面看起来真实，其底层的 3D 结构也会发生剧烈漂移（如 OmniRoam 的 3D 误差比 PanoWorld 高出两个数量级）。

### 7. 优点
*   **首个全景视频世界模型**：率先将全景生成从“图像合成”提升到“几何状态建模”的高度。
*   **端到端 3D 应用支持**：生成的视频可以直接提升（Lift）为 4D 点云，支持任意视角的自由导航，具有极强的下游应用价值。
*   **高质量数据集**：发布的 PanoGeo 数据集填补了全景视频缺乏一致性几何标注的空白。

### 8. 不足与局限
*   **长度限制**：目前仅支持固定长度（93 帧，约 5.8 秒）的生成，长时视频生成仍面临漂移累积问题。
*   **伪真值偏差**：模型依赖于单目深度估计模型（DAP）生成的伪标签，可能会继承这些模型的系统性偏差。
*   **算力需求**：虽然微调是轻量级的，但推理过程涉及两阶段扩散，对实时性应用可能存在挑战。

（完）
