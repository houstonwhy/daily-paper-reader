---
title: "3DEditSafe: Defending 3D Editing Pipelines from Unsafe Generation"
title_zh: 3DEditSafe：防御 3D 编辑流水线中的不安全生成
authors: "Nicole Meng, Zheyuan Liu, Meng Jiang, Yingjie Lao"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15398v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 文本到3D场景编辑与生成管线中的安全性
tldr: 针对3D高斯泼溅等编辑流程中存在的不安全内容生成风险，本文提出了3DEditSafe防御框架。该框架通过在优化过程中引入安全正则化、语义投影和残差抑制等机制，有效约束了不安全语义在3D空间中的传播。作为首个针对文本驱动3D编辑安全性的研究，它揭示了2D防御的局限性，并为构建安全的3D生成系统提供了有效的防御方案。
selection_source: fresh_fetch
motivation: 现有的3D编辑流程在处理不安全提示词时，容易在3D场景中产生并传播连贯的违规（NSFW）内容。
method: 提出3DEditSafe框架，通过结合生成阶段安全引导、渲染视图3D安全正则化、安全语义投影及残差抑制等技术，在优化过程中约束不安全语义的传播。
result: 实验表明，3DEditSafe能有效降低不安全语义对齐和攻击成功率，但也揭示了安全性与生成质量之间存在权衡。
conclusion: 本研究首次探讨并防御了文本驱动3D编辑中的不安全生成问题，强调了在3D表示层级直接实施安全机制的必要性。
---

## 摘要
近期 3D 生成式编辑的进展，特别是基于 3D 高斯泼溅（3DGS）的流水线，已经实现了基于文本提示的高保真、多视图一致的场景操作。然而，我们发现当不安全提示产生的编辑在不同视图间传播和优化时，这些流水线也会引入新的安全风险。在这项工作中，我们研究了 3D 编辑流水线中的不安全生成，并表明此类行为可能导致最终 3D 表示中出现连贯且不良的非办公场所（NSFW）内容。为了解决这一问题，我们提出了 3DEditSafe，这是一个安全正则化的 3D 编辑框架，可在优化过程中约束不安全语义的传播。3DEditSafe 将生成阶段的安全引导与渲染视图 3D 安全正则化、安全语义投影、残差抑制以及掩码感知保留相结合，以引导优化远离不安全的编辑方向。我们在 EditSplat 场景上使用对象兼容的不安全提示基准评估了我们的方法，并表明仅靠 2D 安全引导不足以持续防止不安全的 3D 编辑。3DEditSafe 降低了不安全语义对齐和视图级攻击成功率，同时揭示了安全与质量之间的权衡，即更强的不安全抑制可能会引入伪影或降低不安全提示的保真度。据我们所知，这项工作是首次尝试研究并防御文本驱动 3D 编辑流水线中的不安全生成，强调了对直接作用于优化后的 3D 表示的安全机制的需求。

## Abstract
Recent advances in 3D generative editing, particularly pipelines based on 3D Gaussian Splatting (3DGS), have achieved high-fidelity, multi-view-consistent scene manipulation from text prompts. However, we find that these pipelines also introduce new safety risks when unsafe prompts produce edits that are propagated and optimized across views. In this work, we study unsafe generation in 3D editing pipelines and show that such behavior can lead to coherent, undesirable Not-Safe-For-Work (NSFW) content in the final 3D representation.   To address this, we propose 3DEditSafe, a safety-regularized 3D editing framework that constrains unsafe semantic propagation during optimization. 3DEditSafe combines generation-stage safety guidance with rendered-view 3D safety regularization, safe semantic projection, residue suppression, and mask-aware preservation to steer optimization away from unsafe editing directions. We evaluate our approach on EditSplat scenes using an object-compatible unsafe prompt benchmark and show that 2D safety guidance alone is not consistently sufficient to prevent unsafe 3D edits. 3DEditSafe reduces unsafe semantic alignment and view-level attack success rates, while revealing a safety-quality tradeoff in which stronger unsafe suppression can introduce artifacts or reduce unsafe-prompt fidelity. To our knowledge, this work is the first attempt to study and defend against unsafe generation in text-driven 3D editing pipelines, highlighting the need for safety mechanisms that operate directly on optimized 3D representations.

---

## 论文详细总结（自动生成）

### 论文总结：3DEditSafe - 防御 3D 编辑流水线中的不安全生成

#### 1. 核心问题与整体含义（研究动机和背景）
随着 3D 高斯泼溅（3DGS）等技术的进步，文本驱动的 3D 场景编辑（如 EditSplat）已能实现高保真的场景修改。然而，该论文指出这些系统存在严重的**安全隐患**：当用户输入不安全提示词（如暴力、血腥、NSFW 内容）时，生成的 2D 违规语义会通过多视图优化过程在 3D 空间中被传播、对齐并固化，最终形成连贯且持久的 3D 不良内容。
*   **研究动机**：现有的 2D 扩散模型防御手段（如 SLD）在 3D 编辑流程中表现不佳，因为即使 2D 阶段残留了微弱的不安全信号，3D 优化也会将其放大并使其在空间上保持一致。
*   **核心目标**：开发一种直接作用于 3D 优化过程的安全防御框架，防止不安全语义在 3D 表示中固化。

#### 2. 方法论：核心思想与关键技术
论文提出了 **3DEditSafe** 框架，其核心思想是将安全性约束从单纯的“提示词过滤”扩展到“3D 优化正则化”。

*   **提示词风险分析（Prompt Risk Analysis）**：利用 CLIP 文本嵌入计算输入提示词与预定义不安全概念集（如暴力、色情）的语义相似度，生成风险评分，作为激活后续安全机制的“门控”。
*   **安全语义投影（Semantic Safety Projection）**：为了不直接拒绝用户请求，系统在 CLIP 空间中将提示词向量减去其在不安全方向上的分量，从而获得一个“最接近原意但安全”的引导向量。
*   **渲染视图 3D 安全正则化（Rendered-View 3D Safety Regularization）**：这是核心创新。在 3DGS 优化循环中，对渲染出的视图直接施加损失函数：
    *   **不安全排斥损失**：惩罚渲染图像与不安全概念的 CLIP 相似度。
    *   **安全引导损失**：鼓励渲染图像对齐投影后的安全语义方向。
*   **残差抑制与掩码保护**：针对血腥等内容，专门设计了颜色残差抑制（检测红色、高饱和度像素）和掩码感知保护，防止不安全视觉特征在 3D 资产中“烘焙”成型。

#### 3. 实验设计
*   **数据集与基准**：由于缺乏现成的 3D 不安全编辑基准，作者从 1434 个不安全扩散模型提示词中筛选并适配，构建了一个包含 **30 个提示词-场景对** 的基准。
    *   包含 10 个良性提示词和 20 个涵盖暴力、血腥、恐怖语义的不安全提示词。
    *   适配 10 个 3D 场景（如人脸、熊、恐龙、石马等）。
*   **对比方法**：
    1.  **EditSplat (Default)**：无保护的原始 3D 编辑方法。
    2.  **2D Safety Guidance**：仅在 2D 扩散生成阶段应用安全引导的基线。
    3.  **3DEditSafe (Ours)**：本文提出的全套防御方案。
*   **评估指标**：不安全 CLIP 相似度、视图级攻击成功率（View ASR）、场景级攻击成功率（Scene ASR）、编辑保真度（CLIP Target）及伪影得分。

#### 4. 资源与算力
*   **硬件环境**：使用了 **1 台 NVIDIA A6000 GPU (48GB)**，配备 AMD EPYC 7763 CPU。
*   **软件环境**：基于 Ubuntu 22.04.5 运行。
*   **训练时长**：文中未明确给出每个场景的具体训练分钟数，但提到初始化基于 EditSplat 的 30K 迭代检查点，且 3DEditSafe 在标准优化循环中增加了 CLIP 推理开销。

#### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同类别的 3D 场景上进行了测试，涵盖了从人体到动物、静态物体的多种情况。
*   **充分性**：实验对比了“无防御”和“仅 2D 防御”两种逻辑，证明了 3D 层面正则化的必要性。
*   **客观性**：作者诚实地报告了安全与质量之间的权衡（Trade-off），即强力抑制不安全内容会导致编辑保真度下降或产生视觉伪影，这种客观态度增强了结论的可信度。

#### 6. 主要结论与发现
*   **2D 防御的局限性**：仅在图像生成阶段拦截是不够的，残留的不安全信号会在 3D 优化中被放大。
*   **3DEditSafe 的有效性**：相比原始方法，3DEditSafe 将平均场景级攻击成功率（Scene ASR）从 72.2% 降低至 38.9%，显著抑制了不安全语义的固化。
*   **语义敏感度差异**：人脸和人体场景比非生物场景更容易产生连贯的不安全编辑，这可能与扩散模型中丰富的人体解剖学先验有关。

#### 7. 优点（亮点）
*   **首创性**：这是首个系统性研究并防御文本驱动 3D 编辑中不安全生成的工作。
*   **非拒绝式防御**：通过语义投影而非直接屏蔽，尝试在保持用户编辑意图的同时去除有害成分，更具实用性。
*   **多层级约束**：结合了文本风险分析、2D 图像处理和 3D 空间正则化，防御体系较为完备。

#### 8. 不足与局限
*   **基准规模较小**：30 个测试用例虽然具有代表性，但对于全面评估生成式模型的安全性来说规模仍显不足。
*   **安全-质量权衡**：在抑制不安全内容时，有时会引入模糊或不自然的视觉伪影，安全编辑的真实感仍有提升空间。
*   **依赖预训练模型**：防御效果高度依赖于 CLIP 等多模态模型的语义识别准确率，若攻击者使用规避性词汇，防御可能失效。
*   **应用范围限制**：目前主要针对 EditSplat 框架，对其他 3D 编辑架构（如基于 NeRF 的）的通用性有待进一步验证。

（完）
