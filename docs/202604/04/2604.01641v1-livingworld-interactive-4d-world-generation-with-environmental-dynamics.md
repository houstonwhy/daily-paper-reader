---
title: "LivingWorld: Interactive 4D World Generation with Environmental Dynamics"
title_zh: LivingWorld：具有环境动力学的交互式 4D 世界生成
authors: "Hyeongju Mun, In-Hwan Jin, Sohyeong Kim, Kyeongbo Kong"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01641v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 具有环境动力学的交互式4D世界生成框架
tldr: 本研究提出 LivingWorld，一个能从单张图像生成具有环境动态（如云、水、烟）的交互式 4D 场景框架。针对现有 3D 生成技术多局限于静态几何且难以维持大规模场景动态一致性的问题，该框架通过逐步构建全局一致的运动场，并利用几何感知对齐模块和哈希运动场表示，实现了高效且长时连贯的 4D 序列生成。该方法支持低延迟交互，在 RTX 5090 上仅需数秒即可完成场景扩展与动态更新，为大规模动态环境创作提供了新方案。
selection_source: fresh_fetch
motivation: 现有的 3D 场景生成主要关注静态几何，缺乏对大规模场景中如云、水等环境动态的有效建模与实时交互支持。
method: 引入几何感知对齐模块解决跨视角歧义，并结合紧凑的哈希运动场与双向运动传播技术，实现全局一致的动态生成。
result: 在单块 RTX 5090 上，场景扩展与运动场更新仅需约 12 秒，能够生成长时且时空连贯的 4D 环境序列。
conclusion: LivingWorld 成功实现了高效、交互式的 4D 世界生成，为具有复杂环境动态的大规模场景构建奠定了基础。
---

## 摘要
我们介绍了 LivingWorld，这是一个从单张图像生成具有环境动力学的 4D 世界的交互式框架。尽管 3D 场景生成的最新进展已能实现大规模环境创建，但大多数方法主要集中在重建静态几何，而对云、水或烟雾等场景级环境动力学的探索仍显不足。对这类动力学进行建模具有挑战性，因为在支持低延迟用户反馈的同时，运动必须在不断扩展的场景中保持连贯。LivingWorld 通过在场景扩展时逐步构建全局连贯的运动场来解决这一挑战。为了在扩展过程中保持全局一致性，我们引入了一个几何感知对齐模块，用于解决跨视图的方向和尺度歧义。我们进一步使用紧凑的基于哈希的运动场来表示运动，从而实现对整个场景动力学的高效查询和稳定传播。这种表示还支持渲染过程中的双向运动传播，无需依赖昂贵的基于视频的精细化处理，即可生成长且时间连贯的 4D 序列。在单块 RTX 5090 GPU 上，生成每个新的场景扩展步骤需要 9 秒，随后进行 3 秒的运动对齐和运动场更新，从而实现了具有全局连贯环境动力学的交互式 4D 世界生成。视频演示可在 cvsp-lab.github.io/LivingWorld 查看。

## Abstract
We introduce LivingWorld, an interactive framework for generating 4D worlds with environmental dynamics from a single image. While recent advances in 3D scene generation enable large-scale environment creation, most approaches focus primarily on reconstructing static geometry, leaving scene-scale environmental dynamics such as clouds, water, or smoke largely unexplored. Modeling such dynamics is challenging because motion must remain coherent across an expanding scene while supporting low-latency user feedback. LivingWorld addresses this challenge by progressively constructing a globally coherent motion field as the scene expands. To maintain global consistency during expansion, we introduce a geometry-aware alignment module that resolves directional and scale ambiguities across views. We further represent motion using a compact hash-based motion field, enabling efficient querying and stable propagation of dynamics throughout the scene. This representation also supports bidirectional motion propagation during rendering, producing long and temporally coherent 4D sequences without relying on expensive video-based refinement. On a single RTX 5090 GPU, generating each new scene expansion step requires 9 seconds, followed by 3 seconds for motion alignment and motion field updates, enabling interactive 4D world generation with globally coherent environmental dynamics. Video demonstrations are available at cvsp-lab.github.io/LivingWorld.

---

## 论文详细总结（自动生成）

### 论文总结：LivingWorld - 具有环境动力学的交互式 4D 世界生成

#### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 场景生成技术（如 WonderWorld）虽然能生成大规模环境，但大多局限于**静态几何**。现实世界中的环境（如流动的河流、漂浮的云、烟雾）具有固有的动力学特征，而现有的 4D 生成方法通常针对孤立对象，且依赖高昂的视频驱动优化，难以在场景不断扩展的交互式过程中保持全局运动的一致性和低延迟反馈。
*   **研究动机**：为了在虚拟环境（如仿真、游戏、具身智能）中提供更真实的体验，需要一种能够随场景扩展同步构建、且具备空间一致性的环境动力学建模框架。

#### 2. 方法论
LivingWorld 采用了一种逐步构建全局一致运动场的策略，核心流程如下：
*   **欧拉运动估计（Eulerian Motion Estimation）**：利用预训练模型预测单视图的 2D 像素级速度场，并结合深度图将其提升（Lift）到 3D 空间，生成稀疏的场景流样本。
*   **几何感知对齐模块（Geometry-Aware Alignment）**：由于各视图的运动估计是独立进行的，存在方向和尺度歧义。该模块通过重投影建立空间对应关系，利用 **Kabsch 算法（基于 SVD 的闭式解）** 快速估计旋转和缩放参数，实现新旧视图间运动向量的全局对齐。
*   **哈希运动场（Hash-based Motion Field）**：使用多分辨率哈希编码（类似 Instant-NGP）和轻量级 MLP 构建连续的 3D 速度函数 $F_\theta(x)$。这种表示法解耦了运动与具体的 Gaussian 单元，允许高效查询任意位置的速度。
*   **双向运动传播（Bidirectional Motion Propagation）**：为了防止 Gaussian 粒子在运动过程中产生密度空洞或发散，框架在渲染时同时进行前向和后向欧拉积分，通过时间相关的权重进行平滑混合，从而生成稳定且循环的 4D 序列。

#### 3. 实验设计
*   **数据集/场景**：
    *   **精选基准（Curated Benchmark）**：包含 10 个具有代表性的环境动力学场景（云、河流、海浪、烟雾、火）。
    *   **野外数据集（In-the-wild Dataset）**：从公开图像中收集的多种自然环境。
*   **对比方法**：
    *   **视频生成模型**：Veo 3.1、CogVideoX、Tora（评估其在相机移动下的表现）。
    *   **4D 场景模型**：4DGS-Cinemagraphy（基于视频优化的高开销方法）。
*   **评估指标**：
    *   使用基于 GPT-4o 的非参考指标：**PhysReal**（物理合理性）和 **PhotoReal**（视觉真实感）。
    *   运动一致性指标：**MCA**（平均余弦对齐度）和 **FMV**（流幅值方差）。
    *   **2AFC 人类感知研究**：170 名参与者进行主观偏好评估。

#### 4. 资源与算力
*   **硬件平台**：单块 **NVIDIA RTX 5090 GPU**。
*   **时间开销**：
    *   场景扩展（外扩绘制 + 深度估计）：约 9 秒。
    *   运动更新（估计 + 对齐 + 哈希场训练）：约 3 秒。
    *   **总计**：每个交互步骤约 **12 秒**，支持准实时交互。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了定量对比、定性视觉对比、消融实验以及人类主观实验。
*   **消融实验**：针对“几何感知对齐模块”、“哈希运动场设计”和“双向传播”分别做了对比，验证了各组件对运动稳定性和一致性的贡献。
*   **客观性与公平性**：对比了当前最先进的视频扩散模型和 4DGS 方法，并使用了自动化（GPT-4o）与人工双重评估，实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **高效性**：LivingWorld 在生成速度上比基于视频优化的方法快几个数量级（12秒 vs 数十分钟）。
*   **一致性**：通过几何对齐和全局运动场，解决了大规模场景扩展中运动方向冲突的问题，实现了跨视角的几何一致性。
*   **稳定性**：双向传播技术有效解决了长序列渲染中的粒子漂移和密度缺失问题。

#### 7. 优点（亮点）
*   **交互式 4D 生成**：首次将大规模 3D 场景扩展与实时环境动力学结合。
*   **轻量化对齐**：引入 Kabsch 算法进行运动对齐，既保证了数学上的严谨性，又极大地提升了计算效率。
*   **解耦表示**：哈希运动场的设计使得运动建模不依赖于 Gaussian 粒子的数量，具有良好的扩展性。

#### 8. 不足与局限
*   **刚体运动限制**：该框架主要针对流体类环境动力学（连续速度场），在处理复杂的刚体运动（如旋转的机械零件）时可能会产生几何畸变。
*   **对象级动力学不足**：目前对具有复杂关节运动的对象（如行人、动物）支持有限，尚未实现精细的物体交互建模。
*   **依赖预训练模型**：系统的最终效果仍受限于底层 2D 运动估计模型和深度估计模型的准确性。

（完）
