---
title: "Omni123: Exploring 3D Native Foundation Models with Limited 3D Data by Unifying Text to 2D and 3D Generation"
title_zh: Omni123：通过统一文本到 2D 与 3D 生成，在有限 3D 数据下探索 3D 原生基础模型
authors: "Chongjie Ye, Cheng Cao, Chuanyu Pan, Yiming Hao, Yihao Zhi, Yuanming Hu, Xiaoguang Han"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02289v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 在原生基础模型中统一文本到2D和文本到3D生成
tldr: Omni123是一个3D原生基础模型，旨在解决高质量3D数据稀缺导致的生成不一致问题。它通过将文本、图像和3D数据统一表示为离散Token，并在自回归框架下进行训练，利用丰富的2D数据作为几何先验。该模型引入了交错式X-to-X训练范式，通过语义-视觉-几何循环确保多模态一致性，显著提升了文本引导的3D生成与编辑质量，为构建多模态3D世界模型提供了可扩展路径。
selection_source: fresh_fetch
motivation: 针对高质量3D资产稀缺及现有2D提升3D方法缺乏几何一致性的挑战，探索原生3D基础模型。
method: 将文本、图像和3D数据统一为离散Token，利用自回归框架和交错式X-to-X训练范式，在异构数据集上实现跨模态一致性约束。
result: 实验证明Omni123在文本引导的3D生成和编辑任务中表现优异，显著增强了语义对齐、外观保真度和多视角几何一致性。
conclusion: 该研究展示了通过统一2D与3D生成任务来克服3D数据限制的可行性，为开发大规模多模态3D世界模型奠定了基础。
---

## 摘要
最近的多模态大语言模型在统一的文本和图像理解与生成方面取得了卓越性能，但由于数据有限，将这种原生能力扩展到 3D 领域仍具挑战。与丰富的 2D 图像相比，高质量 3D 资产十分稀缺，导致 3D 合成面临约束不足的问题。现有方法通常依赖间接流水线，即在 2D 空间编辑并通过优化将结果提升至 3D，这往往会牺牲几何一致性。我们提出了 Omni123，这是一种 3D 原生基础模型，在单一自回归框架内统一了文本到 2D 和文本到 3D 的生成。我们的核心见解是，图像与 3D 之间的跨模态一致性可以作为一种隐式结构约束。通过将文本、图像和 3D 表示为共享序列空间中的离散 token，该模型利用丰富的 2D 数据作为几何先验来改进 3D 表示。我们引入了一种交错式 X-to-X 训练范式，在异构配对数据集上协调多种跨模态任务，而无需完全对齐的“文本-图像-3D”三元组。通过在自回归序列中遍历“语义-视觉-几何”循环（例如，从文本到图像到 3D 再到图像），该模型共同强化了语义对齐、外观保真度和多视图几何一致性。实验表明，Omni123 显著提升了文本引导的 3D 生成与编辑效果，为通往多模态 3D 世界模型展示了一条可扩展的路径。

## Abstract
Recent multimodal large language models have achieved strong performance in unified text and image understanding and generation, yet extending such native capability to 3D remains challenging due to limited data. Compared to abundant 2D imagery, high-quality 3D assets are scarce, making 3D synthesis under-constrained. Existing methods often rely on indirect pipelines that edit in 2D and lift results into 3D via optimization, sacrificing geometric consistency. We present Omni123, a 3D-native foundation model that unifies text-to-2D and text-to-3D generation within a single autoregressive framework. Our key insight is that cross-modal consistency between images and 3D can serve as an implicit structural constraint. By representing text, images, and 3D as discrete tokens in a shared sequence space, the model leverages abundant 2D data as a geometric prior to improve 3D representations. We introduce an interleaved X-to-X training paradigm that coordinates diverse cross-modal tasks over heterogeneous paired datasets without requiring fully aligned text-image-3D triplets. By traversing semantic-visual-geometric cycles (e.g., text to image to 3D to image) within autoregressive sequences, the model jointly enforces semantic alignment, appearance fidelity, and multi-view geometric consistency. Experiments show that Omni123 significantly improves text-guided 3D generation and editing, demonstrating a scalable path toward multimodal 3D world models.

---

## 论文详细总结（自动生成）

以下是对论文《Omni123: Exploring 3D Native Foundation Models with Limited 3D Data by Unifying Text to 2D and 3D Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在高质量3D数据极端稀缺（相比近乎无限的2D图像）的情况下，构建一个能够实现原生3D生成与编辑的多模态基础模型。
*   **研究背景**：
    *   目前的3D生成多采用“2D编辑+迭代优化提升至3D”的间接路径，这导致几何一致性差且流程繁琐。
    *   原生3D模型受限于数据量，难以学习到复杂的语义对齐和精细的几何结构。
*   **核心假设**：2D视觉数据中隐含了深层的3D结构信息（形状、外观、空间关系）。通过统一2D和3D生成任务，可以利用海量2D图像作为几何先验，强化3D表示。

### 2. 方法论
*   **核心思想**：提出 **Omni123** 框架，将文本、图像和3D几何统一表示为共享序列空间中的**离散Token**，通过自回归（Autoregressive）框架进行训练。
*   **关键技术细节**：
    *   **统一Token化**：
        *   **文本**：使用 CLIP 和 Qwen3-0.6B 双编码器捕捉语义细节。
        *   **图像**：开发了一种**1D离散视觉Tokenizer**。采用两阶段训练：先训练高保真连续VAE，再通过Q-Former提取1D离散Token（1024个），解决了传统2D Tokenizer序列过长的问题。
        *   **3D几何**：采用 Cube3D（基于3DShape2VecSet），将网格转换为1024个离散几何Token。
    *   **架构设计**：采用双流自回归Transformer（24层双流块+6层单流块）。条件流处理文本，生成流处理图像和3D Token，所有生成参数在模态间共享。
    *   **训练范式（X-to-X）**：
        1.  **预训练（PT）**：在异构数据集上执行文本↔图像、图像↔3D、文本↔3D的交叉生成任务。
        2.  **持续训练（CT）**：引入可学习的**视角Token**（View Tokens），使模型具备视角感知能力（如图像→特定视角的图像）。
        3.  **监督微调（SFT）**：通过交错序列（如：文本→图像→3D→多视角图像）遍历“语义-视觉-几何”循环，强制实现跨模态生成一致性。

### 3. 实验设计
*   **数据集**：
    *   **2D**：63.7M 文本-图像对（含合成数据）。
    *   **3D**：120M 图像-3D对（主要来自Objaverse的渲染图），9M 文本-3D对（经过多粒度标注）。
    *   **合成数据**：利用 Z-Image 和 Meshy 4 生成了 2M 高质量文本-图像-3D三元组。
*   **评测任务与Benchmark**：
    *   **文本转3D形状**：使用 ULIP-T 和 Uni3D-T 指标评估语义对齐。
    *   **指令驱动的3D编辑**：在 **Edit3D-Bench** 上测试，评估指标为倒角距离（CD）和 F1 分数。
    *   **图像重建**：在 ImageNet-1K 上评估 FID、PSNR 和 SSIM。
*   **对比方法**：
    *   **两阶段方法**：Sana/LlamaGen + Hunyuan3D 2.1。
    *   **原生3D模型**：ShapeLLM-Omni (7B), Cube3D (1.6B)。
    *   **编辑方法**：Steer3D, 3DEditFormer, Tailor3D。

### 4. 资源与算力
*   **算力投入**：
    *   **预训练**：使用了 **256 张 NVIDIA H100 GPU**。
    *   **编辑微调**：使用了 32 张 H100 GPU。
*   **训练规模**：
    *   预训练阶段处理了约 **1.16 T (万亿)** 个 Token。
    *   SFT 阶段处理了约 0.15 T 个 Token。
    *   模型参数量约为 **2.2B**。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从基础生成到复杂编辑的多个维度，并对图像 Tokenizer 进行了独立的大规模验证。
*   **充分性**：
    *   对比了当前最先进的 7B 规模模型，证明了其 2B 模型在交错训练下的高效性。
    *   包含了定性展示（大量生成样例）和定量分析。
    *   **客观性**：通过在标准 Benchmark（如 Edit3D-Bench）上运行，并使用第三方预训练模型（ULIP/Uni3D）作为评估器，保证了结果的公正。

### 6. 主要结论与发现
*   **2D先验的有效性**：通过统一训练，2D图像的大规模知识能显著弥补3D数据的不足，提升3D生成的几何合理性和语义对齐度。
*   **交错训练的优势**：遍历“文本-图像-3D”循环比单纯的配对训练更能保证多视角一致性。
*   **参数效率**：2.2B 的 Omni123 在多项指标上超越了 7B 的 ShapeLLM-Omni，证明了架构设计和训练策略比单纯增加参数量更重要。

### 7. 优点
*   **原生统一性**：真正实现了在单一自回归框架下处理 2D 和 3D，无需中间代理或复杂的优化流程。
*   **高效 Tokenizer**：自研的 1D 视觉 Tokenizer 在保持高压缩比的同时，重建质量（FID/PSNR）优于 Cosmos-DI 和 LlamaGen 等主流模型。
*   **数据工程**：提出了一套完整的多粒度 3D 标注和高质量数据合成流水线，解决了 3D 领域“垃圾进、垃圾出”的问题。

### 8. 不足与局限
*   **分辨率限制**：3D Tokenizer 的网格分辨率目前是固定的，可能难以捕捉极细微的拓扑变化。
*   **视角依赖**：持续训练阶段仍依赖于 6 个标准视角（前、后、左、右、上、下），对于任意视角的连续控制尚有提升空间。
*   **场景限制**：目前主要集中在单物体生成，尚未扩展到复杂的 3D 场景生成。
*   **计算成本**：尽管参数量适中，但 1.16T Token 的预训练对算力资源（256x H100）要求极高，普通研究机构难以复现。

（完）
