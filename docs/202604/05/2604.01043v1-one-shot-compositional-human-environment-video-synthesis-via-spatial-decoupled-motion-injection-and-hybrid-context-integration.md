---
title: "ONE-SHOT: Compositional Human-Environment Video Synthesis via Spatial-Decoupled Motion Injection and Hybrid Context Integration"
title_zh: ONE-SHOT：通过空间解耦运动注入与混合上下文集成的组合式人-环境视频合成
authors: "Fengyuan Yang, Luying Huang, Jiazhi Guan, Quanwei Yang, Dongwei Pan, Jianglin Fu, Haocheng Feng, Wei He, Kaisiyuan Wang, Hang Zhou, Angela Yao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01043v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 组合式人物-环境视频合成
tldr: 针对视频生成模型中人景解耦编辑难、3D预处理繁琐的问题，本文提出ONE-SHOT框架。该框架通过规范空间注入机制解耦人体动态与环境，利用Dynamic-Grounded-RoPE实现无3D对齐的空间对应，并结合混合上下文集成确保长视频的一致性。ONE-SHOT在保持生成灵活性的同时，实现了高精度的结构控制，显著提升了长视频合成的质量与创意多样性。
selection_source: fresh_fetch
motivation: 现有的视频生成模型难以在不依赖复杂3D预处理的情况下，实现人物与环境的精细化独立编辑与控制。
method: 提出规范空间注入机制解耦运动与环境，并结合动态接地RoPE与混合上下文集成技术，实现高效的空间对应与长效一致性。
result: 实验证明该方法在结构控制精度和生成多样性上显著优于现有最先进技术，并支持分钟级长视频生成。
conclusion: ONE-SHOT为组合式人景视频合成提供了一种参数高效且无需复杂3D对齐的新方案，具有极高的实用价值。
---

## 摘要
视频基础模型（VFMs）的最新进展彻底改变了以人为中心的视频合成，但对主体和场景进行细粒度且独立的编辑仍然是一个关键挑战。最近通过刚性 3D 几何组合引入更丰富环境控制的尝试，往往在精确控制与生成灵活性之间面临明显的权衡。此外，繁重的 3D 预处理仍然限制了实际的可扩展性。在本文中，我们提出了 ONE-SHOT，这是一个用于组合式人-环境视频生成的参数高效框架。我们的核心见解是将生成过程分解为解耦的信号。具体而言，我们引入了一种规范空间注入机制，通过交叉注意力将人体动力学与环境线索解耦。我们还提出了 Dynamic-Grounded-RoPE，这是一种新颖的位置嵌入策略，能够在无需任何启发式 3D 对齐的情况下，在不同的空间域之间建立空间对应关系。为了支持长时程合成，我们引入了混合上下文集成机制，以在分钟级生成中保持主体和场景的一致性。实验表明，我们的方法显著优于现有最先进的方法，为视频合成提供了卓越的结构控制和创意多样性。我们的项目已发布在：https://martayang.github.io/ONE-SHOT/。

## Abstract
Recent advances in Video Foundation Models (VFMs) have revolutionized human-centric video synthesis, yet fine-grained and independent editing of subjects and scenes remains a critical challenge. Recent attempts to incorporate richer environment control through rigid 3D geometric compositions often encounter a stark trade-off between precise control and generative flexibility. Furthermore, the heavy 3D pre-processing still limits practical scalability. In this paper, we propose ONE-SHOT, a parameter-efficient framework for compositional human-environment video generation. Our key insight is to factorize the generative process into disentangled signals. Specifically, we introduce a canonical-space injection mechanism that decouples human dynamics from environmental cues via cross-attention. We also propose Dynamic-Grounded-RoPE, a novel positional embedding strategy that establishes spatial correspondences between disparate spatial domains without any heuristic 3D alignments. To support long-horizon synthesis, we introduce a Hybrid Context Integration mechanism to maintain subject and scene consistency across minute-level generations. Experiments demonstrate that our method significantly outperforms state-of-the-art methods, offering superior structural control and creative diversity for video synthesis. Our project has been available on: https://martayang.github.io/ONE-SHOT/.

---

## 论文详细总结（自动生成）

### ONE-SHOT：通过空间解耦运动注入与混合上下文集成的组合式人-环境视频合成

#### 1. 核心问题与整体含义
论文针对当前视频基础模型（VFMs）在以人为中心的视频合成中面临的三个核心瓶颈：
*   **3D预处理繁重**：现有方法依赖复杂的3D对齐（如点云与人体动作的精确匹配），限制了实际应用的可扩展性。
*   **生成灵活性受限**：强行引入硬性的3D几何约束往往会导致“过度调节（Over-conditioning）”，损害了模型原有的创意多样性和文本遵循能力。
*   **短时程限制**：难以在长视频（分钟级）中保持主体身份（ID）和环境空间的一致性。

**整体含义**：ONE-SHOT 提出了一种参数高效的框架，通过将生成过程分解为人体动力学、环境几何和摄像机轨迹等解耦信号，实现了在无需复杂3D对齐的前提下，对人物和场景进行独立的组合式控制。

#### 2. 方法论
核心思想是将人体运动注入与环境背景在空间上解耦，并利用混合上下文机制维持长效一致性。
*   **规范空间运动注入（Canonical-Space Injection）**：
    *   将人体动作（SMPL-X）置于一个去除了全局位移的“规范空间”中，通过专门的交叉注意力层注入模型。
    *   这种设计避免了动作信号与环境信号在输入端的直接耦合，减轻了条件竞争。
*   **动态接地旋转位置嵌入（Dynamic-Grounded-RoPE）**：
    *   为了解决规范空间动作与视频网格空间之间的尺度不匹配，引入了缩放因子 $s_h = h_c / h_r$ 和 $s_w = w_c / w_r$。
    *   该机制根据人体在视频中的粗略边界框（BBox）动态调整位置编码，使模型能在无需显式3D对齐的情况下，建立准确的空间对应关系。
*   **混合上下文集成（Hybrid Context Integration）**：
    *   **静态参考**：提取主体的面部和身体特征以保持身份一致性。
    *   **动态记忆**：引入动态更新的上下文记忆 Token，用于追踪长视频生成中的外观变化，防止身份漂移。
*   **参数高效微调**：基于 Wan2.1-14B 基础模型，仅通过 LoRA 和少量新增的注意力层参数进行优化。

#### 3. 实验设计
*   **数据集**：
    *   混合了 EMDB2（动态相机/人体标注）、MotionX（多样化人体动作）、ARKitScenes（室内场景点云）以及约 2.5 万段自采的 Web 视频，总计约 5 万个剪辑。
*   **Benchmark 与对比方法**：
    *   **测试集**：Traj100（用于自重构测试）和自建的 Cross-composition 测试集（用于评估身份/动作/场景的交叉组合）。
    *   **对比方法**：WanAnimate、RealisDance、Uni3C、RealisMotion。
*   **评估指标**：FID、FVD（视觉质量）、Motion Smoothness（动作平滑度）、Background Consistency（背景一致性）、Subject Similarity（主体相似度）等。

#### 4. 资源与算力
*   **模型底座**：使用 Wan2.1-14B 预训练模型。
*   **训练细节**：分辨率为 640x640。LoRA 学习率为 $1 \times 10^{-4}$，新增运动模块学习率为 $1 \times 10^{-5}$。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：进行了自重构（Self-Reconstruction）和交叉组合（Cross-Composition）两类大规模定量实验。
*   **消融实验**：针对 Dynamic-Grounded-RoPE 的有效性、面部参考的重要性、以及上下文记忆对长视频的影响进行了详细消融。
*   **充分性评价**：实验设计较为全面，涵盖了从短视频重构到长视频生成的多种场景。通过与多种 SOTA 方法在相同分辨率和帧数下对比，证明了其在背景稳定性和动作遵循度上的优势，实验结果客观且具有说服力。

#### 6. 主要结论与发现
*   **解耦控制的优越性**：通过规范空间注入，模型在保持高精度动作控制的同时，依然能响应复杂的文本指令（如“拿着长剑”）。
*   **空间对应能力**：Dynamic-Grounded-RoPE 有效解决了跨域空间对齐问题，显著降低了动作漂移。
*   **长视频稳定性**：混合上下文机制使得模型能够生成分钟级的视频，且在摄像机视角循环（Viewpoint Revisit）时仍能保持场景一致。
*   **性能领先**：在 FID 和 FVD 指标上均优于现有方法，尤其在背景一致性和动作平滑度上表现突出。

#### 7. 优点
*   **无需复杂对齐**：摆脱了对精确 3D 全局对齐的依赖，极大简化了推理流程。
*   **组合灵活性高**：支持独立更换身份、动作、场景和相机轨迹，具有极强的实用性。
*   **长效一致性**：通过动态记忆机制解决了生成式模型常见的“身份崩溃”问题。
*   **参数高效**：仅微调少量参数即可迁移强大的 VFM 能力。

#### 8. 不足与局限
*   **依赖前置质量**：生成效果高度依赖于场景点云重建的质量和人体估计模块（如 Human3R）的准确性。
*   **极端情况失效**：在用户提供的 BBox 严重错位或极端遮挡的情况下，可能会出现接地失败。
*   **长视频漂移**：虽然支持分钟级生成，但在极长视频中仍可能存在微小的时空漂移累积。
*   **算力信息缺失**：未披露训练成本，难以评估该方案的训练门槛。

（完）
