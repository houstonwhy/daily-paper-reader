---
title: "MotionScale: Reconstructing Appearance, Geometry, and Motion of Dynamic Scenes with Scalable 4D Gaussian Splatting"
title_zh: MotionScale：利用可扩展 4D 高斯泼溅重建动态场景的外观、几何与运动
authors: "Haoran Zhou, Gim Hee Lee"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29296v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 重建动态4D场景
tldr: MotionScale 针对单目视频 4D 场景重建中几何精度低和运动不一致的问题，提出了一种可扩展的 4D Gaussian Splatting 框架。该方法通过以簇为中心的基变换参数化运动场，并采用包含背景扩展与前景传播的渐进式优化策略，实现了对大规模、长序列动态场景的高保真重建，在保持结构一致性的同时显著提升了运动稳定性。
selection_source: fresh_fetch
motivation: 现有神经渲染方法在复杂动态场景中难以兼顾准确的 3D 几何恢复与长期的运动一致性。
method: 采用以簇为中心的基变换参数化运动场，并结合背景扩展与前景传播的渐进式优化策略进行场景重建。
result: 在多个真实世界基准测试中，该方法在重建质量和时间稳定性上均显著超越了当前最先进的技术。
conclusion: MotionScale 通过创新的运动场建模和优化流程，有效解决了大规模动态场景的高质量 4D 重建难题。
---

## 摘要
从单目视频中对动态 4D 场景进行逼真重建，对于理解物理世界至关重要。尽管神经渲染近期取得了进展，但现有方法在复杂环境中往往难以恢复准确的 3D 几何和时间一致的运动。为应对这些挑战，我们提出了 MotionScale，这是一个 4D 高斯泼溅框架，它能高效扩展到大规模场景和长序列，同时保持高保真的结构和运动连贯性。我们方法的核心是一个可扩展的运动场，它由以聚类为中心的基变换参数化，能够自适应地扩展以捕捉多样且演化的运动模式。为确保长时段内的鲁棒重建，我们引入了一种渐进式优化策略，包含两个解耦的传播阶段：1) 背景扩展阶段，适应新出现的可见区域，优化相机位姿，并显式建模瞬态阴影；2) 前景传播阶段，通过专门的三阶段细化过程强制执行运动一致性。在具有挑战性的真实世界基准测试上进行的大量实验表明，MotionScale 在重建质量和时间稳定性方面均显著优于现有最先进的方法。项目主页：https://hrzhou2.github.io/motion-scale-web/。

## Abstract
Realistic reconstruction of dynamic 4D scenes from monocular videos is essential for understanding the physical world. Despite recent progress in neural rendering, existing methods often struggle to recover accurate 3D geometry and temporally consistent motion in complex environments. To address these challenges, we propose MotionScale, a 4D Gaussian Splatting framework that scales efficiently to large scenes and extended sequences while maintaining high-fidelity structural and motion coherence. At the core of our approach is a scalable motion field parameterized by cluster-centric basis transformations that adaptively expand to capture diverse and evolving motion patterns. To ensure robust reconstruction over long durations, we introduce a progressive optimization strategy comprising two decoupled propagation stages: 1) A background extension stage that adapts to newly visible regions, refines camera poses, and explicitly models transient shadows; 2) A foreground propagation stage that enforces motion consistency through a specialized three-stage refinement process. Extensive experiments on challenging real-world benchmarks demonstrate that MotionScale significantly outperforms state-of-the-art methods in both reconstruction quality and temporal stability. Project page: https://hrzhou2.github.io/motion-scale-web/.

---

## 论文详细总结（自动生成）

### MotionScale：利用可扩展 4D 高斯泼溅重建动态场景

#### 1. 核心问题与研究动机
*   **核心问题**：如何从单目视频中高保真地重建大规模、长序列的动态 4D 场景（包含外观、几何和运动）。
*   **研究背景**：现有的动态神经渲染方法（如基于 NeRF 或 3D 高斯泼溅的方法）在处理复杂环境时，往往面临**几何畸变**和**长期运动漂移**的问题。这主要是因为它们过度依赖视图相关的外观信号，缺乏严格的 3D 结构约束，且运动模型在长序列中容易积累误差，导致几何崩溃。

#### 2. 核心方法论
MotionScale 提出了一种可扩展的 4D 高斯泼溅（4DGS）框架，其核心技术包括：
*   **以簇为中心的可扩展运动场（Cluster-centric Motion Field）**：
    *   将动态高斯点云划分为 $K$ 个不相交的簇。
    *   每个簇由一个全局刚性变换（SE(3)）和一组局部细化基变换（Basis Transformations）组成。
    *   通过学习系数对基变换进行混合，实现非刚性变形的参数化。
*   **自适应控制机制**：
    *   受 3DGS 增密策略启发，该方法能根据运动一致性自动分裂（Split）或修剪（Prune）簇，从而动态调整模型容量以适应复杂的运动模式。
*   **渐进式优化策略**：
    *   **背景扩展阶段**：识别新出现的可见区域并采样新的高斯点，同时通过光度损失精细化相机位姿，并引入专门的“阴影高斯”来建模动态物体的瞬态阴影。
    *   **前景传播阶段（三阶段细化）**：
        1.  **初始对齐**：利用单向跟踪损失将新帧与已优化帧粗略对齐。
        2.  **短期一致性**：在局部时间窗口内进行双向跟踪优化。
        3.  **长期细化**：在全序列范围内采样帧对，联合优化所有参数（包括几何、外观和运动），并触发簇的自适应分裂。

#### 3. 实验设计
*   **数据集**：
    *   **DAVIS**：具有复杂非刚性运动和自然相机移动的单目视频。
    *   **DyCheck**：包含 14 个真实场景，提供 LiDAR 深度和稀疏关键点标注，用于评估长期 3D 跟踪。
    *   **NVIDIA Dynamic Scenes**：校准过的多视图序列，涵盖多样的人类和物体活动。
*   **对比方法**：包括 HyperNeRF、DynIBaR、4D Gaussians、Shape of Motion、GFlow、4D-Fly 等最先进的动态重建模型。
*   **评估指标**：
    *   **渲染/新视角合成**：PSNR、SSIM、LPIPS。
    *   **3D 跟踪**：端点误差（EPE）、5cm/10cm 阈值内的点百分比（$\delta_{3D}$）。
    *   **2D 跟踪**：平均 Jaccard 指数（AJ）、平均位置精度（$\delta_{avg}$）、遮挡准确度（OA）。

#### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA RTX 4090 GPU** 上完成。
*   **软件实现**：基于 PyTorch 实现。
*   **训练时长**：文中未明确给出具体的训练总时长（小时/天），但强调了其簇基运动场在计算上是高效的，且内存占用低，能够处理长序列。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准数据集上进行了详尽的定量和定性对比。
*   **消融实验**：针对“簇基运动场”、“自适应控制”、“位姿精细化”、“阴影建模”以及“前景传播策略”分别进行了消融研究，验证了每个组件的有效性。
*   **充分性评估**：实验设计较为全面，不仅涵盖了视觉渲染质量，还深入评估了底层 3D 几何和运动轨迹的准确性。对比方法涵盖了从 NeRF 系到 GS 系的多种主流架构，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：MotionScale 在所有测试数据集的新视角合成（NVS）和 3D/2D 跟踪任务中均显著优于现有方法。
*   **几何稳定性**：通过渐进式优化和簇约束，该方法能有效抑制长序列中的几何漂移，保持锐利的表面和纹理细节。
*   **阴影处理的重要性**：显式建模阴影高斯不仅提升了背景渲染质量，还防止了前景物体为了补偿阴影而产生的几何扩张（Ghosting 伪影）。

#### 7. 亮点与优点
*   **可扩展性强**：簇基运动场允许模型容量随场景复杂度自适应增长，解决了全局变形场难以处理局部复杂运动的痛点。
*   **时空一致性**：三阶段的前景传播策略有效地将 2D 跟踪先验转化为一致的 3D 运动轨迹。
*   **解耦建模**：将背景、前景和阴影解耦处理，使得系统在处理大规模场景和长视频时更加鲁棒。

#### 8. 不足与局限
*   **依赖预训练先验**：该方法高度依赖于外部 2D 模型（如 SAM2 用于掩码、CoTracker3 用于跟踪、$\pi^3$ 用于深度和位姿）。如果这些初始先验存在严重错误，可能会影响最终的重建效果。
*   **计算开销随时间增长**：虽然单簇计算高效，但随着视频长度增加和簇的分裂，长期细化阶段的全局采样优化可能会面临内存和计算压力的上升。
*   **应用限制**：对于极度缺乏纹理或存在剧烈光照变化的场景，基于光度损失的位姿和几何精细化可能会失效。

（完）
