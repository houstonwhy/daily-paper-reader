---
title: "ProDiG: Progressive Diffusion-Guided Gaussian Splatting for Aerial to Ground Reconstruction"
title_zh: ProDiG：用于从航拍到地面重建的渐进式扩散引导高斯泼溅
authors: "Sirshapan Mitra, Yogesh S. Rawat"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02003v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 3D场景模型合成与重建
tldr: 针对仅有航拍图像时难以重建地面视图的问题，本文提出ProDiG框架。该方法利用扩散模型引导，通过逐步合成中间高度视图并优化高斯泼溅（Gaussian Splatting）表示，实现了从空中到地面的平滑过渡。核心贡献包括几何感知因果注意力模块和距离自适应高斯模块，有效解决了大跨度视角下的几何不一致和尺度变化问题，无需额外地面真值即可生成高质量地面渲染。
selection_source: fresh_fetch
motivation: 现有方法在处理航拍到地面的极端视角变化时，常面临几何不一致及对多高度真值数据的过度依赖。
method: 采用渐进式高度细化策略，结合几何感知因果注意力注入极线结构，并利用距离自适应模块动态调整高斯参数。
result: 实验证明该方法在视觉保真度、几何一致性以及应对极端视角变化的鲁棒性上均显著优于现有技术。
conclusion: ProDiG通过扩散引导的渐进优化，实现了无需地面参考的高质量、几何一致的航拍转地面3D场景重建。
---

## 摘要
仅凭航拍图像生成地面视角和连贯的 3D 场地模型具有挑战性，原因在于极端的视角变化、缺失的中间观测以及巨大的尺度差异。现有方法要么进行事后渲染优化，这通常会导致几何不一致的结果；要么依赖于多高度的真值数据，而这在现实中很难获得。高斯泼溅（Gaussian Splatting）和基于扩散的优化在小范围变化下能提高保真度，但在巨大的航拍到地面跨度下则会失效。为了解决这些局限性，我们提出了 ProDiG（渐进式高度高斯泼溅），这是一个扩散引导的框架，能够将航拍 3D 表示逐步转化为具有地面级保真度的表示。ProDiG 合成中间高度的视图，并利用几何感知的因果注意力模块在每个阶段优化高斯表示，该模块将对极结构注入到参考视图扩散中。距离自适应高斯模块根据相机距离动态调整高斯的尺度和不透明度，确保在巨大视角跨度下实现稳定的重建。这些组件共同实现了渐进式的、具有几何依据的优化，且无需额外的地面真值视角。在合成和真实世界数据集上的广泛实验表明，ProDiG 能够生成视觉逼真的地面渲染图和连贯的 3D 几何结构，在视觉质量、几何一致性以及对极端视角变化的鲁棒性方面显著优于现有方法。

## Abstract
Generating ground-level views and coherent 3D site models from aerial-only imagery is challenging due to extreme viewpoint changes, missing intermediate observations, and large scale variations. Existing methods either refine renderings post-hoc, often producing geometrically inconsistent results, or rely on multi-altitude ground-truth, which is rarely available. Gaussian Splatting and diffusion-based refinements improve fidelity under small variations but fail under wide aerial-to-ground gaps. To address these limitations, we introduce ProDiG (Progressive Altitude Gaussian Splatting), a diffusion-guided framework that progressively transforms aerial 3D representations toward ground-level fidelity. ProDiG synthesizes intermediate-altitude views and refines the Gaussian representation at each stage using a geometry-aware causal attention module that injects epipolar structure into reference-view diffusion. A distance-adaptive Gaussian module dynamically adjusts Gaussian scale and opacity based on camera distance, ensuring stable reconstruction across large viewpoint gaps. Together, these components enable progressive, geometrically grounded refinement without requiring additional ground-truth viewpoints. Extensive experiments on synthetic and real-world datasets demonstrate that ProDiG produces visually realistic ground-level renderings and coherent 3D geometry, significantly outperforming existing approaches in terms of visual quality, geometric consistency, and robustness to extreme viewpoint changes.

---

## 论文详细总结（自动生成）

这是一份关于论文《ProDiG: Progressive Diffusion-Guided Gaussian Splatting for Aerial to Ground Reconstruction》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何仅利用**航拍图像**（Aerial-only）重建出高质量、几何连贯的**地面级**（Ground-level）3D场景模型。
*   **研究动机**：
    *   **视角跨度极大**：航拍与地面视角之间存在极端的角度和尺度差异，中间观测数据缺失。
    *   **现有方法缺陷**：传统高斯泼溅（3DGS）在航拍数据上训练后，外推到地面视角时会出现严重的伪影和几何崩溃；现有的扩散模型优化方法往往缺乏几何约束，容易产生幻觉或跨视角不一致。
    *   **数据获取难**：现有的多高度重建方法通常需要地面真值（Ground-truth）进行配准，但在现实应用（如无人机测绘）中，地面数据往往不可得。

### 2. 方法论
ProDiG 提出了一个扩散引导的渐进式框架，核心思想是将“航拍到地面”的巨大跨度分解为多个中间高度层级进行逐步优化。
*   **渐进式高度细化（Progressive Altitude Refinement）**：
    *   不直接生成地面视图，而是合成一系列中间高度（如原始高度的 90%、70%...10%）的虚拟视图。
    *   在每个阶段，利用扩散模型修复渲染出的“噪声”视图，并将其作为伪标签重新训练 3D 高斯模型。
*   **aeroFix 扩散模块**：
    *   **因果注意力混合（Causal Attention Mixing）**：引入**极线几何（Epipolar Geometry）**约束。在扩散过程中，新视角的查询点只能关注参考视图中对应极线上的特征，从而强制保持几何一致性。
    *   **几何感知调节**：将相机位姿差异和 Plücker 射线嵌入注入扩散模型，增强对空间结构的感知。
    *   **损失函数**：结合了 DSSIM 损失和多尺度 Sobel 边缘损失，以保留建筑边缘等高频结构。
*   **距离自适应高斯模块（Distance-Adaptive Gaussian Module）**：
    *   使用轻量级 MLP 根据高斯点到相机的距离动态调整其尺度（Scale）和不透明度（Opacity），解决了大范围视角切换下的细节层次（LoD）问题。

### 3. 实验设计
*   **数据集**：
    *   **WRIVA**：具有挑战性的真实世界野外数据集，包含大幅度的航拍到地面视角变化。
    *   **MatrixCity**：大规模城市合成数据集，用于精确评估结构和感知质量。
    *   **Aerial MegaDepth**：用于验证模型在不同高度和稀疏视角下的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了 **3DGS**、**2DGS**、**Scaffold-GS**、**GS-MCMC** 以及 **Difix3D+**（目前的 SOTA 扩散优化方法）。
    *   评估指标包括结构性指标（PSNR, SSIM）和感知指标（LPIPS, **DreamSim**，后者更符合人类视觉感知）。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA RTX A6000** GPU。
*   **训练细节**：
    *   初始 3DGS 训练 7,000 次迭代。
    *   采用 5 个阶段的渐进式细化（高度从 90% 降至 10%）。
    *   扩散模型训练使用了约 3.5 万对“噪声-清晰”图像对。
*   **说明**：文中未明确给出总训练时长，但由于涉及 5 阶段的迭代训练和扩散模型推理，其计算开销显著高于标准 3DGS。

### 5. 实验数量与充分性
*   **实验规模**：在两个主要数据集（WRIVA 的多个站点和 MatrixCity）上进行了详尽对比，并进行了跨数据集的泛化实验。
*   **消融实验**：针对渐进策略（5 种不同轨迹）、距离自适应模块、以及 aeroFix 的各个组件（位姿嵌入、极线注意力、损失函数）均做了独立验证。
*   **评价**：实验设计较为充分且客观。通过引入 DreamSim 指标，有效地捕捉了在极端视角变换下传统 PSNR 难以反映的视觉质量提升。

### 6. 主要结论与发现
*   **性能领先**：ProDiG 在所有数据集上的感知质量（DreamSim/LPIPS）均显著优于基准方法，尤其在 WRIVA 真实场景中，PSNR 提升了约 2dB。
*   **几何一致性**：通过极线约束，ProDiG 有效减少了扩散模型常见的“内容拷贝”和“结构幻觉”问题。
*   **渐进式学习的必要性**：实验证明，直接从航拍跳跃到地面会导致模型崩溃，而逐步降低高度能让 3D 表示平滑适应尺度变化。

### 7. 优点
*   **无需地面真值**：实现了真正的“仅航拍”输入下的地面级重建，具有极强的实用价值。
*   **几何与生成结合**：巧妙地将多视图几何（极线约束）融入扩散模型，解决了生成式方法难以保持 3D 一致性的顽疾。
*   **动态尺度处理**：距离自适应模块有效缓解了 3DGS 在处理大跨度缩放时的走样问题。

### 8. 不足与局限
*   **计算成本**：多阶段的“渲染-扩散修复-重训练”循环非常耗时，难以实现快速重建。
*   **依赖初始质量**：如果初始航拍 SfM（运动恢复结构）或点云质量极差，渐进式优化可能难以纠正根本性的几何错误。
*   **动态物体处理**：虽然使用了外观嵌入，但在处理包含大量动态行人或车辆的复杂城市街道时，仍可能存在伪影（如文中提到真值图与重建图在车辆上的差异）。

（完）
