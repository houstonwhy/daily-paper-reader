---
title: "SGS-Intrinsic: Semantic-Invariant Gaussian Splatting for Sparse-View Indoor Inverse Rendering"
title_zh: SGS-Intrinsic：用于稀疏视角室内逆渲染的语义不变高斯泼溅
authors: "Jiahao Niu, Rongjia Zheng, Wenju Xu, Wei-Shi Zheng, Qing Zhang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27516v2"
tags: ["query:3dgen"]
score: 8.0
evidence: 室内3D场景重建与逆向渲染
tldr: 针对室内场景在稀疏视角下逆渲染质量差的问题，本文提出SGS-Intrinsic框架。该方法利用语义和几何先验构建稠密且几何一致的高斯语义场，为逆渲染奠定基础。通过结合混合光照模型与材质先验，实现了材质与光照的精确解耦，并引入光照不变性约束和去阴影模型以增强鲁棒性。实验证明，该方法在重建保真度和逆渲染质量上均优于现有3DGS方法。
selection_source: fresh_fetch
motivation: 现有的3D高斯泼溅逆渲染方法在处理稀疏视角室内场景时，难以实现高质量的几何重建和材质光照解耦。
method: 利用语义和几何先验构建一致的高斯语义场，并结合混合光照模型、材质先验及去阴影模型进行材质与光照的解耦。
result: 在基准数据集上的实验表明，该方法在重建精度和逆渲染效果上均显著优于现有的3DGS逆渲染技术。
conclusion: SGS-Intrinsic为稀疏视角下的室内逆渲染提供了一个稳健的框架，有效提升了复杂光影环境下的材质恢复能力。
---

## 摘要
我们提出了 SGS-Intrinsic，这是一个适用于稀疏视角图像的室内逆渲染框架。与现有的基于 3D 高斯泼溅 (3DGS) 的方法不同，这些方法通常侧重于以物体为中心的重建，且在稀疏视角设置下表现不佳，而我们的方法能够实现高质量的几何重建以及材质和照明的准确解耦。核心思想是在语义和几何先验的指导下，构建一个稠密且几何一致的高斯语义场，为后续的逆渲染提供可靠基础。在此基础上，我们通过结合混合照明模型和材质先验来进行材质-照明解耦，从而有效地捕捉照明与材质之间的相互作用。为了减轻投射阴影的影响并增强材质恢复的鲁棒性，我们引入了照明不变的材质约束以及一个去阴影模型。在基准数据集上的广泛实验表明，与现有的基于 3DGS 的逆渲染方法相比，我们的方法在重建保真度和逆渲染质量方面均有持续提升。我们的代码可在 https://github.com/GrumpySloths/SGS_Intrinsic.github.io 获取。

## Abstract
We present SGS-Intrinsic, an indoor inverse rendering framework that works well for sparse-view images. Unlike existing 3D Gaussian Splatting (3DGS) based methods that focus on object-centric reconstruction and fail to work under sparse view settings, our method allows to achieve high-quality geometry reconstruction and accurate disentanglement of material and illumination. The core idea is to construct a dense and geometry-consistent Gaussian semantic field guided by semantic and geometric priors, providing a reliable foundation for subsequent inverse rendering. Building upon this, we perform material-illumination disentanglement by combining a hybrid illumination model and material prior to effectively capture illumination-material interactions. To mitigate the impact of cast shadows and enhance the robustness of material recovery, we introduce illumination-invariant material constraint together with a deshadowing model. Extensive experiments on benchmark datasets show that our method consistently improves both reconstruction fidelity and inverse rendering quality over existing 3DGS-based inverse rendering approaches. Our code is available at https://github.com/GrumpySloths/SGS_Intrinsic.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《SGS-Intrinsic: Semantic-Invariant Gaussian Splatting for Sparse-View Indoor Inverse Rendering》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**稀疏视角（Sparse-view）**输入下，实现高质量的室内场景**逆渲染（Inverse Rendering）**，即从少量图像中准确分解出几何（Geometry）、材质（Material）和光照（Illumination）。
*   **研究背景与动机**：
    *   **室内场景复杂性**：室内环境存在复杂的近场光照、阴影遮挡以及材质与光照的高度耦合。
    *   **稀疏视角挑战**：在视角极少时，传统的 3D 高斯泼溅（3DGS）容易出现几何重建不稳定、过拟合观测视角以及将阴影“烘焙”到材质（Albedo）中的问题。
    *   **现有方法局限**：现有的 3DGS 逆渲染方法多针对孤立物体，难以处理具有复杂全局光照和遮挡关系的室内全景。

### 2. 方法论
该论文提出了一个名为 **SGS-Intrinsic** 的两阶段框架：

*   **第一阶段：几何与语义重建**
    *   **稠密初始化**：利用 VGGT 模型构建稠密的场景布局点云，而非依赖稀疏的 SfM 点云。
    *   **先验引导**：引入预训练模型（StableNormal 和 LSEG）提供的法线和语义先验进行监督。
    *   **语义一致性约束**：在训练视角和虚拟（伪）视角之间引入语义一致性损失（$L_{corresponding}$），通过区域掩码和多数投票机制缓解稀疏视角下的过拟合。
*   **第二阶段：固定几何下的逆渲染**
    *   **混合光照模型**：结合环境贴图（处理低频环境光）和球面高斯混合模型（SGMs，处理高频近场点光源）。
    *   **轻量化去阴影模块**：使用多分辨率哈希编码器（Hash Encoder）和 MLP 构建遮挡场，显式预测表面点与光源间的可见性（Visibility），防止阴影干扰材质恢复。
    *   **材质不变性约束**：
        *   **光照不变性**：在固定视角下通过扰动光照，强制要求渲染的反射率（Albedo）保持一致。
        *   **跨视角语义一致性**：基于“语义相似区域材质相似”的假设，约束不同视角下相同语义区域的材质属性。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：Interiorverse（场景级）、TensoIR（物体级）。
    *   **真实数据集**：Mip-NeRF 360、FIPT、DL3DV（包含复杂光照环境）。
*   **基准方法（Benchmarks）**：对比了 GSIR、GIGS、R3DG、IRGS、SVGIR 和 GeoSplat 等最先进的基于 3DGS 的逆渲染方法。
*   **评价指标**：PSNR、SSIM、LPIPS（图像质量）；MAE（法线精度）；MSE（粗糙度/反射率精度）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   第一阶段：7000 次迭代。
    *   第二阶段：3000 次迭代。
    *   **总时长**：约 **40 分钟**（另有约 3 分钟的预处理时间，包括 VGGT、SAM2 等模型的推理）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流数据集上进行了测试，涵盖了从物体到复杂室内场景、从合成到真实的多种情况。
*   **消融实验**：针对法线先验、语义一致性、混合光照（SGM）、去阴影模块、光照不变损失等核心组件均做了详尽的消融分析。
*   **鲁棒性测试**：专门测试了模型对几何噪声、法线/语义先验噪声以及相机位姿误差的容忍度。
*   **充分性评价**：实验设计全面，对比方法均为近年顶级会议成果，定量与定性分析互补，客观性较强。

### 6. 主要结论与发现
*   **几何是基础**：在稀疏视角下，通过语义和几何先验构建稳健的几何场是后续材质解耦成功的关键。
*   **显式建模遮挡至关重要**：引入去阴影模块能显著减少材质图中的阴影残留，提高 PBR 渲染的真实感。
*   **语义引导的有效性**：语义特征不仅有助于几何重建，还能作为材质一致性的强力约束，弥补稀疏视角下监督信号不足的问题。
*   **性能领先**：SGS-Intrinsic 在所有测试数据集上的重建保真度和逆渲染质量均显著优于现有 SOTA 方法。

### 7. 优点与亮点
*   **创新的光照模型**：混合光照设计兼顾了室内环境的全局氛围和局部高频亮斑。
*   **去阴影机制**：通过轻量化遮挡场解决 3DGS 逆渲染中常见的阴影“烘焙”痛点。
*   **端到端一致性**：巧妙利用伪视角（Pseudo-view）和语义一致性来增强模型的泛化能力和空间连续性。
*   **实用性强**：训练速度快（40 分钟），且支持光照编辑、物体插入等下游应用。

### 8. 不足与局限
*   **依赖预训练模型**：虽然论文证明了其对噪声有一定鲁棒性，但整体效果仍高度依赖于 VGGT、StableNormal 等外部先验模型的质量。
*   **计算开销**：虽然 40 分钟在逆渲染领域算快，但相比于基础 3DGS 的实时训练仍有差距，且引入了多个大型预训练模型作为预处理步骤。
*   **极端遮挡处理**：在极度复杂的室内死角或完全无光照区域，材质恢复可能仍存在偏差。

（完）
