---
title: "SceneExpander: Expanding 3D Scenes with Free-Form Inserted Views"
title_zh: SceneExpander：利用自由形式插入视图扩展三维场景
authors: "Zijian He, Renjie Liu, Yihao Wang, Weizhi Zhong, Huan Yuan, Kun Gai, Guangrun Wang, Guanbin Li"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27084v2"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用生成模型扩展3D场景
tldr: 针对3D场景构建中迭代扩展的需求，本文提出SceneExpander，旨在将生成模型合成的新视角无缝融入现有实景重建中。面对新视角与原场景几何不对齐导致的全局一致性破坏问题，该方法通过测试时自适应技术，结合锚点蒸馏和插入视角自蒸馏信号，在保持原场景稳定的同时，有效处理几何偏移和伪影，实现了高质量的3D场景扩展。
selection_source: fresh_fetch
motivation: 现有的3D场景编辑难以处理用户插入的、与原场景几何不一致的新视角，导致重建结果缺乏全局多视角一致性。
method: 提出一种基于测试时自适应的参数化前馈3D重建模型，利用锚点蒸馏稳定原场景并结合插入视角自蒸馏来适应几何偏差。
result: 在ETH场景和在线数据集上的实验证明，该方法在处理视角不对齐时具有更优的扩展性能和重建质量。
conclusion: SceneExpander为用户主导的迭代式3D场景扩展提供了一种有效的解决方案，增强了重建模型对非对齐生成内容的鲁棒性。
---

## 摘要
使用三维场景表示进行世界构建在内容创作、模拟和交互体验中变得日益重要，然而实际的工作流程本质上是迭代的：创作者必须在用户控制下重复扩展现有场景。受此研究空白的启发，我们研究了一种以用户为中心的工作流中的三维场景扩展：从多视图图像捕获的真实场景出发，我们通过插入由生成模型合成的额外视图来扩展其覆盖范围。与固定场景中的简单物体编辑或风格迁移不同，插入的视图通常与原始重建在三维空间上不匹配，从而引入几何偏移、幻觉内容或视点相关的伪影，破坏了全局多视图一致性。为了应对这一挑战，我们提出了 SceneExpander，它将测试时自适应应用于参数化前馈三维重建模型，并结合了两种互补的蒸馏信号：锚点蒸馏通过从捕获的视图中提取几何线索来稳定原始场景，而插入视图自蒸馏则保留受观测支持的预测，同时调整潜在几何和外观以适应不匹配的插入视图。在 ETH 场景和在线数据上的实验表明，在存在失配的情况下，该方法具有更好的扩展表现和重建质量。

## Abstract
World building with 3D scene representations is increasingly important for content creation, simulation, and interactive experiences, yet real workflows are inherently iterative: creators must repeatedly extend an existing scene under user control. Motivated by this research gap, we study 3D scene expansion in a user-centric workflow: starting from a real scene captured by multi-view images, we extend its coverage by inserting an additional view synthesized by a generative model. Unlike simple object editing or style transfer in a fixed scene, the inserted view is often 3D-misaligned with the original reconstruction, introducing geometry shifts, hallucinated content, or view-dependent artifacts that break global multi-view consistency. To address the challenge, we propose SceneExpander, which applies test-time adaptation to a parametric feed-forward 3D reconstruction model with two complementary distillation signals: anchor distillation stabilizes the original scene by distilling geometric cues from the captured views, while inserted-view self-distillation preserves observation-supported predictions yet adapts latent geometry and appearance to accommodate the misaligned inserted view. Experiments on ETH scenes and online data demonstrate improved expansion behavior and reconstruction quality under misalignment.

---

## 论文详细总结（自动生成）

### 论文总结：SceneExpander —— 利用自由形式插入视图扩展三维场景

#### 1. 论文的核心问题与整体含义
在三维世界构建（World Building）中，创作者通常需要迭代式地扩展现有场景。目前的挑战在于：当用户使用生成模型（如 AIGC 工具）合成一个新视角并试图将其插入到已有的实景重建场景中时，生成图往往与原场景存在**几何不对齐（3D-misalignment）**。这种不一致性会导致重建模型产生几何偏移、幻觉内容或严重的视点伪影（如重影）。论文的核心目标是提出一种鲁棒的框架，在保持原场景重建质量的同时，能够将非完全 3D 一致的插入视图整合进场景，实现可控的 3D 场景扩展。

#### 2. 论文提出的方法论
SceneExpander 基于前馈 3D 重建模型（以 WorldMirror 为骨干），通过**测试时自适应（Test-Time Adaptation, TTA）**技术来处理不匹配的约束。其核心流程如下：

*   **核心思想**：不要求生成视图绝对精确，而是通过场景特定的微调，让模型在“保留原始证据”和“适应新约束”之间取得平衡。
*   **锚点蒸馏（Anchor Distillation）**：使用一个冻结的“锚点教师”模型，提取原捕获视图的几何线索（相机参数、深度图、法线图），强制学生模型在扩展过程中保持原场景的稳定性。
*   **插入视图自蒸馏（Inserted-view Self-distillation）**：引入一个以指数移动平均（EMA）方式更新的“生成教师”模型。它为插入视图提供动态更新的软标签（Soft Labels），使模型能以较小的权重逐步吸收新内容，避免因硬性拟合噪声导致的几何崩溃。
*   **几何扰动增强与子集采样**：在微调过程中，对输入视图进行全局和局部块状的仿射变换，模拟几何偏移，并随机采样视图子集，以增强模型对空间不一致性的鲁棒性。
*   **随机恢复（Stochastic Restoration）**：定期将部分参数拉回初始权重，防止在长时间自适应过程中产生严重的漂移。

#### 3. 实验设计
*   **数据集**：
    *   **ETH 数据集**：包含具有大基线变化的真实户外场景。
    *   **Online Collection**：涵盖多样的室内外场景。
*   **Benchmark 与对比方法**：
    *   对比了前馈重建模型：**WorldMirror**、**OmniVGGT**。
    *   对比了动态/序列重建模型：**TTT3R**。
    *   对比了基于优化的方法：**3D Gaussian Splatting (3DGS)**。
*   **评估指标**：
    *   **图像保真度**：PSNR、SSIM、LPIPS（针对原捕获区域）。
    *   **VLM 评估**：利用 GPT-5.2 对插入满意度（GPT-IS）和渲染质量（GPT-RQ）进行评分。
    *   **人类评估**：39 名参与者对插入效果和导航稳定性进行排序评分。

#### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但在实现细节中提到：
*   使用 AdamW 优化器，学习率为 $1 \times 10^{-5}$。
*   采用了 3D Gaussian Splatting 作为底层表示，并使用微分渲染器。
*   测试时自适应涉及 T 个步骤的迭代优化（具体 T 值未在正文详述，通常此类 TTA 任务在单卡上需数分钟）。

#### 5. 实验数量与充分性
*   **实验规模**：在 ETH 和在线数据集上进行了广泛测试，涵盖了从室内客厅到户外森林、遗迹等多种复杂场景。
*   **消融实验**：详细验证了锚点蒸馏、数据增强、自蒸馏概率 $p$ 以及随机恢复机制的有效性。
*   **充分性评价**：实验设计较为全面，不仅有客观的物理指标（PSNR 等），还引入了前沿的 VLM（GPT-5.2）评估和大规模人类主观测试，这在缺乏 Ground Truth 的场景扩展任务中是非常客观且必要的。

#### 6. 论文的主要结论与发现
*   **SceneExpander 显著优于基线**：在处理几何不对齐的插入视图时，传统方法（如 3DGS 直接优化）会产生严重的重影，而 SceneExpander 能保持原场景清晰并成功融入新物体。
*   **TTA 的必要性**：对于 AIGC 生成的非一致性内容，固定的前馈推理过于脆弱，而测试时自适应能有效缓解几何冲突。
*   **平衡策略**：通过锚点蒸馏保护“旧知识”与通过自蒸馏学习“新知识”的结合，是实现鲁棒扩展的关键。

#### 7. 优点
*   **用户友好型工作流**：将文本驱动的图像生成与 3D 重建无缝结合，符合实际的迭代创作逻辑。
*   **鲁棒性强**：不依赖于生成模型必须输出完美的 3D 一致性图像，降低了对上游生成器的要求。
*   **评估维度全面**：结合了传统指标、AI 自动评估和人类主观感受，结果具有说服力。

#### 8. 不足与局限
*   **严重失配处理有限**：当插入视图与原场景的几何冲突过于剧烈时（如文中提到的神社横梁案例），模型仍无法完全消除伪影。
*   **幻觉风险**：扩展区域的内容本质上是合成的，可能存在几何上合理但物理上不真实的情况。
*   **计算开销**：作为一种测试时自适应方法，相比于纯前馈模型，它在推理阶段需要额外的优化时间，可能影响实时交互体验。

（完）
