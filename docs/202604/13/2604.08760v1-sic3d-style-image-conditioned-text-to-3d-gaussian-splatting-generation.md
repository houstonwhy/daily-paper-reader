---
title: "SIC3D: Style Image Conditioned Text-to-3D Gaussian Splatting Generation"
title_zh: SIC3D：以风格图像为条件的文本到3D高斯泼溅生成
authors: "Ming He, Zhixiang Chen, Steve Maddock"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08760v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 文本到3D高斯泼溅生成
tldr: SIC3D 是一种基于 3D Gaussian Splatting (3DGS) 的可控文本到 3D 生成框架，旨在解决纯文本驱动生成中纹理模糊和控制力不足的问题。该方法采用两阶段流程：首先从文本生成 3D 几何内容，随后通过参考图像进行风格迁移。核心创新在于引入了变分风格化分数蒸馏（VSSD）损失和缩放正则化，有效捕捉图像的全局与局部纹理，同时保持几何完整性，在生成质量和风格一致性上显著优于现有方法。
selection_source: fresh_fetch
motivation: 针对纯文本驱动的 3D 生成中存在的纹理模糊和缺乏精细控制力的问题，提出引入图像条件来增强生成效果。
method: 采用两阶段生成策略，结合 3DGS 表示，并引入变分风格化分数蒸馏（VSSD）损失和缩放正则化来实现精准的风格迁移。
result: 实验证明 SIC3D 在几何保真度和风格还原度上均优于现有方法，能生成具有高质量纹理和准确风格的 3D 模型。
conclusion: 该研究通过结合图像引导和 3DGS 技术，为实现高可控性、高保真度的 3D 风格化生成提供了有效的技术路径。
---

## 摘要
最近在文本到3D物体生成方面的进展，通过利用2D扩散模型和可微分3D表示，实现了从文本输入合成精细几何结构。然而，由于文本模态的局限性，这些方法通常面临可控性有限和纹理模糊的问题。为了解决这一问题，我们提出了 SIC3D，这是一个基于3D高斯泼溅（3DGS）的可控图像条件文本到3D生成流水线。SIC3D 包含两个阶段：第一阶段利用文本到3DGS生成模型从文本生成3D物体内容；第二阶段将参考图像的风格迁移到3DGS中。在此风格化阶段，我们引入了一种新颖的变分风格化分数蒸馏（VSSD）损失，以有效捕捉全局和局部纹理模式，同时缓解几何与外观之间的冲突。此外，我们还应用了缩放正则化，以防止伪影的出现并保留风格图像中的模式。广泛的实验表明，SIC3D 增强了几何保真度和风格一致性，在定性和定量评估中均优于以往的方法。

## Abstract
Recent progress in text-to-3D object generation enables the synthesis of detailed geometry from text input by leveraging 2D diffusion models and differentiable 3D representations. However, the approaches often suffer from limited controllability and texture ambiguity due to the limitation of the text modality. To address this, we present SIC3D, a controllable image-conditioned text-to-3D generation pipeline with 3D Gaussian Splatting (3DGS). There are two stages in SIC3D. The first stage generates the 3D object content from text with a text-to-3DGS generation model. The second stage transfers style from a reference image to the 3DGS. Within this stylization stage, we introduce a novel Variational Stylized Score Distillation (VSSD) loss to effectively capture both global and local texture patterns while mitigating conflicts between geometry and appearance. A scaling regularization is further applied to prevent the emergence of artifacts and preserve the pattern from the style image. Extensive experiments demonstrate that SIC3D enhances geometric fidelity and style adherence, outperforming prior approaches in both qualitative and quantitative evaluations.

---

## 论文详细总结（自动生成）

以下是关于论文《SIC3D: Style Image Conditioned Text-to-3D Gaussian Splatting Generation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的文本到3D（Text-to-3D）生成技术虽然在几何合成上取得了进展，但由于文本描述的局限性，往往面临**纹理模糊**和**风格控制力不足**的问题。
*   **研究动机**：
    *   纯文本难以精确描述复杂的艺术风格或特定纹理。
    *   现有的3D风格化方法（如基于NeRF的方法）优化速度慢，且容易导致几何畸变或多视角不一致。
    *   3D高斯泼溅（3DGS）虽然高效，但缺乏成熟的图像条件风格化框架。
*   **目标**：开发一个既能保持几何稳定性，又能根据参考图像进行精确风格迁移的3DGS生成流水线。

### 2. 方法论
SIC3D 采用**两阶段生成策略**，将几何构建与外观精细化解耦：
*   **第一阶段：物体几何生成**
    *   利用文本提示词，通过变分分数蒸馏（VSD）和3DGS表示（如 GaussianDreamer 框架）生成一个几何一致的基础3D模型。
*   **第二阶段：风格蒸馏（核心创新）**
    *   **变分风格化分数蒸馏（VSSD）**：在VSD的基础上引入图像条件。使用预训练的 **IP-Adapter** 作为图像条件处理器，将参考图像的风格特征注入扩散模型的交叉注意力层。
    *   **LoRA 与相机投影**：引入轻量级 LoRA 模块和相机投影层，在优化过程中捕捉特定物体的多视角特征，确保风格在不同视角下的连续性。
    *   **缩放正则化（Scaling Regularization）**：针对3DGS在优化中高斯基元可能过度膨胀导致纹理模糊的问题，提出约束高斯基元的近似表面积，强制其保持紧凑，从而获得清晰的边缘和纹理。

### 3. 实验设计
*   **对比基准（Benchmarks）**：
    *   **风格化提示词基线**：使用 GPT-4o 为风格图生成详细描述，附加到文本提示词中进行单阶段生成。
    *   **SOTA 3D风格化方法**：StyleGaussian、G-Style、SGSST。
*   **评估指标**：
    *   **定量指标**：NNFM（风格相似度）、RMSE（像素级偏差）、LPIPS（感知相似度）。
    *   **人类对齐评估**：利用 GPT-4o 作为评估器，计算 ELO 分数，评估物体质量（Object Quality）和风格对齐度（Style Alignment）。
*   **消融实验**：验证了缩放约束、固定相机采样策略以及 LoRA 模块对最终效果的贡献。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 GPU** (80GB)。
*   **显存占用**：第一阶段约 18.7 GB，第二阶段约 18.9 GB。
*   **耗时**：
    *   第一阶段（500步）：约 0.66 秒/步。
    *   第二阶段（500步）：约 0.85 秒/步。
    *   实际应用中，第二阶段通常在 200 步左右即可获得理想效果。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 20 个文本提示词和 9 张风格图像（涵盖写实与艺术风格）。
    *   随机采样了 100 组“文本-图像”组合，每组使用 3 个随机种子生成。
    *   GPT-4o 进行了 350 次成对比较。
*   **充分性评价**：实验设计较为充分。不仅涵盖了主流的定量指标，还引入了先进的 LLM 辅助评估，并针对核心组件进行了详尽的消融实验，客观地展示了该方法在处理不同风格和几何形状时的鲁棒性。

### 6. 主要结论与发现
*   **风格一致性**：SIC3D 在 NNFM 和 LPIPS 指标上均优于对比方法，证明其能更准确地捕捉参考图的色彩和纹理模式。
*   **几何保真度**：通过两阶段解耦，SIC3D 避免了风格化过程对物体基础几何的破坏，ELO 评分显示其在物体质量上排名第一。
*   **正则化有效性**：缩放正则化是消除 3DGS 风格化中常见伪影（如模糊边缘）的关键。

### 7. 优点（亮点）
*   **解耦设计**：成功解决了几何构建与风格迁移之间的冲突，提高了生成的稳定性。
*   **VSSD 损失函数**：将 IP-Adapter 引入 3D 蒸馏领域，提供了一种比纯文本更强大的风格控制手段。
*   **高效性**：基于 3DGS 表示，相比传统的 NeRF 风格化方法，推理和优化速度显著提升。
*   **通用性**：第二阶段的风格化框架不依赖于特定的第一阶段生成器（实验证明其可适配 GaussianDreamer 或 TRELLIS）。

### 8. 不足与局限
*   **多视角一致性风险**：虽然使用了固定视角采样，但由于底层 2D 扩散模型本身缺乏显式的 3D 约束，在某些复杂重叠区域仍可能出现局部纹理不一致。
*   **背景处理**：目前的优化主要集中在物体本身，对于复杂背景的风格化处理提及较少。
*   **依赖预训练模型**：效果高度依赖于 2D 扩散模型（如 Stable Diffusion）和 IP-Adapter 的先验知识，若预训练模型未见过某些极端风格，生成效果可能受限。

（完）
