---
title: "V-CAGE: Vision-Closed-Loop Agentic Generation Engine for Robotic Manipulation"
title_zh: V-CAGE：用于机器人操作的视觉闭环智能体生成引擎
authors: "Yaru Liu, Ao-bo Wang, Nanyang Ye"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09036v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于机器人数据合成的自主场景生成代理框架
tldr: "V-CAGE是一个用于机器人操作数据合成的智能体框架，旨在解决现有VLA模型训练数据缺乏语义一致性和物理可行性的问题。该框架通过修复引导的场景构建实现上下文感知的布局规划，确保目标可达；并引入基于VLM的闭环验证机制作为视觉评论员，严格过滤无效轨迹。此外，它采用感知驱动的压缩算法，在不影响训练效果的前提下减少了90%以上的存储需求。V-CAGE实现了端到端的数据合成自动化，为大规模、高质量机器人数据集的生成提供了高效方案。"
selection_source: fresh_fetch
motivation: 现有机器人场景生成方法因缺乏上下文感知常导致目标不可达，难以满足VLA模型对大规模、高质量且物理可行数据的需求。
method: 提出一种集成修复引导场景构建、VLM闭环验证及感知驱动视频压缩的智能体化框架，实现从布局规划到轨迹校验的自动化流程。
result: 该系统能够高效合成语义丰富且运动学可达的操作数据，并在大幅降低存储成本的同时保证了下游模型训练的有效性。
conclusion: V-CAGE通过语义布局规划与视觉自验证的中心化管理，为构建大规模、高保真的机器人操作数据集提供了自动化的闭环解决方案。
---

## 摘要
扩展视觉-语言-动作（VLA）模型需要大规模的数据集，这些数据集必须在语义上连贯且在物理上可行。然而，现有的场景生成方法通常缺乏上下文感知能力，难以合成嵌入丰富语义信息的高保真环境，经常导致目标位置不可达，从而使任务过早失败。我们提出了 V-CAGE（视觉闭环智能体生成引擎），这是一个用于自主机器人数据合成的智能体框架。与传统的脚本化流水线不同，V-CAGE 作为一个具身智能体系统运行，利用基础模型将高层语义推理与底层物理交互联系起来。具体而言，我们引入了修复引导的场景构建（Inpainting-Guided Scene Construction），以系统地安排上下文感知布局，确保生成的场景在语义上结构化且在运动学上可达。为了确保轨迹的正确性，我们将功能元数据与基于视觉语言模型（VLM）的闭环验证机制相结合，充当视觉评论者（visual critic），严格过滤掉隐性失败并切断错误传播链。最后，为了克服大规模视频数据集的存储瓶颈，我们实现了一种感知驱动的压缩算法，在不损害下游 VLA 训练效果的前提下，实现了超过 90% 的文件大小缩减。通过集中语义布局规划和视觉自我验证，V-CAGE 实现了端到端流水线的自动化，从而能够高度可扩展地合成多样化、高质量的机器人操作数据集。

## Abstract
Scaling Vision-Language-Action (VLA) models requires massive datasets that are both semantically coherent and physically feasible. However, existing scene generation methods often lack context-awareness, making it difficult to synthesize high-fidelity environments embedded with rich semantic information, frequently resulting in unreachable target positions that cause tasks to fail prematurely. We present V-CAGE (Vision-Closed-loop Agentic Generation Engine), an agentic framework for autonomous robotic data synthesis. Unlike traditional scripted pipelines, V-CAGE operates as an embodied agentic system, leveraging foundation models to bridge high-level semantic reasoning with low-level physical interaction. Specifically, we introduce Inpainting-Guided Scene Construction to systematically arrange context-aware layouts, ensuring that the generated scenes are both semantically structured and kinematically reachable. To ensure trajectory correctness, we integrate functional metadata with a Vision-Language Model based closed-loop verification mechanism, acting as a visual critic to rigorously filter out silent failures and sever the error propagation chain. Finally, to overcome the storage bottleneck of massive video datasets, we implement a perceptually-driven compression algorithm that achieves over 90\% filesize reduction without compromising downstream VLA training efficacy. By centralizing semantic layout planning and visual self-verification, V-CAGE automates the end-to-end pipeline, enabling the highly scalable synthesis of diverse, high-quality robotic manipulation datasets.

---

## 论文详细总结（自动生成）

以下是对论文《V-CAGE: Vision-Closed-Loop Agentic Generation Engine for Robotic Manipulation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩展视觉-语言-动作（VLA）模型需要海量、语义连贯且物理可行的专家轨迹数据。
*   **研究背景**：
    *   **数据瓶颈**：现实世界数据采集昂贵且难以覆盖长尾场景；现有仿真合成方法多为“盲目”脚本，缺乏智能体推理能力。
    *   **现有缺陷**：随机放置物体常导致几何冲突（如物体重叠）或运动学不可达；“生成即执行”的开环模式易产生“隐性失败”（如抓取落空但脚本未报错），导致长程任务错误累积。

### 2. 方法论
V-CAGE 将数据合成系统视为一个具身智能体，核心包含以下四个模块：
*   **修复引导的场景构建 (IGSC)**：
    1.  **资产选择**：LLM 根据指令从库中选择相关物体并进行初步无碰撞放置。
    2.  **布局规划与修复**：LLM 生成语义布局描述，利用图像修复模型（Nano Banana）生成目标顶视图，将抽象语义转化为空间视觉参考。
    3.  **坐标恢复**：利用 Grounding DINO 和 DINOv2 进行特征匹配，将修复图像中的像素坐标映射回物理世界坐标。
    4.  **碰撞优化**：通过 L-BFGS-B 算法最小化代价函数，微调物体位置以消除穿模并确保容器包含关系。
*   **基于模板的子任务搜索**：利用预定义的代码框架，结合物体的功能点（Grasp points）元数据，由 LLM 填充执行逻辑，将复杂任务分解为可验证的原子动作。
*   **基于 VLM 的闭环验证**：引入 Gemini 3 作为“视觉评论员”，对每个子任务的执行结果进行视觉状态检查。若验证失败则立即中止并重新采样，从而切断错误传播链。
*   **动作感知感知压缩**：基于机器人交互强度（夹爪状态、运动梯度）提取关键帧，并利用感知指标（ColorVideoVDP）动态调整 HEVC 压缩率，在保持视觉无损的前提下减少 90% 以上的存储体积。

### 3. 实验设计
*   **实验场景**：涵盖厨房、办公室、零售和家庭四类环境。
*   **合成任务**：设计了四个复杂的长程任务：自助结账 (AutoCheckout)、面包打包 (PackBreads)、文具收纳 (PackStationery)、柜子分类 (SortToCabinet)。
*   **Benchmark 与对比**：
    *   **模型**：以 $\pi_{0.5}$ VLA 模型为基础进行微调。
    *   **对比维度**：预训练模型（零样本）vs. 原始数据微调 vs. 压缩数据微调。
    *   **Sim-to-Real**：在 ALOHA-AgileX 硬件平台上进行实机验证（抓取方块任务），对比“仅真机数据”与“真机+仿真数据”的效果。
    *   **消融实验**：对比 IGSC 与随机放置的成功率；对比有无 VLM 验证的数据纯度。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **模型调用**：提到了使用 Gemini 3 作为视觉验证器，以及 DINOv2 等基础模型，这些通常需要较高的推理算力或 API 支持。

### 5. 实验数量与充分性
*   **实验规模**：每个任务生成 100 条专家轨迹；策略评估在每个任务上进行 100 次测试；实机测试进行了 20 次试验。
*   **充分性**：
    *   **消融实验**：针对 IGSC 在不同场景（浴室、厨房等）下的有效性做了详细对比，证明了其在受限空间（如柜子）内的显著优势。
    *   **客观性**：通过“数据纯度”指标量化了 VLM 验证的必要性，证明其能将数据合格率提升至 100%。
    *   **局限性**：虽然实验涵盖了多个场景，但任务类型仍集中在拾取与放置类，对形变物体或流体的覆盖不足。

### 6. 主要结论与发现
*   **数据质量**：V-CAGE 合成的数据能显著提升 VLA 模型在长程任务上的表现（从 0% 提升至最高 100%）。
*   **Sim2Real 增益**：在真机数据极度匮乏（仅 10 条）的情况下，加入 250 条 V-CAGE 仿真轨迹使成功率从 20% 提升至 55%。
*   **压缩效率**：感知驱动的压缩算法在减小 93% 体积的同时，对下游模型训练效果几乎无负面影响。
*   **闭环必要性**：开环生成的轨迹中存在大量“隐性失败”（合格率仅 45%-81%），VLM 验证是确保大规模数据可用性的关键。

### 7. 优点
*   **语义与物理结合**：通过图像修复技术巧妙地将高层语义布局转化为物理可行的坐标，解决了传统方法中物体乱放的问题。
*   **端到端自动化**：实现了从场景构建、任务分解到自动验证、数据压缩的全流程闭环。
*   **存储友好**：针对机器人轨迹特性的压缩算法极大地缓解了大规模 VLA 训练的存储压力。

### 8. 不足与局限
*   **计算成本**：基于拒绝采样（Rejection Sampling）的验证机制虽然保证了质量，但在任务极其复杂时，生成效率会因频繁失败而降低。
*   **任务多样性**：目前主要针对刚性物体的操作，尚未扩展到动态环境、柔性物体或需要精细力控的任务。
*   **VLM 依赖**：系统高度依赖高性能 VLM（如 Gemini 3）的判断准确性，若 VLM 产生幻觉，可能引入错误标注。

（完）
