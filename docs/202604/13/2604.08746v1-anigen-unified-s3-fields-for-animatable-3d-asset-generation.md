---
title: "AniGen: Unified $S^3$ Fields for Animatable 3D Asset Generation"
title_zh: AniGen：用于可动画 3D 资产生成的统一 $S^3$ 场
authors: "Yi-Hua Huang, Zi-Xin Zou, Yuting He, Chirui Chang, Cheng-Feng Pu, Ziyi Yang, Yuan-Chen Guo, Yan-Pei Cao, Xiaojuan Qi"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08746v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 基于单张图像生成可动画化的3D资产
tldr: AniGen是一个从单张图像生成可动画3D资产的统一框架。针对现有模型生成静态形状且后期绑定不稳定的问题，该研究提出了S3场（形状、骨架、皮肤），在共享空间域内统一表示资产。通过置信度衰减骨架场和双皮肤特征场，AniGen能处理复杂的骨架结构并支持任意数量的关节。采用两阶段流匹配管线，实现了在多种类别（动物、人形、机械）上生成高质量、拓扑一致且可直接驱动的3D模型。
selection_source: fresh_fetch
motivation: 现有的3D生成模型多为静态，且后期自动绑定容易导致骨架与几何结构不一致。
method: 提出统一的S3场表示，并结合置信度衰减骨架场和双皮肤特征场，通过两阶段流匹配管线生成资产。
result: 实验表明AniGen在绑定有效性和动画质量上显著优于现有基准，且具备跨类别的泛化能力。
conclusion: AniGen通过统一的场表示和创新的特征解耦，为从单图生成高质量、可直接使用的可动画3D资产提供了有效方案。
---

## 摘要
可动画 3D 资产被定义为配备有关节骨架和蒙皮权重的几何体，是交互式图形学、具身智能体和动画制作的基础。虽然近期的 3D 生成模型可以从图像中合成视觉上合理的形状，但其结果通常是静态的。通过事后自动绑定获取可用的绑定（rigs）是脆弱的，且往往会产生与生成的几何体在拓扑上不一致的骨架。我们提出了 AniGen，这是一个统一的框架，能够根据单张图像直接生成可供动画使用的 3D 资产。我们的核心见解是将形状、骨架和蒙皮表示为在共享空间域上定义的相互一致的 $S^3$ 场（Shape, Skeleton, Skin）。为了实现这些场的鲁棒学习，我们引入了两项技术创新：(i) 一种置信度衰减骨架场，它显式地处理了 Voronoi 边界处骨骼预测的几何歧义；(ii) 一种双重蒙皮特征场，它将蒙皮权重与特定的关节数量解耦，允许固定架构的网络预测任意复杂度的绑定。AniGen 基于两阶段流匹配（flow-matching）流水线，首先合成稀疏的结构支架，然后在结构化潜空间中生成稠密的几何和关节。大量实验表明，AniGen 在绑定有效性和动画质量方面显著优于最先进的顺序基线模型，并能有效地泛化到包括动物、类人生物和机械在内的各种类别的野外图像。

## Abstract
Animatable 3D assets, defined as geometry equipped with an articulated skeleton and skinning weights, are fundamental to interactive graphics, embodied agents, and animation production. While recent 3D generative models can synthesize visually plausible shapes from images, the results are typically static. Obtaining usable rigs via post-hoc auto-rigging is brittle and often produces skeletons that are topologically inconsistent with the generated geometry. We present AniGen, a unified framework that directly generates animate-ready 3D assets conditioned on a single image. Our key insight is to represent shape, skeleton, and skinning as mutually consistent $S^3$ Fields (Shape, Skeleton, Skin) defined over a shared spatial domain. To enable the robust learning of these fields, we introduce two technical innovations: (i) a confidence-decaying skeleton field that explicitly handles the geometric ambiguity of bone prediction at Voronoi boundaries, and (ii) a dual skin feature field that decouples skinning weights from specific joint counts, allowing a fixed-architecture network to predict rigs of arbitrary complexity. Built upon a two-stage flow-matching pipeline, AniGen first synthesizes a sparse structural scaffold and then generates dense geometry and articulation in a structured latent space. Extensive experiments demonstrate that AniGen substantially outperforms state-of-the-art sequential baselines in rig validity and animation quality, generalizing effectively to in-the-wild images across diverse categories including animals, humanoids, and machinery. Homepage: https://yihua7.github.io/AniGen-web/

---

## 论文详细总结（自动生成）

这是一份关于论文《AniGen: Unified $S^3$ Fields for Animatable 3D Asset Generation》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 生成模型（如 TRELLIS、Clay 等）虽然能生成高保真的静态几何形状，但缺乏**可动画性（Animatability）**。
*   **研究背景**：在游戏、VR 和具身智能领域，3D 资产需要配备骨架（Skeleton）和蒙皮权重（Skinning Weights）才能运动。
*   **动机**：传统的“先生成形状、后自动绑定（Post-hoc Auto-rigging）”的顺序流水线非常脆弱。生成的几何体往往存在拓扑噪声或肢体粘连，导致自动绑定工具生成的骨架在解剖学上不正确或蒙皮撕裂。AniGen 的核心动机是将形状、骨架和蒙皮视为**相互纠缠的整体**，通过统一的生成框架实现“生成即绑定”。

### 2. 方法论：核心思想与关键技术
AniGen 提出了 **$S^3$ Fields（Shape, Skeleton, Skin）** 概念，将三种模态统一在共享的空间域中。

*   **核心思想**：利用连续向量场表示离散的骨架和变长的蒙皮权重，使其能够与几何形状在同一个流匹配（Flow-matching）框架下协同进化。
*   **关键技术细节**：
    1.  **置信度衰减骨架场（Confidence-decaying Skeleton Field）**：
        *   骨架不再表示为图，而是表示为指向最近关节及其父节点的向量场。
        *   针对 Voronoi 边界（两个关节中间区域）的预测歧义，引入了显式的置信度标量 $c(x)$。公式定义为：$c_{gt} = 1 - \frac{d_{1st}}{d_{2nd}}$（最近距离与次近距离之比）。
        *   在训练时，利用该置信度对回归损失进行加权，使模型专注于骨骼核心区域。
    2.  **双皮肤特征场（Dual Skin Field）与 SkinAE**：
        *   为了处理不同物体关节数量不一的问题，预训练了一个 **SkinAE（蒙皮自动编码器）**。
        *   它将变长的蒙皮矩阵分解为固定维度的“关节特征”和“顶点特征”，通过互注意力机制（Cross-attention）重建蒙皮权重。
    3.  **两阶段生成管线**：
        *   **阶段 I（稀疏结构流）**：生成形状和骨架的体素支架（Voxel Scaffolds）。
        *   **阶段 II（结构化潜变量流）**：在支架基础上“填充”稠密的几何、骨架向量和蒙皮特征。
    4.  **算法流程**：推理时，通过“场到骨架聚类算法”（Field-to-Skeleton Clustering），将预测的向量场转换为离散的层级骨架。

### 3. 实验设计
*   **数据集**：主要使用 **ArticulationXL**（包含约 33K 个带绑定的 3D 模型，源自 Objaverse-XL）。
*   **Benchmark 与对比方法**：
    *   **对比基准**：采用“TRELLIS（最强 3D 生成器）+ 自动绑定工具”的组合。
    *   **对比工具**：包括 UniRig、Anymate、Puppeteer 和 RigAnything。
*   **评估指标**：
    *   **骨架精度**：关节到关节距离、关节到骨骼距离。
    *   **拓扑一致性**：引入了 **Gromov-Wasserstein (GW) 距离**，用于衡量预测骨架与真值骨架在图结构上的相似性。
    *   **蒙皮精度**：L1、L2 距离及 KL 散度。
    *   **几何质量**：Chamfer 距离、F-Score、PSNR 等。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号（如 A100 或 H100）及总训练时长。
*   **效率优化**：提到了使用 **CUDA 加速的 BVH（层次包围盒）** 技术来处理蒙皮权重的转移监督，将查询时间从 48.6ms 降低到 2.6ms（提升 18.6 倍），这表明该模型在训练时对显存和计算效率有较高要求。
*   **推理速度**：端到端生成一个带绑定的资产约需 **19 秒**，与最快的顺序流水线相当，但远快于需要复杂后处理的方法（如 UniRig 需 146 秒）。

### 5. 实验数量与充分性
*   **实验规模**：在 1K 个测试形状上进行了定量评估，涵盖了动物、人形、机械等多种类别。
*   **消融实验**：针对“置信度设计”、“SkinAE 预训练”以及“不同关节密度控制”进行了详细的消融研究。
*   **充分性评价**：实验设计较为充分。特别是引入 GW 距离来评估骨架拓扑，弥补了传统欧氏距离无法反映结构错误的缺陷。此外，还展示了大量“野外（In-the-wild）”图像生成结果，证明了模型的泛化能力。

### 6. 主要结论与发现
*   **协同生成优于顺序处理**：AniGen 在骨架有效性和动画质量上显著超过了所有“生成+绑定”的基线模型。
*   **拓扑一致性强**：通过 $S^3$ 场，生成的骨架能完美契合几何边界，避免了肢体粘连导致的绑定失败。
*   **几何保真度无损**：实验证明，加入骨架和蒙皮的联合训练并不会降低 3D 形状的生成质量（与纯几何生成器 TRELLIS 相比，差距极小）。
*   **可控性**：通过调节“关节密度”参数，用户可以控制生成骨架的复杂程度（如从简单的机器人关节到复杂的类人关节）。

### 7. 优点（亮点）
*   **统一性**：首个将形状、骨架、蒙皮整合进统一连续场表示的生成模型。
*   **鲁棒性**：置信度衰减机制有效解决了骨架预测中的边界歧义问题。
*   **灵活性**：SkinAE 的设计巧妙地解决了神经网络难以处理变长关节输出的难题，使模型具有类别无关性（Category-agnostic）。
*   **实用性**：生成的资产可直接导入 Blender 或游戏引擎使用，无需二次修正。

### 8. 不足与局限
*   **输入模态限制**：目前主要聚焦于单图生成，未充分利用视频输入（视频能提供更明确的运动动力学线索）。
*   **精密机械对齐**：对于需要极高几何精度的机械结构（如笔记本电脑的转轴），生成的关节可能存在微小偏移，导致闭合时有缝隙。
*   **数据依赖**：生成的骨架风格受限于训练集（ArticulationXL），目前更多倾向于中轴线骨架，而非完全符合生物解剖学的专业生产级骨架。

（完）
