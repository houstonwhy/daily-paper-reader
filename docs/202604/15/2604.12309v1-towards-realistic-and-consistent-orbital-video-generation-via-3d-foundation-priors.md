---
title: Towards Realistic and Consistent Orbital Video Generation via 3D Foundation Priors
title_zh: 迈向通过 3D 基础先验实现逼真且一致的环绕视频生成
authors: "Rong Wang, Ruyi Zha, Ziang Cheng, Jiayu Yang, Pulak Purkait, Hongdong Li"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12309v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于生成视频和场景一致性的3D基础先验
tldr: 本研究针对单图生成环绕视频中长程外推（如背面合成）导致的一致性差和结构扭曲问题，提出了一种利用3D基础模型先验的方法。通过引入全局结构引导和局部几何细节特征，并利用多尺度3D适配器将这些先验注入视频生成模型，实现了高几何一致性和真实感的视频合成。该方法无需显式网格提取，在保持预训练视频模型能力的同时，显著提升了复杂轨迹下的生成质量。
selection_source: fresh_fetch
motivation: 现有的视频生成方法主要依赖像素级注意力，在处理大角度旋转或背面合成时缺乏足够的几何约束，导致物体结构不连贯。
method: 提出利用3D基础模型的全局和局部潜在特征作为辅助约束，并通过多尺度3D适配器将这些几何先验注入视频生成模型。
result: 实验证明该方法在视觉质量、形状真实感和多视图一致性方面优于现有技术，并能稳健地泛化至复杂轨迹和野外图像。
conclusion: 结合3D基础模型的形状先验能有效解决视频生成中的几何一致性难题，为高质量环绕视频合成提供了高效且通用的方案。
---

## 摘要
我们提出了一种从单张物体图像生成几何逼真且一致的环绕视频的新方法。现有的视频生成工作主要依赖像素级注意力机制来增强帧间的视角一致性。然而，这种机制对于长程外推（例如背面视图合成）无法施加足够的约束，因为在这些情况下，与输入图像的像素对应关系非常有限。因此，这些工作往往无法生成具有合理且连贯结构的结果。为了解决这一问题，我们建议利用 3D 基础生成模型中丰富的形状先验作为辅助约束，其动机在于该模型能够建模从大规模 3D 资产库中学习到的逼真物体形状分布。具体而言，我们使用 3D 基础模型编码的两种尺度的潜特征来引导视频生成：(i) 作为整体结构引导的去噪全局潜向量，以及 (ii) 从体积特征投影得到的一组潜图像，用以提供视角相关的细粒度几何细节。与常用的 2.5D 表示（如深度图或法线图）相比，这些紧凑的特征可以建模完整的物体形状，并通过避免显式的网格提取来提高推理效率。为了实现有效的形状条件化，我们引入了一个多尺度 3D 适配器，通过交叉注意力机制将特征 token 注入基础视频模型，这保留了模型在通用视频预训练中的能力，并实现了一个简单且与模型无关的微调过程。在多个基准测试上的广泛实验表明，与现有先进方法相比，我们的方法在视觉质量、形状逼真度和多视角一致性方面均表现优异，并能稳健地泛化到复杂的摄像机轨迹和野外图像。

## Abstract
We present a novel method for generating geometrically realistic and consistent orbital videos from a single image of an object. Existing video generation works mostly rely on pixel-wise attention to enforce view consistency across frames. However, such mechanism does not impose sufficient constraints for long-range extrapolation, e.g. rear-view synthesis, in which pixel correspondences to the input image are limited. Consequently, these works often fail to produce results with a plausible and coherent structure. To tackle this issue, we propose to leverage rich shape priors from a 3D foundational generative model as an auxiliary constraint, motivated by its capability of modeling realistic object shape distributions learned from large 3D asset corpora. Specifically, we prompt the video generation with two scales of latent features encoded by the 3D foundation model: (i) a denoised global latent vector as an overall structural guidance, and (ii) a set of latent images projected from volumetric features to provide view-dependent and fine-grained geometry details. In contrast to commonly used 2.5D representations such as depth or normal maps, these compact features can model complete object shapes, and help to improve inference efficiency by avoiding explicit mesh extraction. To achieve effective shape conditioning, we introduce a multi-scale 3D adapter to inject feature tokens to the base video model via cross-attention, which retains its capabilities from general video pretraining and enables a simple and model-agonistic fine-tuning process. Extensive experiments on multiple benchmarks show that our method achieves superior visual quality, shape realism and multi-view consistency compared to state-of-the-art methods, and robustly generalizes to complex camera trajectories and in-the-wild images.

---

## 论文详细总结（自动生成）

这篇论文《Towards Realistic and Consistent Orbital Video Generation via 3D Foundation Priors》提出了一种结合 3D 基础模型先验知识来增强单图生成环绕视频（Orbital Video）质量的方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：从单张图像生成物体的环绕视频在电商、XR 和游戏设计中具有重要意义。
*   **核心挑战**：现有的视频生成模型（如 SVD）主要依赖像素级注意力机制来维持视图一致性。但在处理大幅度视角变化（如从正面转到背面）时，由于缺乏像素对应关系和 3D 几何常识，模型经常生成扭曲、不连贯或不符合物理规律的物体结构。
*   **整体含义**：论文旨在通过引入 3D 基础模型（3D Foundation Models）学习到的完整形状分布，为视频生成提供强有力的几何约束，从而解决长程视角合成中的结构崩溃问题。

### 2. 方法论
核心思想是将 3D 基础模型的潜在特征作为“形状先验”，通过一个适配器注入到视频扩散模型中。
*   **3D 基础先验提取**：
    *   使用 **Hunyuan3D** 作为基础模型。
    *   **全局引导**：提取一个去噪后的全局潜在向量（Global Latent Vector），代表物体的整体结构。
    *   **局部细节**：从体积特征（Volumetric Features）中投影出一组多视角的潜在图像（Latent Images），提供视角相关的细粒度几何信息。
*   **多尺度 3D 适配器（Multi-Scale 3D Adapter）**：
    *   设计了一个即插即用的模块，通过**交叉注意力机制（Cross-Attention）**将上述全局和局部特征注入到基础视频模型（SVD）的 Transformer 块中。
    *   这种设计允许模型保留预训练视频模型的生成能力，同时接受 3D 几何的软约束。
*   **算法流程**：输入图像 $\rightarrow$ 3D 基础模型提取特征 $\rightarrow$ 3D 适配器处理特征 $\rightarrow$ 注入视频扩散模型 $\rightarrow$ 根据相机轨迹生成 N 帧一致的视频。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Objaverse-XL 的子集（约 16 万个 3D 资产），渲染成合成视频。
    *   **测试集**：Objaverse-XL（150 个未见物体）和 Google Scanned Objects (GSO, 100 个物体)。
*   **Benchmark（评估指标）**：
    *   **视觉质量**：PSNR, SSIM, LPIPS。
    *   **形状真实感**：CLIP-Score（评估语义一致性）。
    *   **几何一致性**：MEt3R（衡量相邻帧间的像素级一致性）。
*   **对比方法**：
    *   **视频生成类**：SV3D, Hi3D。
    *   **新视角合成（NVS）类**：Wonder3D, Era3D。
    *   **3D 重建渲染类**：Hunyuan3D-2, Trellis。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练参数**：
    *   迭代次数：80K iterations。
    *   有效 Batch Size：16。
    *   学习率：$1 \times 10^{-5}$。
*   **推理效率**：在单张 H200 上，提取 3D 先验约需 2.25 秒（1.8s 全局 + 0.45s 局部），每步采样增加约 0.28 秒，整体开销较小，无需耗时的网格提取过程。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准数据集上进行了定量对比，并提供了大量的定性视觉对比图。
*   **消融实验**：针对“有无 3D 先验”、“全局 vs 局部特征”、“适配器设计变体（特征拼接 vs 帧堆叠 vs 交叉注意力）”进行了详细测试。
*   **充分性评价**：实验设计较为全面，不仅对比了同类视频生成模型，还跨领域对比了 3D 重建模型。通过“野外图像（In-the-wild）”和“动态轨迹”实验验证了模型的泛化能力，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **3D 先验的有效性**：引入 3D 基础模型的形状先验能显著提升背面视图的结构合理性，减少物体在旋转过程中的“漂移”和变形。
*   **多尺度策略**：全局特征负责定调（结构），局部特征负责补全（细节），两者结合效果最优。
*   **适配器优势**：交叉注意力适配器比简单的特征拼接更能保护预训练模型的分布，训练更稳定，效果更好。
*   **性能领先**：在所有核心指标（尤其是几何一致性 MEt3R）上均优于 SV3D 和 Hi3D 等现有 SOTA 方法。

### 7. 优点
*   **创新性地结合先验**：不同于以往只用 2.5D（深度）或 2D 引导的方法，直接利用 3D 原生潜空间特征。
*   **推理高效**：通过潜在特征注入避免了显式的 3D 重建和渲染流程，保持了扩散模型的高效推理。
*   **鲁棒性强**：对复杂物体和大幅度相机运动表现出极佳的稳定性。

### 8. 不足与局限
*   **分辨率限制**：生成的视频分辨率受限于基础模型（SVD），对于极细微的纹理细节可能表现不足。
*   **纹理先验缺失**：目前主要利用了 3D 模型的“形状”先验，而 3D 基础模型本身的纹理质量尚不够稳健，因此未充分利用其纹理先验。
*   **失败案例**：在处理极小区域或极其复杂的遮挡场景时，若 3D 基础模型生成的先验本身有误，视频生成也会受到负面影响。

（完）
