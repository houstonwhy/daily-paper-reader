---
title: "Lyra 2.0: Explorable Generative 3D Worlds"
title_zh: Lyra 2.0：可探索的生成式 3D 世界
authors: "Tianchang Shen, Sherwin Bahmani, Kai He, Sangeetha Grama Srinivasan, Tianshi Cao, Jiawei Ren, Ruilong Li, Zian Wang, Nicholas Sharp, Zan Gojcic, Sanja Fidler, Jiahui Huang, Huan Ling, Jun Gao, Xuanchi Ren"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13036v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 可探索的生成式3D世界与场景创建
tldr: Lyra 2.0 针对生成式 3D 场景重建中的空间遗忘和时间漂移问题，提出了一种生成持久、可探索 3D 世界的新框架。该方法通过维护 3D 几何信息进行跨帧检索以解决空间遗忘，并利用自增强历史训练来纠正时间漂移。这使得模型能够生成长程且 3D 一致的视频轨迹，从而通过前馈重建技术恢复出高质量、大规模的 3D 场景。
selection_source: fresh_fetch
motivation: 现有的视频生成模型在长路径探索中容易因空间遗忘导致场景不一致，并因误差累积产生时间漂移。
method: 利用 3D 几何引导信息检索以维持空间一致性，并通过自增强历史训练提升模型纠正自身漂移的能力。
result: 实现了更长且具备 3D 一致性的视频轨迹生成，显著提升了大规模复杂环境的 3D 重建质量。
conclusion: Lyra 2.0 有效结合了视频生成与 3D 重建，为创建可交互、持久的大规模生成式 3D 世界提供了可靠方案。
---

## 摘要
视频生成的最新进展为 3D 场景创建开启了一种新范式：生成受相机控制的、模拟场景漫游的视频，然后通过前馈重建技术将其提升至 3D 空间。这种生成式重建方法结合了视频模型的视觉保真度和创作能力，并能生成可用于实时渲染和模拟的 3D 输出。扩展到大规模复杂环境需要跨越长相机轨迹、大视角变化和位置重访的 3D 一致性视频生成，而目前的视频模型在这些场景下性能会迅速下降。现有的长时程生成方法从根本上受到两种退化形式的限制：空间遗忘（spatial forgetting）和时间漂移（temporal drifting）。随着探索的进行，先前观察到的区域会超出模型的时间上下文，迫使模型在重访时产生结构幻觉。同时，自回归生成会随时间累积微小的合成误差，逐渐扭曲场景的外观和几何结构。我们提出了 Lyra 2.0，这是一个用于大规模生成持久且可探索的 3D 世界的框架。为了解决空间遗忘问题，我们维护逐帧 3D 几何信息，并仅将其用于信息路由——检索相关的历史帧并建立与目标视角的密集对应关系——同时依靠生成先验进行外观合成。为了解决时间漂移问题，我们使用自增强历史记录进行训练，使模型接触到其自身的退化输出，从而教会它纠正漂移而非传播漂移。综上所述，这些技术实现了显著更长且具有 3D 一致性的视频轨迹，我们利用这些轨迹微调前馈重建模型，从而可靠地恢复高质量的 3D 场景。

## Abstract
Recent advances in video generation enable a new paradigm for 3D scene creation: generating camera-controlled videos that simulate scene walkthroughs, then lifting them to 3D via feed-forward reconstruction techniques. This generative reconstruction approach combines the visual fidelity and creative capacity of video models with 3D outputs ready for real-time rendering and simulation. Scaling to large, complex environments requires 3D-consistent video generation over long camera trajectories with large viewpoint changes and location revisits, a setting where current video models degrade quickly. Existing methods for long-horizon generation are fundamentally limited by two forms of degradation: spatial forgetting and temporal drifting. As exploration proceeds, previously observed regions fall outside the model's temporal context, forcing the model to hallucinate structures when revisited. Meanwhile, autoregressive generation accumulates small synthesis errors over time, gradually distorting scene appearance and geometry. We present Lyra 2.0, a framework for generating persistent, explorable 3D worlds at scale. To address spatial forgetting, we maintain per-frame 3D geometry and use it solely for information routing -- retrieving relevant past frames and establishing dense correspondences with the target viewpoints -- while relying on the generative prior for appearance synthesis. To address temporal drifting, we train with self-augmented histories that expose the model to its own degraded outputs, teaching it to correct drift rather than propagate it. Together, these enable substantially longer and 3D-consistent video trajectories, which we leverage to fine-tune feed-forward reconstruction models that reliably recover high-quality 3D scenes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Lyra 2.0: Explorable Generative 3D Worlds》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何利用视频扩散模型生成大规模、长时程且具备 3D 一致性的可探索世界。
*   **研究背景**：当前的“生成式重建”范式（先生成相机控制的视频，再重建为 3D）在处理大规模复杂环境（如跨房间导航或长街探索）时面临两大瓶颈：
    *   **空间遗忘（Spatial Forgetting）**：当相机移动超出有限的上下文窗口后，模型会忘记已生成的区域，导致重访（Revisit）时出现结构幻觉。
    *   **时间漂移（Temporal Drifting）**：自回归生成过程中微小的合成误差会随时间累积，导致颜色偏移、模糊和几何扭曲。

### 2. 方法论
Lyra 2.0 提出了一个“检索-生成-更新”的循环框架，核心技术包括：
*   **抗遗忘机制（空间存储与路由）**：
    *   **3D 缓存（3D Cache）**：维护逐帧的深度图和下采样点云，但不进行全局融合，以避免误差累积。
    *   **几何感知检索**：根据目标视角的可见性分数，从缓存中检索最相关的历史帧作为“空间插槽（Spatial Slots）”。
    *   **规范坐标变形（Canonical Coordinate Warping）**：通过变形后的坐标图建立历史帧与目标视角的密集对应关系，引导模型进行外观合成，而非直接输入带伪影的变形图像。
*   **抗漂移机制（自增强训练）**：
    *   **FramePack 压缩**：对历史帧进行变长补丁化处理，在固定 Token 预算下延长时程。
    *   **自增强训练（Self-Augmentation Training）**：在训练阶段随机对历史上下文添加噪声并进行单步去噪，使模型在训练时就接触到自身生成的退化输出，从而学会纠正而非传播误差。
*   **3D 重建与蒸馏**：
    *   **前馈 3DGS 重建**：微调 Depth Anything v3，使其对生成视频中的微小不一致性更具鲁棒性。
    *   **DMD 蒸馏**：将模型蒸馏至 4 步推理，实现交互式生成速度（提升约 13 倍）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DL3DV（包含 10,000 个真实场景的长视频剪辑）。
    *   **测试集**：DL3DV-Evaluation（域内测试）和 Tanks and Temples（跨域泛化测试）。
*   **对比方法**：包括 GEN3C、Yume1.5、CaM、VMem、SPMem 和 HY-WorldPlay 等具备存储机制或相机控制能力的先进模型。
*   **评估指标**：
    *   传统指标：SSIM, LPIPS, FID。
    *   长视频专项指标（WorldScore）：主观质量、风格一致性、相机控制力。
    *   3D 一致性指标：重投影误差、LPIPS-P（渲染图与生成图的差异）。

### 4. 资源与算力
*   **硬件**：使用了 **64 台 NVIDIA GB200 GPU** 进行并行训练。
*   **训练参数**：训练迭代次数为 7,000 次，Batch Size 为 64。
*   **推理效率**：全模型（35 步）在单台 GB200 上生成 80 帧需 194 秒；蒸馏后的 DMD 模型（4 步）仅需 **15 秒**。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个大型数据集上进行了详尽的定量评估，并提供了大量长达 800 帧以上的定性对比结果。
*   **消融实验**：设计了 5 组消融实验，分别验证了全局点云融合、显式对应融合、FramePack 以及自增强训练的必要性。
*   **充分性评价**：实验覆盖了从视频生成质量到 3D 重建精度的全链路，并包含了“野外（In-the-wild）”图像测试和 Isaac Sim 机器人仿真应用，实验设计客观、公平且具有极强的说服力。

### 6. 主要结论与发现
*   **解耦几何与合成是关键**：将 3D 几何仅用于信息路由（检索和对应），而将外观合成留给扩散先验，能有效解决空间遗忘。
*   **自增强训练能显著抑制漂移**：通过暴露模型于自身的误差分布，模型展现出了强大的自我纠错能力。
*   **生成式 3D 场景已具备实用性**：生成的 3D 资产可以直接导入物理引擎（如 Isaac Sim）进行具身智能仿真。

### 7. 优点
*   **长程持久性**：在处理大规模场景重访时表现优异，解决了视频生成中常见的“回头看就变样”的问题。
*   **高质量重建**：通过微调重建模型，成功弥合了生成视频与真实重建之间的领域差距。
*   **交互性与效率**：DMD 蒸馏和交互式 GUI 使得用户可以实时定义轨迹并扩展世界。

### 8. 不足与局限
*   **静态环境限制**：目前主要针对静态场景，无法显式处理动态物体（如行人、车辆）。
*   **光度不一致性**：受限于训练数据（DL3DV），生成的视频可能继承真实拍摄中的曝光变化，导致 3D 重建出现伪影。
*   **数据偏差**：模型表现仍受限于训练数据的分布，对于极端罕见的场景可能存在泛化风险。

（完）
