---
title: "UniRecGen: Unifying Multi-View 3D Reconstruction and Generation"
title_zh: UniRecGen：统一多视图三维重建与生成
authors: "Zhisheng Huang, Jiahao Chen, Cheng Lin, Chenyu Hu, Hanzhuo Huang, Zhengming Yu, Mengfei Li, Yuheng Liu, Zekai Gu, Zibo Zhao, Yuan Liu, Xin Li, Wenping Wang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01479v2"
tags: ["query:3dgen"]
score: 9.0
evidence: 统一多视图3D重建与基于扩散的生成
tldr: 针对稀疏视图3D建模中重建保真度与生成合理性之间的矛盾，本文提出UniRecGen统一框架。该框架将前馈重建的高效对齐能力与扩散生成的丰富几何先验相结合，通过共享规范空间对齐坐标与表示，并采用解耦协作学习策略。重建模块提供几何锚点，扩散生成器进行结构补全与细化，实现了高保真、一致且完整的3D模型生成。
selection_source: fresh_fetch
motivation: 旨在解决稀疏视图3D建模中，传统重建方法缺乏全局先验而扩散生成方法难以保持多视角一致性的核心矛盾。
method: 提出一种在共享规范空间内运行的统一框架，通过解耦协作学习将重建模块提供的几何锚点与扩散生成器的潜空间增强条件相结合。
result: 实验结果表明，UniRecGen在稀疏观测下生成的3D模型在保真度、完整性和鲁棒性方面均显著优于现有技术。
conclusion: UniRecGen成功实现了重建与生成范式的深度融合，为构建高质量、一致性的3D模型提供了一种有效的统一解决方案。
---

## 摘要
稀疏视图三维建模体现了重建保真度与生成合理性之间的基本矛盾。虽然前馈重建在效率和输入对齐方面表现出色，但往往缺乏结构完整性所需的全局先验。相反，基于扩散的生成提供了丰富的几何细节，但在多视图一致性方面面临挑战。我们提出了 UniRecGen，这是一个将这两种范式整合到单一协作系统中的统一框架。为了克服坐标空间、三维表示和训练目标中的固有冲突，我们将两个模型对齐在一个共享的标准空间内。我们采用了解耦协作学习，在保持训练稳定的同时，实现了推理过程中的无缝协作。具体而言，重建模块经过调整以提供标准几何锚点，而扩散生成器则利用潜空间增强的条件约束来细化并补全几何结构。实验结果表明，UniRecGen 实现了卓越的保真度和鲁棒性，在从稀疏观测中创建完整且一致的三维模型方面优于现有方法。代码可在 https://github.com/zsh523/UniRecGen 获取。

## Abstract
Sparse-view 3D modeling represents a fundamental tension between reconstruction fidelity and generative plausibility. While feed-forward reconstruction excels in efficiency and input alignment, it often lacks the global priors needed for structural completeness. Conversely, diffusion-based generation provides rich geometric details but struggles with multi-view consistency. We present UniRecGen, a unified framework that integrates these two paradigms into a single cooperative system. To overcome inherent conflicts in coordinate spaces, 3D representations, and training objectives, we align both models within a shared canonical space. We employ disentangled cooperative learning, which maintains stable training while enabling seamless collaboration during inference. Specifically, the reconstruction module is adapted to provide canonical geometric anchors, while the diffusion generator leverages latent-augmented conditioning to refine and complete the geometric structure. Experimental results demonstrate that UniRecGen achieves superior fidelity and robustness, outperforming existing methods in creating complete and consistent 3D models from sparse observations. Code is available at https://github.com/zsh523/UniRecGen.

---

## 论文详细总结（自动生成）

以下是对论文《UniRecGen: Unifying Multi-View 3D Reconstruction and Generation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：稀疏视图（Sparse-view）三维建模长期存在于“重建保真度”与“生成合理性”之间的博弈。
    *   **确定性重建**（如前馈回归模型）：擅长对齐输入图像，但由于缺乏全局先验，在遮挡区域往往几何不完整。
    *   **随机性生成**（如扩散模型）：能生成丰富的细节和完整的结构，但难以严格遵循输入图像的微观特征，容易产生“幻觉”。
*   **研究动机**：现有的统一尝试（如将2D扩散模型与3D重建结合）仍受限于2D到3D的表示鸿沟。本文旨在通过**原生3D统一**，将重建模块作为“几何锚点”，引导生成模块进行补全，从而兼顾保真度与完整性。

### 2. 方法论
UniRecGen 采用两阶段模块化流水线，核心思想是在**共享的标准对象空间**内实现重建与生成的协同。
*   **生成兼容的前馈重建（Stage I）**：
    *   基于 VGGT 架构，提出**分支改用（Branch Repurposing）**策略：仅将点图（Pointmap）预测头重塑为在标准坐标系（Canonical Space）下输出，而保持相机位姿和深度预测在参考坐标系内，以保留预训练的几何先验。
    *   通过**相似性对齐（Similarity Alignment）**：利用加权 Procrustes 分析，将深度图导出的点云与标准点图对齐，获得高精度的标准几何锚点。
*   **重建引导的可控生成（Stage II）**：
    *   基于 Hunyuan3D-Omni 扩散模型，引入**潜空间增强的多视图条件（Latent-Augmented Multi-view Conditioning）**。
    *   **核心技术**：不只是简单的点云输入，而是将重建模块的中间特征（VGGT tokens）和相机嵌入注入到 DINO 图像特征中。这种密集的语义+几何特征组合，使扩散模型能更好地理解多视图间的空间关系，实现高保真补全。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 Objaverse-XL 中筛选出的 40,000 个高质量 3D 模型。
    *   **测试集/Benchmark**：Google Scanned Objects (GSO) 和 Toys4K。
*   **对比方法**：
    *   **生成类**：TRELLIS (S/M版本)、Hunyuan3D-MV、LucidFusion。
    *   **重建/单图类**：SAM 3D、VGGT（原始版）。
    *   **最新统一类**：ReconViaGen（同期最强竞争对手）。
*   **评估指标**：Chamfer-L2、Precision、Recall、F-Score、Normal Consistency、Voxel IoU，以及针对位姿的 ATE 和 RPE。

### 4. 资源与算力
论文明确记录了训练所需的算力资源：
*   **硬件**：使用了 **4 台 NVIDIA H800 GPU**。
*   **第一阶段（重建适配）**：训练 80,000 步，耗时约 **5 天**。
*   **第二阶段（生成器训练）**：训练 80,000 步，耗时约 **8 天**。
*   **总计**：约 13 天的四卡 H800 算力投入。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准数据集（GSO, Toys4K）上进行了详尽的定量对比，并提供了大量的定性可视化结果。
*   **消融实验**：针对“标准空间对齐策略”和“多视图条件注入方式”进行了专门的消融研究，验证了“分支改用”和“潜空间增强”的有效性。
*   **客观性**：实验采用了随机采样的 4 视图稀疏输入，并加入了相机平移扰动以模拟真实环境，评估环境较为严苛且贴近实际，对比基准涵盖了最新的 SOTA 模型，具有较高的说服力。

### 6. 主要结论与发现
*   **性能领先**：UniRecGen 在所有几何指标上均显著优于现有方法，尤其在 F-Score 和 IoU 上提升明显，证明了其在处理稀疏视图时的结构完整性。
*   **协同效应**：实验证明，直接的几何引导（点云锚点）比单纯的高层特征注入（如 ReconViaGen 的做法）更能有效减少空间歧义。
*   **标准空间的重要性**：将重建模型适配到标准对象空间是实现与生成模型无缝结合的关键，能显著提升位姿估计的准确性。

### 7. 优点
*   **模块化设计**：重建和生成模块解耦，未来可以独立升级更强大的子模型。
*   **解决坐标冲突**：通过分支改用策略巧妙解决了前馈重建（相机中心）与生成模型（对象中心）之间的坐标系不匹配问题。
*   **高保真度**：利用潜空间特征增强，保留了输入图像的细微纹理和结构特征，减少了生成模型的随机幻觉。

### 8. 不足与局限
*   **计算开销**：尽管是前馈推理，但训练过程（尤其是扩散模型的微调）仍需要昂贵的 H800 算力支持。
*   **场景限制**：目前主要聚焦于孤立的 3D 物体建模，尚未扩展到复杂的场景级（Scene-level）重建。
*   **纹理缺失**：论文重点在于几何形状（Mesh）的生成，对于高质量 PBR 纹理的同步生成和对齐提及较少。
*   **对齐依赖**：如果第一阶段的重建出现严重偏差，可能会误导第二阶段的生成结果。

（完）
