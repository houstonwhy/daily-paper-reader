---
title: "NavCrafter: Exploring 3D Scenes from a Single Image"
title_zh: NavCrafter：从单张图像探索三维场景
authors: "Hongbo Duan, Peiyu Zhuang, Yi Liu, Zhengyang Zhang, Yuxin Zhang, Pengting Luo, Fangming Liu, Xueqian Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02828v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 通过扩散模型从单张图像探索3D场景
tldr: NavCrafter是一个从单张图像生成可导航3D场景的新框架。它利用视频扩散模型捕捉丰富的3D先验，并通过几何感知扩展策略逐步扩大场景覆盖范围。该框架引入了多阶段相机控制机制以实现可控的多视图合成，并结合碰撞感知轨迹规划和增强型3D高斯泼溅（3DGS）技术，在大视角偏移下实现了卓越的新视角合成效果和高保真度的3D重建。
selection_source: fresh_fetch
motivation: 旨在解决从单张图像生成具有大视角偏移和时空一致性的高质量、可控3D场景的挑战。
method: 结合视频扩散模型与几何感知扩展策略，通过多阶段相机控制、碰撞感知轨迹规划及增强型3DGS流水线实现场景构建。
result: 实验证明NavCrafter在大视角新视角合成方面达到了SOTA水平，并显著提升了3D重建的保真度。
conclusion: NavCrafter通过融合视频先验与几何约束，为单图生成大规模、可导航的3D场景提供了一种高效且一致的解决方案。
---

## 摘要
当直接获取三维数据成本高昂或不切实际时，从单张图像创建灵活的三维场景至关重要。我们提出了 NavCrafter，这是一个通过合成具有相机可控性和时空一致性的新视角视频序列，从而从单张图像探索三维场景的新型框架。NavCrafter 利用视频扩散模型捕捉丰富的 3D 先验，并采用几何感知扩展策略来逐步扩大场景覆盖范围。为了实现可控的多视角合成，我们引入了一种多阶段相机控制机制，通过双分支相机注入和注意力调制，利用多样化的轨迹对扩散模型进行条件约束。我们还提出了一种碰撞感知的相机轨迹规划器，以及一个具有深度对齐监督、结构正则化和精细化的增强型 3D 高斯泼溅（3DGS）流水线。大量实验表明，NavCrafter 在大视角偏移下实现了最先进的新视角合成效果，并显著提高了三维重建的保真度。

## Abstract
Creating flexible 3D scenes from a single image is vital when direct 3D data acquisition is costly or impractical. We introduce NavCrafter, a novel framework that explores 3D scenes from a single image by synthesizing novel-view video sequences with camera controllability and temporal-spatial consistency. NavCrafter leverages video diffusion models to capture rich 3D priors and adopts a geometry-aware expansion strategy to progressively extend scene coverage. To enable controllable multi-view synthesis, we introduce a multi-stage camera control mechanism that conditions diffusion models with diverse trajectories via dual-branch camera injection and attention modulation. We further propose a collision-aware camera trajectory planner and an enhanced 3D Gaussian Splatting (3DGS) pipeline with depth-aligned supervision, structural regularization and refinement. Extensive experiments demonstrate that NavCrafter achieves state-of-the-art novel-view synthesis under large viewpoint shifts and substantially improves 3D reconstruction fidelity.

---

## 论文详细总结（自动生成）

以下是对论文《NavCrafter: Exploring 3D Scenes from a Single Image》的结构化深度总结：

### 1. 核心问题与研究背景
*   **研究动机**：从单张图像生成可导航的 3D 场景在电影制作、VR/AR 和机器人领域具有巨大潜力。然而，现有方法在处理单图输入时面临三大挑战：
    1.  **可控性差**：难以精确指定相机运动轨迹。
    2.  **一致性弱**：在长序列或大视角变换下难以维持时空连贯性。
    3.  **几何畸变**：由于缺乏有效的 3D 先验，重建的场景往往存在严重的几何错误和遮挡推理失败。
*   **核心目标**：开发一个能够从单图出发，通过精确相机控制合成高质量视频，并最终构建高保真 3D 场景的框架。

### 2. 方法论
NavCrafter 框架由三个核心模块组成：
*   **模块 I：可控新视角合成（Controllable NVS）**
    *   **相机轨迹表示**：使用 Plücker 嵌入（Plücker Embedding）对每帧的相机射线进行编码，捕捉光线方向和相机中心信息。
    *   **多阶段控制架构**：在视频扩散模型（基于 Wan2.1）中引入双分支注入。首先通过 3D 卷积适配器将轨迹特征直接加到 DiT 块的视频 Token 上；其次利用 LoRA 注意力调制，通过轻量级编码器生成的控制 Token 来调节查询（Query）、键（Key）和值（Value），确保生成内容严格遵循预设轨迹。
*   **模块 II：碰撞感知相机轨迹规划（Collision-Aware Planning）**
    *   **迭代扩展**：采用“合成-重建-再规划”的策略。利用 VGGT 模型生成初始点云，并在当前场景中采样候选视角。
    *   **碰撞优化**：引入碰撞检测器 $G(\cdot)$，通过最小化包含碰撞风险和轨迹平滑度的组合代价函数，自动调整相机路径，避免穿模现象。
*   **模块 III：几何感知 3D 重建（Geometry-aware 3DGS）**
    *   **深度对齐监督**：将 MoGe-2 预测的高质量相对深度与 VGGT 的绝对深度进行校准对齐，提供精确的几何约束。
    *   **结构正则化**：引入 DropGaussian 策略，在训练过程中按进度随机丢弃高斯点，防止在稀疏视角下过拟合。
    *   **多视角精炼**：利用图像扩散模型（Difix3D+）对渲染图进行迭代去噪，提升视觉细节。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K**：包含 8 万个真实世界室内外视频。
    *   **DL3DV**：包含 1 万个具有高质量位姿标注的场景。
    *   **Tanks and Temples**：用于评估模型的跨域泛化能力（Out-of-domain）。
*   **对比方法（Baselines）**：
    *   视频生成类：MotionCtrl, ViewCrafter, Wonderland。
    *   3D 场景生成类：Scene-Splatter, ViewCrafter, Wonderland。
*   **评估指标**：
    *   视觉质量：PSNR, SSIM, LPIPS, FID, FVD。
    *   相机控制精度：旋转误差（$R_{err}$）和位移误差（$T_{err}$）。
    *   重建质量：覆盖率（Coverage）、噪声比（Noise Ratio）、F-score。

### 4. 资源与算力
*   **模型基础**：基于 Transformer 架构的视频扩散模型 Wan2.1。
*   **训练细节**：
    *   使用 Adam 优化器，学习率 $1 \times 10^{-4}$。
    *   训练迭代次数为 30,000 次。
    *   采用 BF16 混合精度训练。
    *   输入视频裁剪为 81 帧，分辨率 256x256。
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量，但考虑到 Wan2.1 的规模和 81 帧的高分辨率 VAE 编码，推测需要高性能计算集群（如 A100 或 H100 级别）。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准数据集上进行了大规模定量测试（每个数据集抽取 100-300 个测试样本）。
*   **消融实验**：针对深度对齐监督、结构正则化和精炼模块分别进行了消融研究，验证了各组件的有效性。
*   **客观性**：通过 Colmap 恢复位姿来测量相机控制误差，并引入了 F-score 等几何评估指标，实验设计较为全面、客观，对比了当前最先进的（SOTA）开源及闭源方法。

### 6. 主要结论与发现
*   **性能领先**：NavCrafter 在所有视觉质量指标（PSNR, SSIM, LPIPS）和分布指标（FID, FVD）上均显著优于现有方法。
*   **控制精准**：相比 MotionCtrl 等方法，NavCrafter 的相机位姿误差大幅降低，证明了多阶段控制架构的优越性。
*   **几何一致性**：通过碰撞感知规划和深度对齐，生成的 3D 场景在完整性和几何准确性上表现更佳，有效解决了视角大幅度偏移时的漂移问题。

### 7. 优点
*   **创新的控制机制**：结合了特征注入和 LoRA 调制，实现了极高精度的相机轨迹遵循能力。
*   **碰撞感知规划**：这是 3D 场景生成领域较少关注但极具实用价值的点，确保了生成的导航路径物理可行。
*   **高质量重建**：通过深度校准和扩散模型精炼，克服了传统 3DGS 在稀疏视角下的模糊和伪影问题。

### 8. 不足与局限
*   **动态场景限制**：目前主要针对静态场景，无法处理视频中的动态物体运动。
*   **长序列漂移**：尽管有一致性增强，但在极长距离的探索中，累积误差仍可能导致几何不一致。
*   **计算开销**：迭代式的“合成-重建”过程以及图像扩散模型的精炼步骤，使得推理时间相对较长，难以实现实时生成。

（完）
