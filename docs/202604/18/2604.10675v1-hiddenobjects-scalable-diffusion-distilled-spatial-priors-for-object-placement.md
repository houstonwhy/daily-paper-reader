---
title: "HiddenObjects: Scalable Diffusion-Distilled Spatial Priors for Object Placement"
title_zh: HiddenObjects：用于物体放置的可扩展扩散蒸馏空间先验
authors: "Marco Schouten, Ioannis Siglidis, Serge Belongie, Dim P. Papadopoulos"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10675v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 自然场景中物体放置的空间先验
tldr: 本研究提出一种从扩散模型中蒸馏物体放置空间先验的方法，解决了手动标注规模受限和传统修复管道伪影导致学习捷径的问题。通过自动化框架构建了包含2700万个标注的大规模数据集HiddenObjects，并将其蒸馏为轻量级模型。实验证明该方法在图像编辑任务中优于人类标注和现有基准，且推理速度提升了23万倍，为自然场景中的物体放置提供了高效且准确的显式先验。
selection_source: fresh_fetch
motivation: 现有的物体放置先验学习受限于手动标注的规模瓶颈或自动修复管道产生的伪影干扰。
method: 开发了一个全自动且可扩展的框架，利用扩散修复管道在真实背景上评估密集物体放置，并构建了HiddenObjects大规模数据集。
result: 该方法在下游图像编辑任务中表现优于人类标注，且蒸馏后的轻量级模型推理速度比原始扩散模型快23万倍。
conclusion: 通过蒸馏扩散模型中的隐式知识，可以获得高质量、可扩展且高效的显式空间先验，显著提升物体放置的准确性。
---

## 摘要
我们提出了一种通过蒸馏文本条件扩散模型中编码的隐式放置知识，来学习自然场景中物体放置的显式、类别条件空间先验的方法。以往的工作要么依赖于手动标注的数据（其规模本质上受限），要么依赖于基于修复（inpainting）的物体移除流水线（其产生的伪影会导致捷径学习）。为了解决这些局限性，我们引入了一个全自动且可扩展的框架，利用基于扩散的修复流水线在高质量真实背景上评估密集的物体放置。通过该流水线，我们构建了 HiddenObjects，这是一个包含 2700 万个放置标注的大规模数据集，涵盖 2.7 万个不同场景，并包含针对不同图像和物体类别的排序边界框插入。实验结果表明，我们的空间先验在下游图像编辑任务中优于稀疏的人类标注（VLM-Judge 评分为 3.90 vs 2.68），并显著超过了现有的物体放置基准和零样本视觉语言模型。此外，我们将这些先验蒸馏到一个轻量级模型中，以实现快速的实际推理（速度提升 230,000 倍）。

## Abstract
We propose a method to learn explicit, class-conditioned spatial priors for object placement in natural scenes by distilling the implicit placement knowledge encoded in text-conditioned diffusion models. Prior work relies either on manually annotated data, which is inherently limited in scale, or on inpainting-based object-removal pipelines, whose artifacts promote shortcut learning. To address these limitations, we introduce a fully automated and scalable framework that evaluates dense object placements on high-quality real backgrounds using a diffusion-based inpainting pipeline. With this pipeline, we construct HiddenObjects, a large-scale dataset comprising 27M placement annotations, evaluated across 27k distinct scenes, with ranked bounding box insertions for different images and object categories. Experimental results show that our spatial priors outperform sparse human annotations on a downstream image editing task (3.90 vs. 2.68 VLM-Judge), and significantly surpass existing placement baselines and zero-shot Vision-Language Models for object placement. Furthermore, we distill these priors into a lightweight model for fast practical inference (230,000x faster).

---

## 论文详细总结（自动生成）

以下是对论文《HiddenObjects: Scalable Diffusion-Distilled Spatial Priors for Object Placement》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在自然场景中插入物体（Object Placement）需要理解复杂的空间规律（如：书应在架子上而非空中）。
*   **现有痛点**：
    1.  **手动标注不可扩展**：现有人类标注数据集规模极小且标注稀疏。
    2.  **自动流水线存在缺陷**：以往通过“移除现有物体并修复背景”来获取数据的方法会留下阴影、边缘模糊等伪影，导致模型学会通过寻找伪影（捷径学习）来放置物体，而非理解语义。
*   **核心目标**：利用扩散模型（Diffusion Models）中蕴含的隐式空间知识，自动提取大规模、高密度、无伪影的显式空间先验。

### 2. 方法论
*   **核心思想**：将物体放置视为一个“生成-验证-排序”的过程。
*   **关键技术流程**：
    1.  **空间先验提取 (Extraction)**：
        *   **修复器 (Inpainter)**：给定背景图和目标类别，在密集的候选边界框（通过滑动窗口生成）内使用扩散模型（Qwen-Image + ControlNet）尝试生成物体。
        *   **验证器 (Verifier)**：使用 Grounded-SAM-2 检测生成的图像，确认目标物体是否真的出现在指定位置。
        *   **排序器 (Ranker)**：使用 ImageReward 模型对验证成功的放置结果进行打分，量化人类对该位置的偏好。
    2.  **加速策略**：通过监测去噪过程中条件分支与无条件分支的 L1 差异，在早期步骤识别并终止无效生成，实现 2.4 倍加速。
    3.  **模型蒸馏 (Distillation)**：
        *   将提取的密集先验（27M 标注）作为监督信号，训练一个轻量级的 **DETR 风格 Transformer 模型**。该模型以 ResNet-50 为骨干，直接预测给定背景和类别的最佳放置边界框及置信度。

### 3. 实验设计
*   **数据集**：构建了 **HiddenObjects** 数据集，包含 2.7 万个背景场景（源自 Places365）和 2700 万个评估过的放置标注（涵盖 50 个 COCO 物体类别）。
*   **Benchmark**：
    *   **OPA 数据集**：用于评估放置准确性的标准基准。
    *   **ImgEdit-Judge**：利用多模态大模型（Qwen2.5-VL）评估下游图像编辑任务的质量。
*   **对比方法**：
    *   **零样本 VLM**：Qwen2.5-VL (7B/72B), InternVL3, LLaVA 系列。
    *   **专用放置模型**：PlaceNet, GracoNet, TerseNet, BootPlace。
    *   **基准线**：人类标注（Human Annotation）、随机放置（Random BBox）。

### 4. 资源与算力
*   **硬件使用**：论文提到在 **NVIDIA H100 GPU** 上进行了推理测试。
*   **效率提升**：
    *   **完整流水线**：每张图需 14.49 分钟，显存占用 65.9 GB。
    *   **蒸馏模型**：每张图仅需 **3.77 毫秒**，显存占用 188.9 MB。
*   **训练细节**：使用 AdamW 优化器，学习率 1e-4，Batch size 128，训练时长未明确给出具体总小时数，但提到使用了早停机制（Early Stopping）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了组件消融实验（对比了不同修复器如 SDXL、FLUX 和不同排序器如 CLIP、Aesthetics）。
    *   在 OPA 和 HiddenObjects 两个数据集上进行了跨数据集验证。
    *   对 25 个类别的空间分布进行了热力图可视化分析。
*   **充分性与客观性**：实验设计较为严谨，引入了第三方 VLM 评判器（ImgEdit-Judge）以减少主观偏差，并详细分析了数据集的中心偏置（Center Bias）和尺度分布，证明了其方法在多样性上的优势。

### 6. 主要结论与发现
*   **优于人类标注**：在图像编辑任务中，使用该方法生成的先验进行引导，其效果评分（3.90）显著优于使用人类标注（2.68），证明了人类标注在放置任务中存在高度模糊性。
*   **蒸馏极具价值**：蒸馏后的轻量级模型在保持高性能的同时，推理速度提升了 **230,000 倍**，显存需求降低了 **300 倍**。
*   **缓解偏置**：相比于传统的物体检测数据集（如 COCO），HiddenObjects 提供的空间分布更均匀，有效缓解了摄影师习惯导致的“中心偏置”。

### 7. 优点
*   **数据质量高**：通过“在干净背景上添加”而非“从已有图像移除”的逻辑，彻底解决了背景伪影干扰学习的问题。
*   **可扩展性强**：全自动流程无需人工干预，可轻松扩展至数百万张图像。
*   **实用性极佳**：蒸馏模型极大地降低了部署门槛，使实时物体放置建议成为可能。

### 8. 不足与局限
*   **小物体生成困难**：受限于扩散模型在极小区域内的生成能力，模型在处理远景或极小比例物体的放置先验时存在缺失。
*   **类别覆盖有限**：目前仅针对 50 个常用类别进行了实验，对于更广泛的长尾类别（Long-tail categories）的泛化能力尚待验证。
*   **继承生成偏置**：空间先验本质上是扩散模型训练数据的反映，可能仍带有某些特定的构图偏好或文化偏见。

（完）
