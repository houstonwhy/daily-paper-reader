---
title: "SketchFaceGS: Real-Time Sketch-Driven Face Editing and Generation with Gaussian Splatting"
title_zh: SketchFaceGS：基于高斯泼溅的实时草图驱动人脸编辑与生成
authors: "Bo Li, Jiahao Kang, Yubo Ma, Feng-Lin Liu, Bin Liu, Fang-Lue Zhang, Lin Gao"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19202v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 基于高斯泼溅的草图驱动3D生成
tldr: SketchFaceGS是首个基于2D草图驱动的实时3D高斯人头模型生成与编辑框架。针对草图稀疏且缺乏深度信息的挑战，该方法采用前馈式从粗到精的架构，通过Transformer预测UV特征图并进行高频细节增强，结合UV掩码融合技术实现精确的实时编辑。该研究在保持高保真度的同时，显著提升了3D人脸创作的直观性与交互效率。
selection_source: fresh_fetch
motivation: 现有的3D高斯人头建模缺乏直观的交互手段，而2D草图虽直观但难以直接推断出稠密且几何一致的3D结构。
method: 提出一种从粗到精的架构，利用Transformer预测UV特征并进行3D增强，配合UV掩码融合策略实现实时编辑。
result: 实验表明，该方法在生成保真度和编辑灵活性上均优于现有技术，支持单次前馈生成高质量可编辑3D头像。
conclusion: SketchFaceGS成功实现了从2D草图到高质量3D高斯人脸的实时转换与精细编辑，为数字人建模提供了高效的新方案。
---

## 摘要
3D高斯表示已成为数字头部建模的一种强大范式，实现了具有实时渲染效果的照片级真实质量。然而，直观且交互式地创建或编辑3D高斯头部模型仍然具有挑战性。尽管2D草图为快速、直观的概念设计提供了理想的交互方式，但它们具有稀疏性、深度歧义性且缺乏高频外观线索，这使得从笔触中推断出密集且几何一致的3D高斯结构变得困难——尤其是在实时约束下。为了应对这些挑战，我们提出了SketchFaceGS，这是首个用于从2D草图实时生成和编辑照片级真实感3D高斯头部模型的草图驱动框架。我们的方法采用了一种前馈式、由粗到精的架构。基于Transformer的UV特征预测模块首先从输入草图中重建出粗糙但几何一致的UV特征图，随后3D UV特征增强模块利用高频、照片级真实的细节对其进行细化，从而生成高保真3D头部。对于编辑，我们引入了UV掩码融合技术，并结合逐层特征融合策略，实现了精确、实时、自由视角的修改。大量实验表明，SketchFaceGS在生成保真度和编辑灵活性方面均优于现有方法，能够通过单次前向传递从草图中生成高质量、可编辑的3D头部。

## Abstract
3D Gaussian representations have emerged as a powerful paradigm for digital head modeling, achieving photorealistic quality with real-time rendering. However, intuitive and interactive creation or editing of 3D Gaussian head models remains challenging. Although 2D sketches provide an ideal interaction modality for fast, intuitive conceptual design, they are sparse, depth-ambiguous, and lack high-frequency appearance cues, making it difficult to infer dense, geometrically consistent 3D Gaussian structures from strokes - especially under real-time constraints. To address these challenges, we propose SketchFaceGS, the first sketch-driven framework for real-time generation and editing of photorealistic 3D Gaussian head models from 2D sketches. Our method uses a feed-forward, coarse-to-fine architecture. A Transformer-based UV feature-prediction module first reconstructs a coarse but geometrically consistent UV feature map from the input sketch, and then a 3D UV feature enhancement module refines it with high-frequency, photorealistic detail to produce a high-fidelity 3D head. For editing, we introduce a UV Mask Fusion technique combined with a layer-by-layer feature-fusion strategy, enabling precise, real-time, free-viewpoint modifications. Extensive experiments show that SketchFaceGS outperforms existing methods in both generation fidelity and editing flexibility, producing high-quality, editable 3D heads from sketches in a single forward pass.

---

## 论文详细总结（自动生成）

这是一份关于论文《SketchFaceGS: Real-Time Sketch-Driven Face Editing and Generation with Gaussian Splatting》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何通过直观的2D草图（Sketch）实时生成并编辑高质量、几何一致的3D人脸模型。
*   **研究背景**：
    *   传统的3D建模（如Mesh）对非专业人士门槛高且耗时。
    *   现有的NeRF或GAN方法在渲染速度、多视角一致性或编辑灵活性上存在局限。
    *   3D高斯泼溅（3DGS）虽然实现了实时渲染，但缺乏直观的交互式编辑手段。
    *   2D草图虽然直观，但其稀疏性和深度歧义性使得直接推断密集的3D高斯结构极其困难。

### 2. 方法论
该论文提出了一种**前馈式、由粗到精（Coarse-to-Fine）**的架构，主要包含两个阶段：
*   **粗糙阶段（Transformer-based UV Prediction）**：
    *   使用双分支Transformer架构：一个分支从草图中提取几何特征，另一个分支从参考图中提取外观特征。
    *   引入**AdaIN对齐网络**，解决草图结构与参考图身份之间的冲突，生成初步的几何一致UV特征图。
*   **精细阶段（3D UV Feature Enhancement）**：
    *   利用预训练的3D-GAN（GGHead）作为先验。
    *   通过U-Net预测全局潜码（Latent Code）和多尺度空间调制参数，对粗糙UV图进行高频细节增强，生成最终的3D高斯属性。
*   **编辑机制（UV Mask Fusion）**：
    *   **UV掩码定位**：将2D编辑区域反向投影到3D高斯点云，再映射回规范化的UV空间生成掩码。
    *   **逐层特征融合**：在StyleGAN生成器的多尺度特征层中进行融合，而非直接在3D空间拼接，从而避免接缝伪影，确保编辑区域与未编辑区域自然过渡。

### 3. 实验设计
*   **数据集**：
    *   训练集：使用GGHead合成的多视角数据集（粗糙阶段）和FFHQ单视图数据集（精细阶段）。
    *   测试集：包含100张艺术家手绘草图和100个交互式编辑案例。
*   **对比方法（Baselines）**：
    *   **生成对比**：S3D、SketchFaceNeRF、Nano-LAM（Gemini 2.5 Flash + LAM的组合）。
    *   **编辑对比**：SketchFaceNeRF、MagicQuill、Nano-LAM。
*   **评估指标**：FID、KID（图像质量）、PSNR、SSIM（身份保持）、推理时间（Time）和帧率（FPS）。

### 4. 资源与算力
*   **文中说明**：论文并未明确列出具体的GPU型号、数量或总训练时长。
*   **性能表现**：提到了推理性能，端到端编辑延迟约为**0.3秒**，渲染速度可达**243 FPS**，强调了其“实时性”和“无需针对单个实例优化”的特性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了定性与定量的生成质量对比。
    *   进行了针对连续编辑（多步编辑）的稳定性测试。
    *   **消融实验**：非常充分，分别针对生成管线（增强模块、AdaIN对齐、全局ID特征等）和编辑模块（融合策略对比）进行了验证。
*   **客观性**：通过FID/KID等标准指标和多视角渲染展示，证明了方法在保真度和一致性上的优势。对比实验涵盖了当前最前沿的基于NeRF和扩散模型的方法，评估较为公正。

### 6. 主要结论
*   SketchFaceGS是首个实现**实时、无需优化**的草图驱动3D高斯人脸生成与编辑框架。
*   通过“从粗到精”的策略，成功桥接了抽象草图与高保真3D生成先验。
*   逐层特征融合技术有效解决了3D编辑中的空间伪影问题，实现了自由视角的平滑编辑。

### 7. 优点与亮点
*   **实时交互**：0.3秒的编辑延迟使其能够支持真正的交互式创作流。
*   **无需优化**：相比SketchFaceNeRF等需要逐例优化的方法，前馈架构极大地提升了效率。
*   **高质量融合**：UV掩码融合策略在保持未编辑区域身份特征的同时，实现了自然的局部修改。
*   **多功能性**：除了生成和编辑，还可扩展到局部外观迁移和真实图像的反演（Inversion）。

### 8. 不足与局限
*   **身份偏移风险**：当草图与参考图的几何差异过大时，尽管颜色能保持，但仍可能出现轻微的身份偏移。
*   **先验限制**：依赖于GGHead先验，对于极罕见的配饰、极端遮挡或超出分布（OOD）的输入处理能力有限。
*   **静态限制**：目前主要针对静态头部模型，尚未直接支持复杂的表情动画（尽管作者提到未来会扩展到动态头像）。

（完）
