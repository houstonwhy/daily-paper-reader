---
title: "CityRAG: Stepping Into a City via Spatially-Grounded Video Generation"
title_zh: CityRAG：通过空间关联的视频生成步入城市
authors: "Gene Chou, Charles Herrmann, Kyle Genova, Boyang Deng, Songyou Peng, Bharath Hariharan, Jason Y. Zhang, Noah Snavely, Philipp Henzler"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19741v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 生成3D一致的可导航环境
tldr: CityRAG 是一种空间定位的视频生成模型，旨在模拟真实的 3D 一致且可导航的城市环境。它通过利用地理注册的大规模语料库作为上下文，将生成过程锚定在物理场景中，同时保留对复杂运动和外观变化的先验知识。该模型能够生成长达数分钟、物理一致的视频序列，支持多种天气和光照条件，并能实现闭环导航，为自动驾驶和机器人仿真提供了强大的重建能力。
selection_source: fresh_fetch
motivation: 现有的视频生成模型难以在多变的天气和动态物体配置下，精确重建具有空间一致性和可导航性的真实物理场景。
method: 提出 CityRAG 模型，利用地理注册数据作为上下文进行检索增强生成，并通过非时间对齐的训练数据实现场景语义与瞬态属性的解耦。
result: 实验证明该模型能生成数分钟长的连贯视频，在数千帧中保持天气和光照一致性，并成功实现闭环和复杂轨迹导航。
conclusion: CityRAG 有效结合了地理空间上下文与生成式先验，为构建大规模、高保真且可交互的城市级仿真环境提供了新途径。
---

## 摘要
我们解决了生成 3D 一致、可导航且具有空间关联性的环境（即真实地点的模拟）这一问题。现有的视频生成模型可以产生与文本（T2V）或图像（I2V）提示一致的合理序列。然而，在任意天气条件和动态物体配置下重建现实世界的能力，对于包括自动驾驶和机器人仿真在内的下游应用至关重要。为此，我们提出了 CityRAG，这是一种视频生成模型，它利用大规模地理注册数据语料库作为上下文，将生成过程关联到物理场景，同时保持对复杂运动和外观变化的习得先验。CityRAG 依赖于时间上不对齐的训练数据，这教会了模型从语义上将底层场景与其瞬态属性解耦。我们的实验表明，CityRAG 可以生成连贯的长达数分钟的、具有物理关联性的视频序列，在数千帧中保持天气和光照条件，实现回环，并能导航复杂轨迹以重建现实世界的地理。

## Abstract
We address the problem of generating a 3D-consistent, navigable environment that is spatially grounded: a simulation of a real location. Existing video generative models can produce a plausible sequence that is consistent with a text (T2V) or image (I2V) prompt. However, the capability to reconstruct the real world under arbitrary weather conditions and dynamic object configurations is essential for downstream applications including autonomous driving and robotics simulation. To this end, we present CityRAG, a video generative model that leverages large corpora of geo-registered data as context to ground generation to the physical scene, while maintaining learned priors for complex motion and appearance changes. CityRAG relies on temporally unaligned training data, which teaches the model to semantically disentangle the underlying scene from its transient attributes. Our experiments demonstrate that CityRAG can generate coherent minutes-long, physically grounded video sequences, maintain weather and lighting conditions over thousands of frames, achieve loop closure, and navigate complex trajectories to reconstruct real-world geography.

---

## 论文详细总结（自动生成）

### CityRAG：通过空间关联的视频生成步入城市 —— 论文深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
现有的视频生成模型（如 T2V 或 I2V）虽然能生成视觉上合理的序列，但往往属于“AI 幻觉”，无法与现实世界的精确地理布局对齐。对于自动驾驶仿真、机器人训练和虚拟旅游等应用，需要模型能够根据给定的轨迹，在保持特定天气和动态物体（如行人、车辆）的同时，精确还原真实世界的静态结构（如建筑、道路、红绿灯）。
**核心问题**：如何生成一个既符合初始图像属性（天气、光照、动态物体），又在空间上严格关联真实物理世界（3D 一致、可导航）的视频环境？

#### 2. 论文提出的方法论
CityRAG 引入了**检索增强生成（RAG）**框架来解决视频生成的空间定位问题。
*   **核心思想**：利用大规模地理注册（Geo-registered）的街景数据作为上下文（Context），将生成过程锚定在物理场景中，同时利用生成先验处理复杂的运动和外观变化。
*   **关键技术细节**：
    *   **时间不对齐训练（Temporally Unaligned Training）**：这是该模型最核心的创新。研究者收集了同一地点在不同时间（如早晨 vs 傍晚）拍摄的成对街景视频。训练时，要求模型利用“早晨”的视频作为参考，去重建“傍晚”的场景。这迫使模型在语义上解耦**静态属性**（从参考视频中提取建筑、道路结构）和**瞬态属性**（从初始图像中提取光照、天气、动态车辆）。
    *   **架构设计**：基于 **Wan 2.1 (14B)** 扩散变压器（DiT）模型。
        *   **空间条件分支**：通过交叉注意力机制注入检索到的街景帧。
        *   **轨迹控制**：将 4x4 相机外参矩阵通过 MLP 处理后，以残差形式注入 DiT 块，实现精确的相机路径控制。
    *   **推理流程**：用户提供初始图像和轨迹 -> 从数据库检索邻近街景视频 -> 模型生成视频。支持通过拼接不同路径的视频来处理复杂的转向（如 90 度转弯）。

#### 3. 实验设计
*   **数据集**：从全球 10 个城市（如巴黎、伦敦、檀香山、旧金山等）收集了 **550 万张全景图**及其位姿。筛选出 130 万对地理对齐但时间不对齐的序列用于训练。
*   **对比方法（Benchmarks）**：
    *   **I2V + 姿态控制**：Gen3C。
    *   **V2V + 姿态控制**：Gen3C 变体、TrajectoryCrafter。
    *   **V2V + 风格迁移**：AnyV2V。
*   **评估指标**：
    *   **新视角合成指标**：PSNR、SSIM、LPIPS（包括针对静态结构的掩码版本 PSNR-S 等）。
    *   **生成质量指标**：FID。
    *   **用户研究**：评估视觉质量、与首帧的连续性、对物理位置的忠实度。

#### 4. 资源与算力
*   **算力消耗**：论文明确指出，模型在 **32 台 NVIDIA A100 GPU** 上训练了约 **1 周**时间（约 20,000 次迭代）。
*   **模型规模**：基于 14B 参数量的 Wan 2.1 基础模型进行微调。

#### 5. 实验数量与充分性
*   **实验规模**：使用了来自 8 个城市的 130 万对数据进行训练，并在另外 2 个完全未见过的城市（伦敦、圣保罗）以及预留测试集上进行评估。
*   **充分性**：
    *   定量实验涵盖了图像重建质量和生成分布相似度。
    *   定性实验展示了长达数千帧（数分钟）的生成效果、360 度极端旋转以及回环（Loop Closure）能力。
    *   **客观性**：通过用户研究（20 名参与者）对比了多种基线模型，并进行了消融实验验证轨迹控制和空间条件的有效性。实验设计较为全面，涵盖了从短视频到长序列导航的多种场景。

#### 6. 论文的主要结论与发现
*   **空间关联性**：CityRAG 是首个能生成数分钟长、且与真实世界地理布局严格一致的视频模型。
*   **解耦能力**：通过时间不对齐训练，模型成功实现了静态几何结构与瞬态环境因素（天气、交通）的自动解耦，无需额外的人工标注。
*   **鲁棒性**：即使检索到的参考视频在空间上存在偏差（如车道不同）或时间上不连续（拼接路径），模型仍能生成平滑且一致的视频。
*   **泛化性**：模型能够泛化到训练集之外的新城市和极端的相机运动（如 360 度旋转）。

#### 7. 优点
*   **长效一致性**：解决了传统视频模型在长序列生成中容易发生的“漂移”和“幻觉”问题。
*   **数据驱动的解耦**：巧妙利用街景数据的多时段特性，避免了复杂的启发式算法。
*   **实用性强**：为自动驾驶提供了一种低成本生成高保真、可控仿真环境的方法。
*   **灵活性**：支持用户自定义轨迹，并能通过 RAG 机制实时获取外部知识。

#### 8. 不足与局限
*   **数据偏差**：训练数据主要集中在西方城市，可能存在地理表示偏差。
*   **环境覆盖有限**：由于街景采集车的限制，数据中缺乏极端天气（大雪、暴雨）和夜间场景。
*   **自回归漂移**：虽然引入了 RAG，但在极长序列生成中，仅靠首帧传递仍可能存在微小的质量退化，未来需结合更强的长上下文管理技术。
*   **隐私风险**：尽管进行了脱敏处理（模糊人脸、车牌），但生成高度真实的现实场景仍涉及潜在的隐私和伦理挑战。

（完）
