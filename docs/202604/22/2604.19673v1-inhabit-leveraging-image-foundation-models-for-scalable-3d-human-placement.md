---
title: "InHabit: Leveraging Image Foundation Models for Scalable 3D Human Placement"
title_zh: InHabit：利用图像基础模型实现可扩展的 3D 人物放置
authors: "Nikita Kister, Pradyumna YM, István Sárándi, Jiayi Wang, Anna Khoreva, Gerard Pons-Moll"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19673v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于填充3D场景的数据生成器
tldr: 针对大规模3D人机交互数据稀缺的问题，本文提出InHabit，一种利用2D基础模型知识自动生成3D场景中人体放置的框架。该方法通过“渲染-生成-提升”流程，结合视觉语言模型和图像编辑模型，在3D场景中生成符合常识且物理合理的SMPL-X人体模型。实验表明，该方法生成的7.8万个样本显著提升了3D重建和接触估计任务的性能，且在用户研究中优于现有技术。
selection_source: fresh_fetch
motivation: 现有的3D人机交互数据获取成本高且缺乏语义常识，限制了具身智能体对复杂环境的理解。
method: 提出“渲染-生成-提升”流程，利用VLM规划动作、图像编辑模型插入人体，并通过优化算法将2D结果提升为与场景几何对齐的3D SMPL-X模型。
result: "在Habitat-Matterport3D上构建了包含7.8万个样本的大规模数据集，显著增强了RGB 3D重建效果，并在感知评估中获得78%的偏好率。"
conclusion: 证明了利用2D基础模型的常识知识可以有效解决3D场景中大规模、高质量人体交互数据生成的难题。
---

## 摘要
训练具身智能体像人类一样理解 3D 场景，需要大规模的人类与多样化环境进行有意义交互的数据，然而此类数据非常稀缺。现实世界的动作捕捉成本高昂且局限于受控环境，而现有的合成数据集则依赖于简单的几何启发式方法，忽略了丰富的场景上下文。相比之下，在互联网规模数据上训练的 2D 基础模型已隐式地获取了关于人与环境交互的常识性知识。为了将这些知识迁移到 3D 领域，我们推出了 InHabit，这是一个全自动且可扩展的数据生成器，用于在 3D 场景中填充交互式人物。InHabit 遵循“渲染-生成-提升”（render-generate-lift）原则：给定一个渲染的 3D 场景，视觉语言模型提出符合上下文语境的有意义动作，图像编辑模型插入人物，随后通过优化程序将编辑结果提升为与场景几何对齐且符合物理规律的 SMPL-X 人体模型。应用于 Habitat-Matterport3D，InHabit 生成了首个大规模写实 3D 人机交互数据集，包含分布在 800 个建筑规模场景中的 7.8 万个样本，并具有完整的 3D 几何、SMPL-X 人体和 RGB 图像。使用我们的样本增强标准训练数据，可以提高基于 RGB 的 3D 人物-场景重建和接触估计的性能；在一项感知用户研究中，我们的数据在 78% 的案例中优于现有最先进技术。

## Abstract
Training embodied agents to understand 3D scenes as humans do requires large-scale data of people meaningfully interacting with diverse environments, yet such data is scarce. Real-world motion capture is costly and limited to controlled settings, while existing synthetic datasets rely on simple geometric heuristics that ignore rich scene context. In contrast, 2D foundation models trained on internet-scale data have implicitly acquired commonsense knowledge of human-environment interactions. To transfer this knowledge into 3D, we introduce InHabit, a fully automatic and scalable data generator for populating 3D scenes with interacting humans. InHabit follows a render-generate-lift principle: given a rendered 3D scene, a vision-language model proposes contextually meaningful actions, an image-editing model inserts a human, and an optimization procedure lifts the edited result into physically plausible SMPL-X bodies aligned with the scene geometry. Applied to Habitat-Matterport3D, InHabit produces the first large-scale photorealistic 3D human-scene interaction dataset, containing 78K samples across 800 building-scale scenes with complete 3D geometry, SMPL-X bodies, and RGB images. Augmenting standard training data with our samples improves RGB-based 3D human-scene reconstruction and contact estimation, and in a perceptual user study our data is preferred in 78% of cases over the state of the art.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **InHabit** 的系统，旨在利用 2D 图像基础模型的常识知识来解决 3D 人机交互（HSI）数据匮乏的问题。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：训练具身智能体（Embodied AI）需要大量人类与 3D 环境交互的数据，但现有数据存在“规模”与“语义质量”的矛盾。
*   **研究背景**：
    *   **现实采集（MoCap）**：成本极高，且受限于实验室环境，场景数量极少（通常少于 30 个）。
    *   **启发式合成**：虽然能大规模生成，但仅依赖几何避障，缺乏语义常识（例如：人会在沙发上看电视，而不仅仅是站在沙发旁边）。
    *   **2D 基础模型的潜力**：视觉语言模型（VLM）和图像生成模型在互联网规模的数据上学习到了丰富的“人与环境如何交互”的常识。

### 2. 方法论：渲染-生成-提升（Render-Generate-Lift）
InHabit 采用全自动流水线，将 2D 知识迁移至 3D 空间：
*   **渲染（Render）**：从现有的 3D 场景（如 HM3D）中自动选择视角并渲染 RGB-D 图像。
*   **生成（Generate - 2D 阶段）**：
    *   **语义推理**：利用 VLM（如 Gemini）分析场景，提出符合逻辑的动作建议（如“在厨房做饭”）。
    *   **图像编辑**：使用图像编辑模型根据 VLM 的建议在渲染图中插入写实的人体，模型会自动调整姿态以适应家具（如坐姿、扶手动作）。
*   **提升（Lift - 3D 阶段）**：
    *   **参数化建模**：将 2D 图像中的人体恢复为 3D SMPL-X 模型。
    *   **几何优化**：利用已知的 3D 场景几何信息，通过优化算法调整人体的姿态（$\theta$）、形状（$\beta$）、平移（$t$）和缩放（$s$）。
    *   **损失函数**：结合了 2D 重投影损失、深度对齐损失、接触损失（鼓励与表面接触）和穿透惩罚（避免穿模）。
*   **后处理过滤**：通过可见性、深度边界一致性和体积过滤器剔除不合理的生成结果（如浮空、严重穿模或非人类物体）。

### 3. 实验设计
*   **数据集**：基于 **Habitat-Matterport 3D (HM3D)** 场景，生成了包含 **7.8 万个样本** 的 **InHabitants** 数据集，涵盖约 800 个建筑规模的场景。
*   **对比基准（Baselines）**：
    *   **GenZI**：基于扩散模型修复的零样本 3D 交互生成方法。
    *   **POSA++**：基于几何接触预测的优化方法（通过 LLM 增强了文本驱动能力）。
*   **评估指标**：CLIP 分数（语义一致性）、接触 F1 分数、PA-MPJPE（姿态准确度）以及感知用户研究。
*   **下游任务验证**：
    *   **接触估计**：在 DAMON 测试集上验证。
    *   **3D 重建**：在 PROX 和 RICH 两个真实世界基准数据集上验证。

### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**具体的 GPU 型号、数量或总训练时长。
*   **自动化程度**：强调了该流程是“全自动且可扩展的”，意味着其生成效率远高于人工标注或动捕采集。

### 5. 实验数量与充分性
*   **实验规模**：生成了 7.8 万个样本，这是目前同类数据集中规模最大的之一。
*   **消融实验**：对全场景几何 vs 局部几何提升、缩放参数 $s$ 的必要性等进行了消融分析。
*   **客观性**：
    *   进行了 **726 份** 用户感知调研，结果显示 InHabit 在 78.1% 的情况下被认为优于竞争对手。
    *   通过在 PROX 和 RICH 等独立第三方真实数据集上进行测试，证明了合成数据对模型泛化能力的实际提升，实验设计较为客观公平。

### 6. 主要结论与发现
*   **知识迁移有效性**：2D 基础模型确实隐式包含了足够的 3D 交互常识，可以有效引导 3D 人体放置。
*   **性能提升**：使用 InHabitants 数据增强后，现有的接触估计模型（DECO）和 HSI 重建模型（Human3R, GRAFT）在真实数据集上的表现均有显著提升。
*   **物理合理性**：结合已知 3D 几何的优化比纯图像驱动的方法能产生更少的穿模和更自然的接触。

### 7. 优点
*   **语义丰富性**：解决了传统合成数据“动作机械、不合逻辑”的问题，生成的交互非常自然（如看书、指点地图、打台球）。
*   **高可扩展性**：无需人工干预，可快速为数千个 3D 场景填充交互人体。
*   **物理对齐**：通过将 2D 生成与 3D 几何约束结合，保证了生成的 3D 模型在物理上是可行的。

### 8. 不足与局限
*   **静态限制**：目前仅生成静态姿态，尚未涉及动态视频或连续动作序列。
*   **场景静态性**：假设场景是固定的，无法模拟改变环境的交互（如打开柜门、移动椅子）。
*   **模型偏差**：依赖于 SMPL-X 成人模型，且过滤器会剔除小孩或非标准体型，存在一定的人群分布偏差。
*   **2D-3D 鸿沟**：图像编辑模型有时会生成与原始 3D 几何冲突的视觉效果（如幻觉出不存在的家具），虽然有过滤器，但仍可能导致样本浪费。

（完）
