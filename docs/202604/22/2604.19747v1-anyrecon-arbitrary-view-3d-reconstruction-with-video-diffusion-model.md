---
title: "AnyRecon: Arbitrary-View 3D Reconstruction with Video Diffusion Model"
title_zh: AnyRecon：基于视频扩散模型的任意视角三维重建
authors: "Yutian Chen, Shi Guo, Renbiao Jin, Tianshuo Yang, Xin Cai, Yawen Luo, Mingxin Yang, Mulin Yu, Linning Xu, Tianfan Xue"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19747v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 利用视频扩散模型进行3D重建
tldr: 针对稀疏视图3D重建中生成模型受限于输入帧数、几何一致性差的问题，本文提出AnyRecon框架。它利用视频扩散模型，通过全局场景记忆缓存和几何感知调节策略，支持任意数量且无序的输入视图。该方法结合了显式3D几何记忆与高效的4步扩散蒸馏技术，实现了在大规模场景和长轨迹下的鲁棒、一致的3D重建。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的3D重建方法通常仅支持极少数输入帧，难以保证大规模或复杂场景下的几何一致性。
method: 提出一种可扩展框架，通过全局视图缓存构建持久化记忆，并引入几何驱动的检索与显式3D几何记忆来耦合生成与重建过程。
result: 实验证明，该方法在处理不规则输入、大视角偏差和长轨迹场景时，表现出极强的鲁棒性和可扩展性。
conclusion: AnyRecon通过结合视频扩散模型与显式几何控制，有效解决了任意视图输入下的高质量3D场景重建难题。
---

## 摘要
稀疏视角三维重建对于从随意拍摄中建模场景至关重要，但对于非生成式重建方法而言仍具挑战性。现有的基于扩散的方法通过合成新视角来缓解这些问题，但它们通常仅以一两帧拍摄图像为条件，这限制了几何一致性，并制约了在大规模或多样化场景中的可扩展性。我们提出了 AnyRecon，这是一个可扩展的框架，用于从任意且无序的稀疏输入中进行重建，在支持灵活的条件基数的同时保留了显式的几何控制。为了支持长程条件建模，我们的方法通过预置的拍摄视角缓存构建了持久的全局场景记忆，并移除了时间压缩，以在大视角变化下保持帧级对应关系。除了更好的生成模型外，我们还发现生成与重建之间的相互作用对于大规模三维场景至关重要。因此，我们引入了一种几何感知条件策略，通过显式的三维几何记忆和几何驱动的拍摄视角检索，将生成与重建耦合在一起。为了确保效率，我们将 4 步扩散蒸馏与上下文窗口稀疏注意力相结合，以降低二次复杂度。广泛的实验证明了在不规则输入、大视角间隙和长轨迹下，该方法具有鲁棒且可扩展的重建能力。

## Abstract
Sparse-view 3D reconstruction is essential for modeling scenes from casual captures, but remain challenging for non-generative reconstruction. Existing diffusion-based approaches mitigates this issues by synthesizing novel views, but they often condition on only one or two capture frames, which restricts geometric consistency and limits scalability to large or diverse scenes. We propose AnyRecon, a scalable framework for reconstruction from arbitrary and unordered sparse inputs that preserves explicit geometric control while supporting flexible conditioning cardinality. To support long-range conditioning, our method constructs a persistent global scene memory via a prepended capture view cache, and removes temporal compression to maintain frame-level correspondence under large viewpoint changes. Beyond better generative model, we also find that the interplay between generation and reconstruction is crucial for large-scale 3D scenes. Thus, we introduce a geometry-aware conditioning strategy that couples generation and reconstruction through an explicit 3D geometric memory and geometry-driven capture-view retrieval. To ensure efficiency, we combine 4-step diffusion distillation with context-window sparse attention to reduce quadratic complexity. Extensive experiments demonstrate robust and scalable reconstruction across irregular inputs, large viewpoint gaps, and long trajectories.

---

## 论文详细总结（自动生成）

这是一份关于论文《AnyRecon: Arbitrary-View 3D Reconstruction with Video Diffusion Model》的结构化深度分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：如何从任意、无序且稀疏的拍摄视角中实现高质量、大尺度的三维场景重建。
*   **研究背景**：
    *   传统的神经重建方法（如 NeRF, 3DGS）依赖密集采样，在稀疏输入下表现不佳。
    *   现有的基于扩散模型的方法（如 ViewCrafter, Uni3C）通常仅能以 1-2 帧为参考，且缺乏显式的几何控制，导致在大视角变化或长轨迹场景中难以保持几何一致性和外观保真度。
    *   **动机**：开发一个能够处理任意数量输入帧、具备强几何约束且计算高效的可扩展重建框架。

### 2. 方法论
AnyRecon 提出了一个将**视频扩散模型**与**显式几何表示**相结合的闭环框架：
*   **核心思想**：通过“生成-重建”的迭代循环，利用扩散模型合成新视角，再将新视角回投影以更新全局 3D 几何记忆，从而指导后续生成。
*   **关键技术细节**：
    *   **全局场景记忆（Global Scene Memory）**：将检索到的参考帧作为 KV 缓存预置在序列开头，使模型能灵活查询任意数量的 3D 背景信息。
    *   **非压缩潜编码（Non-Compressive Latent Encoding）**：弃用传统视频模型中的时间压缩（如 3D-VAE），改用逐帧 2D-VAE，以在大视角跳跃下保持精确的像素级对应。
    *   **几何感知条件策略**：
        *   **3D 几何记忆更新**：利用点云估计模型（如 $\pi^3$）将生成的图像不断融合进全局点云 $M_{geo}$。
        *   **几何驱动检索**：基于目标视角的可见性索引图（Visibility Index Map）计算参考帧的几何贡献度 $s_i$，而非简单的图像相似度，从而在遮挡严重的场景下选择最有效的参考视角。
    *   **效率优化**：采用 **4 步扩散蒸馏（DMD2）** 结合 **上下文窗口稀疏注意力**，将计算复杂度从二次方降至线性，推理速度提升约 20 倍。

### 3. 实验设计
*   **数据集**：
    *   **DL3DV-10K**：用于大规模室内外场景训练和评估。
    *   **Tanks and Temples**：用于验证模型的泛化能力。
*   **Benchmark 设置**：
    *   **插值（Interpolation）**：给定第 1、21、40 帧，合成中间帧。
    *   **外推（Extrapolation）**：给定前 31 帧中的稀疏帧，预测后续未观察到的轨迹。
*   **对比方法**：Difix3D+（基于图像细化）、ViewCrafter（视频扩散）、Uni3C（统一相机控制）。

### 4. 资源与算力
*   **硬件**：使用了 **64 张 NVIDIA A800 GPU**。
*   **训练流程**：
    1.  全自注意力微调（100k 次迭代）。
    2.  稀疏注意力预热（10k 次迭代）。
    3.  DMD2 蒸馏（30k 次迭代）。
*   **优化器**：AdamW，学习率在 $1 \times 10^{-4}$ 到 $1 \times 10^{-5}$ 之间。

### 5. 实验数量与充分性
*   **实验规模**：在 DL3DV 评估集（10 个场景）和 Tanks and Temples（5 个场景）上进行了定量测试。
*   **消融实验**：针对时间压缩（TC）、扩散步数（50 步 vs 4 步）、稀疏注意力、以及全局场景记忆的有无进行了详细对比。
*   **评价**：实验设计较为充分，涵盖了从短程插值到长程（超过 200 帧）大场景重建的多种任务。通过定量指标（PSNR, SSIM, LPIPS）和推理时间对比，客观地展示了该方法在质量与效率之间的平衡。

### 6. 主要结论与发现
*   **几何一致性**：显式的 3D 几何记忆更新能有效防止长轨迹生成中的几何漂移。
*   **输入灵活性**：全局场景记忆允许模型处理无序且数量不定的输入，打破了传统视频模型对时序连续性的依赖。
*   **效率突破**：通过蒸馏和稀疏注意力，在保持高保真度的前提下，将 40 帧序列的生成时间缩短至约 105 秒，显著优于对比方法。

### 7. 优点（亮点）
*   **闭环设计**：将生成式先验与显式几何重建深度耦合，解决了“幻觉”内容与真实观测不一致的问题。
*   **鲁棒性**：在处理大视角偏差（Wide-baseline）和不规则采样输入时表现出极强的稳定性。
*   **高性能推理**：4 步采样的实现使其在实际应用（如手机拍摄后的快速建模）中更具可行性。

### 8. 不足与局限
*   **初始几何依赖**：性能高度依赖于初始 3D 几何记忆的质量。如果初始视角重叠度极低导致点云构建失败，后续生成也会受限。
*   **极端遮挡**：虽然引入了几何驱动检索，但在极度复杂的遮挡环境下，几何引导可能仍存在噪声。
*   **算力门槛**：训练过程依赖大量 A800 算力，对于普通研究者而言复现成本较高。

（完）
