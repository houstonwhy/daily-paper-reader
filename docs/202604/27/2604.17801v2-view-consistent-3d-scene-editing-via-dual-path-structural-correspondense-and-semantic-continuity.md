---
title: View-Consistent 3D Scene Editing via Dual-Path Structural Correspondense and Semantic Continuity
title_zh: 基于双路径结构对应与语义连续性的视角一致 3D 场景编辑
authors: "Pufan Li, Bi'an Du, Shenghe Zheng, Junyi Yao, Wei Hu"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17801v2"
tags: ["query:3dgen"]
score: 8.0
evidence: 文本驱动的3D场景编辑与多视角一致性
tldr: 针对文本驱动3D场景编辑中多视图不一致的难题，本文提出一种基于联合分布建模的视图一致性编辑框架。该方法通过双路径一致性机制，结合投影引导的结构对应和补丁级语义传播，显式引入跨视图依赖。此外，研究者构建了配对多视图编辑数据集以提供可靠监督。实验证明，该方法在复杂场景下能实现精确且视图一致的高质量编辑效果。
selection_source: fresh_fetch
motivation: 现有的3D场景编辑方法主要依赖推理时同步，难以从根本上解决多视图不一致性及泛化性受限的问题。
method: 提出一种双路径一致性机制，利用投影引导的结构对应和补丁级语义传播来显式建模跨视图的结构与语义依赖。
result: 通过构建专门的配对多视图编辑数据集进行训练，该方法在复杂场景的编辑任务中展现出卓越的视图一致性和精确度。
conclusion: 本研究通过将3D编辑重新定义为跨视图联合分布建模，为实现稳健且一致的3D场景编辑提供了有效的技术路径。
---

## 摘要
文本驱动的 3D 场景编辑近年来引起了越来越多的关注。大多数现有方法遵循“渲染-编辑-优化”流水线，即从 3D 场景中渲染多视角图像，使用 2D 图像编辑器进行编辑，然后用于优化底层的 3D 表示。然而，跨视角不一致性仍然是一个主要的瓶颈。尽管最近的方法引入了几何线索、跨视角交互或视频先验来缓解这一问题，但它们在很大程度上仍依赖于推理时的同步，因此在鲁棒性和泛化性方面仍然受限。在这项工作中，我们从分布的角度重新审视了多视角一致的 3D 编辑：3D 场景编辑本质上需要跨视角的联合分布建模。基于这一见解，我们提出了一个视角一致的 3D 编辑框架，该框架在编辑过程中显式地引入了跨视角依赖关系。此外，受结构对应和语义连续性依赖于不同跨视角线索这一观察的启发，我们引入了一种双路径一致性机制，由投影引导的结构引导和补丁级语义传播组成，以实现有效的跨视角编辑。此外，我们构建了一个成对的多视角编辑数据集，为学习编辑场景中的跨视角一致性提供了可靠的监督。大量的实验表明，我们的方法在复杂场景中实现了卓越的编辑性能，具有精确且一致的视角。

## Abstract
Text-driven 3D scene editing has recently attracted increasing attention. Most existing methods follow a render-edit-optimize pipeline, where multi-view images are rendered from a 3D scene, edited with 2D image editors, and then used to optimize the underlying 3D representation. However, cross-view inconsistency remains a major bottleneck. Although recent methods introduce geometric cues, cross-view interactions, or video priors to mitigate this issue, they still largely rely on inference-time synchronization and thus remain limited in robustness and generalization.In this work, we recast multi-view consistent 3D editing from a distributional perspective: 3D scene editing essentially requires a joint distribution modeling across viewpoints.Based on this insight, we propose a view-consistent 3D editing framework that explicitly introduces cross-view dependencies into the editing process. Furthermore, motivated by the observation that structural correspondence and semantic continuity rely on different cross-view cues, we introduce a dual-path consistency mechanism consisting of projection-guided structural guidance and patch-level semantic propagation for effective cross-view editing. Further, we construct a paired multi-view editing dataset that provides reliable supervision for learning cross-view consistency in edited scenes. Extensive experiments demonstrate that our method achieves superior editing performance with precise and consistent views for complex scenes.

---

## 论文详细总结（自动生成）

这篇论文《View-Consistent 3D Scene Editing via Dual-Path Structural Correspondence and Semantic Continuity》针对文本驱动的 3D 场景编辑中存在的视角不一致问题，提出了一种创新的双路径一致性框架。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：当前的 3D 场景编辑（如基于 3D Gaussian Splatting 的编辑）普遍采用“渲染-编辑-优化”的流水线。
*   **核心痛点**：现有的 2D 图像编辑器（如 InstructPix2Pix）是针对单张图像设计的，在处理多视角渲染图时，各视角之间的编辑结果往往在几何结构、材质外观和语义细节上无法对齐，导致最终优化的 3D 模型出现伪影或漂移。
*   **研究动机**：作者认为 3D 编辑本质上应该是对多视角的**联合分布建模**，而非独立的单图编辑。因此，需要一种能够显式引入跨视角依赖关系的机制，使编辑器具备内在的视角一致性。

### 2. 方法论
核心思想是将多视角编辑建模为一个递归的条件生成过程，通过**双路径一致性机制**来解耦并解决结构和语义的不一致。
*   **投影引导的结构引导路径 (Structural Guidance)**：
    *   利用深度估计和相机位姿，将前一帧已编辑的图像重投影到当前视角，生成“结构先验”。
    *   通过一个轻量级的控制路径（类似于 ControlNet 的设计），将这些几何对齐的特征注入到 Diffusion Transformer (DiT) 的主干网络中，确保空间布局的一致性。
*   **补丁级语义传播路径 (Semantic Propagation)**：
    *   为了防止颜色和材质随视角漂移，系统将前一帧的编辑结果编码为补丁级（Patch-level）特征。
    *   采用**参考引导注意力机制 (Reference-guided Attention)**，让当前视角的查询向量（Query）去检索前一帧的键值对（Key/Value），从而实现语义属性的稳定传播。
*   **CVC-Edit 数据集构建**：
    *   由于缺乏成对的多视角编辑监督数据，作者通过将相邻视角图像拼接并利用强大的 2D 编辑器进行联合编辑，再配合 DINOv3 特征进行一致性过滤，构建了一个包含 28 万个样本的训练集。

### 3. 实验设计
*   **数据集/场景**：在 IN2N、Mip-NeRF 360、BlendedMVS 和 LLFF 等主流 3D 场景数据集上进行了验证。
*   **Benchmark 与对比方法**：
    *   对比了近年来的 SOTA 方法：**GaussCtrl (2024)**、**DGE (2024)**、**EditSplat (2025)** 和 **ViP3DE (2026)**。
*   **评价指标**：
    *   **CLIP Similarity**：衡量编辑结果与文本描述的对齐度。
    *   **CLIP Directional Similarity**：衡量编辑方向的准确性。
    *   **DINO Similarity**：通过特征相似度衡量跨视角的一致性。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练时长**：
    *   第一阶段（结构路径训练）：22k 次迭代，耗时约 **6 天**。
    *   第二阶段（语义路径训练）：24k 次迭代，耗时约 **6.5 天**。
    *   **总计约 12.5 天**。
*   **精度**：采用 bfloat16 精度和梯度检查点（Gradient Checkpointing）技术以节省显存。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从局部物体修改（如给雕像加胡须）到全局风格化（如转为水彩画、漫画风）的多种复杂任务。
*   **消融实验**：非常详尽，分别验证了结构路径、语义路径的必要性，并探讨了路径深度（Block 数量）和注入位置（中间层 vs 深层）对结果的影响。
*   **客观性**：对比方法均为最新的顶会论文成果，且使用了统一的 3DGS 优化框架，实验对比具有较高的公平性和说服力。

### 6. 主要结论与发现
*   **联合建模优于推理同步**：通过在训练阶段引入跨视角依赖，比单纯在推理时通过注意力共享等手段实现的一致性更具鲁棒性。
*   **双路径的互补性**：结构路径解决了“东西在哪”的问题，语义路径解决了“东西长什么样”的问题，两者缺一不可。
*   **数据集的重要性**：高质量的 CVC-Edit 数据集是模型能够学习到跨视角传播能力的基石。

### 7. 优点
*   **理论视角新颖**：从概率分布的角度重新审视 3D 编辑，将任务从单图编辑提升到了序列一致性生成。
*   **效果卓越**：在处理复杂场景（如植被、不规则物体）时，其视角稳定性明显优于之前的基于视频先验或深度约束的方法。
*   **通用性强**：基于 FLUX 架构，具备强大的指令遵循能力和高分辨率（1024x1024）处理能力。

### 8. 不足与局限
*   **计算成本高**：训练需要大量的 GPU 资源和时间，且推理时采用递归处理，每一帧都依赖前一帧，无法并行处理所有视角。
*   **误差累积风险**：由于是顺序传播（Sequential Propagation），如果前几帧编辑出现偏差，该误差可能会随视角切换而累积放大。
*   **深度依赖**：结构路径高度依赖深度估计的准确性，在深度图不准（如透明、高反射物体）的情况下，结构对齐可能会失效。

（完）
