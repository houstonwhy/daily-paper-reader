---
title: "Agentic Video Generation: From Text to Executable Event Graphs via Tool-Constrained LLM Planning"
title_zh: 代理式视频生成：通过工具约束的 LLM 规划实现从文本到可执行事件图的转换
authors: "Nicolae Cudlenco, Mihai Masala, Marius Leordeanu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10383v1"
tags: ["query:3dgen"]
score: 8.0
evidence: LLM构建结构化规范并在3D引擎中执行
tldr: 本研究针对神经视频生成模型语义不可靠的问题，提出了一种基于智能体的生成框架。该系统利用LLM构建结构化的时空事件图（GEST），并由3D游戏引擎确定性执行。通过分层多智能体架构和工具约束规划，确保生成的视频在物理逻辑和语义对齐上远超传统神经生成模型，为可控视频生成提供了新范式。
selection_source: fresh_fetch
motivation: 现有视频生成系统虽视觉效果出色，但往往缺乏语义可靠性、物理一致性以及可验证的底层结构。
method: 提出一种分层智能体架构，通过LLM进行叙事规划并结合程序化后端强制执行约束，生成可被3D引擎执行的时空事件图。
result: "该系统在物理有效性（58%）和语义对齐（3.75/5）方面显著优于VEO 3.1和WAN 2.2等主流神经视频生成模型。"
conclusion: 将LLM的逻辑推理与游戏引擎的确定性模拟相结合，能够有效解决视频生成中的语义失真和物理违和问题。
---

## 摘要
现有的多智能体视频生成系统利用 LLM 智能体来编排神经视频生成器，虽然能产生视觉效果惊人的输出，但在语义上不可靠且缺乏基准真相标注。我们提出了一种反转该范式的代理系统：LLM 不直接生成像素，而是构建一个正式的时空事件图（GEST）——这是一种包含角色、动作、物体和时间约束的结构化规范——随后在 3D 游戏引擎中确定性地执行。分阶段的 LLM 细化流水线在这一任务上完全失败（50 次尝试中无一能产生可执行规范），这促使我们采用一种基于关注点分离的根本不同的架构：LLM 通过自然语言推理处理叙事规划，而程序化状态后端通过经过验证的工具调用强制执行所有模拟器约束，从而保证生成的每个规范在构建时即是可执行的。该系统采用分层双智能体架构——负责规划故事的“导演”（Director）和通过基于轮次的状态机构建单个场景的“场景构建器”（Scene Builder）——并配备专门的“关系子智能体”来填充 GEST 形式化中程序化生成留空的逻辑和语义边类型，使其成为首个发挥该表示法全部表达能力的方法。我们分两个阶段进行评估：首先是通过 3 模型 LLM 评审团将自主生成与程序化基准进行对比，代理式叙事在文本和视频对比中分别胜出 79% 和 74%；其次是种子生成，将相同文本输入我们的系统、VEO 3.1 和 WAN 2.2，人工标注显示引擎生成的视频在物理有效性（58% 对比 25% 和 20%）和语义一致性（3.75/5 对比 2.33 和 1.50）上显著优于神经生成器

## Abstract
Existing multi-agent video generation systems use LLM agents to orchestrate neural video generators, producing visually impressive but semantically unreliable outputs with no ground truth annotations. We present an agentic system that inverts this paradigm: instead of generating pixels, the LLM constructs a formal Graph of Events in Space and Time (GEST) -- a structured specification of actors, actions, objects, and temporal constraints -- which is then executed deterministically in a 3D game engine. A staged LLM refinement pipeline fails entirely at this task (0 of 50 attempts produce an executable specification), motivating a fundamentally different architecture based on a separation of concerns: the LLM handles narrative planning through natural language reasoning, while a programmatic state backend enforces all simulator constraints through validated tool calls, guaranteeing that every generated specification is executable by construction. The system uses a hierarchical two-agent architecture -- a Director that plans the story and a Scene Builder that constructs individual scenes through a round-based state machine -- with dedicated Relation Subagents that populate the logical and semantic edge types of the GEST formalism that procedural generation leaves empty, making this the first approach to exercise the full expressive capacity of the representation. We evaluate in two stages: autonomous generation against procedural baselines via a 3-model LLM jury, where agentic narratives win 79% of text and 74% of video comparisons; and seeded generation where the same text is given to our system, VEO 3.1, and WAN 2.2, with human annotations showing engine-generated videos substantially outperform neural generators on physical validity (58% vs 25% and 20%) and semantic alignment (3.75/5 vs 2.33 and 1.50).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“代理式视频生成”（Agentic Video Generation）的新范式，旨在解决当前神经视频生成模型（如 Sora、VEO 等）在语义一致性和物理逻辑方面的缺陷。

### 1. 核心问题与整体含义
*   **研究动机**：现有的多智能体视频生成系统主要驱动神经生成器产生像素，虽然视觉效果好，但经常出现物体凭空消失、角色变形、违反物理规律等语义不可靠问题，且无法提供准确的基准真相（Ground Truth）标注。
*   **核心思想**：论文提出“反转范式”，不直接生成像素，而是让 LLM 智能体构建一个正式的**时空事件图（GEST）**。这是一个结构化的规范，定义了角色、动作、物体及其时间约束。该规范随后在 3D 游戏引擎中进行确定性执行，从而保证视频的物理一致性和语义准确性。

### 2. 方法论
该系统采用分层双智能体架构，核心在于**关注点分离**：LLM 负责叙事逻辑，程序化后端负责约束强制执行。
*   **分层智能体架构**：
    *   **导演智能体（Director Agent）**：负责高层故事规划，包括探索模拟世界、挑选演员皮肤、划分场景序列。
    *   **场景构建器子智能体（Scene Builder Subagent）**：负责具体场景的构建，通过基于轮次的状态机管理动作链、交互和时间依赖。
    *   **关系子智能体（Relation Subagents）**：专门负责填充逻辑（因果、前提）和语义（动机、对比）关系边，使 GEST 达到完整的表达能力。
*   **关键技术细节**：
    *   **工具约束规划（Tool-Constrained Planning）**：智能体不直接修改图，而是通过经过验证的工具调用（如 `start_chain`, `add_temporal_dependency`）进行操作。
    *   **状态后端强制执行**：后端实时跟踪演员状态（姿态、手持物）、POI 容量、物体生命周期，并进行时间环路检测。任何违反模拟器规则的操作都会被拒绝，确保生成的 GEST **在构建时即是可执行的**。

### 3. 实验设计
实验分为两个阶段，旨在评估叙事质量和生成可靠性：
*   **阶段 1：自主生成（Autonomous Generation）**：智能体在没有提示词的情况下自主构思故事。对比对象是**程序化随机生成基准**。使用由 GPT-5.2 Pro、Gemini 3.1 Pro 和 Claude Opus 4.6 组成的 **3 模型 LLM 评审团**进行盲测。
*   **阶段 2：种子生成（Seeded Generation）**：给定相同的文本描述，对比本系统、**VEO 3.1** 和 **WAN 2.2**。
*   **评估指标**：物理有效性（二元判断）、语义对齐（1-5 分李克特量表）、叙事忠实度、演员选角准确性。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练算力，因为该系统主要基于现有的 LLM API 和 3D 引擎执行。
*   **成本开销**：文中提到，每个代理式故事的生成成本约为 **0.25 美元** 的前沿模型 API 调用费用。
*   **执行环境**：使用 GTA San Andreas 引擎通过 Multi Theft Auto (MTA) 插件进行确定性渲染。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 50 次分阶段 LLM 流水线尝试（全部失败）。
    *   自主生成阶段产生了 25 个视频进行评审。
    *   种子生成阶段使用了 25 个种子故事，并收集了 34 份人工标注。
*   **充分性评价**：实验设计较为客观，引入了多模型评审团和人工标注双重验证，并与当前最先进的神经视频生成模型（VEO, WAN）进行了直接对比。虽然样本量（25-50组）在生成任务中不算巨大，但对于验证“可执行性”和“语义对齐”这一核心论点已足够充分。

### 6 主要结论与发现
*   **执行成功率**：传统的“分阶段细化”LLM 流水线成功率为 0%，而本系统的种子生成成功率达到 **80%**。
*   **物理与语义优势**：人工标注显示，引擎生成的视频在**物理有效性**（58% vs 25%/20%）和**语义对齐**（3.75 vs 2.33/1.50）上大幅领先神经生成器。
*   **叙事价值**：LLM 评审团认为代理式生成的叙事在逻辑和动机上显著优于随机程序化生成（胜率达 79%）。
*   **零成本标注**：系统在生成视频的同时，自动产出完美的时空标注（RGB、空间关系图、事件-帧映射），这是神经模型无法做到的。

### 7. 优点
*   **架构创新**：通过工具约束解决了 LLM 在长序列任务中难以维持精确状态一致性的难题。
*   **确定性与可解释性**：生成的 GEST 是透明、可编辑且确定性执行的，彻底消除了“幻觉”导致的物体闪烁或逻辑错误。
*   **多模态闭环**：实现了从故事到规范、再到视频、最后回到自然语言描述的完整闭环。

### 8. 不足与局限
*   **视觉真实度（Fidelity Gap）**：受限于 3D 游戏引擎（GTA SA），视频的视觉质量（贴图、光影）远逊于神经生成模型。
*   **叙事信息损失**：由于模拟器动作词汇有限，约 40%-60% 的叙事细节（如微妙的情绪、复杂的动作融合）在执行过程中会丢失。
*   **稳定性问题**：完全自主生成模式下的成功率仅为 50% 左右，存在循环规划和上下文耗尽的问题。
*   **表现力限制**：目前的引擎缺乏电影化的镜头控制（变焦、跟拍）和动态环境变化（天气、光照）。

（完）
