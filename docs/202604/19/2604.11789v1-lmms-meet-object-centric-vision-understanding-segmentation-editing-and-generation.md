---
title: "LMMs Meet Object-Centric Vision: Understanding, Segmentation, Editing and Generation"
title_zh: LMMs 与以对象为中心的视觉：理解、分割、编辑与生成
authors: "Yuqian Yuan, Wenqiao Zhang, Juekai Lin, Yu Zhong, Mingjian Gao, Binhe Yu, Yunqi Cao, Wentong Li, Yueting Zhuang, Beng Chin Ooi"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11789v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于物体级3D场景生成和编辑的多模态大模型
tldr: 本文全面综述了大语言多模态模型（LMMs）与以对象为中心的视觉技术的融合。针对LMMs在精确对象定位、细粒度空间推理和可控视觉操作方面的局限，文章从理解、分割、编辑和生成四个维度梳理了最新进展，总结了建模范式与学习策略，为构建精确、可靠的以对象为中心的多模态系统提供了结构化视角。
selection_source: fresh_fetch
motivation: 现有LMMs在处理需要精确对象级定位、身份保持及细粒度空间推理的任务时表现不足，难以实现高精度的局部区域修改。
method: 文章将相关研究划分为对象级理解、指代分割、视觉编辑和视觉生成四大主题，并系统总结了其建模范式、学习策略及评估协议。
result: 建立了一个涵盖从全局场景理解到局部对象操作的完整框架，并指出了实现细粒度空间控制和一致性交互的关键技术路径。
conclusion: 以对象为中心的视觉是提升多模态系统精确性与可控性的关键，未来需重点攻克实例持久性、统一跨任务建模及可靠基准测试等挑战。
---

## 摘要
大多模态模型（LMMs）在通用视觉-语言理解方面取得了显著进展，但在需要精确的对象级定位（grounding）、细粒度空间推理和可控视觉操作的任务中仍存在局限。特别是，现有系统往往难以识别正确的实例，在交互过程中保持对象身份的一致性，以及高精度地定位或修改指定区域。以对象为中心的视觉通过促进对视觉实体的显式表示和操作，为解决这些挑战提供了一个原则性框架，从而将多模态系统从全局场景理解扩展到对象级的理解、分割、编辑和生成。本文对 LMMs 与以对象为中心视觉交汇领域的最新进展进行了全面综述。我们将相关文献归纳为四个主要主题：以对象为中心的视觉理解、以对象为中心的指代分割、以对象为中心的视觉编辑以及以对象为中心的视觉生成。我们进一步总结了支持这些能力的关键建模范式、学习策略和评估协议。最后，我们讨论了公开挑战和未来方向，包括鲁棒的实例持久性、细粒度空间控制、一致的多步交互、统一的跨任务建模以及分布偏移下的可靠基准测试。我们希望本文能为开发可扩展、精确且值得信赖的以对象为中心的多模态系统提供结构化的视角。

## Abstract
Large Multimodal Models (LMMs) have achieved remarkable progress in general-purpose vision--language understanding, yet they remain limited in tasks requiring precise object-level grounding, fine-grained spatial reasoning, and controllable visual manipulation. In particular, existing systems often struggle to identify the correct instance, preserve object identity across interactions, and localize or modify designated regions with high precision. Object-centric vision provides a principled framework for addressing these challenges by promoting explicit representations and operations over visual entities, thereby extending multimodal systems from global scene understanding to object-level understanding, segmentation, editing, and generation. This paper presents a comprehensive review of recent advances at the convergence of LMMs and object-centric vision. We organize the literature into four major themes: object-centric visual understanding, object-centric referring segmentation, object-centric visual editing, and object-centric visual generation. We further summarize the key modeling paradigms, learning strategies, and evaluation protocols that support these capabilities. Finally, we discuss open challenges and future directions, including robust instance permanence, fine-grained spatial control, consistent multi-step interaction, unified cross-task modeling, and reliable benchmarking under distribution shift. We hope this paper provides a structured perspective on the development of scalable, precise, and trustworthy object-centric multimodal systems.

---

## 论文详细总结（自动生成）

这篇论文《LMMs Meet Object-Centric Vision: Understanding, Segmentation, Editing and Generation》是一篇关于大多模态模型（LMMs）与以对象为中心（Object-Centric）视觉技术融合的**深度综述论文**。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管现有的 LMMs（如 GPT-4V、LLaVA）在全局场景描述上表现出色，但在**物体级智能**方面存在瓶颈。具体表现为：难以在多个相似实例中精确识别目标、空间推理能力弱、无法在多轮交互中保持物体身份一致性，以及缺乏对局部区域进行精确修改的能力。
*   **整体含义**：论文提出“以对象为中心的视觉”是弥合高层语言推理与底层像素执行之间鸿沟的关键。通过将视觉信号视为离散实体的集合（而非无差别的像素阵列），可以推动多模态系统从“被动观察者”向“主动代理”转变。

### 2. 方法论：核心思想与关键技术
论文将该领域的研究划分为四个核心维度，并总结了各自的技术范式：
*   **对象理解（Understanding）**：
    *   **视觉提示范式**：包括“文本作为提示”（坐标转文本）、“视觉提示作为 Token”（将点/框编码为向量）以及“视觉提示与特征融合”（如 RoI 特征注入）。
    *   **区域编码**：利用坐标、掩码（Mask）或多格式提示（点、线、框）来提取局部特征。
*   **指代分割（Segmentation）**：
    *   **语言-像素接口**：总结了四种主流架构：单嵌入预测（如 LISA）、多嵌入预测（如 PixelLM）、下一 Token 预测（将掩码序列化）和基于注意力的预测（从 Cross-Attention 提取掩码）。
*   **视觉编辑（Editing）**：
    *   **扩散模型编辑**：通过注意力控制（P2P）、特征注入（PnP）和指令微调（InstructPix2Pix）实现。
    *   **自回归编辑**：利用视觉自回归（VAR）进行多尺度或掩码预测。
*   **视觉生成（Generation）**：
    *   **可控生成**：基于布局（Layout）的扩散生成、身份保持的定制化生成。
    *   **维度扩展**：涵盖了从图像到视频（时空一致性）以及 3D（点云、NeRF、3D 高斯泼溅 3DGS、网格 Mesh）的生成技术。

### 3. 实验设计与 Benchmark
由于这是一篇综述论文，其“实验设计”体现为对现有文献中数据集和基准测试的系统梳理：
*   **数据集**：
    *   **理解类**：LVIS、Visual Genome、PACO、Osprey-724K、PixelRefer-2.2M。
    *   **分割类**：RefCOCO 系列、ReasonSeg（推理分割）、ReasonVOS（视频推理分割）。
    *   **编辑/生成类**：MagicBrush、DragBench（拖拽编辑）、LAION-5B、Objaverse（3D 对象库）。
*   **对比方法**：文中横向对比了从早期的模块化方法（如基于检测器的）到现代端到端 LMM 架构（如 InternVL、Qwen-VL、SAM 2 结合型模型）的演进。

### 4. 资源与算力
*   **论文本身**：作为综述，文中未提及撰写此文所消耗的训练算力。
*   **所引用的模型**：文中提到了当前顶尖模型（如 InternVL、Sora、Wan-2.1）通常依赖数千个 GPU（如 H100/A100）进行大规模预训练，并强调了参数规模从亿级向百亿级（10B+）扩展的趋势。

### 5. 实验数量与充分性
*   **文献覆盖**：论文引用了 **250 篇以上** 的核心参考文献，涵盖了 2023 年至 2025 年间该领域的最新突破。
*   **维度充分性**：实验总结不仅限于 2D 图像，还深入探讨了视频中的时空一致性和 3D 场景中的几何一致性，涵盖了理解、分割、编辑、生成四个闭环任务，逻辑非常严密且客观。

### 6. 主要结论与发现
*   **范式统一**：研究正趋向于“理解即分割”，即通过统一的序列生成接口处理所有视觉任务。
*   **时空挑战**：在动态视频和 3D 环境中保持“物体持久性”（Instance Permanence）仍是当前最大的技术挑战。
*   **具身智能**：以对象为中心的 LMM 是通往具身智能（Embodied AI）的必经之路，模型需要理解物体的“功能属性”（Affordance），即不仅知道是什么，还知道如何交互。

### 7. 亮点
*   **分类清晰**：首次系统性地将 LMM 与对象中心视觉结合的任务划分为四个清晰的维度，并提供了详尽的技术路线图。
*   **前瞻性强**：对 3D 生成（如 3DGS）和视频生成（如 Sora 架构）的讨论紧跟前沿，并提出了“空间思维链”（Chain-of-Spatial-Reasoning）等新颖概念。

### 8. 不足与局限
*   **分布偏移风险**：综述指出，现有模型在处理超出训练分布（OOD）的物体或罕见类别时，定位精度会大幅下降。
*   **算力壁垒**：高性能的以对象为中心的 LMM 训练成本极高，开源社区在复现闭源模型（如 GPT-4V）的细粒度控制能力方面仍有差距。
*   **评估标准不一**：不同任务（如编辑与生成）之间的评估指标（如 FID vs. 人类偏好）存在脱节，缺乏统一的端到端可靠基准。

（完）
