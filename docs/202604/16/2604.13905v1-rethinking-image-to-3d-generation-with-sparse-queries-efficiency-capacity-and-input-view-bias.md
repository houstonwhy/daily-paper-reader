---
title: "Rethinking Image-to-3D Generation with Sparse Queries: Efficiency, Capacity, and Input-View Bias"
title_zh: 重新审视基于稀疏查询的图像到 3D 生成：效率、容量与输入视角偏差
authors: "Zhiyuan Xu, Jiuming Liu, Yuxin Chen, Masayoshi Tomizuka, Chenfeng Xu, Chensheng Peng"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13905v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 高效的图像到3D场景生成框架
tldr: 本文提出SparseGen框架，旨在解决图像转3D生成中计算开销大和输入视角偏差的问题。该方法摒弃了传统的密集表示，采用稀疏的3D锚点查询和扩展算子，将场景解码为局部3D高斯原语。通过整流流重建目标进行训练，SparseGen在显著降低内存和推理时间的同时，保持了高保真度的多视角一致性，并有效缓解了对输入视角的过拟合，为高效3D生成提供了新思路。
selection_source: fresh_fetch
motivation: 传统的图像转3D生成方法依赖密集体积网格或三平面表示，导致计算效率低下且容易产生严重的输入视角偏差。
method: 通过一组稀疏的3D锚点查询和学习到的扩展算子，将场景建模为可解码成局部3D高斯原语的紧凑表示。
result: 该模型在大幅减少内存占用和推理时间的同时，实现了高保真度的多视角生成，并有效降低了对输入视角的过拟合。
conclusion: 稀疏集合潜空间扩展是实现高效、低偏差3D生成建模的一种实用且具有原则性的替代方案。
---

## 摘要
我们提出了 SparseGen，这是一个用于高效图像到 3D 生成的新型框架，它在显著提升速度的同时表现出较低的输入视角偏差。与依赖密集体积网格、三平面（triplanes）或像素对齐原语的传统方法不同，我们使用一组紧凑的、可学习的 3D 锚点查询以及一个可学习的扩展算子来建模场景，该算子将每个变换后的查询解码为一小组局部 3D 高斯原语（3D Gaussian primitives）。在没有 3D 监督的情况下，通过修正流（rectified-flow）重建目标进行训练，我们的模型学会了在几何和外观关键处分配表示容量，在保持多视角保真度的同时，显著减少了内存占用和推理时间。我们引入了输入视角偏差和利用率的定量度量，证明了稀疏查询在保持表示效率的同时，减少了对条件视角的过拟合。我们的结果表明，稀疏集合潜变量扩展（sparse set-latent expansion）是高效 3D 生成建模的一种有原则且实用的替代方案。

## Abstract
We present SparseGen, a novel framework for efficient image-to-3D generation, which exhibits low input-view bias while being significantly faster. Unlike traditional approaches that rely on dense volumetric grids, triplanes, or pixel-aligned primitives, we model scenes with a compact sparse set of learned 3D anchor queries and a learned expansion operator that decodes each transformed query into a small local set of 3D Gaussian primitives. Trained under a rectified-flow reconstruction objective without 3D supervision, our model learns to allocate representation capacity where geometry and appearance matter, achieving significant reductions in memory and inference time while preserving multi-view fidelity. We introduce quantitative measures of input-view bias and utilization to show that sparse queries reduce overfitting to conditioning views while being representationally efficient. Our results argue that sparse set-latent expansion is a principled, practical alternative for efficient 3D generative modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking Image-to-3D Generation with Sparse Queries: Efficiency, Capacity, and Input-View Bias》的结构化总结：

### 1. 论文的核心问题与整体含义
该论文针对从单张或少量图像生成 3D 内容的任务，探讨了现有两类主流范式的缺陷：
*   **迭代扩散生成（如 Viewset Diffusion）：** 虽然多视角一致性好，但需要多次去噪步骤，推理速度极慢。
*   **确定性前馈重建（如 LRM, Splatter Image）：** 推理速度快，但存在严重的**“输入视角偏差”**（Input-View Bias），即在靠近输入视角的地方效果好，但在未观察到的新视角下质量大幅下降，且往往依赖密集的 3D 表示（如百万级体素或数万个高斯点），内存开销大。
**核心动机：** 提出一种既能保持生成式模型的多视角一致性，又能实现极速前馈推理，且具备高表示效率、低视角偏差的 3D 生成框架。

### 2. 论文提出的方法论：SparseGen
SparseGen 的核心思想是利用**稀疏查询（Sparse Queries）**来建模 3D 场景，并通过**整流流（Rectified Flow）**实现单步生成。
*   **核心架构：**
    1.  **图像特征提取：** 使用类 DINOv2 的编码器提取图像特征，并加入时间步（timestep）嵌入以支持生成任务。
    2.  **3D 位置感知编码：** 将像素反投影到 3D 空间形成视锥点云，通过卷积网络编码为 3D 位置嵌入，并与图像特征融合。
    3.  **查询-高斯扩展网络：** 维护一组可学习的 3D 锚点查询（Anchor Queries）。这些查询通过 Transformer 解码器与图像特征进行交叉注意力交互，随后由 MLP 头部解码为一组局部 3D 高斯原语（每个查询扩展为 $K$ 个高斯点）。
    4.  **整流流训练：** 采用 Rectified Flow 框架，将高斯噪声映射到 3D 高斯参数空间，支持单步前馈推理。
*   **损失函数：** 仅使用 2D 监督，包括 L2 重建损失、感知损失（LPIPS）、不透明度损失以及针对高斯参数的正则化项（如均值偏移约束）。

### 3. 实验设计
*   **数据集：**
    *   **ShapeNet-SRN (Cars)：** 主要用于定量评估单视角和双视角重建。
    *   **CO3D (Hydrant, Teddybear)：** 用于评估在真实世界类别上的泛化能力。
    *   **Objaverse & GSO：** 用于测试在大规模数据上的泛化性能。
*   **对比方法：**
    *   **生成式基准：** Viewset Diffusion。
    *   **前馈重建基准：** OpenLRM, Splatter Image, pixelNeRF。
*   **评估指标：** PSNR, SSIM, LPIPS, FID（衡量真实感），以及推理时间、表示大小（KB）和新引入的“输入视角偏差”度量。

### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA L40 GPU** 上进行。
*   **训练时长：** SparseGen 在 L40 上训练约需 **3 天**（与 Viewset Diffusion 相当，远低于 LRM 的 300 个 A100 天）。
*   **推理效率：** 生成 3D 表示仅需 **0.027 秒**，比迭代扩散方法快约 **600 倍**。

### 5. 实验数量与充分性
论文进行了较为充分的实验验证：
*   **定量对比：** 在多个数据集上对比了质量、速度和存储。
*   **消融实验：** 验证了整流流、3D 位置嵌入、可学习查询对结果的贡献。
*   **偏差分析：** 专门设计实验量化了输入视角与新视角之间的性能差距，证明了稀疏查询能有效缓解过拟合。
*   **可扩展性分析：** 研究了查询数量（M）对性能的影响。
*   **可视化分析：** 展示了查询的利用率和空间局部性。
实验设计客观，通过引入“输入视角偏差”这一新维度，公平地揭示了确定性模型在非观察视角下的局限性。

### 6. 论文的主要结论与发现
*   **稀疏性即效率：** 仅需 512 个查询（扩展为约 5000 个高斯点）即可达到甚至超过密集表示的效果，表示文件仅 **280KB**。
*   **缓解视角偏差：** 相比确定性回归模型，基于生成式框架的 SparseGen 在新视角下的性能下降更小，生成的未观察区域更具合理性。
*   **高利用率：** 学习到的锚点查询会自动聚焦在物体几何表面，避免了密集体积网格中大量空余空间的计算浪费。
*   **单步生成的优势：** 结合整流流，模型在保持生成能力的同时，消除了扩散模型多步迭代的延迟。

### 7. 优点
*   **极高的推理效率：** 0.027s 的速度使其非常适合实时应用（如 AR/VR、机器人仿真）。
*   **紧凑的表示：** 极小的内存占用（<300KB）有利于移动端部署。
*   **理论贡献：** 深入探讨了“输入视角偏差”问题，并证明了稀疏查询是一种有效的正则化手段。
*   **灵活性：** 天然支持变数量的输入视角（单视角、双视角等），无需重新训练。

### 8. 不足与局限
*   **分辨率限制：** 目前实验主要在 128x128 分辨率下进行，对于极高精度的纹理细节生成可能仍有提升空间。
*   **大规模泛化：** 虽然在 GSO 上做了初步测试，但在处理极其复杂的野外（in-the-wild）场景时，其鲁棒性仍需更大规模的数据验证。
*   **类别限制：** 实验多集中在特定类别（车、水龙头等），全类别通用 3D 生成的能力尚待进一步证明。

（完）
