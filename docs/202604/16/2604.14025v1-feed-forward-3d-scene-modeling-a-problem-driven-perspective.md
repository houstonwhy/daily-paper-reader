---
title: "Feed-Forward 3D Scene Modeling: A Problem-Driven Perspective"
title_zh: 前馈三维场景建模：一种问题驱动的视角
authors: "Weijie Wang, Qihang Cao, Sensen Gao, Donny Y. Chen, Haofei Xu, Wenjing Bian, Songyou Peng, Tat-Jen Cham, Chuanxia Zheng, Andreas Geiger, Jianfei Cai, Jia-Wang Bian, Bohan Zhuang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14025v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 前馈3D场景建模与重建综述
tldr: 本综述系统回顾了可泛化的前馈3D场景建模技术，该技术通过单次前向传播将2D图像直接映射为3D表示，克服了传统方法优化慢、泛化性差的局限。文章提出了一种基于模型设计策略的新型分类法，涵盖特征增强、几何感知、模型效率、增强策略和时序感知五个核心维度，并全面梳理了相关基准、数据集及应用场景，为该领域的未来研究提供了重要参考。
selection_source: fresh_fetch
motivation: 传统3D重建方法受限于逐场景优化的低效性和特定类别的局限性，促使研究者开发高效且具备跨场景泛化能力的前馈模型。
method: 提出一种以问题为驱动、不依赖于输出表示格式的新型分类法，从特征、几何、效率、增强和时序五个关键设计维度组织研究方向。
result: 全面总结了前馈3D建模的通用架构模式，并对现有的基准测试、数据集以及在自动驾驶、机器人等现实世界中的应用进行了系统分类。
conclusion: 文章指出了前馈3D建模在可扩展性、评估标准和世界建模等方面的开放挑战，并为未来研究指明了方向。
---

## 摘要
从二维输入重建三维表示是计算机视觉和图形学中的一项基础任务，是理解物理世界并与之交互的基石。虽然传统方法能够实现高保真度，但受限于缓慢的逐场景优化或特定类别的训练，这阻碍了它们的实际部署和可扩展性。因此，可泛化的前馈三维重建在近年来得到了快速发展。通过学习一个在单次前向传递中将图像直接映射到三维表示的模型，这些方法实现了高效的重建和鲁棒的跨场景泛化。本综述的出发点源于一个关键观察：尽管几何输出表示形式多样（从隐式场到显式原语），现有的前馈方法在高级架构模式上具有相似性，例如图像特征提取骨干网络、多视图信息融合机制以及几何感知设计原则。因此，我们抛开这些表示形式的差异，转而关注模型设计，提出了一种以模型设计策略为中心的新型分类法，该分类法与输出格式无关。我们提出的分类法将研究方向组织为驱动近期研究发展的五个关键问题：特征增强、几何感知、模型效率、增强策略和时间感知模型。为了给该分类法提供实证基础和标准化评估支持，我们还全面回顾了相关的基准测试和数据集，并广泛讨论和分类了基于前馈三维模型的实际应用。最后，我们概述了未来的研究方向，以应对可扩展性、评估标准和世界建模等开放性挑战。

## Abstract
Reconstructing 3D representations from 2D inputs is a fundamental task in computer vision and graphics, serving as a cornerstone for understanding and interacting with the physical world. While traditional methods achieve high fidelity, they are limited by slow per-scene optimization or category-specific training, which hinders their practical deployment and scalability. Hence, generalizable feed-forward 3D reconstruction has witnessed rapid development in recent years. By learning a model that maps images directly to 3D representations in a single forward pass, these methods enable efficient reconstruction and robust cross-scene generalization. Our survey is motivated by a critical observation: despite the diverse geometric output representations, ranging from implicit fields to explicit primitives, existing feed-forward approaches share similar high-level architectural patterns, such as image feature extraction backbones, multi-view information fusion mechanisms, and geometry-aware design principles. Consequently, we abstract away from these representation differences and instead focus on model design, proposing a novel taxonomy centered on model design strategies that are agnostic to the output format. Our proposed taxonomy organizes the research directions into five key problems that drive recent research development: feature enhancement, geometry awareness, model efficiency, augmentation strategies and temporal-aware models. To support this taxonomy with empirical grounding and standardized evaluation, we further comprehensively review related benchmarks and datasets, and extensively discuss and categorize real-world applications based on feed-forward 3D models. Finally, we outline future directions to address open challenges such as scalability, evaluation standards, and world modeling.

---

## 论文详细总结（自动生成）

这篇论文《Feed-Forward 3D Scene Modeling: A Problem-Driven Perspective》是一篇关于**前馈三维场景建模**的深度综述。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的三维重建方法（如 SfM、MVS、NeRF、3DGS）通常依赖于**逐场景优化（Per-scene Optimization）**，计算量大、耗时长，且难以泛化到新场景。
*   **研究动机**：为了实现高效、实时的三维重建，研究界转向了**前馈（Feed-forward）模型**。这类模型通过在大规模数据集上预训练，能够在单次前向传播中直接从图像生成三维表示。
*   **整体含义**：本文摒弃了以往按“输出表示形式”（如网格、点云）分类的传统逻辑，提出了一种**以问题为驱动（Problem-Driven）**的新型分类法，旨在揭示不同方法在解决特征增强、几何感知、效率、增强策略和时序建模等核心挑战时的共同设计模式。

### 2. 论文提出的方法论：核心思想与关键技术
论文将前馈三维建模抽象为一个通用的**编码器-解码器-渲染器**框架，并从五个维度组织研究方向：
*   **通用公式**：
    1.  **编码器 ($\Phi$)**：将输入图像 $I$ 编码为隐式特征图 $F$。
    2.  **解码器 ($\Psi$)**：将特征解码为三维表示 $G$（如 3D 高斯原语、点图等）。
    3.  **渲染器 ($R$)**：可选模块，用于将 $G$ 渲染为新视角图像以进行监督。
*   **五大研究方向**：
    1.  **特征增强**：利用 Transformer、Mamba 等先进架构，或融合视觉基础模型（如 DINOv2、CroCo）的先验知识。
    2.  **几何感知**：通过代价向量（Cost Volumes）、显式几何聚合、无位姿重建（Pose-free）以及预训练几何引导来解决深度歧义。
    3.  **模型效率**：通过特征压缩、表示精简（如减少高斯数量）来降低内存和计算开销。
    4.  **增强策略**：利用合成数据增强或扩散模型（Diffusion Models）进行视觉增强，提升渲染质量。
    5.  **时序感知模型**：针对动态场景，分为在线流式处理、离线批处理和交互式建模。

### 3. 实验设计：数据集、基准与对比方法
*   **数据集分类**：
    *   **几何导向（Geometry-Oriented）**：如 DTU、ScanNet、ETH3D、Replica，提供精确的 3D 真值（点云/深度）。
    *   **视觉导向（Visual-Oriented）**：如 RealEstate10K、DL3DV、NeRF-Synthetic，侧重于高保真新视角合成。
*   **基准测试（Benchmarks）**：
    *   **DTU 3-view NVS**：评估少样本视角合成。
    *   **RealEstate10K 2-view NVS**：评估大规模室内外场景的泛化能力。
    *   **点图估计（Pointmap Estimation）**：在 7-Scenes、NRGBD 等数据集上评估几何精度。
*   **对比方法**：涵盖了从早期的 PixelNeRF、IBRNet 到近期的 DUSt3R、MVSplat、LRM、iLRM 等数十种前沿模型。

### 4. 资源与算力
*   **算力说明**：作为综述论文，本文未提供单一的训练算力数据。但文中专门讨论了**模型效率（§4.3）**，并对比了不同方法（如 MVSplat, DepthSplat, Long-LRM）在推理时的内存占用、高斯数量和推理时间（见图 5）。
*   **趋势**：文中指出，当前趋势是利用高性能 GPU（如 A100/H100）进行大规模预训练，以换取测试时的极速推理（通常在秒级甚至毫秒级）。

### 5. 实验数量与充分性
*   **实验规模**：论文引用了 **395 篇** 参考文献，涵盖了过去几年该领域几乎所有的重要进展。
*   **充分性**：论文通过三个核心表格（Table 2, 3, 4）汇总了不同方法在标准基准上的性能。
*   **客观性**：作者不仅对比了精度（PSNR/SSIM），还客观地分析了不同表示形式（NeRF vs 3DGS vs Pointmap）在效率和质量上的权衡，实验总结非常全面且具有代表性。

### 6. 论文的主要结论与发现
*   **范式转移**：三维重建正从“逐场景优化”转向“大规模预训练+前馈推理”。
*   **表示融合**：3D Gaussian Splatting (3DGS) 和点图（Pointmap）正逐渐取代传统的 NeRF 成为前馈模型的主流输出表示。
*   **基础模型的影响**：引入 2D 视觉基础模型（VFMs）的特征能显著提升模型的泛化能力和几何鲁棒性。
*   **无位姿重建**：能够同时估计相机位姿和几何的“无位姿”前馈模型（如 DUSt3R）是当前的重要突破点。

### 7. 优点：亮点总结
*   **视角独特**：提出的“问题驱动”分类法非常深刻，能够帮助研究者跳出具体的表示格式，理解模型设计的本质挑战。
*   **覆盖全面**：不仅讨论了静态重建，还深入探讨了动态 4D 重建、自动驾驶、机器人操作等实际应用场景。
*   **前瞻性强**：对未来方向（如世界模型、统一感知与重建）的讨论具有很高的启发价值。

### 8. 不足与局限
*   **评估标准不一**：文中指出，目前许多基准测试缺乏真实的 3D 真值，过度依赖 2D 渲染指标（如 PSNR），这可能掩盖了几何上的缺陷。
*   **可扩展性挑战**：虽然前馈模型在小场景表现优异，但在超大规模（城市级）场景中，如何保持全局一致性仍是一个难题。
*   **硬件依赖**：高性能前馈模型对显存要求极高，在移动端或边缘设备上的部署仍受限。

（完）
