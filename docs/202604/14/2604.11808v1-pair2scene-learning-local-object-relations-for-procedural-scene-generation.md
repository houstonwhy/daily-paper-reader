---
title: "Pair2Scene: Learning Local Object Relations for Procedural Scene Generation"
title_zh: Pair2Scene：学习局部物体关系以实现程序化场景生成
authors: "Xingjian Ran, Shujie Zhang, Weipeng Zhong, Li Luo, Bo Dai"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11808v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 程序化3D室内场景生成框架
tldr: 针对3D室内场景生成中数据稀缺和空间关系建模难的问题，本文提出Pair2Scene框架。该方法基于物体放置主要依赖局部关系的观察，通过学习支撑和功能两种局部关系规则，结合场景层级结构和物理算法进行程序化生成。通过在3D-Pairs数据集上训练并结合碰撞感知采样，该框架能生成超越训练分布的高保真、复杂且符合物理逻辑的场景。
selection_source: fresh_fetch
motivation: 现有场景生成方法在处理复杂布局时泛化性差且缺乏精确空间推理能力，而物体放置本质上更多依赖局部依赖而非全局分布。
method: 通过神经网络学习物体间的支撑与功能关系，并结合场景层级结构与碰撞感知拒绝采样进行递归式的程序化生成。
result: 实验证明该框架在生成复杂环境方面优于现有方法，能够产生超越训练数据分布且具备物理与语义合理性的场景。
conclusion: Pair2Scene通过建模局部物体关系，为实现高保真、可扩展且符合物理逻辑的3D场景生成提供了一种有效的程序化方案。
---

## 摘要
生成高保真 3D 室内场景由于数据稀缺以及建模复杂空间关系的难度，仍然是一个重大挑战。当前的方法通常难以扩展到训练分布之外的密集场景，或者依赖于缺乏精确空间推理能力的大语言模型（LLM）或视觉语言模型（VLM）。基于物体放置主要依赖于局部依赖关系而非信息冗余的全局分布这一观察，本文提出了 Pair2Scene，这是一种新型的程序化生成框架，它将学习到的局部规则与场景层级结构及基于物理的算法相结合。这些规则主要捕捉两类物体间的关系，即遵循物理层级的支撑关系和反映语义联系的功能关系。我们通过一个网络对这些规则进行建模，该网络以锚点物体的位置和几何形状为条件，估计依赖物体的空间位置分布。据此，我们从现有的场景数据中整理了一个名为 3D-Pairs 的数据集来训练该模型。在推理过程中，我们的框架通过在层级结构中递归应用该模型来生成场景，并利用碰撞感知的拒绝采样将局部规则对齐为连贯的全局布局。大量实验表明，我们的框架在生成超出训练数据的复杂环境方面优于现有方法，同时保持了物理和语义上的合理性。

## Abstract
Generating high-fidelity 3D indoor scenes remains a significant challenge due to data scarcity and the complexity of modeling intricate spatial relations. Current methods often struggle to scale beyond training distribution to dense scenes or rely on LLMs/VLMs that lack the ability for precise spatial reasoning. Building on top of the observation that object placement relies mainly on local dependencies instead of information-redundant global distributions, in this paper, we propose Pair2Scene, a novel procedural generation framework that integrates learned local rules with scene hierarchies and physics-based algorithms. These rules mainly capture two types of inter-object relations, namely support relations that follow physical hierarchies, and functional relations that reflect semantic links. We model these rules through a network, which estimates spatial position distributions of dependent objects conditioned on position and geometry of the anchor ones. Accordingly, we curate a dataset 3D-Pairs from existing scene data to train the model. During inference, our framework can generate scenes by recursively applying our model within a hierarchical structure, leveraging collision-aware rejection sampling to align local rules into coherent global layouts. Extensive experiments demonstrate that our framework outperforms existing methods in generating complex environments that go beyond training data while maintaining physical and semantic plausibility.

---

## 论文详细总结（自动生成）

这是一份关于论文《Pair2Scene: Learning Local Object Relations for Procedural Scene Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
生成高保真、复杂的3D室内场景面临两大挑战：**数据稀缺**和**全局空间关系建模的复杂性**。
*   **研究动机**：现有的端到端学习方法（如基于Transformer或扩散模型）受限于训练数据的分布，难以生成比训练集更复杂的场景；而基于大语言模型（LLM）的方法虽然具备常识，但缺乏精确的空间推理能力，容易产生物体漂浮或碰撞。
*   **核心观察**：作者认为物体在场景中的放置主要取决于其**局部邻居**（局部依赖），而非冗余的全局分布。通过建模局部规则，可以有效缓解数据不足，并实现从局部到全局的程序化扩展。

### 2. 方法论
Pair2Scene 提出了一种将学习到的局部规则与程序化生成相结合的框架：
*   **核心思想**：将场景分解为一系列局部物体关系元组。定义了两类核心关系：
    1.  **支撑关系 ($R_s$)**：基于物理重力的层级（如桌子支撑电脑）。
    2.  **功能关系 ($R_f$)**：基于语义和功能的邻近性（如键盘相对于电脑的放置）。
*   **关键技术细节**：
    *   **几何感知布局预测器**：使用 Point-MAE 提取物体的点云几何特征，通过 Transformer 块捕捉依赖物体与锚点物体间的空间关系。
    *   **概率建模**：使用**逻辑混合模型 (Mixture of Logistics, MoL)** 来表示物体位置的多峰分布，以捕捉放置位置的多样性。
    *   **程序化组装**：构建“支撑树”和“功能树”来定义生成顺序。
    *   **碰撞感知拒绝采样**：在推理时，通过拒绝采样排除物理冲突的候选位置，并辅以重力模拟进行微调，确保全局一致性。

### 3. 实验设计
*   **数据集**：构建了 **3D-Pairs 数据集**（约 140,000 个关系元组），来源包括 3D-Front（家具布局）、MesaTask（桌面排列）和 InternScenes（真实场景转换数据）。
*   **Benchmark 与对比方法**：
    *   **学习类基准**：ATISS, DiffuScene, LayoutVLM, FactoredScenes。
    *   **程序化/LLM类基准**：Infinigen-Indoors, Holodeck。
*   **评估设置**：
    1.  **3D-Front 拟合与外推**：验证模型在受限分布下的表现及超越训练复杂度的能力。
    2.  **多源复杂场景生成**：验证在极高密度环境下的生成质量。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 (40G) GPU**。
*   **训练细节**：模型训练了 120 个 epoch，使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，总 Batch Size 为 32。
*   **推理效率**：生成一个复杂场景约需 49-69 秒，比 LLM 类方法（如 Holodeck 需 210 秒）更高效。

### 5. 实验数量与充分性
*   **实验规模**：进行了定量评估（FID, KID）、定性视觉对比以及包含 22 名参与者的用户研究。
*   **消融实验**：针对几何预训练、局部关系建模、拒绝采样、重力模拟等关键组件设计了 5 组消融实验。
*   **充分性评价**：实验设计较为全面，不仅对比了生成质量，还通过“Ours-Beyond”变体展示了模型生成比训练集更密集场景的能力。引入物理碰撞率和漂浮率的量化分析，增强了实验的客观性。

### 6. 主要结论与发现
*   **局部建模优于全局建模**：通过学习局部规则，Pair2Scene 在 FID 和 KID 指标上均优于直接预测全局布局的方法。
*   **强大的外推能力**：模型能够生成物体数量远超训练平均水平（如从 4 个增加到 14 个）且物理合理的复杂场景。
*   **物理与语义双重合理性**：结合几何特征和拒绝采样，显著降低了物体的碰撞率和漂浮率，同时保持了符合人类常识的语义排列。

### 7. 优点（亮点）
*   **解耦设计**：将复杂的全局布局问题简化为可学习的局部关系，有效解决了 3D 场景数据稀缺的问题。
*   **几何感知**：不同于仅依赖类别标签的方法，该模型能感知物体的具体形状（点云），从而实现更精准的放置（如非平面的支撑面）。
*   **程序化扩展性**：通过递归应用局部规则，理论上可以生成无限复杂的场景，具有极高的灵活性。

### 8. 不足与局限
*   **风格一致性**：目前主要关注空间位置，在物体检索时无法保证整套家具的视觉风格（如材质、色调）完全统一。
*   **复杂约束处理**：对于受多个锚点共同约束的物体（如被两把椅子夹在中间的茶几）或特殊悬挂关系（如吊灯）的处理能力尚待加强。
*   **推理开销**：虽然比 LLM 快，但由于涉及多次点云编码和拒绝采样，实时性仍有提升空间。

（完）
