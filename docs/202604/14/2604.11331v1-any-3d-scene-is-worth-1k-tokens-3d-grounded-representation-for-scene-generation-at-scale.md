---
title: "Any 3D Scene is Worth 1K Tokens: 3D-Grounded Representation for Scene Generation at Scale"
title_zh: 任何 3D 场景都值 1K 个 Token：面向大规模场景生成的 3D 锚定表示
authors: "Dongxu Wei, Qi Xu, Zhiqi Li, Hangning Zhou, Cong Qiu, Hailong Qin, Mu Yang, Zhaopeng Cui, Peidong Liu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11331v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 用于大规模场景生成的3D基础表示
tldr: 该研究针对现有3D场景生成依赖2D扩散模型导致的冗余和空间不一致问题，提出了一种直接在隐式3D空间进行生成的新框架。通过3D表示自动编码器（3DRAE）将多视图2D语义映射为固定复杂度的3D隐空间表示，并利用3D扩散Transformer（3DDiT）实现高效且空间一致的场景生成。该方法支持任意相机轨迹的解码，无需重复采样，显著提升了大规模3D场景生成的效率和质量。
selection_source: fresh_fetch
motivation: 现有的基于2D多视图或视频扩散的3D生成方法存在严重的表示冗余，且难以保证生成的3D空间一致性。
method: 提出3DRAE将多视图2D特征解耦并压缩为3D隐式表示，并结合3DDiT在3D隐空间内进行高效的扩散建模。
result: 实现了固定复杂度、高语义且空间一致的3D场景生成，并支持在任意相机轨迹下直接解码出图像和点图。
conclusion: 该研究证明了直接在3D隐空间进行大规模场景生成的有效性，为实现高效、一致的3D视觉内容创作提供了新路径。
---

## 摘要
长期以来，3D 场景生成一直由 2D 多视图或视频扩散模型主导。这不仅是因为缺乏场景级的 3D 潜空间表示，还因为大多数场景级 3D 视觉数据以多视图图像或视频的形式存在，这些形式天然地与 2D 扩散架构兼容。通常，这些基于 2D 的方法将 3D 空间外推降级为 2D 时间扩展，这引入了两个根本性问题：(i) 通过 2D 视图表示 3D 场景会导致显著的表示冗余；(ii) 植根于 2D 的潜空间本质上限制了生成的 3D 场景的空间一致性。在本文中，我们首次提出直接在隐式 3D 潜空间内进行 3D 场景生成，以解决这些局限性。首先，我们重新利用冻结的 2D 表示编码器来构建 3D 表示自编码器 (3DRAE)，它将视图耦合的 2D 语义表示锚定到视图解耦的 3D 潜空间表示中。这使得能够以固定的复杂度和丰富的语义，表示从任意数量视图（具有任何分辨率和宽高比）观察到的 3D 场景。随后，我们引入了 3D 扩散 Transformer (3DDiT)，它在该 3D 潜空间中进行扩散建模，在支持多种条件配置的同时，实现了极其高效且空间一致的 3D 场景生成。此外，由于我们的方法直接生成 3D 场景表示，因此可以将其解码为沿任意相机轨迹的图像和可选的点图，而无需像基于 2D 的方法那样进行逐轨迹的扩散采样过程。

## Abstract
3D scene generation has long been dominated by 2D multi-view or video diffusion models. This is due not only to the lack of scene-level 3D latent representation, but also to the fact that most scene-level 3D visual data exists in the form of multi-view images or videos, which are naturally compatible with 2D diffusion architectures. Typically, these 2D-based approaches degrade 3D spatial extrapolation to 2D temporal extension, which introduces two fundamental issues: (i) representing 3D scenes via 2D views leads to significant representation redundancy, and (ii) latent space rooted in 2D inherently limits the spatial consistency of the generated 3D scenes. In this paper, we propose, for the first time, to perform 3D scene generation directly within an implicit 3D latent space to address these limitations. First, we repurpose frozen 2D representation encoders to construct our 3D Representation Autoencoder (3DRAE), which grounds view-coupled 2D semantic representations into a view-decoupled 3D latent representation. This enables representing 3D scenes observed from arbitrary numbers of views--at any resolution and aspect ratio--with fixed complexity and rich semantics. Then we introduce 3D Diffusion Transformer (3DDiT), which performs diffusion modeling in this 3D latent space, achieving remarkably efficient and spatially consistent 3D scene generation while supporting diverse conditioning configurations. Moreover, since our approach directly generates a 3D scene representation, it can be decoded to images and optional point maps along arbitrary camera trajectories without requiring per-trajectory diffusion sampling pass, which is common in 2D-based approaches.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **3DRAE** 和 **3DDiT** 的新框架，旨在解决大规模 3D 场景生成中的效率与一致性难题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：目前的 3D 场景生成主要依赖 2D 多视图或视频扩散模型。这种方式存在两个根本缺陷：
    1.  **表示冗余**：将 3D 场景视为 2D 视图集合会导致大量重叠信息的重复计算（例如，32 张图可能需要 8192 个 Token，但实际独立信息仅需 1024 个）。
    2.  **空间不一致性**：2D 扩散模型缺乏内在的 3D 空间建模能力，通过时间扩展生成的场景容易出现结构漂移和不连贯。
*   **整体含义**：论文探讨了是否可以直接在**隐式 3D 潜空间**中进行生成，从而实现高效且空间一致的 3D 场景合成。

### 2. 方法论
该框架由两个核心组件组成：
*   **3DRAE (3D Representation Autoencoder)**：
    *   **核心思想**：利用预训练且冻结的 2D 编码器（如 DINOv2）提取多视图特征，通过一个“潜空间融合颈部”（Latent Fuse Neck）将任意数量、分辨率和宽高比的视图压缩为**固定长度（如 1024 个）的 3D 潜 Token**。
    *   **解码机制**：使用“潜空间查询解码器”，通过输入目标相机的射线图（Ray Map）作为 Query，从 3D 潜 Token 中检索信息并解码出对应的图像和点图（Point Map）。
*   **3DDiT (3D Diffusion Transformer)**：
    *   **核心思想**：在 3DRAE 学习到的紧凑 3D 潜空间中进行扩散建模。
    *   **算法流程**：采用流匹配（Flow Matching）框架。模型可以根据不同的条件配置（无条件、单视图、稀疏视图）进行去噪，生成完整的 3D 场景表示。
    *   **优势**：生成的是 3D 表示本身，因此可以沿任意相机轨迹直接解码图像，无需像 2D 方法那样进行逐帧的扩散采样。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：整合了 17 个多视图数据集（如 DL3DV, RE10K, Waymo, ScanNet 等），涵盖约 13 万个场景，并利用 ImageNet（128 万张图）进行预训练。
    *   **测试 Benchmark**：从 DL3DV、RE10K、CO3D 和 ScanNet 中提取了 358 个具有大幅度相机运动的场景。
*   **对比方法**：
    *   **回归类**：DepthSplat, LVSM。
    *   **扩散类**：MotionCtrl, ViewCrafter, SEVA。
*   **评估指标**：PSNR、LPIPS（重建质量）；FID（生成质量）；ATE_r、ATE_t（位姿准确度/空间一致性）。

### 4. 资源与算力
*   **硬件**：使用了 **32 台 NVIDIA A800 GPU**。
*   **训练时长**：
    *   3DRAE 训练了 160k 步（约 12 万步主训练 + 后续微调）。
    *   3DDiT 训练了 80k 步（ImageNet 预训练）+ 80k 步（多视图数据训练）。
*   **推理效率**：生成一个 3D 场景仅需约 **2 秒**，远快于 2D 扩散方法的 10 秒至 3 分钟。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（4 个数据集，2 种条件设置）和定性展示。
*   **消融实验**：非常充分，涵盖了：
    1.  不同 2D 编码器（DINOv2, SigLIP2, DA3）对重建与生成的影响。
    2.  3D 潜 Token 数量（256, 512, 1024）的扩展性分析。
    3.  模型参数量（S, B, XL）的缩放效应。
    4.  ImageNet 预训练的有效性验证。
*   **客观性**：通过 Zero-shot 实验（在 Tanks and Temples, Mip-NeRF 360 等未见数据集上测试）证明了模型的泛化能力，实验设计公平且客观。

### 6. 主要结论与发现
*   **1K Token 足够**：仅需 1024 个 Token 即可有效表征复杂的 3D 场景，显著降低了计算冗余。
*   **语义优先**：发现使用语义预训练模型（如 DINOv2）作为编码器，虽然在纯重建指标上略逊于几何模型（如 DA3），但在**生成质量**和**空间一致性**上表现远优。
*   **高效生成**：3D 潜空间建模使得推理速度提升了数倍甚至数十倍，且能保持极高的轨迹对齐精度。

### 7. 优点
*   **架构创新**：首次实现了在固定长度的隐式 3D 潜空间中进行大规模场景扩散生成。
*   **极高效率**：解耦了生成与渲染过程，支持任意轨迹的即时解码。
*   **强一致性**：由于在 3D 空间建模，彻底解决了视频扩散模型中常见的“物体变形”或“场景漂移”问题。
*   **良好的扩展性**：展示了随数据量和模型参数量增加而性能提升的潜力。

### 8. 不足与局限
*   **重建保真度权衡**：在极高密度的视图重建任务中，由于 Token 数量固定，其细节还原能力可能略逊于专门的回归类重建模型。
*   **随机性偏差**：3DDiT 在去噪过程中可能引入随机性，导致生成的场景与初始条件视图在极细微处存在偏差。
*   **依赖位姿**：模型训练和推理仍高度依赖准确的相机位姿输入，对于无位姿数据的处理能力有限。

（完）
