---
title: "ReplicateAnyScene: Zero-Shot Video-to-3D Composition via Textual-Visual-Spatial Alignment"
title_zh: ReplicateAnyScene：通过文本-视觉-空间对齐实现零样本视频到3D场景组合
authors: "Mingyu Dong, Chong Xia, Mingyuan Jia, Weichen Lyu, Long Xu, Zheng Zhu, Yueqi Duan"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10789v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 将视频零样本转换为结构化3D场景
tldr: ReplicateAnyScene是一个零样本框架，旨在将随手拍摄的视频自动转换为结构化的组合式3D场景。针对现有方法依赖人工提示、跨模态整合不足及场景受限等问题，该框架通过五阶段级联流程，在文本、视觉和空间维度上对齐视觉基础模型的通用先验，并将其植入3D表示中。此外，研究者还提出了C3DR基准测试集，实验证明该方法在生成高质量、语义一致且物理合理的3D场景方面优于现有基准。
selection_source: fresh_fetch
motivation: 现有组合式3D重建方法过度依赖人工干预且缺乏有效的跨模态信息整合，难以处理复杂的现实场景。
method: 提出一种五阶段级联流水线，通过在文本、视觉和空间维度上对齐基础模型的通用先验，实现全自动的3D场景构建。
result: 在新提出的C3DR基准测试和广泛实验中，该方法在重建质量和语义一致性上均显著超越了现有基准。
conclusion: 该研究实现了从视频到组合式3D场景的自动化零样本转换，为空间智能和具身智能的发展提供了重要支持。
---

## 摘要
人类具有从视频观测中快速感知和分割物体，甚至在脑海中将其组装成结构化3D场景的先天能力。复制这种被称为“组合式3D重建”的能力，对于空间智能（Spatial Intelligence）和具身智能（Embodied AI）的发展至关重要。然而，由于跨模态信息整合不足，现有方法难以实现实际部署，导致其依赖手动物体提示、依赖辅助视觉输入，并受训练偏差限制而仅限于过于简单的场景。为解决这些局限性，我们提出了 ReplicateAnyScene，这是一个能够将随意拍摄的视频全自动且零样本（zero-shot）地转换为组合式3D场景的框架。具体而言，我们的流水线包含一个五阶段级联过程，用于从视觉基础模型中提取并在文本、视觉和空间维度上结构化地对齐通用先验，将其落地（grounding）为结构化3D表示，并确保所构建场景的语义连贯性和物理合理性。为了促进对该任务更全面的评估，我们进一步引入了 C3DR 基准测试，从多个维度评估重建质量。广泛的实验表明，我们的方法在生成高质量组合式3D场景方面优于现有的基准方法。

## Abstract
Humans exhibit an innate capacity to rapidly perceive and segment objects from video observations, and even mentally assemble them into structured 3D scenes. Replicating such capability, termed compositional 3D reconstruction, is pivotal for the advancement of Spatial Intelligence and Embodied AI. However, existing methods struggle to achieve practical deployment due to the insufficient integration of cross-modal information, leaving them dependent on manual object prompting, reliant on auxiliary visual inputs, and restricted to overly simplistic scenes by training biases. To address these limitations, we propose ReplicateAnyScene, a framework capable of fully automated and zero-shot transformation of casually captured videos into compositional 3D scenes. Specifically, our pipeline incorporates a five-stage cascade to extract and structurally align generic priors from vision foundation models across textual, visual, and spatial dimensions, grounding them into structured 3D representations and ensuring semantic coherence and physical plausibility of the constructed scenes. To facilitate a more comprehensive evaluation of this task, we further introduce the C3DR benchmark to assess reconstruction quality from diverse aspects. Extensive experiments demonstrate the superiority of our method over existing baselines in generating high-quality compositional 3D scenes.

---

## 论文详细总结（自动生成）

### 论文总结：ReplicateAnyScene

#### 1. 核心问题与研究动机
*   **核心问题**：如何从一段随意拍摄的单目视频中，全自动且零样本（Zero-Shot）地重建出具有物体级结构（组合式）的3D场景。
*   **研究动机**：
    *   传统的整体重建方法（如NeRF、3DGS）侧重于视觉保真度，缺乏物体层面的交互能力，不适用于空间智能和具身智能任务。
    *   现有的组合式重建方法存在严重局限：依赖人工提示（Prompt）、需要高精度深度图等辅助输入、容易产生物体碰撞或悬浮等物理不合理现象，且受训练数据偏差影响难以处理复杂场景。
    *   视觉基础模型（VFM）虽在单一任务（如分割、生成）表现优异，但直接堆叠会导致尺度模糊、身份匹配错误和空间布局脱节。

#### 2. 方法论：五阶段级联流水线
该方法的核心思想是通过**文本-视觉-空间**三维度的对齐，将2D语义和感知先验逐步植入3D物理空间。
1.  **渐进式物体发现（Progressive Object Discovery）**：利用视觉语言模型（VLM）扫描视频关键帧，通过动态类别注册表迭代查询，提取非冗余的开放词汇物体类别列表。
2.  **空间引导的视觉去重（Spatial-Guided Visual Deduplication）**：将2D掩码投影到3D空间，利用几何邻近度（点云重叠率）作为准则，合并因遮挡或出入画导致的破碎跟踪片段，确保实例身份的一致性。
3.  **最优视角3D资产生成（Optimal-View 3D Asset Generation）**：不简单依据2D面积，而是计算投影后的**3D表面积**，选择信息量最大的视角，调用SAM3D生成高质量的3D网格资产。
4.  **迭代视觉-空间对齐（Iterative Visual-Spatial Alignment）**：采用“渲染-匹配-优化”循环。利用MASt3R建立真实帧与渲染帧的稠密视觉对应关系，通过Umeyama算法求解相似变换，精细化物体位姿。
5.  **语义感知场景细化（Semantic-Aware Scene Refinement）**：利用VLM推理物体间的空间关系（如“支撑”、“贴墙”），根据预设规则修正物体的倾斜、悬浮或穿插，确保物理合理性。

#### 3. 实验设计
*   **基准测试（Benchmark）**：引入了 **C3DR** 基准，包含50个复杂场景，来源涵盖 Replica、ScanNet、ScanNet++ 以及 YouTube 真实视频。
*   **对比方法**：主要对比了 **SimRecon**（最新的优化式方法）和 **MetaScenes**（基于检索的方法）。
*   **评估指标**：
    *   **文本维度**：类别召回率（Rec）、实例F1分数。
    *   **视觉维度**：PSNR、SSIM、LPIPS（全参考）及 MUSIQ（无参考感知指标）。
    *   **几何维度**：倒角距离（CD）、F-Score、法线一致性（NC）。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A6000** GPU。
*   **耗时**：处理一个场景平均耗时约 **27分钟**（每个物体约1.6分钟）。
*   **训练**：该方法为零样本框架，无需针对特定场景进行大规模重训练，主要依赖预训练基础模型的推理。

#### 5. 实验数量与充分性
*   **实验规模**：在50个涵盖室内、真实世界及合成数据的多样化场景上进行了测试，实验规模在同类研究中属于中上水平。
*   **消融实验**：针对流水线的全部五个阶段分别进行了详尽的消融研究（如：去重 vs 不去重、最大表面积 vs 最大像素面积、有无语义细化等），验证了每个模块的必要性。
*   **客观性**：通过引入第三方高保真模型（如Rodin、Trellis等）辅助构建Ground Truth，并由专业建模师手动校准布局，确保了评估基准的公正性。

#### 6. 主要结论与发现
*   **性能领先**：ReplicateAnyScene 在所有评估维度（文本、视觉、几何）上均显著优于现有基准。
*   **自动化程度高**：实现了从原始视频到结构化3D场景的端到端自动化，无需任何人工干预。
*   **物理合理性**：通过语义对齐，有效解决了组合式重建中常见的物体“漂浮”和“穿模”问题。
*   **泛化能力**：零样本特性使其能够处理视频中出现的各种开放词汇物体。

#### 7. 优点（亮点）
*   **多模态对齐机制**：巧妙地解决了不同视觉基础模型之间信息碎片化的问题。
*   **最优视角选择策略**：提出的3D表面积最大化准则比传统的2D面积准则更科学，显著提升了资产生成质量。
*   **C3DR基准**：填补了组合式3D重建领域缺乏标准化、多维度评估工具的空白。

#### 8. 不足与局限
*   **自然场景限制**：目前主要针对室内环境，在处理具有高度不规则几何形状的复杂自然场景（如茂密植被）时仍面临挑战。
*   **无界环境对齐**：对于无边界（Unbounded）的户外环境，物体的空间对齐和背景合成仍是开放性难题。
*   **依赖基础模型**：重建质量高度依赖于底层VFM（如SAM3D、MASt3R）的性能上限。

（完）
