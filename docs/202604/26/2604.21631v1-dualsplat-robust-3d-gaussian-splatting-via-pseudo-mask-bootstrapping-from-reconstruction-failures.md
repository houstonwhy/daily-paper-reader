---
title: "DualSplat: Robust 3D Gaussian Splatting via Pseudo-Mask Bootstrapping from Reconstruction Failures"
title_zh: DualSplat：通过重建失败中的伪掩码自举实现鲁棒的 3D 高斯泼溅
authors: "Xu Wang, Zhiru Wang, Shiyun Xie, Chengwei Pan, Yisong Chen"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21631v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于场景重建的鲁棒3D高斯泼溅
tldr: 针对3D高斯泼溅（3DGS）在处理包含瞬时物体图像时性能下降的问题，本文提出DualSplat框架。该方法采用“从失败中学习”的策略，利用初次重建中瞬时物体产生的破碎伪影，结合光度残差、特征不匹配和SAM2生成伪掩码。随后在第二阶段利用这些掩码引导优化，并配合轻量级MLP在线修正，有效解决了静态场景重建与动态物体检测之间的循环依赖，在多个基准数据集上实现了更鲁棒的渲染效果。
selection_source: fresh_fetch
motivation: 现有方法在处理非一致性物体时存在循环依赖：准确检测瞬时物体需要高质量重建，而高质量重建又依赖于准确的物体掩码。
method: 提出一种两阶段框架，利用初次重建失败产生的伪影构建伪掩码，并结合SAM2与轻量级MLP在第二阶段优化中实现鲁棒的3DGS重建。
result: 在RobustNeRF和NeRF On-the-go数据集上的实验证明，DualSplat在处理瞬时物体密集的场景时，性能显著优于现有基准方法。
conclusion: DualSplat通过将重建失败转化为显式先验，成功打破了动态场景建模中的循环依赖，显著提升了3DGS在复杂现实环境下的鲁棒性。
---

## 摘要
虽然 3D 高斯泼溅 (3DGS) 实现了实时照片级渲染，但当训练图像包含违反多视图一致性的瞬态物体时，其性能会显著下降。现有方法面临循环依赖问题：准确的瞬态检测需要重建良好的静态场景，而干净的重建本身又依赖于可靠的瞬态掩码。我们通过 DualSplat 解决了这一挑战，这是一个“从失败到先验”（Failure-to-Prior）的框架，它将第一阶段的重建失败转化为第二阶段重建的显式先验。我们观察到，仅在部分视图中出现的瞬态物体在保守的初始训练期间通常表现为不完整的碎片。我们利用这些失败，通过结合光度残差、特征不匹配和 SAM2 实例边界来构建物体级伪掩码。这些伪掩码随后引导干净的第二阶段 3DGS 优化，同时一个轻量级 MLP 通过从先验监督逐渐转向自一致性来在线细化这些掩码。在 RobustNeRF 和 NeRF On-the-go 上的实验表明，DualSplat 优于现有基准，在瞬态物体密集的场景和瞬态区域表现出尤为明显的优势。

## Abstract
While 3D Gaussian Splatting (3DGS) achieves real-time photorealistic rendering, its performance degrades significantly when training images contain transient objects that violate multi-view consistency. Existing methods face a circular dependency: accurate transient detection requires a well-reconstructed static scene, while clean reconstruction itself depends on reliable transient masks. We address this challenge with DualSplat, a Failure-to-Prior framework that converts first-pass reconstruction failures into explicit priors for a second reconstruction stage. We observe that transients, which appear in only a subset of views, often manifest as incomplete fragments during conservative initial training. We exploit these failures to construct object-level pseudo-masks by combining photometric residuals, feature mismatches, and SAM2 instance boundaries. These pseudo-masks then guide a clean second-pass 3DGS optimization, while a lightweight MLP refines them online by gradually shifting from prior supervision to self-consistency. Experiments on RobustNeRF and NeRF On-the-go show that DualSplat outperforms existing baselines, demonstrating particularly clear advantages in transient-heavy scenes and transient regions.

---

## 论文详细总结（自动生成）

这是一份关于论文《DualSplat: Robust 3D Gaussian Splatting via Pseudo-Mask Bootstrapping from Reconstruction Failures》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：3D 高斯泼溅（3DGS）在处理包含“瞬时物体”（如行人、车辆、临时遮挡物）的现实场景图像时，由于这些物体违反了多视图一致性，会导致重建结果中出现严重的鬼影和伪影。
*   **现有局限**：目前的鲁棒重建方法面临**循环依赖**问题——准确检测瞬时物体需要一个已经重建良好的静态背景来暴露差异，而干净的背景重建又依赖于准确的瞬态掩码。如果两者同时从零开始优化，错误会相互强化，导致静态区域被误删或动态物体被嵌入几何结构中。
*   **研究动机**：作者观察到，瞬时物体由于可见性稀疏，在初始重建中往往表现为“不完整的碎片”。本文旨在将这些“重建失败”转化为显式先验，从而打破上述循环依赖。

### 2. 方法论：核心思想与技术流程
DualSplat 提出了一个**“从失败到先验”（Failure-to-Prior）**的两阶段框架：

*   **第一阶段：失败暴露（Stage I）**
    *   进行一次保守的初始 3DGS 重建。
    *   **实例级残差筛选**：利用 SAM2 提取实例掩码，计算每个实例区域内的平均光度残差。通过自适应阈值（随迭代次数收紧）识别潜在的瞬时物体实例。
*   **构建物体级伪掩码（Pseudo-Mask Construction）**
    *   **特征不匹配分析**：利用 FiT3D 提取具有 3D 感知能力的特征，计算真实图像与第一阶段渲染图之间的余弦相似度。
    *   **多准则融合**：结合光度残差、特征相似度和 SAM2 边界，筛选出高召回率的物体级伪掩码（Mpseudo）。
*   **第二阶段：先验引导重建（Stage II）**
    *   **在线掩码细化**：引入一个轻量级 MLP，输入图像特征和深度残差（来自 Depth Anything v2），预测瞬时概率图。
    *   **监督策略转移**：训练初期由伪掩码先验主导监督（Lprior），防止瞬时物体渗入几何；随着几何稳定，监督逐渐转向基于特征一致性和残差边界的自一致性（Lrobust）。

### 3. 实验设计
*   **数据集**：
    *   **RobustNeRF**：包含 Statue, Android, Crab, Yoda 等 5 个场景。
    *   **NeRF On-the-go**：包含 Mountain, Fountain, Corner, Patio 等 6 个场景，涵盖低、中、高不同程度的遮挡。
*   **Benchmark 与对比方法**：
    *   对比了 vanilla **3DGS** 以及多种鲁棒重建变体：**WildGaussians**、**DeSplat**、**SpotLessSplats**、**DeGauss** 和 **RobustSplat**。
    *   评价指标采用标准的 PSNR、SSIM 和 LPIPS。

### 4. 资源与算力
*   **实现细节**：基于 PyTorch 和 RobustSplat 代码库实现。
*   **训练时长**：第一阶段运行 10k 次迭代，第二阶段运行 30k 次迭代。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 RTX 3090）及具体数量。但提到由于采用了两阶段设计和 SAM2 掩码生成，整体训练时间较传统 3DGS 有所增加。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准数据集的 11 个不同场景上进行了测试，涵盖了从简单到极具挑战性的遮挡环境。
*   **消融实验**：非常充分。作者分别验证了延迟加密（DD）、伪掩码（PM）、MLP 模块、深度正则化以及不同特征提取器（DINOv2, SD, FiT3D, ResNet 等）对结果的影响。
*   **客观性**：通过分层分析（分别评估瞬时区域和静态背景的指标），证明了性能提升确实源于对动态干扰的抑制，而非简单的背景平滑。

### 6. 主要结论与发现
*   **性能领先**：DualSplat 在两个数据集上的平均指标均达到最优（SOTA），尤其在瞬时物体密集的场景中优势明显。
*   **先验的重要性**：实验表明伪掩码模块贡献了最大的性能增益（PSNR 提升约 3.56 dB），证明了将重建失败转化为外部先验的有效性。
*   **特征提取器的影响**：FiT3D 由于经过 3D 感知微调，在识别视图不一致区域方面显著优于 DINOv2 或 Stable Diffusion 特征。

### 7. 优点（亮点）
*   **范式创新**：提出了“从失败到先验”的思路，有效解决了鲁棒重建中的循环依赖问题。
*   **解耦设计**：将瞬时物体检测与最终场景优化解耦，使得重建过程更加稳定，不易陷入局部最优。
*   **多模态融合**：巧妙结合了 2D 实例分割（SAM2）、3D 感知特征（FiT3D）和单目深度（Depth Anything），构建了极其稳健的先验。

### 8. 不足与局限
*   **计算开销**：两阶段训练加上 SAM2 的预处理增加了总耗时，不适合极速重建需求。
*   **泛化能力**：MLP 掩码预测器是针对每个场景单独训练的，缺乏跨场景的通用性。
*   **特定失效模式**：当瞬时物体在大量视图中长时间停留时（例如长时间停放的车辆），系统可能将其误认为静态场景的一部分而无法剔除。
*   **边界伪影**：MLP 在物体边缘有时会产生误报（False Positives）。

（完）
