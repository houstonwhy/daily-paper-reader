---
title: "SceneOrchestra: Efficient Agentic 3D Scene Synthesis via Full Tool-Call Trajectory Generation"
title_zh: SceneOrchestra：通过全工具调用轨迹生成实现高效的智能体化 3D 场景合成
authors: "Yun He, Kelin Yu, Matthias Zwicker"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19907v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 通过工具调用轨迹生成的智能体3D场景合成
tldr: 针对现有3D场景合成框架中“执行-评审-反思”循环导致的效率低下和工具调用不佳问题，本文提出SceneOrchestra。该框架通过可训练的编排器直接生成完整的工具调用轨迹，并利用判别器进行两阶段微调，消除了逐步渲染评审的延迟。实验证明，该方法在提升场景生成质量的同时，显著缩短了运行时间，实现了高效且高质量的3D场景合成。
selection_source: fresh_fetch
motivation: 现有的3D场景合成方法依赖逐步评审循环，导致推理延迟高且工具调用序列非最优。
method: 提出一种包含编排器和判别器的框架，通过两阶段训练实现完整工具调用轨迹的生成与优化。
result: 实验结果显示，该方法在保持或提升场景生成质量的同时，大幅降低了系统运行时间。
conclusion: SceneOrchestra证明了通过预先生成完整工具轨迹而非逐步反馈，可以显著提升智能体驱动的3D场景合成效率。
---

## 摘要
最近用于 3D 场景合成的智能体框架通过集成异构的生成和编辑工具，提升了真实感和多样性。这些工具被组织成由现成的大语言模型（LLM）编排的工作流。目前的方法通常采用“执行-评审-反思”循环：在每一步中，编排器执行一个工具，渲染中间结果以供评审，然后决定下一步的工具及其参数。然而，这种设计有两个关键局限性。首先，下一步的工具选择和参数配置是由启发式规则驱动的，这可能导致次优的执行流、不必要的工具调用、输出质量下降以及运行时间增加。其次，在每一步之后渲染和评审中间结果会引入额外的延迟。为了解决这些问题，我们提出了 SceneOrchestra，这是一个可训练的编排框架，它优化了工具调用执行流并消除了逐步评审循环，从而提高了效率和输出质量。SceneOrchestra 由一个编排器和一个判别器组成，我们使用两阶段训练策略对其进行微调。在第一阶段，编排器学习上下文感知的工具选择和完整的工具调用轨迹生成，而判别器则被训练用于评估完整轨迹的质量，使其能够从多个候选轨迹中选择最佳轨迹。在第二阶段，我们进行交替训练，判别器适应编排器不断演变的轨迹分布，并将其判别能力蒸馏回编排器。在推理时，我们仅使用编排器根据指令生成并执行完整的工具调用轨迹，而无需判别器。大量实验表明，与之前的工作相比，我们的方法在减少运行时间的同时，实现了最先进的场景质量。

## Abstract
Recent agentic frameworks for 3D scene synthesis have advanced realism and diversity by integrating heterogeneous generation and editing tools. These tools are organized into workflows orchestrated by an off-the-shelf LLM. Current approaches typically adopt an execute-review-reflect loop: at each step, the orchestrator executes a tool, renders intermediate results for review, and then decides on the tool and its parameters for the next step. However, this design has two key limitations. First, next-step tool selection and parameter configuration are driven by heuristic rules, which can lead to suboptimal execution flows, unnecessary tool invocations, degraded output quality, and increased runtime. Second, rendering and reviewing intermediate results after each step introduces additional latency. To address these issues, we propose SceneOrchestra, a trainable orchestration framework that optimizes the tool-call execution flow and eliminates the step-by-step review loop, improving both efficiency and output quality. SceneOrchestra consists of an orchestrator and a discriminator, which we fine-tune with a two-phase training strategy. In the first phase, the orchestrator learns context-aware tool selection and complete tool-call trajectory generation, while the discriminator is trained to assess the quality of full trajectories, enabling it to select the best trajectory from multiple candidates. In the second phase, we perform interleaved training, where the discriminator adapts to the orchestrator's evolving trajectory distribution and distills its discriminative capability back into the orchestrator. At inference, we only use the orchestrator to generate and execute full tool-call trajectories from instructions, without requiring the discriminator. Extensive experiments show that our method achieves state-of-the-art scene quality while reducing runtime compared to previous work.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **SceneOrchestra** 的 3D 场景合成框架，旨在解决现有智能体（Agentic）方法在生成 3D 场景时效率低下和工具调用非最优的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：当前的 3D 场景合成正从单一模型转向集成多种异构工具（如布局生成、物体放置、物理优化等）的智能体框架。
*   **核心问题**：现有的智能体框架（如 SceneWeaver）通常采用“执行-评审-反思”的循环模式。这种模式存在两大缺陷：
    1.  **效率低下**：每一步工具调用后都需要渲染中间结果并由大模型评审，引入了巨大的计算延迟。
    2.  **决策次优**：工具选择和参数配置依赖于启发式规则，往往导致不必要的工具调用或生成质量下降。
*   **研究目标**：开发一个能够“一次性”生成完整工具调用轨迹的训练化编排框架，在提升生成质量的同时大幅缩短运行时间。

### 2. 方法论
SceneOrchestra 由**编排器（Orchestrator）**和**判别器（Discriminator）**两个微调后的 LLM 组成，核心思想是通过两阶段训练实现从“逐步反馈”到“全轨迹预测”的转变。

*   **核心组件**：
    *   **编排器**：负责根据用户指令直接生成完整的工具调用序列（轨迹）。
    *   **判别器**：负责评估不同轨迹的质量，辅助编排器的进化。
*   **关键技术细节**：
    *   **评分机制**：引入“组合得分（Composition Score）”，综合考虑物理有效性（碰撞、越界）、视觉语义质量（真实感、功能性）以及时间成本（运行时间惩罚）。
    *   **第一阶段：独立训练**。编排器经历四个子阶段：逐步 SFT（学习基础工具选择）、轨迹级 SFT（学习全序列格式）、逐步 DPO（优化局部决策）、轨迹级 DPO（优化全局规划）。判别器则学习对轨迹进行排序。
    *   **第二阶段：交替训练**。判别器适应编排器新生成的轨迹分布，随后通过 DPO 将判别器的评估能力“蒸馏”回编排器。
*   **推理流程**：推理时仅需编排器，输入指令后直接输出全轨迹并执行，无需中间渲染和评审。

### 3. 实验设计
*   **场景与数据集**：涵盖 10 种房间类型，分为“已见（Seen）”和“未见（Unseen）”两类（如卧室、健身房、办公室等）。
*   **基准方法（Baselines）**：
    *   **单体方法**：LayoutGPT, Holodeck, I-Design。
    *   **智能体方法**：SceneWeaver（当前最先进水平）。
*   **评估指标**：
    *   **物理指标**：物体数量、越界数量、碰撞对数。
    *   **视觉语义指标**：真实感、功能性、布局正确性、场景完整性（由 GPT-4 作为裁判评分）。
    *   **效率指标**：平均生成时间。

### 4. 资源与算力
*   **硬件环境**：所有训练和推理实验均在**单张 NVIDIA RTX A6000 GPU** 上完成。
*   **模型基础**：编排器和判别器均基于 **Qwen3-4B-Instruct** 进行微调。
*   **训练时长**：文中未明确给出总训练小时数，但提到 SceneWeaver 生成一个场景平均需 1.5 小时，而本方法通过离线收集轨迹进行训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 10 种房间类型进行了定量对比，每种类型生成 5 个场景取平均值。
    *   针对 10 条复杂的长指令进行了专门测试。
    *   进行了包含 18 名参与者的用户主观研究（User Study）。
*   **消融实验**：详细测试了 DPO、逐步训练、判别器以及交替训练对最终效果的贡献。
*   **充分性评价**：实验设计较为全面，涵盖了泛化性测试（未见房间类型）、复杂指令理解以及主客观评估，对比基准涵盖了该领域最新的主流方法，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **质量提升**：在物理有效性和视觉语义得分上，SceneOrchestra 均优于现有的智能体和单体方法。
*   **效率飞跃**：相比 SceneWeaver，本方法将生成时间**缩短了约 70%**（例如，某些场景从 91 分钟降至 27 分钟）。
*   **泛化能力**：即使是训练中未出现的房间类型，编排器也能生成合理且高质量的工具调用轨迹。

### 7. 优点与亮点
*   **高效编排**：通过全轨迹生成消除了昂贵的“渲染-评审”循环，解决了智能体框架落地的核心痛点。
*   **训练策略创新**：结合 SFT 和 DPO 的四阶段训练，以及判别器引导的交替训练，有效地将复杂的编排逻辑内化到模型中。
*   **端到端优化**：不仅优化了生成质量，还通过惩罚项将“效率”直接纳入了优化目标。

### 8. 不足与局限
*   **幻觉风险**：编排器有时会产生幻觉，输出错误的工具名称或参数格式，导致调用失败（需通过重试机制缓解）。
*   **性能上限受限**：其表现最终受限于底层工具箱（Tools）的能力。如果底层工具无法处理某些复杂交互（如关节物体），编排器也无法突破这一限制。
*   **数据依赖**：训练依赖于从现有框架（如 SceneWeaver）中收集的初始轨迹，若初始数据质量极差，可能会影响微调效果。

（完）
