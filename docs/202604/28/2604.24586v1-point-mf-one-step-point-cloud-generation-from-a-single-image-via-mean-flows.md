---
title: "Point-MF: One-step Point Cloud Generation from a Single Image via Mean Flows"
title_zh: Point-MF：基于均值流的单图一步点云生成
authors: "Yuta Baba, Keiji Yanai"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24586v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 从图像进行单步点云生成
tldr: 本研究针对单图点云重建中扩散模型推理速度慢的问题，提出了Point-MF框架。该框架基于均值流（Mean Flow）理论，在点云空间直接学习平均速度场，实现了仅需单次网络评估（1-NFE）的高质量重建。Point-MF采用适配均值流的扩散Transformer并结合DINOv3特征，通过引入去噪空间锚点损失稳定大步长生成，在保持高精度的同时实现了毫秒级推理。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的点云重建方法虽然精度高，但由于需要多次迭代导致推理速度慢且计算成本高。
method: 提出一种基于均值流的单步重建框架，利用扩散Transformer架构结合DINOv3特征，并引入去噪空间锚点辅助损失来优化生成质量。
result: 在ShapeNet-R2N2和Pix3D数据集上，Point-MF在重建质量与推理速度之间取得了极佳平衡，实现了毫秒级的单步点云生成。
conclusion: Point-MF证明了通过均值流和针对性架构设计，可以在不依赖VAE的情况下实现高效、高质量的单步单图点云重建。
---

## 摘要
单图点云重建必须从单张 RGB 图像中推断出完整的 3D 几何结构，包括被遮挡的部分。虽然基于扩散模型的重建方法达到了很高的精度，但它们通常需要多次去噪迭代，导致推理速度慢且计算开销大。我们提出了 Point-MF，这是一个基于均值流（Mean-Flow）的框架，用于低 NFE（网络函数评估次数）的单图点云重建，该框架将兼容均值流的架构与辅助损失相结合。具体而言，Point-MF 直接在点云空间中运行以学习均值速度场，并实现了仅需单次网络函数评估（1-NFE）的一步重建，且不依赖于基于 VAE 的潜表征。为了使均值流在大间隔跳跃下依然有效，Point-MF 采用了专为均值流设置定制的扩散 Transformer（Diffusion Transformer），通过轻量级令牌适配器以冻结的 DINOv3 图像特征为条件，并配备了显式的间隔/时间调节。此外，我们引入了去噪空间锚点（Denoised Space Anchor），这是一种针对由预测速度场导出的去噪空间估计值 $x_θ$ 的集合距离辅助损失，旨在稳定大步长生成并减少离群点和密度伪影。在 ShapeNet-R2N2 和 Pix3D 数据集上，与多步扩散基准模型和具有竞争力的前馈模型相比，Point-MF 在重建质量和推理速度之间取得了极佳的平衡，同时能以毫秒级延迟生成高质量点云。

## Abstract
Single-image point cloud reconstruction must infer complete 3D geometry, including occluded parts, from a single RGB image. While diffusion-based reconstructors achieve high accuracy, they typically require many denoising iterations, resulting in slow and expensive inference. We propose Point-MF, a Mean-Flow-based framework for low-NFE single-image point cloud reconstruction that couples a Mean-Flow-compatible architecture with an auxiliary loss. Specifically, Point-MF operates directly in point-cloud space to learn the mean velocity field and enables one-step reconstruction with a single network function evaluation (1-NFE), without relying on VAE-based latent representations. To make Mean Flow effective under large interval jumps, Point-MF employs a Diffusion Transformer tailored to the Mean-Flow setting, conditioned on frozen DINOv3 image features via a lightweight token adapter and equipped with explicit interval/time conditioning. Moreover, we introduce Denoised Space Anchor, a set-distance auxiliary loss on the denoised-space estimate $x_θ$ induced by the predicted velocity field, to stabilize large-step generation and reduce outliers and density artifacts. On ShapeNet-R2N2 and Pix3D, Point-MF strikes a strong balance between reconstruction quality and inference speed compared to multi-step diffusion baselines and competitive feedforward models, while generating high-quality point clouds with millisecond-level latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Point-MF**，一种基于均值流（Mean Flows）的单图点云重建框架。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何从单张 RGB 图像中高效、准确地重建完整的 3D 点云（包括被遮挡部分）。
*   **研究背景**：
    *   **前馈模型**（如 RGB2point）推理快，但在处理遮挡和不确定性时往往产生过于平滑或模糊的结果。
    *   **扩散模型**（如 PC²）能生成高质量、多样化的几何结构，但需要数百次迭代（NFE），推理速度极慢且计算昂贵。
*   **研究动机**：在不使用复杂的 VAE 潜空间（Latent Space）的情况下，利用均值流理论实现“一步到位”（1-NFE）的高质量点云生成，兼顾前馈模型的速度和生成模型的质量。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将点云生成建模为从噪声到数据的轨迹，通过学习**均值速度场（Mean Velocity Field）**而非瞬时速度，使得模型能够直接预测大时间跨度后的状态，从而实现单步生成。
*   **关键技术细节**：
    *   **均值流公式化**：基于 Flow Matching 框架，通过 Jacobian-vector product (JVP) 计算均值流恒等式，构建可训练的回归目标。
    *   **扩散 Transformer (DiT) 架构**：直接在点云空间运行，保持点云的排列不变性。
    *   **图像条件注入**：使用冻结的 **DINOv3** 提取全局和局部特征。引入 **Post-MHSA Adapter**（轻量级令牌适配器）来精炼图像特征，并通过 Cross-Attention 与点云特征融合。
    *   **去噪空间锚点 (DSA) 辅助损失**：这是本文的关键创新。在训练中，利用预测的速度场推导出一个“去噪后的点云估计值” $x_\theta$，并计算其与真实点云（GT）之间的 **APML**（自适应概率匹配损失）。这能稳定大步长生成，减少离群点和密度分布不均。

### 3. 实验设计
*   **数据集**：
    *   **ShapeNet-R2N2**：包含 13 类物体的合成数据集。
    *   **Pix3D**：包含真实世界图像与 3D 模型配对的数据集。
*   **评估指标**：L1 Chamfer Distance (CD)、Earth Mover's Distance (EMD)、F-Score、推理时间（Latency）和显存占用（VRAM）。
*   **对比方法**：
    *   **扩散类**：PC² (256-NFE)、BDM (276-NFE)。
    *   **前馈类**：RGB2point、ULSP、DIFFER 等。

### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA RTX A6000 GPU**。
*   **训练细节**：总计 120,000 个迭代步数（含 10,000 步预热），Batch Size 为 128。
*   **推理算力**：在单张 **NVIDIA RTX A4000 GPU** 上进行测试。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了全面的定量对比。
    *   提供了丰富的定性可视化结果（包括不同类别的重建效果）。
    *   **消融实验**：验证了 Post-MHSA Adapter (PMA) 和 DSA 损失的有效性，并对 DSA 的权重参数 $\lambda_{base}$ 以及不同的集合距离度量（MSE, CD, APML）进行了敏感性分析。
*   **充分性评价**：实验设计较为充分，对比了不同范式的代表性模型，且在真实数据集（Pix3D）上的验证增强了说服力。

### 6. 主要结论与发现
*   **性能领先**：Point-MF 在 1-NFE 下的 CD、EMD 和 F-Score 指标均优于需要数百步迭代的扩散模型（如 PC²）。
*   **速度优势**：推理延迟仅为 **63.45 毫秒**，比扩散模型快约 350-450 倍，且显存占用更低。
*   **几何一致性**：DSA 损失显著提升了点云的表面对齐度和密度均匀性，解决了均值流在大步长更新时容易偏离流形的问题。

### 7. 优点
*   **高效性**：实现了真正意义上的单步高质量 3D 重建，具有极强的实用价值。
*   **无需潜空间**：直接在点云空间操作，避免了训练复杂 VAE 的负担，简化了系统流程。
*   **鲁棒性**：结合 DINOv3 强大的表征能力，对真实世界图像（Pix3D）表现出良好的泛化性。
*   **姿态无关**：训练和推理均不需要相机姿态信息。

### 8. 不足与局限
*   **训练开销**：由于需要计算 JVP（雅可比向量积），训练时的计算量和内存需求较高。
*   **分辨率限制**：直接在点云空间操作，其计算成本随点数增加而增长，生成极高密度点云（如数万点）可能面临挑战（目前依赖外部上采样工具）。
*   **场景复杂度**：目前主要集中在物体级重建，对于带有复杂背景或严重遮挡的大规模场景重建效果尚待进一步验证。

（完）
