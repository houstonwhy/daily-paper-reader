---
title: "Zero-to-CAD: Agentic Synthesis of Interpretable CAD Programs at Million-Scale Without Real Data"
title_zh: Zero-to-CAD：无需真实数据的百万级可解释 CAD 程序代理式合成
authors: "Mohammadmehdi Ataei, Farzaneh Askari, Kamal Rahimi Malekshan, Pradeep Kumar Jayaraman"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24479v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 3D CAD程序的智能体合成
tldr: 针对现有3D数据集缺乏CAD建模历史的问题，本文提出Zero-to-CAD框架。该框架采用代理搜索策略，将大语言模型与反馈驱动的CAD环境结合，在无需真实数据的情况下合成了100万个包含丰富操作的可执行CAD程序。通过发布10万个高质量模型子集并微调视觉语言模型，研究证明了该合成数据在多视图图像重建可编辑CAD程序任务中的卓越性能，为CAD人工智能的发展提供了关键资源。
selection_source: fresh_fetch
motivation: 现有大规模3D数据集多为边界表示或网格，缺乏体现设计意图的参数化建模历史。
method: 通过将大语言模型嵌入反馈驱动的CAD环境，利用代理搜索、工具调用和文档查询迭代生成并验证CAD代码。
result: 合成了约100万个可执行且可编辑的CAD序列，并在图像转CAD重建任务中超越了包括GPT-4在内的强基准模型。
conclusion: Zero-to-CAD 成功弥补了几何规模与参数化可解释性之间的鸿沟，为缺乏真实建模历史的CAD AI训练提供了有效途径。
---

## 摘要
计算机辅助设计 (CAD) 模型由其构建历史定义：这是一种编码了设计意图的参数化配方。然而，现有的的大规模 3D 数据集主要由边界表示 (B-Reps) 或网格组成，剥离了这一关键的过程信息。为了解决这种稀缺性，我们推出了 Zero-to-CAD，这是一个用于合成可执行 CAD 构建序列的可扩展框架。我们将合成建模为一个代理式搜索问题：通过将大语言模型 (LLM) 嵌入到反馈驱动的 CAD 环境中，我们的系统利用工具和文档查询，迭代地生成、执行并验证代码，以提升几何有效性和操作多样性。这种代理式方法能够合成约一百万个可执行、可读且可编辑的 CAD 序列，涵盖了除“草图-拉伸”工作流之外的丰富操作词汇。我们还发布了一个包含 10 万个高质量模型的精选子集，这些模型是根据几何多样性筛选出来的。为了证明该数据集的实用性，我们在合成数据上微调了一个视觉语言模型，用于从多视图图像中重建可编辑的 CAD 程序，其表现优于包括 GPT-5.2 在内的强基准模型，并在没有真实构建历史训练数据的情况下，有效地引导了序列生成能力。Zero-to-CAD 弥合了几何规模与参数化可解释性之间的差距，为下一代 CAD AI 提供了重要的资源。

## Abstract
Computer-Aided Design (CAD) models are defined by their construction history: a parametric recipe that encodes design intent. However, existing large-scale 3D datasets predominantly consist of boundary representations (B-Reps) or meshes, stripping away this critical procedural information. To address this scarcity, we introduce Zero-to-CAD, a scalable framework for synthesizing executable CAD construction sequences. We frame synthesis as an agentic search problem: by embedding a large language model (LLM) within a feedback-driven CAD environment, our system iteratively generates, executes, and validates code using tools and documentation lookup to promote geometric validity and operation diversity. This agentic approach enables the synthesis of approximately one million executable, readable, editable CAD sequences, covering a rich vocabulary of operations beyond sketch-and-extrude workflows. We also release a curated subset of 100,000 high-quality models selected for geometric diversity. To demonstrate the dataset's utility, we fine-tune a vision-language model on our synthetic data to reconstruct editable CAD programs from multi-view images, outperforming strong baselines, including GPT-5.2, and effectively bootstrapping sequence generation capabilities without real construction-history training data. Zero-to-CAD bridges the gap between geometric scale and parametric interpretability, offering a vital resource for the next generation of CAD AI.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《Zero-to-CAD: Agentic Synthesis of Interpretable CAD Programs at Million-Scale Without Real Data》** 的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：计算机辅助设计（CAD）的核心价值在于其“构建历史”（Construction History），即一系列参数化的操作步骤。然而，现有的 3D 数据集（如 ABC, Objaverse）大多只提供最终的几何快照（B-Rep 或网格），丢失了设计意图。
*   **核心问题**：高质量、大规模且包含复杂操作（如圆角、布尔运算、放样等）的 CAD 构建序列数据极度匮乏。现有数据集规模小且操作单一（多局限于“草图-拉伸”）。
*   **整体含义**：本文提出了 **Zero-to-CAD** 框架，通过“代理式合成”（Agentic Synthesis）的方法，在完全不依赖真实 CAD 历史数据的情况下，利用大语言模型（LLM）的先验知识，自主生成了 100 万个可执行、可解释且复杂的 CAD 程序。

### 2. 论文提出的方法论
该框架将 CAD 生成视为一个**代理式搜索问题**，核心流程如下：
*   **代理式反馈循环（Agentic Loop）**：将 LLM 嵌入到 CAD 解释器环境中。模型不仅编写代码，还能通过工具调用（execute_and_validate）观察执行错误，并利用文档查询工具（lookup_documentation）自主修复语法或逻辑错误。
*   **两阶段生成协议**：
    1.  **目录生成（Stage 1）**：LLM 充当“机械图书馆员”，生成 65 个类别的机械零件描述（如支架、齿轮、外壳），确保语义多样性。
    2.  **代码生成（Stage 2）**：根据描述生成 CadQuery（基于 Python 的 CAD 库）代码。引入 19 条设计原则（如参数化命名、对称性、避免硬编码）来引导生成高质量代码。
*   **验证框架**：包含三阶段验证：
    *   **执行验证**：确保代码无语法错误且能运行。
    *   **几何验证**：检查拓扑有效性、单连通体、最小面数（>7 面）和正体积。
    *   **导出验证**：确保能成功导出为 STL 和 STEP 格式。
*   **多样性策略**：通过随机化提示词结构和引入参考代码片段（Template-guided）来防止模型陷入重复模式。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Zero-to-CAD 合成的 100 万个序列（含代码、图像、STEP/STL）。
    *   **测试集**：Zero-to-CAD 内部测试集（1万个）以及 **ABC 数据集**（从中抽取 1000 个作为分布外 OOD 测试）。
*   **任务场景**：**图像转序列（Image-to-Sequence）**。给定 8 张多视图渲染图，模型需直接生成可执行的 CadQuery 代码。
*   **Benchmark 与对比方法**：
    *   **基准模型**：Qwen3-VL-2B-Instruct（Base 版）。
    *   **对比方法**：**GPT-5.2**（High/Medium 推理级别）。
    *   **评估指标**：成功率（Success Rate）、体素 IoU（Intersection-over-Union）、倒角距离（Chamfer Distance）。

### 4. 资源与算力
*   **数据合成阶段**：
    *   **时长**：约 1 周。
    *   **算力**：动态调用内部闲置资源，GPU 数量在 2 到 80 块之间波动（用于 LLM 推理）；CPU 核心数峰值达到 3000 个（用于 CadQuery 执行验证）。
    *   **规模**：处理了约 600 亿个输入 token。
*   **模型训练阶段**：
    *   **硬件**：16 块 **NVIDIA H100 80GB GPU**。
    *   **配置**：全量微调 Qwen3-VL-2B，Batch Size 为 16，训练 3 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文生成了 100 万量级的数据，这在 CAD 序列领域是前所未有的。
*   **充分性**：
    *   进行了**分布内（In-distribution）**和**分布外（OOD）**的对比实验。
    *   对比了当前最强的闭源模型（GPT-5.2），证明了专用合成数据微调的优越性。
    *   提供了详细的消融统计（如修复循环的次数、操作覆盖率分布等）。
*   **客观性**：通过体素 IoU 等硬性几何指标进行评估，而非仅依赖 LLM 自评，实验设计较为客观。

### 6. 论文的主要结论与发现
*   **合成数据可行性**：LLM 内部蕴含的机械设计先验知识可以通过代理反馈循环转化为精确的 CAD 程序，无需任何真实历史数据。
*   **性能超越**：在合成数据上微调的 2B 规模小模型，在 CAD 重建任务上的表现显著优于通用的 GPT-5.2（IoU 0.747 vs 0.485）。
*   **引导效应（Bootstrapping）**：合成数据能够有效地引导模型处理真实世界（ABC 数据集）的几何形状，证明了合成到真实的迁移潜力。
*   **可解释性**：生成的代码具有人类可读的变量名和逻辑结构，远优于以往通过坐标链转译生成的代码（如 CAD-Recode）。

### 7. 优点
*   **规模与多样性**：首次将具备复杂操作（布尔、圆角等）的 CAD 序列数据集推向百万量级。
*   **闭环自纠错**：引入代理式搜索，解决了 LLM 容易产生幻觉或语法错误的问题，保证了数据集的可执行性。
*   **高质量代码**：强调“可解释性”和“参数化”，生成的代码不仅是几何的堆砌，更符合工程逻辑。

### 8. 不足与局限
*   **缺乏视觉反馈**：在合成阶段，代理主要依赖文本错误信息和拓扑指标，没有引入视觉检查，导致部分零件虽然拓扑正确但全局几何逻辑不合理（如孔位偏移）。
*   **制造约束缺失**：未考虑“面向制造的设计”（DFM）规则，生成的零件可能在现实中难以加工。
*   **OOD 性能差距**：虽然在 ABC 数据集上表现尚可，但相比分布内表现仍有明显下降（IoU 从 0.74 降至 0.37），说明合成数据的风格与真实人类设计仍有差异。
*   **计算成本**：虽然使用了闲置资源，但百万次代理调用和验证的计算开销依然巨大，难以被普通研究机构复现。

（完）
