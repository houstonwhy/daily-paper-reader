---
title: "BrickNet: Graph-Backed Generative Brick Assembly"
title_zh: BrickNet：基于图的生成式积木组装
authors: "Peter Kulits, Cordelia Schmid"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22984v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 3D积木物体和场景的生成模型
tldr: 本研究提出了 BrickNet，一个用于生成复杂乐高积木组装序列的语言模型。针对以往研究仅限于简单体素结构的局限，作者构建了包含超过 10 万个 LDraw 模型的大规模数据集。为了解决直接预测 3D 位姿导致的物理约束失效问题，BrickNet 采用基于图的程序表示法，通过零件间的连接关系来参数化结构，显著提升了生成序列的物理合理性与复杂零件处理能力。
selection_source: fresh_fetch
motivation: 现有的积木生成研究局限于简单的体素堆叠，难以处理具有复杂连接语义和多样化零件的真实乐高结构。
method: 提出一种基于图的程序表示方法，通过零件间的连接关系而非直接的 3D 位姿来定义和生成组装序列。
result: 成功构建了包含 10 万个高质量人类设计模型的数据集，并证明了图表示法在维持物理约束方面的优越性。
conclusion: BrickNet 通过引入连接性优先的图表示，为复杂物理结构的自动化设计和生成提供了有效的解决方案。
---

## 摘要
我们训练了一个语言模型来生成乐高积木的搭建序列。虽然之前的研究局限于离散的、类体素的塔状结构，但我们考虑了更广泛的零件集合，涵盖了具有多样化连接语义的数千种零件类型。为实现这一目标，我们首先收集了一个包含超过 100,000 个由人类设计的 LDraw 积木物体和场景的大规模数据集。我们设置的复杂性使得自回归地组装满足物理约束的结构变得具有挑战性。当直接预测积木姿态时，搭建序列在少数几步之后就会迅速失效。尽管零件被放置在三维空间中，但定义整体的是零件之间的空间关系。考虑到这一点，我们设计了一种基于图的程序表示，通过连接性对结构进行参数化，从而提高了生成序列的物理合理性。为了支持未来的应用，我们将数据集和模型公开用于研究目的。项目地址：https://kulits.github.io/BrickNet

## Abstract
We train a language model to generate LEGO-brick build sequences. While prior work has been restricted to discrete, voxel-like towers, we consider a much broader set of pieces, encompassing thousands of part types with diverse connection semantics. To enable this, we first collect a large-scale dataset of over 100,000 human-designed LDraw brick objects and scenes. The complexity of our setting makes it challenging to autoregressively assemble structures that satisfy physical constraints. When predicting block pose directly, build sequences quickly become invalid after a small number of steps. Although pieces are placed in 3D space, it is the spatial relationships of the parts which define the whole. With this in mind, we design a graph-based program representation that parametrizes structure through connectivity, improving the physical grounding of generated sequences. To enable future applications, we make our dataset and models available for research purposes. https://kulits.github.io/BrickNet

---

## 论文详细总结（自动生成）

这篇论文介绍了 **BrickNet**，一个旨在生成复杂乐高（LEGO）积木组装序列的生成式框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 3D 积木生成研究（如 BrickGPT）大多局限于简单的体素化网格和极少数零件类型（通常小于 10 种）。然而，真实的乐高系统包含数千种零件和复杂的连接语义（如铰链、轴、球头）。
*   **核心挑战**：在自回归生成过程中，直接预测零件的 3D 坐标（位姿）会导致数值精度误差累积，使得生成的结构在几步之后就出现物理失效（如零件重叠或悬空）。
*   **研究意义**：论文旨在通过引入一种基于图的表示法，使语言模型（LLM）能够理解并生成具有复杂连接关系的真实积木结构。

### 2. 方法论
*   **核心思想**：将积木结构视为一个**连接图（Connectivity Graph）**，其中节点是零件，边是零件间的物理连接。生成过程不再是预测绝对坐标，而是预测“如何将新零件连接到现有结构上”。
*   **关键技术细节**：
    *   **连接语义分类**：将连接分为五类：凸点（Stud）、铰链（Hinge）、轴（Axle）、球头（Ball）和固定（Fixed）。每种类型都有特定的自由度参数。
    *   **图参数化表示**：通过对连接图进行生成生成树（Spanning Tree）采样，将 3D 结构序列化为文本程序。每个步骤包含：[零件类型 | 颜色 | 连接点 A | 连接点 B | 旋转/位移参数]。
    *   **执行引擎**：该程序可被执行，通过局部 SE(3) 变换恢复出每个零件在 3D 空间中的精确 6-DoF 位姿。
    *   **零件标注**：利用程序化和手动结合的方式，为 LDraw 库中的 9,743 种零件标注了精确的连接位点。

### 3. 实验设计
*   **数据集**：
    *   **BrickNet-PT (预训练)**：包含 320,808 个样本，涵盖 9,743 种零件，总计超过 4000 万个放置实例。
    *   **BrickNet-SFT (微调)**：包含 67,185 个带文本描述（由 Gemini 2.5 生成）的高质量样本，零件数在 4 到 100 之间。
*   **对比基准 (Benchmark)**：
    *   **BrickGPT**：此前最先进的基于文本的积木生成模型。
    *   **BrECS**：一种基于体素约束的顺序组装方法。
*   **评估指标**：连接有效性（Connectivity Validity）、碰撞率（Collision Rate）、以及感知相似度指标（VQAScore, SigLIP 2, PE）。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen 3 系列模型，参数量涵盖 0.6b、1.7b、4b、8b 和 14b。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。但提到对序列长度进行了 4,096 token 的截断，并采用了标准交叉熵损失进行微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **无条件生成**：对 5 种规模的模型进行了采样测试（每种 216 个序列）。
    *   **文本条件生成**：在 512 个保留测试集上进行了评估。
    *   **消融实验**：对比了“基于图的表示”与“基于位姿的表示”，并分析了预训练数据量对性能的影响。
*   **充分性与公平性**：实验设计较为全面，涵盖了模型缩放定律（Scaling Laws）的验证。通过对比实验清晰地展示了图表示法在维持物理约束方面的压倒性优势。

### 6. 主要结论与发现
*   **表示法的优越性**：基于图的表示法在连接有效性上远超直接位姿预测（平均有效步数从不到 50 步提升至 94 步以上）。
*   **模型缩放效应**：随着模型参数增加，生成的物理合理性（减少无效放置）有所提升，但在文本匹配的感知质量上，1.7b 之后的模型表现出边际收益递减。
*   **预训练的重要性**：在大规模无标注积木数据上进行预训练，显著提升了下游文本驱动生成任务的性能。

### 7. 优点
*   **数据规模突破**：贡献了目前最大的、基于人类设计的真实乐高零件数据集。
*   **创新的表示法**：将复杂的 3D 几何约束简化为离散的图连接逻辑，极大地降低了 LLM 处理 3D 结构的难度。
*   **通用性**：不仅支持简单的方块，还能处理带有旋转、滑动等动态语义的复杂零件。

### 8. 不足与局限
*   **碰撞处理**：虽然连接性得到了保证，但模型在处理长序列时的**零件间碰撞（Inter-part collisions）**依然是一个挑战（有效步数在考虑碰撞后降至约 20 步）。
*   **序列长度限制**：实验限制在 100 个零件以内，而现实中的大型乐高模型往往包含数千个零件。
*   **领域知识依赖**：该方法高度依赖于预先标注的零件连接位点库，对于库外的新零件（Out-of-vocabulary parts）缺乏泛化能力。

（完）
