---
title: Repurposing 3D Generative Model for Autoregressive Layout Generation
title_zh: 将 3D 生成模型重新利用于自回归布局生成
authors: "Haoran Feng, Yifan Niu, Zehuan Huang, Yang-Tian Sun, Chunchao Guo, Yuxin Peng, Lu Sheng"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16299v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于生成物理上合理的3D场景的3D布局生成
tldr: LaviGen是一个将3D生成模型重新利用于3D布局生成的框架。与以往从文本推断布局的方法不同，它直接在原生3D空间中运行，通过自回归过程建模物体间的几何关系和物理约束。该方法结合了改进的3D扩散模型和双引导自展开蒸馏机制，显著提升了场景生成的物理合理性和计算效率，在LayoutVLM基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有3D布局生成方法往往缺乏对原生3D空间中几何关系和物理约束的显式建模，导致生成的场景一致性不足。
method: 提出LaviGen框架，利用自回归过程结合改进的3D扩散模型及双引导自展开蒸馏机制，实现对物体间关系的精确建模。
result: "在LayoutVLM基准测试中，LaviGen的物理合理性比现有最先进方法提升了19%，且计算速度加快了65%。"
conclusion: 该研究证明了通过重新利用3D生成模型并结合自回归建模，可以高效生成空间准确且物理一致的3D场景布局。
---

## 摘要
我们介绍了 LaviGen，这是一个将 3D 生成模型重新利用于 3D 布局生成的框架。与以往从文本描述推断物体布局的方法不同，LaviGen 直接在原生 3D 空间中运行，将布局生成建模为一个自回归过程，显式地对物体间的几何关系和物理约束进行建模，从而产生连贯且符合物理规律的 3D 场景。为了进一步增强这一过程，我们提出了一种改进的 3D 扩散模型，该模型集成了场景、物体和指令信息，并采用双重引导自展开蒸馏机制来提高效率和空间准确性。在 LayoutVLM 基准测试上的广泛实验表明，LaviGen 实现了卓越的 3D 布局生成性能，其物理合理性比现有最先进方法高出 19%，计算速度快 65%。我们的代码已在 https://github.com/fenghora/LaviGen 公开。

## Abstract
We introduce LaviGen, a framework that repurposes 3D generative models for 3D layout generation. Unlike previous methods that infer object layouts from textual descriptions, LaviGen operates directly in the native 3D space, formulating layout generation as an autoregressive process that explicitly models geometric relations and physical constraints among objects, producing coherent and physically plausible 3D scenes. To further enhance this process, we propose an adapted 3D diffusion model that integrates scene, object, and instruction information and employs a dual-guidance self-rollout distillation mechanism to improve efficiency and spatial accuracy. Extensive experiments on the LayoutVLM benchmark show LaviGen achieves superior 3D layout generation performance, with 19% higher physical plausibility than the state of the art and 65% faster computation. Our code is publicly available at https://github.com/fenghora/LaviGen.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **LaviGen** 的框架，旨在通过重新利用（Repurposing）预训练的 3D 生成模型，实现高质量、符合物理规律的自回归 3D 布局生成。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在 3D 空间中根据文本指令自动排列物体，生成既符合语义逻辑又符合物理约束（无碰撞、不悬空、不越界）的场景布局。
*   **研究背景**：
    *   早期方法缺乏空间知识，导致物理合理性差。
    *   近期基于大语言模型（LLM）的方法（如 LayoutGPT）将布局视为文本（JSON），虽有语义逻辑但缺乏 3D 几何直觉，常出现物体重叠或漂浮。
    *   基于视觉优化的方法（如 LayoutVLM）计算成本高且缺乏对 3D 结构的根本理解。
*   **核心动机**：利用 3D 生成模型中蕴含的强大几何先验，直接在原生 3D 空间中进行布局生成，从而自然地解决物理一致性问题。

### 2. 方法论
LaviGen 将布局生成建模为一个自回归过程，核心思想是“逐个放置物体并更新场景状态”。
*   **自回归 3D 布局扩散模型**：
    *   **输入集成**：将当前场景状态（Voxel 形式）、目标物体和文本指令编码后输入模型。
    *   **身份感知嵌入（Identity-aware Embedding）**：在旋转位置编码（RoPE）中加入身份标识位，帮助模型区分“已存在的场景”和“新加入的物体”，确保空间对齐。
*   **双引导自展开蒸馏（Dual-guidance Self-rollout Distillation）**：
    *   **解决暴露偏差**：自回归生成容易累积误差。该策略让模型在训练时基于自身的输出进行“自展开”。
    *   **双重引导**：
        1.  **整体引导（Holistic Guidance）**：使用非自回归的 3D 生成模型作为老师，监督最终场景的全局质量。
        2.  **步进引导（Step-wise Guidance）**：使用因果自回归模型作为老师，监督每一步物体放置的准确性。
*   **物体位姿计算**：通过比较更新前后的场景差异提取表面点，利用 ICP 算法和最小二乘法将原始物体网格拟合到生成的体素区域，确定最终的旋转、缩放和位移。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Objaverse-XL、ABO、3D-FUTURE、HSSD 等约 50 万个 3D 资产。
    *   **布局训练**：使用 3D-FRONT 和 InternScenes（约 1.5 万个高质量室内场景）。
*   **基准测试（Benchmark）**：采用 LayoutVLM 提出的基准。
*   **对比方法**：LayoutGPT、Holodeck、I-Design、LayoutVLM。
*   **评估指标**：无碰撞得分（CF）、边界内得分（IB）、位置/旋转相干性、PSA（物理接地语义对齐）以及推理时间（T）。

### 4. 资源与算力
*   **硬件**：使用了 **16 张 NVIDIA A100 (80GB) GPU**。
*   **训练时长**：
    *   基础模型训练：400K 步。
    *   教师模型微调：100K 步（文中另一处提到 20 轮或 20K 步，以补充材料为准）。
    *   蒸馏阶段：5K 步。
*   **模型规模**：采用了 30 亿参数（3B）的扩散 Transformer（DiT）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主成分对比实验（针对 8-10 个物体的复杂场景）。
    *   **消融实验**：系统验证了身份感知嵌入、整体引导和步进引导的有效性。
    *   **应用扩展**：展示了在布局补全（Layout Completion）和布局编辑（Layout Editing）上的表现。
    *   **用户研究**：邀请 43 名参与者进行了 430 次主观评估。
*   **充分性评价**：实验设计较为全面，涵盖了定量指标、定性视觉对比、消融分析及人类主观评价，客观地证明了其在物理合理性上的显著优势。

### 6. 主要结论与发现
*   **物理合理性飞跃**：LaviGen 在物理合理性指标上比 SOTA 方法提升了 **19%**，显著减少了物体碰撞和越界。
*   **效率提升**：推理速度比 LayoutVLM 快约 **65%**，且无需耗时的迭代优化。
*   **原生 3D 优势**：直接在 3D 空间操作使得模型能够处理复杂的空间交互，并自然支持布局编辑和补全任务。

### 7. 优点
*   **范式创新**：将 3D 生成先验引入布局任务，摆脱了 LLM 纯文本生成的局限。
*   **物理一致性强**：通过原生 3D 建模，从根本上解决了物体“打架”或“漂浮”的问题。
*   **多功能性**：一个框架同时支持生成、补全和编辑，具有很强的实用潜力。
*   **训练稳定性**：提出的双引导蒸馏机制有效缓解了长序列生成的误差累积。

### 8. 不足与局限
*   **分辨率限制**：目前采用 64³ 的体素网格，对于极小物体（如小摆件）的分辨率不足，可能导致拟合误差。
*   **语义一致性仍有提升空间**：虽然物理性极佳，但在极其复杂的文本指令理解上，有时仍略逊于纯 LLM 方法（受限于 3D 场景标注数据的质量和规模）。
*   **数据依赖**：高性能依赖于高质量的 3D 场景数据集，而这类数据的获取成本远高于文本或图像。

（完）
