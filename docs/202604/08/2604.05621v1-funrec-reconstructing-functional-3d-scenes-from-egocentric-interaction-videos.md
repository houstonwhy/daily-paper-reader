---
title: "FunRec: Reconstructing Functional 3D Scenes from Egocentric Interaction Videos"
title_zh: FunRec：从第一视角交互视频中重建功能性三维场景
authors: "Alexandros Delitzas, Chenyangguang Zhang, Alexey Gavryushin, Tommaso Di Mario, Boyang Sun, Rishabh Dabral, Leonidas Guibas, Christian Theobalt, Marc Pollefeys, Francis Engelmann, Daniel Barath"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05621v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 从交互视频中重建功能性3D场景
tldr: FunRec 是一种从第一视角 RGB-D 交互视频中重建功能性 3D 场景的方法。与依赖受控环境或 CAD 先验的现有方法不同，它能直接处理自然的人类交互序列，自动识别关节部件、估计运动学参数并重建静态与动态几何。该方法在部件分割和运动估计精度上显著优于前人，支持导出为仿真格式，为机器人交互和场景理解提供了新途径。
selection_source: fresh_fetch
motivation: 现有的关节物体重建方法通常依赖受控环境或 CAD 模型，难以处理自然场景下的复杂人类交互视频。
method: 通过分析第一视角视频，自动识别关节部件、估计运动学参数并追踪 3D 运动，在规范空间内重建可交互的 3D 几何模型。
result: "在真实和模拟基准测试中，FunRec 在部件分割 mIoU 上提升了 50%，且运动估计误差降低了 5-10 倍。"
conclusion: FunRec 实现了从野外交互视频到功能性 3D 数字孪生的高效转换，为仿真和机器人场景交互提供了强大的技术支持。
---

## 摘要
我们提出了 FunRec，这是一种直接从第一视角 RGB-D 交互视频中重建室内场景功能性三维数字孪生的方法。与现有的依赖受控设置、多状态捕捉或 CAD 先验的关节重建方法不同，FunRec 直接在野外人类交互序列上运行，以恢复可交互的三维场景。它能自动发现关节部件，估计其运动学参数，跟踪其三维运动，并在规范空间中重建静态和运动几何体，从而生成兼容仿真的网格。在新的真实和模拟基准测试中，FunRec 大幅超越了以往的工作，在部件分割方面实现了高达 +50 mIoU 的提升，关节和姿态误差降低了 5-10 倍，且重建精度显著提高。我们进一步展示了在仿真 URDF/USD 导出、手部引导的可利用性映射以及机器人-场景交互方面的应用。

## Abstract
We present FunRec, a method for reconstructing functional 3D digital twins of indoor scenes directly from egocentric RGB-D interaction videos. Unlike existing methods on articulated reconstruction, which rely on controlled setups, multi-state captures, or CAD priors, FunRec operates directly on in-the-wild human interaction sequences to recover interactable 3D scenes. It automatically discovers articulated parts, estimates their kinematic parameters, tracks their 3D motion, and reconstructs static and moving geometry in canonical space, yielding simulation-compatible meshes. Across new real and simulated benchmarks, FunRec surpasses prior work by a large margin, achieving up to +50 mIoU improvement in part segmentation, 5-10 times lower articulation and pose errors, and significantly higher reconstruction accuracy. We further demonstrate applications on URDF/USD export for simulation, hand-guided affordance mapping and robot-scene interaction.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FunRec** 的系统，旨在从单目第一视角（Egocentric）RGB-D 交互视频中重建具有功能性的三维数字孪生（Digital Twins）。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从人类在真实环境中的自然交互视频中，自动恢复场景的静态几何以及其中关节物体（如门、抽屉、冰箱）的运动学参数（轴线、运动类型、范围）和动态几何。
*   **研究动机**：
    *   **静态局限性**：现有的 3D 重建方法（如 ScanNet）大多只关注静态场景，忽略了物体如何随交互而改变状态，这对机器人感知和具身智能（Embodied AI）至关重要。
    *   **现有方法瓶颈**：现有的关节重建方法通常依赖于受控的实验室环境、已知的 CAD 模型先验，或者需要对同一场景进行多次不同状态的手动扫描，难以应用于非结构化的真实世界（In-the-wild）视频。
    *   **交互即监督**：人类的交互自然地揭示了哪些部件是可动的、运动轴在哪里以及被遮挡的内部结构。

### 2. 方法论（Methodology）
FunRec 是一个**无需训练（Training-free）**、基于优化的流水线，其核心流程如下：
*   **片段构建与分类**：利用视觉语言模型（VLM，如 Gemini）将视频划分为静态和动态片段，并识别交互类型（平移或旋转）。
*   **动态片段处理**：
    *   **相机位姿估计**：结合 RoMA 特征匹配和 SupeRANSAC，在排除手部和交互物体干扰的情况下估计相机轨迹。
    *   **运动聚类**：利用 TAPIP3D 提取稀疏 3D 轨迹，通过拟合直线（平移）或圆弧（旋转）模型，并使用 HDBSCAN 聚类，将轨迹归属到特定的关节部件。
    *   **像素级分割**：将 3D 轨迹投影回 2D，结合 SAM 和 SAM2 的视频传播能力，生成精确的部件分割掩码。
*   **联合优化**：
    *   建立位姿图，通过 Ceres Solver 联合优化部件的 6D 位姿和运动学参数（轴线 $a$、枢轴点 $p$、运动状态 $\lambda$ 或 $\theta$）。
*   **重建与融合**：
    *   使用双 TSDF（截断符号距离函数）体积：一个用于静态背景，一个用于在规范空间（Canonical Space）中重建关节部件。
    *   **全局对齐**：利用 PREDATOR 算法将不同片段的子地图对齐到统一坐标系下。

### 3. 实验设计
*   **数据集**：
    1.  **HOI4D**：现有的实验室环境数据集，包含单物体交互。
    2.  **RealFun4D（新提出）**：包含 351 个真实世界视频，涵盖 4 个国家的 60 个公寓，具有高度多样性。
    3.  **OmniFun4D（新提出）**：基于 OmniGibson 渲染的 127 个高质量光线追踪模拟序列，提供完美真值。
*   **对比方法**：
    *   4D 重建基准：MonST3R, SpatialTrackerV2。
    *   6D 物体追踪：BundleSDF。
    *   关节重建：ArtGS。
*   **评估指标**：轴线误差（角度/位置）、运动状态误差、6D 位姿精度（ADD/ADD-S）、重建质量（Chamfer Distance）以及分割 mIoU。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **性质分析**：由于 FunRec 是一个**基于优化（Optimization-based）**而非基于深度学习训练的方法，它不需要大规模的预训练算力，但其推理过程涉及 VLM 调用、SAM2 传播和非线性最小二乘优化（Ceres Solver），这些步骤在处理长视频时仍需一定的计算资源。

### 5. 实验数量与充分性
*   **实验规模**：在三个数据集上进行了广泛测试，评估了 60 个具有代表性的交互序列。
*   **充分性**：实验涵盖了从模拟到真实、从单物体到全场景的多种情况。
*   **客观性**：论文不仅对比了最先进的基准，还通过提供真值深度（GT Depth）等方式公平地测试了基准方法的上限。结果显示 FunRec 在部件分割上提升了约 50 mIoU，运动误差降低了 5-10 倍，证明了其稳健性。

### 6. 主要结论
*   **性能领先**：FunRec 在所有指标上均大幅超越现有方法，尤其是在处理真实世界中的遮挡和复杂相机运动时表现优异。
*   **功能性恢复**：成功实现了从视频到“可模拟”场景的转化，能够自动生成 URDF/USD 格式文件。
*   **下游应用**：证明了该模型可直接用于机器人（如 Boston Dynamics Spot）学习人类演示并重复交互任务。

### 7. 优点
*   **端到端功能性**：不仅重建几何，还恢复了运动学逻辑，直接打通了“视频-仿真-机器人”的链路。
*   **无需训练**：利用基础模型（VLM, SAM2）的泛化能力，无需针对特定类别进行训练。
*   **鲁棒性**：通过联合优化和双 TSDF 机制，有效处理了交互过程中的严重遮挡和动态模糊。

### 8. 不足与局限
*   **依赖深度输入**：该方法目前依赖 RGB-D 输入，对于纯 RGB 视频的适用性未充分讨论（虽然可以结合深度估计模型，但精度会受损）。
*   **计算延迟**：由于涉及多步骤优化和 VLM 调用，目前可能无法达到实时处理。
*   **复杂关节限制**：主要针对平移和旋转两种基本关节，对于更复杂的链式关节或非刚性变形物体的处理能力尚待验证。
*   **VLM 依赖**：第一步的片段分类和类型识别高度依赖 VLM 的推理能力，若 VLM 误判，后续流程将受影响。

（完）
