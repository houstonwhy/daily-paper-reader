---
title: "SEM-ROVER: Semantic Voxel-Guided Diffusion for Large-Scale Driving Scene Generation"
title_zh: SEM-ROVER：用于大规模驾驶场景生成的语义体素引导扩散模型
authors: "Hiba Dahmani, Nathan Piasco, Moussab Bennehar, Luis Roldão, Dzmitry Tsishkou, Laurent Caraffa, Jean-Philippe Tarel, Roland Brémond"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06113v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 语义体素引导的扩散模型用于大规模驾驶场景生成
tldr: SEM-ROVER 是一种用于大规模驾驶场景生成的 3D 生成框架。针对现有方法在几何一致性和规模扩展上的局限，该研究提出了基于 Σ-Voxfield 网格的离散表示法，并结合语义引导的扩散模型和 3D 位置编码来捕捉空间结构。通过渐进式空间外扩技术，该框架实现了大规模、多视角一致的城市户外场景生成，且无需针对每个场景进行优化，在保持较低计算成本的同时生成了高质量的真实感图像。
selection_source: fresh_fetch
motivation: 现有的驾驶场景生成方法难以在保持 3D 几何一致性的同时，扩展到大规模户外环境。
method: 提出基于 Σ-Voxfield 网格的语义引导扩散模型，利用局部体素邻域和 3D 位置编码进行生成，并通过渐进式外扩实现大规模场景构建。
result: 实验证明该方法能生成多样化的大规模城市场景，支持多种传感器配置和相机轨迹，且具备良好的多视角一致性。
conclusion: SEM-ROVER 为大规模、高质量且具有几何一致性的 3D 驾驶场景生成提供了一种高效且可扩展的解决方案。
---

## 摘要
室外驾驶场景的可扩展生成需要能够在多个视角间保持一致并扩展到大区域的 3D 表示。现有解决方案要么依赖于蒸馏到 3D 空间的图像或视频生成模型，这会损害几何一致性并将渲染限制在训练视角，要么局限于小规模 3D 场景或以物体为中心的生成。在这项工作中，我们提出了一个基于 $Σ$-Voxfield 网格的 3D 生成框架，这是一种离散表示，其中每个被占据的体素存储固定数量的有色表面样本。为了生成这种表示，我们训练了一个语义条件的扩散模型，该模型在局部体素邻域上运行，并使用 3D 位置编码来捕捉空间结构。我们通过在重叠区域进行渐进式空间外扩（outpainting）来扩展到大规模场景。最后，我们使用延迟渲染模块渲染生成的 $Σ$-Voxfield 网格以获得逼真的图像，从而实现无需逐场景优化的大规模多视角一致 3D 场景生成。大量实验表明，我们的方法可以生成多样化的大规模城市室外场景，并能以各种传感器配置和摄像机轨迹渲染成逼真的图像，同时与现有方法相比保持了适中的计算成本。

## Abstract
Scalable generation of outdoor driving scenes requires 3D representations that remain consistent across multiple viewpoints and scale to large areas. Existing solutions either rely on image or video generative models distilled to 3D space, harming the geometric coherence and restricting the rendering to training views, or are limited to small-scale 3D scene or object-centric generation. In this work, we propose a 3D generative framework based on $Σ$-Voxfield grid, a discrete representation where each occupied voxel stores a fixed number of colorized surface samples. To generate this representation, we train a semantic-conditioned diffusion model that operates on local voxel neighborhoods and uses 3D positional encodings to capture spatial structure. We scale to large scenes via progressive spatial outpainting over overlapping regions. Finally, we render the generated $Σ$-Voxfield grid with a deferred rendering module to obtain photorealistic images, enabling large-scale multiview-consistent 3D scene generation without per-scene optimization. Extensive experiments show that our approach can generate diverse large-scale urban outdoor scenes, renderable into photorealistic images with various sensor configurations and camera trajectories while maintaining moderate computation cost compared to existing approaches.

---

## 论文详细总结（自动生成）

以下是对论文《SEM-ROVER: Semantic Voxel-Guided Diffusion for Large-Scale Driving Scene Generation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持 3D 几何一致性的同时，实现大规模（如 100,000 $m^2$）且具有真实感的驾驶场景生成。
*   **研究背景**：
    *   **现有方法的局限**：基于图像/视频的扩散模型虽能生成逼真画面，但缺乏持久的 3D 结构，导致多视角不一致；基于 3D 卷积的方法在处理大规模高分辨率场景时，计算和内存开销呈指数级增长，难以扩展。
    *   **需求**：仿真、数据合成和场景编辑需要一种既能保证 3D 一致性，又能无限扩展，且支持自由视角渲染的生成框架。

### 2. 方法论
论文提出了一种名为 **SEM-ROVER** 的框架，其核心技术包括：
*   **$\Sigma$-Voxfield 表示法**：这是一种离散的表面近似表示。每个被占据的体素（Voxel）存储固定数量（$n=20$）的有色表面样本点。这种表示法将几何与光度属性耦合，且固定基数的特性使其能直接作为 Transformer 的 Token 处理。
*   **语义引导的 3D 扩散模型**：
    *   使用 **1D 扩散 Transformer (DiT)** 在局部体素邻域上运行。
    *   **输入**：将 $\Sigma$-Voxfield 中的点坐标和 RGB 值堆叠为特征向量。
    *   **条件**：引入语义标签（如道路、建筑、植被）和 3D 正弦位置编码，使模型感知空间结构。
*   **渐进式空间外扩（Outpainting）**：为了生成大规模场景，模型不一次性处理全量数据，而是通过 **RePaint** 策略在重叠的局部区域间迭代生成。这种方式使单步计算量保持恒定，从而实现无限场景扩展。
*   **延迟渲染模块（Deferred Rendering）**：
    *   先将生成的 $\Sigma$-Voxfield 通过 2D 高斯泼溅（2DGS）渲染为初步 3D 缓冲图。
    *   再利用基于 Stable Diffusion 的延迟渲染器（ASD 或 VSD 变体）进行增强，补全天空、远景并提升图像真实感，同时确保时间一致性。

### 3. 实验设计
*   **数据集**：使用了两个主流自动驾驶数据集：**Waymo Open Dataset (WOD)** 和 **PandaSet**。
*   **基准方法（Benchmarks）**：
    *   **InfiniCube**：基于 HDMap 引导的体素到视频生成模型。
    *   **GEN3C**：基于视频扩散和 3D 缓存的通用场景生成方法。
*   **评估指标**：
    *   **图像质量**：FID、KID（针对观测视角和偏移后的新视角）。
    *   **3D 特征空间**：使用 F3D 和 MMD 指标评估生成的 3D 点云分布。

### 4. 资源与算力
*   **算力设备**：
    *   **扩散模型训练**：使用 2 台 24GB 显存的 GPU（性能等同于 **RTX 4090**），训练时长约 **4 天**。
    *   **延迟渲染器（ASD）**：在 1 台 GPU 上微调约 **4 天**。
    *   **延迟渲染器（VSD）**：在 1 台拥有 156GB 显存的高端 GPU 上训练约 **1 周**。
*   **推理效率**：生成一个场景约需 20 分钟，显存占用仅为 **8 GB**（远低于 InfiniCube 的 75 GB 和 GEN3C 的 43 GB）。

### 5. 实验数量与充分性
*   **实验规模**：在 WOD 和 PandaSet 上各使用了 60 个白天的序列进行训练，总计约 45 万个训练样本。
*   **消融实验**：针对语义引导、点排序策略等核心组件进行了消融研究，验证了各部分的必要性。
*   **应用展示**：展示了语义编辑（增删车辆）、局部修复（Inpainting）和超大规模场景（10 万平方米）生成的案例。
*   **评价**：实验设计较为全面，涵盖了定性对比、定量指标及下游应用，对比实验在相同数据集下进行，具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **3D 一致性优越**：通过直接在 3D 空间生成 $\Sigma$-Voxfield，模型在多视角和长轨迹下的几何稳定性显著优于基于视频插值的方法。
*   **可扩展性强**：空间外扩策略成功解耦了生成成本与场景规模的关系，证明了在有限算力下生成超大规模环境的可行性。
*   **语义控制有效**：模型能准确遵循语义布局生成对应的几何和纹理，支持精细的场景编辑。

### 7. 优点（亮点）
*   **创新的表示法**：$\Sigma$-Voxfield 巧妙地平衡了点云的灵活性与体素的结构化，适合 Transformer 处理。
*   **低显存需求**：相比竞争对手，其推理显存占用极低（8GB），更易于在消费级硬件上部署。
*   **无需逐场景优化**：采用前馈生成流程，避免了类似 3DGS 或 NeRF 常见的耗时逐场景优化过程。

### 8. 不足与局限
*   **静态场景限制**：目前仅专注于静态背景生成，未包含动态物体（如行驶的车辆、行人）及其时序演化。
*   **外观控制力有限**：虽然有语义引导，但对光照、材质、天气等细粒度风格属性缺乏显式的控制参数。
*   **生成速度**：尽管计算量恒定，但 20 分钟的生成时间对于实时交互应用仍显稍慢。

（完）
