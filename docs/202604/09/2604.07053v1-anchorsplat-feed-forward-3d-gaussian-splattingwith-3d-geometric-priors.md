---
title: "AnchorSplat: Feed-Forward 3D Gaussian SplattingWith 3D Geometric Priors"
title_zh: AnchorSplat：基于 3D 几何先验的前馈 3D 高斯泼溅
authors: "Xiaoxue Zhang, Xiaoxu Zheng, Yixuan Yin, Tiao Zhao, Kaihua Tang, Michael Bi Mi, Zhan Xu, Dave Zhenyu Chen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07053v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 最先进的前馈3D场景重建
tldr: 针对现有前馈高斯重建模型过度依赖像素对齐导致表示受限的问题，本文提出 AnchorSplat。该框架利用 3D 几何先验（如稀疏点云或体素）引导锚点对齐的高斯表示，实现了独立于图像分辨率和视角数量的场景级重建。通过引入高斯精炼器进一步优化，该方法在减少高斯基元数量的同时，显著提升了渲染质量和视图一致性，在 ScanNet++ 等基准测试中达到 SOTA 水平。
selection_source: fresh_fetch
motivation: 现有的像素对齐前馈模型将高斯表示与输入图像紧密耦合，限制了其在复杂场景下的几何感知能力和计算效率。
method: 提出一种由 3D 几何先验引导的锚点对齐高斯表示法，并结合高斯精炼器通过少量前馈计算对中间结果进行优化。
result: 在 ScanNet++ v2 评估中表现优异，以更少的高斯基元实现了更具视图一致性的高质量重建效果。
conclusion: AnchorSplat 证明了将 3D 几何先验引入前馈高斯重建能有效提升场景级建模的效率与保真度。
---

## 摘要
最近的前馈高斯重建模型采用了像素对齐的方案，将每个 2D 像素映射到一个 3D 高斯，从而使高斯表示与输入图像紧密耦合。在本文中，我们提出了 AnchorSplat，这是一种用于场景级重建的新型前馈 3DGS 框架，它直接在 3D 空间中表示场景。AnchorSplat 引入了一种由 3D 几何先验（如稀疏点云、体素或 RGB-D 点云）引导的锚点对齐高斯表示，从而实现了更具几何感知能力的可渲染 3D 高斯，且该表示独立于图像分辨率和视图数量。这种设计显著减少了所需的高斯数量，在提高计算效率的同时增强了重建保真度。除了锚点对齐设计外，我们还利用高斯精炼器（Gaussian Refiner）仅通过几次前馈过程来调整中间高斯。在 ScanNet++ v2 NVS 基准测试上的实验表明，该方法达到了 SOTA 性能，在保持更高视图一致性的同时，使用了显著更少的高斯原语，优于以往的方法。

## Abstract
Recent feed-forward Gaussian reconstruction models adopt a pixel-aligned formulation that maps each 2D pixel to a 3D Gaussian, entangling Gaussian representations tightly with the input images. In this paper, we propose AnchorSplat, a novel feed-forward 3DGS framework for scene-level reconstruction that represents the scene directly in 3D space. AnchorSplat introduces an anchor-aligned Gaussian representation guided by 3D geometric priors (e.g., sparse point clouds, voxels, or RGB-D point clouds), enabling a more geometry-aware renderable 3D Gaussians that is independent of image resolution and number of views. This design substantially reduces the number of required Gaussians, improving computational efficiency while enhancing reconstruction fidelity. Beyond the anchor-aligned design, we utilize a Gaussian Refiner to adjust the intermediate Gaussiansy via merely a few forward passes. Experiments on the ScanNet++ v2 NVS benchmark demonstrate the SOTA performance, outperforming previous methods with more view-consistent and substantially fewer Gaussian primitives.

---

## 论文详细总结（自动生成）

以下是对论文《AnchorSplat: Feed-Forward 3D Gaussian Splatting With 3D Geometric Priors》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的前馈（Feed-Forward）3D 高斯泼溅（3DGS）模型大多采用“像素对齐”方案，即每个像素映射为一个 3D 高斯。这种方式导致高斯表示与输入图像分辨率、视角数量深度耦合，存在以下缺陷：
    *   **冗余与不足并存**：平坦区域高斯过多，复杂区域覆盖不足。
    *   **几何不一致**：缺乏 3D 空间内的邻域交互，容易产生浮点伪影（floaters）和破碎表面。
    *   **效率低下**：随着视角增加，高斯原语数量线性增长，计算开销巨大。
*   **研究动机**：提出一种直接在 3D 空间中表示场景的“锚点对齐”框架，利用 3D 几何先验引导重建，实现更高效、更具几何感知能力的场景级重建。

### 2. 方法论
AnchorSplat 框架由三个核心组件组成：
*   **锚点预测器 (Anchor Predictor)**：
    *   利用预训练的多视图立体（MVS）模型（如 MapAnything）从输入图像预测深度图和相机位姿。
    *   将像素反投影到 3D 空间，并使用最远点采样（FPS）算法进行下采样，生成稀疏的 3D 锚点（Anchors）。
*   **高斯解码器 (Gaussian Decoder)**：
    *   使用 CNN 提取多视图特征，并将其投影到 3D 锚点上。
    *   采用基于 Transformer 的架构处理锚点特征，建模 3D 空间交互。
    *   通过 MLP 预测每个锚点对应的 4 个高斯属性（中心偏移、不透明度、缩放、旋转、球谐系数）。
*   **高斯精炼器 (Gaussian Refiner)**：
    *   一个即插即用的模块，通过计算渲染图像与真实图像之间的误差，利用 Point Transformer 进一步微调高斯属性，提升细节保真度和视图一致性。

### 3. 实验设计
*   **数据集**：主要在 **ScanNet++ v2**（场景级 NVS 基准）上进行评估。此外还在 **Replica**、**ARKitScenes** 和 **Tanks and Temples (T&T)** 等室内外数据集上进行了泛化性测试。
*   **对比方法**：
    *   **优化类方法**：3DGS、Mip-Splatting。
    *   **前馈类方法**：AnySplat（目前最先进的体素对齐前馈模型）。
*   **评估指标**：RGB 质量（PSNR, SSIM, LPIPS）、深度准确性（AbsRel, $\delta_1$）、效率（高斯数量、重建时间）。

### 4. 资源与算力
*   **硬件**：使用了 **64 台 Ascend 910B3 (64GB) NPU**。
*   **训练细节**：
    *   第一阶段（解码器）：训练 5k 步（模型参数量 84M）。
    *   第二阶段（精炼器）：训练 5k 步（模型参数量 31M）。
    *   精度：使用 bfloat16 混合精度训练。
*   **推理效率**：在 ScanNet++ 上，重建时间约为 3.9s - 6.1s（视输入视角数量而定），显著快于优化类方法。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从极稀疏（3-5 视图）到极密集（128-256 视图）的多种输入场景。
*   **消融实验**：非常充分，分别验证了：
    1.  输入模态（RGB、深度、相机射线）的影响。
    2.  每个锚点生成高斯数量的影响（最终选定为 4）。
    3.  不同数据集的泛化表现。
    4.  特征聚合策略（均值池化效果最好）。
    5.  不同 MVS 后端（MapAnything vs DA3）的影响。
*   **客观性**：通过与 SOTA 方法 AnySplat 的直接可视化对比和定量指标对比，证明了其在减少 20 倍高斯数量的情况下仍能获得更好的几何结构。

### 6. 主要结论与发现
*   **锚点对齐的优越性**：相比像素对齐，锚点对齐能显著减少冗余高斯，使表示独立于图像分辨率。
*   **几何一致性**：引入 3D 几何先验后，重建的表面更干净，有效消除了浮点伪影和重影。
*   **效率提升**：AnchorSplat 在保持高保真度的同时，高斯数量仅为 AnySplat 的约 1/20，重建速度提升了约一倍。
*   **精炼器的作用**：Gaussian Refiner 能有效修复解码器生成的初始空洞，增强边界锐度。

### 7. 优点（亮点）
*   **解耦设计**：成功将 3D 表示与 2D 输入像素解耦，解决了前馈模型中高斯数量随视角线性增长的痛点。
*   **几何感知**：通过 3D 空间内的 Transformer 交互，实现了比像素对齐方法更稳健的几何一致性。
*   **灵活性**：框架支持集成各种 3D 信号（点云、体素等），且精炼器模块是可选的即插即用组件。

### 8. 不足与局限
*   **对先验的依赖**：模型性能高度依赖于初始 MVS 模块提供的几何先验。如果初始深度预测存在大规模缺失或严重错误，受限的高斯生长范围可能无法覆盖这些“空洞”区域。
*   **静态场景限制**：目前主要针对静态场景重建，尚未讨论在动态场景或大规模城市级场景中的表现。
*   **计算资源要求**：虽然推理快，但训练过程仍需要大量的 NPU/GPU 算力支持。

（完）
