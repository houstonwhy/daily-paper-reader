---
title: "Dream-Cubed: Controllable Generative Modeling in Minecraft by Training on Billions of Cubes"
title_zh: Dream-Cubed：通过在数十亿个方块上训练实现 Minecraft 中的可控生成建模
authors: "Tim Merino, Sam Earle, Ryunosuke Iwai, Julian Togelius, Edoardo Cetin"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.22847v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 交互式3D环境的生成式建模
tldr: 本研究提出 Dream-Cubed，一个包含数百亿体素的大规模 Minecraft 世界数据集，并开发了一系列基于立方体单元的 3D 扩散模型。该模型直接在方块空间操作，支持高效且具有语义基础的交互式生成（如补全和扩展）。通过对离散和连续扩散公式、数据组合及架构设计的深入研究，实现了高质量、可控的 3D 环境生成，并开源了数据集和模型。
selection_source: fresh_fetch
motivation: 旨在解决现有 3D 生成模型在构建大规模、可交互且具有语义一致性的结构化环境方面的效率和可控性挑战。
method: 构建了包含数百亿体素的大规模数据集，并研究了在方块空间直接运行的离散与连续 3D 扩散模型架构。
result: 模型能够实现高质量的 3D 环境生成，支持交互式补全与扩展，并通过改进的 FID 指标和人类偏好实验验证了其优越性。
conclusion: Dream-Cubed 为结构化、可交互 3D 环境的高效生成奠定了基础，并为未来相关领域的研究提供了开源资源。
---

## 摘要
我们介绍了 Dream-Cubed，这是一个体素分辨率的大规模 Minecraft 世界数据集，以及一系列利用方块作为强大组合单元来高效生成交互式 3D 环境的模型。Dream-Cubed 包含数百亿个 token，这些 token 源自经过精心策划的程序化生物群落（biome）地形和高质量人类创作地图的混合。我们利用该数据集对用于体素生成的 3D 扩散模型进行了首次大规模研究，分析了离散和连续扩散公式、数据组成以及架构设计选择。我们的模型直接在方块空间中运行，在实现高效且具有语义基础的生成的同时，还支持交互式用户工作流，例如基于用户创作方块的补全（inpainting）和扩展（outpainting）。为了定量评估我们的模型，我们调整了 FID 指标以评估真实世界渲染图与生成世界渲染图之间的语义差异，并通过人类偏好研究验证了生成质量。我们发布了完整的数据集、代码和所有预训练模型，希望这能为结构化、交互式 3D 环境的高效生成建模的未来研究奠定基础。

## Abstract
We introduce Dream-Cubed, a large-scale dataset of Minecraft worlds at voxel resolution, and a family of models using cubes as powerful compositional units for efficient generation of interactive 3D environments. Dream-Cubed comprises tens of billions of tokens from a carefully curated mixture of procedural biome terrain and high-quality human-authored maps. We use this dataset to conduct the first large-scale study of 3D diffusion models for voxel generation, analyzing discrete and continuous diffusion formulations, data compositions, and architectural design choices. Our models operate directly in the space of blocks, enabling efficient and semantically grounded generation while supporting interactive user workflows such as inpainting and outpainting from user-authored blocks. To quantitatively evaluate our models, we adapt the FID metric to assess semantic differences between real and generated world renderings, and validate generation quality through a human preference study. We release the full dataset, code, and all our pretrained models, which we hope will provide a foundation for future research in efficient generative modeling for structured, interactive 3D environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Dream-Cubed** 的项目，旨在通过大规模数据集和 3D 扩散模型实现 Minecraft 世界的高效、可控生成。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究动机**：尽管生成式 AI 在图像、视频和文本领域取得了巨大进步，但在**交互式 3D 环境**（如 Minecraft）的生成上关注较少。Minecraft 的世界由具有语义意义的“方块”（体素）组成，是研究结构化、可编辑 3D 内容的理想平台。
*   **核心问题**：如何构建大规模、高质量的体素数据集，并开发出既能保持语义一致性又能支持用户交互控制（如补全、扩展）的生成模型？

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将 Minecraft 的方块直接视为“Token”，在方块空间（而非像素或潜空间）直接进行 3D 扩散建模。
*   **关键技术**：
    *   **数据集构建**：创建了包含 200 多万个 32×32×32 块（Chunk）的数据集，涵盖程序化地形（15 种生物群落）、洞穴、村庄以及专业人类玩家创作的地图。
    *   **模型架构**：采用 **3D 扩散 Transformer (DiT)** 作为骨干网络（约 2.8 亿参数），利用自适应层归一化（AdaLN）注入生物群落标签和时间步信息。
    *   **两种扩散公式对比**：
        1.  **离散掩码扩散 (MD4)**：将方块视为类别 Token，通过随机掩码和迭代取消掩码来生成。
        2.  **连续空间 DDPM**：利用 OpenAI 的文本嵌入模型将方块名称转化为 16 维向量，在连续嵌入空间进行加噪和去噪。
    *   **可控生成工作流**：利用 MD4 的特性，通过固定部分方块作为硬约束，实现**局部补全 (Inpainting)**、**跨生物群落混合**以及**滑动窗口式的大规模世界扩展 (Outpainting)**。

### 3. 实验设计
*   **数据集/场景**：
    *   **核心集**：166 万个程序化地形块。
    *   **人类增强集**：35 万个来自专业建筑师的高质量地图块。
*   **对比实验**：
    *   **模型对比**：MD4 vs. DDPM。
    *   **补丁大小 (Patch Size)**：对比了 $p=2$ 和 $p=4$ 的性能。
    *   **数据组成**：对比了“平衡分布”、“自然发生分布”和“村庄增强分布”对生成质量的影响。
*   **评估指标**：
    *   **FID (Fréchet Inception Distance)**：将 3D 块渲染为 2D 图像后计算语义差异。
    *   **人类偏好研究**：招募 19 名有经验的玩家进行双盲对比测试。
    *   **分布指标**：Jensen-Shannon 散度和词汇覆盖率。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA H100 GPU** 进行分布式训练。
*   **训练时长**：总计约 **192 GPU 小时**。
    *   Patch 2 模型训练了 20 个 Epoch。
    *   Patch 4 模型训练了 160 个 Epoch（以保证看到的 Token 总数一致）。
*   **推理成本**：Patch 2 模型生成一个块约需 2.5 分钟，Patch 4 约需 25 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，包括扩散算法选择、Patch 大小敏感度、数据集配比等。
*   **充分性与客观性**：实验设计较为全面，不仅有自动化的 FID 指标，还引入了人类主观评价来验证 FID 的有效性。通过对比“自然分布”和“人工策划分布”，客观揭示了数据多样性对结构化生成的重要性。

### 6. 主要结论与发现
*   **算法表现**：离散扩散 (MD4) 和连续扩散 (DDPM) 在生成质量上表现相当，但 **MD4 在可控性（如硬约束补全）上更具天然优势**。
*   **Patch 大小**：较小的 Patch ($p=2$) 能显著提升结构化内容（如村庄、建筑）的生成质量。
*   **人类偏好**：人类受试者在多数情况下认为模型生成的块质量**优于或等同于** Minecraft 原生的程序化地形（可能是因为分类器自由引导 CFG 强化了生物群落的特征）。
*   **数据策略**：针对复杂结构（如村庄）进行定向过采样能显著提升该类别的生成效果。

### 7. 优点（亮点）
*   **直接方块操作**：跳过了复杂的像素渲染或潜空间编码，生成的输出直接在游戏中可用，且支持精确的方块级控制。
*   **大规模开源**：发布了首个包含数百亿体素的 Minecraft 数据集及预训练模型，填补了领域空白。
*   **强大的泛化力**：模型能够处理用户手动绘制的极端模式（如螺旋线、火山），并生成连贯的补全地形。

### 8. 不足与局限
*   **推理速度慢**：扩散模型的迭代采样导致生成速度较慢，难以实现实时的交互式编辑。
*   **版本限制**：数据集基于 Minecraft 1.12.2 版本，未包含后续版本更新的新生物群落和方块。
*   **评估局限**：FID 指标基于 2D 渲染，难以完全捕捉 3D 内部结构（如房屋内部是否空心、洞穴的连通性）和功能性（如门是否可达）。
*   **伦理风险**：训练数据包含人类创作者的作品，虽然获得了许可，但大规模生成此类内容可能对社区创作生态产生冲击。

（完）
