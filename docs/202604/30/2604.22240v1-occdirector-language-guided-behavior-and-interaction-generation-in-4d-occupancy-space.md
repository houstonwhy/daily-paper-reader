---
title: "OccDirector: Language-Guided Behavior and Interaction Generation in 4D Occupancy Space"
title_zh: OccDirector：4D 占用空间中语言引导的行为与交互生成
authors: "Zhuding Liang, Tianyi Yan, Dubing Chen, Jiasen Zheng, Huan Zheng, Cheng-zhong Xu, Yida Wang, Kun Zhan, Jianbing Shen"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22240v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于驾驶模拟的语言引导4D占据网格生成
tldr: 本研究针对自动驾驶模拟中4D占用空间生成依赖几何先验且缺乏复杂交互控制的问题，提出了OccDirector框架。该框架仅凭自然语言指令即可生成物理合理的4D体素动态，充当“场景导演”角色。技术上采用VLM驱动的时空MMDiT及历史前缀锚定策略，确保长时交互一致性。此外，研究还推出了包含85k条多级语言标注的OccInteract数据集。实验证明，OccDirector在生成质量和指令遵循方面达到了SOTA水平，实现了从外观合成到语言驱动行为编排的范式转变。
selection_source: fresh_fetch
motivation: 现有的4D占用生成模型过度依赖刚性几何条件，难以通过自然语言实现复杂的多智能体时空交互编排。
method: 提出基于VLM驱动的时空MMDiT架构，并结合历史前缀锚定策略，将自然语言脚本直接映射为连贯的4D体素动态。
result: 在包含85k条标注的OccInteract数据集上，该方法在生成质量和指令遵循能力上均显著优于现有技术。
conclusion: OccDirector成功将4D占用生成从单纯的外观合成提升到了语言驱动的行为编排新高度。
---

## 摘要
生成式世界模型日益依赖 4D 占用（4D occupancy）来实现逼真的自动驾驶模拟。然而，现有的生成框架依赖于僵化的几何条件（如显式轨迹）或简单的属性级文本，无法编排复杂的、序列化的多智能体交互。为了弥补这一语义-时空鸿沟，我们提出了 OccDirector，这是一个仅以自然语言为条件生成 4D 占用动态的开创性框架。OccDirector 充当“场景导演”，将自然语言剧本映射为符合物理规律的体素动态，而无需几何先验。在技术上，它采用了一种由视觉语言模型（VLM）驱动的时空 MMDiT，并配备了历史前缀锚定策略，以确保长时程交互的一致性。此外，我们推出了 OccInteract-85k，这是一个独特标注了多级语言指令的新型数据集：涵盖了从静态布局到复杂的多智能体行为，并配套了一个基于 VLM 的新型评估基准。大量实验表明，OccDirector 实现了最先进的生成质量和前所未有的指令遵循能力，成功地将范式从外观合成转向了语言驱动的行为编排。

## Abstract
Generative world models increasingly rely on 4D occupancy for realistic autonomous driving simulation. However, existing generation frameworks depend on rigid geometric conditions (e.g., explicit trajectories) or simplistic attribute-level text, failing to orchestrate complex, sequential multi-agent interactions. To address this semantic-spatiotemporal gap, we propose OccDirector, a pioneering framework that generates 4D occupancy dynamics conditioned solely on natural language. Operating as a ``scenario director'', OccDirector maps natural language scripts into physically plausible voxel dynamics without requiring geometric priors. Technically, it employs a VLM-driven Spatio-Temporal MMDiT equipped with a history-prefix anchoring strategy to ensure long-horizon interaction consistency. Furthermore, we introduce OccInteract-85k, a novel dataset uniquely annotated with multi-level language instructions: ranging from static layouts to intricate multi-agent behaviors, alongside a novel VLM-based evaluation benchmark. Extensive experiments demonstrate that OccDirector achieves state-of-the-art generation quality and unprecedented instruction-following capabilities, successfully shifting the paradigm from appearance synthesis to language-driven behavior orchestration.

---

## 论文详细总结（自动生成）

这是一份关于论文《OccDirector: Language-Guided Behavior and Interaction Generation in 4D Occupancy Space》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：自动驾驶（AD）模拟需要逼真的 4D 占用空间（4D Occupancy）生成模型。然而，现有方法存在两大局限：
    1.  **依赖刚性几何条件**：如必须预设精确的 BEV 布局或车辆轨迹，这在编排复杂交互时非常繁琐且不直观。
    2.  **语义-时空鸿沟**：现有的文本驱动模型多使用 CLIP 等简单的文本编码器，仅能处理天气、时间等全局属性，无法理解“超车后紧急制动”等复杂的时空逻辑和多智能体交互。
*   **核心问题**：如何仅通过自然语言脚本（Script）作为“导演指令”，生成物理合理、时空连贯且具备复杂交互能力的 4D 占用动态场景。

### 2. 方法论
OccDirector 框架将 4D 生成任务建模为受语言约束的体素流匹配过程：
*   **联合潜空间表示**：
    *   **占用空间令牌化**：使用预训练的 Occ-VAE 将离散的 4D 占用网格压缩为连续的潜变量，并进行补丁化（Patchify）处理。
    *   **VLM 语义提取**：采用冻结的视觉语言模型（VLM，如 Qwen3-VL）作为编码器，捕捉复杂的程序化语义。
    *   **令牌精炼器（Token Refiner）**：通过轻量级双向 Transformer 桥接 VLM 特征与空间布局之间的模态鸿沟。
*   **可扩展 4D 生成骨干网络**：
    *   **时空 MMDiT**：基于多模态扩散 Transformer 架构，采用**时空分离注意力（STSA）**机制。先进行帧内 3D 空间联合注意力（文本与体素交互），再进行跨帧的时间自注意力，解决了 4D 数据计算量巨大的瓶颈。
*   **交互一致性预测（历史前缀锚定策略）**：
    *   **训练阶段**：引入随机令牌替换策略，将部分噪声序列替换为干净的历史帧，强制模型学习从已知历史预测未知未来的能力。
    *   **推理阶段**：采用步进式锚定（Step-wise Anchoring），在每一步 ODE 求解时强制覆盖历史前缀，确保生成的未来动态与给定背景严格一致。

### 3. 实验设计
*   **数据集：OccInteract-85k**。这是论文贡献之一，包含三个子集：
    1.  **Static**：单帧布局标注（73.5k）。
    2.  **Env**：环境交互剪辑（7.6k，如路口转弯）。
    3.  **Agent**：多智能体交互（4.6k，包含紧急避让等高风险场景）。
*   **对比方法**：
    *   几何驱动 SOTA：DOME, COME, DynamicCity。
    *   文本驱动基准：将 DynamicCity 的控制端替换为 CLIP 或 T5 编码器。
*   **评估指标**：
    *   **视觉质量**：IS, FID, KID（单帧）；FVD（视频）。
    *   **指令遵循**：提出基于 VLM（如 Gemini-3.0-pro）的自动评分基准，从场景完整性、结构正确性、语义对齐三个维度打分。

### 4. 资源与算力
*   **硬件环境**：推理资源测试使用了 **NVIDIA A100 (80GB)**。
*   **训练细节**：训练迭代次数为 100k 次，Batch Size 为 32，学习率为 1e-4。
*   **效率表现**：得益于 STSA 机制，显存占用随视频帧数线性增长，且对文本长度（L）不敏感（L 从 77 增加到 2048，显存增加小于 0.1%）。

### 5. 实验数量与充分性
*   **实验规模**：在 85k 条样本的大规模数据集上进行了验证。
*   **消融实验**：针对 STSA 架构、令牌精炼器、历史锚定比例、CFG 尺度等关键组件做了详尽的对比。
*   **客观性保障**：
    *   引入了**用户研究（User Study）**，证明 VLM 自动评分与人类主观感受高度相关（相关系数 0.88）。
    *   使用了**跨 VLM 验证**（GPT-5.2, Gemini, Qwen），证明结论在不同评价模型下均成立。
    *   实验设计涵盖了从静态到动态、从常规到安全关键（Safety-critical）的多种场景，较为全面。

### 6. 主要结论与发现
*   **性能领先**：OccDirector 在视频生成质量（FVD 275）上显著优于基准方法（如 DynamicCity+CLIP 的 FVD 为 388）。
*   **语义理解力强**：能够处理否定词（如“没有其他车辆”）、基数词以及复杂的相对运动逻辑（如“切入”、“超车”）。
*   **长时一致性**：历史锚定策略有效解决了长序列生成中的“场景漂移”问题，支持多阶段叙事生成。
*   **范式转变**：证明了 4D 占用生成可以从“轨迹工程”转向更高效、更自然的“语言编排”。

### 7. 优点（亮点）
*   **首创性**：第一个实现纯文本引导的、具备复杂交互能力的 4D 占用生成框架。
*   **架构优雅**：STSA 机制在保持高性能的同时显著降低了 4D 数据的计算开销。
*   **数据贡献**：OccInteract-85k 数据集及其自动化标注管线为该领域提供了重要的基础设施。
*   **评估创新**：利用 VLM 作为裁判，解决了 4D 占用数据难以通过传统指标衡量语义对齐度的难题。

### 8. 不足与局限
*   **数据伪影**：受限于原始训练数据（如 nuScenes），生成的场景中可能继承传感器的盲区或自车体素缺失。
*   **超长序列挑战**：虽然有锚定策略，但在极长序列（如超过 8 秒）生成中，物理一致性的累积误差仍是一个挑战。
*   **语义粒度限制**：目前仅支持 11 类语义标签，尚无法模拟转向灯、刹车灯或行人的细微手势等高阶交互信号。
*   **VLM 偏见**：自动评估模型（如 GPT-5.2）可能存在主观偏见或对非真实感体素数据的理解偏差。

（完）
