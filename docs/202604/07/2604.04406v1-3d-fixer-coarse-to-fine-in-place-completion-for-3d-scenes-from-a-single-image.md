---
title: "3D-Fixer: Coarse-to-Fine In-place Completion for 3D Scenes from a Single Image"
title_zh: 3D-Fixer：从单张图像进行 3D 场景的由粗到细原位补全
authors: "Ze-Xin Yin, Liu Liu, Xinjie Wang, Wei Sui, Zhizhong Su, Jian Yang, Jin Xie"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04406v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 单视图组合式3D场景生成
tldr: 针对单视图3D场景生成中布局恢复与资产补全的挑战，本文提出3D-Fixer。该方法采用原位补全范式，利用粗糙几何作为空间锚点，通过粗到精的生成方案和遮挡鲁棒特征对齐策略，在无需显式姿态对齐的情况下实现高质量3D资产补全。此外，发布了大规模数据集ARSG-110K，实验证明其在精度和效率上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有方法在复杂场景的泛化能力与姿态优化的计算效率之间存在难以调和的矛盾。
method: 提出一种原位补全范式，利用双分支条件网络和粗到精的生成策略，以局部点云为锚点直接生成完整的3D资产。
result: 在包含11万场景的大规模数据集ARSG-110K上，该方法在几何准确度上显著优于MIDI和Gen3DSR等主流模型。
conclusion: 3D-Fixer通过结合生成先验与原位补全技术，实现了高效且高保真的单图3D场景重建。
---

## 摘要
从单视图进行组合式 3D 场景生成需要同时恢复场景布局和 3D 资产。现有方法主要分为两类：前馈生成方法和逐实例生成方法。前者通过高效的网络推理直接预测具有显式 6DoF 位姿的 3D 资产，但在复杂场景中的泛化能力较差；后者通过分而治之的策略提高了泛化能力，但面临耗时的位姿优化问题。为了弥补这一差距，我们引入了 3D-Fixer，一种新型的原位补全范式。具体而言，3D-Fixer 扩展了 3D 物体生成先验，以原始位置的部分可见点云为条件生成完整的 3D 资产，这些点云是从几何估计方法获得的碎片化几何中裁剪出来的。与之前需要显式位姿对齐的工作不同，3D-Fixer 使用碎片化几何作为空间锚点，以保持布局的忠实度。其核心是，我们提出了一种由粗到细的生成方案，以解决遮挡下的边界歧义，并辅以双分支条件网络和遮挡鲁棒特征对齐（ORFA）策略以实现稳定训练。此外，为了解决数据稀缺瓶颈，我们提出了 ARSG-110K，这是迄今为止最大的场景级数据集，包含超过 11 万个多样化场景和 300 万张具有高保真 3D 真值的标注图像。大量实验表明，3D-Fixer 达到了最先进的几何精度，显著优于 MIDI 和 Gen3DSR 等基准方法，同时保持了扩散过程的效率。代码和数据将在 https://zx-yin.github.io/3dfixer 公开。

## Abstract
Compositional 3D scene generation from a single view requires the simultaneous recovery of scene layout and 3D assets. Existing approaches mainly fall into two categories: feed-forward generation methods and per-instance generation methods. The former directly predict 3D assets with explicit 6DoF poses through efficient network inference, but they generalize poorly to complex scenes. The latter improve generalization through a divide-and-conquer strategy, but suffer from time-consuming pose optimization. To bridge this gap, we introduce 3D-Fixer, a novel in-place completion paradigm. Specifically, 3D-Fixer extends 3D object generative priors to generate complete 3D assets conditioned on the partially visible point cloud at the original locations, which are cropped from the fragmented geometry obtained from the geometry estimation methods. Unlike prior works that require explicit pose alignment, 3D-Fixer uses fragmented geometry as a spatial anchor to preserve layout fidelity. At its core, we propose a coarse-to-fine generation scheme to resolve boundary ambiguity under occlusion, supported by a dual-branch conditioning network and an Occlusion-Robust Feature Alignment (ORFA) strategy for stable training. Furthermore, to address the data scarcity bottleneck, we present ARSG-110K, the largest scene-level dataset to date, comprising over 110K diverse scenes and 3M annotated images with high-fidelity 3D ground truth. Extensive experiments show that 3D-Fixer achieves state-of-the-art geometric accuracy, which significantly outperforms baselines such as MIDI and Gen3DSR, while maintaining the efficiency of the diffusion process. Code and data will be publicly available at https://zx-yin.github.io/3dfixer.

---

## 论文详细总结（自动生成）

以下是对论文《3D-Fixer: Coarse-to-Fine In-place Completion for 3D Scenes from a Single Image》的结构化总结：

### 1. 核心问题与整体含义
该论文关注的是**单视图组合式 3D 场景生成**。其核心挑战在于如何同时恢复场景的复杂布局和高质量的 3D 资产。
*   **研究动机**：现有的前馈生成方法（如 MIDI）虽然高效，但在复杂场景中泛化能力差；而“分而治之”的逐实例方法（如 Gen3DSR）虽然泛化性好，但依赖耗时的位姿优化且容易产生对齐错误。
*   **核心思想**：提出 **3D-Fixer**，引入一种**原位补全（In-place Completion）**范式。它不进行显式的位姿对齐，而是直接以几何估计模型生成的“碎片化点云”为空间锚点，在原始位置补全被遮挡的几何结构。

### 2. 方法论
3D-Fixer 扩展了预训练的物体级生成先验（基于 TRELLIS 架构），主要包含以下关键技术：
*   **原位补全范式**：利用几何基础模型（如 MoGe v2）提取可见部分的碎片几何，将其作为条件输入，直接在场景坐标系下生成完整物体。
*   **双分支条件网络**：一个分支保持原始物体生成先验，另一个可训练分支处理场景级的 2D（图像特征）和 3D（碎片点云）上下文线索。
*   **由粗到细（Coarse-to-Fine）生成方案**：
    1.  **粗结构补全**：在扩大的边界框内预测物体的准确边界（AABB）。
    2.  **精细形状细化**：在预测的精确边界内生成高分辨率几何。
    3.  **遮挡感知纹理化**：应用与观察视角对齐的光泽纹理。
*   **遮挡鲁棒特征对齐（ORFA）**：针对遮挡导致的训练不稳定，引入知识蒸馏损失，利用冻结的清洁输入模型作为教师，引导模型在遮挡下保持结构合理性。

### 3. 实验设计
*   **数据集**：
    *   **ARSG-110K（本文贡献）**：包含 11 万个场景、300 万张图像、18 万个高质量资产，是目前最大的组合式 3D 场景数据集。
    *   **测试集**：MIDI 测试集、Gen3DSR 测试集、ScanNet（真实场景）、Toys4K（合成场景）。
*   **基准方法（Baselines）**：对比了 MIDI（扩散模型）、Gen3DSR（优化模型）、Total3D、InstPIFu、SSR、DiffCAD 等。
*   **评估指标**：场景级和物体级的倒角距离（CD）、F-Score（FS）、边界框 IoU 以及推理时间。

### 4. 资源与算力
论文在补充材料中详细说明了算力使用情况：
*   **GPU 型号与数量**：使用了 **32 台 NVIDIA RTX 5090 GPU**。
*   **训练时长**：
    *   第一阶段模型微调：150K 步。
    *   第二阶段模型微调：450K 步。
    *   场景级训练（Coarse/Fine/Texture 模块）：各约 80K-90K 步。
    *   总训练规模巨大，体现了该模型对算力的高需求。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的定量和定性实验。定量实验覆盖了 4 个不同的合成和真实数据集；消融实验针对网络层数、粗到细策略、对齐损失（AL）、深度比例嵌入等关键组件进行了验证。
*   **充分性与公平性**：实验对比了从早期几何方法到最新扩散方法的多种路径，并在真实世界场景（ScanNet）和复杂户外场景上验证了泛化性，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能领先**：3D-Fixer 在几何准确度上显著优于现有 SOTA 方法（如在 MIDI 测试集上，物体级 F-Score 从 53.58 提升至 94.39）。
*   **效率优势**：相比需要数分钟优化的 Gen3DSR，3D-Fixer 仅需约 30 秒即可完成场景生成，保持了前馈扩散的高效性。
*   **布局忠实度**：通过原位补全范式，模型能够更好地利用几何先验，避免了传统方法中常见的位姿漂移问题。

### 7. 优点
*   **范式创新**：提出的“原位补全”有效解决了单视图场景重建中“对齐难”的痛点。
*   **数据贡献**：发布的 ARSG-110K 数据集填补了高质量、大规模场景级 3D 数据的空白。
*   **鲁棒性强**：通过 ORFA 和粗到细策略，在处理严重遮挡和边界歧义时表现出色。

### 8. 不足与局限
*   **依赖初始几何**：该方法的效果高度依赖于前端几何估计模型（如 MoGe v2）的质量。如果初始碎片点云存在严重畸变，补全效果会受限。
*   **算力门槛高**：训练过程依赖大量顶级 GPU（32x 5090），普通研究机构难以复现或微调。
*   **端到端程度**：目前仍是一个多阶段流水线（分解-补全-细化-纹理），未来若能实现更紧密的端到端整合可能进一步提升效率。

（完）
