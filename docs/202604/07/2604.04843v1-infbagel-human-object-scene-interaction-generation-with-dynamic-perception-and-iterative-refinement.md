---
title: "InfBaGel: Human-Object-Scene Interaction Generation with Dynamic Perception and Iterative Refinement"
title_zh: InfBaGel：具有动态感知和迭代细化的人-物-场景交互生成
authors: "Yude Zou, Junji Gong, Xing Gao, Zixuan Li, Tianxing Chen, Guanjie Zheng"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04843v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 具有动态感知的入-物-场景交互生成
tldr: 本研究针对人-物-场景交互（HOSI）生成中动态变化推理难和标注数据稀缺的问题，提出了InfBaGel框架。该框架采用粗到精的指令驱动生成方式，结合一致性模型的迭代去噪过程，通过动态感知策略实时更新场景上下文。此外，引入碰撞感知引导以减少物理伪影，并利用混合训练策略解决数据不足。实验证明，该方法在HOSI和HOI生成任务上均达到SOTA水平，且具有极强的泛化能力。
selection_source: fresh_fetch
motivation: 现有的HOSI生成研究在处理动态物体-场景变化时推理能力不足，且面临高质量标注数据匮乏的挑战。
method: 提出一种基于一致性模型的迭代细化框架，结合动态感知策略、碰撞感知引导以及合成伪HOSI数据的混合训练方法。
result: 实验结果显示，该方法在HOSI和HOI生成指标上均优于现有技术，并能有效减少物理穿透且支持实时生成。
conclusion: InfBaGel通过动态上下文感知和高效的物理约束，实现了高质量、实时的复杂人-物-场景交互生成。
---

## 摘要
人-物-场景交互（HOSI）生成在具身智能、模拟和动画领域具有广泛的应用。与人-物交互（HOI）和人-场景交互（HSI）不同，HOSI 生成需要对动态的物体-场景变化进行推理，但面临标注数据有限的问题。为了解决这些问题，我们提出了一种由粗到精的指令条件交互生成框架，该框架与一致性模型（consistency model）的迭代去噪过程显式对齐。具体而言，我们采用了一种动态感知策略，利用前一次细化的轨迹来更新场景上下文，并在一致性模型的每个去噪步骤中为后续细化提供条件，从而产生一致的交互。为了进一步减少物理伪影，我们引入了一种碰撞感知引导，在不需要细粒度场景几何的情况下，减轻采样过程中的碰撞和穿透，从而实现实时生成。为了克服数据稀缺，我们设计了一种混合训练策略，通过将体素化的场景占用注入到 HOI 数据集中来合成伪 HOSI 样本，并与高保真 HSI 数据进行联合训练，在保留真实场景感知的同时进行交互学习。大量的实验表明，我们的方法在 HOSI 和 HOI 生成方面均达到了最先进的性能，并且对未见场景具有很强的泛化能力。项目主页：https://yudezou.github.io/InfBaGel-page/

## Abstract
Human-object-scene interactions (HOSI) generation has broad applications in embodied AI, simulation, and animation. Unlike human-object interaction (HOI) and human-scene interaction (HSI), HOSI generation requires reasoning over dynamic object-scene changes, yet suffers from limited annotated data. To address these issues, we propose a coarse-to-fine instruction-conditioned interaction generation framework that is explicitly aligned with the iterative denoising process of a consistency model. In particular, we adopt a dynamic perception strategy that leverages trajectories from the preceding refinement to update scene context and condition subsequent refinement at each denoising step of consistency model, yielding consistent interactions. To further reduce physical artifacts, we introduce a bump-aware guidance that mitigates collisions and penetrations during sampling without requiring fine-grained scene geometry, enabling real-time generation. To overcome data scarcity, we design a hybrid training startegy that synthesizes pseudo-HOSI samples by injecting voxelized scene occupancy into HOI datasets and jointly trains with high-fidelity HSI data, allowing interaction learning while preserving realistic scene awareness. Extensive experiments demonstrate that our method achieves state-of-the-art performance in both HOSI and HOI generation, and strong generalization to unseen scenes. Project page: https://yudezou.github.io/InfBaGel-page/

---

## 论文详细总结（自动生成）

以下是对论文《InfBaGel: Human-Object-Scene Interaction Generation with Dynamic Perception and Iterative Refinement》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**人-物-场景交互（HOSI）生成**这一复杂任务。现有的研究多集中在单纯的人-物交互（HOI）或人-场景交互（HSI），但在处理三者同时存在的HOSI时面临两大挑战：
*   **动态物体-场景变化：** 当人移动大型物体时，场景布局会随之改变，传统的静态场景编码无法捕捉这种演变，导致交互不一致。
*   **标注数据稀缺：** 捕获高质量、多样化的HOSI运动数据成本极高，现有数据集在物体种类、场景配置和任务指令上存在局限，限制了模型的泛化能力。
**研究目标：** 开发一个能够根据高层指令（如文本、目标位置）生成物理一致、实时且具有强泛化能力的HOSI生成框架。

### 2. 方法论
InfBaGel 采用了一种**由粗到精（Coarse-to-Fine）**的自回归生成架构，核心技术包括：
*   **动态感知编码器（Dynamic Perception Encoder）：** 不同于一次性编码静态场景，该模型在迭代去噪过程中，利用前一步生成的轨迹实时更新场景的体素（Voxel）状态，捕捉环境的动态变化。
*   **一致性模型（Consistency Model, CM）：** 将扩散模型蒸馏为一致性模型，使其能在极少数步数（1-16步）内生成高质量运动。这为实时迭代细化提供了可能，因为每一步都能直接输出“清晰”的运动预测用于更新场景感知。
*   **碰撞感知引导（Bump-aware Guidance）：** 在采样阶段引入轻量级引导，利用预计算的体素距离场将人体和物体“推离”障碍物，无需复杂的网格碰撞检测即可减少物理穿透。
*   **混合数据训练策略（Hybrid Data Training）：** 
    *   将HSI数据（提供真实场景几何）与合成的HOSI数据结合。
    *   **合成方法：** 在HOI数据中，根据人体和物体的运动轨迹提取占用空间，并将其余空间体素化为障碍物，从而在无标注的情况下生成伪HOSI样本。

### 3. 实验设计
*   **数据集与场景：**
    *   **HOSI Benchmark（测试集）：** 包含469个序列，涉及7类大型可操纵物体，置于67个来自TRUMANS数据集的未见（Unseen）室内场景中。
    *   **训练集：** 混合了OMOMO（HOI数据集）和LINGO（HSI数据集）。
*   **对比方法：** 与 TRUMANS 和 LINGO 等最先进的场景感知基线方法进行对比。
*   **评估指标：** 任务成功率（S%）、目标距离（Th/To）、足部滑动（FS）、人体-物体穿透（Pbody）、场景穿透深度（Pmean/max）以及生成速度（FPS）。

### 4. 资源与算力
*   **硬件：** 使用了 **4 张 NVIDIA A100 GPU**。
*   **训练细节：** 训练时长约为 500 个 epoch，采用 Adam 优化器，学习率为 0.0001，Batch Size 为 2048。
*   **推理效率：** 在开启碰撞引导的情况下，生成速度可达 **28.75 FPS**，实现了实时生成。

### 5. 实验数量与充分性
*   **实验规模：** 论文在HOSI和标准HOI（OMOMO）两个基准上进行了广泛测试。
*   **消融实验：** 针对动态感知（DP）、碰撞引导（G）、一致性模型（CM）以及混合数据比例进行了详尽的消融研究。
*   **充分性评价：** 实验设计客观且公平。通过在67个完全未见的场景中进行测试，验证了模型的零样本（Zero-shot）泛化能力。对比实验涵盖了物理真实性、任务准确性和计算效率多个维度，论证充分。

### 6. 主要结论与发现
*   **性能领先：** InfBaGel 在 HOSI 任务上的成功率达到 **83.16%**，远超基线方法 LINGO（53.09%）和 TRUMANS（1.92%）。
*   **动态感知的必要性：** 动态更新场景上下文能显著减少运动过程中的碰撞，提高复杂任务的完成度。
*   **效率与质量并重：** 一致性模型不仅加速了推理，其提供的清晰中间预测对于准确更新动态场景状态至关重要。
*   **数据策略奏效：** 混合训练策略成功解决了HOSI数据匮乏问题，使模型能够从HOI数据中学习交互细节，从HSI数据中学习场景避障。

### 7. 优点（亮点）
*   **实时性：** 结合一致性模型，克服了传统扩散模型推理慢的痛点，支持实时交互生成。
*   **强泛化性：** 采用体素化表示和混合训练，使其能直接应用于从未见过的复杂室内环境。
*   **物理一致性：** 碰撞感知引导在不增加过多计算负担的前提下，显著改善了人体/物体与场景的穿透问题。

### 8. 不足与局限
*   **精度权衡：** 引入碰撞引导虽然减少了穿透，但在某些情况下会略微降低到达精确目标位置的准确度（存在物理约束与目标达成之间的权衡）。
*   **体素分辨率限制：** 依赖体素表示可能无法捕捉极细微的几何特征（如细小的开关或把手）。
*   **数据依赖：** 尽管使用了混合训练，但模型性能仍受限于合成场景逻辑的合理性，对于极度复杂的动态环境（如多个移动物体相互干扰）可能仍有挑战。

（完）
