---
title: "M2StyleGS: Multi-Modality 3D Style Transfer with Gaussian Splatting"
title_zh: M2StyleGS：基于高斯泼溅的多模态 3D 风格迁移
authors: "Xingyu Miao, Xueqi Qiu, Haoran Duan, Yawen Huang, Xian Wu, Jingjing Deng, Yang Long"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03773v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 使用文本描述和3DGS进行场景的3D风格迁移
tldr: "针对传统3D风格迁移仅支持固定参考图像的局限，本文提出M2StyleGS，一种基于3D高斯泼溅（3DGS）的多模态实时风格化技术。该方法利用CLIP提取文本或图像的风格特征，并通过细分流实现精确的特征对齐。此外，引入观测损失和抑制损失以提升风格匹配度并减少颜色偏移。实验证明，该方法在视觉质量和一致性上显著优于现有技术，一致性提升达32.92%。"
selection_source: fresh_fetch
motivation: 旨在解决传统3D风格迁移输入形式单一的问题，满足用户对文本和多样化图像等灵活输入的需求。
method: 结合3DGS表示与CLIP多模态知识，通过细分流特征对齐及观测与抑制损失函数实现精确的风格映射。
result: "实验结果显示M2StyleGS在视觉质量上表现优异，且在视图一致性方面比前序工作提升了高达32.92%。"
conclusion: M2StyleGS成功实现了支持多模态输入的实时3D场景风格迁移，显著增强了生成视图的艺术效果与时空一致性。
---

## 摘要
传统的 3D 风格迁移方法依赖于固定的参考图像将艺术图案应用于 3D 场景。然而，在虚拟现实或增强现实等实际应用中，用户往往更倾向于更灵活的输入方式，包括文本描述和多样化的图像。在这项工作中，我们提出了一种名为 M2StyleGS 的新型实时风格化技术，用于生成一系列精确颜色映射的视图。该技术利用 3D 高斯泼溅（3DGS）作为 3D 表示，并使用由 CLIP 提炼的多模态知识作为参考风格。M2StyleGS 通过采用一种名为细分流（subdivisive flow）的精确特征对齐方式解决了异常变换问题，增强了映射后的 CLIP 文本-视觉组合特征向 VGG 风格特征的投影。此外，我们引入了观察损失（observation loss）以帮助风格化场景在生成过程中更好地匹配参考风格，以及抑制损失（suppression loss）以在整个解码过程中抑制参考颜色信息的偏移。通过整合这些方法，M2StyleGS 可以利用文本或图像作为参考来生成一组风格增强的新视图。实验表明，M2StyleGS 实现了更好的视觉质量，并在一致性方面比以往的工作提升了高达 32.92%。

## Abstract
Conventional 3D style transfer methods rely on a fixed reference image to apply artistic patterns to 3D scenes. However, in practical applications such as virtual or augmented reality, users often prefer more flexible inputs, including textual descriptions and diverse imagery. In this work, we introduce a novel real-time styling technique M2StyleGS to generate a sequence of precisely color-mapped views. It utilizes 3D Gaussian Splatting (3DGS) as a 3D presentation and multi-modality knowledge refined by CLIP as a reference style. M2StyleGS resolves the abnormal transformation issue by employing a precise feature alignment, namely subdivisive flow, it strengthens the projection of the mapped CLIP text-visual combination feature to the VGG style feature. In addition, we introduce observation loss, which assists in the stylized scene better matching the reference style during the generation, and suppression loss, which suppresses the offset of reference color information throughout the decoding process. By integrating these approaches, M2StyleGS can employ text or images as references to generate a set of style-enhanced novel views. Our experiments show that M2StyleGS achieves better visual quality and surpasses the previous work by up to 32.92% in terms of consistency.

---

## 论文详细总结（自动生成）

### M2StyleGS: 基于高斯泼溅的多模态 3D 风格迁移论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的 3D 风格迁移方法通常局限于使用单一的参考图像作为风格来源。然而，在 VR/AR 等实际应用中，用户需要更灵活的输入方式（如文本描述或多样化的图像组合）。现有的多模态方法（如 ConRF）在将 CLIP 特征空间映射到 VGG 风格空间时，由于特征分布对齐过于粗糙，常导致生成结果出现颜色异常、纹理模糊以及风格与描述不符的问题。本文旨在通过 3D 高斯泼溅（3DGS）技术，实现一种能够精确响应文本或图像指令、且具备高视觉一致性的实时 3D 风格迁移方案。

#### 2. 核心方法论
M2StyleGS 的核心思想是利用 3DGS 的显式表示能力，结合 CLIP 的多模态知识进行风格精炼。
*   **细分流（Subdivisive Flow）特征对齐**：这是本文的核心创新。作者认为 CLIP 和 VGG 的特征分布不一致，简单的线性映射（MSE 损失）效果较差。因此，引入了基于常微分方程（ODE）的流匹配方法，将 CLIP 特征流向 VGG 特征空间。通过分阶段（Round）的欧拉法模拟，精确引导特征投影，解决了颜色畸变问题。
*   **AdaIN 风格转换**：利用对齐后的特征，通过自适应实例归一化（AdaIN）模块将风格应用到 3D 高斯的颜色嵌入中。
*   **增强型辅助损失函数**：
    *   **观测损失（Observation Loss）**：引入预训练 2D 风格迁移模型作为“观察者”，引导 3D 场景生成的视图在视觉上接近高质量的 2D 艺术作品。
    *   **抑制损失（Suppression Loss）**：利用多尺度判别器，在解码过程中全局抑制偏离参考风格的错误颜色偏移。

#### 3. 实验设计
*   **数据集/场景**：使用了 3D 视觉领域常用的 **LLFF** 数据集和 **Tanks & Temples** 数据集。
*   **对比方法（Baselines）**：
    *   **图像引导类**：StyleRF, StyleGaussian, ConRF。
    *   **文本引导类**：CLIPStyler (2D), CLIPNeRF, ConRF。
*   **评估指标**：使用 **LPIPS** 和 **RMSE** 衡量短程和长程的视图一致性；使用 **SIM**（余弦相似度）和 **FID** 衡量特征对齐的准确性。

#### 4. 资源与算力
论文正文部分提到该方法支持“实时渲染”和“即时风格化”，但**未明确说明**具体的硬件配置（如 GPU 型号、数量）及具体的训练时长。相关细节通常在论文附录中提供，但在提取的文本中未包含。

#### 5. 实验数量与充分性
*   **实验规模**：论文展示了多个场景（如 Fern, Trex, Flower 等）的定性对比图，并提供了详尽的定量数据表。
*   **消融实验**：针对“细分流”的轮数（Round）以及两种辅助损失函数（$L_{obs}$ 和 $L_{sup}$）分别进行了消融研究。
*   **评价**：实验设计较为全面，涵盖了从 2D 到 3D、从图像到文本的横向对比，通过定量指标证明了其在一致性上的显著提升（最高达 32.92%），实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   M2StyleGS 成功实现了基于 3DGS 的多模态风格迁移，能够生成高质量、高一致性的艺术化 3D 场景。
*   **细分流**方法比传统的直接映射能更有效地对齐跨模型（CLIP 到 VGG）的特征分布。
*   引入 2D 观测先验和全局抑制损失能显著提升 3D 场景在不同视角下的色彩稳定性。

#### 7. 优点
*   **多模态灵活性**：同时支持文本和图像输入，极大地扩展了用户创作的自由度。
*   **高一致性**：相比于基于 NeRF 的方法，结合 3DGS 并在特征层进行精确对齐，显著减少了视角切换时的闪烁和伪影。
*   **技术创新**：将 ODE 流匹配引入风格特征对齐，为解决跨域特征转换提供了新思路。

#### 8. 不足与局限
*   **依赖预训练模型**：观测损失依赖于现有的 2D 风格迁移模型，如果 2D 模型本身存在偏差，可能会影响 3D 效果。
*   **计算开销**：虽然渲染是实时的，但细分流的多轮迭代对齐在训练阶段可能会增加额外的计算负担。
*   **复杂语义理解**：尽管使用了 CLIP，但在处理极其复杂或抽象的文本描述时，可能仍存在风格理解的局限性。

（完）
