---
title: "ScenarioControl: Vision-Language Controllable Vectorized Latent Scenario Generation"
title_zh: "ScenarioControl: 视觉-语言可控的矢量化潜空间场景生成"
authors: "Lili Gao, Yanbo Xu, William Koch, Samuele Ruffino, Luke Rowe, Behdad Chalaki, Dmitriy Rivkin, Julian Ost, Roger Girgis, Mario Bijelic, Felix Heide"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17147v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 视觉语言可控的3D场景生成
tldr: ScenarioControl是首个支持视觉-语言控制的驾驶场景生成框架。它通过文本或图像引导，在矢量化潜空间中合成包含地图、动态交通参与者及传感器观测的真实3D场景。该方法引入了跨全局控制机制，有效结合了多模态指令与稀疏矢量元素，实现了对道路布局和交通状况的精细控制，并支持长时程、时空一致的场景生成，为自动驾驶仿真提供了高保真、可控的方案。
selection_source: fresh_fetch
motivation: 现有的驾驶场景生成方法缺乏灵活的多模态控制手段，难以根据具体的文本或图像指令生成精确且真实的矢量化场景。
method: 提出一种在矢量化潜空间中生成场景的方法，并结合跨注意力机制与轻量级全局上下文分支，实现对稀疏场景元素的多模态精细控制。
result: 实验证明该方法在控制一致性和生成保真度方面均优于现有技术，并能生成长时程且时空一致的驾驶场景。
conclusion: ScenarioControl通过创新的多模态控制机制，为自动驾驶仿真提供了高保真、可控且多样化的场景生成方案。
---

## 摘要
我们介绍了 ScenarioControl，这是首个用于学习型驾驶场景生成的视觉-语言控制机制。给定文本提示或输入图像，ScenarioControl 可以合成多样且逼真的 3D 场景演变——包括地图、随时间变化的交互式参与者 3D 框、行人、驾驶基础设施以及自车摄像头观测。该方法在矢量化潜空间中生成场景，该空间共同表示道路结构和动态智能体。为了将多模态控制与稀疏的矢量化场景元素联系起来，我们提出了一种跨全局控制机制，该机制将交叉注意力与轻量级全局上下文分支相结合，在保持真实性的同时，实现了对道路布局和交通状况的细粒度控制。该方法从场景中不同参与者的视角生成时间一致的场景演变，支持驾驶场景的长时程延续。为了便于训练和评估，我们发布了一个包含与矢量化地图结构对齐的文本注释的数据集。大量实验验证了 ScenarioControl 的控制遵循度和保真度在所有实验中均优于所有测试方法。项目网页：https://light.princeton.edu/ScenarioControl

## Abstract
We introduce ScenarioControl, the first vision-language control mechanism for learned driving scenario generation. Given a text prompt or an input image, Scenario-Control synthesizes diverse, realistic 3D scenario rollouts - including map, 3D boxes of reactive actors over time, pedestrians, driving infrastructure, and ego camera observations. The method generates scenes in a vectorized latent space that represents road structure and dynamic agents jointly. To connect multimodal control with sparse vectorized scene elements, we propose a cross-global control mechanism that integrates crossattention with a lightweight global-context branch, enabling fine-grained control over road layout and traffic conditions while preserving realism. The method produces temporally consistent scenario rollouts from the perspectives different actors in the scene, supporting long-horizon continuation of driving scenarios. To facilitate training and evaluation, we release a dataset with text annotations aligned to vectorized map structures. Extensive experiments validate that the control adherence and fidelity of ScenarioControl compare favorable to all tested methods across all experiments. Project webpage: https://light.princeton.edu/ScenarioControl

---

## 论文详细总结（自动生成）

以下是对论文《ScenarioControl: Vision-Language Controllable Vectorized Latent Scenario Generation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的自动驾驶仿真方法在“真实性”与“可控性”之间存在断层。传统规则驱动仿真（如 CARLA）可控但缺乏真实感；基于生成式模型（如扩散模型）的方法虽能生成逼真图像，但往往缺乏对底层矢量化场景结构（如车道拓扑、智能体交互）的精细控制，且难以通过自然语言或单张图像进行直观引导。
*   **研究动机**：为了安全评估自动驾驶系统，需要生成大量罕见且关键的边缘场景（Edge Cases）。作者旨在开发一种能够通过**文本指令**或**前视图像**直接控制生成完整 3D 矢量化场景（包含地图、动态参与者及传感器观测）的框架。

### 2. 方法论
*   **核心思想**：提出一种**矢量化潜空间扩散模型（Vectorized Latent Diffusion Model）**，将道路结构和动态智能体联合建模，并通过创新的“跨全局控制机制”实现多模态引导。
*   **关键技术细节**：
    *   **场景表示**：在 BEV（鸟瞰图）基础上增加了高度（$z$）和物体高度（$h$）信息，以便后续进行精确的传感器仿真和投影。
    *   **跨全局控制机制（Cross-Global Control）**：
        *   **交叉注意力分支**：处理细粒度的局部控制，将图像/文本特征与稀疏的矢量化 Token 对齐。
        *   **轻量级全局上下文分支**：使用一组可学习的潜 Token 聚合全局意图，捕捉场景的宏观特征（如“十字路口”或“多车道高速”）。
    *   **多模态调节**：
        *   **文本控制**：使用预训练文本编码器提取特征。
        *   **图像控制**：结合视觉主干网络特征与深度估计图，增强空间感知。
    *   **辅助模块**：
        *   **数量预测器（$f_{count}$）**：从控制信号中推断智能体和车道的数量。
        *   **碰撞惩罚项（$L_{col}$）**：在扩散训练中加入几何约束，减少生成的物体重叠。
    *   **下游生成**：生成的矢量场景可输入行为模拟器产生轨迹，再通过 LoRA 微调的视频生成模型（如 Wan 2.2）生成高保真传感器视频。

### 3. 实验设计
*   **数据集**：
    *   **nuPlan**：主要训练和评估集。作者利用 GPT-4.1-mini 为 50 万个场景生成了高质量的文本描述。
    *   **Waymo Open Motion Dataset**：用于验证模型的跨数据集泛化能力。
*   **Benchmark 与对比方法**：
    *   **对比方法**：Scenario Dreamer（SOTA 矢量生成）、TrafficGen（智能体放置）以及多种注意力机制变体（如 Gated, Linear, Deformable Attention 等）。
    *   **评估指标**：智能体准确率（AP）、碰撞率（Collision Rate）、控制遵循度（CCS 相似度、SPG 扰动间隙、CSC 敏感度相关性）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**具体的 GPU 型号、数量及总训练时长。
*   **模型规模**：提到了视频生成部分使用了 **Wan 2.2 (5B)** 模型进行 LoRA 适配，这暗示了推理和微调阶段需要较高的显存支持（通常为 A100 或 H100 级别）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 **50 万条** 文本-场景对进行训练，规模庞大。
    *   进行了详尽的**消融实验**（表 3），对比了 9 种不同的注意力机制，验证了“跨全局控制”的优越性。
    *   包含泛化性实验（Waymo 数据集）和针对特定模块（碰撞损失、数量预测）的功能分析。
*   **充分性评价**：实验设计较为全面，不仅涵盖了定量指标，还通过定性可视化展示了长时程生成和多视角一致性，客观地证明了模型在多模态控制下的鲁棒性。

### 6. 主要结论与发现
*   **控制力显著提升**：在图像和文本条件下，ScenarioControl 在所有控制遵循度指标上均优于基准模型，尤其在智能体放置的准确性上提升明显。
*   **泛化能力强**：模型在未见过的 Waymo 数据集上表现出良好的零样本/微调迁移能力。
*   **结构与视觉统一**：证明了通过矢量化中间表示可以有效地连接抽象的语言指令与具象的传感器视频生成，保持了时空一致性。

### 7. 优点
*   **首创性**：首次实现了视觉-语言双模态控制的矢量化驾驶场景生成。
*   **细粒度控制**：通过跨全局机制解决了稀疏矢量元素与稠密视觉/文本特征之间的对齐难题。
*   **实用性高**：生成的场景直接兼容现有的自动驾驶仿真堆栈（如地图格式、行为模型）。
*   **数据贡献**：发布了大规模的驾驶场景文本标注数据集，填补了行业空白。

### 8. 不足与局限
*   **视野限制**：单张图像条件下的生成受限于相机视角（FOV），视野外的场景补全仍具有随机性。
*   **性能权衡**：引入数量预测器（$f_{count}$）虽然降低了碰撞率，但有时会导致漏掉遮挡或远处的智能体，从而降低 AP。
*   **计算开销**：虽然矢量生成较快，但后续的高保真视频生成（5B 参数模型）在实时性方面可能存在挑战。

（完）
