---
title: Co-generation of Layout and Shape from Text via Autoregressive 3D Diffusion
title_zh: 通过自回归 3D 扩散实现文本驱动的布局与形状协同生成
authors: "Zhenggang Tang, Yuehao Wang, Yuchen Fan, Jun-Kun Chen, Yu-Ying Yeh, Kihyuk Sohn, Zhangyang Wang, Qixing Huang, Alexander Schwing, Rakesh Ranjan, Dilin Wang, Zhicheng Yan"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16552v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 通过扩散模型实现文本到场景的布局与形状生成
tldr: 针对现有文本生成场景方法在布局与物体生成上难以兼顾且一致性差的问题，本文提出 3D-ARD+ 模型。该模型结合了自回归生成与扩散模型，通过两阶段过程（场景空间粗粒度生成与物体空间细粒度生成）实现布局与形状的协同生成。基于 230K 室内场景数据集训练，7B 参数规模的模型在处理复杂空间布局和语义描述方面表现优异，显著提升了交互式场景创建的质量。
selection_source: fresh_fetch
motivation: 现有的文本转场景技术往往无法同时兼顾复杂的空间布局和精细的物体形状，且难以保持与文本描述的一致性。
method: 提出 3D-ARD+ 模型，通过自回归方式生成多模态序列，并结合扩散模型分两步生成场景空间和物体空间的 3D 潜变量。
result: 在包含 230K 个室内场景的大规模数据集上进行训练和评估，证明了模型能够根据复杂的文本指令生成并放置具有非平凡空间布局和语义的物体。
conclusion: 该研究为交互式 3D 场景创建提供了一种新范式，实现了文本驱动下布局与形状的高质量协同生成。
---

## 摘要
最近的文本到场景生成方法大大减少了创建 3D 场景所需的人工。然而，这些方法的重点通常在于生成场景布局或生成物体，很少有方法能同时兼顾两者。即使在 LLM 的帮助下，生成的场景布局往往也比较简单。此外，生成的场景经常与包含物体形状、外观和空间排列等复杂描述的文本输入不一致。我们提出了一种顺序文本到场景生成的新范式，并为交互式场景创建提出了一种新颖的生成模型。其核心是一个 3D 自回归扩散模型 3D-ARD+，它统一了多模态标记序列上的自回归生成和下一个物体 3D 潜变量的扩散生成。为了生成下一个物体，该模型首先使用一个自回归步骤，在当前文本指令和已合成 3D 场景的约束下，在场景空间中生成粗粒度的 3D 潜变量。随后，它使用第二个步骤在较小的物体空间中生成 3D 潜变量，这些潜变量可以被解码为细粒度的物体几何形状和外观。我们策划了一个包含 23 万个室内场景及配对文本指令的大型数据集用于训练。我们在挑战性场景上评估了 7B 规模的 3D-ARD+，并展示了该模型能够遵循文本指令所规定的复杂空间布局和语义来生成并放置物体。

## Abstract
Recent text-to-scene generation approaches largely reduced the manual efforts required to create 3D scenes. However, their focus is either to generate a scene layout or to generate objects, and few generate both. The generated scene layout is often simple even with LLM's help. Moreover, the generated scene is often inconsistent with the text input that contains non-trivial descriptions of the shape, appearance, and spatial arrangement of the objects. We present a new paradigm of sequential text-to-scene generation and propose a novel generative model for interactive scene creation. At the core is a 3D Autoregressive Diffusion model 3D-ARD+, which unifies the autoregressive generation over a multimodal token sequence and diffusion generation of next-object 3D latents. To generate the next object, the model uses one autoregressive step to generate the coarse-grained 3D latents in the scene space, conditioned on both the current seen text instructions and already synthesized 3D scene. It then uses a second step to generate the 3D latents in the smaller object space, which can be decoded into fine-grained object geometry and appearance. We curate a large dataset of 230K indoor scenes with paired text instructions for training. We evaluate 7B 3D-ARD+, on challenging scenes, and showcase the model can generate and place objects following non-trivial spatial layout and semantics prescribed by the text instructions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Co-generation of Layout and Shape from Text via Autoregressive 3D Diffusion》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的文本转 3D 场景生成通常面临“顾此失彼”的问题：要么侧重于生成场景布局（Layout），要么侧重于生成单个物体（Object），很少能同时高质量地完成两者。现有的 LLM 辅助方法生成的布局往往过于简单，且难以遵循文本中关于物体形状、外观及复杂空间关系（如“放在右侧床头柜顶面的后右角”）的非平凡描述。
**核心动机**：提出一种顺序生成的范式，通过自回归扩散模型，根据详细的文本指令逐步在场景中放置具有精确形状和位置的物体，实现布局与形状的协同生成。

### 2. 方法论
论文提出了 **3D-ARD+** 模型，其核心思想是将场景生成视为多模态标记序列的自回归预测任务。
*   **核心架构**：基于 DiT（Diffusion Transformer）架构，拥有 7B 参数。模型同时处理三种 Token：
    *   **文本 Token**：使用 BAGEL 分词器处理指令。
    *   **3D 理解 Token**：将已生成的物体通过 3D VAE 编码为潜变量，作为后续生成的上下文。
    *   **3D 生成 Token**：代表当前步骤要生成的物体潜变量。
*   **关键技术细节**：
    *   **广义因果注意力（Generalized Causal Attention）**：文本间采用标准因果注意力；3D 理解 Token 间采用无限制注意力；生成 Token 对文本和历史 3D Token 采用因果注意力，但其内部采用无限制注意力。
    *   **两阶段生成（3D-ARD+）**：
        1.  **场景空间生成**：在全局场景坐标系下生成粗粒度潜变量（确定位置和大致形状）。
        2.  **物体空间生成**：在局部物体坐标系下生成高分辨率潜变量，以捕捉精细几何和外观。
    *   **算法流程**：采用 **Rectified Flow（整流场）** 匹配目标进行训练。推理时，通过 Euler 采样器迭代去噪，最后使用 3D VAE 和 3D 高斯泼溅（3DGS）解码器输出最终结果。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：策划了一个包含 **230K** 个室内场景（卧室、客厅、厨房等）的大规模数据集，每个场景包含 2-15 个物体。利用 VLM（视觉语言模型）自动生成了分步骤的组装指令。此外还加入了 1M 个单物体资产以增强多样性。
    *   **测试集**：50 组未见过的挑战性顺序指令。
*   **对比方法（Baselines）**：
    *   **TRELLIS-text XL**：目前最强的单物体/简单场景生成模型。
    *   **MIDI**：多实例扩散模型（图像转场景）。
    *   **BB.+TRELLIS**：使用真值边界框配合 TRELLIS 生成。
    *   **LLM-layout+TRELLIS**：用 LLM 预测布局，再用 TRELLIS 填充物体。
*   **评估指标**：KID、KDD（基于 DINOv2）、CLIP-text/image 分数、P-FID（基于点云的 FID）和 P-IS。

### 4. 资源与算力
*   **模型规模**：7B 参数。
*   **算力投入**：使用了 **128 张 Nvidia H100 GPU**。
*   **训练时长**：训练了约 **1 周**（120K 步）。
*   **推理配置**：使用 Euler 采样器，50 步采样，支持 KV 缓存以加速自回归过程。

### 5. 实验数量与充分性
*   **实验规模**：论文在 23 万个场景的大规模数据上训练，并在 50 个复杂测试案例上进行了详尽对比。
*   **消融实验**：对比了 3D-ARD（仅粗粒度）与 3D-ARD+（带细粒度细化）的效果，并研究了单物体数据加入的影响以及 Patch Size 对生成质量的作用。
*   **充分性评价**：实验设计较为全面，涵盖了定量指标和定性视觉对比。通过引入多种 Baseline（包括 LLM 辅助和纯扩散模型），客观地证明了自回归扩散在处理复杂空间关系上的优势。

### 6. 主要结论与发现
*   **协同生成优势**：3D-ARD+ 能够精准遵循复杂的空间指令（如“靠左放置”、“在...之上”），在布局一致性上显著优于 LLM 预测布局的方法。
*   **细化阶段的必要性**：增加物体空间的细化步骤（+ 阶段）能显著减少纹理伪影并提升几何平滑度。
*   **自回归能力**：模型展现了强大的上下文感知能力，能根据已有的物体（如桌子）准确放置后续物体（如台灯、书本）。

### 7. 优点
*   **范式创新**：将场景生成简化为统一的多模态自回归任务，避免了 LLM 布局预测与 3D 生成之间的脱节。
*   **高质量数据管线**：开发了一套自动化的 VLM 管线，将静态 3D 场景转化为顺序的组装指令，解决了训练数据匮乏的问题。
*   **高保真度**：结合了 3DGS 技术，生成的物体在几何细节和外观上达到了 SOTA 水平。

### 8. 不足与局限
*   **空间冲突**：尽管有空间约束，生成的物体间偶尔仍会出现轻微的重叠或缝隙，VAE Token 的粒度可能不足以处理极精细的接触。
*   **初始物体缩放风险**：如果第一个放置的物体（如沙发）尺寸过大，可能会导致后续物体没有足够的空间放置。
*   **推理效率**：由于采用了 7B 规模的 Transformer 架构且需要多步自回归，生成复杂场景的时间比单物体生成器长 5-10 倍。
*   **数据偏差**：主要集中在室内场景，对于室外或超大规模环境的泛化能力尚未验证。

（完）
