---
title: View-Consistent 3D Scene Editing via Dual-Path Structural Correspondense and Semantic Continuity
title_zh: 通过双路径结构对应与语义连续性实现视角一致的三维场景编辑
authors: "Pufan Li, Bi'an Du, Shenghe Zheng, Junyi Yao, Wei Hu"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17801v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 文本驱动的3D场景编辑
tldr: 针对文本驱动的3D场景编辑中多视图不一致的问题，本文提出了一种基于联合分布建模的视图一致性编辑框架。该方法通过双路径一致性机制，即投影引导的结构引导和补丁级语义传播，显式引入跨视图依赖。此外，研究者还构建了配对的多视图编辑数据集以提供可靠监督。实验证明，该方法在复杂场景中实现了精确且高度一致的编辑效果，显著提升了3D场景编辑的质量。
selection_source: fresh_fetch
motivation: 现有的3D场景编辑方法主要依赖推理时的同步机制，难以从根本上解决多视图不一致性问题，且在鲁棒性和泛化性方面存在局限。
method: 提出一种双路径一致性机制，结合投影引导的结构对应和补丁级语义传播，并利用新构建的配对多视图数据集进行监督学习。
result: 大量实验表明，该方法在处理复杂场景时能够生成比现有技术更精确、视图更一致的编辑结果。
conclusion: 通过将3D编辑重新定义为跨视角的联合分布建模，并引入显式的结构与语义约束，本研究为实现高质量、视图一致的3D场景编辑提供了有效方案。
---

## 摘要
文本驱动的三维场景编辑近年来引起了越来越多的关注。大多数现有方法遵循“渲染-编辑-优化”的流程，即从三维场景中渲染多视图图像，使用二维图像编辑器进行编辑，然后用于优化底层三维表示。然而，跨视图不一致性仍然是一个主要的瓶颈。尽管最近的方法引入了几何线索、跨视图交互或视频先验来缓解这一问题，但它们在很大程度上仍依赖于推理时的同步，因此在鲁棒性和泛化性方面仍然受限。在这项工作中，我们从分布的角度重新审视了多视图一致的三维编辑：三维场景编辑本质上需要跨视角的联合分布建模。基于这一见解，我们提出了一个视角一致的三维编辑框架，该框架在编辑过程中显式地引入了跨视图依赖关系。此外，受结构对应和语义连续性依赖于不同跨视图线索这一观察的启发，我们引入了一种双路径一致性机制，由投影引导的结构引导和补丁级语义传播组成，以实现有效的跨视图编辑。此外，我们构建了一个成对的多视图编辑数据集，为学习编辑场景中的跨视图一致性提供了可靠的监督。大量实验表明，我们的方法在复杂场景中实现了卓越的编辑性能，具有精确且一致的视图。

## Abstract
Text-driven 3D scene editing has recently attracted increasing attention. Most existing methods follow a render-edit-optimize pipeline, where multi-view images are rendered from a 3D scene, edited with 2D image editors, and then used to optimize the underlying 3D representation. However, cross-view inconsistency remains a major bottleneck. Although recent methods introduce geometric cues, cross-view interactions, or video priors to mitigate this issue, they still largely rely on inference-time synchronization and thus remain limited in robustness and generalization.In this work, we recast multi-view consistent 3D editing from a distributional perspective: 3D scene editing essentially requires a joint distribution modeling across viewpoints.Based on this insight, we propose a view-consistent 3D editing framework that explicitly introduces cross-view dependencies into the editing process. Furthermore, motivated by the observation that structural correspondence and semantic continuity rely on different cross-view cues, we introduce a dual-path consistency mechanism consisting of projection-guided structural guidance and patch-level semantic propagation for effective cross-view editing. Further, we construct a paired multi-view editing dataset that provides reliable supervision for learning cross-view consistency in edited scenes. Extensive experiments demonstrate that our method achieves superior editing performance with precise and consistent views for complex scenes.

---

## 论文详细总结（自动生成）

### 论文总结：通过双路径结构对应与语义连续性实现视角一致的三维场景编辑

#### 1. 核心问题与整体含义（研究动机和背景）
文本驱动的3D场景编辑通常采用“渲染-编辑-优化”流程，即先将3D场景渲染为2D图像，用2D扩散模型编辑后再反向优化3D表示（如3D Gaussian Splatting）。
*   **核心痛点**：现有的2D编辑器主要针对单图设计，独立编辑多视角图像会导致几何不匹配、外观漂移和语义不连贯（即“多视图不一致性”）。
*   **研究动机**：作者认为3D编辑本质上应是**跨视角的联合分布建模**，而非独立的单图建模。因此，需要一种能够显式引入跨视图依赖关系的框架，从根本上学习视角间的一致性，而非仅在推理阶段进行简单的同步。

#### 2. 方法论：核心思想与关键技术
该方法将一致性拆分为**结构对应（Structural Correspondence）**和**语义连续性（Semantic Continuity）**，并设计了双路径机制：
*   **核心思想**：利用前一视角的编辑结果作为参考，通过几何投影引导结构，通过特征传播引导语义。
*   **关键技术细节**：
    1.  **投影引导的结构路径**：利用深度图和相机位姿，将前一帧已编辑的图像重投影到当前视角，生成“结构线索”。该线索通过一个轻量级的条件分支注入到扩散转换器（DiT）的骨干网络中，作为残差特征引导空间对齐。
    2.  **补丁级语义传播路径**：将前一帧编辑结果编码为Patch特征，通过参考引导注意力机制（Reference-guided Attention）注入到DiT的中层。当前视角在生成时会检索并复用前一帧的颜色、材质等语义属性。
    3.  **CVC-Edit 数据集构建**：由于缺乏配对的跨视图编辑数据，作者通过将相邻视角拼接并联合编辑的方式生成伪标签，并利用 DINOv3 特征进行多准则过滤（全局/局部相似度），构建了一个包含 280K 样本的高质量数据集。
*   **算法流程**：采用两阶段训练（先练结构分支，再练语义注入），推理时按视角序列递归执行：$I'_t = E(I_t, c | P_{t-1 \to t}, I'_{t-1})$。

#### 3. 实验设计
*   **数据集/场景**：使用了 IN2N、Mip-NeRF 360、BlendedMVS 和 LLFF 等经典数据集中的复杂场景。
*   **Benchmark（评估指标）**：
    *   **CLIP Similarity**：衡量编辑结果与文本描述的匹配度。
    *   **CLIP Directional Similarity**：衡量编辑方向的一致性。
    *   **DINO Similarity**：衡量不同视角渲染图之间的一致性。
*   **对比方法**：GaussCtrl (2024), DGE (2024), EditSplat (2025), ViP3DE (2026)。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练时长**：
    *   第一阶段（结构分支）：22k 次迭代，约耗时 **6 天**。
    *   第二阶段（语义分支）：24k 次迭代，约耗时 **6.5 天**。
    *   总计训练时间约为 **12.5 天**。
*   **精度**：使用 bfloat16 精度和梯度检查点技术以节省显存。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了多种编辑任务（材质替换、风格化、物体修改、季节变换等）。
*   **消融实验**：非常详尽，分别验证了结构路径、语义路径的必要性，并探讨了结构分支的深度（层数）以及语义注入的最佳位置（中层优于浅层或深层）。
*   **客观性**：对比了近年来（包括尚未正式发表的 2026 年前沿工作）的最先进方法，实验设计较为公平，覆盖了室内外多种复杂场景。

#### 6. 主要结论与发现
*   **联合建模的优越性**：显式建模跨视图依赖比单纯在推理时做对齐（如 GaussCtrl）效果更好。
*   **双路径的互补性**：结构路径解决了几何错位（如胡须的位置），语义路径解决了属性漂移（如材质的颜色）。
*   **数据集的重要性**：构建的 CVC-Edit 数据集为模型提供了可靠的监督信号，是实现高质量一致性的关键。

#### 7. 优点（亮点）
*   **理论视角新颖**：从概率分布建模的角度重新定义了 3D 编辑问题。
*   **解耦设计合理**：将一致性拆分为结构和语义两条路径，符合视觉感知的逻辑，且便于分阶段训练。
*   **数据贡献**：构建并开源（预期）的大规模配对编辑数据集对社区具有重要价值。
*   **效果显著**：在处理复杂、大范围场景编辑时，其稳定性和细节保留明显优于现有方法。

#### 8. 不足与局限
*   **序列依赖风险**：采用递归编辑模式（$t-1$ 引导 $t$），如果初始视角编辑失败或中间产生误差，可能会导致误差沿视角序列累积。
*   **算力门槛高**：依赖 H20 等高性能 GPU 且训练周期较长，普通研究者难以复现。
*   **实时性限制**：虽然 3DGS 渲染很快，但 2D 编辑过程仍需逐帧运行扩散模型，整体编辑效率受限于序列处理速度。
*   **几何变形能力**：虽然结构路径能保持对应，但对于需要剧烈几何形变的编辑（如把球变成方块），其表现尚未在文中得到充分探讨。

（完）
