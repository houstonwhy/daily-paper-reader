---
title: "Scal3R: Scalable Test-Time Training for Large-Scale 3D Reconstruction"
title_zh: Scal3R：面向大规模三维重建的可扩展测试时训练
authors: "Tao Xie, Peishan Yang, Yudong Jin, Yingfeng Cai, Wei Yin, Weiqiang Ren, Qian Zhang, Wei Hua, Sida Peng, Xiaoyang Guo, Xiaowei Zhou"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08542v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 从长视频中进行大规模3D场景重建
tldr: 针对长视频序列的大规模3D重建难题，本文提出Scal3R框架。传统前馈模型受限于内存和全局上下文缺失，难以在长序列中保持一致性。Scal3R引入一种轻量级神经全局上下文表示，通过自监督测试时训练（TTT）快速适配，在不显著增加计算开销的前提下，大幅提升了模型对超大规模场景的感知能力和重建精度。
selection_source: fresh_fetch
motivation: 现有前馈重建模型在处理长视频序列时，因内存限制和缺乏全局上下文约束，难以保证重建的准确性与一致性。
method: 提出一种由轻量级子网络构成的神经全局上下文表示，通过自监督目标在测试阶段进行快速适配，以压缩并保留长程场景信息。
result: 在KITTI Odometry和Oxford Spires等大规模基准测试中，该方法在保持高效的同时，取得了领先的位姿精度和最先进的3D重建效果。
conclusion: Scal3R证明了通过测试时训练引入全局上下文，是实现高效、高精度大规模3D场景重建的有效途径。
---

## 摘要
本文研究了从长视频序列中进行大规模三维场景重建的任务。最近的前馈重建模型通过直接从 RGB 图像回归三维几何结构，在没有显式三维先验或几何约束的情况下取得了令人鼓舞的结果。然而，由于内存容量有限以及无法有效捕捉全局上下文线索，这些方法在长序列上往往难以保持重建的准确性和一致性。相比之下，人类可以自然地利用对场景的全局理解来辅助局部感知。受此启发，我们提出了一种新型的神经全局上下文表示，它能高效地压缩并保留长程场景信息，使模型能够利用广泛的上下文线索来增强重建的准确性和一致性。该上下文表示通过一组轻量级神经子网络实现，这些子网络在测试时通过自监督目标进行快速自适应，从而在不产生显著计算开销的情况下大幅增加了内存容量。在包括 KITTI Odometry 和 Oxford Spires 数据集在内的多个大规模基准测试上的实验表明，我们的方法在处理超大场景方面具有有效性，在保持效率的同时实现了领先的位姿精度和最先进的三维重建精度。代码可在 https://zju3dv.github.io/scal3r 获取。

## Abstract
This paper addresses the task of large-scale 3D scene reconstruction from long video sequences. Recent feed-forward reconstruction models have shown promising results by directly regressing 3D geometry from RGB images without explicit 3D priors or geometric constraints. However, these methods often struggle to maintain reconstruction accuracy and consistency over long sequences due to limited memory capacity and the inability to effectively capture global contextual cues. In contrast, humans can naturally exploit the global understanding of the scene to inform local perception. Motivated by this, we propose a novel neural global context representation that efficiently compresses and retains long-range scene information, enabling the model to leverage extensive contextual cues for enhanced reconstruction accuracy and consistency. The context representation is realized through a set of lightweight neural sub-networks that are rapidly adapted during test time via self-supervised objectives, which substantially increases memory capacity without incurring significant computational overhead. The experiments on multiple large-scale benchmarks, including the KITTI Odometry~\cite{Geiger2012CVPR} and Oxford Spires~\cite{tao2025spires} datasets, demonstrate the effectiveness of our approach in handling ultra-large scenes, achieving leading pose accuracy and state-of-the-art 3D reconstruction accuracy while maintaining efficiency. Code is available at https://zju3dv.github.io/scal3r.

---

## 论文详细总结（自动生成）

以下是对论文《Scal3R: Scalable Test-Time Training for Large-Scale 3D Reconstruction》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在长视频序列（公里级场景）中实现高效且全局一致的三维重建。
*   **研究背景**：
    *   传统方法（如SfM/SLAM）依赖相机内参、辅助传感器或复杂的全局优化，且在弱纹理区域易失效。
    *   新兴的前馈模型（如VGGT）虽能直接回归几何，但受限于Transformer的平方级计算复杂度，难以处理超长序列。
    *   现有的分块处理策略（如VGGT-Long）虽然解决了计算量问题，但由于缺乏全局上下文，容易导致局部误差累积和块间不一致。
*   **研究动机**：模仿人类利用全局场景理解来辅助局部感知的策略，引入一种可扩展的全局上下文表示。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将**测试时训练（Test-Time Training, TTT）**引入三维重建框架，利用轻量级神经子网络作为动态内存，在推理过程中实时压缩、存储并共享长程场景信息。
*   **关键技术细节**：
    *   **神经全局上下文存储（GCM）**：在VGGT骨干网络中嵌入GCM模块。它由多个**自适应内存单元（AMU）**组成，AMU是轻量级的MLP子网络。
    *   **测试时自监督更新**：在推理阶段，模型将输入特征投影为Query、Key、Value。通过最小化Key与Value之间的点积损失（自监督目标），在“内循环”中快速更新AMU的权重，使其存储当前块的上下文。
    *   **全局上下文同步（GCS）**：为了打破分块处理的孤岛效应，利用多GPU并行时的通信机制（All-reduce），将各块计算的梯度进行聚合与广播，实现全序列范围内的上下文共享。
    *   **推理流程**：长序列分块 -> 多GPU并行处理（带GCM/GCS） -> 块间对齐与融合 -> 最终公里级重建。

### 3. 实验设计
*   **数据集与场景**：
    *   **大规模/长序列**：Virtual KITTI (合成)、KITTI Odometry (城市驾驶)、Oxford Spires (公里级室内外混合)。
    *   **几何精度评估**：ETH3D (高分辨率室内外)。
    *   **补充基准**：ScanNet++、TUM-RGBD、Waymo。
*   **对比方法**：
    *   **前馈/流式模型**：VGGT-Long, FastVGGT, TTT3R, CUT3R, STream3R, StreamVGGT。
    *   **学习型SLAM**：MASt3R-SLAM, VGGT-SLAM。
    *   **传统SfM/SLAM**：COLMAP, DROID-SLAM, DPVO++。
*   **评价指标**：位姿精度（ATE, RRE, RTE）、几何精度（Chamfer Distance, F1-score）、资源消耗（内存、FPS）。

### 4. 资源与算力
*   **训练阶段**：使用了 **32张 NVIDIA A800 GPU** 进行联合训练，总计 60k 次迭代，耗时约 **3天**。
*   **推理阶段**：
    *   支持多GPU并行（通过GCS同步）。
    *   也可在单张 RTX 4090 上运行（串行处理块），内存占用约 10.32GB，处理速度约 2.53 FPS（在KITTI长序列上）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成到真实、从室内到室外、从短序列到数公里长序列的多种场景。
*   **消融实验**：针对子网络的**状态容量（State Size）**和**全局上下文机制（GCM/GCS）**进行了专门测试，验证了各组件对降低漂移的贡献。
*   **充分性与公平性**：实验对比了包括传统优化方法在内的10余种基线，且在不同长度和密度的视频流上均有测试，结果较为客观、全面。

### 6. 主要结论与发现
*   **精度领先**：Scal3R 在位姿估计和3D重建精度上均达到了 SOTA 水平，尤其在超长序列（如 KITTI 00 序列，3.7公里）上表现出极低的漂移。
*   **内存优势**：相比于直接增加 Token 数量的方法，基于 TTT 的神经内存能以极小的计算开销提供更大的存储容量。
*   **全局一致性**：GCS 机制显著提升了分块重建的对齐精度，减少了因局部观测不足导致的几何扭曲。

### 7. 优点
*   **可扩展性强**：通过 TTT 绕过了 Transformer 的平方复杂度限制，能够处理理论上无限长的序列。
*   **无需先验**：作为前馈模型，不需要预知相机内参或依赖 IMU/LiDAR 传感器。
*   **全局感知**：成功将长程依赖引入分块处理流程，解决了长视频重建中的一致性难题。

### 8. 不足与局限
*   **光照敏感性**：在出现剧烈光照变化（如隧道出入口、突发强光）时，跨块的对应关系会变弱，可能导致全局对齐失败。
*   **极端稀疏性限制**：如果视频帧间重叠度极低（如公里级场景仅有几十张图像），局部几何约束不足，模型仍可能失效。
*   **推理延迟**：虽然支持并行，但相比于一些轻量级实时 SLAM 系统（如 DPVO++），其 FPS 仍较低，尚不适用于高动态实时交互场景。

（完）
