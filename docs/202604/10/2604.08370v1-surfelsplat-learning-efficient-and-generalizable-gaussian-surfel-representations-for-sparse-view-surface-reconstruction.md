---
title: "SurfelSplat: Learning Efficient and Generalizable Gaussian Surfel Representations for Sparse-View Surface Reconstruction"
title_zh: SurfelSplat：面向稀疏视图表面重建的学习型高效且可泛化高斯面元表示
authors: "Chensheng Dai, Shengjun Zhang, Min Chen, Yueqi Duan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08370v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于场景重建的可泛化高斯面元表示
tldr: 针对3D高斯泼溅在表面重建中依赖密集视图且逐场景优化耗时的问题，本文提出SurfelSplat。这是一个前馈框架，能从稀疏视图中生成可泛化的像素对齐高斯面元。通过引入基于奈奎斯特采样定理的跨视图特征聚合模块和低通滤波器，解决了几何属性恢复不准确的问题。该方法在DTU基准上实现了与SOTA相当的精度，且推理速度提升100倍，无需逐场景训练。
selection_source: fresh_fetch
motivation: 现有的3DGS表面重建方法通常需要密集输入且逐场景优化极其耗时，限制了其在快速、稀疏视图场景下的应用。
method: 提出一种基于奈奎斯特采样定理的前馈框架，利用空间采样率引导的低通滤波器和跨视图特征融合网络来回归精确的高斯面元几何属性。
result: 在DTU数据集上取得了与最先进方法相当的重建质量，且预测速度提升了100倍，单场景处理时间缩短至1秒以内。
conclusion: SurfelSplat通过结合采样理论与前馈网络，实现了高效且具备泛化能力的稀疏视图三维表面重建。
---

## 摘要
3D 高斯泼溅（3DGS）在三维场景重建中展现了卓越的性能。除了新视角合成，它在多视图表面重建方面也显示出巨大潜力。现有方法采用基于优化的重建流水线，可实现精确且完整的表面提取。然而，这些方法通常需要密集的输入视图，且逐场景优化耗时较长。为解决这些局限性，我们提出了 SurfelSplat，这是一个前馈框架，能够从稀疏视图图像中生成高效且可泛化的像素对齐高斯面元（Gaussian surfel）表示。我们观察到，由于像素对齐基元的空间频率超过了奈奎斯特（Nyquist）采样率，传统的前馈结构难以恢复准确的高斯面元几何属性。因此，我们提出了一种基于奈奎斯特采样定理的跨视图特征聚合模块。具体而言，我们首先利用空间采样率引导的低通滤波器来调整高斯面元的几何形式。随后，我们将滤波后的面元投影到所有输入视图中以获取跨视图特征相关性。通过专门设计的特征融合网络处理这些相关性，我们最终能够回归出具有精确几何形状的高斯面元。在 DTU 重建基准上的广泛实验表明，我们的模型达到了与最先进方法相当的结果，并能在 1 秒内预测高斯面元，在无需昂贵的逐场景训练的情况下实现了 100 倍的加速。

## Abstract
3D Gaussian Splatting (3DGS) has demonstrated impressive performance in 3D scene reconstruction. Beyond novel view synthesis, it shows great potential for multi-view surface reconstruction. Existing methods employ optimization-based reconstruction pipelines that achieve precise and complete surface extractions. However, these approaches typically require dense input views and high time consumption for per-scene optimization. To address these limitations, we propose SurfelSplat, a feed-forward framework that generates efficient and generalizable pixel-aligned Gaussian surfel representations from sparse-view images. We observe that conventional feed-forward structures struggle to recover accurate geometric attributes of Gaussian surfels because the spatial frequency of pixel-aligned primitives exceeds Nyquist sampling rates. Therefore, we propose a cross-view feature aggregation module based on the Nyquist sampling theorem. Specifically, we first adapt the geometric forms of Gaussian surfels with spatial sampling rate-guided low-pass filters. We then project the filtered surfels across all input views to obtain cross-view feature correlations. By processing these correlations through a specially designed feature fusion network, we can finally regress Gaussian surfels with precise geometry. Extensive experiments on DTU reconstruction benchmarks demonstrate that our model achieves comparable results with state-of-the-art methods, and predict Gaussian surfels within 1 second, offering a 100x speedup without costly per-scene training.

---

## 论文详细总结（自动生成）

以下是对论文《SurfelSplat: Learning Efficient and Generalizable Gaussian Surfel Representations for Sparse-View Surface Reconstruction》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**稀疏视图**（如仅两张图像）下，实现**高效**且**具备泛化能力**的高质量三维表面重建。
*   **研究背景**：
    *   传统的 3D 高斯泼溅（3DGS）及其变体（如 2DGS）虽然渲染快、精度高，但依赖密集的输入视图，且需要针对每个场景进行数十分钟甚至数小时的优化，无法直接泛化到新场景。
    *   现有的前馈网络（如 MVSplat）虽然能快速生成高斯基元，但在处理“像素对齐”的高斯面元（Surfels）时，往往无法准确恢复几何属性（如法向量），导致面元倾向于平行于图像平面而非贴合实际表面。
*   **核心发现**：作者指出，这种几何失效的本质原因是像素对齐基元的**空间频率超过了奈奎斯特（Nyquist）采样率**，违反了信号处理的基本原理。

### 2. 方法论
SurfelSplat 提出了一个基于奈奎斯特采样定理引导的前馈框架，核心流程如下：
*   **初始特征提取**：利用图像编码器、极线变换器（Epipolar Transformer）和代价体积（Cost Volumes）提取多视图特征，并初步预测深度图和高斯属性。
*   **奈奎斯特引导的面元自适应（Surfel Adaptation）**：
    *   根据相机内参和深度计算空间采样率 $\hat{\nu} = f_x f_y / d^2$。
    *   在频率域应用自适应低通滤波器，动态调整高斯面元的缩放矩阵，确保其空间频率低于奈奎斯特阈值。这保证了网络能够从离散采样中学习到稳定的几何特征。
*   **跨视图特征聚合（Feature Aggregation）**：
    *   将自适应后的高斯面元投影回所有输入视图，识别出包含相关几何信息的图像区域。
    *   使用交叉注意力（Cross-Attention）机制，将这些区域的特征聚合到原始高斯特征中，增强几何感知能力。
*   **最终回归**：利用细化后的特征再次回归高斯面元的参数（中心、缩放、旋转、不透明度、颜色），生成最终的 2D 高斯辐射场。

### 3. 实验设计
*   **数据集**：主要在 **DTU** 基准数据集（128个场景）上进行评估，并在 **BlendedMVS** 数据集上验证泛化性。
*   **实验设置**：聚焦于**稀疏视图（2视图）**重建任务，输入分辨率为 256×320。
*   **对比方法**：
    *   **神经隐式方法**：NeuS, NeuSurf, VolRecon, UFORecon。
    *   **基于优化的 3DGS 方法**：2DGS, GausSurf, FatesGS。
*   **评价指标**：Chamfer Distance (CD) 衡量重建精度，Normal MSE 衡量法线准确度，以及推理时间。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A6000 GPU**。
*   **训练时长**：
    *   第一阶段：在 RealEstate10K 数据集上预训练 300,000 次迭代。
    *   第二阶段：在 DTU 数据集上微调 2,000 次迭代。
*   **推理效率**：单场景推理仅需 **1秒** 左右。

### 5. 实验数量与充分性
*   **实验规模**：在 DTU 的 15 个测试场景上进行了详尽的定量对比，并提供了 BlendedMVS 的定性结果。
*   **消融实验**：针对“面元自适应模块”和“特征聚合模块”分别做了消融研究，验证了各组件对降低 Chamfer 距离和法线误差的贡献。
*   **理论验证**：专门绘制了面元自适应前后的空间频率分布图，实验性地证明了该模块确实使所有高斯基元符合了奈奎斯特准则。
*   **充分性评价**：实验设计较为全面，涵盖了精度、效率、泛化性和理论验证，对比对象包含了最新的 SOTA 模型，结果具有较强的说服力。

### 6. 主要结论
*   **精度领先**：在稀疏视图下，SurfelSplat 的重建精度（CD 指标）优于现有的前馈和优化类方法。
*   **速度极快**：相比于需要逐场景优化的方法（如 NeuSurf 需 14 小时，2DGS 需 10 分钟），SurfelSplat 实现了 **100倍以上的加速**，达到了秒级推理。
*   **几何准确**：通过奈奎斯特引导的优化，成功解决了前馈网络中高斯面元法线预测不准的问题，生成的表面更加平滑且贴合真实几何。

### 7. 优点与亮点
*   **理论结合深刻**：将经典的信号处理理论（奈奎斯特采样定理）引入深度学习 3D 重建，为解决几何坍缩问题提供了坚实的物理依据。
*   **端到端前馈**：无需逐场景优化，具备极强的实用价值，适合实时或大规模场景处理。
*   **高质量表面**：生成的 2D 高斯面元在保持渲染质量的同时，具备了极佳的几何一致性。

### 8. 不足与局限
*   **分辨率敏感性**：由于采用像素对齐方式，高斯基元的数量随分辨率增加而剧增（如 1024x1024 分辨率下会产生超过 100 万个面元），这会显著降低推理和渲染速度。
*   **遮挡区域限制**：对于输入视图未覆盖的区域，重建效果受限，可能需要引入生成式模型（如扩散模型）来填补空白。
*   **冗余表示**：直接合并不同视角预测的高斯组会导致重叠区域存在冗余基元，增加了计算负担。
*   **泛化范围**：虽然在 DTU 和 BlendedMVS 上表现良好，但在面对相机姿态重叠极小或极端的场景时，深度预测的可靠性仍有待提升。

（完）
