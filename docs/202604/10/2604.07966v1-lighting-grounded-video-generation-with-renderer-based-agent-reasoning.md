---
title: Lighting-grounded Video Generation with Renderer-based Agent Reasoning
title_zh: 基于渲染器智能体推理的受光照约束视频生成
authors: "Ziqi Cai, Taoyu Yang, Zheng Chang, Si Li, Han Jiang, Shuchen Weng, Boxin Shi"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07966v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 以显式3D场景属性为条件的视频合成
tldr: 针对扩散模型在视频生成中对光照、布局和相机轨迹控制力不足的问题，本文提出了 LiVER 框架。该框架通过引入包含 3D 场景属性标注的大规模数据集，利用渲染器生成的控制信号实现场景属性的解耦。结合轻量化调节模块、渐进式训练策略及自动化场景智能体，LiVER 实现了高保真、时序一致且可精确编辑的视频合成，为可控视频生成树立了新标准。
selection_source: fresh_fetch
motivation: 现有的视频扩散模型难以对光照、布局和相机等关键场景要素进行显式且解耦的精确控制。
method: 提出 LiVER 框架，利用 3D 场景表示渲染控制信号，并结合轻量化调节模块和场景智能体将高层指令转化为 3D 控制参数。
result: 实验证明 LiVER 在照片级真实感和时序一致性方面达到领先水平，并实现了对场景要素的精确解耦控制。
conclusion: 该研究通过结合 3D 渲染与扩散模型，显著提升了视频生成的控制力，适用于电影制作和虚拟生产等领域。
---

## 摘要
扩散模型在视频生成方面取得了显著进展，但其可控性仍然是一个主要限制。布局、光照和摄像机轨迹等关键场景要素通常相互纠缠或仅被微弱地建模，限制了它们在电影制作和虚拟制作等需要显式场景控制的领域中的应用。我们提出了 LiVER，这是一个用于场景可控视频生成的扩散框架。为实现这一目标，我们引入了一个新颖的框架，该框架以显式的 3D 场景属性为条件进行视频合成，并由一个包含物体布局、光照和摄像机参数密集标注的新型大规模数据集提供支持。我们的方法通过从统一的 3D 表示中渲染控制信号来解耦这些属性。我们提出了一个轻量级条件模块和一种渐进式训练策略，将这些信号集成到基础视频扩散模型中，确保了稳定的收敛和高保真度。我们的框架支持广泛的应用，包括底层 3D 场景完全可编辑的图像到视频和视频到视频合成。为了进一步提高易用性，我们开发了一个场景智能体，能够自动将高层用户指令转换为所需的 3D 控制信号。实验表明，LiVER 在实现精确、解耦的场景要素控制的同时，达到了最先进的照片级真实感和时间一致性，为可控视频生成树立了新标准。

## Abstract
Diffusion models have achieved remarkable progress in video generation, but their controllability remains a major limitation. Key scene factors such as layout, lighting, and camera trajectory are often entangled or only weakly modeled, restricting their applicability in domains like filmmaking and virtual production where explicit scene control is essential. We present LiVER, a diffusion-based framework for scene-controllable video generation. To achieve this, we introduce a novel framework that conditions video synthesis on explicit 3D scene properties, supported by a new large-scale dataset with dense annotations of object layout, lighting, and camera parameters. Our method disentangles these properties by rendering control signals from a unified 3D representation. We propose a lightweight conditioning module and a progressive training strategy to integrate these signals into a foundational video diffusion model, ensuring stable convergence and high fidelity. Our framework enables a wide range of applications, including image-to-video and video-to-video synthesis where the underlying 3D scene is fully editable. To further enhance usability, we develop a scene agent that automatically translates high-level user instructions into the required 3D control signals. Experiments show that LiVER achieves state-of-the-art photorealism and temporal consistency while enabling precise, disentangled control over scene factors, setting a new standard for controllable video generation.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **LiVER**，一个旨在实现受光照约束且场景可控的视频生成框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频生成模型（如扩散模型）虽然在视觉质量上取得了巨大进步，但在**可控性**方面存在严重局限。特别是光照、物体布局和相机轨迹这三个关键要素往往相互纠缠，难以进行显式且精确的独立控制。
*   **研究动机**：现有的 3D 感知视频生成方法多侧重于几何和相机控制，忽略了物理上精确的光照表现（如反射、阴影、环境遮挡）。这导致生成的视频在处理皮肤、金属或玻璃等材质时，光照效果往往不真实。

### 2. 方法论
LiVER 的核心思想是将 3D 渲染器的物理精确性与扩散模型的生成能力相结合。
*   **基于渲染器的智能体推理 (Renderer-based Agent Reasoning)**：
    *   **场景构建**：利用 LLM 智能体将文本描述解析为结构化场景图，从库中检索 3D 资产并优化其位姿。
    *   **光照与相机规划**：智能体根据文本线索选择 HDR 环境贴图，并规划平滑的相机轨迹。
*   **光照约束场景代理 (Lighting-grounded Scene Proxy)**：
    *   不直接使用复杂的 3D 表示，而是通过渲染器（Blender）生成一组 2D 渲染通道作为代理信号，包括：**漫反射 (Diffuse)**、**粗糙 GGX (Rough GGX)** 和 **光泽 GGX (Glossy GGX)**。这些信号捕捉了从低频环境光到高频镜面反射的物理特征。
*   **视频生成架构**：
    *   基于 **Wan2.2-5B** 基础模型，引入轻量级**条件编码器**和**适配器**。
    *   将场景代理特征以空间残差的形式注入视频潜空间（Latent Space）。
*   **三阶段训练策略**：
    1.  **条件路径训练**：冻结主网络，仅训练代理编码器和适配器。
    2.  **联合 LoRA 微调**：解冻 LoRA 层，平衡控制力与视觉质量。
    3.  **光照多样性扩展**：混合真实视频与合成数据，增强模型对复杂光照的泛化能力。

### 3. 实验设计
*   **数据集 (LiVERSet)**：包含约 1.1 万个视频剪辑（每段 81 帧，720x1280 分辨率）。
    *   **LiVER-Real**：真实世界视频，通过算法重建 3D 几何和估计 HDR 环境光。
    *   **LiVER-Syn**：基于 PBR 材质合成的视频，具有动态旋转的光照效果。
*   **对比方法 (Baselines)**：
    *   **CameraCtrl**：侧重相机控制。
    *   **MotionCtrl**：统一控制相机和物体运动。
    *   **VideoFrom3D**：基于粗糙几何生成视频。
*   **评估指标**：
    *   质量：FVD、FID、CLIP 相似度。
    *   控制精度：相机位姿误差 (ATE/RPE)、光照误差 (SI-MSE)、布局误差 (mIoU)。

### 4. 资源与算力
*   **硬件使用**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练细节**：总 batch size 为 16，训练步数约为 **10 万步 (100K steps)**。
*   **优化器**：使用 AdamW，学习率为 $1 \times 10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：在 1000 个视频的测试集上进行了定量评估，并针对 25 名参与者进行了人工评价（User Study）。
*   **消融实验**：
    *   验证了**合成数据**的必要性（不使用合成数据会导致光照控制失效）。
    *   验证了**阶段性训练策略**的有效性（直接端到端训练会导致模型收敛困难且生成停滞）。
*   **评价**：实验设计较为全面，涵盖了从底层物理指标（位姿、光照）到高层感知质量（FVD、用户偏好）的多个维度，对比实验在相同测试集下进行，具有较好的客观性。

### 6. 主要结论与发现
*   LiVER 在保持高视频质量的同时，实现了对光照、布局和相机的**解耦控制**。
*   通过引入物理渲染通道（如 GGX）作为中介，扩散模型能够学习到复杂的物理交互（如旋转环境光下的阴影和反射变化）。
*   该框架支持灵活的编辑工作流：用户可以手动修改 3D 代理（移动物体、更换灯光），模型能实时反映这些修改。

### 7. 优点
*   **物理一致性**：通过渲染器代理解决了纯数据驱动模型难以模拟复杂物理现象的问题。
*   **解耦性强**：成功分离了光照与几何属性，支持独立编辑。
*   **易用性**：引入智能体推理，降低了用户构建复杂 3D 约束的门槛。
*   **高质量数据集**：贡献了带有密集物理标注的 LiVERSet，对社区有参考价值。

### 8. 不足与局限
*   **几何依赖性**：由于初始 3D 重建较为粗糙，模型在很大程度上仍依赖文本描述来合成精细的几何和材质细节。
*   **提示词敏感度**：最终输出的质量（尤其是几何一致性）对用户提供的 Prompt 较为敏感。
*   **推理开销**：虽然文中强调了轻量级模块，但生成过程涉及 3D 渲染和大型 DiT 模型，实时性可能受限（文中未详细讨论推理耗时）。

（完）
