---
title: "AnchorSplat: Feed-Forward 3D Gaussian Splatting with 3D Geometric Priors"
title_zh: AnchorSplat：基于 3D 几何先验的前馈 3D 高斯泼溅
authors: "Xiaoxue Zhang, Xiaoxu Zheng, Yixuan Yin, Tiao Zhao, Kaihua Tang, Michael Bi Mi, Zhan Xu, Dave Zhenyu Chen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07053v2"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于场景级重建的前馈3D高斯泼溅框架
tldr: 针对现有前馈3D高斯重建模型过度依赖像素对齐导致表征受限的问题，本文提出AnchorSplat。该框架引入了由3D几何先验（如稀疏点云或体素）引导的锚点对齐高斯表征，使重建独立于图像分辨率和视图数量。通过大幅减少高斯基元数量并结合高斯精炼器，AnchorSplat在ScanNet++等基准上实现了更具视图一致性的SOTA重建效果。
selection_source: fresh_fetch
motivation: 现有的像素对齐方法将高斯表征与输入图像紧密耦合，限制了场景级重建的几何感知能力和计算效率。
method: 提出一种基于3D几何先验的锚点对齐高斯表征，并利用高斯精炼器通过少量前馈步骤优化中间高斯参数。
result: 在ScanNet++ v2基准测试中，该方法以更少的高斯基元实现了超越现有技术的视图一致性和重建精度。
conclusion: AnchorSplat证明了将3D几何先验引入前馈高斯重建能显著提升场景级建模的效率与保真度。
---

## 摘要
最近的前馈高斯重建模型采用像素对齐的表述方式，将每个 2D 像素映射到 3D 高斯，使得高斯表示与输入图像紧密耦合。在本文中，我们提出了 AnchorSplat，这是一种用于场景级重建的新型前馈 3DGS 框架，它直接在 3D 空间中表示场景。AnchorSplat 引入了由 3D 几何先验（如稀疏点云、体素或 RGB-D 点云）引导的锚点对齐高斯表示，从而实现了更具几何感知能力的可渲染 3D 高斯，且独立于图像分辨率和视图数量。这种设计显著减少了所需的高斯数量，在提高计算效率的同时增强了重建保真度。除了锚点对齐设计外，我们还利用高斯精炼器（Gaussian Refiner）仅通过几次前馈推理即可调整中间高斯。在 ScanNet++ v2 NVS 基准测试上的实验表明，该方法达到了 SOTA 性能，在保持更高视图一致性的同时，使用显著更少的高斯原语（primitives）超越了以往的方法。

## Abstract
Recent feed-forward Gaussian reconstruction models adopt a pixel-aligned formulation that maps each 2D pixel to a 3D Gaussian, entangling Gaussian representations tightly with the input images. In this paper, we propose AnchorSplat, a novel feed-forward 3DGS framework for scene-level reconstruction that represents the scene directly in 3D space. AnchorSplat introduces an anchor-aligned Gaussian representation guided by 3D geometric priors (e.g., sparse point clouds, voxels, or RGB-D point clouds), enabling a more geometry-aware renderable 3D Gaussians that is independent of image resolution and number of views. This design substantially reduces the number of required Gaussians, improving computational efficiency while enhancing reconstruction fidelity. Beyond the anchor-aligned design, we utilize a Gaussian Refiner to adjust the intermediate Gaussiansy via merely a few forward passes. Experiments on the ScanNet++ v2 NVS benchmark demonstrate the SOTA performance, outperforming previous methods with more view-consistent and substantially fewer Gaussian primitives.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AnchorSplat** 的新型前馈 3D 高斯泼溅（3DGS）框架，旨在解决现有模型在场景级重建中的效率与几何一致性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的前馈 3DGS 重建模型大多采用“像素对齐（Pixel-aligned）”策略，即每个 2D 像素映射为一个 3D 高斯。
*   **研究动机**：
    *   **冗余性**：高斯数量随输入视图和分辨率线性增长，导致计算开销巨大。
    *   **几何不一致**：由于缺乏 3D 空间的全局约束，容易产生“漂浮物（floaters）”、表面破碎和多视图不一致的伪影。
    *   **分辨率敏感**：重建质量高度依赖输入图像的分辨率和深度估计的准确性。
*   **整体含义**：AnchorSplat 试图通过“锚点对齐（Anchor-aligned）”的设计，将场景表征从 2D 像素网格中解耦，直接在 3D 空间中利用几何先验进行重建。

### 2. 方法论
AnchorSplat 包含三个核心组件，形成了一个端到端的前馈流水线：
*   **锚点预测器（Anchor Predictor）**：
    *   利用预训练的多视图立体（MVS）模型（如 MapAnything）预测深度和相机位姿。
    *   将深度图反投影到 3D 空间，并使用**最远点采样（FPS）**算法进行下采样，生成稀疏且分布均匀的 3D 锚点（Anchors）。
*   **高斯解码器（Gaussian Decoder）**：
    *   使用 CNN 提取多视图图像特征，并将其投影到 3D 锚点上。
    *   采用基于 Transformer 的架构处理锚点特征，建模空间交互。
    *   通过 MLP 预测每个锚点对应的 4 个高斯原语的属性（中心偏移、不透明度、缩放、旋转和球谐系数）。
*   **高斯精炼器（Gaussian Refiner）**：
    *   这是一个即插即用的模块，通过计算渲染图像与真实图像之间的误差（Rendering Error），并将其反投影回 3D 空间。
    *   利用 Point Transformer 进一步微调高斯属性，提升边界清晰度和颜色一致性。
*   **损失函数**：结合了渲染损失（$L_1$, SSIM, LPIPS）、深度损失以及针对高斯不透明度和体积的正则化项。

### 3. 实验设计
*   **数据集**：主要在 **ScanNet++ V2**（高保真室内场景）上进行评估，并在 ARKitScenes、Replica 和 Tanks and Temples (T&T) 上进行了泛化性测试。
*   **Benchmark**：对比了优化类方法（3DGS, Mip-Splatting）和前馈类 SOTA 方法（AnySplat）。
*   **评估指标**：
    *   图像质量：PSNR, SSIM, LPIPS。
    *   几何精度：绝对相对误差（AbsRel）, $\delta_1$。
    *   效率：高斯数量（NumGS）, 重建耗时（ReconTime）。

### 4. 资源与算力
*   **硬件**：使用了 **64 台华为昇腾（Ascend）910B3 (64GB) NPU**。
*   **训练细节**：
    *   采用 bfloat16 精度和 AdamW 优化器。
    *   第一阶段（解码器）训练 5k 步，第二阶段（精炼器）训练 5k 步。
    *   模型参数量：解码器 84M，精炼器 31M。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的对比，包括不同输入视图数量（3, 5, 32, 48, 64, 128, 256 视图）下的表现。
*   **消融实验**：非常充分，涵盖了输入模态（RGB、深度、相机射线）、每个锚点的高斯数量、不同数据集的泛化性、特征聚合策略（均值/最大池化）以及不同 MVS 后端的影响。
*   **客观性**：通过与 AnySplat 的直接对比，展示了在极少高斯数量（约 1/20）下实现更优质量的结果，实验设计逻辑严密，对比公平。

### 6. 主要结论与发现
*   **高效性**：AnchorSplat 使用的高斯数量远少于像素对齐方法，显著降低了显存占用和渲染开销。
*   **几何一致性**：锚点对齐设计使得模型对遮挡和低纹理区域更具鲁棒性，生成的几何结构更完整、更清晰。
*   **精炼器作用**：Gaussian Refiner 能有效修复初始预测中的空洞和模糊，显著提升视觉保真度。
*   **可扩展性**：在极多视图（256路）下，AnySplat 会因显存溢出（OOM）失败，而 AnchorSplat 依然能稳定运行。

### 7. 优点（亮点）
*   **架构创新**：从像素对齐转向锚点对齐，解决了前馈 3DGS 走向大规模场景重建的核心瓶颈。
*   **即插即用**：精炼器模块不依赖完整重训，具有很强的实用性。
*   **性能卓越**：在 ScanNet++ 这种复杂场景上实现了 SOTA 性能，且重建速度极快（约 3-7 秒）。

### 8. 不足与局限
*   **先验依赖**：高度依赖预训练 MVS 模型提供的几何先验。如果初始深度估计存在严重偏差或缺失，模型难以在完全空白的区域生成准确的高斯。
*   **固定预算**：目前的锚点采样是静态的，对于几何极其复杂的局部区域，可能存在采样密度不足的问题。
*   **应用限制**：虽然支持大场景，但对于动态场景或光影变化剧烈的环境，文中未做深入探讨。

（完）
