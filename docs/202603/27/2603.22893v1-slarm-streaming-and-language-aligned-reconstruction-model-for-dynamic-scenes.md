---
title: "SLARM: Streaming and Language-Aligned Reconstruction Model for Dynamic Scenes"
title_zh: SLARM：面向动态场景的流式语言对齐重建模型
authors: "Zhicheng Qiu, Jiarui Meng, Tong-an Luo, Yican Huang, Xuan Feng, Xuanfu Li, ZHan Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22893v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 动态场景重建与语言对齐表示
tldr: SLARM是一个统一的动态场景重建、语义理解和实时流式推理的前馈模型。它通过高阶运动建模捕捉复杂非均匀运动，并利用LSeg蒸馏语义特征实现语言对齐。该模型采用基于窗口的因果注意力机制，支持低延迟流式处理，在动态估计、渲染质量和场景解析方面均达到SOTA水平，显著提升了运动精度和重建效果。
selection_source: fresh_fetch
motivation: 旨在解决动态场景重建中复杂运动捕捉难、语义理解缺失以及实时流式处理内存开销大的问题。
method: 结合高阶运动建模、LSeg语义蒸馏以及基于窗口的因果注意力机制，构建了一个端到端的流式推理框架。
result: "在运动精度上提升了21%，重建PSNR提高1.6 dB，且场景分割mIoU相比现有方法提升了20%。"
conclusion: SLARM通过语义与几何的紧密耦合，实现了高效、高精度的动态场景实时重建与语义查询。
---

## 摘要
我们提出了 SLARM，这是一种统一了动态场景重建、语义理解和实时流式推理的前馈模型。SLARM 通过高阶运动建模捕捉复杂的非均匀运动，且仅在可微渲染上进行训练，无需任何光流监督。此外，SLARM 从 LSeg 中蒸馏语义特征，以获得语言对齐的表示。这一设计实现了通过自然语言进行语义查询，且语义与几何之间的紧密耦合进一步增强了动态重建的准确性和鲁棒性。此外，SLARM 使用基于窗口的因果注意力机制处理图像序列，在不累积内存开销的情况下实现了稳定、低延迟的流式推理。在这一统一框架内，SLARM 在动态估计、渲染质量和场景解析方面均达到了最先进的水平，相比现有方法，其运动准确度提升了 21%，重建 PSNR 提升了 1.6 dB，分割 mIoU 提升了 20%。

## Abstract
We propose SLARM, a feed-forward model that unifies dynamic scene reconstruction, semantic understanding, and real-time streaming inference. SLARM captures complex, non-uniform motion through higher-order motion modeling, trained solely on differentiable renderings without any flow supervision. Besides, SLARM distills semantic features from LSeg to obtain language-aligned representations. This design enables semantic querying via natural language, and the tight coupling between semantics and geometry further enhances the accuracy and robustness of dynamic reconstruction. Moreover, SLARM processes image sequences using window-based causal attention, achieving stable, low-latency streaming inference without accumulating memory cost. Within this unified framework, SLARM achieves state-of-the-art results in dynamic estimation, rendering quality, and scene parsing, improving motion accuracy by 21%, reconstruction PSNR by 1.6 dB, and segmentation mIoU by 20% over existing methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SLARM**（Streaming and Language-Aligned Reconstruction Model）的框架，旨在解决动态场景下 3D 重建、语义理解与实时流式推理的统一问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 重建方法（如 NeRF 或 3DGS）大多局限于静态场景，或者在处理动态场景时存在优化耗时长（逐场景过拟合）、运动建模过于简单（假设匀速运动）、缺乏高层语义理解以及无法实现低延迟流式推理等问题。
*   **研究动机**：针对自动驾驶和具身智能（Embodied AI）的需求，需要一个能够同时处理复杂非均匀运动、具备语言对齐语义能力、且能以恒定延迟进行增量更新的前馈（Feed-forward）模型。

### 2. 方法论：核心思想与关键技术
SLARM 采用基于 Transformer 的前馈架构，直接从图像序列生成 4D 高斯喷点（4DGS）。
*   **高阶运动建模**：不同于以往的匀速假设，SLARM 使用**三阶泰勒展开**来建模位移 $\Gamma(\Delta t)$，显式预测速度、加速度和加加速度（Jerk）。公式表达为：$\Gamma(\Delta t) = \sum_{l=0}^{L-1} \frac{m_l \cdot (\Delta t)^{l+1}}{(l+1)!}$。这使得模型能捕捉如行人肢体摆动等复杂的非线性运动。
*   **语言对齐的语义蒸馏**：从 2D 基础模型 **LSeg** 中蒸馏语义特征到 4D 高斯基元中。通过将高斯属性与 CLIP 文本嵌入空间对齐，实现了通过自然语言直接查询 3D 场景中的动态物体。
*   **流式推理架构**：采用**基于窗口的因果注意力机制**（Window-based Causal Attention）。模型独立处理每一帧，同时传播紧凑的隐藏状态，确保推理时的内存开销不随时间增加，支持长序列的实时部署。
*   **自监督学习**：运动估计完全基于可微渲染的颜色一致性（RGB Loss）和感知损失（LPIPS），无需真实的光流（Scene Flow）标注。

### 3. 实验设计
*   **数据集**：主要在 **Waymo Open Dataset (WOD)** 上进行，这是一个大规模自动驾驶基准，包含丰富的动态物体和多视角视频。
*   **Benchmark 与对比方法**：
    *   **重建质量**：对比了 LGM、GS-LRM、MapAnything 和 STORM。
    *   **运动估计（Scene Flow）**：对比了 NSFP、NSFP++ 和 STORM。
    *   **语义分割**：对比了 EfficientViT、Mask2Former、SegFormer、OffSeg 和 LSeg 等 SOTA 2D 分割模型。
*   **评估指标**：PSNR、SSIM（图像质量）；D-RMSE（深度精度）；EPE3D、角度误差（运动精度）；mIoU、Acc（语义精度）。

### 4. 资源与算力
*   **硬件支持**：使用了 **64 台华为昇腾（Ascend）910B NPU**。
*   **训练时长**：总计训练约 **4 天**。
*   **训练参数**：Batch size 为 64，迭代次数为 200k 次。

### 5. 实验数量与充分性
*   **实验规模**：使用了 1000 条驾驶序列（798 条训练，202 条验证），每条序列约 200 帧，数据量充足。
*   **消融实验**：
    *   验证了运动建模阶数（1-4 阶）的影响，证明 3 阶是性能与效率的平衡点。
    *   验证了语义引导对动态重建的提升作用（语义一致性作为正则项）。
    *   对比了流式（Online）与非流式（Offline）模式在速度和内存上的差异。
*   **评价**：实验设计涵盖了几何、运动、语义三个维度，对比对象包括了最新的前馈重建模型和经典的分割模型，实验结果较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：SLARM 在动态区域的重建 PSNR 提升了 1.6 dB，运动准确度提升了 21%，语义分割 mIoU 提升了 20%。
*   **语义增强几何**：实验发现，引入语言对齐的语义特征不仅能实现查询，还能通过语义一致性约束显著提高运动估计的鲁棒性。
*   **流式优势**：SLARM-W（窗口模式）在保持高精度的同时，实现了线性的推理时间和稳定的内存占用，证明了其在实时系统中的潜力。

### 7. 优点（亮点）
*   **统一框架**：在一个前馈模型中解决了重建、运动和语义三个任务，实现了任务间的互补。
*   **高阶建模**：通过泰勒展开解决了动态场景中非匀速运动捕捉的难题。
*   **语言交互**：支持零样本（Zero-shot）语义查询，能够无缝集成到大语言模型（LLM）驱动的决策系统中。
*   **工程实用性**：针对流式推理的设计非常符合自动驾驶等实际应用场景。

### 8. 不足与局限
*   **位姿依赖**：模型目前仍需要准确的相机位姿（Camera Poses）作为输入，在位姿估计不准的情况下性能会受限。
*   **材质挑战**：由于依赖光度一致性，模型在处理玻璃、镜面等复杂反射材质时表现欠佳。
*   **自校准缺失**：目前尚未集成相机自校准功能，未来需要进一步探索在无位姿输入下的重建能力。

（完）
