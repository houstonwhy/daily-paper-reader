---
title: Repurposing Geometric Foundation Models for Multi-view Diffusion
title_zh: 将几何基础模型重新用于多视图扩散
authors: "Wooseok Jang, Seonghu Jeon, Jisang Han, Jinhyeok Choi, Minkyung Kwon, Seungryong Kim, Saining Xie, Sainan Liu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22275v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于几何一致性生成的项多视图扩散模型
tldr: 针对新视角合成（NVS）中几何一致性不足的问题，本文提出几何潜扩散（GLD）框架。该方法将几何基础模型的特征空间重新利用为多视图扩散的潜空间，而非传统的视角独立VAE空间。GLD利用这些特征中蕴含的跨视图几何对应关系，实现了高保真度的图像重建和极佳的3D一致性，在训练效率和生成质量上均显著优于现有方法。
selection_source: fresh_fetch
motivation: 现有的多视图生成方法多采用视角独立的VAE潜空间，难以保证生成结果在不同视角间的几何一致性。
method: 提出GLD框架，将几何基础模型的特征空间作为扩散模型的潜空间，以利用其内置的跨视图几何对应关系。
result: GLD在图像质量和3D一致性上超越了VAE和RAE，训练速度提升4.4倍以上，且在无生成式预训练下仍具竞争力。
conclusion: 几何基础模型的特征空间是进行多视图扩散生成的理想潜空间，能有效提升生成的几何一致性与训练效率。
---

## 摘要
虽然生成式潜空间的最新进展推动了单图像生成的重大进步，但用于新视角合成（NVS）的最佳潜空间在很大程度上仍未被探索。特别是，NVS 要求在不同视角之间实现几何一致的生成，但现有方法通常在与视角无关的 VAE 潜空间中运行。在本文中，我们提出了几何潜扩散（Geometric Latent Diffusion, GLD），这是一个将几何基础模型的几何一致特征空间重新用作多视图扩散潜空间的框架。我们表明，这些特征不仅支持高保真 RGB 重建，还编码了强大的跨视图几何对应关系，为 NVS 提供了一个非常合适的潜空间。我们的实验表明，GLD 在 2D 图像质量和 3D 一致性指标上均优于 VAE 和 RAE，同时与 VAE 潜空间相比，训练速度提高了 4.4 倍以上。值得注意的是，尽管 GLD 在没有此类生成式预训练的情况下从头开始训练其扩散模型，但它与利用大规模文本到图像预训练的最新方法相比仍具有竞争力。

## Abstract
While recent advances in generative latent spaces have driven substantial progress in single-image generation, the optimal latent space for novel view synthesis (NVS) remains largely unexplored. In particular, NVS requires geometrically consistent generation across viewpoints, but existing approaches typically operate in a view-independent VAE latent space. In this paper, we propose Geometric Latent Diffusion (GLD), a framework that repurposes the geometrically consistent feature space of geometric foundation models as the latent space for multi-view diffusion. We show that these features not only support high-fidelity RGB reconstruction but also encode strong cross-view geometric correspondences, providing a well-suited latent space for NVS. Our experiments demonstrate that GLD outperforms both VAE and RAE on 2D image quality and 3D consistency metrics, while accelerating training by more than 4.4x compared to the VAE latent space. Notably, GLD remains competitive with state-of-the-art methods that leverage large-scale text-to-image pretraining, despite training its diffusion model from scratch without such generative pretraining.

---

## 论文详细总结（自动生成）

这是一份关于论文《Repurposing Geometric Foundation Models for Multi-view Diffusion》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多视图扩散模型（用于新视角合成 NVS）大多在视角独立的 2D VAE 潜空间中运行。这种空间缺乏跨视角的几何约束，导致生成结果在 3D 一致性上表现不佳，且训练效率低下。
*   **研究动机**：作者思考是否可以利用已经编码了丰富几何结构的“几何基础模型”（如 Depth Anything 3, VGGT）的特征空间作为扩散模型的潜空间，从而让模型原生具备几何感知能力，而非依赖外部的深度图或复杂的几何监督。

### 2. 方法论（Geometric Latent Diffusion, GLD）
*   **核心思想**：将几何基础模型（以 Depth Anything 3 为主）的中间层特征作为扩散的目标空间。这些特征天然包含跨视图的对应关系。
*   **关键技术细节**：
    *   **三阶段流水线**：
        1.  **RGB 解码器训练**：训练一个轻量级 ViT 解码器，证明几何特征空间足以重建高保真 RGB 图像。
        2.  **边界层确定（Boundary Layer）**：几何模型通常有多个特征层。作者发现 Level 1 是最佳边界：它既保留了足够的几何一致性（类似深层），又保留了丰富的纹理细节（类似浅层）。
        3.  **级联特征生成**：显式扩散生成 Level 1 特征，然后通过级联模型生成 Level 0 特征以确保对齐。更深层的 Level 2 和 3 则通过冻结的几何编码器前向传播直接获得。
    *   **扩散架构**：采用解耦扩散 Transformer (DiT)，引入 3D 自注意力机制和 PRoPE（相机作为相对位置编码），并使用 Plücker 射线嵌入进行相机姿态调节。
    *   **零时几何解码**：由于潜空间本身来自几何模型，生成的潜变量可以直接通过原有的几何解码器输出深度图和相机姿态，无需额外训练。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：RealEstate10K, DL3DV, HyperSim, TartanAir。
    *   **测试集**：域内（Re10K, DL3DV）、域外/零样本（Mip-NeRF 360）、深度评估（ETH3D）。
*   **Benchmark 与对比方法**：
    *   **潜空间基准**：对比了 VAE 和 DINOv2 潜空间。
    *   **SOTA 方法**：对比了 MVGenMaster, Matrix3D, CAMEO, NVComposer, CAT3D 等。
*   **评估指标**：2D 指标（PSNR, SSIM, LPIPS）和 3D 指标（ATE 轨迹误差、RPE 姿态误差、重投影误差、MEt3R 一致性指标）。

### 4. 资源与算力
*   **GPU 型号与数量**：使用了 **8 台 NVIDIA B200 GPU**。
*   **训练时长**：
    *   扩散模型训练了 **175k 次迭代**。
    *   RGB 解码器训练了约 **170k 步**。
*   **效率提升**：相比 VAE 潜空间，GLD 的收敛速度快了 **4.4 倍**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（涵盖 3 个数据集、7 种以上 SOTA 方法）和定性可视化。
*   **消融实验**：
    *   不同边界层（Level 0-3）的影响分析。
    *   级联生成 vs. 独立生成的对比。
    *   不同输入视角数量（N=1, 2, 4）的鲁棒性测试。
    *   更换不同几何骨干网络（如 VGGT）的泛化性测试。
*   **充分性评价**：实验设计非常全面，不仅关注图像质量，还通过外部相机估计器验证了生成的几何准确性，客观地证明了该潜空间的优越性。

### 6. 主要结论与发现
*   **几何潜空间的优越性**：几何基础模型的特征空间比 VAE 或 DINO 空间更适合 NVS 任务，能显著提升 3D 一致性。
*   **训练效率**：由于潜空间自带几何先验，模型不需要从头学习几何对应关系，训练速度大幅提升。
*   **竞争力**：尽管 GLD 是从零开始训练（From Scratch），没有利用大规模文本到图像（T2I）的生成预训练，但其表现仍能媲美甚至超越许多基于 Stable Diffusion 微调的 SOTA 方法。

### 7. 优点
*   **创新性强**：首次系统性地将几何基础模型的特征空间重新定义为生成任务的潜空间。
*   **多功能性**：生成图像的同时，能够“免费”获得高质量、一致的深度图和 3D 点云。
*   **高效性**：通过边界层选择和特征传播机制，平衡了计算成本与生成质量。

### 8. 不足与局限
*   **推理延迟**：由于需要两阶段采样（Level 1 和 Level 0），推理速度慢于单阶段的 VAE 扩散模型。
*   **极端场景失效**：在极度稀疏的视角输入或严重的遮挡情况下，模型仍可能产生幻觉或伪影。
*   **依赖预训练模型**：GLD 的上限受限于所选几何基础模型（如 DA3）的表征能力，如果基础模型在某些特定领域（如极端光照）表现不佳，GLD 也会受影响。

（完）
