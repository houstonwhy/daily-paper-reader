---
title: "OnlinePG: Online Open-Vocabulary Panoptic Mapping with 3D Gaussian Splatting"
title_zh: OnlinePG：基于 3D 高斯泼溅的在线开放词汇全景建图
authors: "Hongjia Zhai, Qi Zhang, Xiaokun Pan, Xiyu Zhang, Yitong Dong, Huaqi Zhang, Dan Xu, Guofeng Zhang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18510v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 使用3D高斯泼溅进行在线开放词汇全景地图构建
tldr: OnlinePG是一个基于3D高斯泼溅（3DGS）的在线全景建图系统，旨在解决现有方法在具身智能应用中缺乏实时性或实例级理解的问题。该系统采用局部到全局的范式，通过滑动窗口和3D片段聚类图实现局部一致性，并利用空间属性网格和双向二分匹配更新全局地图。结合视觉语言模型（VLM）特征，OnlinePG实现了高效的在线开放词汇场景理解，在多个数据集上表现优异且具备实时性。
selection_source: fresh_fetch
motivation: 现有的开放词汇场景理解方法多为离线模式或缺乏实例级理解，难以满足机器人实时感知和交互的需求。
method: 提出一种基于3DGS的局部到全局建图框架，通过3D片段聚类图融合局部实例，并利用空间属性网格进行全局地图的鲁棒匹配与更新。
result: 在广泛使用的实验数据集上，该方法在保持实时效率的同时，性能优于现有的在线建图方法。
conclusion: OnlinePG成功集成了几何重建与开放词汇感知，为具身智能提供了一个高效且准确的在线全景建图解决方案。
---

## 摘要
具有在线全景建图能力的开放词汇场景理解对于具身应用感知环境并与之交互至关重要。然而，现有方法大多是离线的，或者缺乏实例级理解，限制了它们在现实世界机器人任务中的适用性。在本文中，我们提出了 OnlinePG，这是一个新颖且有效的系统，它在在线环境下利用 3D 高斯泼溅（3D Gaussian Splatting）集成了几何重建和开放词汇感知。在技术上，为了实现在线全景建图，我们采用了带有滑动窗口的高效局部到全局范式。为了构建局部一致性地图，我们构建了一个 3D 分段聚类图，该图综合利用几何和语义线索，将滑动窗口内不一致的分段融合为完整的实例。随后，为了更新全局地图，我们为局部 3D 高斯地图构建了具有空间属性的显式网格，并通过鲁棒的双向二分 3D 高斯实例匹配将其融合到全局地图中。最后，我们利用 3D 空间属性网格内融合的视觉语言模型（VLM）特征来实现开放词汇场景理解。在广泛使用的数据集上进行的大量实验表明，我们的方法在在线方法中取得了更优的性能，同时保持了实时效率。

## Abstract
Open-vocabulary scene understanding with online panoptic mapping is essential for embodied applications to perceive and interact with environments. However, existing methods are predominantly offline or lack instance-level understanding, limiting their applicability to real-world robotic tasks. In this paper, we propose OnlinePG, a novel and effective system that integrates geometric reconstruction and open-vocabulary perception using 3D Gaussian Splatting in an online setting. Technically, to achieve online panoptic mapping, we employ an efficient local-to-global paradigm with a sliding window. To build local consistency map, we construct a 3D segment clustering graph that jointly leverages geometric and semantic cues, fusing inconsistent segments within sliding window into complete instances. Subsequently, to update the global map, we construct explicit grids with spatial attributes for the local 3D Gaussian map and fuse them into the global map via robust bidirectional bipartite 3D Gaussian instance matching. Finally, we utilize the fused VLM features inside the 3D spatial attribute grids to achieve open-vocabulary scene understanding. Extensive experiments on widely used datasets demonstrate that our method achieves better performance among online approaches, while maintaining real-time efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《OnlinePG: Online Open-Vocabulary Panoptic Mapping with 3D Gaussian Splatting》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持实时性的前提下，实现机器人等具身智能设备在未知环境中的**在线、实例级、开放词汇**全景建图。
*   **研究背景**：
    *   现有的 3D 场景理解方法大多是**离线**的，依赖预先采集的完整数据集，无法满足机器人实时探索的需求。
    *   现有的在线方法往往缺乏**实例级（Instance-level）**的理解，或者在处理来自视觉语言模型（VLM）的噪声 2D 先验时表现不佳，导致 3D 实例分割不连续或语义漂移。
    *   3D 高斯泼溅（3DGS）虽然在重建速度上具有优势，但如何将其与开放词汇语义及全景分割高效结合仍是挑战。

### 2. 方法论
OnlinePG 采用了一种**局部到全局（Local-to-Global）**的建图范式，核心流程如下：
*   **场景表示**：使用 3D 高斯（3DGS）表示几何与外观，并引入**显式稀疏体素网格（Grid-based Spatial Attributes）**来存储语言特征（F）、置信度（C）、实例标签（T）和权重（K）。
*   **局部一致性地图构建**：
    *   **滑动窗口机制**：维护一个关键帧滑动窗口，利用 LSeg 和 EntitySeg 提取 2D 特征和掩码。
    *   **多线索片段聚类图**：构建一个以 3D 高斯片段为顶点的图，通过**几何重叠（体素占用比）**、**语义相似度（特征余弦相似度）**和**视角共视一致性**三种线索计算边权重，将噪声 2D 片段聚类为一致的 3D 局部实例。
*   **局部到全局地图融合**：
    *   **双向二分匹配（Bidirectional Bipartite Matching）**：利用匈牙利算法在局部实例和全局地图之间进行双向匹配，确保新探索区域与历史区域的鲁棒对齐。
    *   **全局更新策略**：根据匹配结果更新全局体素网格中的语义特征和实例标签，通过权重增减机制处理实例的合并与更替。

### 3. 实验设计
*   **数据集**：
    *   **ScanNetV2**：选取 10 个典型室内场景序列。
    *   **Replica**：选取 8 个高质量室内场景。
*   **基准方法（Benchmarks）**：
    *   **离线方法**：LangSplat, OpenGaussians, OpenScene, InstanceGaussian, PanoGS。
    *   **在线方法**：O2V-Mapping, OnlineAnySeg。
*   **评价指标**：3D 点云级别的平均交并比（mIoU）、平均准确率（mAcc），以及针对“物（Thing）”和“类（Stuff）”的全景重建质量（PRQ）。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **NVIDIA RTX 4090 GPU** 和 AMD Ryzen 9 7950X CPU 的桌面计算机。
*   **运行效率**：
    *   在简单场景下可达 **18 FPS**，复杂场景约为 **10 FPS**（不含 VLM 推理时间）。
    *   具体耗时分解：3DGS 优化约 410ms（5 帧/20 迭代），片段聚类约 350ms，全局融合约 1400ms（由于滑动窗口步进触发，频率低于帧率）。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集的 18 个场景上进行了定量评估，并提供了大量的定性可视化结果（如语义分割图、实例查询图、PCA 特征可视化）。
*   **消融实验**：针对聚类线索（几何/语义/视角）、匹配策略（单向/双向/最近邻）以及特征网格分辨率（3cm-50cm）进行了详细对比。
*   **客观性**：实验对比了当前最先进的在线和离线模型，并客观指出了在线方法与离线 SOTA 之间仍存在的微小差距，实验设计较为公平、全面。

### 6. 主要结论与发现
*   **性能领先**：OnlinePG 在在线方法中取得了最佳的语义和全景分割性能，显著优于 O2V-Mapping 和 OnlineAnySeg。
*   **缩小差距**：通过局部聚类和双向匹配，该方法极大地缩小了在线建图与离线方法之间的性能鸿沟，甚至在某些指标上（如 ScanNetV2 的 PRQ）超越了部分离线方法。
*   **鲁棒性**：多线索聚类能有效抑制 2D VLM 产生的多视角不一致和过分割噪声。

### 7. 优点与亮点
*   **高效的局部到全局范式**：通过滑动窗口平衡了局部一致性与全局融合的计算开销。
*   **多线索融合**：不依赖单一的语义或几何信息，结合视角一致性显著提升了实例分割的准确度。
*   **双向匹配机制**：解决了在线建图中新旧区域匹配不稳定的问题，增强了地图更新的鲁棒性。
*   **实时性**：在保证高质量全景理解的同时，维持了可接受的实时运行速度。

### 8. 不足与局限
*   **动态场景限制**：目前仅支持静态环境建图，无法处理移动物体。
*   **输入依赖**：仍需要准确的深度信息和相机位姿（Pose）作为输入，尚未实现完全的 Pose-free 或 Depth-free 重建。
*   **VLM 开销**：虽然建图过程实时，但前端 2D VLM（如 CLIP/LSeg）的推理开销在文中未计入 FPS，实际部署时需考虑这部分算力压力。

（完）
