---
title: "OrbitNVS: Harnessing Video Diffusion Priors for Novel View Synthesis"
title_zh: OrbitNVS：利用视频扩散先验进行新视角合成
authors: "Jinglin Liang, Zijian Zhou, Rui Huang, Shuangping Huang, Yichen Gong"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19613v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 利用视频扩散先验进行3D物体视角合成
tldr: 本研究提出 OrbitNVS，将新视角合成（NVS）重新定义为轨道视频生成任务。通过将预训练视频扩散模型的强大视觉先验引入 NVS，并结合相机适配器、法向图生成分支及像素级监督，解决了单视角下不可见区域合成及几何与外观一致性难题。实验证明，该方法在 GSO 和 OmniObject3D 数据集上显著优于现有方法，大幅提升了合成质量与一致性。
selection_source: fresh_fetch
motivation: 现有 NVS 方法在处理单视角输入时，难以在保持几何与外观一致性的同时合成高质量的未观测区域。
method: 将 NVS 转化为轨道视频生成，利用视频扩散先验，并引入相机适配器、法向图引导分支及像素空间监督来增强控制力与细节。
result: 在 GSO 和 OmniObject3D 基准测试中，OrbitNVS 在单视角设置下的 PSNR 分别比现有最优方法提升了 2.9 dB 和 2.4 dB。
conclusion: OrbitNVS 证明了利用视频生成先验并结合几何引导，能有效提升 3D 物体新视角合成的真实感与多视角一致性。
---

## 摘要
新视角合成 (NVS) 旨在根据有限数量的已知视角生成 3D 物体未见过的视角。现有方法在为未观察区域合成合理的视角时往往面临困难，特别是在单视角输入的情况下，且在保持几何和外观一致性方面仍面临挑战。为了解决这些问题，我们提出了 OrbitNVS，它将 NVS 重新表述为轨道视频生成任务。通过定制的模型设计和训练策略，我们将预训练的视频生成模型适配到 NVS 任务中，利用其丰富的视觉先验来实现高质量的视角合成。具体而言，我们在视频模型中加入了相机适配器，以实现精确的相机控制。为了增强 3D 物体的两个关键属性——几何和外观，我们设计了一个法向图生成分支，并利用法向图特征通过注意力机制引导目标视角的合成，从而提高了几何一致性。此外，我们应用了像素空间监督，以缓解由潜空间中的空间压缩引起的外观模糊。大量实验表明，OrbitNVS 在 GSO 和 OmniObject3D 基准测试中显著优于之前的方法，特别是在具有挑战性的单视角设置下（例如，PSNR 分别提升了 2.9 dB 和 2.4 dB）。

## Abstract
Novel View Synthesis (NVS) aims to generate unseen views of a 3D object given a limited number of known views. Existing methods often struggle to synthesize plausible views for unobserved regions, particularly under single-view input, and still face challenges in maintaining geometry- and appearance-consistency. To address these issues, we propose OrbitNVS, which reformulates NVS as an orbit video generation task. Through tailored model design and training strategies, we adapt a pre-trained video generation model to the NVS task, leveraging its rich visual priors to achieve high-quality view synthesis. Specifically, we incorporate camera adapters into the video model to enable accurate camera control. To enhance two key properties of 3D objects, geometry and appearance, we design a normal map generation branch and use normal map features to guide the synthesis of the target views via attention mechanism, thereby improving geometric consistency. Moreover, we apply a pixel-space supervision to alleviate blurry appearance caused by spatial compression in the latent space. Extensive experiments show that OrbitNVS significantly outperforms previous methods on the GSO and OmniObject3D benchmarks, especially in the challenging single-view setting (\eg, +2.9 dB and +2.4 dB PSNR).

---

## 论文详细总结（自动生成）

以下是对论文《OrbitNVS: Harnessing Video Diffusion Priors for Novel View Synthesis》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：新视角合成（NVS）在仅有极少数（如单张）输入图像时，难以推断物体未观测区域的合理外观，且难以保持多视角间的几何与外观一致性。
*   **研究动机**：作者认为 NVS 本质上是一个**视觉常识推理问题**而非单纯的几何重建。视频数据包含了丰富的物理世界常识，因此利用大规模预训练的视频生成模型作为先验，比单纯微调 2D 图像模型更能解决 NVS 中的遮挡推断和空间一致性挑战。
*   **整体含义**：将 NVS 任务重新定义为“轨道视频生成”任务，通过适配相机控制和几何引导，释放视频扩散模型的潜力。

### 2. 方法论
OrbitNVS 基于预训练的视频生成模型 **Wan2.1-I2V-14B**，采用了以下关键技术：
*   **轨道视频生成框架**：将 NVS 建模为给定参考帧和相机轨迹的视频补全任务。
*   **相机适配器 (Camera Adapter)**：
    *   使用 **Plücker 坐标**表示相机位姿，计算每个像素的成像射线。
    *   在 Diffusion Transformer (DiT) 层中插入适配器，通过 **AdaLN (自适应层归一化)** 机制调节特征图，实现精确的视角控制。
*   **法向图生成分支 (Normal Map Branch)**：
    *   引入一个与 RGB 分支共享参数的辅助分支，专门生成法向图。
    *   两个分支在自适应注意力层进行交互，利用法向图提供的几何线索引导 RGB 合成，增强 3D 结构一致性。
*   **像素空间后训练 (Pixel-Space Post-Training)**：
    *   针对潜空间扩散模型（LDM）在压缩时丢失高频细节的问题，引入第二阶段训练。
    *   通过 VAE 解码器将潜变量映射回像素空间进行直接监督，利用梯度回传优化 DiT，提升纹理清晰度。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 Step1X-3D 数据集（从 Objaverse-V1 过滤出的 32 万个 3D 资产），渲染 129 帧轨道视频及法向图。
    *   **测试集**：GSO (1k 资产) 和 OmniObject3D (6k 资产)，均未出现在训练集中。
*   **Benchmark 设置**：设置了三种难度的相机轨迹（仰角波动幅度分别为 0°、30°、60°）。
*   **对比方法**：
    *   **SV3D**：同样基于视频扩散模型的 SOTA 方法。
    *   **SEVA**、**EscherNet**：当前领先的基于图像扩散模型的 NVS 方法。
*   **评价指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似性）。

### 4. 资源与算力
*   **硬件**：使用了 8 台机器，每台配备 8 张 **NVIDIA A800 (80G)** GPU，总计 **64 张 GPU**。
*   **训练时长**：
    *   第一阶段（潜空间损失）：约 60 小时。
    *   第二阶段（潜空间 + 像素空间损失）：约 66 小时。
    *   **总计约 126 小时**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单视角推理、多视角鲁棒性、不同相机轨迹控制力等多个维度。
*   **消融实验**：针对“法向图分支”和“像素空间损失”分别做了定性和定量对比，验证了各模块的有效性。
*   **充分性评价**：实验设计非常充分。作者不仅在两个主流 3D 扫描数据集上进行了大规模测试，还探讨了通过文本提示（VLM 生成）进行可控合成的可能性。对比实验涵盖了同类技术的最新 SOTA，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：OrbitNVS 在单视角设置下表现极其出色，在 GSO 和 OmniObject3D 上 PSNR 分别提升了 **2.9 dB** 和 **2.4 dB**。
*   **推理能力**：模型展现了强大的常识推理能力，例如能从机器人背面推断出正面的细节，或从房屋正面推断出背面的窗户。
*   **几何与细节**：法向图分支显著改善了复杂结构（如竹篮编织）的生成质量；像素空间训练有效解决了条形码等高频纹理的模糊问题。
*   **文本控制**：通过修改文本 Prompt（如将“红花”改为“蓝玫瑰”），可以实现对 NVS 结果的语义编辑。

### 7. 优点
*   **范式创新**：成功将通用的视频生成能力转化为专业的 3D 视角合成能力，利用了视频模型中蕴含的物理世界先验。
*   **高一致性**：通过法向图引导和视频模型的时序一致性，解决了传统方法中常见的视角切换时的“闪烁”或几何畸变。
*   **控制精准**：引入相机适配器后，对任意轨道轨迹的控制精度优于以往方法。

### 8. 不足与局限
*   **计算开销**：模型基于 14B 参数的视频模型，且涉及像素空间梯度回传，推理和训练的计算成本较高。
*   **物体中心化限制**：目前主要针对孤立的 3D 物体（轨道视频），对于复杂场景或大规模环境的 NVS 适用性尚待验证。
*   **依赖高质量 3D 数据**：虽然利用了视频先验，但微调过程仍高度依赖于高质量渲染的 3D 资产数据集。

（完）
