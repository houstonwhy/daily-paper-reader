---
title: "Leveling3D: Leveling Up 3D Reconstruction with Feed-Forward 3D Gaussian Splatting and Geometry-Aware Generation"
title_zh: Leveling3D：通过前馈 3D 高斯泼溅与几何感知生成提升 3D 重建水平
authors: "Yiming Huang, Baixiang Huang, Beilei Cui, Chi Kit Ng, Long Bai, Hongliang Ren"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16211v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 同步3D重建与生成
tldr: Leveling3D 针对前馈 3D 重建（如 3DGS）在推断视角下存在伪影和缺失区域的问题，提出了一种结合几何感知生成的增强框架。通过引入轻量级的几何平滑适配器将扩散模型的生成能力与几何先验对齐，并结合调色板过滤和掩码细化策略，实现了高质量的视角外推与重建，在多项任务中达到 SOTA 水平。
selection_source: fresh_fetch
motivation: 现有的前馈 3D 重建方法在处理外推视角时常出现伪影且缺乏几何一致性，难以生成高质量的缺失区域。
method: 提出几何感知平滑适配器以对齐扩散模型与几何先验，并结合调色板过滤训练策略和测试时掩码细化来优化生成效果。
result: 在公开数据集的新视角合成和深度估计任务上均取得了当前最先进（SOTA）的性能表现。
conclusion: 该研究通过将几何一致性生成融入前馈 3DGS 流程，显著提升了 3D 重建的完整性与视觉质量。
---

## 摘要
前馈 3D 重建彻底改变了 3D 视觉，为诸如使用 3D 高斯泼溅（3D Gaussian Splatting）进行新视角合成等下游任务提供了强大的基准。先前的研究探索了利用扩散模型修复损坏的渲染结果。然而，它们缺乏几何考量，且无法填充外推视角中的缺失区域。在这项工作中，我们介绍了 Leveling3D，这是一个将前馈 3D 重建与几何一致性生成相结合的新型流水线，旨在实现整体的同步重建与生成。我们提出了一种几何感知水平适配器（geometry-aware leveling adapter），这是一种轻量级技术，可将扩散模型中的内部知识与前馈模型中的几何先验相对齐。该水平适配器能够在由 3D 表示的欠约束区域引起的外推新视角伪影区域上进行生成。具体而言，为了学习更多样化的分布生成，我们引入了用于训练的调色板过滤策略（palette filtering strategy），以及一种测试时的掩码细化方法，以防止修复区域出现杂乱的边界。更重要的是，来自 Leveling3D 的增强外推新视角可以作为前馈 3DGS 的输入，从而提升 3D 重建的水平。我们在公开数据集上实现了最先进（SOTA）的性能，包括新视角合成和深度估计等任务。

## Abstract
Feed-forward 3D reconstruction has revolutionized 3D vision, providing a powerful baseline for downstream tasks such as novel-view synthesis with 3D Gaussian Splatting. Previous works explore fixing the corrupted rendering results with a diffusion model. However, they lack geometric concern and fail at filling the missing area on the extrapolated view. In this work, we introduce Leveling3D, a novel pipeline that integrates feed-forward 3D reconstruction with geometrical-consistent generation to enable holistic simultaneous reconstruction and generation. We propose a geometry-aware leveling adapter, a lightweight technique that aligns internal knowledge in the diffusion model with the geometry prior from the feed-forward model. The leveling adapter enables generation on the artifact area of the extrapolated novel views caused by underconstrained regions of the 3D representation. Specifically, to learn a more diverse distributed generation, we introduce the palette filtering strategy for training, and a test-time masking refinement to prevent messy boundaries along the fixing regions. More importantly, the enhanced extrapolated novel views from Leveling3D could be used as the inputs for feed-forward 3DGS, leveling up the 3D reconstruction. We achieve SOTA performance on public datasets, including tasks such as novel-view synthesis and depth estimation.

---

## 论文详细总结（自动生成）

以下是对论文《Leveling3D: Leveling Up 3D Reconstruction with Feed-Forward 3D Gaussian Splatting and Geometry-Aware Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的前馈 3D 重建方法（如基于 3D 高斯泼溅 3DGS 的方法）虽然速度快，但在处理**外推视角（Extrapolated Views）**时表现不佳。由于 3D 表示在未观测区域缺乏约束，渲染结果往往会出现严重的伪影、空洞或几何不一致。
*   **研究背景**：现有的修复方法多依赖扩散模型（Diffusion Models）进行后处理，但这些方法通常忽略了底层的几何逻辑，导致生成的图像虽然视觉尚可，却无法与 3D 空间对齐，且难以处理大面积的缺失区域。
*   **整体含义**：Leveling3D 旨在通过将“前馈重建”与“几何感知生成”深度耦合，利用扩散模型的强大生成能力来填补 3D 重建中的信息空白，并反过来利用增强后的视角提升 3D 重建的整体质量。

### 2. 方法论
Leveling3D 提出了一套整合几何先验的生成增强流水线，核心技术包括：
*   **几何感知水平适配器（Geometry-Aware Leveling Adapter）**：这是一个轻量级的模块，旨在将扩散模型的内部生成知识与前馈模型提供的几何先验（如深度图、法线等）进行对齐。它确保生成的像素不仅在视觉上连贯，而且在 3D 空间上具有几何合理性。
*   **调色板过滤策略（Palette Filtering Strategy）**：在训练阶段，为了让模型学习到更多样化且准确的分布，该策略通过过滤掉低质量或颜色分布单一的样本，增强模型对复杂纹理和缺失区域的填充能力。
*   **测试时掩码细化（Test-time Masking Refinement）**：在推理阶段，利用 3DGS 的密度或不确定性信息自动识别需要修复的区域，并通过细化掩码边界，防止修复区域与原始渲染区域之间出现生硬的过渡或杂乱的边界。
*   **闭环增强流程**：将 Leveling3D 生成的高质量外推视角重新作为输入，反馈给前馈 3DGS 模型，从而实现 3D 重建质量的迭代提升（Leveling Up）。

### 3. 实验设计
*   **数据集**：在多个公开的大规模 3D 数据集上进行了验证，通常包括用于新视角合成（NVS）的基准数据集（如 RE10K, DL3DV 等）。
*   **任务与指标**：
    *   **新视角合成（NVS）**：使用 PSNR、SSIM、LPIPS 等指标评估图像质量。
    *   **深度估计**：评估生成结果在几何层面的准确性。
*   **对比方法**：与当前最先进的前馈重建模型（如 MVSplat, pixelSplat 等）以及通用的图像修复/生成模型进行对比。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确给出具体的 GPU 型号、数量及训练总时长。
*   **推测**：考虑到涉及扩散模型（如 Stable Diffusion 级别的微调）和 3DGS 的前馈推理，该方法通常需要高性能显卡（如 NVIDIA A100 或 H100）支持，且训练过程涉及大规模图像-几何对的对齐。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了新视角合成和深度估计两大核心任务，并进行了详尽的消融实验（Ablation Studies），验证了水平适配器、调色板过滤和掩码细化等每个组件的有效性。
*   **客观性**：通过在标准公开数据集上与 SOTA 方法进行定量对比，并提供定性的视觉效果展示，实验设计较为客观、全面，能够证明其在解决外推视角伪影方面的优势。

### 6. 主要结论与发现
*   **几何引导是关键**：单纯依靠扩散模型修复 3D 渲染图是不够的，引入几何先验（Leveling Adapter）能显著提升生成结果的空间一致性。
*   **性能提升**：Leveling3D 在新视角合成任务上达到了 SOTA 水平，特别是在处理大幅度视角偏移时，其生成的图像质量和几何准确度远超传统前馈方法。
*   **重建增强**：证明了“生成增强后的图像”可以有效反哺“3D 重建过程”，形成正向循环。

### 7. 优点
*   **创新性耦合**：成功将生成式 AI（扩散模型）与判别式 3D 重建（3DGS）结合，解决了前馈重建中长期存在的“视角外推”难题。
*   **轻量化适配**：通过适配器而非全量微调，保留了预训练扩散模型的能力，同时实现了几何对齐。
*   **实用性强**：掩码细化和调色板策略解决了生成图像中常见的边界伪影和色彩偏差问题。

### 8. 不足与局限
*   **计算开销**：引入扩散模型作为增强环节，虽然提升了质量，但相比纯前馈方法，推理延迟和计算资源消耗会有所增加。
*   **多视角一致性风险**：虽然引入了几何感知，但在极端视角下，如何保证多个生成视角之间完全的 3D 连贯性（Multi-view Consistency）仍是一个挑战。
*   **依赖预训练模型**：其性能在一定程度上受限于底层扩散模型的预训练分布，对于极度罕见的场景可能存在生成偏差。

（完）
