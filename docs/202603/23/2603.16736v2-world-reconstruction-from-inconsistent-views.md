---
title: World Reconstruction From Inconsistent Views
title_zh: 从不一致视图中进行世界重建
authors: "Lukas Höllein, Matthias Nießner"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16736v2"
tags: ["query:3dgen"]
score: 8.0
evidence: 从视频扩散模型中重建3D世界
tldr: 针对视频扩散模型生成的视频帧在3D一致性上的不足，本文提出一种从不一致视图重建3D世界的新方法。通过将视频帧非刚性对齐到全局一致坐标系，利用几何基础模型生成点云，并结合改进的ICP算法、全局优化及创新的逆变形渲染损失，实现了高精度、可探索的3D场景重建，成功将视频生成模型转化为一致的3D世界生成器。
selection_source: fresh_fetch
motivation: 视频扩散模型生成的图像质量虽高，但帧间缺乏3D一致性，导致难以直接进行高质量的3D重建。
method: 提出一种非刚性迭代帧到模型ICP算法进行初步对齐，并结合全局优化和逆变形渲染损失来精细化3D点云与场景。
result: 实验证明该方法在重建质量上优于现有基准，能够从不一致的视频序列中生成清晰、详细且可探索的3D环境。
conclusion: 该研究有效解决了视频生成中的不一致性问题，为利用视频扩散模型构建一致性3D世界提供了强有力的技术支撑。
---

## 摘要
视频扩散模型能够生成高质量且多样化的世界；然而，输出序列中的单个帧往往缺乏 3D 一致性，这使得 3D 世界的重建变得困难。为此，我们提出了一种新方法，通过将视频帧非刚性地对齐到一个全局一致的坐标系中来处理这些不一致性，从而产生清晰且细节丰富的点云重建。首先，几何基础模型将每一帧提升为像素级的 3D 点云，由于上述不一致性，这些点云包含未对齐的表面。接着，我们提出了一种定制的非刚性迭代帧到模型 ICP，以获得所有帧之间的初始对齐，随后通过全局优化进一步使点云更加清晰。最后，我们将该点云作为 3D 重建的初始化，并提出了一种新颖的反向变形渲染损失，从而从不一致的视图中创建高质量且可探索的 3D 环境。我们证明了我们的 3D 场景达到了比基准模型更高的质量，有效地将视频模型转化为具有 3D 一致性的世界生成器。

## Abstract
Video diffusion models generate high-quality and diverse worlds; however, individual frames often lack 3D consistency across the output sequence, which makes the reconstruction of 3D worlds difficult. To this end, we propose a new method that handles these inconsistencies by non-rigidly aligning the video frames into a globally-consistent coordinate frame that produces sharp and detailed pointcloud reconstructions. First, a geometric foundation model lifts each frame into a pixel-wise 3D pointcloud, which contains unaligned surfaces due to these inconsistencies. We then propose a tailored non-rigid iterative frame-to-model ICP to obtain an initial alignment across all frames, followed by a global optimization that further sharpens the pointcloud. Finally, we leverage this pointcloud as initialization for 3D reconstruction and propose a novel inverse deformation rendering loss to create high quality and explorable 3D environments from inconsistent views. We demonstrate that our 3D scenes achieve higher quality than baselines, effectively turning video models into 3D-consistent world generators.

---

## 论文详细总结（自动生成）

这篇论文《World Reconstruction From Inconsistent Views》由慕尼黑工业大学的研究团队提出，旨在解决视频扩散模型（VDM）生成的视频在3D空间中不一致的问题，从而实现高质量的3D世界重建。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视频扩散模型（如 Sora, Gen-3 等）虽然能生成视觉惊艳的视频，但其生成的帧序列往往缺乏严格的**3D几何一致性**（即存在“生成漂移” Generative Drift）。如果直接使用这些视频进行3D重建（如 Gaussian Splatting），会导致几何对齐错误、渲染模糊和严重的浮空伪影。
*   **研究动机**：现有的方法大多尝试在生成阶段通过增加相机控制或一致性约束来解决，但这通常需要昂贵的模型微调且难以完全消除漂移。本文提出一种**轻量级的重建侧方案**，通过非刚性对齐技术，将不一致的视频帧转化为一致的3D世界。

### 2. 方法论
该方法分为三个主要阶段：
*   **场景初始化（Scene Initialization）**：
    *   利用几何基础模型（如 DepthAnything-3）为每个视频帧估计深度图和相机位姿。
    *   提出一种**基于体素的置信度过滤方案**，剔除不可靠的点，形成初始的稠密点云。
*   **几何对齐（Geometry Alignment）**：
    *   **非刚性迭代帧到模型 ICP**：通过优化相机外参和一个基于哈希网格 MLP 的变形场，将各帧点云非刚性地对齐到参考空间。
    *   **损失函数**：结合了点到平面数据损失、颜色对齐损失、基于 RoMa 匹配器的稀疏对应损失以及 ARAP（尽可能刚性）正则化。
    *   **全局优化**：在初步对齐后，联合优化所有相机和变形网络，以进一步消除表面重影，使几何边缘更锐利。
*   **非刚性感知的高斯溅射优化（Non-rigid Gaussian Splatting）**：
    *   **逆变形优化**：学习一个从一致的“规范空间”到各不一致输入帧空间的逆向映射。
    *   **3D场景整合**：在训练 2D Gaussian Splatting (2DGS) 时，先将规范空间的高斯球通过逆变形场变换到输入帧空间再进行渲染。这种“非刚性感知渲染损失”允许模型利用不一致的图像训练出一致的3D表示。

### 3. 实验设计
*   **数据源/场景**：使用了多种最先进的视频扩散模型生成的视频，包括 Wan-2.2, ViewCrafter, Gen3C, Seva, Voyager, Genie3 和 HY-WorldPlay。涵盖了室内外多种场景和复杂的相机运动。
*   **Benchmark（基准）**：采用 **WorldScore** 评测体系，包括 3D 一致性（重投影误差）、光度一致性（变形误差）以及图像质量指标（CLIP-IQA+, CLIP Aesthetic）。
*   **对比方法**：
    *   3DGS-MCMC（随机初始化）。
    *   DA3（基于 DepthAnything-3 初始化的 3DGS）。
    *   VGGT-X（基于刚性对齐的最新基准）。
    *   WorldExplorer（针对大规模场景生成的基准）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A6000 GPU**。
*   **耗时**：
    *   几何对齐阶段：平均处理 50 帧图像约需 **25 分钟**，显存占用约 20GB。
    *   高斯溅射优化：5000 次迭代约需 **10 分钟**，显存占用约 8GB。
*   **模型规模**：初始点云约 150 万个高斯点。

### 5. 实验数量与充分性
*   **实验规模**：针对 7 种不同的视频生成模型进行了测试，每种模型包含 2-3 个典型场景（共约 16 个核心测试场景）。
*   **消融实验**：非常充分，分别验证了“仅刚性对齐”、“无逆变形损失”、“无点过滤”、“无稀疏对应”以及“无全局优化”对最终效果的影响。
*   **客观性**：实验不仅对比了渲染质量，还通过 3D 重建指标量化了一致性，并展示了在大规模 360 度场景下的扩展能力，证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **非刚性对齐是关键**：单纯的刚性对齐无法处理视频生成中的几何扭曲，而非刚性对齐能有效将重叠的多个表面“压缩”成单一、清晰的几何表面。
*   **提升显著**：该方法在 3D 一致性和图像保真度上均显著优于现有基准，能够生成可从新视角探索的高质量 3D 环境。
*   **通用性**：该方法不依赖于特定的视频生成模型，具有很强的即插即用属性。

### 7. 优点
*   **创新性**：首次系统性地通过非刚性对齐和逆变形渲染损失来解决视频生成的 3D 不一致性问题。
*   **高质量渲染**：成功消除了 3D 重建中常见的“浮空碎片”和模糊感，使 AI 生成的视频真正具备了 3D 资产的价值。
*   **轻量化**：相比于重新训练一个一致性视频模型，这种重建侧的优化方案成本更低、更易推广。

### 8. 不足与局限
*   **幻觉处理能力有限**：该方法主要解决“几何漂移”（物体位置微调），但如果视频中出现了“幻觉”（如物体突然凭空出现或消失），该方法仍会将其视为静态几何的一部分进行平均化处理。
*   **计算开销**：虽然比微调模型轻量，但每生成一个场景仍需约 35 分钟的额外处理时间，无法做到完全实时生成。
*   **依赖初始化**：重建质量高度依赖于第一阶段 DepthAnything-3 提供的初始深度和位姿质量。

（完）
