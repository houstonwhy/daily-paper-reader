---
title: "GIFSplat: Generative Prior-Guided Iterative Feed-Forward 3D Gaussian Splatting from Sparse Views"
title_zh: GIFSplat：基于生成先验引导的稀疏视图迭代前馈 3D 高斯泼溅
authors: "Tianyu Chen, Wei Xiang, Kang Han, Yu Lu, Di Wu, Gaowen Liu, Ramana Rao Kompella"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22571v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 生成先验引导的迭代前馈3D高斯泼溅
tldr: GIFSplat 提出了一种基于生成先验引导的迭代前馈 3D 高斯泼溅（3DGS）框架，旨在解决稀疏无位姿视图下的 3D 重建难题。该方法通过残差更新逐步优化 3D 场景，并巧妙地将冻结的扩散先验蒸馏到高斯参数中，无需测试时梯度优化即可实现高效且高质量的重建。在多个数据集上，GIFSplat 显著超越了现有前馈基准，在保持秒级推理速度的同时大幅提升了渲染质量。
selection_source: carryover_cache
motivation: 现有前馈 3D 重建方法受限于单次预测范式，在处理稀疏视图和域外数据时缺乏推理优化能力且难以有效融入生成先验。
method: 提出一种纯前馈的迭代细化框架，通过残差更新优化 3D 场景，并利用扩散先验在无需反向传播的情况下增强渲染效果。
result: 在 DL3DV、RealEstate10K 和 DTU 数据集上，GIFSplat 的 PSNR 提升高达 2.1 dB，且无需相机位姿即可维持秒级推理。
conclusion: GIFSplat 证明了迭代前馈机制与生成先验的结合能在保证效率的前提下，显著提升稀疏视图 3D 重建的鲁棒性与精度。
---

## 摘要
前馈 3D 重建相比于逐场景优化具有显著的运行时间优势，而后者在推理时速度较慢，且在稀疏视图下往往表现脆弱。然而，现有的前馈方法在性能提升方面仍有潜力，特别是在域外数据上，且一旦引入生成先验，就难以维持秒级的推理时间。这些局限性源于现有前馈流水线中的单次预测范式：模型受到容量的严格限制，缺乏推理时的细化，且不适合持续注入生成先验。我们提出了 GIFSplat，这是一个用于稀疏无姿态视图 3D 高斯泼溅（3D Gaussian Splatting）的纯前馈迭代细化框架。少量仅前向的残差更新利用渲染证据逐步细化当前的 3D 场景，在效率和质量之间实现了良好的平衡。此外，我们将冻结的扩散先验从增强的新视图渲染中蒸馏为高斯级的线索，无需梯度反向传播或不断扩大的视图集，从而在保持前馈效率的同时，实现了带有生成先验的逐场景自适应。在 DL3DV、RealEstate10K 和 DTU 数据集上，GIFSplat 一致优于最先进的前馈基准模型，PSNR 提升高达 +2.1 dB，且在不需要相机姿态或任何测试时梯度优化的情况下，维持了秒级的推理时间。

## Abstract
Feed-forward 3D reconstruction offers substantial runtime advantages over per-scene optimization, which remains slow at inference and often fragile under sparse views. However, existing feed-forward methods still have potential for further performance gains, especially for out-of-domain data, and struggle to retain second-level inference time once a generative prior is introduced. These limitations stem from the one-shot prediction paradigm in existing feed-forward pipeline: models are strictly bounded by capacity, lack inference-time refinement, and are ill-suited for continuously injecting generative priors. We introduce GIFSplat, a purely feed-forward iterative refinement framework for 3D Gaussian Splatting from sparse unposed views. A small number of forward-only residual updates progressively refine current 3D scene using rendering evidence, achieve favorable balance between efficiency and quality. Furthermore, we distill a frozen diffusion prior into Gaussian-level cues from enhanced novel renderings without gradient backpropagation or ever-increasing view-set expansion, thereby enabling per-scene adaptation with generative prior while preserving feed-forward efficiency. Across DL3DV, RealEstate10K, and DTU, GIFSplat consistently outperforms state-of-the-art feed-forward baselines, improving PSNR by up to +2.1 dB, and it maintains second-scale inference time without requiring camera poses or any test-time gradient optimization.

---

## 论文详细总结（自动生成）

### GIFSplat：基于生成先验引导的稀疏视图迭代前馈 3D 高斯泼溅论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在保证**前馈（Feed-forward）推理效率**的同时，解决稀疏视图下 3D 重建的**精度不足**和**缺乏场景自适应细化**的问题。
*   **研究动机**：
    *   **传统优化法（如 3DGS）**：虽然质量高，但推理时需要数千次梯度下降，速度慢且在稀疏视图下容易崩溃。
    *   **现有前馈法（如 AnySplat）**：虽然快（毫秒级），但属于“单次预测（One-shot）”，受模型容量限制，在复杂或域外场景中容易产生伪影，且难以有效融入生成式先验。
    *   **生成先验集成**：现有的结合扩散模型的方法通常需要漫长的逐场景优化，无法维持秒级推理。

#### 2. 方法论
GIFSplat 提出了一个纯前馈的迭代细化框架，主要包含三个核心组件：
*   **高斯初始化器（Gaussian Initializer）**：基于 AnySplat 改进，从稀疏无位姿视图中预测相机参数和初始 3D 高斯（3DGS）状态。
*   **迭代高斯头（Iterative Gaussian Head）**：
    *   **核心思想**：模拟优化过程，但采用**纯前向残差更新**。
    *   **技术细节**：在每个步骤 $t$，渲染当前 3D 状态并计算其与输入视图的特征差异（观测证据 $o_i$）。利用权重共享的 Transformer 模块（采用窗口注意力机制以提高效率）预测高斯参数的残差 $\Delta g$，逐步更新位置、缩放、颜色和透明度。
*   **生成先验融合（Generative Prior Fusion）**：
    *   **核心思想**：将冻结的扩散模型（Difix）作为“增强器”。
    *   **流程**：增强当前步骤的渲染图，提取增强前后的特征差作为“先验线索 $p_i$”，并将其蒸馏到高斯级别。这种方式无需测试时梯度回传，避免了视图集爆炸。

#### 3. 实验设计
*   **数据集**：
    *   **DL3DV**：大型场景数据集，用于 8 视图输入测试。
    *   **RealEstate10K**：室内外场景，用于 2 视图输入及不同重叠度（小/中/大）测试。
    *   **DTU**：用于评估模型的**跨域泛化能力**（在 RealEstate10K 上训练，在 DTU 上直接测试）。
*   **Benchmark 与对比方法**：
    *   **需位姿方法**：pixelNeRF, pixelSplat, MVSplat。
    *   **无位姿方法**：DUSt3R, MAST3R, NoPoSplat, FLARE, AnySplat。
*   **评价指标**：PSNR, SSIM, LPIPS。

#### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA DGX H200 (141GB GPU)**。
*   **训练时长**：
    *   **DL3DV**：初始化器微调 30K 步，迭代头训练 300K 步。
    *   **RealEstate10K**：初始化器微调 50K 步，迭代头训练 100K 步。
*   **推理效率**：在增加迭代和先验的情况下，依然保持在**秒级**（约 1-3 秒，视迭代次数而定）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2 视图到 8 视图的多种配置，并在三个主流数据集上进行了验证。
*   **消融实验**：详细分析了迭代细化模块、窗口注意力机制、生成先验对性能的影响。
*   **超参数分析**：研究了迭代步数 $T$ 对精度和耗时的权衡（发现 $T=3$ 是最佳平衡点）。
*   **客观性**：对比了大量最新的 SOTA 方法，并专门进行了跨数据集的泛化性测试，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能领先**：GIFSplat 在所有数据集上均优于现有的前馈基准，PSNR 提升最高达 **2.1 dB**。
*   **迭代有效性**：即使不使用生成先验，纯迭代前馈（IFSplat）也显著优于单次预测方法。
*   **先验增益**：引入扩散先验能有效修复稀疏视图下的几何畸变（如门框、墙角）并增强纹理细节。
*   **效率保持**：在无需相机位姿和测试时优化的前提下，实现了高质量重建与秒级推理的平衡。

#### 7. 优点
*   **架构创新**：将“迭代优化”的思想引入“纯前馈”框架，通过权重共享保持了模型参数量的恒定。
*   **高效先验注入**：通过特征空间残差蒸馏扩散先验，避免了传统方法中昂贵的反向传播。
*   **鲁棒性强**：在无相机位姿输入的情况下，对稀疏视图和域外数据表现出极强的鲁棒性。

#### 8. 不足与局限
*   **场景限制**：目前主要针对**静态场景**，尚未扩展到动态视频或 4D 重建。
*   **输入模态固定**：模型目前仅处理图像输入，尚未利用深度图或法线图等辅助几何先验。
*   **计算开销随步数线性增长**：虽然是秒级，但随着迭代步数增加，显存和时间开销仍会线性上升，对于超大规模场景可能存在压力。

（完）
