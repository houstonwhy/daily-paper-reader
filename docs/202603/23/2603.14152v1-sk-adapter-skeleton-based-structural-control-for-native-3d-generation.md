---
title: "SK-Adapter: Skeleton-Based Structural Control for Native 3D Generation"
title_zh: SK-Adapter：基于骨架的原生 3D 生成结构控制
authors: "Anbang Wang, Yuzhuo Ao, Shangzhe Wu, Chi-Keung Tang"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14152v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 原生3D生成的结构控制
tldr: 针对原生3D生成模型缺乏精确结构控制的问题，本文提出了SK-Adapter框架。该方法将3D骨架作为核心控制信号，通过轻量级适配器网络将关节坐标和拓扑结构编码为可学习Token，并注入冻结的生成模型中。此外，研究者构建了包含2.4万个样本的Objaverse-TMS数据集。实验证明，SK-Adapter在保持生成质量的同时实现了鲁棒的结构控制，并支持局部3D编辑。
selection_source: fresh_fetch
motivation: 现有的原生3D生成模型难以通过文本或图像提示实现精确的结构化关节控制。
method: 提出一种轻量级适配器，将3D骨架编码为Token并通过交叉注意力机制注入冻结的3D生成骨干网络。
result: 在保持基础模型几何和纹理质量的同时，实现了显著优于基线的结构控制能力，并支持局部区域编辑。
conclusion: SK-Adapter为原生3D生成提供了高效且精确的骨架控制手段，填补了该领域在结构化操纵方面的空白。
---

## 摘要
原生 3D 生成模型在保真度和速度方面取得了显著成就，但仍面临一个关键限制：无法规定精确的结构关节，且原生 3D 空间内的精确结构控制仍未得到充分探索。本文提出了 SK-Adapter，这是一个简单且高效的框架，为原生 3D 生成解锁了精确的骨架操纵。相比于在精确结构方面可能存在歧义的文本或图像提示词，我们将 3D 骨架视为一类控制信号。SK-Adapter 是一个轻量级的结构适配器网络，它将关节坐标和拓扑结构编码为可学习的 token，并通过交叉注意力机制注入到冻结的 3D 生成主干网络中。这种巧妙的设计使模型不仅能有效地“关注”特定的 3D 结构约束，还能保留其原始的生成先验。为了弥补数据差距，我们贡献了 Objaverse-TMS 数据集，这是一个包含 2.4 万个文本-网格-骨架对的大规模数据集。大量实验证实，我们的方法在保持基础模型几何和纹理质量的同时，实现了鲁棒的结构控制，显著优于现有基准。此外，我们将此能力扩展到局部 3D 编辑，实现了在骨架引导下对现有资产进行特定区域的编辑，这是以前的方法无法实现的。项目页面：https://sk-adapter.github.io/

## Abstract
Native 3D generative models have achieved remarkable fidelity and speed, yet they suffer from a critical limitation: inability to prescribe precise structural articulations, where precise structural control within the native 3D space remains underexplored. This paper proposes SK-Adapter, a simple and yet highly efficient and effective framework that unlocks precise skeletal manipulation for native 3D generation. Moving beyond text or image prompts, which can be ambiguous for precise structure, we treat the 3D skeleton as a first-class control signal. SK-Adapter is a lightweight structural adapter network that encodes joint coordinates and topology into learnable tokens, which are injected into the frozen 3D generation backbone via cross-attention. This smart design allows the model to not only effectively "attend" to specific 3D structural constraints but also preserve its original generative priors. To bridge the data gap, we contribute Objaverse-TMS dataset, a large-scale dataset of 24k text-mesh-skeleton pairs. Extensive experiments confirm that our method achieves robust structural control while preserving the geometry and texture quality of the foundation model, significantly outperforming existing baselines. Furthermore, we extend this capability to local 3D editing, enabling the region specific editing of existing assets with skeletal guidance, which is unattainable by previous methods. Project Page: https://sk-adapter.github.io/

---

## 论文详细总结（自动生成）

以下是对论文《SK-Adapter: Skeleton-Based Structural Control for Native 3D Generation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的原生 3D 生成模型（如 Trellis）虽然速度快、保真度高，但缺乏精确的**结构控制能力**。
*   **研究动机**：
    *   **提示词局限性**：文本提示词语义模糊，图像提示词存在视角局限，均无法精确描述复杂的 3D 关节运动（如“膝盖弯曲 60 度”）。
    *   **现有方案缺陷**：此前的研究（如 SKDream）多采用“2D 提升”策略，将 3D 骨架投影为 2D 图像进行引导，这会导致空间歧义、深度信息丢失以及多视图不一致产生的几何伪影。
    *   **原生空间需求**：为了使生成的 3D 资产直接用于游戏和动画管线，需要在原生 3D 空间内直接注入骨架控制信号。

### 2. 方法论
SK-Adapter 采用轻量级适配器架构，在保持预训练 3D 生成模型（Trellis）权重冻结的前提下，注入结构引导。
*   **核心思想**：将 3D 骨架视为一组稀疏的空间 Token，通过拓扑感知编码和交叉注意力机制，实现骨架与 3D 体素潜空间的对齐。
*   **关键技术细节**：
    1.  **拓扑感知编码（Topology-Aware Encoding）**：利用图相对位置编码（GRPE）处理骨架。它不仅编码关节的 3D 坐标，还通过“拓扑距离”和“边缘关系”（如父子、兄弟、末端执行器）捕捉层级连接性。
    2.  **骨架交叉注意力（Skeletal Cross-Attention）**：在生成主干的 Transformer 块中插入新的注意力层。体素特征作为 Query，骨架 Token 作为 Key 和 Value，使模型能动态“关注”特定的 3D 结构约束。
    3.  **零初始化线性层（Zero-initialized Linear）**：适配器输出通过零初始化的层注入主干，确保训练初期模型输出为“零信号”，从而保护原始模型的生成先验不被破坏。
    4.  **训练范式**：基于潜流匹配（Latent Flow Matching, LFM），仅优化适配器部分（约 1.51 亿参数）。
*   **编辑功能**：结合掩码生成技术，支持基于骨架的局部区域添加、替换和姿态重塑（Repose）。

### 3. 实验设计
*   **数据集**：
    *   **Objaverse-TMS**：论文贡献的大规模数据集，包含 2.4 万个“文本-网格-骨架”三元组，解决了结构引导 3D 生成的数据匮乏问题。
*   **Benchmark**：
    *   **TMS-eval**：包含 140 个测试实例，涵盖类人、动物和其他物体，确保拓扑多样性。
*   **对比方法**：
    *   **SKDream**：基于 2D 投影提升的 SOTA 方法。
    *   **SpaceControl**：基于 Trellis 的无需训练的物理空间引导方法。
*   **评估指标**：
    *   **结构对齐**：ReRigging Score（通过第三方模型对生成结果重新绑定骨架，计算与输入骨架的倒角距离）。
    *   **语义对齐**：CLIP Score。
    *   **视觉质量**：PickScore（人类偏好）、KD-DINO（分布保真度）。

### 4. 资源与算力
*   **训练配置**：训练 200 个 epoch，Batch Size 为 16，学习率为 $1 \times 10^{-5}$。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和数量。
*   **推理效率**：SK-Adapter 生成单个样本耗时小于 15 秒，显著快于 SKDream 的 40 秒。

### 5. 实验数量与充分性
*   **实验规模**：在 140 个平衡类别的测试集上进行了定量评估，并提供了大量的定性可视化结果。
*   **消融实验**：针对“交叉注意力机制”和“拓扑编码器”进行了对比实验，验证了各组件对结构控制和生成质量的贡献。
*   **充分性评价**：实验设计较为全面，涵盖了生成、编辑、动画应用等多个维度。通过 ReRigging Score 这一创新指标，客观地量化了 3D 结构的对齐精度，对比实验具有较强的说服力。

### 6. 主要结论与发现
*   **结构控制精度**：SK-Adapter 在 ReRigging Score 上显著优于基线，证明了在原生 3D 空间注入信号能有效消除空间歧义。
*   **质量保持**：由于采用了冻结主干的 PEFT 策略，模型在实现强力控制的同时，完全保留了基础模型的高质量纹理和几何细节。
*   **通用性**：该方法不仅适用于类人生物，也能有效处理具有复杂拓扑的动物和机械物体。

### 7. 优点
*   **高效轻量**：仅需训练少量参数即可实现强大的控制力，推理速度快。
*   **原生 3D 处理**：避开了 lossy 的 2D 投影过程，生成的资产具有更好的一致性。
*   **数据集贡献**：Objaverse-TMS 为后续 3D 结构控制研究提供了重要资源。
*   **应用广泛**：生成的模型天然适配动画管线，支持 LBS（线性混合蒙皮）驱动。

### 8. 不足与局限
*   **基础模型依赖**：生成质量受限于预训练主干（Trellis），在面部细节、精细纹理（如狼尾巴）上仍存在基础模型的固有缺陷。
*   **复杂拓扑挑战**：当骨架极其复杂（如手指交叉或高密度关节）时，模型可能会产生局部几何混乱。
*   **语义偏差**：文本编码器与 3D 生成器之间的对齐限制了某些特定、复杂属性的准确表达。

（完）
