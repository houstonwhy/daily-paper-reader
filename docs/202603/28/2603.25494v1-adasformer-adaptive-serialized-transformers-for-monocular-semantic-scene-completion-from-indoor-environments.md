---
title: "AdaSFormer: Adaptive Serialized Transformers for Monocular Semantic Scene Completion from Indoor Environments"
title_zh: AdaSFormer：用于室内环境单目语义场景补全的自适应序列化 Transformer
authors: "Xuzhi Wang, Xinran Wu, Song Wang, Lingdong Kong, Ziping Zhao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25494v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 室内3D环境的语义场景补全
tldr: 针对室内单目语义场景补全（MSSC）中复杂布局和遮挡导致的建模难题，本文提出AdaSFormer框架。该方法通过引入具有可学习偏移的自适应序列化Transformer、中心相对位置编码以及卷积调制层归一化，有效降低了Transformer的内存开销并提升了细粒度重建能力。在NYUv2和Occ-ScanNet数据集上的实验证明，该模型在保持高效性的同时达到了当前最先进的性能水平。
selection_source: carryover_cache
motivation: 室内场景复杂的空间布局和严重的遮挡使得传统Transformer在处理单目语义场景补全时面临内存开销大和细粒度细节丢失的问题。
method: 提出了包含可学习偏移的自适应序列化Transformer、中心相对位置编码以及卷积调制层归一化三个核心模块来优化特征提取与融合。
result: 在NYUv2和Occ-ScanNet等主流数据集上的广泛实验表明，AdaSFormer取得了当前最先进的性能表现。
conclusion: AdaSFormer通过改进Transformer的序列化机制和特征表示，为室内单目语义场景补全提供了一个高效且精准的解决方案。
---

## 摘要
室内单目语义场景补全（MSSC）由于复杂的空间布局和严重的遮挡，比室外场景更具挑战性。虽然 Transformer 非常适合建模全局依赖关系，但其高昂的内存开销和在重建细粒度细节方面的困难限制了它们在室内 MSSC 中的应用。为了解决这些局限性，我们提出了 AdaSFormer，这是一个专为室内 MSSC 设计的序列化 Transformer 框架。我们的模型包含三个关键设计：(1) 具有可学习偏移的自适应序列化 Transformer，可动态调整感受野；(2) 捕捉丰富空间信息的中心相对位置编码；(3) 桥接卷积和 Transformer 特征之间异构表示的卷积调制层归一化。在 NYUv2 和 Occ-ScanNet 上的大量实验表明，AdaSFormer 达到了最先进的性能。代码已在 https://github.com/alanWXZ/AdaSFormer 公开。

## Abstract
Indoor monocular semantic scene completion (MSSC) is notably more challenging than its outdoor counterpart due to complex spatial layouts and severe occlusions. While transformers are well suited for modeling global dependencies, their high memory cost and difficulty in reconstructing fine-grained details have limited their use in indoor MSSC. To address these limitations, we introduce AdaSFormer, a serialized transformer framework tailored for indoor MSSC. Our model features three key designs: (1) an Adaptive Serialized Transformer with learnable shifts that dynamically adjust receptive fields; (2) a Center-Relative Positional Encoding that captures spatial information richness; and (3) a Convolution-Modulated Layer Normalization that bridges heterogeneous representations between convolutional and transformer features. Extensive experiments on NYUv2 and Occ-ScanNet demonstrate that AdaSFormer achieves state-of-the-art performance. The code is publicly available at: https://github.com/alanWXZ/AdaSFormer.

---

## 论文详细总结（自动生成）

以下是对论文《AdaSFormer: Adaptive Serialized Transformers for Monocular Semantic Scene Completion from Indoor Environments》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：室内单目语义场景补全（MSSC）旨在从单张 RGB 图像推断出完整的 3D 几何结构和语义标签。
*   **研究背景**：
    *   **室内挑战**：相比室外，室内环境空间布局更复杂（墙壁、家具重叠），且遮挡现象极其严重。
    *   **现有技术局限**：传统的卷积神经网络（CNN）感受野有限，难以处理长程依赖；而标准 Transformer 处理高分辨率 3D 体素时，内存开销呈立方级增长，且难以捕捉细粒度几何细节。
*   **研究目的**：提出一种高效的混合架构，结合 Transformer 的全局建模能力与 CNN 的局部精细化能力，同时解决 Transformer 在 3D 任务中的计算瓶颈。

### 2. 方法论：核心思想与关键技术
AdaSFormer 采用“2D 编码 -> 深度估计 -> 3D 投影 -> 3D 编码解码”的流程，其核心创新在于 3D 编码器中的三个设计：
*   **自适应序列化注意力（ASA）**：
    *   **核心思想**：将 3D 体素沿空间填充曲线（如 Z 曲线）序列化为 1D 序列，并进行分组注意力计算以降低复杂度。
    *   **关键技术**：引入**可学习的偏移（Learnable Shifts）**。通过 **Gumbel-Softmax** 算子实现离散偏移量的端到端训练，使模型能根据输入内容动态调整序列化的起始点，从而灵活改变感受野覆盖范围，更好地捕捉物体结构。
*   **中心相对位置编码（CRPE）**：
    *   **核心思想**：不再使用绝对位置编码，而是计算每个体素相对于场景中心（所有占据体素的平均坐标）的**偏航角（Yaw）和俯仰角（Pitch）**。
    *   **作用**：增强模型对空间信息丰富度分布的感知，帮助模型识别关键结构区域。
*   **卷积调制层归一化（CMLN）**：
    *   **核心思想**：在 Transformer 层与卷积层（DDR 块）交替时，利用体素特征动态生成归一化参数（$\gamma$ 和 $\beta$）。
    *   **作用**：桥接了 Transformer 的全局特征与 CNN 的局部特征，解决了异构表示之间的对齐难题。

### 3. 实验设计
*   **数据集**：
    *   **NYUv2**：包含 1,449 张室内深度图转换的体素标注，分为 795 张训练和 654 张测试。
    *   **Occ-ScanNet**：大规模室内基准，包含约 4.5 万个训练帧和 1.9 万个验证帧。
*   **Benchmark 与对比方法**：
    *   对比了 MonoScene、NDC-Scene、ISO、MonoMRN、TPVFormer、GaussianFormer 等 SOTA 方法。
    *   **评价指标**：场景补全（SC）的 IoU，以及语义场景补全（SSC）的平均 IoU（mIoU）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100 GPU** 上进行。
*   **训练细节**：使用随机梯度下降（SGD）优化器，初始学习率在 NYUv2 上为 0.1，在 Occ-ScanNet 上为 0.003；采用多项式学习率衰减策略；训练时长为 100 个 Epoch。
*   **效率表现**：论文明确给出了延迟测试，AdaSFormer 的推理延迟为 **178.9 ms**，显著优于对比方法 ISO（535.2 ms）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了全面的性能对比。
    *   **消融实验**：针对 ASA、CRPE、CMLN 三个核心组件分别进行了剥离实验；针对 ASA 的偏移策略（Vanilla vs Gumbel vs Annealed）和 CRPE 的编码方式进行了深入探讨。
    *   **可视化分析**：提供了多组定性对比图，展示了模型在处理复杂遮挡和细小物体（如椅子、桌子）时的补全效果。
*   **评价**：实验设计客观、充分，涵盖了性能、效率、组件贡献度等多个维度，对比基准统一。

### 6. 主要结论与发现
*   **性能领先**：在 NYUv2 上达到 32.50% mIoU，在 Occ-ScanNet 上达到 45.33% mIoU，均刷新了 SOTA 记录。
*   **效率优势**：通过序列化 Transformer 机制，在保持全局感受野的同时，内存消耗远低于传统 Transformer，且推理速度比之前的领先方法快了近 3 倍。
*   **自适应性**：实验证明可学习的序列化偏移能显著提升模型对物体边界和场景布局的建模能力。

### 7. 优点（亮点）
*   **创新的自适应机制**：首次在序列化 Transformer 中引入可学习偏移，解决了固定分组导致的感受野受限问题。
*   **轻量化与高性能的平衡**：通过 CMLN 巧妙融合了 CNN 和 Transformer，既保留了 CNN 的局部细节恢复能力，又获得了 Transformer 的全局推理能力。
*   **空间感知增强**：CRPE 利用几何角度信息，为 3D 场景理解提供了一种比传统位置编码更具物理意义的表示。

### 8. 不足与局限
*   **实时性挑战**：虽然比 ISO 快，但 178.9 ms 的延迟（约 5.6 FPS）对于需要高帧率的实时机器人导航任务仍显不足。
*   **依赖预处理**：模型性能在一定程度上受限于前端深度估计网络（如 Depth Anything）的准确性。
*   **应用限制**：目前主要针对室内静态场景，尚未讨论在动态环境或超大规模室外场景中的泛化能力。

（完）
