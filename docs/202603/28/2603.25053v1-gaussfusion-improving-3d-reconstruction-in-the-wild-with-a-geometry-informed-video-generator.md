---
title: "GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator"
title_zh: GaussFusion：利用几何感知视频生成器改进野外环境下的三维重建
authors: "Liyuan Zhu, Manjunath Narayana, Michal Stary, Will Hutchcroft, Gordon Wetzstein, Iro Armeni"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25053v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于3D重建的几何感知视频生成器
tldr: GaussFusion 针对 3D 高斯泼溅（3DGS）在野外场景中常见的伪影、闪烁和模糊问题，提出了一种结合几何信息的视频生成增强方法。该方法通过渲染包含深度、法线等信息的几何缓冲视频，并利用视频到视频生成器进行细化，显著提升了重建质量。其主要贡献包括引入几何感知生成器、伪影合成流水线以及实现实时运行的高效变体，在保持时空一致性的同时消除了重建缺陷。
selection_source: fresh_fetch
motivation: 旨在解决 3DGS 在相机位姿误差、覆盖不全或噪声初始化下产生的漂浮物、闪烁和模糊等重建伪影问题。
method: 利用包含深度、法线、不透明度和协方差的几何缓冲视频，通过视频到视频生成器对 3DGS 渲染帧进行时空一致的细化。
result: 在多个新视角合成基准上达到 SOTA 性能，且其实时变体在保持高质量的同时可达 21 FPS 的运行速度。
conclusion: GaussFusion 通过将几何先验与视频生成相结合，有效提升了 3DGS 在复杂野外环境下的重建鲁棒性与视觉质量。
---

## 摘要
我们提出了 GaussFusion，这是一种通过几何感知视频生成来改进野外环境下三维高斯泼溅（3DGS）重建的新方法。GaussFusion 缓解了常见的 3DGS 伪影，包括由相机位姿误差、覆盖不完整和噪声几何初始化引起的漂浮物、闪烁和模糊。与以往局限于单一重建流水线的基于 RGB 的方法不同，我们的方法引入了一个几何感知的视频到视频生成器，能够优化基于优化和前馈方法的 3DGS 渲染。给定现有的重建结果，我们渲染一个编码了深度、法线、不透明度和协方差的高斯基元视频缓冲区，生成器对其进行细化，以产生时间上连贯且无伪影的帧。我们还引入了一个伪影合成流水线，用于模拟多种退化模式，从而确保鲁棒性和泛化能力。GaussFusion 在新视角合成基准测试中达到了最先进的性能，其高效变体在保持相似性能的同时能以 21 FPS 的速度实时运行，从而支持交互式三维应用。

## Abstract
We present GaussFusion, a novel approach for improving 3D Gaussian splatting (3DGS) reconstructions in the wild through geometry-informed video generation. GaussFusion mitigates common 3DGS artifacts, including floaters, flickering, and blur caused by camera pose errors, incomplete coverage, and noisy geometry initialization. Unlike prior RGB-based approaches limited to a single reconstruction pipeline, our method introduces a geometry-informed video-to-video generator that refines 3DGS renderings across both optimization-based and feed-forward methods. Given an existing reconstruction, we render a Gaussian primitive video buffer encoding depth, normals, opacity, and covariance, which the generator refines to produce temporally coherent, artifact-free frames. We further introduce an artifact synthesis pipeline that simulates diverse degradation patterns, ensuring robustness and generalization. GaussFusion achieves state-of-the-art performance on novel-view synthesis benchmarks, and an efficient variant runs in real time at 21 FPS while maintaining similar performance, enabling interactive 3D applications.

---

## 论文详细总结（自动生成）

### GaussFusion：利用几何感知视频生成器改进野外环境下的三维重建

#### 1. 核心问题与整体含义（研究动机和背景）
三维高斯泼溅（3DGS）虽然在实时渲染和高质量重建方面表现出色，但在处理“野外”场景（如稀疏视角、相机位姿误差、覆盖不全）时，极易产生漂浮物（floaters）、闪烁和模糊等伪影。现有的基于生成先验的修复方法大多仅依赖 RGB 图像或简单的深度图，难以区分真实的场景内容与几何错误。
**GaussFusion 的核心动机**是：通过引入丰富的几何基元信息（如法线、不透明度、协方差等）作为视频生成器的约束，使模型能够“看穿”几何错误并生成时空一致的清晰渲染，从而反过来优化 3DGS 的底层表示。

#### 2. 方法论
GaussFusion 提出了一套从“渲染-细化-优化”的闭环流程：
*   **高斯基元缓冲区（GP-Buffer）：** 相比于传统的 RGB 渲染，该方法额外提取了像素对齐的几何特征，包括：颜色（C）、不透明度（A）、深度（D）、表面法线（N）以及**几何不确定性（U，即逆投影协方差）**。这些多模态信息为识别和修复伪影提供了关键线索。
*   **几何感知视频生成器：** 基于流匹配（Flow Matching）的 Transformer 架构（DiT）。核心组件是**几何适配器（Geometry Adapter, GA）**，它作为一个并行侧网络，将 GP-Buffer 特征注入到主生成流中，实现几何引导的视频到视频（V2V）转换。
*   **伪影合成流水线：** 为了提高泛化性，研究者设计了复杂的模拟策略，包括随机稀疏采样、多种初始化方式（SfM、随机点云、前馈模型预测）以及模拟前馈模型特有的退化模式（如尖刺几何、半透明高斯），构建了超过 7.5 万对高质量/退化视频数据集。
*   **两阶段蒸馏：** 采用分布匹配蒸馏（DMD）技术，将原本需要 30-50 步的扩散过程压缩至 4 步，实现了实时推理。

#### 3. 实验设计
*   **数据集：** 主要在 **DL3DV**（大规模场景数据集）和 **RE10K**（真实房地产视频数据集）上进行评估。
*   **Benchmark 与对比方法：**
    *   **优化类基准：** Splatfacto (3DGS 基础实现)。
    *   **生成修复类：** GenFusion (CVPR 2025)、Difix3D+ (CVPR 2025)、ExploreGS (ICCV 2025)。
    *   **前馈类：** MVSplat360 (针对特定前馈模型的修复)。
*   **评估指标：** PSNR、SSIM、LPIPS（感知相似度）和 FID（图像质量）。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA H200 GPU** 进行训练。
*   **训练细节：** 训练时长约为 100K 步，批大小（Batch Size）为 8，分辨率为 480×832。
*   **推理效率：** 经过蒸馏后的 4 步模型在单块 H200 上可达到 **15.11 - 16 FPS** 的实时处理速度。

#### 5. 实验数量与充分性
论文进行了多维度的实验验证：
*   **定量对比：** 在两个主流数据集上全面超越了 2025 年的最先进方法。
*   **消融实验：** 验证了 GP-Buffer 中各模态（深度、法线、协方差等）的贡献，证明了加入几何信息能显著提升 PSNR（从 19.1 提升至 20.7）。
*   **架构对比：** 对比了“几何适配器（GA）”与简单的“卷积融合”，证明 GA 收敛更快且性能更优。
*   **泛化性测试：** 实验涵盖了基于优化的 3DGS 和基于前馈（Feed-forward）的 3DGS（如 DepthSplat, MVSplat），证明了该模型不依赖于特定的重建算法。
**评价：** 实验设计非常充分，对比对象均为最新的 SOTA 方法，且消融实验逻辑严密，客观地展示了各组件的价值。

#### 6. 主要结论与发现
*   **几何信息至关重要：** 仅靠 RGB 无法有效修复严重的几何伪影，GP-Buffer 提供的结构化信息是消除漂浮物的关键。
*   **视频一致性：** 相比于单帧修复（如 Difix3D+），视频生成器能显著减少视角切换时的闪烁，提高 3D 重建的整体一致性。
*   **联合训练优势：** 在多种退化模式（优化误差+前馈误差）上混合训练，能显著提升模型在单一任务上的表现。

#### 7. 优点
*   **通用性强：** 不绑定特定的 3DGS 算法，可作为通用的“后处理插件”提升任何 3DGS 重建质量。
*   **实时性：** 通过 DMD 蒸馏实现了 16 FPS 的推理，这在基于扩散/流匹配的视频生成任务中非常罕见，具备实际应用潜力。
*   **几何引导设计：** 引入“逆协方差”作为几何不确定性的度量，是一个非常巧妙且符合 3DGS 特性的创新。

#### 8. 不足与局限
*   **长视频一致性：** 虽然在 81 帧的窗口内表现良好，但对于超长视频，采用滑动窗口法仍可能存在长期漂移或不连贯风险。
*   **运动模糊敏感：** 在剧烈运动或严重运动模糊的输入下，VAE 编码器可能会失效，导致生成质量下降。
*   **算力门槛：** 尽管推理快，但训练过程依赖 8 卡 H200 等顶级算力，普通开发者难以复现训练过程。
*   **动态场景限制：** 目前主要针对静态场景重建，尚未扩展到动态 4D 场景。

（完）
