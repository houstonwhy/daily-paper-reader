---
title: "MV2UV: Generating High-quality UV Texture Maps with Multiview Prompts"
title_zh: MV2UV：利用多视角提示生成高质量 UV 纹理贴图
authors: "Zheng Zhang, Qinchuan Zhang, Yuteng Ye, Zhi Chen, Penglei Ji, Mengfei Li, Wenxiao Zhang, Yuan Liu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15436v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 为3D资产生成高质量纹理
tldr: 针对3D资产纹理生成中存在的多视角不一致和遮挡区域缺失问题，本文提出MV2UV。该方法结合了多视角生成的2D先验与UV空间的修复能力，通过在UV空间构建生成模型，同时解决视角冲突并补全未见区域。实验证明，MV2UV在生成质量上优于现有方法，特别是在处理复杂遮挡和不一致性方面表现出色。
selection_source: fresh_fetch
motivation: 现有多视角纹理生成方法难以保证视角一致性，而UV修复方法则受限于数据匮乏且难以利用强大的2D扩散先验。
method: 提出一种结合多视角提示的UV空间生成模型，利用2D生成先验来修复多视角图像中的缺失部分并消除不一致性。
result: 实验结果显示，该方法在纹理生成的质量上显著提升，尤其是在处理被遮挡区域和多视角冲突部分时效果更佳。
conclusion: MV2UV通过融合多视角先验与UV空间细化，为高质量、一致性的3D纹理生成提供了一种有效的解决方案。
---

## 摘要
为 3D 资产生成高质量纹理是一项具有挑战性的任务。现有的多视角纹理生成方法存在多视角不一致以及不可见部分纹理缺失的问题，而 UV 修复纹理方法由于 UV 数据不足导致泛化能力较差，且无法很好地利用 2D 图像扩散先验。在本文中，我们提出了一种名为 MV2UV 的新方法，该方法结合了来自多视角生成的 2D 生成先验和 UV 精细化的修复能力，以获取高质量的纹理贴图。我们的核心思想是采用一种 UV 空间生成模型，在解决多视角图像不一致性的同时，同步修复多视角图像中的不可见部分。实验表明，我们的方法比现有方法具有更好的纹理生成质量，特别是在不可见的遮挡部分和多视角不一致的部分。

## Abstract
Generating high-quality textures for 3D assets is a challenging task. Existing multiview texture generation methods suffer from the multiview inconsistency and missing textures on unseen parts, while UV inpainting texture methods do not generalize well due to insufficient UV data and cannot well utilize 2D image diffusion priors. In this paper, we propose a new method called MV2UV that combines 2D generative priors from multiview generation and the inpainting ability of UV refinement to get high-quality texture maps. Our key idea is to adopt a UV space generative model that simultaneously inpaints unseen parts of multiview images while resolving the inconsistency of multiview images. Experiments show that our method enables a better texture generation quality than existing methods, especially in unseen occluded and multiview-inconsistent parts.

---

## 论文详细总结（自动生成）

以下是对论文《MV2UV: Generating High-quality UV Texture Maps with Multiview Prompts》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：为3D资产生成高质量纹理是3D内容创作的关键。现有方法主要分为两类：
    *   **多视角投影法**：先生成多视角图像再投影回UV图。缺点是视角间不一致会导致纹理模糊或冲突，且遮挡区域（不可见部分）纹理缺失。
    *   **直接UV生成法**：在UV空间直接生成纹理。缺点是UV布局缺乏3D空间逻辑，难以利用强大的2D图像扩散先验，导致结构逻辑错误。
*   **核心问题**：如何在利用2D扩散模型强大先验的同时，既能保证多视角的一致性，又能完美补全遮挡区域的纹理？
*   **整体含义**：MV2UV提出了一种“多视角提示引导”的UV生成框架，将生成的视角图像视为语义参考而非直接投影源，从而在UV空间内自主解决冲突并实现语义一致的补全。

### 2. 论文提出的方法论
*   **核心思想**：采用两阶段框架。第一阶段利用多视角扩散模型生成参考图；第二阶段将这些图作为“语义提示（Prompts）”，通过带有几何感知的扩散模型在UV空间生成最终纹理。
*   **关键技术细节**：
    *   **几何位置编码（XYZ-based PE）**：利用3D坐标（XYZ）和法线作为位置编码。这使得UV图上的每个像素能精确地“对齐”到多视角图像中具有相同3D坐标的区域。
    *   **参考注意力机制（Reference Attention）**：在扩散模型中引入参考层，让UV特征图通过交叉注意力机制从多视角特征中提取颜色和纹理信息。
    *   **UV自注意力机制（UV Self-Attention）**：针对UV孤岛（Islands）断裂问题，利用3D坐标引导自注意力，使在3D空间邻近但在UV空间分离的区域能够相互通信，保证纹理连续性。
    *   **架构改进**：基于Stable Diffusion XL (SDXL) 进行微调，冻结原始权重，并行加入参考注意力和UV自注意力分支。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 Objaverse 的子集（约8万个样本），经过重新网格化和UV展开。
    *   **测试集**：Google Scanned Objects (GSO) 和 Digital Twin Catalog (DTC)，涵盖家具、织物等多种类别。
*   **Benchmark与对比方法**：
    *   对比了 **TEXGen**（UV生成类）、**Hunyuan3D 2.1**（多视角类）、**UniTEX**、**MV-Adapter** 等前沿方法。
*   **评价指标**：FID（图像质量）、KID（分布差异）、PSNR/SSIM（重建精度）。

### 4. 资源与算力
*   **训练配置**：在8万个样本上训练了10个Epoch。
*   **超参数**：学习率为 5e-5，Batch Size 为 32。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体训练时长。但考虑到 SDXL 的微调规模和 80k 样本量，预计需要高性能 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 GSO 和 DTC 数据集上各随机抽取 200 个实例进行定量评估。
    *   针对“遮挡区域补全”进行了专门的定量对比（10个复杂自遮挡样本）。
    *   进行了详细的消融实验，分别验证了几何位置编码、参考注意力、UV自注意力的必要性。
*   **充分性评价**：实验设计较为充分，不仅对比了整体质量，还针对该方法的核心卖点（解决不一致性、补全遮挡）做了专项验证。对比基准包含了最新的工业级模型（如混元3D 2.1），具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：MV2UV 在 FID 指标上显著优于现有方法（在 GSO 上提升 50.8，在 DTC 上提升 14.7）。
*   **冲突解决**：相比直接投影，MV2UV 能自动识别并调和多视角图像中的颜色/结构冲突，生成更清晰的细节。
*   **语义补全**：对于视角不可见的遮挡区域，模型能根据邻近区域的语义信息进行合理“脑补”，补全质量比基准方法提升 2 倍。
*   **几何感知**：引入 3D 坐标作为位置编码是实现跨模态（视角到UV）精确对齐的关键。

### 7. 优点（亮点）
*   **设计巧妙**：将多视角图像从“数据源”转变为“语义提示”，避开了硬性投影带来的伪影问题。
*   **解决断裂感**：UV自注意力机制有效解决了传统UV贴图在接缝处的纹理不连续问题。
*   **鲁棒性强**：实验证明即使输入的参考图存在明显不一致，模型仍能生成和谐的UV图。

### 8. 不足与局限
*   **文本渲染缺陷**：由于 VAE 编解码器的容量限制和分辨率瓶颈，模型在处理带有文字（如标签、书籍封面）的纹理时会出现模糊或扭曲。
*   **分辨率限制**：目前纹理分辨率上限为 1024x1024，对于超高质量的影视级资产可能仍显不足。
*   **依赖前置模型**：最终效果高度依赖第一阶段多视角生成模型的质量。

（完）
