---
title: "From Part to Whole: 3D Generative World Model with an Adaptive Structural Hierarchy"
title_zh: 从局部到整体：具有自适应结构层级的 3D 生成式世界模型
authors: "Bi'an Du, Daizong Liu, Pufan Li, Wei Hu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21557v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于单图3D生成的3D生成式世界模型
tldr: 本研究针对单图3D生成中跨类别泛化和结构复杂性处理的难题，提出了一种具有自适应结构层级的3D生成世界模型。该模型通过从图像Token中推断软组合掩码，自主发现潜在结构槽位，并利用自适应槽位门控机制动态调整结构复杂度。结合类无关的原型库实现跨类别几何先验共享，显著提升了模型在处理新颖布局和复杂结构时的泛化能力与生成质量。
selection_source: fresh_fetch
motivation: 现有的3D生成方法多采用整体建模或固定数量的部件划分，难以应对多样化的语义类别和复杂的结构变化，且容易导致过拟合或结构缺失。
method: 提出一种自适应槽位门控机制来动态确定部件激活概率，并结合类无关的几何原型库，通过轻量级3D扩散去噪器实现从局部到整体的结构化生成。
result: 实验证明该方法在跨类别迁移和部件数量外推方面表现优异，能够生成结构紧凑且表达力强的3D模型。
conclusion: 通过引入自适应结构层级和通用几何原型，该模型有效解决了单图3D生成中的组合泛化难题，为构建通用的3D生成世界模型提供了新思路。
---

## 摘要
单图像 3D 生成是现实世界中视觉到图形（vision-to-graphics）模型的核心。然而，在稀疏监督下，实现跨多样语义类别和高度多变结构复杂性的可靠泛化仍然是一个根本性挑战。现有方法通常以整体（monolithic）方式对物体建模，或者依赖固定数量的部件，包括最近的部件感知模型（如 PartCrafter），这些模型仍需要用户手动指定部件数量，费时费力。这种设计容易导致过拟合、结构组件破碎或缺失，以及在遇到新颖物体布局时组合泛化能力有限。为此，本文将单图像 3D 生成重新构思为在灵活的 3D 潜空间中学习自适应的“局部-整体”层级。我们提出了一种新型的“从局部到整体”的 3D 生成式世界模型，该模型通过直接从图像 token 中推断软性组合掩码，自主发现潜在的结构槽位（structural slots）。具体而言，一种自适应槽位门控机制动态确定每个槽位的激活概率，并平滑地整合不同物体中的冗余槽位，确保生成的结构在跨类别时保持紧凑且具有表现力。每个提取出的槽位随后与一个可学习的、类别无关的原型库（prototype bank）对齐，通过现实世界中的通用几何原型实现强大的跨类别形状共享和去噪。此外，引入了一个轻量级 3D 去噪器，通过统一的扩散目标重建几何形状和外观。实验表明，该方法在跨类别迁移和部件数量外推方面取得了持续提升，消融实验证实了原型库在形状先验共享以及槽位门控在结构自适应方面的互补优势。

## Abstract
Single-image 3D generation lies at the core of vision-to-graphics models in the real world. However, it remains a fundamental challenge to achieve reliable generalization across diverse semantic categories and highly variable structural complexity under sparse supervision. Existing approaches typically model objects in a monolithic manner or rely on a fixed number of parts, including recent part-aware models such as PartCrafter, which still require a labor-intensive user-specified part count. Such designs easily lead to overfitting, fragmented or missing structural components, and limited compositional generalization when encountering novel object layouts. To this end, this paper rethinks single-image 3D generation as learning an adaptive part-whole hierarchy in the flexible 3D latent space. We present a novel part-to-whole 3D generative world model that autonomously discovers latent structural slots by inferring soft and compositional masks directly from image tokens. Specifically, an adaptive slot-gating mechanism dynamically determines the slot-wise activation probabilities and smoothly consolidates redundant slots within different objects, ensuring that the emergent structure remains compact yet expressive across categories. Each distilled slot is then aligned to a learnable, class-agnostic prototype bank, enabling powerful cross-category shape sharing and denoising through universal geometric prototypes in the real world. Furthermore, a lightweight 3D denoiser is introduced to reconstruct geometry and appearance via unified diffusion objectives. Experiments show consistent gains in cross-category transfer and part-count extrapolation, and ablations confirm complementary benefits of the prototype bank for shape-prior sharing as well as slot-gating for structural adaptation.

---

## 论文详细总结（自动生成）

以下是对论文《From Part to Whole: 3D Generative World Model with an Adaptive Structural Hierarchy》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：单图像 3D 生成在处理多样化语义类别和复杂结构时，难以实现可靠的泛化。
*   **研究背景**：
    *   **现有方法的局限性**：大多数方法采用“整体式（Monolithic）”建模（难以恢复细长或遮挡结构）或“固定部件（Fixed-part）”建模（需要手动指定部件数量，导致简单物体过度分割或复杂场景分割不足）。
    *   **缺乏共享先验**：现有模型通常将部件视为独立组件，无法捕捉跨类别的通用结构先验（如桌腿、把手等重复出现的几何模式）。
*   **整体含义**：本文将 3D 生成重新定义为学习一种**自适应的“局部-整体”层级结构**，通过动态发现潜在结构槽位（Slots）和共享几何原型，构建一个更具通用性的 3D 生成式世界模型。

### 2. 方法论
核心思想是构建一个基于 3D 潜空间（Latent Space）的结构化世界状态，包含以下三个关键组件：
*   **结构化潜槽位（Structured Latent Slots）**：
    *   利用预训练的 3D VAE 将物体表示为一组潜在槽位。
    *   引入**掩码流匹配（Masked Flow-matching）**目标：仅对对应真实部件的激活槽位进行监督，使模型能灵活处理不同数量的部件。
*   **自适应槽位门控（Adaptive Slot-Gating）**：
    *   通过一个门控网络从图像特征（DINOv2）中预测每个槽位的激活概率 $\hat{\alpha}$。
    *   使用二元交叉熵损失（$L_{ce}$）和软计数损失（$L_{count}$）进行训练，使模型能根据输入图像自主决定部件数量。
*   **类无关原型库（Class-agnostic Prototype Bank）**：
    *   维护一组可学习的几何原型向量。每个激活槽位通过注意力机制与原型对齐，并将原型引导的残差注入潜变量中。
    *   **作用**：促进跨类别的几何先验共享，增强模型对新颖布局的组合泛化能力。
*   **训练流程**：分为两步：1) 槽位与原型的预热训练（冻结主干）；2) 主干感知的联合微调。

### 3. 实验设计
*   **数据集**：
    *   **物体级**：Objaverse（高质量子集）、ShapeNet-Core、ABO（Amazon Berkeley Objects）。
    *   **场景级**：3D-Front（室内场景）。
*   **Benchmark 与指标**：
    *   **全局保真度**：Chamfer Distance (CD)、F-Score。
    *   **部件独立性**：Voxel IoU（衡量部件间的重叠与穿插程度，越低越好）。
*   **对比方法**：
    *   **单体生成模型**：TripoSR / TripoSG。
    *   **部件感知模型**：HoloPart、PartCrafter。
    *   **场景生成模型**：MIDI（多实例扩散模型）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到基于预训练的 PartCrafter 风格的 3D VAE + DiT 架构进行构建，使用了 DINOv2 提取图像特征。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流物体数据集和一个场景数据集上进行了大规模测试。
    *   进行了详细的**消融实验**，分别验证了自适应槽位门控（ASG）、原型库（PB）以及预热阶段（Warm-up）的有效性。
*   **充分性与公平性**：
    *   实验覆盖了从简单物体到复杂室内场景的多种尺度。
    *   对比了最新的 SOTA 方法，并使用了标准化的几何度量指标。
    *   通过可视化展示了在遮挡和复杂布局下的生成效果，证明了实验的客观性。

### 6. 主要结论与发现
*   **自适应性**：自适应槽位门控能有效处理不同复杂度的物体，避免了手动指定部件数量的繁琐。
*   **泛化性**：原型库显著提升了跨类别生成的质量，使得模型能利用“常识性”的几何结构来修补遮挡部分。
*   **性能提升**：在 Objaverse 和 3D-Front 数据集上，该方法在 F-Score 和 CD 指标上均优于 PartCrafter 和 MIDI，且部件间的穿插（IoU）更少，结构更清晰。

### 7. 优点（亮点）
*   **结构化思维**：将 3D 生成从单纯的像素/体素回归提升到了“结构化世界状态”的理解高度。
*   **动态灵活性**：解决了部件数量固定的硬伤，使模型能根据图像内容动态调整模型容量。
*   **先验共享**：原型库的设计巧妙地平衡了“特定实例的细节”与“跨类别的通用几何规律”。

### 8. 不足与局限
*   **计算开销**：虽然引入了轻量级去噪器，但多槽位的扩散过程和原型对齐可能会增加推理时的内存占用。
*   **依赖预训练模型**：性能在很大程度上受限于底层 3D VAE 的重建质量和 DiT 主干的初始能力。
*   **复杂场景限制**：虽然在 3D-Front 上表现良好，但对于极度拥挤或无序的室外大规模场景，其槽位分配机制的稳定性仍有待验证。

（完）
