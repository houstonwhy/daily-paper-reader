---
title: "RealMaster: Lifting Rendered Scenes into Photorealistic Video"
title_zh: RealMaster：将渲染场景提升为写实视频
authors: "Dana Cohen-Bar, Ido Sobol, Raphael Bensadoun, Shelly Sheynin, Oran Gafni, Or Patashnik, Daniel Cohen-Or, Amit Zohar"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23462v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 将渲染场景提升为写实视频的生成模型
tldr: RealMaster 旨在解决 3D 引擎渲染视频缺乏真实感与视频生成模型缺乏精确控制及 3D 一致性的矛盾。该方法利用视频扩散模型，通过锚点传播策略生成高质量配对数据集，并训练 IC-LoRA 模型，将 3D 渲染视频提升为保持几何与动力学一致性的照片级真实视频，在 GTA-V 等复杂场景中表现优异。
selection_source: fresh_fetch
motivation: 现有的视频生成模型难以实现精确控制和 3D 一致性，而 3D 引擎渲染结果往往缺乏真实感。
method: 采用基于锚点的传播策略生成配对数据，并训练 IC-LoRA 以实现从渲染视频到真实视频的语义转换与几何保持。
result: 在 GTA-V 序列上的评估显示，RealMaster 在提升照片级真实感的同时，完美保留了原始 3D 控制的几何、动力学和身份信息。
conclusion: RealMaster 成功弥合了模拟与现实之间的差距，为受控的高质量视频生成提供了有效的解决方案。
---

## 摘要
最先进的视频生成模型展现了卓越的写实感，但缺乏将生成内容与特定场景需求对齐所需的精确控制。此外，由于缺乏底层的显式几何结构，这些模型无法保证 3D 一致性。相反，3D 引擎对每个场景元素都提供了细粒度的控制，并在设计上提供了原生的 3D 一致性，但其输出往往仍受困于“恐怖谷”效应。弥合这种从模拟到现实（sim-to-real）的差距既需要结构精度（输出必须精确保留输入的几何结构和动力学），也需要全局语义转换（必须对材质、光照和纹理进行整体转换以实现写实感）。我们提出了 RealMaster，这是一种利用视频扩散模型将渲染视频提升为写实视频的方法，同时保持与 3D 引擎输出的完全对齐。为了训练该模型，我们通过一种基于锚点的传播策略生成了一个配对数据集，其中第一帧和最后一帧的写实感得到了增强，并利用几何条件线索在中间帧之间进行传播。然后，我们在这些配对视频上训练了一个 IC-LoRA，将流水线的高质量输出蒸馏到一个能够超越流水线限制进行泛化的模型中，从而处理序列中间出现的物体和角色，并实现无需锚帧的推理。在复杂的 GTA-V 序列上进行的评估表明，RealMaster 显著优于现有的视频编辑基准，在提高写实感的同时，保留了原始 3D 控制所指定的几何结构、动力学和身份特征。

## Abstract
State-of-the-art video generation models produce remarkable photorealism, but they lack the precise control required to align generated content with specific scene requirements. Furthermore, without an underlying explicit geometry, these models cannot guarantee 3D consistency. Conversely, 3D engines offer granular control over every scene element and provide native 3D consistency by design, yet their output often remains trapped in the "uncanny valley". Bridging this sim-to-real gap requires both structural precision, where the output must exactly preserve the geometry and dynamics of the input, and global semantic transformation, where materials, lighting, and textures must be holistically transformed to achieve photorealism. We present RealMaster, a method that leverages video diffusion models to lift rendered video into photorealistic video while maintaining full alignment with the output of the 3D engine. To train this model, we generate a paired dataset via an anchor-based propagation strategy, where the first and last frames are enhanced for realism and propagated across the intermediate frames using geometric conditioning cues. We then train an IC-LoRA on these paired videos to distill the high-quality outputs of the pipeline into a model that generalizes beyond the pipeline's constraints, handling objects and characters that appear mid-sequence and enabling inference without requiring anchor frames. Evaluated on complex GTA-V sequences, RealMaster significantly outperforms existing video editing baselines, improving photorealism while preserving the geometry, dynamics, and identity specified by the original 3D control.

---

## 论文详细总结（自动生成）

这是一份关于论文《RealMaster: Lifting Rendered Scenes into Photorealistic Video》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视频生成模型（如 Sora、Gen-2）虽能生成极具真实感的视频，但缺乏对场景元素的精确控制和 3D 一致性保证。相反，传统的 3D 渲染引擎（如 GTA-V 游戏引擎）拥有完美的几何控制，但输出往往带有“塑料感”，处于“恐怖谷”效应中。
*   **核心问题**：如何弥合“模拟到现实”（Sim-to-Real）的差距？这需要同时满足两个矛盾的目标：**结构精度**（保留原始几何、动力学和身份）与**全局语义转换**（将材质、光照和纹理彻底写实化）。
*   **整体含义**：RealMaster 将视频扩散模型定位为 3D 引擎之上的“第二阶段渲染器”，既保留了图形学的可控性，又赋予了生成模型的写实感。

### 2. 方法论
RealMaster 采用两阶段框架：
*   **阶段一：高质量配对数据生成（Sparse-to-Dense 策略）**
    *   **关键帧增强**：利用图像编辑模型（如 Qwen-Image-Edit）将渲染视频的第一帧和最后一帧转换为写实图像，作为“外观锚点”。
    *   **边缘引导传播**：使用视频生成模型（VACE），以原始视频的边缘图（Edge Maps）为结构约束，将两个锚点的写实外观传播到中间帧。
    *   **数据过滤**：通过 ArcFace 人脸识别模型计算身份相似度，剔除身份漂移严重的样本，最终构建高质量的“渲染-写实”视频配对数据集。
*   **阶段二：模型训练（蒸馏与泛化）**
    *   **核心架构**：在预训练视频扩散模型（Wan2.2 T2V-A14B）基础上训练 **IC-LoRA**（In-Context LoRA）适配器。
    *   **训练机制**：将渲染的视频作为“参考 Token”与带噪声的 Token 连接，训练模型学习从渲染图到写实图的映射。
    *   **优势**：训练后的模型不再依赖首尾锚帧，能够处理序列中途出现的物体，且推理速度更快。

### 3. 实验设计
*   **数据集/场景**：
    *   主要使用 **SAIL-VOS**（基于 GTA-V 引擎的合成数据集），包含复杂的人机交互、光照变化和高速运动。
    *   泛化测试使用 **CARLA-LOC**（自动驾驶模拟器数据集）。
*   **Benchmark 与对比方法**：
    *   对比了三个强力基准：**Runway-Aleph**、**LucyEdit** 和 **Editto**（专门针对 Sim-to-Real 训练的模型）。
*   **评估指标**：
    *   **写实感**：利用 GPT-4o 进行视觉评分（GPT-RS）。
    *   **身份/结构保持**：ArcFace 相似度、DINO 特征距离。
    *   **时间一致性**：VBench 的闪烁度（Flickering）和运动平滑度（Smoothness）。
    *   **人工评价**：45 名参与者进行的 675 组成对偏好测试。

### 4. 资源与算力
*   **硬件使用**：使用了 **1 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   训练步数：1,200 步。
    *   批大小（Batch Size）：8。
    *   训练时长：文中提到仅需“几个小时”（a few hours）。
    *   数据规模：经过过滤后保留了 1,216 个高质量视频剪辑。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 SAIL-VOS 验证集上抽取了 100 个复杂序列进行定量对比。
    *   进行了详尽的**消融实验**，对比了边缘引导 vs 深度引导、多锚点 vs 双锚点传播的效果。
    *   进行了**跨数据集泛化实验**（从 GTA-V 到 CARLA）。
*   **充分性评价**：实验设计较为充分且客观。通过引入 GPT-4o 评分和人工盲测，弥补了传统指标（如 PSNR/SSIM）在生成任务中的局限性。消融实验清晰地证明了“边缘图”在保持人脸特征方面优于“深度图”。

### 6. 主要结论与发现
*   **性能领先**：RealMaster 在写实感评分（GPT-RS）和结构保持（DINO/ArcFace）上均显著优于 Runway 和 Editto 等基准。
*   **边缘图的重要性**：在 Sim-to-Real 任务中，边缘信息比深度信息更能有效保留角色的面部表情和细微身份特征。
*   **泛化能力**：尽管只在 GTA 数据上训练，模型能直接应用于自动驾驶模拟器（CARLA），并能通过提示词（Prompt）添加原渲染引擎不支持的动态天气效果（如雨雪、积水反射）。

### 7. 优点
*   **创新的数据生成方案**：通过“首尾锚点+视频传播”巧妙解决了缺乏真实配对视频数据的难题。
*   **平衡性极佳**：成功解决了视频编辑中常见的“改得太少（不真实）”或“改得太多（丢失原意）”的矛盾。
*   **实用性强**：IC-LoRA 训练轻量化，且支持在推理时通过文本控制（如改变天气），扩展了 3D 引擎的功能。

### 8. 不足与局限
*   **上限受限**：最终的写实效果受限于第一阶段使用的图像编辑模型（Qwen-Image-Edit），如果关键帧不够真实，最终视频也会有瑕疵。
*   **小物体处理**：对于远处的微小物体，模型倾向于保守处理，写实化提升不明显。
*   **高速运动伪影**：在相机极速移动或角色剧烈运动时，仍会出现时间上的伪影（这是底层视频扩散模型的固有局限）。
*   **动作真实性**：模型只改变外观，不修正原始 3D 引擎中可能存在的僵硬动作或不合理的物理规律。

（完）
