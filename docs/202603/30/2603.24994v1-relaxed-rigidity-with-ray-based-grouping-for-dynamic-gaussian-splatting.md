---
title: Relaxed Rigidity with Ray-based Grouping for Dynamic Gaussian Splatting
title_zh: 基于射线分组的动态高斯泼溅松弛刚性方法
authors: "Junoh Leea, Junmyeong Lee, Yeon-Ji Song, Inhwan Bae, Jisu Shin, Hae-Gon Jeon, Jin-Hwa Kim"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24994v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 使用高斯泼溅重建动态3D场景
tldr: 本研究针对动态3D高斯泼溅在单目视频中难以保持物理一致性运动的问题，提出了一种基于射线的松弛刚性约束方法。通过在视图空间对同一射线上的高斯点进行分组，并施加局部几何保持约束，该方法无需光流等外部先验即可实现更真实的运动建模。实验证明，该方法显著提升了动态场景重建的几何稳定性和时间一致性。
selection_source: fresh_fetch
motivation: 现有动态3DGS方法在单目视频中难以维持高斯点的局部几何结构，导致运动不真实且过度依赖外部先验。
method: 提出一种基于射线的视图空间分组策略，对同一射线上的高斯点施加空间分布约束，以显式保持其在时间维度上的局部几何稳定性。
result: 在挑战性单目数据集上的实验表明，该方法在重建质量和时间一致性方面均显著优于现有最先进技术。
conclusion: 通过引入基于射线的局部几何约束，该研究为实现无需外部引导的物理一致性动态场景重建提供了有效途径。
---

## 摘要
使用 3D 高斯泼溅（3D Gaussian Splatting）进行动态 3D 场景重建已展现出巨大的潜力。然而，建模真实的运动仍然是一个关键挑战，因为大多数方法无法使高斯的运动与现实世界的物理动力学保持一致。这种不一致在单目视频数据集中尤为突出，无法维持连贯的运动会破坏局部几何结构，最终导致重建质量下降。因此，许多最先进的方法严重依赖外部先验（如光流或 2D 轨迹）来强制执行时间连贯性。在这项工作中，我们提出了一种新颖的方法，旨在显式地保留 4D 场景中高斯随时间变化的局部几何结构。我们的核心思想是引入一种视图空间射线分组策略，将同一条射线穿过的高斯进行聚类，且仅考虑那些 α 混合权重超过阈值的高斯。然后，我们对这些分组施加约束以维持一致的空间分布，从而有效地保留其局部几何结构。该方法通过确保局部几何随时间保持稳定，强制执行更符合物理规律的运动模型，从而消除了对外部引导的依赖。我们通过将该方法集成到两个不同的基准模型中证明了其有效性。在具有挑战性的单目数据集上进行的广泛实验表明，我们的方法显著优于现有方法，实现了卓越的时间连贯性和重建质量。

## Abstract
The reconstruction of dynamic 3D scenes using 3D Gaussian Splatting has shown significant promise. A key challenge, however, remains in modeling realistic motion, as most methods fail to align the motion of Gaussians with real-world physical dynamics. This misalignment is particularly problematic for monocular video datasets, where failing to maintain coherent motion undermines local geometric structure, ultimately leading to degraded reconstruction quality. Consequently, many state-of-the-art approaches rely heavily on external priors, such as optical flow or 2D tracks, to enforce temporal coherence. In this work, we propose a novel method to explicitly preserve the local geometric structure of Gaussians across time in 4D scenes. Our core idea is to introduce a view-space ray grouping strategy that clusters Gaussians intersected by the same ray, considering only those whose $α$-blending weights exceed a threshold. We then apply constraints to these groups to maintain a consistent spatial distribution, effectively preserving their local geometry. This approach enforces a more physically plausible motion model by ensuring that local geometry remains stable over time, eliminating the reliance on external guidance. We demonstrate the efficacy of our method by integrating it into two distinct baseline models. Extensive experiments on challenging monocular datasets show that our approach significantly outperforms existing methods, achieving superior temporal consistency and reconstruction quality.

---

## 论文详细总结（自动生成）

以下是对论文《Relaxed Rigidity with Ray-based Grouping for Dynamic Gaussian Splatting》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：动态 3D 高斯泼溅（3DGS）在重建动态场景时，难以保持高斯点运动与物理真实动力学的一致性。
*   **研究背景**：
    *   在单目视频中，由于缺乏多视图约束，高斯点的运动往往是欠约束或过约束的，导致局部几何结构随时间坍塌或产生“漂浮物”（floaters）。
    *   **现有方案局限**：
        1.  **依赖外部先验**：如光流或深度估计，但这些 2D 代理信号往往不准确且与 3D 几何不一致。
        2.  **严格刚性假设**：如基于 K 近邻（KNN）的分组约束，忽略了高斯点的缩放和不透明度属性，且无法处理非刚性变形。

### 2. 方法论
该论文提出了一种无需外部先验的物理一致性约束框架，核心包含三个部分：
*   **基于射线的射线分组（Ray-based Grouping, RG）**：
    *   **核心思想**：利用 3DGS 渲染管线中天然的可见性排序，将同一条相机射线穿过且对像素颜色贡献显著（$\alpha$-blending 权重 $w_i$ 大于阈值 $\tau$）的高斯点划分为一组。
    *   **优势**：相比 KNN，这种方法能自动适应物体的缩放和遮挡关系，将空间上邻近且视觉相关的点聚集在一起。
*   **松弛刚性约束（Relaxed Rigidity）**：
    *   **运动连贯性正则化（MCR）**：通过余弦相似度惩罚组内高斯点运动方向的不一致，但允许运动幅值存在差异，从而支持非刚性变形。
*   **频谱正则化（Spectral Regularization, SR）**：
    *   **核心思想**：计算组内高斯点位置的协方差矩阵，并约束其特征值（Eigenvalues）在时间步之间保持稳定。
    *   **作用**：这能保持点群的局部空间分布形状（体积和比例），同时允许整体的旋转和平移。
*   **高效计算（Welford 算法）**：
    *   引入 Welford 迭代算法，在单次渲染过程中递归计算协方差矩阵，显著降低了内存开销和计算复杂度。

### 3. 实验设计
*   **数据集**：
    *   **D-NeRF**：合成动态场景（8个场景），360度单目视角。
    *   **HyperNeRF**：真实世界场景，包含环境噪声和拓扑变化。
    *   **NeRF-DS**：包含高反光（specular）物体的真实动态场景。
*   **基准模型（Baselines）**：将该方法集成到四种主流 4DGS 框架中：**RTD**、**Ex4DGS**、**MoDec-GS** 和 **Grid4D**。
*   **对比指标**：PSNR、SSIM、MS-SSIM、LPIPS。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 3090 GPU**。
*   **训练时长**：由于引入了 SVD（奇异值分解）和协方差计算，训练时间比原始基准模型增加约 **2 到 3 倍**。
*   **渲染开销**：该方法仅在训练阶段施加约束，**不改变推理模型结构**，因此渲染速度（FPS）与基准模型持平。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集、共计数十个场景上进行了验证。
*   **消融实验**：对比了 KNN 分组与射线分组（RG）的效果，以及 MCR、SR 和传统 ARAP（尽可能刚性）约束的差异。
*   **评价**：实验设计全面，涵盖了合成到真实的多种复杂情况，通过可视化高斯运动轨迹证明了物理一致性的提升，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：在所有基准模型上均实现了性能增长。在 D-NeRF 数据集上，平均 PSNR 提升了 **1.19 dB**。
*   **几何稳定性**：射线分组策略能有效过滤背景干扰，使高斯点更牢固地“附着”在物体表面，减少了运动过程中的形状畸变。
*   **通用性**：证明了该约束项是模型无关的（Model-agnostic），可以作为通用插件提升现有动态 3DGS 方法的质量。

### 7. 优点
*   **无需先验**：摆脱了对光流、深度图等第三方模型的依赖，降低了数据预处理成本。
*   **物理合理性**：通过“松弛刚性”平衡了刚性约束与变形灵活性，更符合真实物理世界的非刚性运动。
*   **集成简便**：直接利用渲染管线的中间产物进行分组，算法逻辑清晰，易于集成。

### 8. 不足与局限
*   **训练成本**：训练时间的显著增加（2-3倍）可能会限制其在大规模场景或快速迭代需求下的应用。
*   **超参数敏感性**：权重阈值 $\tau$ 和正则化系数 $\lambda$ 可能需要针对不同场景进行微调以达到最优效果。
*   **极端运动处理**：对于极高速或拓扑结构剧烈变化的场景，仅靠局部几何保持可能仍不足以完全消除伪影。

（完）
