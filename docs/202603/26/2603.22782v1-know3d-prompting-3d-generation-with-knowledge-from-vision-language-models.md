---
title: "Know3D: Prompting 3D Generation with Knowledge from Vision-Language Models"
title_zh: Know3D：利用视觉语言模型知识引导 3D 生成
authors: "Wenyue Chen, Wenjue Chen, Peng Li, Qinghe Wang, Xu Jia, Heliang Zheng, Rongfei Jia, Yuan Liu, Ronggang Wang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22782v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用视觉语言模型知识的语言可控3D生成
tldr: 针对现有3D生成模型在处理未观测区域时存在的随机性和不可控问题，本文提出Know3D框架。该方法通过潜空间隐藏状态注入技术，将多模态大语言模型（VLM）的丰富语义知识引入3D生成过程。利用扩散模型作为桥梁，将VLM的语义理解转化为几何指导，实现了语言可控的3D资产背面生成。这一创新有效弥补了抽象文本指令与几何重建之间的鸿沟，显著提升了生成结果的语义一致性。
selection_source: fresh_fetch
motivation: 现有3D生成模型在处理未观测区域时缺乏稳健的全局结构先验，导致生成的几何形状往往具有随机性且难以符合用户意图。
method: 提出Know3D框架，通过潜空间隐藏状态注入将VLM的语义知识引入扩散模型，从而指导3D生成过程中的背面幻觉。
result: 成功实现了对3D资产背面生成的语义可控性，使生成的未观测区域能够精准对齐用户的文本指令。
conclusion: 该研究证明了利用多模态大模型的知识可以有效增强3D生成的几何重建能力，为未来可控3D生成提供了重要参考。
---

## 摘要
3D 生成的最新进展提高了合成 3D 资产的保真度和几何细节。然而，由于单视图观测固有的歧义性，以及有限的 3D 训练数据导致缺乏鲁棒的全局结构先验，现有模型生成的未观测区域往往具有随机性且难以控制，有时可能无法符合用户意图或产生不合理的几何结构。在本文中，我们提出了 Know3D，这是一个通过潜隐状态注入将多模态大语言模型的丰富知识融入 3D 生成过程的新型框架，实现了 3D 资产背视图的语言可控生成。我们利用了一个基于 VLM-diffusion 的模型，其中 VLM 负责语义理解和引导。扩散模型充当桥梁，将语义知识从 VLM 传递到 3D 生成模型。通过这种方式，我们成功弥合了抽象文本指令与未观测区域几何重建之间的鸿沟，将传统的随机背视图幻觉转化为语义可控的过程，为未来的 3D 生成模型展示了一个极具前景的方向。

## Abstract
Recent advances in 3D generation have improved the fidelity and geometric details of synthesized 3D assets. However, due to the inherent ambiguity of single-view observations and the lack of robust global structural priors caused by limited 3D training data, the unseen regions generated by existing models are often stochastic and difficult to control, which may sometimes fail to align with user intentions or produce implausible geometries. In this paper, we propose Know3D, a novel framework that incorporates rich knowledge from multimodal large language models into 3D generative processes via latent hidden-state injection, enabling language-controllable generation of the back-view for 3D assets. We utilize a VLM-diffusion-based model, where the VLM is responsible for semantic understanding and guidance. The diffusion model acts as a bridge that transfers semantic knowledge from the VLM to the 3D generation model. In this way, we successfully bridge the gap between abstract textual instructions and the geometric reconstruction of unobserved regions, transforming the traditionally stochastic back-view hallucination into a semantically controllable process, demonstrating a promising direction for future 3D generation models.

---

## 论文详细总结（自动生成）

以下是对论文《Know3D: Prompting 3D Generation with Knowledge from Vision-Language Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：从单张图像生成 3D 模型是一个“病态问题”，因为输入仅包含可见部分的信息。现有模型在生成不可见区域（如背面）时，主要依赖模型内部学习到的先验，导致生成结果具有**随机性**和**不可控性**。
*   **研究背景**：
    *   **数据稀缺**：相比于海量的 2D 图像和文本，高质量的 3D 数据集规模有限，导致 3D 生成模型缺乏足够的全局结构常识。
    *   **语义失真**：现有模型无法根据用户的具体意图（如“给杯子背面加个手柄”）来定制不可见区域，且容易产生物理上不合理的几何结构。
*   **整体含义**：本文旨在通过引入多模态大语言模型（VLM）的丰富语义知识，将 3D 生成中原本随机的“背面幻觉”转化为一个**语义可控**的过程。

### 2. 方法论
*   **核心思想**：利用多模态扩散模型作为“桥梁”，将 VLM 的抽象语义理解转化为图像空间的结构先验，并将其注入到 3D 生成网络中。
*   **关键技术细节**：
    1.  **语义感知的前后视图生成**：基于 `Qwen-Image-Edit` 进行微调，增强其空间意识（理解“背面”概念）和姿态稳定性。通过随机提示策略（Stochastic Prompting）训练模型根据文本指令生成合理的背视图图像。
    2.  **知识提取与注入**：研究发现多模态扩散转换器（MMDiT）的中间层隐藏状态（Hidden States）包含丰富的空间和语义信息。
    3.  **特征注入机制**：在 3D 生成模型（基于 `TRELLIS.2`）中设计了并行的交叉注意力（Cross-Attention）分支，将提取的 MMDiT 特征注入到 3D 潜空间。
*   **算法流程**：
    *   输入：单张图像 $I$ + 背面描述文本 $T$。
    *   步骤一：VLM 编码输入，扩散模型在特定时间步 $t$ 提取隐藏状态 $H_{DiT}$。
    *   步骤二：将 $H_{DiT}$ 作为条件信号，通过两阶段（粗糙结构生成、精细几何恢复）生成最终的 3D 资产。
    *   **目标函数**：采用条件流匹配（Conditional Flow Matching, CFM）目标函数进行优化。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 `TexVerse` 数据集中的 5k 个高质量 3D 资产进行视图生成微调，60k 个资产进行 3D 生成训练。
    *   **测试集**：在 `HY3D-Bench` 数据集上进行定量评估。
*   **Benchmark 与指标**：
    *   **语义一致性**：使用 ULIP 和 Uni3D 指标。
    *   **几何质量**：使用 PSNR、SSIM、LPIPS 和 Chamfer Distance (CD)。
*   **对比方法**：
    *   **单视图 3D 生成**：Hunyuan3D-2.1, TRELLIS, TRELLIS.2, Step1X-3D, Hi3DGen, Direct3D-S2。
    *   **多视图基准**：将生成的背视图输入到 Hunyuan3D-2mv 中作为对比。

### 4. 资源与算力
*   **GPU 型号与数量**：所有实验均在 **32 台 NVIDIA A800 GPU** 上完成。
*   **训练时长/规模**：
    *   **背视图生成微调**：20k 次迭代，全局 Batch Size 为 32。
    *   **3D 生成训练**：70k 次迭代，全局 Batch Size 为 64。
*   **技术手段**：使用了 LoRA（秩为 64）进行高效微调，以保留预训练模型的泛化能力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的 SOTA 对比实验（涵盖 7 种主流模型）。
    *   **消融实验**：针对 MMDiT 的提取时间步 $t$（测试了 0, 0.25, 0.5, 0.75）以及不同的特征表示（VAE 潜变量、DINOv3 特征、MMDiT 隐藏状态）进行了详细对比。
*   **充分性与公平性**：实验涵盖了定量指标和定性视觉对比，并专门针对“语义可控性”展示了多组不同文本指令下的生成结果。对比实验在相同的 Benchmark 下进行，评估过程较为客观。

### 6. 主要结论与发现
*   **语义可控性**：Know3D 成功实现了通过文本指令改变 3D 资产背面结构的功能，这是此前单视图模型难以做到的。
*   **特征优越性**：实验证明 MMDiT 的中间层隐藏状态（尤其是 $t=0.25$ 时）在引导 3D 生成方面优于传统的 VAE 特征或 DINO 特征，因为它兼具空间感知和语义鲁棒性。
*   **性能提升**：在 HY3D-Bench 上，Know3D 在语义一致性和几何精度指标上均达到了竞争性或领先的水平。

### 7. 优点（亮点）
*   **创新架构**：巧妙地将 VLM 作为“大脑”，扩散模型作为“桥梁”，解决了 3D 数据不足导致的结构先验缺失问题。
*   **中间态利用**：不直接使用生成的像素图像，而是利用扩散模型的中间隐藏状态，增强了对生成过程中可能出现的伪影或错误的容错性。
*   **交互性强**：为用户提供了对 3D 模型盲区进行创造性干预的手段。

### 8. 不足与局限
*   **依赖性风险**：3D 生成的质量高度依赖于底层 VLM 的理解能力。如果 VLM 误解了指令（如将“单肩包”理解为“双肩包”），生成的 3D 形状也会随之出错。
*   **计算开销**：推理过程中需要运行大型 VLM 和扩散模型提取特征，相比纯 3D 原生模型，推理延迟可能更高。
*   **应用限制**：目前主要集中在背面生成的可控性，对于更复杂的遮挡情况（如物体内部结构）尚未深入探讨。

（完）
