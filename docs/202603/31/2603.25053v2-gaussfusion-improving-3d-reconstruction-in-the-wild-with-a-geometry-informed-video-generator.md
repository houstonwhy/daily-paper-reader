---
title: "GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator"
title_zh: GaussFusion：利用几何感知视频生成器改进野外环境下的 3D 重建
authors: "Liyuan Zhu, Manjunath Narayana, Michal Stary, Will Hutchcroft, Gordon Wetzstein, Iro Armeni"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25053v2"
tags: ["query:3dgen"]
score: 8.0
evidence: 利用视频生成改进3D重建
tldr: GaussFusion 针对 3D 高斯泼溅（3DGS）在野外场景中存在的伪影、闪烁和模糊等问题，提出了一种结合几何信息的视频生成方法。该方法通过渲染包含深度、法线等信息的几何缓冲视频，并利用视频到视频生成器进行细化，显著提升了重建质量。其贡献包括引入几何感知生成器、伪影合成流水线以及实现 15 FPS 的实时变体，在多项基准测试中达到领先水平。
selection_source: fresh_fetch
motivation: 现有的 3DGS 重建在处理相机位姿误差、覆盖不全和噪声初始化时，容易产生漂浮物、闪烁和模糊等伪影。
method: 提出一种几何感知的视频生成器，利用包含深度、法线、不透明度和协方差的视频缓冲来细化 3DGS 渲染结果，并结合伪影合成流水线增强鲁棒性。
result: 在多项新视角合成基准测试中取得了最先进的性能，且其实时变体能以 15 FPS 的速度运行，支持交互式应用。
conclusion: GaussFusion 通过将几何信息整合进视频生成过程，有效解决了 3DGS 的重建缺陷，为高质量、实时的 3D 重建提供了新方案。
---

## 摘要
我们提出了 GaussFusion，这是一种通过几何感知视频生成来改进野外环境下 3D 高斯泼溅（3DGS）重建的新方法。GaussFusion 缓解了常见的 3DGS 伪影，包括由相机位姿误差、覆盖不完整和噪声几何初始化引起的漂浮物、闪烁和模糊。与以往局限于单一重建流水线的基于 RGB 的方法不同，我们的方法引入了一个几何感知的视频到视频生成器，可以优化基于优化和前馈方法的 3DGS 渲染。给定现有的重建结果，我们渲染一个编码了深度、法线、不透明度和协方差的高斯原语视频缓冲区，生成器对其进行细化，以产生时间相干且无伪影的帧。我们还引入了一个伪影合成流水线，用于模拟多种退化模式，从而确保鲁棒性和泛化能力。GaussFusion 在新视角合成基准测试中达到了最先进的性能，其高效变体在保持相似性能的同时能以 15 FPS 的速度实时运行，从而支持交互式 3D 应用。

## Abstract
We present GaussFusion, a novel approach for improving 3D Gaussian splatting (3DGS) reconstructions in the wild through geometry-informed video generation. GaussFusion mitigates common 3DGS artifacts, including floaters, flickering, and blur caused by camera pose errors, incomplete coverage, and noisy geometry initialization. Unlike prior RGB-based approaches limited to a single reconstruction pipeline, our method introduces a geometry-informed video-to-video generator that refines 3DGS renderings across both optimization-based and feed-forward methods. Given an existing reconstruction, we render a Gaussian primitive video buffer encoding depth, normals, opacity, and covariance, which the generator refines to produce temporally coherent, artifact-free frames. We further introduce an artifact synthesis pipeline that simulates diverse degradation patterns, ensuring robustness and generalization. GaussFusion achieves state-of-the-art performance on novel-view synthesis benchmarks, and an efficient variant runs in real time at 15 FPS while maintaining similar performance, enabling interactive 3D applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：解决 3D 高斯泼溅（3DGS）在野外场景（In-the-wild）重建中常见的伪影问题，如“漂浮物”（floaters）、视角切换时的闪烁、模糊以及由于相机位姿误差或覆盖不足导致的几何错误。
*   **研究动机**：
    *   现有的生成式增强方法多仅依赖 RGB 信息，在处理严重的几何缺失或大面积伪影时存在多义性。
    *   现有的优化模型通常针对特定的重建流水线（如仅针对优化型或仅针对前馈型），缺乏跨范式的泛化能力。
    *   推理速度慢，难以支持实时交互应用。

### 2. 方法论
*   **核心思想**：将 3DGS 的几何属性提取为像素对齐的视频缓冲区（GP-Buffer），并利用一个几何感知的视频生成器（Video-to-Video）将低质量渲染图转化为高质量、时间相干的图像，最后反馈用于更新 3D 表示。
*   **关键技术细节**：
    *   **GP-Buffer（高斯原语缓冲区）**：除了颜色（C），还渲染并编码了不透明度（A）、深度（D）、表面法线（N）和几何不确定性（U，基于逆投影协方差）。这些多模态信息为生成器提供了明确的几何引导。
    *   **几何感知视频生成器**：基于 Wan-2.1（Flow-based DiT）架构。引入了**几何适配器（Geometry Adapter, GA）**，这是一个并行侧向网络，通过自注意力和交叉注意力机制将 GP-Buffer 特征注入主生成网络的 Transformer 块中。
    *   **伪影合成流水线**：通过随机下采样视频帧（模拟稀疏视角）、多种初始化策略（SfM、随机点云、MapAnything）以及引入前馈模型的典型错误，合成超过 7.5 万对“脏/净”视频数据进行训练。
    *   **两阶段蒸馏策略**：利用分布匹配蒸馏（DMD）将多步扩散模型压缩为 4 步推理模型，实现极速生成。
    *   **3D 更新**：使用细化后的视频帧作为伪标签，通过标准光度损失重新优化 3D 高斯参数。

### 3. 实验设计
*   **数据集**：主要在 **DL3DV-10K**（大规模场景数据集）和 **RE10K**（真实房地产视频数据集）上进行评估。
*   **Benchmark 与对比方法**：
    *   **对比基准**：新视角合成（NVS）质量和 3D 重建一致性。
    *   **对比对象**：包括 Splatfacto（基础 3DGS）、GenFusion、Difix3D+、ExploreGS 以及针对前馈模型的 MVSplat360。
*   **评估指标**：PSNR、SSIM、LPIPS（感知相似度）和 FID（图像质量）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   训练步数：100,000 步。
    *   有效 Batch Size：32（通过梯度累积实现）。
    *   技术栈：PyTorch 2.7.1, CUDA 12.6, FlashAttention-2, ZeRO-2 显存优化。
    *   分辨率：训练视频分辨率为 480×832，包含 81 帧。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量评估**：在两个大型数据集的测试集上进行了详尽对比。
    *   **消融实验**：针对输入模态（RGB vs. GP-Buffer 各组件）、伪影模拟策略、架构设计（GA vs. 直接融合）进行了多组实验。
    *   **跨范式验证**：验证了模型在优化型（Splatfacto）和前馈型（DepthSplat, MVSplat）重建上的通用性。
*   **充分性评价**：实验设计非常充分且客观。通过引入多种前馈模型的输出作为测试对象，证明了模型并非过拟合于某种特定算法，具有很强的鲁棒性。

### 6. 主要结论与发现
*   **几何信息至关重要**：加入深度、法线和协方差等几何线索后，模型在处理“漂浮物”和几何空洞方面的能力显著优于纯 RGB 模型。
*   **跨范式泛化**：通过混合伪影训练，单一模型可以同时修复优化型和前馈型重建的缺陷。
*   **效率突破**：蒸馏后的变体在保持高性能的同时，达到了 **15-16 FPS** 的推理速度，比同类视频增强方法快一个数量级。
*   **3D 一致性**：细化后的视频不仅单帧好看，且在重新优化 3DGS 后能显著提升整体 3D 结构的准确性。

### 7. 优点（亮点）
*   **多模态融合**：创新性地将 3DGS 的内部几何属性（如逆协方差）转化为图像空间的不确定性图，为扩散模型提供了极佳的先验。
*   **实时性**：通过 DMD 蒸馏实现了真正可用的实时增强，这在视频扩散领域非常难得。
*   **通用性**：不绑定于特定的重建算法，可作为任何 3DGS 流水线的后处理插件。

### 8. 不足与局限
*   **动态模糊限制**：在处理极快运动或严重运动模糊的视频时，由于 VAE 编码器的限制，生成质量会有所下降。
*   **长视频一致性**：目前采用 81 帧滑动窗口处理长视频，虽然有双向衔接策略，但仍无法完全保证超长序列（数千帧）的全局绝对一致性。
*   **计算开销**：尽管推理快，但训练过程仍需要昂贵的 H200 集群支持。

（完）
