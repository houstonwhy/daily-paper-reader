---
title: "KitchenTwin: Semantically and Geometrically Grounded 3D Kitchen Digital Twins"
title_zh: KitchenTwin：语义与几何对齐的 3D 厨房数字孪生
authors: "Quanyun Wu, Kyle Gao, Daniel Long, David A. Clausi, Jonathan Li, Yuhao Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24684v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 3D场景合成与数字孪生
tldr: KitchenTwin 针对具身智能训练中数字孪生环境的尺度模糊和坐标不一致问题，提出了一种语义与几何对齐的 3D 厨房数字孪生构建框架。该方法利用视觉语言模型（VLM）引导的几何锚点机制恢复真实物理尺度，并通过几何感知配准流程融合全局点云与局部物体网格。实验证明该方法在几何一致性和物体对齐方面表现优异，并提供了开源的度量级数字孪生数据集。
selection_source: fresh_fetch
motivation: 现有的基于 Transformer 的单目视频重建方法存在固有的尺度模糊和坐标不一致问题，难以构建精确的具身智能数字孪生环境。
method: 提出一种 VLM 引导的几何锚点机制来恢复物理尺度，并结合重力对齐、曼哈顿世界约束和无碰撞细化进行几何感知配准。
result: 在真实厨房环境中的实验表明，该方法显著提升了跨网络物体对齐精度、几何一致性以及多原语拟合等下游任务的性能。
conclusion: KitchenTwin 为构建具有精确度量尺度和语义信息的 3D 数字孪生提供了有效方案，并贡献了高质量的开源数据集。
---

## 摘要
具身智能（Embodied AI）的训练与评估需要具有精确度量几何和语义对齐的以物体为中心的数字孪生环境。近期基于 Transformer 的前馈重建方法可以从稀疏单目视频中高效预测全局点云，但这些几何结构存在固有的尺度二义性和坐标约定不一致的问题。这种不匹配阻碍了这些无量纲点云预测与局部重建的物体网格之间的可靠融合。我们提出了一种新型的尺度感知 3D 融合框架，通过将视觉对齐的物体网格与 Transformer 预测的全局点云进行配准，来构建度量一致的数字孪生。我们的方法引入了一种由视觉语言模型（VLM）引导的几何锚点机制，通过恢复准确的真实世界度量尺度来解决这一根本性的坐标不匹配问题。为了融合这些网络，我们提出了一种几何感知配准流水线，通过重力对齐的垂直估计、曼哈顿世界（Manhattan-world）结构约束以及无碰撞局部细化，显式地增强了物理合理性。在真实室内厨房环境中的实验表明，该方法在下游任务（包括多原语拟合和度量测量）中提升了跨网络物体对齐和几何一致性。此外，我们还引入了一个开源的室内数字孪生数据集，其中包含具有度量尺度的场景以及语义对齐且配准的以物体为中心的网格标注。

## Abstract
Embodied AI training and evaluation require object-centric digital twin environments with accurate metric geometry and semantic grounding. Recent transformer-based feedforward reconstruction methods can efficiently predict global point clouds from sparse monocular videos, yet these geometries suffer from inherent scale ambiguity and inconsistent coordinate conventions. This mismatch prevents the reliable fusion of these dimensionless point cloud predictions with locally reconstructed object meshes. We propose a novel scale-aware 3D fusion framework that registers visually grounded object meshes with transformer-predicted global point clouds to construct metrically consistent digital twins. Our method introduces a Vision-Language Model (VLM)-guided geometric anchor mechanism that resolves this fundamental coordinate mismatch by recovering an accurate real-world metric scale. To fuse these networks, we propose a geometry-aware registration pipeline that explicitly enforces physical plausibility through gravity-aligned vertical estimation, Manhattan-world structural constraints, and collision-free local refinement. Experiments on real indoor kitchen environments demonstrate improved cross-network object alignment and geometric consistency for downstream tasks, including multi-primitive fitting and metric measurement. We additionally introduce an open-source indoor digital twin dataset with metrically scaled scenes and semantically grounded and registered object-centric mesh annotations.

---

## 论文详细总结（自动生成）

以下是对论文《KitchenTwin: Semantically and Geometrically Grounded 3D Kitchen Digital Twins》的结构化总结：

### 1. 核心问题与研究动机
具身智能（Embodied AI）和机器人仿真需要高度精确的 3D 数字孪生环境。然而，现有的 3D 重建技术存在两个核心痛点：
*   **尺度模糊性：** 基于 Transformer 的前馈重建模型（如 Pi-Long）生成的全局点云是无量纲的（Dimensionless），缺乏真实物理尺度。
*   **坐标与物理不一致：** 现有的 3D 生成模型（如 SAM3D）生成的物体网格处于孤立的局部坐标系中，直接融合会导致物体漂浮、嵌入墙体或比例失调，无法满足机器人抓取等任务对厘米级精度的要求。

### 2. 方法论
论文提出了一个包含三个并行流的尺度感知融合框架：
*   **流 A：全局场景重建与尺度恢复：** 使用 Pi-Long 生成全局点云。引入 **VLM 引导的几何锚点机制**，利用视觉语言模型（如 Gemini）结合网页搜索获取场景中特定锚点物体（如标准柜子）的真实物理尺寸，计算缩放因子 $s$，将无量纲点云转换为度量空间 $P_{scaled}$。
*   **流 B：物体接地与网格生成：** 利用 Grounded-SAM-2 进行跨帧物体跟踪。提出一种**启发式评分函数**，根据遮挡程度和图像边缘截断情况选择最佳视角，再通过 SAM3D 将 2D 掩码提升为高质量的独立 3D 物体网格 $M_i$。
*   **流 C：几何感知配准（核心）：**
    *   **粗对齐：** 通过 RANSAC 拟合地面以确定重力方向，并评估多个偏航角（Yaw）假设，选择投影重叠度最高的初始位姿。
    *   **精细配准（TrICP）：** 使用修剪迭代最近点算法（Trimmed ICP）处理局部网格与全局点云之间的部分重叠问题。
    *   **结构约束与碰撞处理：** 引入**曼哈顿世界假设**，将物体旋转对齐到正交轴；通过 2D 边界框检测处理物体间的穿透冲突（如将嵌入式烤箱推平至柜体表面）。

### 3. 实验设计
*   **数据集：** 引入了自建的 **KitchenTwin 数据集**，包含真实的北美厨房环境、多视角视频、精确的语义标注以及通过物理尺子测量的真实地面标尺（Ground-truth scale）。
*   **基准方法（Benchmark）：** 对比了原始的 SAM3D 方案（即直接聚合生成的物体网格，不进行尺度恢复和几何对齐）。
*   **评价指标：** 提出了 **NVSS-IoU**（新视角语义合成交并比），将重建的 3D 场景重新投影回 2D 平面，计算其与真实掩码的重叠度，以此严格衡量 6-DoF 位姿、尺度和形状的准确性。

### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架主要基于前馈（Feedforward）模型（如 Pi-Long 和 SAM3D）进行推理和几何优化，而非从头训练大型模型，其计算开销主要集中在推理和 TrICP 迭代优化上。

### 5. 实验数量与充分性
*   **实验规模：** 针对厨房场景中的 12 类典型物体（瓶子、冰箱、咖啡机、橱柜、洗碗机、凳子、烤箱等）进行了详细测试。
*   **消融实验：** 进行了三组关键消融实验，分别验证了“物理锚点机制”、“TrICP 算法”和“几何接地约束（碰撞处理等）”的必要性。
*   **充分性评价：** 实验设计较为客观，通过 NVSS-IoU 指标量化了尺度模糊带来的灾难性后果（基准方法 mIoU 仅为 0.0246，而本文方法为 0.5715）。虽然场景类别集中在厨房，但对于验证度量精度这一核心目标是充分的。

### 6. 主要结论与发现
*   **VLM 是恢复尺度的有效工具：** 通过 VLM 检索常识性的物体尺寸，可以成功解决单目重建中固有的尺度二义性。
*   **几何约束至关重要：** 简单的网格堆叠无法构建可用的数字孪生，必须引入重力对齐、曼哈顿约束和碰撞检测才能确保物理合理性。
*   **跨网络融合可行：** 证明了可以将高效的全局点云预测网络与高质量的局部物体生成网络在统一的度量空间内完美融合。

### 7. 优点
*   **实用性强：** 直接解决了具身智能中“看得见但摸不到（尺度不对）”的问题。
*   **创新性配准：** 结合了 VLM 常识与传统几何优化（TrICP），在处理遮挡和部分重叠方面表现稳健。
*   **开源贡献：** 提供了带有精确物理度量和物体级网格标注的厨房数据集，填补了现有数据集在度量验证方面的空白。

### 8. 不足与局限
*   **环境假设：** 严重依赖曼哈顿世界假设（物体多为正交排列），对于不规则形状或非结构化环境（如杂乱的户外场景）的泛化能力有限。
*   **VLM 依赖：** 尺度恢复的精度取决于 VLM 对物体尺寸估计的准确性，若场景中缺乏标准尺寸的已知物体，系统性能可能下降。
*   **实时性：** 虽然使用了前馈网络，但多步配准和碰撞处理流程可能难以达到极高帧率的实时交互要求。

（完）
