---
title: "SonoWorld: From One Image to a 3D Audio-Visual Scene"
title_zh: SonoWorld：从单张图像到 3D 视听场景
authors: "Derong Jin, Xiyi Chen, Ming C. Lin, Ruohan Gao"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28757v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从单张图像生成3D视听场景
tldr: SonoWorld是首个实现从单张图像生成可导航3D视听场景的框架。它通过将单图外扩为360°全景并构建3D几何，结合语言引导放置声音锚点，生成与场景语义和几何对齐的空间音频。该研究解决了视觉场景生成中缺乏沉浸式声音的问题，支持自由视角的视听渲染及声源分离等多种应用。
selection_source: fresh_fetch
motivation: 现有的3D视觉场景生成技术缺乏与之匹配的沉浸式声音，无法仅凭单张图像构建完整的视听体验。
method: 该框架通过全景外扩、3D场景重建、语言引导的声音锚点放置以及渲染与几何语义对齐的空间音频来实现。
result: 实验和用户研究表明，该方法能有效生成空间对齐的视听场景，并在单样本声学学习和声源分离任务中表现优异。
conclusion: SonoWorld填补了单图生成3D视听场景的空白，为沉浸式多模态内容创作提供了高效的解决方案。
---

## 摘要
视觉场景生成领域的巨大进步现已能将单张图像转化为可探索的 3D 世界，然而，若没有声音，沉浸感仍是不完整的。我们提出了 Image2AVScene 任务，即从单张图像生成 3D 视听场景，并介绍了 SonoWorld，这是首个应对这一挑战的框架。从单张图像出发，我们的流水线外扩绘制出 360° 全景图，将其提升为可导航的 3D 场景，放置语言引导的声音锚点，并为点源、面源和环境声源渲染高阶立体声（ambisonics），从而产生与场景几何及语义对齐的空间音频。在新构建的真实世界数据集上的定量评估和受控用户研究证实了我们方法的有效性。除了自由视点视听渲染外，我们还展示了在单样本声学学习和视听空间声源分离方面的应用。项目网站：https://humathe.github.io/sonoworld/

## Abstract
Tremendous progress in visual scene generation now turns a single image into an explorable 3D world, yet immersion remains incomplete without sound. We introduce Image2AVScene, the task of generating a 3D audio-visual scene from a single image, and present SonoWorld, the first framework to tackle this challenge. From one image, our pipeline outpaints a 360° panorama, lifts it into a navigable 3D scene, places language-guided sound anchors, and renders ambisonics for point, areal, and ambient sources, yielding spatial audio aligned with scene geometry and semantics. Quantitative evaluations on a newly curated real-world dataset and a controlled user study confirm the effectiveness of our approach. Beyond free-viewpoint audio-visual rendering, we also demonstrate applications to one-shot acoustic learning and audio-visual spatial source separation. Project website: https://humathe.github.io/sonoworld/

---

## 论文详细总结（自动生成）

### SonoWorld: 从单张图像到 3D 视听场景 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
尽管 3D 视觉场景生成（从单张图像生成可探索的 3D 世界）已取得巨大进步，但这些生成的场景通常是“静音”的，缺乏真实世界中不可或缺的多模态沉浸感。
*   **核心问题**：如何从单张 RGB 图像出发，同步生成几何一致、语义对齐且支持自由视角导航的 3D 视觉场景及其对应的空间声场（Spatial Sound Field）。
*   **任务定义**：论文提出了 **IMAGE2AVSCENE** 任务，旨在构建一个既能“看”又能从任意位置“听”的交互式 3D 环境。

#### 2. 方法论：核心思想与技术流程
SonoWorld 是一个**无需训练（Training-free）**的框架，其核心思想是将视觉重建、语义定位与基于物理的空间音频渲染相结合。

*   **视觉场景生成**：
    1.  **相机校准与全景外扩**：利用 GeoCalib 估计输入图的焦距和倾角，通过全景外扩模型（如 WorldGen）生成 360° 等距柱状投影全景图。
    2.  **3D 提升**：使用 3D 高斯泼溅（3DGS）技术（如 Marble 或 HunyuanWorld）将全景图转化为具有稠密几何的 3D 场景。
*   **360° 视听语义接地（Grounding）**：
    1.  **声源提议**：利用大语言模型（GPT-5 或 LLaVA）识别图中潜在的声源类别（如瀑布、鸟鸣、环境风声）及其声学属性。
    2.  **实例分割与细化**：结合开放词汇分割（X-Decoder）和 SAM2，在全景图中精确定位声源区域，并利用深度图将其投影到 3D 空间作为“声音锚点”。
*   **空间音频编码与渲染**：
    1.  **音频合成**：使用文本转音频模型（MMAudio）生成各声源的原始波形。
    2.  **可微分渲染**：根据声源类型（点源、面源、环境音）计算距离衰减和空气吸收，将其编码为高阶立体声（Ambisonics）系数。
    3.  **双耳渲染**：通过头相关传输函数（HRTF）将 Ambisonics 解码为支持 6-DoF 导航的双耳音频。

#### 3. 实验设计
*   **数据集**：
    *   **SONOSCENE 360**：论文贡献了一个新数据集，包含 68 个真实世界的剪辑，涵盖喷泉、厨房、泳池、桥梁等 6 类场景，配有同步的 360° 视频和一阶 Ambisonics 音频记录。
    *   **合成场景**：使用扩散模型生成的图像进行定性评估。
*   **Benchmark 与对比方法**：
    *   对比了 **SEE-2-SOUND**、**ViSAGe**、**OmniAudio** 等空间音频生成方法，以及单声道生成模型 **MMAudio**。
*   **评估指标**：
    *   **空间指标**：到达角（DoA）误差、球面能量图相关系数（CC）、曲线下面积（AUC）。
    *   **语义指标**：基于 CLAP 的音频-文本相似度（D-CLAP T）和音频-音频相似度（D-CLAP A/R）。

#### 4. 资源与算力
*   **训练算力**：由于该框架是 **Training-free**（无需训练）的，因此不涉及大规模模型训练的算力消耗。它直接调用预训练的视觉和音频基础模型。
*   **推理算力**：论文提到在 **Apple M3 Pro 笔记本电脑**上，音频回调延迟小于 1ms，能够满足 48kHz 采样率下的实时交互需求。
*   **模型依赖**：使用了 GPT-5/LLaVA-Next-34B 作为 VLM 引擎，Marble/HunyuanWorld 作为 3D 后端。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 68 个真实剪辑上进行了详尽的定量对比。
    *   开展了涉及 **50 名参与者**的用户研究，涵盖 12 个场景（6 真实 + 6 合成）。
    *   进行了多维度的**消融实验**，验证了面源建模（Areal sources）、音量均衡化（Equalization）以及掩码合并策略的有效性。
*   **充分性评价**：实验设计较为全面，不仅涵盖了客观的物理指标，还通过主观听感测试验证了沉浸感。消融实验证明了各模块对空间准确性的贡献，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：SonoWorld 在所有空间和语义指标上均显著优于现有基线。相比最强基线，DoA 误差降低了约 47%，空间能量图相关性提升了 239% 以上。
*   **多功能性**：该框架不仅能生成场景，还能扩展到**单样本房间声学学习**（One-shot room acoustic learning）和**视听空间声源分离**任务，表现出极强的泛化能力。
*   **实时性**：证明了在消费级硬件上实现高质量 3D 视听交互的可行性。

#### 7. 优点（亮点）
*   **首创性**：首次实现了从单图到完整 3D 视听场景的端到端生成。
*   **异构声源处理**：能够同时处理点源（如鸟）、面源（如河流）和环境音，这比传统的单一对象音频生成更符合真实场景。
*   **可微分设计**：音频渲染流程是全可微分的，这为后续的 3D 视听逆向优化任务奠定了基础。
*   **无需训练**：模块化设计使其能够直接利用现有的视觉/语言/音频大模型，易于部署和升级。

#### 8. 不足与局限
*   **动态场景限制**：目前主要针对静态图像，无法处理视频中复杂的声源移动或动态几何变化（如警车驶过的多普勒效应）。
*   **依赖基础模型**：生成的音频质量和语义准确性高度依赖于底层 T2A 模型和 VLM 的性能，若基础模型产生幻觉，音频也会失真。
*   **遮挡建模简化**：虽然引入了简单的遮挡启发式算法，但对于复杂室内环境中的声波反射、绕射等物理现象的建模仍较为简化。

（完）
