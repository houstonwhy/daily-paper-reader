---
title: "SceneExpander: Expanding 3D Scenes with Free-Form Inserted Views"
title_zh: SceneExpander：利用自由形式插入视图扩展 3D 场景
authors: "Zijian He, enjie Liu, Yihao Wang, Weizhi Zhong, Huan Yuan, Kun Gai, Guangrun Wang, Guanbin Li"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27084v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 通过插入生成模型合成的视图来扩展3D场景
tldr: 本研究针对3D场景构建中的迭代扩展需求，提出了SceneExpander框架。该方法允许用户在真实场景的多视图重建基础上，通过插入由生成模型合成的新视角来扩展场景范围。针对新视角与原场景可能存在的3D几何不对齐和一致性问题，SceneExpander采用测试时自适应策略，利用锚点蒸馏稳定原始几何，并通过插入视角自蒸馏来协调不一致的预测。该方法在保持原始场景质量的同时，实现了高质量的场景扩展。
selection_source: fresh_fetch
motivation: 传统的3D场景编辑难以处理由生成模型引入的、与原场景几何不一致的自由视角扩展需求。
method: 采用测试时自适应技术，结合锚点蒸馏以保留原始场景特征，并利用自蒸馏机制使模型适应新插入视角的几何与外观。
result: 实验表明，该方法在处理视角错位方面表现优异，显著提升了扩展场景的重建精度和视觉一致性。
conclusion: SceneExpander通过创新的蒸馏策略解决了3D场景扩展中的对齐难题，为交互式3D内容创作提供了有力支持。
---

## 摘要
使用 3D 场景表示进行世界构建对于内容创作、模拟和交互体验变得越来越重要，然而实际的工作流程本质上是迭代的：创作者必须在用户控制下反复扩展现有场景。受此研究空白的启发，我们研究了一种以用户为中心的 3D 场景扩展工作流：从多视图图像捕获的真实场景开始，我们通过插入由生成模型合成的额外视图来扩展其覆盖范围。与固定场景中的简单对象编辑或风格迁移不同，插入的视图通常与原始重建在 3D 上不一致，从而引入几何偏移、幻觉内容或破坏全局多视图一致性的视图相关伪影。为了应对这一挑战，我们提出了 SceneExpander，它将测试时自适应（test-time adaptation）应用于参数化前馈 3D 重建模型，并结合了两种互补的蒸馏信号：锚点蒸馏（anchor distillation）通过从捕获的视图中蒸馏几何线索来稳定原始场景，而插入视图自蒸馏（inserted-view self-distillation）则保留受观测支持的预测，同时调整潜在几何和外观以适应不一致的插入视图。在 ETH 场景和在线数据上的实验表明，在存在不一致的情况下，该方法具有更好的扩展行为和重建质量。

## Abstract
World building with 3D scene representations is increasingly important for content creation, simulation, and interactive experiences, yet real workflows are inherently iterative: creators must repeatedly extend an existing scene under user control. Motivated by this research gap, we study 3D scene expansion in a user-centric workflow: starting from a real scene captured by multi-view images, we extend its coverage by inserting an additional view synthesized by a generative model. Unlike simple object editing or style transfer in a fixed scene, the inserted view is often 3D-misaligned with the original reconstruction, introducing geometry shifts, hallucinated content, or view-dependent artifacts that break global multi-view consistency. To address the challenge, we propose SceneExpander, which applies test-time adaptation to a parametric feed-forward 3D reconstruction model with two complementary distillation signals: anchor distillation stabilizes the original scene by distilling geometric cues from the captured views, while inserted-view self-distillation preserves observation-supported predictions yet adapts latent geometry and appearance to accommodate the misaligned inserted view. Experiments on ETH scenes and online data demonstrate improved expansion behavior and reconstruction quality under misalignment.

---

## 论文详细总结（自动生成）

以下是对论文《SceneExpander: Expanding 3D Scenes with Free-Form Inserted Views》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：3D 场景构建（World Building）通常是一个迭代过程，创作者需要不断扩展现有场景。虽然现有的 3D 编辑技术（如风格迁移、对象添加）已较为成熟，但**场景扩展（Scene Expansion）**——即在现有 3D 重建基础上，通过插入一个包含新区域的生成视图来扩大场景范围——仍面临巨大挑战。
*   **核心问题**：由生成模型（如 AIGC 工具）合成的新视图往往与原始 3D 几何在空间上**不完全对齐（3D-misaligned）**。这种几何冲突会导致重建过程中出现重影（Ghosting）、几何不稳定或原始场景质量退化。
*   **整体含义**：论文旨在提出一种稳健的框架，允许用户通过文本引导生成新视角，并将其无缝、一致地集成到现有的 3D 重建中，即使生成图存在几何偏差。

### 2. 方法论：核心思想与技术细节
SceneExpander 基于前馈 3D 重建模型（如 WorldMirror），通过**测试时自适应（Test-Time Adaptation, TTA）**来微调模型参数，其核心技术包括：
*   **核心思想**：不将插入视图视为硬性监督信号，而是将其视为一种“弱约束”，通过蒸馏机制在保留原始场景记忆的同时，逐步诱导模型适应新内容。
*   **锚点蒸馏（Anchor Distillation）**：使用一个冻结的“锚点老师”模型，提取原始捕获视图的相机参数、深度图和法线图作为参考，确保在加入新视图时，原始区域的几何结构保持稳定，防止“灾难性遗忘”或漂移。
*   **插入视图自蒸馏（Inserted-view Self-distillation）**：引入一个基于指数移动平均（EMA）更新的“生成老师”模型。该模型为插入视图提供动态更新的软标签（Soft Labels），使学生模型能够以平滑的方式吸收新视角的几何和外观信息，从而缓解几何不对齐带来的冲击。
*   **几何扰动增强（Geometry-Perturbation Augmentation）**：在微调过程中对输入视图进行全局和局部块状的仿射变换，模拟视角漂移和局部几何不一致，从而增强模型处理错位数据的鲁棒性。
*   **随机恢复（Stochastic Restoration）**：定期将部分参数重置为初始权重，以抑制长时微调过程中可能产生的误差累积。

### 3. 实验设计
*   **数据集**：
    *   **ETH Dataset**：真实户外场景，具有宽基线多视图观测。
    *   **Online Collection**：涵盖多样的室内外场景。
*   **对比方法（Baselines）**：
    *   前馈重建模型：WorldMirror, OmniVGGT。
    *   动态/序列重建模型：TTT3R。
    *   优化类方法：3D Gaussian Splatting (3DGS)。
*   **评估指标**：
    *   **图像保真度**：PSNR, SSIM, LPIPS（针对原始捕获区域）。
    *   **扩展质量（VLM 评估）**：利用 GPT-5.2 作为评估器，评分指标为插入满意度（GPT-IS）和渲染质量（GPT-RQ）。
    *   **人类评估**：39 名参与者对插入满意度（H-IS）和渲染质量（H-RQ）进行排序评分。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到使用 AdamW 优化器，学习率为 $1 \times 10^{-5}$，并使用了微分渲染器进行 3D 高斯泼溅（3DGS）表示。由于该方法属于测试时自适应（TTA），通常针对每个特定场景进行短时间的在线微调。

### 5. 实验数量与充分性
*   **实验规模**：在 ETH 和在线数据集上进行了广泛测试，涵盖了森林、室内、古迹、花园等多种复杂场景。
*   **消融实验**：设计了 5 组消融实验，分别验证了锚点蒸馏、子集采样与数据增强、自蒸馏概率 $p$ 以及随机恢复机制的有效性。
*   **充分性评价**：实验设计较为充分且客观。通过引入 GPT-5.2 这种客观的第三方视觉大模型评估，结合传统图像指标和主观人类实验，多维度地证明了该方法在处理“几何不对齐”这一核心痛点上的优越性。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：相比于直接优化（如 3DGS）或纯前馈模型，SceneExpander 在存在几何错位的情况下，能有效消除重影伪影，保持原始场景的完整性。
*   **平衡质量与扩展**：锚点蒸馏是保持原始区域质量的关键，而 EMA 自蒸馏机制则成功地将不一致的生成内容整合进了统一的 3D 空间。
*   **TTA 的价值**：证明了对于 AIGC 引入的不确定性，测试时自适应比重新训练或强制一致性生成更具灵活性和实用性。

### 7. 优点
*   **工作流创新**：提出了一种以用户为中心的“文本->视图插入->3D 扩展”的迭代式世界构建流。
*   **解决核心痛点**：针对生成视图与真实重建之间的几何冲突，给出了系统性的蒸馏解决方案。
*   **高质量渲染**：在扩展区域提供了连贯的几何和外观，支持平滑的相机导航。

### 8. 不足与局限
*   **严重错位的限制**：当插入视图与原场景存在极度严重的结构性冲突（如物体位置完全矛盾）时，模型仍可能产生局部错误（如文中提到的神社横梁案例）。
*   **依赖预训练模型**：其性能上限受限于底层前馈重建模型（WorldMirror）的泛化能力。
*   **合成痕迹**：扩展区域的内容本质上是合成的，可能存在幻觉对象，不适用于对真实性要求极高的取证类场景。

（完）
