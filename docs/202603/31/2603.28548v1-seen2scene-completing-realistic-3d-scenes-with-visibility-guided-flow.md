---
title: "Seen2Scene: Completing Realistic 3D Scenes with Visibility-Guided Flow"
title_zh: Seen2Scene：利用可见性引导流补全逼真的 3D 场景
authors: "Quan Meng, Yujin Chen, Lei Li, Matthias Nießner, Angela Dai"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28548v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 基于流匹配的场景补全与生成方法，支持文本或部分输入
tldr: Seen2Scene是首个基于流匹配（Flow Matching）的3D场景补全与生成方法，直接利用不完整的真实世界扫描数据进行训练。通过引入可见性引导的流匹配技术，该方法能有效屏蔽真实扫描中的未知区域，并结合稀疏Transformer和TSDF表示法，实现了对复杂现实环境的高质量补全。相比依赖合成数据的传统方法，它在真实感和准确性上表现更优。
selection_source: fresh_fetch
motivation: 传统3D场景补全方法高度依赖完整的合成数据，难以有效处理真实世界扫描中因遮挡产生的缺失区域。
method: 提出可见性引导的流匹配技术，利用稀疏Transformer处理TSDF体积，并结合3D布局框、文本或局部扫描作为条件信号。
result: 实验证明该模型在补全准确性和生成质量上均优于现有基准，能够生成连贯且高度真实的复杂3D场景。
conclusion: Seen2Scene证明了直接从不完整的真实扫描中学习是可行的，为现实世界3D场景的理解与生成提供了高效的新方案。
---

## 摘要
我们提出了 Seen2Scene，这是首个基于流匹配（flow matching）的方法，直接在不完整的真实世界 3D 扫描数据上进行训练，用于场景补全和生成。与以往依赖完整（因此通常是合成的）3D 数据的方法不同，我们的方法引入了可见性引导流匹配，它显式地掩盖了真实扫描中的未知区域，从而能够从真实世界的局部观测中进行有效学习。我们使用编码在稀疏网格中的截断符号距离场（TSDF）体素来表示 3D 场景，并采用稀疏 Transformer 来高效建模复杂的场景结构，同时掩盖未知区域。我们采用 3D 布局框作为输入条件信号，且我们的方法可以灵活地适应各种其他输入，如文本或局部扫描。通过直接从真实世界的不完整 3D 扫描中学习，Seen2Scene 能够为复杂、凌乱的真实环境实现逼真的 3D 场景补全。实验表明，我们的模型能够生成连贯、完整且逼真的 3D 场景，在补全准确度和生成质量方面均优于基准方法。

## Abstract
We present Seen2Scene, the first flow matching-based approach that trains directly on incomplete, real-world 3D scans for scene completion and generation. Unlike prior methods that rely on complete and hence synthetic 3D data, our approach introduces visibility-guided flow matching, which explicitly masks out unknown regions in real scans, enabling effective learning from real-world, partial observations. We represent 3D scenes using truncated signed distance field (TSDF) volumes encoded in sparse grids and employ a sparse transformer to efficiently model complex scene structures while masking unknown regions. We employ 3D layout boxes as an input conditioning signal, and our approach is flexibly adapted to various other inputs such as text or partial scans. By learning directly from real-world, incomplete 3D scans, Seen2Scene enables realistic 3D scene completion for complex, cluttered real environments. Experiments demonstrate that our model produces coherent, complete, and realistic 3D scenes, outperforming baselines in completion accuracy and generation quality.

---

## 论文详细总结（自动生成）

以下是对论文《Seen2Scene: Completing Realistic 3D Scenes with Visibility-Guided Flow》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 场景生成和补全模型高度依赖于**完整的合成数据集**（如 3D-FRONT），导致模型在处理**真实世界扫描数据**时表现不佳。真实扫描数据（如 ScanNet++）由于传感器遮挡和覆盖范围限制，天然具有不完整性和噪声。
*   **研究动机**：为了消除合成数据与真实数据之间的领域差距（Domain Gap），研究者希望开发一种能够直接在**不完整的真实 3D 扫描**上进行训练的生成模型，使其能够“想象”出缺失的结构，同时保持对观测到几何形状的忠实度。

### 2. 方法论：核心思想与关键技术
Seen2Scene 采用了一种基于**流匹配（Flow Matching）**的架构，核心在于利用可见性信息来引导学习过程。
*   **数据表示**：使用**截断符号距离场（TSDF）**体素网格。TSDF 的优势在于它能自然编码“已知表面”、“已知空域”和“未知区域”（相机未观测到的区域）。
*   **掩码稀疏变分自编码器（Masked Sparse VAE）**：
    *   首先将高分辨率 TSDF 块压缩为紧凑的几何潜变量（Latents）。
    *   **关键点**：在训练 VAE 时，显式地掩盖掉未知区域，仅对观测到的表面和空域进行重建损失计算，从而学习到干净的几何特征表示。
*   **可见性引导流匹配（Visibility-Guided Flow Matching）**：
    *   使用**稀疏 Transformer（Sparse DiT）**作为骨干网络，学习从高斯噪声到几何潜变量的概率路径。
    *   **核心创新**：在流匹配的损失函数中，通过可见性掩码剔除未知区域的 Token。这意味着模型只在有监督的观测区域学习速度场，而对未知区域保持不可知，从而避免学习到扫描缺失带来的错误分布。
*   **条件控制与补全**：
    *   **布局控制**：输入 3D 边界框和语义标签（通过 CLIP 编码）。
    *   **扫描补全**：引入 **ControlNet** 分支，将局部扫描作为条件注入预训练的生成模型中，引导模型填充缺失部分。
    *   **大场景生成**：采用 **MultiDiffusion** 策略，通过重叠块（Chunks）的平滑融合实现大规模场景的连贯生成。

### 3. 实验设计
*   **数据集**：
    *   **ScanNet++** & **ARKitScenes**：大规模真实世界室内场景扫描。
    *   **3D-FRONT**：合成室内场景（用于对比和基础训练）。
*   **Benchmark 与对比方法**：
    *   **场景补全任务**：对比了 SG-NN（稀疏生成网络）和 NKSR（神经核表面重建）。
    *   **场景生成任务**：对比了 BlockFusion、LT3SD 和 WorldGrow。
*   **评估指标**：
    *   几何指标：倒角距离（CD）、L2 距离。
    *   生成质量：DINOv2-FID、Uni3D 弗雷歇点云距离（U3D-FPD）。
    *   **感知评估**：使用 **Qwen3-VL-8B** 作为自动评估器，从几何质量、结构逻辑、语义连贯性和内容密度四个维度打分。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA H100 GPU**。
*   **精度**：采用 BF16 混合精度训练。
*   **超参数**：Batch size 为 64，学习率为 $10^{-4}$，使用 AdamW 优化器。
*   **时长**：文中未明确给出总训练小时数，但提到了使用了 1000 步预热（Warmup）和余弦退火策略。

### 5. 实验数量与充分性
*   **实验规模**：在三个大型数据集上进行了测试，评估样本量达到 4000 个。
*   **消融实验**：非常充分，涵盖了：
    1.  **掩码训练的影响**：证明了如果不掩盖未知区域，模型会错误地将缺失部分学成“空域”。
    2.  **真实数据训练的必要性**：对比了仅用合成数据训练的效果，证明了真实数据对处理复杂布局（如乱放的衣服）至关重要。
    3.  **语义编码方式**：对比了 CLIP 嵌入与独热编码（One-hot），证明了 CLIP 在处理长尾类别和同义词时的优越性。
*   **客观性**：通过引入第三方 VLM（Qwen3-VL）进行盲测打分，增加了评估的客观性，减少了人工评估的主观偏差。

### 6. 主要结论与发现
*   **直接学习可行性**：证明了直接从不完整、有噪声的真实扫描中学习 3D 几何分布是可行的，且效果优于仅依赖合成数据的方法。
*   **可见性掩码的关键作用**：在 VAE 和流匹配阶段引入可见性掩码，是处理真实世界数据不完整性的核心技术。
*   **泛化能力**：模型不仅能做补全，还能通过 LLM 将文本转化为布局，进而生成高质量的 3D 场景，展现了极强的多模态灵活性。

### 7. 优点（亮点）
*   **首创性**：第一个将流匹配应用于真实世界不完整扫描的 3D 场景补全工作。
*   **可见性感知**：巧妙地利用了 TSDF 的可见性属性来解决数据缺失问题，而非简单地进行数据增强。
*   **系统性强**：从底层表示（TSDF）、压缩（VAE）、生成（Flow Matching）到应用（ControlNet/LLM）构建了一套完整的流水线。
*   **评估创新**：利用最新的多模态大模型（VLM）进行 3D 几何质量评估，符合当前技术趋势。

### 8. 不足与局限
*   **分辨率限制**：受限于 TSDF 体素网格和 VAE 的压缩倍率，极细微的几何细节（如电线、小摆件）可能会丢失。
*   **缺乏外观信息**：目前仅关注几何形状（Geometry），不包含纹理、材质或光照信息，无法直接用于照片级渲染。
*   **计算开销**：虽然使用了稀疏 Transformer，但处理大规模 3D 场景的推理时间仍可能高于某些轻量级回归模型。
*   **布局依赖**：生成质量在很大程度上依赖于输入布局框的准确性，如果 LLM 生成的布局不合理，几何生成也会受限。

（完）
