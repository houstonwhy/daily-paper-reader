---
title: "I3DM: Implicit 3D-aware Memory Retrieval and Injection for Consistent Video Scene Generation"
title_zh: I3DM：用于一致性视频场景生成的隐式 3D 感知记忆检索与注入
authors: "Jia Li, Han Yan, Yihang Chen, Siqi Li, Xibin Song, Yifu Wang, Jianfei Cai, Tien-Tsin Wong, Pan Ji"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23413v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于一致性视频场景生成的隐式3D感知记忆
tldr: 针对视频生成中长期场景一致性（尤其是重访旧区域）难以维持的问题，本文提出I3DM。该方法避开了显式3D重建的误差累积，通过预训练的新视角合成模型提取隐式3D特征进行记忆检索，并利用3D对齐注入模块将历史内容隐式扭曲至目标视角。实验证明，I3DM在重访一致性、生成质量和相机控制精度上均优于现有技术，为高质量视频场景生成提供了新思路。
selection_source: fresh_fetch
motivation: 现有的视频生成方法在处理场景重访时，常因显式3D重建的误差累积或简单视角检索在复杂遮挡下的失效而导致场景不一致。
method: 提出一种隐式3D感知记忆机制，利用新视角合成模型的中间特征进行鲁棒检索，并通过3D对齐注入模块将历史帧内容隐式扭曲并自适应地融合到当前生成中。
result: 实验结果显示，该方法在重访一致性、生成保真度和相机控制精度方面均显著超越了现有的最先进方法。
conclusion: I3DM通过隐式3D记忆检索与注入，有效解决了长视频生成中的场景重访一致性难题，实现了高保真且受控的视频场景生成。
---

## 摘要
尽管视频生成取得了显著进展，但在重新访问先前探索过的区域时保持长期场景一致性仍然具有挑战性。现有解决方案要么依赖于显式构建 3D 几何，这会受到误差累积和尺度歧义的影响；要么依赖于朴素的摄像机视场（FoV）检索，这在复杂遮挡下通常会失败。为了克服这些局限性，我们提出了 I3DM，一种用于一致性视频场景生成的创新隐式 3D 感知记忆机制，该机制绕过了显式 3D 重建。我们方法的核心是 3D 感知记忆检索策略，它利用预训练的前馈新视角合成（FF-NVS）模型的中间特征来评估视角相关性，从而在高度遮挡的场景下也能实现鲁棒检索。此外，为了充分利用检索到的历史帧，我们引入了一个 3D 对齐记忆注入模块。该模块将历史内容隐式扭曲到目标视角，并根据可靠的扭曲区域自适应地调节生成过程，从而提高了重访一致性和精确的摄像机控制。大量实验表明，我们的方法优于现有最先进的方法，在重访一致性、生成保真度和摄像机控制精度方面均取得了卓越的表现。

## Abstract
Despite remarkable progress in video generation, maintaining long-term scene consistency upon revisiting previously explored areas remains challenging. Existing solutions rely either on explicitly constructing 3D geometry, which suffers from error accumulation and scale ambiguity, or on naive camera Field-of-View (FoV) retrieval, which typically fails under complex occlusions. To overcome these limitations, we propose I3DM, a novel implicit 3D-aware memory mechanism for consistent video scene generation that bypasses explicit 3D reconstruction. At the core of our approach is a 3D-aware memory retrieval strategy, which leverages the intermediate features of a pre-trained Feed-Forward Novel View Synthesis (FF-NVS) model to score view relevance, enabling robust retrieval even in highly occluded scenarios. Furthermore, to fully utilize the retrieved historical frames, we introduce a 3D-aligned memory injection module. This module implicitly warps historical content to the target view and adaptively conditions the generation on reliable warping regions, leading to improved revisit consistency and accurate camera control. Extensive experiments demonstrate that our method outperforms state-of-the-art approaches, achieving superior revisit consistency, generation fidelity, and camera control precision.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **I3DM** 的新型视频生成框架，旨在解决长视频场景生成中“重访一致性”这一核心难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在长程视频生成中，当摄像机轨迹重新回到之前访问过的区域时，模型往往会产生“转向即忘”（turn-and-forget）现象，即生成与之前不一致的内容，破坏了场景的真实感。
*   **研究背景**：
    *   **显式 3D 几何方法**：依赖点云或 3D 高斯重建，但存在尺度歧义和误差累积，导致摄像机导航不准。
    *   **隐式多视角方法**：基于视场（FoV）重叠检索历史帧，但忽略了几何遮挡，在复杂场景下会检索到无关帧，导致内容重复或不一致。
*   **研究动机**：利用预训练新视角合成（NVS）模型的隐式 3D 先验，在不进行显式重建的情况下实现鲁棒的记忆检索与对齐。

### 2. 方法论
I3DM 由两个核心模块组成：
*   **隐式 3D 感知记忆检索（3D-aware Memory Retrieval）**：
    *   **核心思想**：利用预训练前馈 NVS 模型（如 LVSM）的中间层特征，这些特征天然编码了多视角间的 3D 对应关系。
    *   **技术细节**：使用一个轻量级评分 CNN 处理 LVSM 的特征，预测目标视角的空间不确定性图（Uncertainty Map）。
    *   **算法流程**：采用“贪婪最大覆盖策略”（Greedy Maximum Coverage）。首先固定上一帧为锚点，然后迭代选择能为目标视角提供最大增量信息覆盖的历史帧，从而确保检索到的记忆既相关又具有空间互补性。
*   **自适应 3D 对齐记忆注入（Adaptive 3D-aligned Memory Injection）**：
    *   **核心思想**：将检索到的历史帧通过 NVS 模块隐式“扭曲”（Warp）到目标视角，为扩散模型提供几何对齐的引导。
    *   **技术细节**：为了解决 NVS 在外插或遮挡区域产生的伪影，研究者将 NVS 模块与视频扩散模型（基于 Wan-DiT）进行**联合微调**。这使得模型能自适应地识别可靠的对齐区域，并在不可靠区域依赖生成先验。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K (Re10K)**：主要训练和测试集，包含大量室内外真实场景。
    *   **Tanks-and-Temples (T&T)**：用于评估模型的跨域泛化能力。
*   **Benchmark 与协议**：采用“循环轨迹”（Cycle-trajectory）评估协议，即摄像机先前进再原路返回，通过比较重访帧的相似度来衡量一致性。
*   **对比方法**：
    *   显式几何类：Gen3C, WorldWarp。
    *   隐式/混合类：WorldPlay, Vmem。
*   **评价指标**：FID/FVD（质量）、$R_{err}$/$T_{err}$（相机控制精度）、PSNR/SSIM/LPIPS（重访一致性）。

### 4. 资源与算力
论文明确说明了算力消耗：
*   **记忆检索模块训练**：使用 4 张 NVIDIA RTX 4090 GPU，训练时长约 10 小时。
*   **视频生成模块训练**：基于 Wan-2.1-1.3B 模型，使用 4 张 NVIDIA H200 GPU，联合微调时长约 1.8 天。
*   **推理开销**：检索模块的开销仅占总生成时间的 3.3% 左右，具有较高的效率。

### 5. 实验数量与充分性
*   **实验规模**：在 Re10K 测试集上随机选择了 200 个场景进行长视频生成（每段 456 帧），并在 T&T 数据集上进行了泛化测试。
*   **消融实验**：非常详尽，涵盖了检索策略（时间、随机、FoV、几何、I3D-TopK）、注入机制（无记忆、无对齐、冻结 NVS vs. 微调 NVS）以及特征层选择的对比。
*   **评价**：实验设计客观、公平，通过定量指标和定性视觉对比充分验证了各模块的有效性。

### 6. 主要结论与发现
*   **超越 SOTA**：I3DM 在重访一致性（PSNR 提升显著）和相机控制精度上全面优于现有显式和隐式方法。
*   **隐式先验的威力**：证明了 FF-NVS 模型的中间特征比简单的 FoV 重叠或不稳定的显式重建更能有效地处理遮挡下的记忆检索。
*   **联合微调的必要性**：发现冻结的 NVS 模块产生的伪影会干扰相机控制，而联合微调能显著提升导航的准确性。

### 7. 优点（亮点）
*   **避开显式重建**：巧妙地解决了 3D 重建中的尺度歧义问题，使相机控制更加精准。
*   **遮挡鲁棒性**：通过学习到的不确定性图进行检索，能够有效应对复杂的遮挡场景。
*   **端到端优化**：将 NVS 对齐与扩散生成结合，实现了几何引导与生成能力的平衡。

### 8. 不足与局限
*   **数据集偏差**：目前主要在 Re10K 上训练，对于非真实感场景（如动画、游戏）的泛化能力有待进一步验证。
*   **长程漂移**：虽然解决了重访一致性，但在极长序列中仍可能存在颜色偏移或场景畸变的累积误差。
*   **内存增长**：随着视频变长，记忆库线性增长，可能在处理超长视频时遇到内存瓶颈。

（完）
