---
title: Training-Free Instance-Aware 3D Scene Reconstruction and Diffusion-Based View Synthesis from Sparse Images
title_zh: 无需训练的实例感知三维场景重建与基于扩散的稀疏图像视角合成
authors: "Jiatong Xia, Lingqiao Liu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21166v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 3D场景重建与基于扩散的视角合成
tldr: 本文提出一种无需训练的3D室内场景重建与渲染系统，仅需少量无位姿RGB图像即可实现高保真建模。该系统通过扭曲引导的异常移除构建鲁棒点云，利用2D到3D实例提升机制实现场景理解，并结合3D感知扩散模型优化视图合成。该方法不仅解决了稀疏输入下的几何缺失问题，还支持无需重训练的对象级场景编辑，为高效、可编辑的3D内容生成提供了新方案。
selection_source: fresh_fetch
motivation: 传统3D重建方法依赖密集视图和耗时的逐场景优化，且难以在稀疏输入下实现灵活的实例级编辑。
method: 该系统整合了基于扭曲的点云去噪、2D掩码到3D实例的传播机制，以及利用扩散模型细化投影渲染的混合管线。
result: 实验证明该方法在稀疏视角下能生成高质量、实例感知的重建结果，并支持一致性的对象移除等场景编辑操作。
conclusion: 该研究展示了无需场景特定优化即可实现高效、可编辑3D内容生成的可能性，为3D场景理解与合成开辟了新方向。
---

## 摘要
我们介绍了一种新颖的、无需训练的系统，用于从一组稀疏的、无位姿的 RGB 图像中重建、理解和渲染三维室内场景。与需要密集视图和逐场景优化的传统辐射场方法不同，我们的流水线在无需任何训练或位姿预处理的情况下实现了高保真结果。该系统集成了三项关键创新：(1) 一个鲁棒的点云重建模块，利用基于扭曲的异常移除策略过滤不可靠的几何结构；(2) 一种扭曲引导的 2D 到 3D 实例提升机制，将 2D 分割掩码传播为一致的、实例感知的 3D 表示；(3) 一种新颖的渲染方法，将点云投影到新视角，并使用 3D 感知扩散模型对渲染结果进行精细化。我们的方法利用扩散模型的生成能力来补偿缺失的几何结构并增强真实感，特别是在稀疏输入条件下。我们进一步证明，通过仅修改点云，我们的流水线可以自然地支持物体级场景编辑（如实例移除），从而在无需重新训练的情况下合成一致的、编辑后的视图。我们的结果为不依赖于特定场景优化的、高效且可编辑的 3D 内容生成确立了新方向。

## Abstract
We introduce a novel, training-free system for reconstructing, understanding, and rendering 3D indoor scenes from a sparse set of unposed RGB images. Unlike traditional radiance field approaches that require dense views and per-scene optimization, our pipeline achieves high-fidelity results without any training or pose preprocessing. The system integrates three key innovations: (1) A robust point cloud reconstruction module that filters unreliable geometry using a warping-based anomaly removal strategy; (2) A warping-guided 2D-to-3D instance lifting mechanism that propagates 2D segmentation masks into a consistent, instance-aware 3D representation; and (3) A novel rendering approach that projects the point cloud into new views and refines the renderings with a 3D-aware diffusion model. Our method leverages the generative power of diffusion to compensate for missing geometry and enhances realism, especially under sparse input conditions. We further demonstrate that object-level scene editing such as instance removal can be naturally supported in our pipeline by modifying only the point cloud, enabling the synthesis of consistent, edited views without retraining. Our results establish a new direction for efficient, editable 3D content generation without relying on scene-specific optimization. Project page: https://jiatongxia.github.io/TID3R/

---

## 论文详细总结（自动生成）

以下是对论文《Training-Free Instance-Aware 3D Scene Reconstruction and Diffusion-Based View Synthesis from Sparse Images》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：传统的三维重建方法（如 NeRF 和 3DGS）通常依赖密集的图像输入和耗时的逐场景优化（Per-scene optimization）。在 casual capture（随意拍摄）或稀疏视角（Sparse views）且无相机位姿（Unposed）的情况下，这些方法往往难以生成高质量结果，且缺乏对场景中单个物体的语义理解。
*   **核心问题**：如何从极少量的（10-20张）、未标注位姿的 RGB 图像中，快速实现具备实例感知能力、可编辑且高保真的 3D 场景重建与视角合成？
*   **整体含义**：论文提出了一种名为 **TID3R** 的统一管线，通过结合前馈几何预测、多视角几何一致性约束和生成式扩散模型，实现了无需训练（Training-free）的 3D 场景建模与渲染。

### 2. 方法论
该系统由三个核心模块组成：
*   **鲁棒几何重建**：
    *   利用前馈模型（如 MV-DUSt3R）从稀疏图像预测初始点云、深度图和相机参数。
    *   **关键技术：基于扭曲的异常点消除（Warping-based anomaly elimination）**。通过将点云投影到其他视角并计算深度一致性，剔除那些深度值显著小于参考视角的“悬浮”异常点，从而获得干净的几何结构。
*   **实例感知感知（2D-to-3D）**：
    *   使用基础模型（如 SAM）生成 2D 掩码。
    *   **关键技术：基于扭曲的实例统一（Warping-based instance unification）**。利用估计的几何信息将 2D 掩码投影到 3D 空间，并通过计算跨视角投影掩码的 mIoU（平均交并比）来合并同一物体的点组，实现一致的 3D 实例分割。
*   **扩散驱动的渲染管线**：
    *   将 3D 点云投影到目标视角生成稀疏的 RGB 图像和掩码。
    *   **关键技术：3D 感知扩散细化**。使用预训练的 **See3D** 模型，以原始图像为参考，对投影产生的空洞进行修复和纹理细化，生成光影真实的图像。
    *   **编辑支持**：支持对象移除。在移除点云后，通过对参考图像进行掩码处理（Reference masking），确保扩散模型生成的背景与修改后的几何结构保持一致。

### 3. 实验设计
*   **数据集**：
    *   **ScanNet**：用于评估实例分割（5个场景）和深度估计/渲染（7个场景）。
    *   **ScanNet++**：用于评估高分辨率下的重建与渲染质量。
*   **Benchmark 与对比方法**：
    *   **实例分割**：对比了 DEVA 和 PE3R。
    *   **深度估计**：对比了 DepthAnythingV2, PE3R, MV-DUSt3R+。
    *   **视角合成**：对比了 3DGS, 2DGS, GaussianGrouping, DNGaussian, CoR-GS, SE-GS（均为针对稀疏视角优化的变体）以及 LVSM（基于扩散的视角合成模型）。
*   **评价指标**：AP/AP50/AP75（分割）；RMSE/$\delta < 1.25$（深度）；PSNR/SSIM/LPIPS（渲染质量）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA 4090 GPU** 上进行。
*   **耗时**：
    *   该方法总耗时约 **3.5 分钟**（其中重建约 1 分钟，推理/渲染约 2.5 分钟）。
    *   相比之下，NeRF 需要约 149.5 分钟，3DGS 需要约 9 分钟（含位姿处理）。
    *   **注意**：该方法无需场景特定的训练时长，属于即插即用型。

### 5. 实验数量与充分性
*   **实验规模**：论文在 ScanNet 和 ScanNet++ 的多个代表性室内场景上进行了测试，每组实验选取 10-20 张稀疏图像作为输入。
*   **消融实验**：针对“实例统一策略”、“异常点消除”以及“渲染掩码策略”分别做了消融对比，验证了各组件对最终质量的贡献。
*   **充分性评价**：实验设计较为全面，不仅对比了传统的优化类方法，还对比了最新的前馈重建模型。通过定量指标和定性视觉对比（如对象移除效果），客观地展示了该方法在稀疏输入下的优势。

### 6. 主要结论与发现
*   **性能领先**：在稀疏视角下，TID3R 的渲染质量（PSNR 18.15）显著优于需要优化的 3DGS 变体（最高约 17.12）。
*   **几何鲁棒性**：基于扭曲的异常移除机制能有效解决前馈模型常见的几何噪声问题。
*   **编辑灵活性**：点云作为中间表示，结合扩散模型的修复能力，使得对象移除等编辑操作比在辐射场（NeRF/3DGS）中更简单且效果更自然。
*   **无需位姿**：系统能够自主估计位姿并完成对齐，降低了数据采集的门槛。

### 7. 优点
*   **高效性**：完全无需逐场景训练，数分钟内即可完成从图像到可编辑 3D 场景的转化。
*   **多功能集成**：在一个框架内统一了重建、分割、渲染和编辑任务。
*   **生成式增强**：巧妙利用扩散模型的先验知识解决了点云渲染固有的空洞和稀疏问题。

### 8. 不足与局限
*   **渲染速度**：虽然无需训练，但扩散模型的推理速度（Inference）尚未达到实时水平（10张图需2.5分钟），慢于优化后的 3DGS 渲染。
*   **位姿敏感性**：渲染特定视角时依赖于前馈网络预测的位姿精度，若初始位姿估计偏差过大，会影响视角对齐。
*   **场景范围**：目前主要针对室内场景（ScanNet 风格），在室外超大规模场景或极度缺乏纹理的区域，几何重建的可靠性可能下降。

（完）
