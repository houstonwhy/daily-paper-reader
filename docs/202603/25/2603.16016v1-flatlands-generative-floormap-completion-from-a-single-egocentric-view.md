---
title: "FlatLands: Generative Floormap Completion From a Single Egocentric View"
title_zh: FlatLands：基于单张第一视角图像的生成式平面图补全
authors: "Subhransu S. Bhattacharjee, Dylan Campbell, Rahul Shome"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.16016v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 单视角生成式平面图补全
tldr: 本研究针对室内导航中单视角图像视野受限的问题，提出了FlatLands数据集和基准，旨在从单张第一人称视角图像生成完整的鸟瞰图（BEV）地面图。该数据集包含来自1.7万个真实场景的27万余条观测数据，并提供了对齐的可见性与地面真值图。通过对比确定性、集成及生成式模型，研究展示了端到端单目RGB到地面图生成的有效性，为具身导航中的不确定性感知映射提供了重要工具。
selection_source: fresh_fetch
motivation: 单张第一人称视角图像仅能捕捉局部地面信息，难以满足室内导航对完整度量可通行性地图的需求。
method: 构建了包含27万余条观测的大规模数据集FlatLands，并提出一种端到端的单目RGB到地面图生成框架。
result: 建立了一个包含分布内与分布外评估协议的严谨基准，并对比了训练无关、确定性、集成及随机生成式模型的表现。
conclusion: FlatLands为具身导航中的不确定性感知室内映射和生成式补全任务提供了一个标准化的测试平台。
---

## 摘要
单张第一视角图像通常只能捕捉到地板的一小部分，然而，周围环境的完整度量可通行性地图能更好地服务于室内导航等应用。我们推出了 FlatLands，这是一个用于单视图鸟瞰图（BEV）平面图补全的数据集和基准。该数据集包含来自 6 个现有数据集的 17,656 个真实度量室内场景的 270,575 次观测，并配有对齐的观测图、可见性图、有效性图和地面真值 BEV 地图；该基准包括分布内和分布外评估协议。我们对比了无需训练的方法、确定性模型、集成模型以及随机生成模型。最后，我们将该任务实例化为一个端到端的单目 RGB 到平面图的流水线。FlatLands 为具身导航中的不确定性感知室内建图和生成式补全提供了一个严谨的测试平台。

## Abstract
A single egocentric image typically captures only a small portion of the floor, yet a complete metric traversability map of the surroundings would better serve applications such as indoor navigation. We introduce FlatLands, a dataset and benchmark for single-view bird's-eye view (BEV) floor completion. The dataset contains 270,575 observations from 17,656 real metric indoor scenes drawn from six existing datasets, with aligned observation, visibility, validity, and ground-truth BEV maps, and the benchmark includes both in- and out-of-distribution evaluation protocols. We compare training-free approaches, deterministic models, ensembles, and stochastic generative models. Finally, we instantiate the task as an end-to-end monocular RGB-to-floormaps pipeline. FlatLands provides a rigorous testbed for uncertainty-aware indoor mapping and generative completion for embodied navigation.

---

## 论文详细总结（自动生成）

以下是对论文《FlatLands: Generative Floormap Completion From a Single Egocentric View》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：具身智能设备（如机器人）通过单张第一视角（Egocentric）RGB图像只能看到视野内极小部分的地面，而导航任务需要完整的鸟瞰图（BEV）度量可通行性地图。
*   **研究背景**：从单视图推断未观察区域的地图是一个具有高度歧义的**贝叶斯逆问题**（一个观测对应多个合理补全）。现有的室内建图方法往往依赖多帧融合或确定性预测，缺乏对空间不确定性的建模。
*   **整体含义**：本文通过引入大规模真实数据集 **FlatLands** 和生成式补全框架，旨在探索如何利用生成模型（如扩散模型、流匹配）从有限观测中采样出符合物理规律且多样化的室内平面图。

### 2. 方法论：核心思想与技术细节
论文将任务解构为两个阶段：
*   **感知阶段（Perception Stage）**：
    *   使用单目深度估计（Depth Pro）和语义分割（SegFormer）从RGB图像提取深度图和地面掩码。
    *   利用相机内参进行正交投影，生成 BEV 观测图 $F_{obs}$ 和未观测区域掩码 $U$。
*   **生成补全阶段（Generative Stage）**：
    *   **核心思想**：训练一个条件生成模型 $q_\theta$，在给定 $(F_{obs}, U)$ 的条件下采样出 $K$ 个可能的补全结果。
    *   **关键技术**：
        *   **证据钳位（Evidence Clamping）**：在生成过程中，强制保留已观测区域的标签，仅对未观测区域进行采样，确保后验一致性。
        *   **FM+XAttn 模型**：在流匹配（Flow Matching）基础上引入交叉注意力机制（Cross-Attention），使生成器能更好地捕捉全局布局线索。
        *   **分类器自由引导（CFG）**：用于平衡生成样本的保真度与多样性。

### 3. 实验设计
*   **数据集（FlatLands）**：整合了 6 个真实室内数据集（Matterport3D, ScanNet, ScanNet++, ARKitScenes, 3RScan, ZInD），包含 17,656 个场景和 270,575 条对齐的观测数据。
*   **Benchmark 协议**：
    *   **分布内（ID）**：使用前 5 个数据集的测试集。
    *   **分布外（OOD）**：将 ScanNet++ 设为严格的测试专用集，验证跨建筑风格的泛化能力。
*   **对比方法（11 种）**：
    *   **基准线**：全障碍物、全地面、最近邻传播、均匀随机。
    *   **确定性模型**：U-Net, PConv-UNet, LaMa。
    *   **集成模型**：LaMa-Ensemble（多种子集成）。
    *   **随机生成模型**：扩散模型（Diffusion）、流匹配（Flow Matching）、FM+XAttn。
*   **评估指标**：除了 IoU 和 F1，引入了 **UMR**（未观测区域误报率）和 **MES**（掩码能量分数，用于同时衡量保真度与多样性）。

### 4. 资源与算力
*   **训练算力**：使用了 **4 张 NVIDIA A100 (40GB) GPU** 进行分布式训练。
*   **训练时长**：总计 300,000 个训练步数（约 280 个 A100 GPU 小时）。
*   **推理算力**：推理可在消费级显卡（NVIDIA RTX 4090）上运行。
*   **效率**：确定性模型 > 12 FPS；生成式模型由于需要多次迭代，速度慢 20-80 倍。

### 5. 实验数量与充分性
*   **实验规模**：在超过 2.8 万条测试观测数据上进行了评估，规模庞大。
*   **消融实验**：涵盖了采样数量 $K$ 的敏感度、CFG 引导强度的影响、交叉注意力机制的有效性。
*   **充分性与公平性**：
    *   所有模型在相同的输入接口和评估掩码下对比，确保了公平性。
    *   通过“边界方差分解”深入分析了不确定性的来源（是模型随机性还是布局歧义性）。
    *   提供了端到端（RGB $\to$ Map）的完整流水线实验，证明了方法在真实传感器噪声下的鲁棒性。

### 6. 主要结论与发现
*   **生成式模型占优**：随机生成模型（尤其是 FM+XAttn）在 Oracle Best-of-K 指标上全面超越确定性模型，能捕捉到更合理的空间布局。
*   **不确定性定位**：条件流模型（FM+XAttn）能将不确定性准确地定位在结构歧义的边界区域，而集成模型（Ensemble）的方差往往弥散在整个空间，无法有效指导导航决策。
*   **泛化能力**：模型在 OOD 数据集上保持了排名一致性，说明学习到了通用的室内几何先验。

### 7. 优点与亮点
*   **数据集贡献**：填补了室内 BEV 度量平面图补全领域缺乏大规模真实基准的空白。
*   **指标创新**：提出的 MES 指标为评估带掩码的生成式补全任务提供了科学的度量手段。
*   **实用性强**：不仅关注算法，还构建了从原始 RGB 到地图的端到端流水线，直接对接机器人导航需求。

### 8. 不足与局限
*   **语义缺失**：目前仅处理二值可通行性（地面 vs 障碍物），未包含房间类型、物体类别等语义信息。
*   **推理延迟**：生成式模型的多步迭代导致推理速度较慢，难以直接满足高频实时控制的需求。
*   **极端情况失效**：在观测极度稀疏（如大面积遮挡）时，所有模型都会出现“边界泄漏”现象，将地面过度延伸到障碍物区域。
*   **简化假设**：假设了简化的针孔相机模型和水平地面，未考虑楼梯、坡道等复杂地形。

（完）
