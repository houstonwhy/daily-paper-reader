---
title: "AeroScene: Progressive Scene Synthesis for Aerial Robotics"
title_zh: AeroScene：面向空中机器人的渐进式场景合成
authors: "Nghia Vu, Tuong Do, Dzung Tran, Binh X. Nguyen, Hoan Nguyen, Erman Tjiputra, Quang D. Tran, Hai-Nguyen Nguyen, Anh Nguyen"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23224v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 用于渐进式3D场景合成的分层扩散模型
tldr: AeroScene 针对无人机仿真环境构建耗时且难以扩展的问题，提出了一种分层扩散模型。该模型通过层级感知标记化和多分支特征提取，实现了从全局布局到局部细节的渐进式 3D 场景合成。研究不仅在多个基准测试中超越了现有方法，还生成了超过 1000 个可直接用于 NVIDIA Isaac Sim 的高质量物理仿真场景，显著提升了无人机导航等下游任务的训练效率。
selection_source: fresh_fetch
motivation: 现有的无人机仿真环境高度依赖人工创建，导致场景生成效率低且难以大规模扩展。
method: 提出一种分层扩散模型，利用层级感知标记化和多分支特征提取来兼顾全局布局与局部语义一致性。
result: 在多个基准测试中表现优于前沿方法，并生成了 1000 多个可直接用于物理仿真的高保真 3D 场景。
conclusion: AeroScene 为机器人领域提供了一种高效、可扩展的 3D 场景生成方案，有效支持了无人机导航等复杂任务的仿真需求。
---

## 摘要
生成模型在多个领域展现了巨大影响，但其在机器人场景合成方面的潜力仍有待挖掘。这种差距在无人机模拟器中尤为明显，目前的模拟环境仍高度依赖人工构建，不仅耗时且难以扩展。在这项工作中，我们提出了 AeroScene，一种用于渐进式 3D 场景合成的分层扩散模型。我们的方法利用层次感知分词（hierarchy-aware tokenization）和多分支特征提取，对全局布局和局部细节进行推理，确保了物理合理性和语义一致性。这使得 AeroScene 特别适用于为导航、降落和栖息等空中机器人任务生成逼真的场景。我们通过在最新收集的数据集和公开基准测试上的广泛实验证明了其有效性，结果表明 AeroScene 显著优于现有方法。此外，我们利用 AeroScene 生成了一个包含 1,000 多个具备物理属性、高保真 3D 场景的大规模数据集，这些场景可直接集成到 NVIDIA Isaac Sim 中。最后，我们展示了这些生成的环境在下游无人机导航任务中的实用性。我们的代码和数据集已在 aioz-ai.github.io/AeroScene/ 公开。

## Abstract
Generative models have shown substantial impact across multiple domains, their potential for scene synthesis remains underexplored in robotics. This gap is more evident in drone simulators, where simulation environments still rely heavily on manual efforts, which are time-consuming to create and difficult to scale. In this work, we introduce AeroScene, a hierarchical diffusion model for progressive 3D scene synthesis. Our approach leverages hierarchy-aware tokenization and multi-branch feature extraction to reason across both global layouts and local details, ensuring physical plausibility and semantic consistency. This makes AeroScene particularly suited for generating realistic scenes for aerial robotics tasks such as navigation, landing, and perching. We demonstrate its effectiveness through extensive experiments on our newly collected dataset and a public benchmark, showing that AeroScene significantly outperforms prior methods. Furthermore, we use AeroScene to generate a large-scale dataset of over 1,000 physics-ready, high fidelity 3D scenes that can be directly integrated into NVIDIA Isaac Sim. Finally, we illustrate the utility of these generated environments on downstream drone navigation tasks. Our code and dataset are publicly available at aioz-ai.github.io/AeroScene/

---

## 论文详细总结（自动生成）

以下是对论文《AeroScene: Progressive Scene Synthesis for Aerial Robotics》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的无人机（空中机器人）仿真环境高度依赖人工建模，导致场景创建耗时、难以大规模扩展，且往往缺乏物理真实感和复杂的层级结构（如全局航道与局部着陆点的统一）。
*   **研究动机**：虽然生成式模型在图像和3D物体生成上取得进展，但在机器人场景合成（尤其是需要兼顾宏观布局和微观交互细节的空中机器人场景）中应用不足。作者旨在提出一种能够自动生成大规模、高保真、符合物理规律且可直接用于仿真的3D场景的方法。

### 2. 方法论
AeroScene 采用了一种**分层扩散模型（Hierarchical Diffusion Model）**，核心流程如下：
*   **层级感知标记化（Hierarchy-aware Tokenization）**：将场景中的物体根据“可标记化分数”划分为**粗粒度（Coarse）**（如建筑、地形）和**细粒度（Fine）**（如家具、障碍物）两个分支。
*   **多分支特征提取**：
    *   **粗粒度分支**：使用轻量级 3D CNN 提取宏观结构特征。
    *   **细粒度分支**：利用图神经网络（GNN）处理物体间的空间邻接关系。
*   **跨尺度渐进注意力机制（Cross-scale Progressive Attention）**：通过堆叠的注意力块实现“自上而下”（全局引导局部）和“自下而上”（局部反馈全局）的信息融合。
*   **引导目标函数（Guidance Objectives）**：在推理阶段引入三种物理/语义引导：
    1.  **碰撞规避引导**：利用 3D IoU 惩罚物体重叠。
    2.  **粗到细引导**：确保细粒度物体放置在合理的粗粒度区域内。
    3.  **语义约束引导**：基于学习到的类别先验确保物体组合的逻辑性。
*   **仿真集成**：生成的布局直接嵌入 NVIDIA Isaac Sim，转化为具备物理属性的仿真环境。

### 3. 实验设计
*   **数据集**：
    *   **3D-FRONT**：经典的室内场景基准。
    *   **AeroScene Dataset（新贡献）**：包含 1016 个场景（室内外结合），拥有 16 万个物体，涵盖 23 类粗粒度物体和 47 类细粒度物体，并标注了无人机降落区。
*   **对比方法（Baselines）**：ATISS、Diffusion-SDF、DiffuScene、PhyScene。
*   **评估指标**：FID/KID（感知相似度）、碰撞率（CR）、粗细一致性（CFC）、语义合理性（SP）。
*   **下游任务验证**：在生成的场景中进行无人机导航与交互任务（从起点导航至特定建筑楼顶降落）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU**。
*   **训练配置**：采用 Adam 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 64，训练时长为 **800 个 Epoch**。
*   **推理效率**：在 A100 上生成一个场景大约需要 **2 分钟**（包含 100 步反向扩散），与同类扩散模型基准相当。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型数据集上进行了定量对比，每个方法平均生成 1000 个场景进行评估。
    *   进行了详细的**消融实验**，分别验证了碰撞引导、粗到细引导和语义引导的必要性。
    *   完成了 300 条无人机飞行轨迹的测试，成功率达 91%。
*   **充分性评价**：实验设计较为全面，涵盖了从生成质量到下游机器人任务实用性的全链条验证。对比实验选择了近年最先进的扩散模型和物理感知模型，评估指标客观且具有针对性。

### 6. 主要结论与发现
*   **性能领先**：AeroScene 在所有指标上均优于现有方法，特别是在降低碰撞率和提高层级一致性方面表现突出。
*   **层级推理的重要性**：通过显式建模粗细尺度，模型能更好地处理室内外过渡和复杂的物体堆叠关系。
*   **仿真价值**：生成的场景不仅视觉逼真，且由于经过了物理引导，能够直接支持无人机进行复杂的感知、规划和控制算法测试。

### 7. 优点与亮点
*   **层级化思维**：不同于以往将场景视为平铺物体集的做法，AeroScene 模仿了人类设计场景的“先大后小”逻辑。
*   **物理就绪（Physics-ready）**：通过引导函数直接解决生成场景中常见的物体穿模问题，使其具备极高的工程实用价值。
*   **数据集贡献**：提供了一个专门为空中机器人设计的、包含层级标注和降落区标注的大规模数据集。

### 8. 不足与局限
*   **静态限制**：目前仅能生成静态场景布局，未考虑动态障碍物（如行人、车辆）或环境变化（如风力、光照动态）。
*   **Sim-to-Real 差距**：实验主要在仿真环境中完成，尚未在真实的物理无人机上进行大规模的迁移验证。
*   **推理耗时**：虽然 2 分钟生成一个场景在离线构建中可以接受，但对于需要实时生成环境的在线应用来说仍然较慢。

（完）
